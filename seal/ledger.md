# spec-to-code map

> Maps spec clauses to the code coordinates that ground them, so the next
> session opens a coordinate instead of re-searching — locating the code is the
> expensive half, and this file exists so it is paid once. Written and checked
> by machines (`evidence-check`); committed so it follows worktrees and other
> machines.
>
> This is SpecSeal's ledger for SpecSeal, and it is the gathered half of one.
> **A work item writes `seal/ledger/<work-item-id>.md` instead of appending
> here** — two branches never queue at one file, because no two work items
> share an id. The checker reads both addresses. At each release,
> `.github/scripts/fold_ledger.py` moves every fragment into this file under a
> `## X.Y.Z — <date>` heading, one `###` section per work item marked with
> `<!-- specs/<work-item-id> -->` (the marker's text names the work item by its
> sub-directory and did not change when the root did), and removes the
> fragment. So this file grows once per release, by the rows that release's
> work items wrote, and the areas above those sections are the rows from
> before the fragments existed.

## Coordinates

| Item | Value |
|---|---|
| Coordinate notation | `path#anchor@hash` from the repo root |
| Trust exceptions | none |

A coordinate names **content, never a position**. The anchor is a symbol name
where the language has one — `.py` is read with Python's own parser — and a
distinctive line of text otherwise; the hash covers the region under it. A row
carries no line number and no commit SHA, so nothing in it goes stale for a
reason unrelated to the claim, and the check calls git for nothing — the one
exception is `--migrate`, which consults the old stamp's commit before it
trusts a line number it is rewriting.

The **Checked** column carries the date somebody read the code. Re-verifying a
row is re-reading it and running `evidence-check --reverify`, which recomputes
the hash and names what it changed.

**A row whose anchor a change removes is removed with it.** Its claim went
with the code; the new claim is a new row, in the work item's own fragment.
So a claim leaves this file when the code it was about does, and the fold at
the next release brings the new claim back in with the rest of its fragment.

## Scope decisions

Judgments that don't follow from code or documents alone.

| Decision | Content | Grounds |
|---|---|---|
| The ledger opens at one commit | Every row below is stamped at this tree's first commit rather than left to the header alone | A row with no stamp of its own measures from the header, and a header nobody re-reads re-dates every claim at once. Stamping them all at the baseline costs nothing today and makes the first genuine re-verification visible |

## Routing declarations

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The Review and Destination axes are strict; a value outside the vocabulary makes the file not a declaration | `hooks/routing.py#REVIEW_ANSWERS@4ed4dacd`, `hooks/routing.py#DESTINATION_ANSWERS@3ab60a49` | Read, not run. `parse` returns `None` unless both answers are members | 2026-08-31 | The Implementation axis is deliberately lenient at the same coordinate |

## Rider stamps

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Every `# RIDER:` carries the date and SHA it was verified at, and the SHA is an ancestor of HEAD | `tests/test_a_rider_reaches_its_file.py#"STAMP = re.compile(r"Verified \d{4}-\d{2}-\d{2} at ([0-9a-f]{7,40})\b")"@05033bb1` | Executed: the suite passes on this tree, and the eleven riders are stamped at the baseline commit | 2026-08-31 | The stamps were re-cut for this tree; the commits they named before do not exist here |

## Edits that reach the commit gate

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The gate reads a heredoc body as commands, so a heredoc that only patches a file is judged by what its body says | `hooks/commit-review-gate.py#_hides_a_commit@2eab9355` | **Executed**, twice independently: a body carrying a commit on its own line returns True under both `cat > f <<'EOF'` and `python3 - <<'PY'`, and False for a body carrying none | 2026-08-31 | Deliberate, per legacy #75: a commit hidden in a body used to walk straight past. The agent files now route file edits through the `Edit` tool, so no command line reaches the reader at all. Whether the reader should skip a body being written to a file is Q1 of `specs/1788184145-the-gate-stops-the-session-editing-its-tests/questions.md` |
| Only a segment whose command word is `git` with the `commit` subcommand is counted, so a commit sitting inside a string literal is not | `hooks/commit-review-gate.py#_hides_a_commit@2eab9355`, assembled in `commit_invocations` at `hooks/commit-review-gate.py#commit_invocations@bd3f5b98` | **Executed**, round 1's probe. Whole fixture files handed to `_hides_a_commit` are clean — `tests/test_gate_judges_the_repo_it_commits_to.py` and `tests/test_what_the_reader_understands.py` both — while `agents/smith.md` and `README.md` TRIP, each only on its own `[no-review]` waiver example | 2026-08-31 | This refutes both readings that preceded it. It is NOT "the fixtures are shell command strings, so the reader finds a commit in them", and it is NOT "the documents are what trip it": issue #34's eight-line PARTIAL patch of a fixture trips as well. A fragment is judged on its own quoting, not on the file it came from |
| A segment the reader cannot expand counts as a commit, so a body with no `git` in it at all can stop the session | `hooks/commit-review-gate.py#_hides_a_commit@2eab9355`, resolved by `_eval_hides_a_commit` at `hooks/commit-review-gate.py#_eval_hides_a_commit@11406469`, against `EXPANDS` at `` hooks/cmdline.py#"EXPANDS = "$`*?[]{}""@6d56a043 `` | **Executed**, four bodies: `eval "$CMD"` TRIPS · `eval $(cat f)` TRIPS · `eval "echo hello"` clean (no expansion character) · `print('hello')` clean | 2026-08-31 | Fail-closed by design — the docstring says nothing can tell *reduces to a commit* from *reduces to something else* without running the shell. This is the branch a reader misses: searching a patch for a commit and finding none does not clear it. `tests/test_what_the_reader_understands.py#"("an eval", "eval 'cd %s'; git commit -m x", "eval 'cd %s'"),"@770c7def` is an `eval` fixture, so a partial patch of that file is exactly the case |
| Prose added to a document can flip the gate's verdict on that whole document without containing a commit at all | `hooks/commit-review-gate.py#_hides_a_commit@2eab9355` | **Executed** while fixing round 1. `agents/warden.md` is clean as a whole; adding one paragraph with a single apostrophe (`the fragment's own quoting`) made it TRIP, because the odd quote flipped the reader's state and put the `[no-review]` row in its probe table into command position. Removing the apostrophe restored `clean`. Round 2 confirmed the parity at the same coordinate: 0 → clean · 1 → TRIPS · 2 → clean · 3 → TRIPS | 2026-08-31 | **What is parity-sensitive is the apostrophes ADDED above the waiver row, not the file's absolute count** — measured after round 2's edit, `agents/warden.md` holds 11 apostrophes above that row, an odd number, and the file is still clean. Backticks and double quotes interact, so only the whole-file verdict answers this; a raw count does not. Measure above the waiver row, never below, because quote state flows forward. Pinned by `tests/test_edits_go_through_the_edit_tool.py::test_the_warden_file_does_not_itself_trip_the_commit_gate`. `agents/smith.md` is deliberately not pinned — it has tripped at its own waiver example since before this work |

## Who checked the last round's fixes

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The last round's fixes are read by nobody, and the box saying the review passed is ticked by the session that wrote them | `seal/specs/1788184145-the-gate-stops-the-session-editing-its-tests/rounds/round-3.md#"# 1788184145-the-gate-stops-the-session-editing-its-tests — review round 3">"<!-- Round 3 is the last of the run. The four findings below were raised by the"@fb6b6574` | **Read**, in the record's own comment: four findings *raised by the reviewer, verified by the orchestrator and fixed by the orchestrator* in one commit *rather than by a fourth round*, beside `- [x] Pass` | 2026-09-01 | The second measured case, one work item after issue #33's own. The first is `1788137177-the-axis-nobody-was-asked`, where round 2 found seven defects inside round 1's fixes and round 2's own then went in unread — the whole hit rate on the one set anybody opened. The fixing commit is named in round 3's own record rather than here: a row records what was read, and the commit it was read at is in this work item's `plan.md` Status column. **This row cites a work-item directory** — re-pointed from `specs/` to `seal/specs/` by #79's move, and the one permanent row that reads one; `settle` (#83) removes that directory and has to re-anchor or drop this row when it does (#79 Q7) |
| Reading `Fixes checked by` on the last record alone makes `round-N` unreachable | `skills/code-review/scripts/chain_check.py#main@f02ece96` | **Executed**, by mutation: with the read scoped to `records[-1]`, breaking the sibling lookup outright (`if name in siblings`) left `test_a_later_round_that_exists_is_a_checker` GREEN, because the last record's cell is the only one opened and it can never hold a `round-N` | 2026-09-04 | A checker has to be a LATER round; the last record is the highest-numbered one, so no such record exists. The value is dead under that scope, which is why this read is per-record where `Pass` is not. The trade is Q2 of this work item's `questions.md`. Re-read 2026-09-04 under work item 1788501054, which added a per-record call beside this one in `main`: the claim is about which RECORDS `checked_by` is asked of, and that is unchanged. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved `main` by the `--worktree` flag: it sets the `WORKTREE` global and prints one line saying the working tree is read. Which records `checked_by` is asked of — every record, in the same loop — is unchanged, so the claim holds |
| A cell inside an HTML comment is not the row, because the shared reader strips comments before splitting | `skills/code-review/scripts/chain_check.py#checked_by@62c3bd1b`, through `skills/verify/scripts/unverified_check.py#strip_comments@31da39ce` | **Executed**: a record whose only such row sits inside `<!-- … -->` fails with the *no `Fixes checked by` row* error, and replacing `reader.readable(text)` with `text.splitlines()` turns that exit 1 into exit 0 | 2026-09-01 | The template explains the field in a comment beside it, so a record that keeps the template's comment is the ordinary case rather than a contrived one. Pinned from both sides — `test_a_cell_inside_a_comment_is_not_the_row` and `test_the_template_row_is_a_row_and_not_an_explanation` |
| An unrecognised value is refused rather than read as a checker | `skills/code-review/scripts/chain_check.py#checked_by@62c3bd1b`, `skills/code-review/scripts/chain_check.py#"SEPARATORS = " " + chr(0x2014) + chr(0x2013) + "-:,""@9750da73` | **Executed**: `the session that wrote them` exits 1 naming the three values. The separator set had to include the SPACE — without it `nobody — <why>`, the one spelling every document shows, was itself refused, which the case asserting it passes is what found. The set is also what makes `nobodys fault, really` an unrecognised value rather than a `nobody` with a reason | 2026-09-01 | The direction `CLOSED_WORDS` already takes for a verdict cell, at `skills/code-review/scripts/chain_check.py#"CLOSED_WORDS = {"fixed", "answered", "withdrawn", "not a defect", "agreed, fixed"}"@9750da73`. Read loosely, a session's own name would answer a field whose whole purpose is refusing it |
| Not one verdict cell in this repository is spelled the way `CLOSED_WORDS` and `FIX_WORDS` are, so both refusals that read one had never fired | `skills/code-review/scripts/chain_check.py#verdict_of@3fd16b52` | **Executed**, by counting the cells and then by mutation. All fourteen closed verdicts across `specs/1788184145-…/rounds/round-{1,2,3}.md` read `**fixed**`, ten of them with a commit after the word; zero read the bare `fixed` the sets hold. So `closed_with_a_fix` answered False on every record that exists, and `open_blocking` read a 🔴 closed as `**fixed** \`sha\`` as still open. Reverting the normalizer to its lowercase-and-strip form turns three cases red | 2026-09-01 | Only the HEAD of the cell is consulted, which is what keeps a fix word deeper in a long `answered` cell out of `FIX_WORDS` — `answered — the finding it confirms was **fixed** in \`sha\`` is `answered`, and a substring reader turns that case red. Round 1's 🔴 1. HOW the commit is separated from the word changed in round 2; the row below carries it. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved `verdict_of` by one branch: a word in `HOME_WORDS` with nothing after it comes back `deferred (no home)`, in neither set and OPEN. The head-of-cell match this row is about is the same loop, and a fix word deeper in an `answered` cell is still `answered` |
| A pattern that has to recognise a commit fails on the commits it does not recognise | `` skills/code-review/scripts/chain_check.py#"# The vocabulary as a match ORDER, longest spelling first, so the `fixed`"@af4d69e9 `` | **Executed**, three ways. The round-1 normalizer cut the cell where a citation began and required a digit inside the hex run, so `**fixed** \`deadbee\`` and `**fixed** \`defaced\`` normalized to `fixed deadbee` and `fixed defaced` — neither in `CLOSED_WORDS`, and a 🔴 properly closed read as still open. Prefix matching against `VOCAB` returns `fixed` for both, and against every verdict cell in `specs/*/rounds/round-*.md`: **34 cells, 34 recognised**. Restoring the cut turns eight cases red | 2026-09-04 | A seven-character abbreviation carries no digit about one time in 959, and this repository's house style is `**fixed** \`sha\``, so it arrives eventually. Round 2's 🔴 1, and the repository owner's answer was to change the structure rather than add a second special case. `VOCAB` is derived from `CLOSED_WORDS`, not written out again, for the reason the `FIX_WORDS` assertion exists. **Longest-first ordering is defensive and nothing exercises it today** — no word in the vocabulary is a prefix of another, so the sort is what would keep an ADDED `agreed` from taking the row from `agreed, fixed`, and no case here can go red under it. Re-read 2026-09-04 under work item 1788501054, whose `ORDER_FROM` and `NOT_YET` constants landed inside this comment's region: the claim is about how `verdict_of` matches a commit, and neither constant touches that. `commissioned_fixes` is now a THIRD reader of the same normalizer, so the claim carries further than it did. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**: the anchored region gained `DEFERRED`, `HOME_WORDS`, `NO_HOME` and the assertion that a deferral is not a fix word. `VOCAB` is still derived from `CLOSED_WORDS`, now six words, none a prefix of another, so longest-first is still unexercised; how a commit after `fixed` is matched is untouched |
| A round number being higher is not evidence that the round read the fixes | `skills/code-review/scripts/chain_check.py#reviewed_later@84e63187` | **Executed**: two records committed together carry one `Target SHA`, and round 1 naming round 2 exited 0 before this. Now the same fixture fails, and so does a checker whose `Target SHA` is an ancestor of this record's | 2026-09-01 | Only positively established inversions are refused — the same commit, or the checker's tree an ancestor of this one's — because a squash discards the commits a round reviewed and *cannot be compared* is the ordinary state of a merged record. Round 1's 🟡 5 |
| A `Target SHA` row may name TWO commits, and reading one side at its first defeated the rule | `skills/code-review/scripts/chain_check.py#reviewed_later@84e63187` | **Executed**: `templates/sdd-round.md#"# <work-item-id> — review round <N>">"\| Target SHA \| <the commit this round actually reviewed — both, if HEAD moved mid-review> \|"@5f5b9325` allows both when HEAD moved mid-review, and with the checker read at its newest against this record's FIRST, round 1 writing `A and B` beside round 2 writing `B` exited 0 — where two records at a single shared SHA correctly exited 1. Both sides are now read at their newest; the case fails, and reading either side at its first turns one case red each | 2026-09-01 | A round that read exactly what this round read has seen nothing new, and how many commits each row happens to list is not a fact about who read what. Round 2, and it is a hole rather than a test gap: no fixture had ever written two SHAs |
| A squash leaves two records naming the same commit git can no longer resolve, and that inversion is still established | `skills/code-review/scripts/chain_check.py#reviewed_later@84e63187` | **Executed**: three records where the first two name one unresolvable 40-hex commit and the third a real one — the first fails with the same-commit refusal, the second passes because its checker's cell differs. Deleting the `set(mine) == set(theirs)` path turns it green | 2026-09-01 | Reachability is claimed only for `records[-1]`, so an unresolvable commit on an earlier record is not an error by itself — which is what makes this path reachable at all rather than shadowed by the reachability failure |
| A refusal that would go red on merged history nobody can repair is one people learn to skip, so `Pass` beside `nobody` is grandfathered | `skills/code-review/scripts/chain_check.py#STRICT_FROM@d99e9e1a`, `skills/code-review/scripts/chain_check.py#checked_by@62c3bd1b` | **Executed**: a work item whose directory name begins with a second at or after the cutoff fails on `Pass` beside `nobody`; one before it prints and exits 0, and `specs/1788184145-…/rounds/round-3.md` is that case in this repository. Inverting the comparison turns five cases red in both directions at once | 2026-09-01 | Q1's answer, and the option is the repository owner's rather than either the row's or the reviewer's. One constant serves every repository, because a fresh install creates every work item after it |
| The boundary is `>=` and the work item sitting exactly on it is this one | `skills/code-review/scripts/chain_check.py#checked_by@62c3bd1b` | **Executed**: a fixture at `specs/<STRICT_FROM>-…` with `Pass` checked beside `nobody` exits 1, and narrowing the comparison to `>` turns it green. The second is read out of `chain_check.py` rather than typed, so the case follows the cutoff if it ever moves. Confirmed on a real record too: a `round-2.md` written into this work item's own `rounds/` in a fresh clone during round 2's fix pass fails the same way | 2026-09-01 | `STRICT_FROM` is the id of the work item that added the rule, so the first item held to it is the one that wrote it — which is the whole of what *a fresh install creates every work item after it* rests on. The two existing fixtures are hundreds of millions of seconds either side and held the boundary at neither. Round 2 |
| The answer a run ends on had no field, in either the reviewer's report format or the record | `agents/warden.md#"## Role">"Say plainly whether you opened anything that needs a fix, because the run"@3ef220b5`, `` templates/sdd-round.md#"\| Needs a fix \| <`yes — <what>` · `no`. The reviewer's own answer — what stands after the colon in its `Needs a fix:` line, never the whole line> \|"@78f81d49 `` | **Read**: `agents/warden.md` told the reviewer to say plainly whether it opened anything needing a fix, and neither the Report section nor the record template had anywhere for that answer to go | 2026-09-04 | The `Needs a fix` row now carries it, copied from the reviewer's line rather than re-derived from the verdict table — a 🟡 answered with grounds is a finding that needs no fix. No check reads the row; enforcing it is Q4's neighbour. Round 1's 🟡 10, and `agents/warden.md` refuses this shape in its own words: *a question with no field to sit in becomes a seal taken over an axis nobody decided* |

## Evidence drift

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|

## What the cost meter can read

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A turn is one assistant message carrying at least one tool_use, keyed by the message id with the row uuid and then the row itself as fallbacks — per-block counting pinned tools per turn at ~1.00 structurally | `skills/verify/scripts/session_cost.py#load@d7f90cd4`, `skills/verify/scripts/session_cost.py#strip_pipe@82b0813e` | **Executed**: six new cases in `tests/test_session_cost.py`, each shown red against the per-block code first — a two-call message read 1.0 where 2.0 is true, a split message was two turns, a keyless transcript is one turn per row (the old floor, never an inflated ratio), and a message's tokens counted once per call | 2026-09-06 | The phase commit that carried this is in the work item's `plan.md` Status column rather than here; a ledger row names no commit at all. Review round 1 established three more parser facts (executed there, read here): a result row never creates a turn even when its uuid differs from its call's; an unresolved tool_use joins the turn count but not the call count and drags the ratio down — 0.5 on a synthetic, negligible at a 65-call scale; non-monotonic timestamps split one turn into two gap groups and overstate model time, which an append-only transcript does not produce. **Re-read 2026-09-05 in work item 1788613827's round 1 fix pass**, which put the type check under the three-step fallback: an id that is not a string is skipped for the next candidate, where it used to go straight into a `set` and raise `TypeError: unhashable type`. The order of the three keys and the one-turn-per-row floor are unchanged; `message_key` is where they now live. **Re-read 2026-09-06 in the same work item's round 2 fix pass**, which guarded the one field in `load` whose readers had no guard of any kind — a `tool_use` block's `name`, normalised by `tool_name` before it is stored. Nothing about turn keying moved: the message id still outranks the row uuid, the row position is still the floor, and the three cases that pin that order are green. What the fix touches is the tuple `load` puts in `pending`, whose `name` slot two later readers consume |
| Model time runs from a turn's last result to the next turn's first call, so a batch's internal waits are the tools running, not the model thinking | `skills/verify/scripts/session_cost.py#analyse@59d3d713` | **Executed**: `test_the_gap_after_a_batch_measures_from_its_last_result` books 7s where the per-call reading booked 15s, and the split-message case holds the intra-turn wait at zero model seconds | 2026-09-01 | The line-numbering caveat this row used to carry is gone with line numbers: the coordinate names a symbol and the hash covers its body, so there is no numbering for the tripwire to miss and nothing to re-stamp. The advisory wording forks at a ratio of exactly 1.0: above it, `most turns send a single call` replaces the one-at-a-time claim the number no longer supports. **Re-read 2026-09-05 in work item 1788613827's round 1 fix pass**, which added `call_turns` to what `analyse` returns — `tools_per_turn`'s own denominator, so the printed report can say which turns it counted. No gap, no turn boundary and no model-time arithmetic moved |
| The handoff before round 1 and the progress channel are protocol rather than habit | `docs/review-handoff-protocol.md#"## The handoff before round 1"@885c10ea` | **Executed**: four cases in `tests/test_the_handoff_before_round_one.py`, each red before the section existed — coordinates rather than prose, the three labels, where to measure, the Status column and its stall signal, and the pointer at the meter | 2026-09-04 | **Draft 1.2**, and the section now carries FOUR requirements — work item 1788501054 added *a command with more than one form names the form, and says what the other one is for*, and moved the count sentence with it. No conformance rule was added at either draft: a spawn prompt is not a file the protocol can check. The meter had sat unreferenced through a full day of measurements nobody took. Re-read 2026-09-04 against the enlarged section; the claim is unchanged and the note said `Draft 0.6` until that read. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added one paragraph to the section: the draft pull request precedes the handoff rather than travelling in it, opened when the build's last phase closes, with the review skill's section named as the owner. The four requirements and the pointer at the meter are unchanged. **Re-read 2026-09-05 in work item 1788613827's phase 4**, after phase 3 added two paragraphs to the `### After the run — the per-segment bars` subsection — the bars judge a segment and the run-level table judges a run, with a pointer at the table's owner. Nothing about the handoff's four requirements, the progress channel or the pointer at the meter moved. **Re-read 2026-09-05 in the same work item's round 1 fix pass**, which reworded one sentence of the same subsection — the run-level table's five glossed row names are gone, the pointer at the owner is where it was, and none of the four requirements is touched |

## 0.4.0 — 2026-09-02

<!-- specs/1788229400-every-branch-appends-to-the-same-two-files -->
### 1788229400-every-branch-appends-to-the-same-two-files

Rows for the work item that closed #46 and #52.

A coordinate here names content, never a position: a symbol name where the
language has one, a distinctive line of text otherwise, and a hash of the
region under it. No row carries a line number or a commit, so nothing in one
goes stale for a reason unrelated to the claim it makes.

#### A coordinate names content, not a position

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| An anchor resolves to a symbol span via the stdlib `ast`, or to a quoted line of text, and to nothing else | `skills/evidence-check/scripts/evidence_check.py#resolve_unit@c6839923`, `skills/evidence-check/scripts/evidence_check.py#py_spans@4045ba55` | **Executed**: `handler`, `Box` and `Box.open` each resolve to their own span, decorators included, and a file that will not parse yields `None`, which is the one case that falls through to the generic text rule — a file that parses and lacks the symbol is BROKEN | 2026-09-01 | Decorators are in the span because a decorator carries behaviour. No dependency is added — `ast` ships with Python |
| A change inside the anchored region drifts; an edit that only moves the region does not | `skills/evidence-check/scripts/evidence_check.py#check_ledger@71818d3e`, `skills/evidence-check/scripts/evidence_check.py#content_hash@5c2ba0ae` | **Executed against this repository's own ledger**, not only a fixture: inserting a statement into `_hides_a_commit` reports `1 drifted`, and inserting a line above everything in the same file leaves `34 ok` | 2026-09-01 | The second edit moved every line number in the file. Under the line-number scheme it was the case that read OK while pointing at the wrong lines |
| Trailing whitespace and blank lines are normalised away; indentation is not | `skills/evidence-check/scripts/evidence_check.py#normalise@e7d71707` | **Executed** at the unit and by mutation: normalising indentation too leaves a dedent silent, and that mutation turns exactly one case red | 2026-09-01 | In Python a dedent moves a statement out of the block it belonged to. A hash that shrugged at it would go quiet where the edit matters most |
| The row's own recorded hash decides a tie between places AND whether an unsure place is still the row's unit — **at the major level only** | `skills/evidence-check/scripts/evidence_check.py#check_ledger@71818d3e` | **Executed**: two C# overloads of one name resolve to the one whose content reconstructs the row's hash, exit 0; with a hash no place holds, `BROKEN … locator is ambiguous — 2 places … (none holds the recorded content)`, exit 2; a lone resurrected place that reconstructs nothing is BROKEN with the repo-wide scan naming the destination | 2026-09-01 | A claim row's hash is the minor region's, and two unrelated units can share one identical line, so it licenses neither move there: the tie stands and an unsure place stays DRIFTED, which is `CLAUDE.md`'s own rule (round 8, 🔴 A and 🔴 B). Reverses the rule this row used to carry — *an OK would be a claim about whichever one the code reached first* — which holds for a checker picking arbitrarily and not for evidence the row itself carries (`questions.md` §Q3) |
| A markdown heading owns its section; a `#` comment in Python owns only its own block | `skills/evidence-check/scripts/evidence_check.py#text_regions@3f1f5b41` | **Executed**: `## A` runs past a nested `### A1` to the next `## B`, and the same bytes in a `.py` file are a comment run instead. Found by migrating the real ledger, where a 23-line comment block resolved to one line | 2026-09-01 | The level comparison survived a mutation until a nested heading was added to the fixture — the case had nothing to tell `k <= level` from `k is not None` |
| Re-verifying is a separate command, and the check never rewrites what it checks | `skills/evidence-check/scripts/evidence_check.py#reverify@6eae425b` | **Executed**: `--reverify` rewrites a drifted row's hash and names it; an ordinary run and a `--strict` run both leave the file byte-identical; a row whose anchor is gone is left alone | 2026-09-01 | A check that refreshed what it was checking would report OK for ever |

#### What a broken row is told, and what fixes it

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A BROKEN row names its destination when the content provably moved, graded by evidence: hash identity fixes, a name alone is a labelled fact | `skills/evidence-check/scripts/evidence_check.py#content_matches@c1133829` | **Executed**: a same-file rename prints `(renamed?)`, a cross-file move `(moved?)`; a name with different content prints `same name at … (content differs)` and is never rewritten; a hash match suppresses the name line; two matches are counted, never named | 2026-09-01 | The comparison RECONSTRUCTS — the candidate's name substituted with the row's locator — because a unit's name is inside its own hashed region, so a raw hash comparison can never see a rename. Found when the ordered feature could not fire at all |
| `--reverify` re-anchors what reconstruction proves, and the hash follows the locator; the scan is bounded and says so past its caps | `skills/evidence-check/scripts/evidence_check.py#reverify@6eae425b`, `skills/evidence-check/scripts/evidence_check.py#scan_candidates@bce22d7e` | **Executed**: a whole-file rename heals to zero broken; renamed-and-edited and two-identical-units are left; measured at 110 ms for one broken row against 200 files and 36 ms degraded past the cap, against a 130 ms clean run | 2026-09-01 | Same extension only, 256 KB size cap, 200-file cap, built lazily so the clean path pays nothing |
| A 0.1.0 row is loud, never invisible, and `--migrate` ships this branch's own migration | `skills/evidence-check/scripts/evidence_check.py#old_format_rows@68ad9a74`, `skills/evidence-check/scripts/evidence_check.py#migrate@21149e70` | **Executed**: an old ledger that read `0 ok`, exit 0 — the release blocker, pinned verbatim before the fix — now fails as OLD-FORMAT naming the migrator; `--migrate` converts to enclosing units, drops stamps, keeps dates, is idempotent, and leaves-and-names EOF, missing files and rows it can only half prove | 2026-09-01 | A red build naming one command against a green build checking nothing |
| The post-commit advisory prints the checker's own lines and never blocks | `hooks/evidence-advisor.py#main@b37509d9`, `hooks/evidence-advisor.py#failing_rows@b51f42d6` | **Executed** through `dispatch.py post-bash`: a commit after a rename prints the row, the graded hint and the `--reverify` remedy; silent when clean, when only drifted, when no commit ran, and when the repository never opted in | 2026-09-02 | Advisory by design: a pre-commit block would fight the commit-early rule, and a line printed on every commit is a line people learn to skip |
| The pre-anchor ledger migrates itself at session start, once per repo, and never over uncommitted work | `hooks/ledger-migrate.py#main@2b930da5`, `hooks/ledger-migrate.py#dirty@a82428bb` | **Executed**: the first start migrates and prints one line with the migrated, left and unproven counts; the second is silent; an uncommitted ledger file is skipped without stamping the marker, so the next clean start migrates; a scratch-marked repo and a git that cannot answer are both left alone | 2026-09-01 | 9 mutations over every branch, all killed. The engine is `--migrate`'s own — `skills/evidence-check/scripts/evidence_check.py#migrate@21149e70` — so the hook adds boundaries, not a second migration |

#### The changelog fragments

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A fragment counts as gathered when its marker comment is in `CHANGELOG.md`, never by matching its text | `.github/scripts/gather_changelog.py#marker@b1e2b91c`, `.github/scripts/gather_changelog.py#fragments@a8c92cdb`, `.github/scripts/gather_changelog.py#ungathered@6c94ca94` | **Executed**: re-wording a released entry leaves `--check` green, and deleting the marker turns it red | 2026-09-02 | Matching the text works exactly once — the first copy-edit to a released entry would reopen its fragment forever |
| Gathering is idempotent and ordered by work item id | `.github/scripts/gather_changelog.py#fragments@a8c92cdb`, `.github/scripts/gather_changelog.py#ungathered@6c94ca94`, `.github/scripts/gather_changelog.py#section@e5fb4146`, `.github/scripts/gather_changelog.py#insert@c985894f` | **Executed**: a second run exits 1 with the entry present once, and the earlier id sorts above the later one in the written section | 2026-09-02 | The id is unix seconds, so the order is chronological and, more to the point, does not depend on the filesystem |
| The release pull request fails while a fragment is ungathered, and no other pull request runs the check | `.github/workflows/hygiene.yml#"echo "base is ${{ github.base_ref }} — fragments are gathered when the release reaches main"; exit 0"@0cb0ca06` | Read, not run — the branch condition is the same shape as the version-bump step above it at `:30`. The script's own two directions are executed | 2026-09-01 | On a feature pull request every fragment on the branch is legitimately ungathered, which is why the step skips itself |

#### What round 5's fixes hold

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A ledger nobody can read is reported, never read as an empty one | `skills/evidence-check/scripts/evidence_check.py#check_ledger@71818d3e`, `skills/evidence-check/scripts/evidence_check.py#read@c5f1f40e` | **Executed**: with `read` answering `None`, the check returns one BROKEN row rather than `[]`, `--migrate` counts it in `left` and `--reverify` exits non-zero — all three refuse to answer "nothing to do" | 2026-09-01 | The direction `tests/test_gates_do_not_fail_open.py` exists for, one mechanism further out than the decode it was written about |
| A row may not read outside the repository it is placed in, and the containment test is against the repo the placement returned | `skills/evidence-check/scripts/evidence_check.py#place@9917ee93`, `skills/evidence-check/scripts/evidence_check.py#contained@59bfe28b` | **Executed**: `../outside/creds.py#secret` is BROKEN at exit 2 in the check, LEFT in `--migrate`, and left byte-identical by `--reverify`; a `--map` prefix still resolves into its own checkout at exit 0 | 2026-09-01 | Present since 0.1.0 in the `path:line` form, so this is a new guard rather than a regression close. Symlinks are resolved, so a citation that leaves the tree through one is refused too |
| The ledger writer replaces the real file, keeping a symlink and the mode it found | `skills/evidence-check/scripts/evidence_check.py#write_atomic@8bd97a9a` | **Executed**: writing through a symlinked ledger leaves the link in place, updates the file behind it, and preserves mode 664; a plain ledger stays 644 | 2026-09-01 | `mkstemp` creates at 0600 and git tracks nothing but the exec bit, so the demotion this replaces was invisible in the diff |
| `--migrate`'s since-the-stamp proof reads the file under the root it was given | `skills/evidence-check/scripts/evidence_check.py#content_at@9698d08a` | **Executed**: a `--migrate` run from a subdirectory, with a same-named decoy at the repository top level, migrates the row and reports no unproven rewrite; the top-level run does the same | 2026-09-01 | `git show <sha>:<rel>` resolves against the top level; `./` is what makes it resolve against `-C` |
| Cross-repo intent is read per ROW wherever the coordinate can decide it | `skills/evidence-check/scripts/evidence_check.py#cross_repo_intent@18c8c5b0` | **Executed**: with one `--map` declared, a local file rename still prints `identical content at src/moved.py#handler`; a parity repository with no `--map` still reports one EXTERNAL | 2026-09-02 | A row whose prefix is not among the declared maps is a local row. An unprefixed row under `parity.md` or `--default-repo` stays undecidable, and SKILL.md's Known limits says what that costs |
| The declaration rule reports how sure it is instead of being asked to be right: a resurrected candidate is flagged to its callers | `skills/evidence-check/scripts/evidence_check.py#generic_units@d742c0b2`, `skills/evidence-check/scripts/evidence_check.py#resolve_unit@c6839923` | **Executed**: `public new void Render(int x)` and `case loading(String)` answer `([(1, 2)], True)` and `([(2, 2)], True)`, a file with no candidate at all answers `([], False)` rather than claiming a resurrection, a one-line `render(y)` with no semicolon is marked while the multi-line `function render(x) {` is not, an unblocked declaration answers `False`, `return render(y);` is still not a declaration, and `render(1);` is refused structurally by ending the statement | 2026-09-01 | Round 4's 🔴 1, round 5's 🔴 C and round 6's 🔴 J are three attempts at one rule and the 3+ Fix Rule fired. The uncertainty leaves the classifier rather than being argued inside it |
| The session-start migration carries the warning the command prints | `hooks/ledger-migrate.py#main@2b930da5` | **Executed**: a ledger whose stamp names no reachable commit migrates with `1 rewritten without the since-the-stamp proof` in the session-start line | 2026-09-01 | This path runs without anyone asking for it and `stamp(root)` means it is never offered again, so the warning belongs here more than on the typed command |

#### What round 6's fixes hold

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A unit that moved out of any non-`.py` file, leaving a call behind, is BROKEN with its destination named — the answer `ast` already gives `.py` | `skills/evidence-check/scripts/evidence_check.py#check_ledger@71818d3e` | **Executed** in both shapes: `render` moved out of `src/app.js` leaving `return render(y);`, and out of `src/app.kt` leaving a semicolon-less `render(y)`. Both report `BROKEN … identical content at …#render (moved?)`, exit 2; `--reverify` re-anchors to the destination and the recheck exits 0 | 2026-09-01 | It read DRIFTED and `--reverify` anchored the row onto the call site permanently — round 4's 🔴 2 returning for every file that is not `.py` |
| `--reverify` never writes onto a place the declaration rule is unsure of, never contradicts the check's verdict, and never answers a flagged row with silence | `skills/evidence-check/scripts/evidence_check.py#reverify@6eae425b` | **Executed**: a place the rule is unsure of with no provable destination leaves the ledger byte-identical and reports `0 rows re-verified`; a five-row ledger covering every BROKEN and DRIFTED shape gets a line per row, each naming the coordinate the check named, claim included; an unchanged unsure row prints nothing and re-verifies nothing | 2026-09-01 | This command MAKES the hash, so it cannot use one to tell a declaration from a call. The cost is stated in Known limits: a keyword-shaped declaration whose content changed in place is re-verified by hand |
| Every branch of the placement runs the containment test, `--default-repo` included | `skills/evidence-check/scripts/evidence_check.py#place@9917ee93`, `skills/evidence-check/scripts/evidence_check.py#contained@59bfe28b` | **Executed**: with `src/creds.py` in the default repo a symlink out of the tree, the row reports `escapes the repository` at exit 2 where it used to report `1 ok` at exit 0 | 2026-09-01 | Two branches were guarded in round 5 and the third was not. `realpath` degrades to a lexical normalisation where links cannot be resolved, which still catches `..` |

#### What round 7's fixes hold

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A place the declaration rule is unsure of is named with the hash to record, so a row citing one can still be written by hand | `skills/evidence-check/scripts/evidence_check.py#check_ledger@71818d3e`, `skills/evidence-check/scripts/evidence_check.py#recorded_here@cf7cec26` | **Executed**: a `.cs` file holding only `public new void Render(int x) {` reported `locator not found` while the unit sat at lines 1-2. It now names `1-2@<hash>`, and putting that hash in the row makes the check say `1 ok` at exit 0 | 2026-09-01 | Round 6 made such a unit unrecordable in a ledger at all: `--reverify` refused it and nothing printed the hash the refusal told a person to record |
| `--migrate` answers an unsure place the way `--reverify` does, unless the old stamp vouches for the cited lines | `skills/evidence-check/scripts/evidence_check.py#migrate@21149e70`, `skills/evidence-check/scripts/evidence_check.py#file_units@51c206a6` | **Executed**: an unstamped row citing lines 1-2 of a `.cs` file is left with the same hash prescription and the ledger is byte-identical; a row whose stamped commit holds the same lines still migrates | 2026-09-01 | The exception is evidence rather than a softening. `proved` says the cited lines have not moved since the person wrote them, which is evidence about that place; `--reverify` has none of that and always refuses |

<!-- specs/1788272986-the-fixes-are-what-open-the-next-round -->
### 1788272986-the-fixes-are-what-open-the-next-round

Rows for the work item that closed #57.

#### The fix surface is a row the pull-request check refuses to lose

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A record of a work item begun on or after the cutoff needs `Contract changes` and `New units`, and a changed unit is listed with its reach | `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68` | **Executed** — `tests/test_the_fixes_name_their_surface.py`: a missing row fails naming what it buys, a `;`-separated entry with no `→`/`->` (or an empty half) fails naming the entry, an empty cell fails on any record, and a commented row is not the row | 2026-09-04 | Read on every record, like `Fixes checked by`, because every round has its own fixes. Only the ABSENT row is grandfathered by `SURFACE_FROM` — a malformed one is always the author's to fix. **Re-read 2026-09-04 under work item 1788501054, whose pending arm went into this unit**: that second sentence used to end at *the author's to fix*, and it is now narrower than the code. The arm has a grandfathering of its own, `ORDER_FROM`, and it reaches a row that is PRESENT and well formed — before that cutoff, `none — the fixes are not yet written` beside a `round-N` checker prints instead of failing, which `test_the_pending_surface_only_prints_before_the_cutoff` runs. What the sentence still says truly is that a MALFORMED row is refused at any age; the claim above it is untouched |
| `none` is an answer in both rows, with or without a reason after a separator | `skills/code-review/scripts/chain_check.py#says_none@f2a20d93` | **Executed**: `none`, `` `none` — the fixes changed no signature `` and `none, nothing added` pass; `nonempty` would not — the boundary is a separator, matched as a prefix the way `verdict_of` matches its vocabulary | 2026-09-01 | `none — the fixes are not yet written` is the honest mid-run value, which is why the draft excuse is not needed here |
| Records of work items begun before `SURFACE_FROM` print rather than fail | `skills/code-review/scripts/chain_check.py#SURFACE_FROM@e8df3db3` | **Executed**: the same missing rows on a `1787700000` item exit 0 with a notice per row, a no-timestamp item is excused too, and the item at the cutoff second is held to the rule (`>=`, read from the module so a moved cutoff moves the case) | 2026-09-01 | The value is this work item's own id, so the first item held to the rule is the one that wrote it — the property `STRICT_FROM` already has. Every existing record in this repository predates it; none was edited |

#### What round 1 settled

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| An unreadable round record cannot read as "no rows required" | `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68`, `skills/code-review/scripts/chain_check.py#main@f02ece96` | **Executed** (round 1's probe): a garbage record exits 1 — `fix_surface` returns nothing for it, and `checked_by` errors on the same record in the same loop, so the silence is never the whole answer | 2026-09-04 | Merged from round 1's evidence-todo. Re-read 2026-09-04 under work item 1788501054: `written_late` also returns nothing for an unreadable record, so the sentence is now true of three readers in that loop rather than two, and `checked_by` still errors on the same record. **Re-read again 2026-09-04 in that item's round 3 fix pass**, which drifted this anchor a second time: what moved is prose in `fix_surface`'s docstring recording the pending arm's key and the two states it does not reach, and the `if text is None: return [], []` this row is about is untouched. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved `main` by the `--worktree` flag alone; the loop that asks three readers of one record is untouched, and `read_record`'s working-tree branch returns None on a file it cannot open the way the `HEAD` read does, so an unreadable record is still refused rather than silent |
| The decode-failure fail-open class cannot reach `chain_check.py` | `skills/code-review/scripts/chain_check.py#git@0d87d1f9` | Read: `git()` pins `encoding="utf-8", errors="replace"`, so the locale-decode → `None` → silent-allow path `tests/test_gates_do_not_fail_open.py` covers in hooks is unconstructible here | 2026-09-02 | Round 1's 🟡 2, answered — the state that file pins cannot be built against this reader |
| A cell of only separators is refused in both fix-surface rows | `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68` | **Executed**: `\| ; \|` and `\| — \|` on a post-cutoff item exit 1 naming both rows — `tests/test_the_fixes_name_their_surface.py::test_a_cell_of_only_separators_is_not_an_answer`, seen red against the unguarded loop first | 2026-09-04 | Round 1's 🟡 1. Re-anchored from the pre-fix claim ("passes") to the refusal that closed it. **Re-read 2026-09-04 under work item 1788501054, whose pending arm went into this unit**: the refusal is unmoved and the re-read is what says so rather than the hash. The arm sits INSIDE the `says_none` branch, which stands above this check, and a cell of only separators never satisfies `says_none` — `;` normalizes to the empty string and `—` does not start with the word — so such a cell falls past the new branch to the same refusal on the same values. Executed at HEAD: `test_a_cell_of_only_separators_is_not_an_answer` green, and both rows still named |

#### The review-skill rules the same measurement bought

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The verifying round treats what `New units` names as a finding surface, not a verification surface | `skills/code-review/SKILL.md#"## Orchestrator: the run ends with a verifying round"@4993f5fa`, `agents/warden.md#"## Role">"judge them as code — *is this correct* — never as fixes. A finding there is"@98a430c2` | Read, pinned by `test_the_fixes_name_their_surface.py` with the inverted spellings refused beside the sentences | 2026-09-04 | Without the exemption, *answers rather than new findings* tells the reviewer to skip the one set of units nobody has reviewed — round 4's fix commit created eight, four defective. Re-read 2026-09-04 under work item 1788501054, which appended two subsections to the anchored region: the exemption's sentence is untouched, and the work item's own round 1 is a worked instance — six units created by a fix pass, and the record naming them was what the verifying round opened. **Re-read again 2026-09-04 in that item's round 3 fix pass**, which appended a third paragraph to the same region: it records what the pending fix-surface refusal does NOT reach, which is about `Fixes checked by` rather than about the exemption, and the exemption's sentence is still untouched. **Re-read 2026-09-05 in the same item's round 7 fix pass**, which added three sentences to the section's `Needs a fix` paragraph: a `fixed` verdict now also stops the floor's count, so the row stays the reviewer's while the bound reads the verdict column. That is about the count and not about the exemption, whose sentence is untouched a fourth time. **Re-read 2026-09-05 in the same item's round 9 fix pass**, which corrected the section's own count-rule paragraph — it had stated the first stop as the whole rule while the `Needs a fix` paragraph 47 lines above stated both, the two-answers-in-one-document shape. The exemption's sentence is untouched a fifth time. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**: the section's *nothing runs away* sentence was replaced by the reopening bounded to one, linking the spec's subsection, and a paragraph on record-located corrections was added. The exemption's sentence is untouched a sixth time |
| The axes table carries a security row, and a paste-ready fix at an OS boundary states its assumed precondition | `skills/code-review/SKILL.md#"## Comparison axes"@1c5f6051`, `skills/code-review/SKILL.md#"## Findings format"@063f14ca` | Read, pinned by `test_review_axes.py` (the row and its probes) and the new test file (the sentence and the five boundary premises) | 2026-09-01 | Security was named in stage 2 and absent from the table, and the table is what makes an axis mandatory. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved `Findings format` alone: the 🟡 line now reads *a defect the release would ship* and a ⬜ line sits beside it. The sentence this row pins — a paste-ready fix at an OS boundary states its precondition — and the axes table's security row are untouched |

<!-- specs/1788277657-one-bar-misreads-two-of-the-three-segment-kinds -->
### 1788277657-one-bar-misreads-two-of-the-three-segment-kinds

Rows for the work item that wrote the per-segment bars and the resume rule
(issue #51, observations 1 and 2).

#### One bar no longer judges all three segment kinds

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The bars are written beside the meter they interpret, one per segment kind, and they refuse nothing — a reviewing segment at tools per turn ≥ 1.8, an implementing segment on `repeats = 0` and calls per deliverable, a verifying segment exempt | `docs/review-handoff-protocol.md#"### After the run — the per-segment bars"@6b8c8a9e` | **Executed** — pinned by `tests/test_the_handoff_before_round_one.py::test_the_protocol_names_a_bar_per_segment_kind` and the draft-number pin (`::test_the_title_and_the_status_section_agree_on_the_draft`), the latter planted first and seen red against the 0.7 Status line before round 1's 🔴 1 fix landed; the cross-instrument pin (`::test_the_advisory_and_the_tying_paragraph_name_one_value`) reads the threshold out of `session_cost.py` and the tying sentence out of the protocol, so either moving alone turns it red | 2026-09-02 | The small-round nuance is in the section: a 23-call round read 1.64 with few independent batches to rise on, so the bar reads rounds of ordinary size. The advisory (1.2, in `session_cost.py`) and the bar (1.8) are tied by the section's closing paragraph — the script cannot tell segment kinds apart, the orchestrator can. **Re-read 2026-09-05 in work item 1788613827's phase 4**, after phase 3 added the run-level table's two paragraphs to this section. The three bars, their thresholds and the tying paragraph are untouched, and so is the `1.2` in `session_cost.py` that `test_the_advisory_and_the_tying_paragraph_name_one_value` reads out by regex — the new paragraphs say what the table judges and point at its owner rather than restating a bar. **Re-read 2026-09-05 in the same work item's round 1 fix pass**, which took the five glossed row names out of that paragraph after round 1's finding 8. The three bars, their thresholds, the small-round nuance and the tying paragraph are untouched, and the case that reads the `1.2` out of `session_cost.py` is green |
| A fix pass is obtained by resuming the implementing session; the fresh spawn is the fallback for a session that no longer exists, and it costs the handoff before round 1 | `skills/code-review/SKILL.md#"## Orchestrator: a fix pass resumes the implementer"@c0dc63a1` | **Executed** — pinned by `tests/test_review_axes.py::test_the_fix_pass_resumes_the_implementer` (the sentence, the 282-call measurement, the fallback clause); `tests/test_docs_line_wrap.py` green on the file (it is wrap-covered), and the anchored regions on either side of the insertion kept their content (`evidence-check --strict` clean) | 2026-09-02 | Three measurements, no counterexample: 282 calls / 45 minutes as a fresh spawn (#33) against 30 / 3.9 (#29) and 26 / 5.2 (the #57 chain) as resumes. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `## Fixes` table paragraph to the section — `close` applies it, and the pass writes no phase record. The resume-first sentence, its 282-call measurement and the fallback clause are untouched |

<!-- specs/1788302682-the-release-check-never-watched-bin -->
### 1788302682-the-release-check-never-watched-bin

Rows for the work item that put `bin/` under the release check's eye and
pinned the list of roots it watches (issue #10).

#### The release check watches every root that ships

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A release pull request that changes `bin/` must move the version, because the loader puts `bin/` on the Bash tool's PATH while the plugin is enabled | `.github/workflows/hygiene.yml#"- name: a change to what ships must move the version"@2b250a60` | **Executed** — `tests/test_the_release_check_watches_what_ships.py::test_every_shipping_root_is_watched[bin]` reads the pattern out of the step and matches `bin/anything`; seen red against the five-root pattern before the fix, and red again with `bin` removed from the fixed pattern. The real engine agrees: `grep -E` with the step's pattern kept `bin/unverified-check` and dropped `docs/x.md` and `install.sh` | 2026-09-02 | That `bin/` ships: executed — this session's PATH carries the plugin cache's `bin/` and `command -v evidence-check` resolves into it; read — the Claude Code plugin reference's *File locations* row for `bin/`, and `README.md` §CLI |
| Every tracked top-level entry of the repository is classified as shipping or staying home, and an entry nobody has classified fails the suite | `tests/test_the_release_check_watches_what_ships.py#test_every_top_level_entry_is_classified@e3ed00ee` | **Executed** — dropping `evals` from `STAYS_HOME` turned the test red naming `['evals']`; `test_the_pattern_names_nothing_this_file_has_not_classified` holds the pattern to exactly the `SHIPS` set, so a root added to the workflow alone is red too | 2026-09-02 | The pattern is matched with Python's `re`, not by spawning `grep`: the suite runs on windows-latest |

<!-- specs/1788305134-the-reader-stops-where-it-need-not -->
### 1788305134-the-reader-stops-where-it-need-not

Rows for the work item that re-applied the command reader's two needless
stops onto this history and proved the aimed reset that came with them.

#### A path the command wrote out is one the gate can read

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A directory the command wrote out for itself is one the gate can judge, and one only the environment holds is still not: the substitution runs in front of the `EXPANDS` test and the test stays | `hooks/cmdline.py#_substitute@2f406f2b`, `hooks/cmdline.py#_expanded@4497dc1f`, `tests/test_a_path_the_command_wrote_out.py#test_the_named_form_answers_what_the_written_out_form_answers@7a229992`, `tests/test_a_path_the_command_wrote_out.py#test_an_environment_variable_is_still_unresolvable@4d09142f` | **Executed** 2026-09-02, both gate tests in the scope run; the operand with a `$` the environment cannot fill still arrives at the prompt with the `$` in it | 2026-09-02 | Only a `cd` destination and a `git -C` value are filled; the command WORD is left alone |
| Only a segment the reader understands binds a name, a prefix assignment does not — `A=1 true` is that command's environment, not the shell's — and an array assignment `A=(x)` empties the name rather than binding `(x)` | `hooks/cmdline.py#_bind@71c43e63`, `tests/test_a_path_the_command_wrote_out.py#test_an_assignment_in_front_of_a_command_is_that_commands_environment@debc83c6`, `tests/test_a_path_the_command_wrote_out.py#test_an_array_assignment_empties_the_name@573b1c1c` | **Executed** 2026-09-02; bash 3.2.57 prints an empty line for `A=1 true; echo "$A"`; round 1 measured `SB=(/three); git -C "$SB"` composing `(/three)` onto the session directory before the array rule | 2026-09-02 | |
| A segment that may have written a name forgets it, wherever the writer stands in the segment — `IFS= read -r SB`, `command read SB`, `export SB=/two` — and so do `((SB=3))`, `let "SB = 3"` and `${SB:=…}` / `${SB=…}` in any argument | `hooks/cmdline.py#_forget@3625dd18`, `tests/test_a_path_the_command_wrote_out.py#test_a_name_a_segment_may_have_written_is_forgotten@607d33aa`, `tests/test_a_path_the_command_wrote_out.py#test_only_a_bare_name_operand_is_forgotten@93a5575b`, `tests/test_a_path_the_command_wrote_out.py#test_the_writers_round_one_found_are_forgotten@8a20c894` | **Executed** 2026-09-02; narrowing the operator set `"=+["` to `"="`, loosening `end == len(tok)`, keeping the `((` on the token, keeping `let` to bare names and dropping the `${…=` scan each turn a test red | 2026-09-02 | Round 2 of the original item found this reading the FIRST token only; round 1 of this item found the three writers |
| A refused segment that carries no name keeps the environment; one reaching text this reader never sees empties it; a loop forgets the name it iterates | `hooks/cmdline.py#_unseen@cc410763`, `hooks/cmdline.py#_runs@0b4ff88e`, `hooks/cmdline.py#OPAQUE@66928428`, `hooks/cmdline.py#LOOP_WORDS@3311e8e9`, `tests/test_a_path_the_command_wrote_out.py#test_a_refused_segment_that_carries_no_name_keeps_the_environment@d389eedc`, `tests/test_a_path_the_command_wrote_out.py#test_a_refused_segment_that_can_write_a_name_forgets_it@16859db1`, `tests/test_a_path_the_command_wrote_out.py#test_the_blind_sweep_forgets_bare_names_only@c6f51034` | **Executed** 2026-09-02; restoring the wide reset and narrowing it to `_forget` each turn a test red; the blind sweep's bare-name rule had no pin and has one now | 2026-09-02 | This unit was written after the original branch's last commit and reviewed by nobody before this item |
| A statement inside a compound command's body is forgotten rather than bound; the body is an entry on a stack, closed only by its own closer in command position, and `)` closes only a `(` | `hooks/cmdline.py#_nesting@0472ff22`, `hooks/cmdline.py#_leads@aebb2626`, `hooks/cmdline.py#CLOSES@1c9663a2`, `hooks/cmdline.py#walk_directories>"env = _bind(env, tokens) if not stack else _forget(env, tokens)"@bbd066e1`, `tests/test_a_path_the_command_wrote_out.py#test_a_bodys_later_statement_is_not_a_top_level_one@938252f5`, `tests/test_a_path_the_command_wrote_out.py#test_the_body_ends_at_its_closer@b3f334ba`, `tests/test_a_path_the_command_wrote_out.py#test_what_opens_a_body_and_what_closes_one@90c2c062` | **Executed** 2026-09-02: round 0's differential of the wide reset against the aimed one over 1,790 inputs found 82 fail-opens; round 1 found two more the integer count let through (a multi-line `case` arm pattern `a )`, the glued `f(){`); after the stack, 2,109 inputs against bash 3.2.57 — 0 fail-open, 0 resolutions lost. 7 mutations of the count and 4 of the stack each red | 2026-09-02 | After the stack two shapes still answer where bash disagrees: a quoted closer written ALONE as a command inside a body that does not run (`if false; then "fi"; SB=/three; fi`), and a subshell whose first statement is a subshell (`( ( echo hi ); SB=/three; true )`) — round 3 found the second and the repository owner recorded it as issue #72 rather than fixing it here. A quoted `")"` no longer does, and a closer in argument position never did. A glued closer (`make)`) is not counted and costs a prompt; a `case` arm that matches prompts too; and round 2's rule — a spaced `)` belonging to the segment's own `SB=(`, `$(` or `<(` never pops the subshell — leaves the subshell open when the two closers share a segment (`SB=( a b ) )`), a prompt |
| A call to a function this string defined empties the environment; a function or alias defined outside the string is the edge that stays open | `hooks/cmdline.py#_definitions@26895d7d`, `hooks/cmdline.py#walk_directories>"if _runs(tokens) in defined:"@13ca7709`, `tests/test_a_path_the_command_wrote_out.py#test_a_call_to_a_function_the_string_defined_empties_the_environment@7680a0eb` | **Executed** 2026-09-02; `f() { SB=/three; }; SB=/one; f; git -C "$SB"` answered `/one` where bash has `/three` before it; disabling the check turns the test red | 2026-09-02 | Round 1 🟡 4. The call is read by `_runs`, so `time f`, `then f` and `true && f` reach it |
| A reserved word or a relocator behind a prefix is not a simple command — the refusal the first word meets is asked again of the word that runs | `hooks/cmdline.py#understood>"if (toks[at] in RESERVED and toks[at] not in CONDITIONS) or toks[at] in RELOCATORS:"@110dda9c`, `tests/test_a_path_the_command_wrote_out.py#test_a_reserved_word_behind_a_prefix_is_not_a_simple_command@cc51a29b` | **Executed** 2026-09-02; `! for SB in /two /three` answered `/one` where bash has `/three` before it | 2026-09-02 | The directory half inherits it fail-closed: `time pushd <x>` now stops |
| The pipeline branch forgets, and `lastpipe` is why: which stage runs in the current shell depends on a shell option this process cannot see | `hooks/cmdline.py#walk_directories>"elif known:"@040ed27a`, `tests/test_a_path_the_command_wrote_out.py#test_the_pipeline_branch_forgets_and_lastpipe_is_why@29011d13` | **Executed** 2026-09-02; turning the branch into `pass` turns the test red, where round 2 of the original item found nothing reaching it | 2026-09-02 | |
| Whatever the reader answers about a name, bash answers the same — a prompt is exempt, an answer is not | `tests/test_the_reader_agrees_with_bash.py#test_whatever_the_reader_answers_bash_answers_too@7a3421a8`, `tests/test_the_reader_agrees_with_bash.py#WRAPPERS@021beee1` | **Executed** 2026-09-02 under bash 3.2.57, one bash process for the whole corpus; skipped where `usable_bash()` is false | 2026-09-02 | Round 1 🟡 13: the round-0 differential lived in a scratchpad and every wrapper in it was one line, which is how 🔴 2 and 🔴 3 escaped it |

#### A `((` inside a `${…}` word is a word to both paren models

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Both paren models count `${…}` nesting, so a `((` or a `#` inside an expansion is a word part, a heredoc opener inside one opens no body, and an unbalanced `${` still lets a real heredoc open | `hooks/cmdline.py#drop_comments@1aee4b3d`, `hooks/cmdline.py#_heredoc_split@74553aaa`, `hooks/cmdline.py#_closes@8587742d`, `tests/test_what_the_reader_understands.py#test_a_hash_inside_an_expansion_closes_no_brace_in_either_model@4eb81103`, `tests/test_what_the_reader_understands.py#test_a_heredoc_opener_inside_an_expansion_opens_no_body@35f77220`, `tests/test_what_the_reader_understands.py#test_an_unbalanced_expansion_still_lets_a_heredoc_open@3e34217f` | **Executed** 2026-09-02 in the scope run | 2026-09-02 | An expansion counts only when a `}` closes it, quotes tracked, because an unbalanced count hid every opener after it |

#### An agent can write a probe that commits

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
<!-- The one row that stood here is removed by work item 1788433011-every-spawn-prompt-is-retyped-from-memory: it claimed both agent definitions carry the three probe shapes, and they now point at `skills/agent-contract/SKILL.md` §7 and §8 instead. The new claim is that work item's ledger fragment, row L7. The heading is left standing because the marker and section structure above it are what `fold_ledger.py` wrote. -->

<!-- specs/1788310269-the-implementer-leaves-a-mark -->
### 1788310269-the-implementer-leaves-a-mark

Rows for the work item that gave the routing declaration's third axis its
reader: a mark written when `smith` is spawned, and a notice when a
declaration says `smith` and no mark stands.

#### The mark — spawning smith leaves one, nothing else does

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Spawning `smith` (`specseal:smith` or a project-local `smith`) through the `pre-agent` group writes the checked-out branch name to `<git-dir>/specseal-implementer` and prints nothing; `specseal:warden`, `specseal:scribe`, `general-purpose` and `smith-helper` write nothing | `hooks/implementer.py#is_smith@a2add402`, `hooks/implementer-mark.py#main@735e7b4d`, `hooks/dispatch.py#GROUPS@b3d45306`, `tests/test_the_implementer_is_recorded.py#test_spawning_smith_leaves_a_mark@c674c0e8`, `tests/test_the_implementer_is_recorded.py#test_spawning_any_other_agent_leaves_none@445771e0`, `tests/test_the_implementer_is_recorded.py#test_the_project_local_spelling_of_the_agent_counts_too@801911f7` | **Executed** 2026-09-02 through `dispatch.py pre-agent`; a substring test in `is_smith` and dropping the gate from the group each turn a test red | 2026-09-02 | The qualifier is dropped with `rsplit(":", 1)`, never a substring test Round 1: `is_smith` accepts any qualifier — `other:smith` and `a:b:smith` mark too — and rejects `smithy`, `Smith`, empty, `None`, non-strings. |
| A repository with no `seal/` gets no file written, and a mark for one branch does not stand for another | `hooks/implementer-mark.py#main@735e7b4d`, `hooks/implementer.py#write@62fc5f6e`, `hooks/implementer.py#stands@bb7066de`, `tests/test_the_implementer_is_recorded.py#test_a_repository_that_never_opted_in_gets_no_files_written@28cfe89b`, `tests/test_the_implementer_is_recorded.py#test_the_mark_does_not_answer_for_another_branch@f743387b` | **Executed** 2026-09-02; removing the opt-in test and writing `HEAD` instead of the branch each turn a test red | 2026-09-02 | Keyed on the branch, not HEAD: a work item commits many times and the implementer does not change when it does Round 1: in a linked worktree the mark and the notice marker live in the per-worktree git dir (`--absolute-git-dir`), so "once per repository" is once per checkout. |
| A mark gate that is unparseable or absent leaves the worktree guard's verdict on an `isolation: "worktree"` spawn byte-identical, and the mark is written before the group decides | `hooks/dispatch.py#run_gate@0859390d`, `tests/test_the_implementer_is_recorded.py#test_a_broken_mark_gate_leaves_the_worktree_guards_verdict_alone@9dced271` | **Executed** 2026-09-02 on a copy of `hooks/`; narrowing `run_gate`'s catch from `Exception` to `ImportError` turns the test red. Also measured with `cmdline.py` broken: the group prints nothing and the mark is still written | 2026-09-05 | Issue #26's second objection, measured; `questions.md` Q1 carries whether the issue closes. **Re-read 2026-09-05 at the 0.8.1 release preparation**, which found the content under `run_gate` moved by one line: the rider stamp inside it names `881fb0f` where it named `16c16c7`, the SHA the main-history rewrite moved (`0946350`). The catch is still `Exception` and the mark is still written before the group decides, so the claim holds |

#### The notice — said once, and only where the answer went unhonoured

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| After a command that invokes `git commit`, where the declaration for this branch answers `smith` and no mark stands, one line names `seal/specs/<item>/routing.md` (joined with `os.path.join`); with the mark present nothing is said; once per repository per session | `hooks/implementer-notice.py#main@b9dbc9af`, `hooks/implementer-notice.py#already_told@6ee06150`, `tests/test_the_implementer_is_recorded.py#test_a_declared_smith_with_no_mark_is_noticed_after_a_commit@cc15e53f`, `tests/test_the_implementer_is_recorded.py#test_the_notice_names_the_file_the_way_the_platform_spells_it@508df1e8`, `tests/test_the_implementer_is_recorded.py#test_with_the_mark_present_nothing_is_said@9af529d7`, `tests/test_the_implementer_is_recorded.py#test_the_notice_is_said_once_per_repository_per_session@bbaa6907` | **Executed** 2026-09-02 through `dispatch.py post-bash`; disabling the mark check, the once-per-session marker, and the item name each turn a test red | 2026-09-02 | The per-session marker lives at `<git-dir>/specseal-implementer-notice/<session-id>`, the id `basename`d as the commit gate's is Round 1: the notice also fires for a `git commit` whose `tool_response` reports failure; the marker is spent there. |
| `the session`, no `Implementation` row, and an answer outside the vocabulary are all silent; so is a command that does not commit, including prose mentioning one | `hooks/implementer-notice.py#commits@c9de56e9`, `hooks/implementer-notice.py#main@b9dbc9af`, `hooks/routing.py#parse@294bd641`, `tests/test_the_implementer_is_recorded.py#test_the_other_answer_is_not_a_defect@74c9e56e`, `tests/test_the_implementer_is_recorded.py#test_a_declaration_without_the_row_says_nothing@27f97b56`, `tests/test_the_implementer_is_recorded.py#test_an_unreadable_answer_says_nothing_either@fdf3fba9`, `tests/test_the_implementer_is_recorded.py#test_a_command_that_does_not_commit_says_nothing@817ca64b` | **Executed** 2026-09-02; firing on anything but `the session` and dropping the `commits()` test each turn a test red | 2026-09-02 | `parse()` folds "no row" and "unreadable row" into one `None`, so the notice cannot tell them apart and does not try Round 1: `commits()` is deliberately blind to a commit under `eval`, `bash -c`, a heredoc body, or as the first statement of a loop or conditional body — missing one costs a reminder, never a decision, and the reserved-word case is the one `parse_git`'s rider records for both gates. |
| The commit gate's output is byte-identical with the row and without it, and the notice never carries a permission decision | `hooks/commit-review-gate.py#judge@6ca45c3c`, `tests/test_the_implementer_is_recorded.py#test_the_commit_gate_decides_identically_with_the_row_and_without@494cd2e3`, `tests/test_the_implementer_is_recorded.py#test_the_notice_never_carries_a_permission_decision@9af5cc7b` | **Executed** 2026-09-02; a `permissionDecision` printed ahead of the notice turns the second test red | 2026-09-02 | `judge` reads `routed` for its truth only; nothing in it opens `implementation` |

<!-- specs/1788326734-the-ledger-fragments-are-never-gathered -->
### 1788326734-the-ledger-fragments-are-never-gathered

Rows for the work item that gave the ledger fragments a release step
(issue #78): the fold into `seal/ledger.md`, and the guard that refuses it
while a reviewer-verified fact is still outside the ledger.

#### The fold is a move, marked and ordered

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Every fragment under `seal/ledger/` is read in work item id order, its text moved into `ledger.md` under `## X.Y.Z — <date>` and `### <id>`, and the file removed only after the ledger is on disk | `.github/scripts/fold_ledger.py#fragments@c8fe3dfd`, `.github/scripts/fold_ledger.py#section@1b72e077`, `.github/scripts/fold_ledger.py#append@e091419b`, `.github/scripts/fold_ledger.py#main@2e8fcb28` | **Executed** on a copy of this repository's tree, twice: before this work item's own fragment existed, six fragments and 55 table rows; with it, seven and 99 (round 1, the reviewer). Every row found byte-identical in the folded `map.md`, `.specseal/map/` gone; the earlier id sorts above the later one; a second run exits 1 with `nothing to fold`. Mutation: dropping the `os.remove` turns 10 cases red | 2026-09-02 | The section is appended, where the changelog gather inserts at the top — a ledger is read by area and its top holds the notation; nothing measures from the position (`questions.md` Q2) |
| A fragment's `# <id>` title is dropped, blank lines above it too, and every other heading outside a code fence moves down two levels; every other line is copied byte for byte — split on `\n` alone, trailing whitespace kept, a `#` line inside a ``` or `~~~` fence left as text, each fence closed by the next line starting with its own three characters — looser than CommonMark, which the rider at that line records | `.github/scripts/fold_ledger.py#demote@bfb7c668` | **Executed**: the fixture's `## The area this work item wrote` lands as `####`, the `# <id>` line is absent, the preamble survives, a row holding an escaped pipe arrives unchanged, a row holding U+2028 arrives as one row, the last row keeps its trailing tab, a `# not a heading` line inside a fence is not demoted, a title after a whitespace-only line yields one `### <id>` and not two, and a ``` line inside a `~~~` fence does not close it | 2026-09-05 | Round 1's 🟡 5, probes A–C: `strip()`, `splitlines()` and the fence were each a way the move was not byte for byte; round 2's 🟡 4 added the blank first line and the `~~~` form. Today's fragments hold none of the five shapes, so the real fold was 99/99 either way. **Re-read 2026-09-05 at the 0.8.1 release preparation**, which found the content under `demote` moved by one line: its rider stamp names `5a831e8` where it named `9edc59f`, moved by the same rewrite. The split is still on `\n` alone and nothing about the demotion changed, so the claim holds |
| A folded work item is marked with `<!-- specs/<id> -->` on a line of its own, and a fragment present while its marker stands in `ledger.md` is refused with nothing written; the marker quoted inline in prose is not a mark, and one standing alone inside a code fence is | `.github/scripts/fold_ledger.py#marker@2befe8df`, `.github/scripts/fold_ledger.py#is_marked@0fe8ff3a`, `.github/scripts/fold_ledger.py#folded@1ba269a5` | **Executed**: re-creating a folded fragment makes the fold exit 1 naming it, the ledger byte-identical and the fragment still there; re-wording a folded note leaves `--check` green; a real work item's marker quoted inline in the ledger's prose lets the fold run and exit 0 (round 2 executed eight shapes: inline and padded lines are not marks, a bare line inside a fence is, and fold and `--check` agree on each). Mutation: not writing the marker turns 18 cases red | 2026-09-02 | The same marker string `gather_changelog.py` writes into `CHANGELOG.md`, so one comment shape names a work item in both gathered files. Round 1's 🟡 3: the substring test refused, with advice that would have a person delete the only copy of the rows |
| The messages print `/`-joined paths on every platform; disk paths are built from them through one helper | `.github/scripts/fold_ledger.py#under@fae97017`, `.github/scripts/fold_ledger.py#LEDGER@3d869849`, `.github/scripts/fold_ledger.py#FRAGMENTS@dafda07f` | **Executed**: `--check` and the already-folded refusal print no backslash, and the fold on a copy of this tree still removes every fragment through `under()` | 2026-09-02 | Round 1's 🔴 1: `os.path.join` printed `.specseal\map` on Windows, where three assertions expected `/`, and `test.yml` runs the suite on `windows-latest`. The backslash test goes red only on the Windows leg, where `os.path.join` differs; on the other two it pins the `/` form (round 2, 🟡 3: with the constants reverted it passes on macOS) |
| The checker de-duplicates on `(coordinate, hash)` within one file, so a fold moves its total by exactly the rows two files cited identically | `skills/evidence-check/scripts/evidence_check.py#check_ledger@71818d3e` | **Executed** on copies of this tree: 141 ok → 140 ok before this fragment existed, 155 → 154 with it (round 1, the reviewer); 0 drifted, 0 broken either time. The pair is `skills/code-review/scripts/chain_check.py#main`, cited by `map.md` and the `1788272986-…` fragment | 2026-09-02 | `seen` is a local of one `check_ledger` call. A release reading a smaller total after the fold should look here before looking for a lost row (`spec.md` S6) |
| An empty fragment gets no section and no marker, and is removed and named | `.github/scripts/fold_ledger.py#section@1b72e077` | **Executed**: a fragment holding only its title prints `(empty, removed)` and leaves no `1788300000-empty` in the ledger | 2026-09-02 | A marker with nothing under it would make `--check` say the rows arrived when there were none |
| `--check` counts markers on a line of their own, so the ledger's header quoting the marker's shape is not a work item | `.github/scripts/fold_ledger.py#MARKER_LINE_RE@95d471a3`, `.github/scripts/fold_ledger.py#main@2e8fcb28` | **Executed**: on the folded copy, a substring count read 7 where six fragments had been folded; the line-anchored count reads 6 | 2026-09-02 | Found by the first real run, not by the fixture, whose ledger header does not quote the marker |

#### The guard

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A line outside a table whose first word is `drained` closes the whole file; otherwise every table body row is open unless its first cell begins with ✅; a header and its separator are not rows | `.github/scripts/fold_ledger.py#open_rows@9bec3304` | **Executed**: `drained` above the table, below it, and in bold all pass; every row ✅, a header with no body row, no table, and no file all pass; `not drained yet`, one ✅ beside one bare row, and `drained` inside a table cell all refuse. Mutation: removing the `drained` rule turns 24 cases red, removing the ✅ rule turns 1 red | 2026-09-02 | The four-step rule in `spec.md` §"The rule for an open evidence-todo row", written so a person can apply it by hand; all three files in this tree, this work item's included, are closed by the `drained` line |
| Lines are split on `\n` alone, so a cell holding U+2028, U+0085 or a form feed before the word `drained` does not close the file | `.github/scripts/fold_ledger.py#open_rows@9bec3304` | **Executed**: a row whose cell carries U+2028 followed by `drained` reads as one open row and refuses the fold | 2026-09-02 | Round 1's 🟡 4, probe E: `splitlines()` read the cell's tail as a line of its own, which closed the file — the silent direction for a guard |
| Every `seal/specs/*/evidence-todo.md` in the tree is read, and one open row refuses the fold before anything is written or removed, naming the file and the count | `.github/scripts/fold_ledger.py#open_items@b2ff3354`, `.github/scripts/fold_ledger.py#report_open@6c7b8ee7`, `.github/scripts/fold_ledger.py#main@2e8fcb28` | **Executed**: an open row in a work item with no fragment left (`1600000000-released-long-ago`) refuses the fold; the ledger is byte-identical and every fragment still there. Mutation: skipping the guard at the fold turns 5 cases red | 2026-09-02 | "Released" and "present" are the same set on a branch cut from the release branch (`questions.md` Q1) |
| The release pull request runs `--check`, which fails for a fragment left behind or an open row and otherwise says how many work items it saw marked | `.github/workflows/hygiene.yml#"echo \"base is ${{ github.base_ref }} — fragments are folded when the release reaches main\"; exit 0"@31c3f233`, `docs/branch-and-release.md#"**The fold refuses while a verified fact has not reached the ledger.** A"@44b10fe7`, `CONTRIBUTING.md#"The fold refuses, naming the file, while any `seal/specs/<id>/evidence-todo.md`"@0e773e27` | **Executed** for the script: `--check` exits 1 naming each fragment before the fold and an open file after it, exits 0 with `6 work items marked` on the folded copy of this repository (`7` once this fragment existed, round 1). **Read** for the workflow: the step exits 0 with a message on a pull request whose base is not `main` and runs `fold_ledger.py --check` only when it is, the same shape as the changelog step above it | 2026-09-02 | No release pull request has run the step yet; `overview.md` §Not verified names the owner |

<!-- specs/1788331011-two-roots-hold-three-lifetimes -->
### 1788331011-two-roots-hold-three-lifetimes

<!-- This work item's ledger rows: one file per work item so that two branches
never queue at seal/ledger.md, folded into it at the release. Phase 1's rows first;
phases 2 and 3 append their own. Every row below records something executed
unless it says Read. -->

#### The opt-in is the root

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S2 · `home()` returns `<repo>/seal/` first and `<git-common-dir>/seal/` second, and nothing else is read | `hooks/optin.py#home_at@702af479`, `hooks/optin.py#git_common_dir@bf3e8dd5` | **Executed**: `tests/test_optin_home.py`, 31 passed — both places in order, the shared root winning when both exist, a linked worktree's common directory found through git, a main worktree's without a process | 2026-09-03 | Q3 (a). The fast path answers `<root>/.git` when it is a directory; only a `.git` FILE or none asks `git rev-parse --git-common-dir`, so an opted-in repository pays no extra process per gated command |
| S3 · `.specseal/` opts nothing in | `hooks/optin.py#HOME@a6ab9232` | **Executed**: the commit gate and the review-skill gate are silent over a fixture holding `.specseal/map.md` and no `seal/` (`tests/test_optin_home.py`), and `hooks/root-migrate.py` is the one hook that still spells the old ledger's name (`tests/test_the_root_migrates_itself.py::test_the_hook_reads_the_old_names_and_nothing_else_does`) | 2026-09-02 | |
| S4 · the opt-out is the FILE `<git-common-dir>/specseal-scratch`; a directory of that name is not it, and `.specseal/scratch` is read by nothing | `hooks/optin.py#SCRATCH@c26a5fef`, `hooks/optin.py#home_at@702af479` | **Executed**: the file makes `home()` `""` and both gates silent; a directory leaves the repository opted in and the gate firing; the old file beside a `seal/` leaves it opted in; the marker reaches a linked worktree through the shared common directory | 2026-09-03 | Q4 (a). `test_this_repository_carries_no_scratch_marker` removed from `tests/test_release_hygiene.py`, because a file under `.git/` cannot be committed |

#### The readers of the root

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S11 · the commit gate reads `seal/specs/<id>/routing.md` and its prompt names that path; a commit confined to `docs/` and `seal/` leaves the parity arm asleep | `hooks/routing.py#declarations@ce8e578c`, `hooks/routing.py#WORK_ITEMS@0e4ea8b9`, `hooks/commit-review-gate.py#DOC_ROOTS@a8a0f353` | **Executed**: `tests/test_routing_is_recorded.py`, `tests/test_waiver_decided_at_start.py` and `tests/test_gate_judges_the_repo_it_commits_to.py` green, in the 358-case slice run after the code commits and again in the 245-case run after the move | 2026-09-02 | `declarations()` joins `optin.WORK_ITEMS` under `optin.home_at(root)`, so a root under the git directory (#80) is read by the same code; `routing.WORK_ITEMS` is the `/`-joined spelling `chain_check.py` lists a tree under. Two cases that pinned *writing a declaration creates no home* now pin the opposite, because the whole work item lives under the root and the root is the opt-in |
| S13 · the `<!-- specs/<id> -->` markers keep their text | `.github/scripts/gather_changelog.py#marker@b1e2b91c`, `.github/scripts/fold_ledger.py#marker@2befe8df`, `.github/scripts/fold_ledger.py#MARKER_LINE_RE@95d471a3` | **Executed** on this branch after the move: `gather_changelog.py --check` exits 1 naming only the two unreleased fragments (`seal/specs/1788326734-…/changelog.md` and this work item's), every released entry still found by its marker; `fold_ledger.py --check` exits 1 naming the seven unfolded `seal/ledger/*.md` and no open evidence-todo row | 2026-09-02 | Q2 (a) |
| S10 · a declaration the pull request only renamed is not one it made; the set judged is what it added or edited plus the one for this branch | `skills/code-review/scripts/chain_check.py#changed@09879809`, `skills/code-review/scripts/chain_check.py#changed_routing@cb23b76f` | **Executed** on this branch against `origin/release/v0.4.0` after the move: exactly one declaration judged, this work item's, failing for `rounds/ holds no round-N.md` because no round has run yet; none of the fourteen renamed declarations named. Three new cases in `tests/test_chain_check_at_the_pull_request.py`: an exact rename not judged, the branch's own still judged after the move, a rename carrying an edit judged | 2026-09-02 | Exact renames only (`R100`). `changed()` reads `--name-status -M` and returns the renamed set beside the paths, so the round records' membership test is unchanged. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which gave `changed` a `WORKTREE` branch: the diff from the merge base plus untracked files. Both branches read `--name-status -M` and return the renamed set beside the paths, so the exact-rename reading this row is about is the same under either |

#### The migration hook

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S5 · a clean old layout moves once, every move a staged `git mv`, one line printed, a marker of its own stamped | `hooks/root-migrate.py#main@66b39ece`, `hooks/root-migrate.py#moves@e5a5ac7e`, `hooks/root-migrate.py#run_moves@19c082e2`, `hooks/dispatch.py#GROUPS@b3d45306` | **Executed**, twice: `tests/test_the_root_migrates_itself.py` (22 passed) on a fixture, and the hook itself over this repository — `moved .specseal/ and 15 work items into seal/ (1 ledger row re-pointed) — review the diff and commit`, 127 staged `R100` renames, nothing unstaged, `~/.claude/specseal/root-migrated` carrying this root | 2026-09-02 | The move landed in `e2a0dfe`, whose subject is the orchestrator's `docs/flow.md` edit: that commit swept the staged move in, because both sessions share this checkout. The hook runs before `ledger-migrate.py` in the session-start group, and the test pins the order |
| S6 · a dirty tree is refused with the spec's line, and a git that cannot answer reads as dirty | `hooks/root-migrate.py#dirty@b14ba675` | **Executed**: an unstaged edit, a staged edit and an untracked file under the old roots each refuse and stamp nothing; a raising `subprocess.run` reads as dirty; the next clean start moves | 2026-09-02 | A staged rename or deletion with a clean worktree is NOT dirty: that is what a stopped run of this hook leaves, and a resume has to see past its own earlier steps or a stopped move can never finish |
| S7 · a stopped move resumes, and stamps only when nothing old is left | `hooks/root-migrate.py#move@56fa3ab2`, `hooks/root-migrate.py#run_moves@19c082e2` | **Executed**: a half-move by hand (`seal/ledger.md` committed, the rest not) is finished; a `git_mv` failing on the third unit prints `moved 2 of 5 into seal/ and stopped at … The next session start continues`, stamps nothing, and the next start finishes and stamps; a directory already at its destination is merged file by file rather than moved inside it | 2026-09-02 | |
| S8 · rows citing a moved file follow it with their hashes untouched, and the checker's totals are the same before and after | `hooks/root-migrate.py#repoint@b06e288e` | **Executed** on the fixture: three rows re-pointed, the hash set identical, `evidence_check.py` totals equal on both sides (`0 drifted · 0 broken`). On this repository: `160 ok · 0 drifted · 0 broken` before the move at `b3eb50c`; `148 ok · 12 drifted · 0 broken` after it, every drifted anchor a unit this phase's code commits edited and none under a moved path; `160 ok · 0 drifted · 0 broken` again after `--reverify` and a re-read of the twelve (`a4206b0`) | 2026-09-02 | The one row under a moved path (Q7, in `seal/ledger.md`) is `ok` on both sides; its Notes cell names `settle` (#83) |
| S9 · a repository carrying `.specseal/scratch` is not migrated | `hooks/root-migrate.py#main@66b39ece` | **Executed**: the throwaway line prints, nothing moves, no stamp | 2026-09-02 | |
| A repository with nothing old is silent, and one that has `seal/` is stamped so an old branch checked out later is not moved again | `hooks/root-migrate.py#main@66b39ece` | **Executed**: a fresh repository prints nothing and is not stamped; after the move and its commit, `git switch -c old HEAD~1` brings `.specseal/` back and the hook says nothing | 2026-09-02 | The spec table's row *nothing old left · stamped, if not already* is applied to a repository that has the new root; one with neither layout is not this hook's business, and stamping it would list every repository on the machine |

#### This repository

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · `seal/` holds the tree, history follows every moved file, and neither old root exists | `tests/test_release_hygiene.py#test_this_repository_has_one_root_laid_out_by_lifetime@bb46ecc8` | **Executed**: the test green; `git log --follow --oneline -- seal/ledger.md` reaches `2f51b8c`, `32e5c84`, `56f01ae` and beyond, and the same on this work item's `routing.md` reaches `e95474a`, read by hand | 2026-09-02 | `seal/README.md` is the one file `--follow` does not cross: it was rewritten whole from `templates/seal-README.md` (Q5), and git pairs no rename at that similarity |

#### Phase 2 — everything re-pointed by script

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S15 · no document, skill, agent, template or workflow names `.specseal/` or a bare `specs/<…>` as a live path; each line that keeps the old name is listed with a reason, and a reason no line carries fails too | `tests/test_no_document_names_the_old_roots.py#test_no_shipped_document_names_the_old_roots@085c430d`, `tests/test_no_document_names_the_old_roots.py#KEEP@af9dc8b0`, `tests/test_no_document_names_the_old_roots.py#test_every_keep_entry_is_still_in_use@d37c9161` | **Executed**: 11 passed over the tracked `.md`, `.yml` and `.sh` files under the workflows, templates, skills, agents, docs, the root documents and `seal/README.md`; the matcher flags the four old shapes and passes the four new ones. Run between the two script passes it failed on the five SDD template headers the first pass had skipped, which is how the miss was found | 2026-09-02 | `README.md` and `README.ko.md` join `SCANNED` in phase 3, when they move together. `.py` files are outside the scan; the plan's grep over `hooks/` and `skills/` was read by hand and its remaining hits are `root-migrate.py` (on purpose), `optin.py` (history and one Windows example path), and history lines in `chain_check.py` and `evidence_check.py` |
| S12 · the workflow hands `unverified_check.py --baseline` the work items' new home | `.github/workflows/hygiene.yml#"--baseline \"origin/${{ github.base_ref }}\" seal/specs/"@f2bbe0a4` | **Executed** with the same argument against `c8dc0cf`: `13 overviews · 29 open · 12 closed · 0 unreadable`, exit 0. With the old argument this pull request reads as every overview deleted, because the base carries them under `specs/` and HEAD carries none there | 2026-09-02 | The step itself runs only in GitHub Actions; the pull request is where it is seen |
| S14 · the once-per-repo moment creates `seal/`, says in three lines what it created and that the root's presence is the opt-in, asks the mode question — shared or local, since #80 — and then the parity question, and nothing else | `skills/implement/SKILL.md#"## Document layout — two roots, three lifetimes / ### Bootstrap — create what's missing"@5fd3b434` | **Read**: the section after the rewrite. **Executed**: `tests/test_the_set_a_work_item_always_has.py` and the bootstrap assertion in `tests/test_chain_hooks_hardening.py` (the skill names `seal/README.md`) green in the 167-case slice | 2026-09-03 | Q6 (a). The mode question was named as arriving with local mode and arrived with #80; re-read 2026-09-02 against the rewritten section  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). What this claim asserts is untouched by that bullet, so only the section hash moved |
| S13 · the documents that describe the fold keep the marker text `<!-- specs/<work-item-id> -->`, and the S15 allowlist carries it under Q2 | `docs/branch-and-release.md#"one `###` section per work item marked with `<!-- specs/<work-item-id> -->`,"@720db6e2` | **Executed**: `tests/test_the_ledger_fragments_fold_at_release.py` green after the template rename it opens by name, and `test_every_keep_entry_is_still_in_use` finds the marker line | 2026-09-02 | |
| the export rules in `seal/README.md` name their destinations from the repository root, and the file is `templates/seal-README.md` byte for byte | `templates/seal-README.md#"## Export rules — drain before closing"@d7e76996` | **Executed**: `diff templates/seal-README.md seal/README.md` empty after the script copied the template over; `tests/test_the_set_a_work_item_always_has.py::test_the_two_layout_trees_no_longer_show_it` green | 2026-09-02 | The tree line `└── specs/<work-item-id>/` stays relative to `seal/`, and is allowlisted as such |
| the review-chain specification's two opt-in headings and the parity arm's silence row name `seal/` | `docs/review-chain-spec.md#"### Review arm — opt-in: `seal/` at the repo root"@da008fd8`, `docs/review-chain-spec.md#"### Parity arm — opt-in: `seal/parity.md` at the repo root"@4b9d8901` | **Read** after the rewrite. **Executed**: `tests/test_waiver_decided_at_start.py` and `tests/test_routing_is_recorded.py` green; they pin the declaration's path by substring, which `seal/specs/<work-item-id>/routing.md` still contains | 2026-09-05 | Phase 3 tightens those assertions to the full path. Re-read 2026-09-04 twice under work item 1788501054, which added four `#####` subsections inside this `###` region: both headings still name `seal/` and the parity arm's silence row is untouched, so the claim holds. **Re-read a third time 2026-09-04 in that item's round 3 fix pass**, which added a table row and three paragraphs to one of those subsections: what they say is which cells the pending fix-surface refusal does not reach, both headings are still the two this row names, and the parity arm's silence row is still untouched. The hash was set by hand from a one-row scratch ledger rather than by a `--reverify` over this file, because that run also re-stamps S8, whose claim is false. **Re-read 2026-09-05 in work item 1788501054's round 7 fix pass**, which added one paragraph inside the anchored region's floor subsection — the count's second stop — and touched neither opt-in heading nor the parity arm's silence row; `seal/` is named where it was. The hash was set from a one-row scratch ledger again, for the same reason. **Re-read 2026-09-05 in that item's round 9 fix pass**: one row of the floor subsection's exits table moved — two quiet records now fail only when none closed on a fix — and neither opt-in heading nor the parity arm's silence row moved; `seal/` is named where it was. Scratch-stamped a third time. **Re-read 2026-09-05 in round 10's fix pass**: the exits-table row moved once more, to stop stating the count with a *none of them* quantifier that was false whenever the stopping record was counted; the opt-in headings and the parity arm's row are still where they were. Scratch-stamped a fourth time. **Re-read 2026-09-05 in round 11's fix pass**: the exits-table row's trailing clause moved, to stop blaming the allowed verifying round; the opt-in headings and the parity arm's row are still where they were. Scratch-stamped a fifth time. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which grew the `###` region by the `--worktree` paragraph, the subsection *The reopening — one, and then the run is capped*, the `deferred <home>` exits-table row, and the generator and moratorium paragraphs under *What the record carries*. Both opt-in headings still name `seal/` and the parity arm's silence row is untouched. Scratch-stamped a sixth time, from a scratch ledger holding this pass's drifted rows alone. **Re-read 2026-09-05 at the 0.8.1 release preparation**, after work item 1788597030's round 2 fix pass rewrote the generator paragraph of the *What the record carries* subsection inside this `###` region — the record now carries every fenced block under the probes table. Both opt-in headings and the parity arm's silence row are untouched, so the claim holds |

#### Phase 3 — the fixtures and the READMEs

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S15 · `README.md` and `README.ko.md` name `seal/` as the live root, move together, and join the scan; their *coming up from 0.3.x* section is where a person is told how the move happens and how to make it by hand | `README.md#"### Coming up from 0.3.x"@6a34a62d`, `README.ko.md#"### 0.3.x 에서 올라오는 경우"@e583911d`, `tests/test_no_document_names_the_old_roots.py#SCANNED@b7192cf7`, `tests/test_no_document_names_the_old_roots.py#KEEP@af9dc8b0` | **Executed**: both editions in one commit (`git show --stat` read: two files, nothing else); `tests/test_no_document_names_the_old_roots.py` green with both READMEs scanned, and `test_every_keep_entry_is_still_in_use` finds the four lines added to `KEEP`; `tests/test_docs_line_wrap.py` green over both. The by-hand sequence was run verbatim on a fixture in the old layout: five `R100` renames staged, `evidence_check.py .` names the one row citing a moved file as BROKEN with the new path in its hint, and `git mv` leaves `.specseal/` and `specs/` empty on disk — which is why the block ends with `rmdir` and, since round 1, `evidence-check --reverify .` | 2026-09-02 | The section names no version but `0.3.x`, because `test_no_loaded_file_hardcodes_the_running_version` reads both READMEs and the running version becomes `0.4.0` at the release. The section and `hooks/root-migrate.py` share a lifetime: whoever deletes the hook deletes the section |
| S11 · every test fixture that opts a repository in creates `seal/`, writes `seal/parity.md`, `seal/ledger.md` and `seal/specs/<id>/routing.md`, and the tests that read this repository's own tree read `seal/specs/` | `tests/conftest.py#declare_routing@ae0c1556`, `tests/test_gate_judges_the_repo_it_commits_to.py#make_repo@accf3e80`, `tests/test_a_path_the_command_wrote_out.py#make_repo@1c06faf3` | **Executed**: the 31 test files that named the old roots at `07a0fc8`, plus `tests/test_the_root_migrates_itself.py` and the S15 test, run as one command: 947 passed | 2026-09-02 | The substitution itself (58 across 22 files, then 7 across 5) is the commit before this phase's READMEs; its own run was 559 passed over the 22 files, and the 947 above is the phase's verification of the same fixtures |
| the assertions that pinned the declaration's path by substring name the full path | `tests/test_waiver_decided_at_start.py#test_the_skill_names_where_the_answer_is_written@66f52d49`, `tests/test_waiver_decided_at_start.py#test_the_smith_carries_both_halves_rather_than_only_citing_them@bc692b74`, `tests/test_waiver_decided_at_start.py#test_the_preset_block_carries_it_too@f281e160`, `tests/test_routing_is_recorded.py#test_the_first_prompt_names_the_declaration_as_a_way_out@5c0f1bf0`, `tests/test_routing_is_recorded.py#test_the_second_prompt_names_it_too@35072029` | **Executed**: green in the 947-case run at 3aab0b99; after 46c658d the five gate-and-path test files re-run, 195 passed (macOS), and `ntpath.join` of the four parts shows the old literal absent and the new expression present; the three waiver cases assert `seal/specs/<work-item-id>/routing.md` and the two gate-prompt cases `os.path.join(...)`, which `specs/<work-item-id>/routing.md` no longer satisfies | 2026-09-02 | Closes the phase 2 note that these passed by substring. The two gate-prompt cases assert the path as `os.path.join("seal", "specs", "<work-item-id>", "routing.md")`, because `declaration_hint` joins it the way the platform spells it (#80) — PR #95's windows leg, run 33643327406, failed on the `/` literal in the second one |
| S6 and the refusal table say what the hook does: a staged rename or deletion with a clean worktree column is not dirty, the stamp when nothing old is left needs `seal/` to exist, the moved line drops what there was none of and names a foreign entry under `specs/`, and a template that cannot be read leaves the moved README as it was | `hooks/root-migrate.py#dirty@b14ba675`, `hooks/root-migrate.py#main@66b39ece`, `hooks/root-migrate.py#rewrite_readme@ef8bb911`, `hooks/root-migrate.py#old_items@ee6922ab` | **Read**: the four functions against the spec's S6 row, the six-row table and step 3, and the spec rewritten to them. **Executed**: `tests/test_the_root_migrates_itself.py` green in the 947-case run, one case per row of the table | 2026-09-02 | The code is kept in each case because it is the design's intent applied to a state the spec had not considered — a resume reading its own earlier steps, a repository with neither layout — and `questions.md` Q8 is where the one judgment call among them already sits |

#### What round 1 settled

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| 🔴 1 · the units are what git tracks: an ignored file under the old roots is not the hook's to move and stays where it is, `.specseal/` may remain on disk holding nothing else, and when git cannot say the directory listing stands in so that `dirty()` refuses | `hooks/root-migrate.py#tracked_names@8502455c`, `hooks/root-migrate.py#entries@0547e4ef`, `hooks/root-migrate.py#moves@e5a5ac7e`, `hooks/root-migrate.py#old_items@ee6922ab` | **Executed**: `test_an_ignored_file_directly_under_the_old_root_does_not_stop_the_move` seen red at 4516166 with the reviewer's line (`stopped at .specseal/.DS_Store: fatal: not under version control`), green at f2c9570; the second start says nothing and the marker stands | 2026-09-02 | The printed line's parenthetical for a foreign entry reads *not tracked as a SpecSeal work item* now, since what stays is decided by git and the pattern together |
| 🟡 2 · `seal` present as a file prints a line and stamps nothing instead of raising out of `main()`; a re-point that fails after every unit has moved prints the `evidence-check --reverify .` line and stamps nothing | `hooks/root-migrate.py#git_mv@d7052e63`, `hooks/root-migrate.py#main@66b39ece`, `hooks/root-migrate.py#MoveError@f5f84a96` | **Executed**: `test_a_file_named_seal_stops_with_a_line_not_an_exception` red at 4516166 (`FileExistsError` out of `main()`), and `test_a_repoint_that_fails_after_the_moves_says_so_and_stamps_nothing` red (`OSError` out of `main()`); both green at f2c9570 | 2026-09-02 | `MoveError` gained `resumable`; the two shapes a person has to settle end with *Settle that by hand, then the next session start continues* |
| 🟡 3 · a destination that already holds the file is named with the two ways to keep one, and the line does not promise a continuation | `hooks/root-migrate.py#taken@0b407a26`, `hooks/root-migrate.py#move@56fa3ab2` | **Executed**: `test_a_destination_already_holding_the_file_is_named_and_left_to_the_person` red at 4516166 with `destination exists … The next session start continues.`, green at f2c9570; the file-by-file resume path raises the same error per file | 2026-09-02 | |
| 🟡 4 · a row citing an entry under `specs/` that is not a work item stays with it, and the checker's totals are equal before and after | `hooks/root-migrate.py#repoint_path@3f1714a2`, `hooks/root-migrate.py#ITEM_RE@6a959f91` | **Executed**: `test_a_row_citing_a_foreign_specs_entry_is_left_where_it_is` red at 4516166 (the row went to `seal/specs/notes/…`, `4 ok · 1 broken`), green at f2c9570 | 2026-09-02 | Spec step 6 says *`specs/<id>/` where `<id>` is a work item* |
| `chain_check.py --baseline origin/release/v0.4.0` judges one declaration, this work item's, and names none of the fourteen renamed ones | `skills/code-review/scripts/chain_check.py#changed@09879809`, `skills/code-review/scripts/chain_check.py#changed_routing@cb23b76f` | **Executed** at f2c9570 (round 1 ran it at 4516166 with the same result): one declaration judged, failing only for round-1's unchecked `Pass` and its `nobody` cell, which round 2 sets | 2026-09-02 | Drained from `evidence-todo.md`. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which gave `changed` a `WORKTREE` branch for a local check before the record's commit; CI keeps the default, so what a pull request judges is the `HEAD` diff this row measured |
| `hooks/dispatch.py session-start` delivers the hook's `systemMessage` and writes the marker; the dispatch path is what a real session runs | `hooks/dispatch.py#GROUPS@b3d45306`, `hooks/root-migrate.py#main@66b39ece` | **Executed** at f2c9570 on an old-layout fixture with `HOME` redirected (round 1's P7 re-run): `moved .specseal/ and 1 work item into seal/ (1 ledger row re-pointed) — review the diff and commit` on stdout, the root in `root-migrated`, `.specseal/` and `specs/` gone, five entries staged | 2026-09-02 | Drained from `evidence-todo.md`; closes the dispatch half of the memo's Not verified row. A real repository other than this one is still the owner's |
| the README's by-hand sequence and the hook leave the same `git ls-files` set, and `--reverify` closes the rows the hand sequence leaves broken | `README.md#"### Coming up from 0.3.x"@6a34a62d`, `README.ko.md#"### 0.3.x 에서 올라오는 경우"@e583911d`, `hooks/root-migrate.py#moves@e5a5ac7e` | **Executed**: `test_the_readmes_by_hand_sequence_yields_the_hooks_tracked_set` (both editions) reads the block out of the document and runs it on a copy of the fixture — red at 4516166 (three broken rows after `evidence-check .`), green at f2c9570 with `0 broken` | 2026-09-02 | Drained from `evidence-todo.md` (P6); the block also carries the `[no-review]` waiver for the staged commit (🟡 5) |
| the checker and the unverified reader at the close of the fix pass | `skills/evidence-check/scripts/evidence_check.py#main@8c95a52c`, `skills/verify/scripts/unverified_check.py#main@86d5e3b9` | **Executed** at f2c9570: `evidence_check.py --strict .` 217 ok · 0 drifted · 0 broken · 0 external · 0 old-format; `unverified_check.py seal/specs/` 13 overviews · 29 open · 13 closed · 0 unreadable | 2026-09-04 | Drained from `evidence-todo.md`; round 1 read 208 ok · 0 drifted · 0 broken and 13 · 29 · 12 · 0 at 4516166. Re-read 2026-09-04 after work item 1788501054 rewrote both `main` functions: the claim names the two entry points a fix pass closes on and both are still those, so it holds. **The numbers in the cell are the reading at f2c9570 and are not current** — the same tree now reads 517 ok · 1 drifted · 0 broken, the drift being S8 |

#### What round 2 settled

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| 🟡 A · a symbolic-link `.specseal/` is refused before anything else, at every session start, with a line naming the README's by-hand section; nothing moves and nothing is stamped | `hooks/root-migrate.py#main@66b39ece` | **Executed**: `test_a_symlinked_specseal_is_refused_not_half_moved` red at a99fd65 with round 2's symptom (`moved 1 work item into seal/ (0 ledger rows re-pointed)`, stamped, `seal/ledger.md` never written), green at 9f400d0; two starts in a row print the line and stamp nothing | 2026-09-02 | Round 1's Deferred read this as failing like 🔴 1; it did not fail, it half-moved under a stamp. The refusal table has the row |
| 🟢 B · `dirty()` answers true when `git ls-files` cannot, so the directory listing `entries()` falls back to never moves anything | `hooks/root-migrate.py#dirty@b14ba675`, `hooks/root-migrate.py#entries@0547e4ef`, `hooks/root-migrate.py#tracked_names@8502455c` | **Executed**: `test_when_git_ls_files_cannot_answer_the_move_is_refused_as_dirty` (only `ls-files` patched to raise) red at a99fd65 — the move went ahead from the listing — green at 9f400d0 with the dirty line, nothing moved, no stamp | 2026-09-02 | Spec step 4's last sentence and the 🔴 1 row above are true now; before this they described an intention |
| 🟢 C · a tracked FILE with a work item's name under `specs/` is not a work item: it stays and the printed line names it as left where it is | `hooks/root-migrate.py#old_items@ee6922ab`, `hooks/root-migrate.py#ITEM_RE@6a959f91` | **Executed**: `test_an_item_shaped_tracked_file_under_specs_stays_and_is_named` red at a99fd65 (`moved .specseal/ and 2 work items`), green at 9f400d0: one work item moved, `left specs/1788000001-a-file.md` in the line, the file where it was | 2026-09-02 | §"Which entries of `specs/` are SpecSeal's" says a directory, and now says a file of that shape stays and is named |
| 🟢 D · the `cannot create` line names the parent that could not be made | `hooks/root-migrate.py#git_mv@d7052e63` | **Executed**: `test_a_file_named_seal_stops_with_a_line_not_an_exception` asserts `cannot create seal/:` for the top-level case. **Executed** by round 3 for the nested parent: `.specseal/map/` with `seal/ledger` a committed file says `cannot create seal/ledger/: [Errno 17] …`, non-resumable, not stamped; at 207b432 the same state said `cannot create seal/:` | 2026-09-02 | The `seal`-as-file test passes on both sides, so the nested probe is the discriminator |
| 🟢 E · `by_hand_block` is bounded at the next heading, asserts the block is in its section, and names the Korean edition by its full heading | `tests/test_the_root_migrates_itself.py#by_hand_block@7df09672`, `tests/test_the_root_migrates_itself.py#HEADINGS@353b1f0d` | **Executed**: `test_by_hand_block_fails_loudly_when_the_block_leaves_its_section` — a copy of each README with the block moved under a later heading raises the assertion, the real README still yields its lines. Its red state is round 2's probe (`IndexError` on an edited copy), not re-run against the old helper | 2026-09-02 | The lines run under `bash -c`, which is why a lost block had to fail rather than fall through to a later block |
| 🟢 F · a `specs/<id>` that is a relative symbolic link is moved as the link and dangles at the new depth; its rows are re-pointed and the marker is stamped | `hooks/root-migrate.py#move@56fa3ab2` | **Executed** on an old-layout fixture at 9f400d0: `specs/1788000000-item → ../vault-item` moved to `seal/specs/`, target unreachable there, `moved .specseal/ and 1 work item into seal/ (1 ledger row re-pointed)`, checker `2 ok · 1 broken`, stamped | 2026-09-02 | Git's own behaviour, the same by hand; recorded, not fixed. The by-hand reader meets it in the checker's BROKEN line |

#### What round 3 settled

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| 🟡 H · a `specs/` that is a symbolic link holding work items is refused after the unit and marker checks, at every session start, with a line naming the README's by-hand section; nothing moves, no row is re-pointed, nothing is stamped | `hooks/root-migrate.py#main@66b39ece` | **Executed**: `test_a_symlinked_specs_holding_work_items_is_refused_not_half_moved` red at e19c9ee with round 3's symptom (`moved .specseal/ into seal/ (3 ledger rows re-pointed; left specs/1788000000-a-work-item, specs/notes where it is …)`, stamped, the rows pointing at a `seal/specs/<id>/` that never appeared), green at bb6c5e3; two starts in a row print the line, `seal/` absent, `.specseal/map.md` byte-identical, nothing stamped | 2026-09-02 | 🟡 A on the other root. A linked `specs/` holding no item-shaped directory is not refused, because the name is not SpecSeal's then |
| 🟢 I · the `.specseal/` link line also prints on a migrated repository that kept `.specseal → seal` as a link for old tooling | `hooks/root-migrate.py#main@66b39ece`, `seal/specs/1788331011-two-roots-hold-three-lifetimes/spec.md#"### What the session prints, and what it refuses"@fc949be5` | **Read**: the link test sits before the unit and marker checks, so a link is refused whatever is behind it; the spec chose "tested first, at every session start until the link is gone", and its row now says the line is not a half-move there | 2026-09-02 | Recorded, not fixed: nothing moves and nothing is stamped, so the cost is one line per session start until the link goes |

#### What CI settled

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| the by-hand test runs the README's `bash` block under `bash -c`, and skips on an executed precondition where `bash` on PATH is not a shell; the hook itself is not what skips | `tests/test_the_root_migrates_itself.py#test_the_readmes_by_hand_sequence_yields_the_hooks_tracked_set@dce22c33`, `tests/conftest.py#shell_probe@498bd763` | **Executed**: PR #90's windows leg (`https://github.com/MichaelYcJo/SpecSeal/actions/runs/33628905815`) failed both editions of the test at `tracked(hand) == tracked(repo)` with the hand copy untouched — `shell=True` is `cmd.exe` there — and passed every other migration test; both editions green here under `bash -c` on macOS, and `shell_probe("bash")` is the precondition `test_what_the_reader_understands.py` already executes before its own bash spawns | 2026-09-02 | The skip is bash-only coverage lost on a runner whose `bash` is the WSL stub; Windows keeps the hook's own tests, which are what a Windows repository runs |

## 0.5.0 — 2026-09-03

<!-- specs/1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in -->
### 1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in

<!-- This work item's ledger rows: one file per work item so that two branches
never queue at seal/ledger.md, folded into it at the release. Phase 3 wrote
every row from the executed facts of phases 1 and 2, and re-ran in a scratch
fixture the ones a fixture can re-run. Every row records something executed
unless it says Read. -->

###### The root at either place

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · `home()` reads `<repo>/seal/` and then `<git-common-dir>/seal/`, and nothing else: no config key, no marker, no environment variable | `hooks/optin.py#home_at@702af479`, `tests/test_optin_home.py#test_the_mode_is_read_from_the_folder_and_from_no_key@7a19d7ef` | **Executed**: the case reads the module's source for `config`, `environ` and `getenv` and finds none, then sets `git config specseal.mode local` and `SPECSEAL_MODE=local` on a repository with `seal/` at neither place, which stays out; `tests/test_optin_home.py` 38 passed at `8d05b00` | 2026-09-02 | The read side shipped in 0.4.0 (`seal/ledger.md` §1788331011, S2). This row pins what is NOT read |
| S2 · a linked worktree resolves to the main tree's common `seal/`, whichever shape git's answer has | `hooks/optin.py#git_common_dir@bf3e8dd5`, `tests/test_optin_home.py#test_a_linked_worktree_resolves_to_the_main_trees_common_root@96c818bb`, `tests/test_optin_home.py#test_gits_common_dir_answer_is_joined_whichever_shape_it_has@530122cd`, `tests/test_optin_home.py#test_a_file_named_seal_under_the_git_directory_is_not_a_root@f8c65710` | **Executed**: `git worktree add` under `subprocess`, `local_home(main)`, then `os.path.samefile(optin.home(other), main/.git/seal)`; a relative and an absolute `--git-common-dir` answer in forward slashes both land; a `seal` FILE under the git directory is not a root; the worktree's own `<tree>/seal/` wins over the common one | 2026-09-02 | `.git` is a FILE in the worktree, so the path is asked of git rather than spelled `.git/seal` |
| S3 · nothing under the local root is a commit candidate, and no `.gitignore` line exists for it | `tests/test_optin_home.py#test_the_local_root_is_never_a_commit_candidate_and_needs_no_gitignore@2252acaf` | **Executed**: a declaration, a round record and a ledger fragment under `<common>/seal/`; `git status --porcelain` is empty, `git add -A` stages nothing, no `.gitignore` exists. Re-run by phase 3 in a scratch fixture with a ledger under `.git/seal/ledger/`: `git status --porcelain` printed 0 lines | 2026-09-02 | Git never lists its own directory |
| the local-mode fixture: `local_home(repo)` creates `<common>/seal/` and works from a linked worktree; `declare_routing(…, home=)` writes under it; both `make_repo` helpers take `local=` | `tests/conftest.py#local_home@494e0175`, `tests/conftest.py#declare_routing@ae0c1556` | **Executed**: red against HEAD's conftest before the helper moved (`ImportError: cannot import name 'local_home' from 'conftest'`, 1 error in 0.55s), 38 passed after (`8d05b00`) | 2026-09-02 | Every case in `tests/test_local_mode_resolves_under_the_git_dir.py` builds on it; a fixture built from a linked worktree lands in the main tree |

###### The joiners

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S6 · the evidence advisor globs `ledger.md` and `ledger/*.md` under the resolved root; `docs/**/_evidence.md` stays under the tree | `hooks/evidence-advisor.py#failing_rows@b51f42d6`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_evidence_advisor_reports_a_broken_row_from_the_local_ledger@9fb26dd7`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_evidence_advisor_reads_the_local_ledger_from_a_linked_worktree@633fea57`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_evidence_advisor_still_reads_the_pre_0_10_address_under_the_tree@30bee5a2` | **Executed**: red at `8d05b00` (7 failed, 7 passed in the file), green at `018f0c2`: a BROKEN row at `.git/seal/ledger.md` and at `.git/seal/ledger/f.md` is reported after the commit, from the main tree and from a linked worktree; an uncommitted-address `docs/**/_evidence.md` is still read | 2026-09-02 | Spelling `seal/` under the repository root is what left a local-mode ledger unread at every commit |
| S6 · the ledger migration hook globs the same two under the resolved root, and `dirty()` asks git only about paths inside the tree | `hooks/ledger-migrate.py#HOME_GLOBS@b6ae7858`, `hooks/ledger-migrate.py#ledgers@e313c23a`, `hooks/ledger-migrate.py#dirty@a82428bb`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_ledger_migration_hook_migrates_a_pre_anchor_ledger_under_the_local_root@4d69576f`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_ledger_migration_hook_is_not_refused_from_a_linked_worktree@58e7c623`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_a_dirty_ledger_under_the_tree_is_still_refused_in_local_mode@241804b6` | **Executed**: red at `8d05b00`, green at `018f0c2`: a `path:line` ledger under `.git/seal/ledger/` is rewritten to anchors, from a linked worktree without the `uncommitted` refusal, and an uncommitted `docs/**/_evidence.md` is still refused. Phase 3 measured what git says for a pathspec that climbs out of the tree, in a plain repository: `git status --porcelain -- ../outside/ledger.md` prints `fatal: ../outside/ledger.md: '../outside/ledger.md' is outside repository at '…'`, exit 128 | 2026-09-02 | That exit is what `dirty()` read as work in progress at every session start from a linked worktree. A `../` or `.git/` path is never asked about now, because nothing under the git directory can be uncommitted work |
| Q5 · the commit gate's stop text names the declaration under the resolved root, relative to the repository; the literal `seal/specs/…` only when no root exists | `hooks/commit-review-gate.py#judge@6ca45c3c`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_commit_gate_fires_without_one_and_names_the_resolved_path@5b734ad8`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_stop_text_from_a_linked_worktree_is_a_path_a_person_can_type@6f88cc2e`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_in_shared_mode_the_stop_text_still_names_the_tree_root@7192e986`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_commit_gate_is_silent_from_a_linked_worktree_too@fcebb909` | **Executed**: red at `8d05b00`, green at `018f0c2`: local mode is told `.git/seal/specs/<work-item-id>/routing.md` and not the literal; a linked worktree is told the `../` path `os.path.relpath(home, other)` builds; shared mode is told `seal/specs/<work-item-id>/routing.md`; a declaration under the local root keeps the gate silent from the main tree and from a worktree | 2026-09-02 | Joined with `os.path.join`, the way `implementer-notice.py` prints its path; the windows leg is CI's (S11) |
| Q2 · the plugin's checker resolves the root through `hooks/optin.py`; the vendored copy reads `<root>/seal/`; `--ledger` bypasses both | `skills/evidence-check/scripts/evidence_check.py#seal_home@15082932`, `tests/test_evidence_check.py#test_a_copy_under_a_plugin_tree_without_a_skill_beside_it_is_still_vendored@46f93249`, `tests/test_evidence_check.py#test_the_plugins_copy_resolves_the_root_under_the_git_directory@9be0cf27`, `tests/test_evidence_check.py#test_the_tree_root_still_wins_for_the_plugins_copy@e5e29b6b`, `tests/test_evidence_check.py#test_the_plugins_copy_reads_the_parity_config_under_the_resolved_root@e3946dd2`, `tests/test_evidence_check.py#vendored_copy@2700987f`, `tests/test_evidence_check.py#test_the_ledger_flag_overrides_the_resolver_either_way@341fe2fc` | **Executed**: three cases red before `ca77fc7`, 234 passed across the seven files that load the checker after. By CLI, in a scratch fixture with `src/service.py` committed, no `<repo>/seal/`, and `.git/seal/ledger/1788354065-x.md` citing `src/service.py#handler` and `src/service.py#gone` — by phase 1 at `7e44106` and again by phase 3 — `bin/evidence-check .` prints `.git/seal/ledger/1788354065-x.md` · `BROKEN src/service.py#gone locator not found` · `total: 1 ok · 0 drifted · 1 broken · 0 external · 0 old-format`, exit 2; the same tree through a copy of the script alone in `tools/` prints `no evidence ledgers found — nothing to check`, exit 0 | 2026-09-04 | The vendored copy runs in CI, which is shared mode. A resolver that answers "" or raises also reads `<root>/seal/`: a CLI a person is watching, not a gate. Re-read 2026-09-04 after work item 1788501054 appended to `seal_home`'s docstring: `--ledger` still bypasses both resolvers, so the claim holds, and what changed is that the run now NAMES the ledgers the bypass skipped. **Re-read 2026-09-05 in round 4's fix pass, which found the row's middle clause held by nothing.** *The vendored copy reads `<root>/seal/`* is decided by `os.path.isfile(skill)`, and dropping that conjunct left every evidence suite green because every vendored fixture also lacked `hooks/optin.py` three levels up, so the first conjunct decided each time. A copy three directories under a tree that HAS a plugin, with no skill beside it, separates them: against a local-mode repository the answers differ, so the clause was true and undefended. It is executed now, against the case added to this row |
| S9 / Q3 · with nothing old left, `root-migrate.py` stamps when `seal/` is a directory at either place, read with two `isdir` calls rather than `home_at`; what moves and where it moves is unchanged | `hooks/root-migrate.py#has_root@c5ee44e7`, `hooks/root-migrate.py#main@66b39ece`, `tests/test_the_root_migrates_itself.py#test_a_repository_with_only_a_local_root_is_silent_and_stamped@0288304e`, `tests/test_the_root_migrates_itself.py#test_a_linked_worktree_of_a_local_mode_repository_is_stamped_too@dfe753ad`, `tests/test_the_root_migrates_itself.py#test_a_local_root_beside_an_old_layout_is_moved_into_the_tree@e48601aa`, `tests/test_the_root_migrates_itself.py#test_a_file_named_seal_under_the_git_directory_stamps_nothing@0d225ea3` | **Executed**: the first two red before `529976d`, green after; the other two held before the change and pin it: an old layout beside `.git/seal/` still moves into `<repo>/seal/` with `moved .specseal/ and 1 work item into seal/`, and a `seal` FILE under the git directory stamps nothing | 2026-09-02 | Fail direction: stamps more, the quieter mistake; a missed move prints nothing and the README's by-hand sequence is the way out. Read directly so the scratch marker does not hide the root from this rule |
| S10 · every new path fails toward "not opted in": `git_common_dir` answering "" makes `home_at` and `home` ""; the scratch marker under the common directory takes local mode back | `hooks/optin.py#git_common_dir@bf3e8dd5`, `tests/test_gates_do_not_fail_open.py#test_the_common_directory_reader_that_decoded_nothing_answers_empty@0d357944`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_a_scratch_marker_under_the_common_directory_keeps_every_gate_silent@ffbbbed7` | **Executed**: `subprocess.run` returning `stdout=None` for `--git-common-dir` on a tree whose `.git` is a FILE answers "" from `git_common_dir`, `home_at` and `home`, `opted_in` False, nothing raised — green on first run, no code change needed; `.git/specseal-scratch` beside a local root holding a declaration and two ledgers leaves the commit gate, the review-skill gate, the implementer notice, the evidence advisor and the ledger migration silent | 2026-09-02 | The house rule of `tests/test_gates_do_not_fail_open.py`: a dead reader thread sets `stdout` to `None` and the exception does not propagate. No gate blocks more than it did |

###### First setup and the documents

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S4 / S12 · first setup asks one `AskUserQuestion` with two options, shared first as the default; each says what it creates and installs; the path is spelled through `git rev-parse --git-common-dir`; a repository with `seal/` at either place is not asked; the parity question follows; the "nowhere else" sentence is gone | `skills/implement/SKILL.md#"### Bootstrap — create what's missing"@5fd3b434`, `tests/test_first_setup_asks_once.py#test_the_two_options_are_named_in_order_with_shared_as_the_default@518663da`, `tests/test_first_setup_asks_once.py#test_each_option_says_what_it_creates_and_what_it_installs@8b6bebf9`, `tests/test_first_setup_asks_once.py#test_the_root_is_spelled_through_the_common_dir_never_the_literal@6497e08b`, `tests/test_first_setup_asks_once.py#test_a_repository_with_the_root_at_either_place_is_never_asked@6abb28a0`, `tests/test_first_setup_asks_once.py#test_the_parity_question_follows_the_mode_question@9d887343`, `tests/test_first_setup_asks_once.py#test_the_sentence_that_said_nowhere_else_is_gone@ee4398df` | **Executed**: the six red at `7e44106` (30 failed, 1 passed for the whole file), green at `d962cf8`. **Executed** by phase 3: `git diff 0b2697c..a497e03 -- hooks/` adds no line naming `AskUserQuestion` — no hook asks anything new | 2026-09-03 | Prompt budget: one question, once per repository, in the batch the skill already asks  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). This claim's own text is untouched by that bullet, so only the section hash moved |
| Q7 · a repository still on the 0.3.x layout is told, not asked: the session-start hook moves it into `<repo>/seal/` | `skills/implement/SKILL.md#"### Bootstrap — create what's missing"@5fd3b434`, `tests/test_first_setup_asks_once.py#test_an_unmoved_old_layout_is_not_asked_but_told@b8d038c3` | **Executed**: red at `7e44106`, green at `d962cf8` | 2026-09-03 | A repository that committed `.specseal/` chose shared; a local root created beside it would be moved into the tree by the hook anyway  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). This claim's own text is untouched by that bullet, so only the section hash moved |
| S5 / Q4 / Q9 · the template clones the plugin at `v<version>` into `$RUNNER_TEMP`, fetches `refs/pull/*/head`, runs the two shipped checks; the rows that read this repository do not travel; the skill writes it only when absent, and local installs nothing | `templates/hygiene.yml#"- name: the plugin, at the release this file was written against"@523a7706`, `templates/hygiene.yml#"- name: the unverified record is readable, and rows leave it closed"@535e0547`, `templates/hygiene.yml#"- name: a declared review chain has the round record it claimed"@51172dd4`, `tests/test_first_setup_asks_once.py#test_the_template_runs_the_two_shipped_checks_from_the_plugin_at_its_tag@919869ee`, `tests/test_first_setup_asks_once.py#test_the_rows_that_read_this_repository_do_not_travel@2cddbf11`, `tests/test_first_setup_asks_once.py#test_the_skill_writes_the_workflow_only_when_absent_and_local_installs_nothing@39b68a17` | **Executed**: red at `7e44106`, green at `2af85e8`: both scripts, the refspec and `v<version>` named; none of `plugin.json`, `gather_changelog`, `fold_ledger`, `README.ko.md`; the skill text names the template, "only when" absent and that local installs nothing. **Read**: `actions/checkout` resolves `path:` under `GITHUB_WORKSPACE` and refuses one outside it, which is why the step is `git clone` (Q9). **Not executed**: the workflow on a runner | 2026-09-02 | Q9's default; `overview.md` §Not verified names who answers it |
| S7 · the template's header says why local mode installs no copy: both checks read committed files, and a workflow there would go green having examined nothing | `templates/hygiene.yml#"# Why local mode installs no copy: both checks read committed files —"@290d6a87`, `tests/test_first_setup_asks_once.py#test_the_template_header_says_why_local_mode_installs_none@cf47993a` | **Executed** by phase 3 in a scratch fixture, on a feature branch that committed no records: `chain_check.py --baseline main` prints `this pull request declared neither way, so the review-chain check examined nothing`, exit 0; `unverified_check.py --baseline main seal/specs/` prints `no such path: seal/specs/ — and nothing under it at main either`, exit 2. The test red at `7e44106`, green at `2af85e8` | 2026-09-02 | Phase 2 had read the two exits from the scripts; phase 3 ran them. `chain_check.py`'s line ends `routing.md todeclare` — a missing space, named in `overview.md` §Not done |
| S8 / S14 · the switch is two moves, given in both READMEs under *First run* and in the root's README template, which names both places and what local mode gives up | `README.md#"### Shared or local"@46c3e465`, `README.ko.md#"### shared 인가 local 인가"@4caf634c`, `templates/seal-README.md#"# seal/ — what this plugin maintains"@152f5483`, `tests/test_first_setup_asks_once.py#test_the_readme_gives_both_moves_under_first_run@f6db6498`, `tests/test_first_setup_asks_once.py#test_the_gates_table_says_the_root_may_sit_under_the_git_dir@48c5d497`, `tests/test_first_setup_asks_once.py#test_the_seal_readme_names_both_places@9c059d6f`, `tests/test_first_setup_asks_once.py#test_the_seal_readme_is_the_template_verbatim@a72995e6` | **Executed** by phase 3 in the scratch fixture: `mv "$(git rev-parse --git-common-dir)/seal" seal && git add seal` leaves `A  seal/ledger/1788354065-x.md` staged and `.git/seal` gone; `git rm -r --cached seal && mv seal "$(git rev-parse --git-common-dir)/seal"` leaves `git status --porcelain` empty and the ledger back under `.git/seal/ledger/`. The tests red at `7e44106`, green at `2eba4be` and `32e178c`; `tests/test_docs_line_wrap.py` green on the READMEs | 2026-09-03 | Run on a fixture whose root held one file; the commit each direction ends with was not made, because a commit in an opted-in fixture meets the commit gate. `seal/README.md` is the template verbatim, which `hooks/root-migrate.py#rewrite_readme` depends on. **Re-read 2026-09-03** by work item 1788360817, which added three lines to that template's Layout tree: the template still names both places and still says what local mode gives up, so the claim is unchanged and only the whole-document hash moved |
| Q8 · the design record's two sentences are corrected and this work item's four decisions sit in "Decided after the thread", in both editions | `docs/one-root-by-lifetime.md#"## Decided after the thread"@1ead2dde`, `docs/one-root-by-lifetime.ko.md#"## 스레드 뒤에 정해진 것"@f8dfc67c`, `tests/test_first_setup_asks_once.py#test_the_record_no_longer_says_the_checks_would_refuse@9cdbcc76`, `tests/test_first_setup_asks_once.py#test_the_scratch_row_moved_from_left_open_to_decided@e9f35d9a`, `tests/test_first_setup_asks_once.py#test_the_decided_table_carries_this_work_items_rows@6357fae8` | **Executed**: red at `7e44106`, green at `3d69594`: "would refuse a pull request with no round records" is gone from both editions, the `scratch` row sits under *Decided after the thread* and not under *Decisions left open*, and the four rows are present | 2026-09-02 | The record is the policy `spec.md` cites, and a policy the code contradicts gets fixed (`implement` §1) |

###### What phase 3 fed back

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S5 · the template puts the plugin on the runner with `git clone --depth 1 --branch v<version>` into `$RUNNER_TEMP`, not with `actions/checkout` | `seal/specs/1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in/spec.md#"## User scenarios & acceptance *(mandatory)*"@c0f587f5` | **Read**: S5 rewritten by phase 3 to what `templates/hygiene.yml` does, marked *inferred during implementation*; the executed half is the S5 / Q4 / Q9 row above | 2026-09-02 | Q9 (a). `--strict .` on this repository after the fragment's hashes were written: totals in `overview.md`'s proof block |

###### What round 1 settled

<!-- The reviewer's four executed facts (rounds/evidence-todo.md, drained by
the round-1 fix pass), the row seal/ledger.md §1788331011 S12 could no
longer carry, and the fixes the round asked for — each test red first, the
red output in the body of the commit named. Hashes by `--reverify`. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S7 · with the declaration only under `.git/seal/`, `chain_check.py --baseline main` exits 0 ("examined nothing") and `unverified_check.py --baseline main seal/specs/` exits 2 ("no such path") | `skills/code-review/scripts/chain_check.py#declared_for_this_branch@0a4852e0`, `skills/verify/scripts/unverified_check.py#main@86d5e3b9` | **Executed** by the round-1 reviewer (P2) in a local fixture and reproduced by the orchestrator, not taken from phase 3: exit 0 / exit 2. The fix pass changed one string in `declared_for_this_branch` (the missing space before `declare`, 🟢 10) and re-ran `tests/test_chain_check_at_the_pull_request.py` | 2026-09-02 | Round 1 evidence row 1. The round record and its evidence row spelled the unit `#committed_declarations`, a name the file does not have; the "examined nothing" line is in `declared_for_this_branch`, and `--strict` said so (BROKEN) before this row was corrected |
| Q5 · the gate's hint from a linked worktree whose path has spaces can be typed as-is as the declaration, and the gate is silent afterwards | `hooks/commit-review-gate.py#judge@6ca45c3c`, `hooks/commit-review-gate.py#declaration_hint@9096166c` | **Executed** by the reviewer (P6): main tree and worktree under a directory with a space in its name; the hint `../../a dir/main tree/.git/seal/specs/<work-item-id>/routing.md` typed as the declaration, the gate silent after. The fix pass moved the hint into `declaration_hint`, built only when the arm stands, and re-ran the four sibling shapes green at 1f27eac | 2026-09-02 | Round 1 evidence row 2 |
| S9 · a stamped local-mode repository that later checks out an old-layout branch moves nothing and stages nothing | `hooks/root-migrate.py#has_root@c5ee44e7`, `hooks/root-migrate.py#main@66b39ece`, `tests/test_the_root_migrates_itself.py#test_a_stamped_local_mode_repository_leaves_an_old_layout_branch_alone@a8142aa2` | **Executed** by the reviewer (P5) and pinned by the fix pass at 2388341: `.git/seal/` only → start → stamped; `old-layout` checked out with `.specseal/` and `specs/<id>/` → start prints nothing, no `<repo>/seal/`, `git status --porcelain` empty | 2026-09-02 | Round 1 evidence row 3, tests-todo row 5 |
| S2 · in a linked worktree the marks live under `--git-dir` (per worktree) and the root under `--git-common-dir` (shared by the clone) | `hooks/commit-review-gate.py#read_mark@a5f284ae`, `hooks/optin.py#git_common_dir@bf3e8dd5` | **Executed** by the reviewer (P7): the two `rev-parse` answers differ in a linked worktree, per-worktree against common | 2026-09-02 | Round 1 evidence row 4. `--reverify` on `.git/seal/ledger.md` is last-writer-wins across worktrees, which the design record's "Shared or local" accepts |
| S12 (§1788331011) · every checker's default is the new path: the advisor, the checker's cross-repo intent, the fold and gather scripts | `hooks/evidence-advisor.py#failing_rows@b51f42d6`, `skills/evidence-check/scripts/evidence_check.py#cross_repo_intent@18c8c5b0`, `.github/scripts/fold_ledger.py#LEDGER@3d869849`, `.github/scripts/fold_ledger.py#open_items@b2ff3354`, `.github/scripts/gather_changelog.py#fragments@a8c92cdb` | **Executed** by the fix pass: `evidence_check.py --strict .` with no ledger argument lists `seal/ledger.md` and `seal/ledger/1788354065-….md`. **Read**: the per-unit test files are the ones §1788331011's row named, green at that item's close and not re-run by this pass | 2026-09-02 | Moved here from `seal/ledger.md` §1788331011 S12 by the round-1 fix pass (❓ 8). That row cited six anchors and #80 removed one (`hooks/ledger-migrate.py#LEDGER_GLOBS`); a row whose anchor a change removes is REMOVED and the new claim goes to the branch's fragment (CLAUDE.md). The rule says nothing about a row with more than one anchor; whether it should is the owner's, named in the PR body |
| 🔴 1 / 🟢 6 · the review arm's hint is built only when the arm stands, and a root with no relative spelling is named by its absolute path | `hooks/commit-review-gate.py#declaration_hint@9096166c`, `tests/test_gates_do_not_fail_open.py#test_the_commit_gates_hint_survives_a_root_with_no_relative_spelling@ccad81fa` | **Executed**: red at f049003 with `os.path.relpath` made to raise in a local fixture — `ValueError` out of `judge`, stdout empty — green at 1f27eac: `deny`/`ask` printed and the hint reads `<absolute home>/specs/<work-item-id>/routing.md` | 2026-09-02 | Windows' other-drive shape, induced so every leg runs it; the real shape is S11's, CI's windows leg |
| 🔴 2 / 🟡 5 · `dirty()` skips a path with no relative spelling; in local mode the migration notice names `evidence-check .` instead of a diff | `hooks/ledger-migrate.py#dirty@a82428bb`, `hooks/ledger-migrate.py#main@2b930da5`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_ledger_migration_hook_survives_a_ledger_with_no_relative_spelling@6c13fba5`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_in_local_mode_the_migration_notice_does_not_promise_a_diff@eb3c1d62`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_in_shared_mode_the_migration_notice_still_ends_with_the_diff@56f72b73` | **Executed**: red at f049003 (the `ValueError` out of `dirty`; "review the diff and commit" printed for a `.git/seal/` root), green at 2078984: the migration proceeds under the raise; "this root has no git history, so run `evidence-check .`" for a `.git/seal/` root, "review the diff and commit" for a committed `<repo>/seal/` | 2026-09-02 | Local is "not the shared root", which from a linked worktree is also a root outside this tree |
| S8 / 🟡 3 · both switch moves go through `$(git rev-parse --show-toplevel)/seal` in both READMEs and in the root README template, and land the same from a subdirectory | `README.md#"### Shared or local"@46c3e465`, `README.ko.md#"### shared 인가 local 인가"@4caf634c`, `templates/seal-README.md#"# seal/ — what this plugin maintains"@152f5483`, `tests/test_first_setup_asks_once.py#switch_block@4d7c195d`, `tests/test_first_setup_asks_once.py#test_the_switch_block_lands_the_root_at_the_tree_root_from_a_subdirectory@3e55bae9` | **Executed**: the block read out of each README and run under `bash -c` from `src/` — red at 2078984 (`A  src/seal/ledger/f.md`), green at f53d615: `A  seal/ledger/f.md` one way, the ledger back under `.git/seal/ledger/` and status empty the other | 2026-09-03 | `seal/README.md` is the template byte for byte, pinned by `test_the_seal_readme_is_the_template_verbatim`. **Re-read 2026-09-03** by work item 1788360817: all four switch commands still go through `$(git rev-parse --show-toplevel)/seal`, so the claim is unchanged and only the whole-document hash moved |
| S5 / 🟡 4 · the bootstrap reads the version from `$CLAUDE_PLUGIN_ROOT/.claude-plugin/plugin.json` with a one-line command; the READMEs name `$CLAUDE_PLUGIN_ROOT/templates/hygiene.yml` | `skills/implement/SKILL.md#"### Bootstrap — create what's missing"@5fd3b434`, `tests/test_first_setup_asks_once.py#test_the_version_is_read_from_the_plugin_root_and_not_from_the_tree@9949c6e1` | **Executed**: the test red at 2078984, green at f53d615; the one-liner run with `CLAUDE_PLUGIN_ROOT` set to this checkout prints `v0.4.0` | 2026-09-03 | The spelling `skills/update/SKILL.md` already used  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). This claim's own text is untouched by that bullet, so only the section hash moved |

<!-- specs/1788360817-the-pull-request-language-is-fixed-inside-a-skill -->
### 1788360817-the-pull-request-language-is-fixed-inside-a-skill

<!-- This work item's ledger rows: one file per work item so that two branches
never queue at seal/ledger.md, folded into it at the release.

Every row here is over PROSE, and that is the work item rather than a gap in
it. Nothing in `hooks/` reads `seal/config.md`: judging what language a
commit message is in means being wrong about names, identifiers and quoted
English, and a wrong stop blocks a correct commit (`questions.md` Q3). So
each row's code grounds are the document that carries the rule and the case
that pins it, and "Executed" means the case was run, not that a session was
observed obeying the rule. The one thing no case here can reach is named in
`overview.md` §Not verified. -->

###### The row, and where a session reads it from

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · the skill names both the file and the row, so a session knows where to look and what to look for | `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_skill_names_the_file_and_the_row@cbca742e` | **Executed**: 24 passed in the new file at `56ec644`. Both halves are asserted separately — a path with no row name and a row name with no path each leave a session unable to act | 2026-09-03 | The row is the only thing in `config.md` today; the table is open for more items later |
| S2 / 🟡 3 · every way of NOT naming a language lands on English, and there are FOUR: no file, no such row, an empty value, and a file that cannot be read or does not parse as that table | `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `tests/test_the_pull_request_language_is_the_repositorys.py#configured_language@4e389a05`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_every_way_of_not_naming_a_language_lands_on_english@b00fbf02`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_an_unreadable_config_lands_on_english_too@ea7e7742`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_skill_names_every_way_of_not_naming_one@73ae726b`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_default_is_stated_where_the_path_is@8fc71b49` | **Executed**: the rule is now RUN, not only asserted in prose — a reader built to the skill's sentence answers `English` for every shape, the unreadable one built as a `config.md` that is a directory (`IsADirectoryError`, an `OSError`, the class chosen so a permission error lands the same way). 46 passed in the file | 2026-09-03 | Round 1 🟡 3: the first draft named two absences and stopped, so a session meeting an empty value could stop, ask, or guess. Round 2 ❓ 8 settled the COUNT at four — "cannot be read or does not parse" is one way with two spellings — and `UNNAMED` keeps five ids for it, which its comment now says. The old row cited `test_absence_means_english_in_both_of_its_spellings`, which round 1 split |
| S3 · the root is resolved `<repo>/seal/` then `<git-common-dir>/seal/`, never spelled as the first alone | `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_root_is_resolved_rather_than_spelled@6181a8fe` | **Executed**: both `git rev-parse --git-common-dir` and `<repo>/seal/` are asserted present, so neither place can be dropped alone. **Read**: `hooks/optin.py#home_at` is the code that does this for the hooks; no hook reads `config.md`, so the skill's sentence is the whole mechanism here | 2026-09-03 | Spelling `<repo>/seal/` and stopping is what left a local-mode ledger unread at every commit in #80 — the same mistake, one release earlier |

###### The five places that used to require English

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · four of the five surfaces stopped requiring English of every repository, and each says what it defers to instead | `skills/commit-pr-convention/SKILL.md#"## Commit messages"@531fbfec`, `skills/commit-pr-convention/SKILL.md#"## Pull request titles"@5727ac84`, `skills/commit-pr-convention/SKILL.md#"## Before you open it"@d77e425c`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_surface_no_longer_requires_english_of_everyone@fc88e520`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_surface_defers_to_the_row_instead@1bd58194` | **Executed**: eight parametrised cases, four asserting the retired phrase is gone and four asserting a replacement rule is present. The absence half alone would pass on a file that removed the rule and put nothing back | 2026-09-03 | The retired phrases are pinned verbatim, so a re-wrap that keeps the requirement still fails |
| S1 · the fifth surface, the pull request body, whose old text is two words | `skills/commit-pr-convention/SKILL.md#"## Pull request bodies"@9b68f07b`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_pull_request_body_section_no_longer_opens_with_english@f907d8aa` | **Executed**: pinned by POSITION rather than by phrase — the section's first line — because a bare `**English.**` would also match inside a sentence merely describing the default. The case asserts the section still exists before reading it, so a rename turns it red rather than blind | 2026-09-03 | The one surface where the obvious pin would have been a false one. **Re-read 2026-09-05 in work item 1788613827's phase 4**, after phase 3 added the chain section's shape and the pointer at the comparison table's owner to this section. Both paragraphs sit after the writing-style sentence, so the section's FIRST line — which is what this row's pin reads — is where it was |

###### What the row deliberately does not reach

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S4 · the prefix vocabulary is not translated, said in the skill AND in the template | `templates/config.md#"## What no row governs"@0d626d6d`, `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_prefix_vocabulary_is_not_translated@71909871` | **Executed**: red on first run against the template, which said the prefixes "stay as they are" without the words the case pins; green after the template was reworded, at `56ec644` | 2026-09-04 | Both files, because a person writing the config reads the template and never the skill. `기능:` would break both things a prefix is for: a person scanning a log, and anything that parses one. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `deferred` in `deferred <home>` to the section's list of verdict words; the sentence this row pins is untouched |
| S5 · branch names stay ASCII whatever the row says | `templates/config.md#"## What no row governs"@0d626d6d`, `skills/commit-pr-convention/SKILL.md#"## Branch names"@3fb194d3`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_branch_names_stay_ascii@beb29230` | **Executed**: asserted in both files. The exclusion is repeated at the branch-names section itself, so a reader who arrives there without having read the language section still gets it | 2026-09-04 | A branch name is typed into a shell and pasted into a URL. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `deferred` in `deferred <home>` to the section's list of verdict words; the sentence this row pins is untouched |
| S6 · the response language is not in the repository file, and both documents say why | `templates/config.md#"## What no row governs"@0d626d6d`, `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_response_language_stays_out_of_the_repository_file@84baaa91` | **Executed**: `response language` and `person's setting` are both asserted, in both files — where alone reads as an arbitrary boundary, and the first person to look for the setting adds a row here | 2026-09-04 | Issue #82's third done-when row, and `docs/one-root-by-lifetime.md` before it. Two people in one repository can want different answers there and the same one here. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `deferred` in `deferred <home>` to the section's list of verdict words; the sentence this row pins is untouched |

###### The mirror, and the file itself

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S7 · the mirror is `pr.<lang>.md`, named for its own language, given in both directions | `skills/commit-pr-convention/SKILL.md#"## Pull request bodies"@9b68f07b`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_mirror_is_named_for_its_own_language@6da33877`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_existing_mirrors_are_consistent_with_the_rule@e77eb16e` | **Executed**: the rule and both examples are asserted, and the twelve committed mirrors are globbed and checked against the rule rather than assumed to match it — `{pr.ko.md}`, with `seal/config.md` asserted absent so the English default is what the check is entitled to assume | 2026-09-03 | Nothing was renamed: the name was under-specified rather than wrong (`questions.md` Q2). One direction alone reads as an example, and an example is what a session copies literally. **Re-read 2026-09-05 in work item 1788613827's phase 4**, after phase 3 added two paragraphs about the chain section to this section; the mirror's name, both directions and the twelve committed mirrors are untouched |
| S8 · `templates/config.md` is one `\| Item \| Value \|` table whose first row is `Pull request language` → `English` | `templates/config.md#"# Repository config"@45edf260`, `tests/test_the_pull_request_language_is_the_repositorys.py#items@ebbda132`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_template_is_one_item_value_table_whose_first_row_is_the_language@f9b09e4d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_check_can_fail@62978cef` | **Executed**: the first run of the parser returned `('---', '---')` as row one — a markdown separator has three pipes and two cells exactly as a real row does — and the fix is a `SEPARATOR` pattern skipped before `ROW` is tried. The failure case asserts three things the reader must NOT find: a table in prose, `parity.md`'s `\| Field \| Value \|` header, and a separator read as a row | 2026-09-03 | The shape is `parity.md`'s on purpose, so a reader written for one reads the other |
| S9 · the four documents that draw the root list `config.md`; `seal/README.md` stays the template verbatim | `templates/seal-README.md#"## Layout"@508a6f02`, `seal/README.md#"## Layout"@508a6f02`, `README.md#"## The ledger"@9dc4f39b`, `README.ko.md#"## 저장소에 남는 기억"@10672628`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_layout_lists_the_config@ff803454` | **Executed**: four parametrised cases, red for all four before the edits and green at `56ec644`; `tests/test_first_setup_asks_once.py#test_the_seal_readme_is_the_template_verbatim` green, so `hooks/root-migrate.py#rewrite_readme` still writes a README describing the current root. `tests/test_docs_line_wrap.py` green on the README pair and on `templates/config.md`, which joins `COVERED` at birth | 2026-09-03 | Both READMEs move together — the hygiene workflow warns when they do not. The Korean edition carries the same two lines |

###### What round 1 changed

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| 🟡 1 / 🟡 4 / r3 1 · the path of every TRACKED file under `templates/`, at any depth and tracked dotfiles included, occurs somewhere in the shipped prose documents. What runs is a substring test over `git ls-files templates` against a `.md`-only corpus — not a judgment that the document naming it tells anybody to start from it | `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `skills/implement/SKILL.md#"### 3. The SDD file set"@5cf66afe`, `skills/implement/SKILL.md#"### Bootstrap — create what's missing"@5fd3b434`, `skills/code-review/SKILL.md#"## Cross-session records — `seal/specs/<work-item-id>/`"@986af880`, `tests/test_the_pull_request_language_is_the_repositorys.py#shipped_templates@b1407676`, `tests/test_the_pull_request_language_is_the_repositorys.py#unreachable_templates@c2c2c08d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_every_template_is_named_by_a_document_that_ships@9b339354`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_templates_check_reads_prose_only_and_descends@d9f15821` | **Executed**: four unreachable at `1280de9` and none at the working tree. Round 2 🟡 4 narrowed the corpus to `.md` and found two more — `sdd-round.md` was passing on four comments in `chain_check.py` and `ledger.md` on a Korean style example where the file is the specimen rather than the subject — closed by naming each where a session already reads: the round record's fields in `code-review`, and the bootstrap line that used to say `seal/ledger.md` is "created empty". Round 3 finding 1: the listing came from a working-tree glob while the corpus came from `git ls-files`, so an untracked `templates/.DS_Store` — gitignored, and one arrives from opening the folder in Finder — was reported as an unreachable template. Reproduced in a scratch repository: the glob returns it and `git status --porcelain` says nothing about it; `git ls-files` does not | 2026-09-03 | **What this check is worth, stated rather than implied.** It proves a path APPEARS in prose, and round 3's census found six of the twelve templates resting on a single sentence each — all six honest today, and nothing in the check could tell if one stopped being. That is the judgment the clause above deliberately no longer claims. Round 1's clause said a session "has something to copy", which a corpus holding Python had already made false for two templates. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which rewrote the review skill's paragraph on where a record starts: `round_record.py new` writes it in the template's shape. `templates/sdd-round.md` is still named there, which is what the substring test this row is about reads |
| 🟡 2 · the mirror's home is resolved the same way the config's is, and it does NOT go under the git directory | `skills/commit-pr-convention/SKILL.md#"## Pull request bodies"@9b68f07b`, `templates/config.md#"## What no row governs"@0d626d6d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_mirror_is_named_for_its_own_language@6da33877` | **Read**: the three reasons the skill gives for that location — a reviewer can open it, it is versioned, it survives the merge — are exactly what a root under the git directory takes away, since nothing under it is ever a commit candidate (`seal/README.md`, and `hooks/optin.py#home_at` for where that root is). Both documents now say so and send the mirror beside the documents the pull request touches | 2026-09-04 | The config was resolved two ways and the mirror spelled one way, in the same section. Nothing executes this: no code reads either path. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `deferred` in `deferred <home>` to `templates/config.md`'s list of verdict words; nothing about the mirror's home moved. **Re-read again 2026-09-05 in work item 1788613827's phase 4**, which moved `## Pull request bodies` by two paragraphs about the chain section's shape; the mirror's home and the reason given for it are untouched, and `templates/config.md` is not a file that branch changed |
| 🟡 6 / 🟡 5 · BELOW the first row, any line this table does not own ends it — a row of another shape, a separator, and a repeated header alike. Above the first row a separator and a blank line are still tolerated, because there they are the table's own furniture | `tests/test_the_pull_request_language_is_the_repositorys.py#items@ebbda132`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_row_of_another_table_ends_this_one@7185bf43`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_second_table_further_down_is_not_read_as_more_rows@05399cad`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_separator_below_the_rows_ends_the_table@7d76f220`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_repeated_header_ends_the_table@abbbba7e`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_junk_above_the_first_row_is_still_tolerated@323648c8` | **Executed**: the pre-fix reader returned `[('Pull request language', 'Korean'), ('Something else', 'x')]` for a stray separator below the rows and `[('Pull request language', 'Korean'), ('Pull request language', 'English')]` for a repeated header. Both now stop at the first row, and the tolerant direction above the header is pinned so the fix does not become a stricter rule than intended | 2026-09-03 | Round 1 closed one shape and wrote a claim wider than the code, which round 2 🟡 5 read back. `HEADER` and `SEPARATOR` were both tested before `ROW` and both `continue`d — the same defect one line further along |
| 🟡 4 · a whole-document anchor makes any edit to that document another work item's drift, which a fragment-scoped check cannot see | `seal/ledger/1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in.md`, `templates/seal-README.md#"# seal/ — what this plugin maintains"@152f5483` | **Executed**: `evidence-check --strict .` read `325 ok · 1 drifted` against the base's `299 ok · 0 drifted`; the scoped run over this item's own fragment read `27 ok · 0 drifted` and could not see it. **Read**: both claims re-read against the template as it stands — it still names both places, still says what local mode gives up, and all four switch commands still go through `$(git rev-parse --show-toplevel)/seal`. Only the hash moved, so `--reverify` is the whole fix | 2026-09-03 | Three lines added to a Layout tree drifted two rows about a paragraph forty lines away. The lesson is in `overview.md`: a scoped check answers for the fragment and says nothing about what the branch did to anyone else's |

###### What round 2 changed — two fixes that reproduced the defect they closed

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| 🔴 1 · a language the file has no code for answers "no mirror name to refuse" rather than raising, and the case skips with a reason | `tests/test_the_pull_request_language_is_the_repositorys.py#mirror_to_refuse@b2cdcb1d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_language_this_file_has_no_code_for_does_not_raise@3361ceb7`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_existing_mirrors_are_consistent_with_the_rule@e77eb16e` | **Executed**: indexing the three-entry dictionary with `French` raises `KeyError`, which is a traceback rather than a failing assertion; `mirror_to_refuse` returns `None` for it and the two directions the skill gives as examples both still compute. `templates/config.md` licenses "a language's English name" in as many words, so `French` is a legitimate config | 2026-09-03 | Round 1 🟡 5 was *a legitimate config turns this red*, and its fix turned a legitimate config red one line over. A table that must list every language is always one short, so the missing entry is a gap in the dictionary rather than a defect in the repository |
| 🟡 2 · the reader resolves both homes in order, `<repo>/seal/` then `$(git rev-parse --git-common-dir)/seal/`, and the tree root wins where both exist | `tests/test_the_pull_request_language_is_the_repositorys.py#config_homes@cd3e41b7`, `tests/test_the_pull_request_language_is_the_repositorys.py#configured_language@4e389a05`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_reader_finds_a_config_under_the_git_directory@4ca1ee42`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_tree_root_still_wins_over_the_git_directory@9cd7beed` | **Executed** in a scratch repository holding a Korean row at `.git/seal/config.md`: the one-home reader answered `English`, the two-home reader answers `Korean`. With a Japanese row at `<repo>/seal/` beside it, the tree root wins. The ROOT is resolved by directory existence first, the way `hooks/optin.py#home_at` does, so an unreadable config inside the resolved root is one of the four ways rather than a reason to look in the other place | 2026-09-03 | Round 1 fixed this in prose and shipped the same defect in the executable model of that prose, in one commit. `English` is the default meaning *nobody said*, so the wrong answer arrived silently |
| 🟡 3 · both the skill and the template resolve the mirror's home, exclude the git directory, and give the reason | `skills/commit-pr-convention/SKILL.md#"## Pull request bodies"@9b68f07b`, `templates/config.md#"## What no row governs"@0d626d6d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_mirrors_home_is_resolved_and_excludes_the_git_directory@4ca11cd9` | **Executed**: two parametrised cases, three assertions each — where it must not go, why, and where it does instead. Round 1's fix was prose in the file whose whole method is pinning prose, and nothing pinned it | 2026-09-04 | The three assertions are separate because the fix has three parts and any one can be edited away alone. Excluding the git directory without the reason reads as a preference to the next editor. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `deferred` in `deferred <home>` to `templates/config.md`'s list of verdict words; nothing about the mirror's home moved. **Re-read again 2026-09-05 in work item 1788613827's phase 4**, which moved `## Pull request bodies` by two paragraphs about the chain section's shape; all three assertions the two parametrised cases make are untouched |
| 🟡 7 · every committed mirror is named for a language code this file knows, checked before the body's own is excluded | `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_existing_mirrors_are_consistent_with_the_rule@e77eb16e`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_mirror_named_for_a_country_is_caught@e1492f3b` | **Executed**: the twelve mirrors are globbed and each code checked against `LANGUAGE_CODES`, and a second case asserts `kr` is not among the known codes — the check that would go green for the near miss if somebody added it | 2026-09-03 | `kr` is a country and `ko` the language. Round 1 narrowed from `set(mirrors) == {"pr.ko.md"}` to "not the body's own", which was right in principle and dropped the check that a mirror is named for a language at all |

###### What round 3 changed — two sentences and a lookup, each narrower than it claimed

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| r3 2 · a language spelled with emphasis or in another case resolves to the same code; `None` means the language is ABSENT | `tests/test_the_pull_request_language_is_the_repositorys.py#as_language_name@8877b08e`, `tests/test_the_pull_request_language_is_the_repositorys.py#mirror_to_refuse@b2cdcb1d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_language_spelled_differently_is_the_same_language@f588e5cc`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_a_language_that_is_genuinely_absent_still_answers_none@6f7c216d` | **Executed**: the exact lookup answered `None` for `**Korean**`, `korean`, `KOREAN`, ` Korean `, `_Korean_` and `` `Korean` `` — six spellings a person types into a table cell — and the normalised one answers `pr.ko.md` for all six. `French` and `Korean (KR)` still answer `None` | 2026-09-03 | Round 2's fix made `None` mean "no name to refuse" and round 3 found it also meaning "spelled differently", which made the mirror case SKIP on a message that was false. Normalisation stops at emphasis and case: resolving `Korean (KR)` would be inventing an answer rather than reading the row |
| r3 3 / r4 2 · `templates/sdd-round.md` still carries every field `ROUND_RECORD_FIELDS` expects of it, and `skills/code-review/SKILL.md` no longer claims an order the template does not have | `skills/code-review/SKILL.md#"## Cross-session records — `seal/specs/<work-item-id>/`"@986af880`, `tests/test_the_pull_request_language_is_the_repositorys.py#ROUND_RECORD_FIELDS@1e6ac0cf`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_round_template_carries_the_fields_it_is_expected_to@a270cfb5`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_sentence_no_longer_claims_an_order@308e9031` | **Executed**: one parametrised case per expected field — eleven when this was written and twelve since `Loses a record or crashes` joined the list — all present in the template; and the absence half over the skill, so the corrected sentence cannot ship beside the old one. **Read**: the template's header table and its sections interleave the order the skill's row lists, and the template carries `PR`, which that row does not name | 2026-09-03 | **What this pins is the TEMPLATE, and nothing else.** Round 4's census, taken 2026-09-03 when the list held eleven entries: the list is hand-copied from the template and checked against the template, and four of those eleven are absent from the skill while `PR` and `## Verdicts` match there only as substrings. So the row said "every field the skill names" for a check that never read the skill. Drift on the skill's side stays invisible; closing that needs a second list and a second direction, which round 4 forbade as a new unit and round 5 can weigh on its merits Re-read 2026-09-03 by the orchestrator while #96 edited the same section to say where the two todo files go: the claim's own two clauses are untouched by that paragraph — the template still carries the eleven fields and the order sentence is still absent — so only the section hash moved. A section anchor on a document under active edit drifts for reasons that have nothing to do with its claim; that is measured three times in #82 and twice here — the same section moved again in #96's round 2, and the two clauses were re-read against it again, green both times (66 cases, and the order sentence still absent). Re-read 2026-09-04 in round 1's fix pass of `1788472135-…`, which is where the count came out: the list had grown to twelve, so the claim's first clause was false as written. The count is gone from the claim rather than raised — a number in a clause is a claim that goes stale on every addition — and the census sentence above is dated to the day it was taken. `seal/ledger/1788472135-…#F7` carries what the count is now.. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which rewrote the section's paragraphs to name `round_record.py new` where the orchestrator's hand was named. The claim's two clauses are untouched by those paragraphs: the order sentence is still absent, and the template is what the fields case reads |
| r3 4 · the bootstrap bullet says the ledger's CLAUSE TABLES arrive empty, which is what the section's fourth bullet already means | `skills/implement/SKILL.md#"### Bootstrap — create what's missing"@5fd3b434` | **Read**: `templates/ledger.md` ships an `\| Item \| Value \|` table with two filled rows and placeholder rows in two more, and the live `seal/ledger.md` carries the first of them. "No rows" was false about the file it names | 2026-09-03 | Round 2 replaced "created empty" — which was true of the old state and said nothing about the source — with a sentence that named the source and was wrong about it. The meaning a session needs is that no evidence rows arrive, and the fourth bullet says exactly that |

###### What round 4 changed — the same call, three arguments later

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| r4 1 / r4 3 · the templates listing raises rather than answering `[]` when git cannot answer, and returns a path in the spelling prose can name | `tests/test_the_pull_request_language_is_the_repositorys.py#shipped_templates@b1407676`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_an_untracked_file_under_templates_is_not_a_template@fa55addf` | **Executed** in a scratch repository whose prose names both of its templates: without `core.quotePath=false` and `-z`, `git ls-files` returned `"templates/\355\225\234\352\265\255\354\226\264.md"` and the check called that file unreachable; with them it returns `templates/한국어.md` and nothing is unreachable. Without `check=True`, a path that is not a repository answered `[]` silently; with it, `CalledProcessError` at exit 128. The isolated case run with the helper stubbed to `lambda root: []` now fails on the existence assertion, where before it passed on two `== []` | 2026-09-03 | Failing toward "there are no templates" is the direction every reader in this repository is built to avoid, and it was the one this helper took. The quoting matters here beyond tidiness: this work item exists so a repository can write in a language that is not English, and a template named in that language was the one the check refused |

<!-- Round 4's own finding, which is about this run rather than any line in
it: three rounds running, the unit a fix ADDED carried the next round's
finding. This pass adds none, so the next round's usual yield surface is
empty. The pattern is recorded in `overview.md` and deferred to issue #89. -->

###### What round 5 changed — the sibling call round 4 did not reach

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| r5 3 · the corpus listing carries the same quoting arguments as the templates listing, so a prose document whose name is not ASCII or holds a space reaches the corpus | `tests/test_the_pull_request_language_is_the_repositorys.py#test_every_template_is_named_by_a_document_that_ships@9b339354`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_templates_check_reads_prose_only_and_descends@d9f15821` | **Executed** in a scratch repository whose only prose is `docs/안내.md` and `docs/two words.md`: the pre-fix listing returned `['docs/two', 'words.md', '"docs/\\354\\225\\210\\353\\202\\264.md"']` and both templates were reported unreachable; the fixed listing returns both names whole and nothing is unreachable. `.split()` splits on ANY whitespace, which is what cut the second name in two, and `unreachable_templates`'s `except OSError: continue` is what made the loss silent | 2026-09-03 | Round 4 applied these arguments to one of two sibling calls twenty lines apart. `check=True` is deliberately NOT here: the assertion on the next line already turns an empty listing red |
| r5 1 / r5 2 · the two comments say what their arguments actually do — `check=True` closes one state rather than three, and `-z` rather than `core.quotePath=false` is what turns the quoting off | `tests/test_the_pull_request_language_is_the_repositorys.py#shipped_templates@b1407676` | **Executed** by the reviewer over eight repository states and four quoting variants, re-read here against the arguments as they stand. A repository with no tracked `templates/` answers `[]` at exit 0 and that is the true answer; a stubbed helper never reaches git and the existence assertion catches it | 2026-09-03 | Comment-only, no runtime effect. The cost of the old wording was the next reader believing an empty `templates/` goes red, and pruning the wrong one of two arguments. `core.quotePath=false` is kept and now says why: it is what would be needed if `-z` were ever dropped |
| r5 3, what row 14 does NOT pin | `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_templates_check_reads_prose_only_and_descends@d9f15821` | **Executed**: a scratch copy of the file with the corpus listing's `-z` reverted passes all 66 cases. The row's assertion feeds `unreachable_templates` a clean list, so it pins that such a document is not DROPPED once it arrives, and says nothing about the listing that produces it. The listing itself runs only against this repository, whose document names are all ASCII | 2026-09-03 | Stated rather than left to be found, because the row would otherwise read as a regression pin. Closing the other half needs a listing that takes a root — a new unit, which round 4's constraint forbids and round 6 can weigh on its merits |

<!-- specs/1788398967-local-modes-records-never-leave-the-clone -->
### 1788398967-local-modes-records-never-leave-the-clone

<!-- This work item's ledger rows: one file per work item so that two branches
never queue at seal/ledger.md, folded into it at the release.

Every "Executed" here names a run whose output was read. Two rows say
"Executed" about a claim that was FALSE when it was written and is corrected
in place — the `extractall` pair — because the measurement is the row's whole
value: a later session reading only the corrected sentence would not know the
claim had ever been the other way, and would be tempted to widen it back. -->

<!-- Four rows OUTSIDE this fragment drifted on this branch and were
re-verified rather than re-pointed, because every one of their claims still
holds. Three cite `hooks/optin.py#home_at` — two in `seal/ledger.md` and one
in #80's fragment — which this work rewrote without changing what it answers.
Two cite the whole-document anchor on `templates/seal-README.md`, which this
work added a paragraph to.

The second group is #80's own 🟡 4 — *a whole-document anchor makes any edit
to that document another work item's drift, which a fragment-scoped check
cannot see* — and it has now demonstrated itself. Nothing here acts on it; it
is recorded so the next reader knows that row describes something observed
twice rather than once. -->

#### The root's two places

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S16 · both places `seal/` is read from have one address, so a command that must NAME the mode it is not in does not spell either path | `hooks/optin.py#home_paths@60b24e92`, `tests/test_optin_home.py#test_home_paths_names_the_two_places_shared_first@539620e0` | **Executed**: 42 passed in `tests/test_optin_home.py` at `f2fdcbe`, the existing 38 unchanged. Seen red first: the same call against `git show HEAD:hooks/optin.py` raised `AttributeError` | 2026-09-03 | `home_at` answers *where the root is*; `--into shared\|local` asks *where would it be*, which no existing function could answer. Spelling `<repo>/seal/` and stopping is what left a local-mode ledger unread at every commit in #80 |
| S16 · the pair costs no extra `git` call, so three gates importing this module pay nothing for it | `hooks/optin.py#home_at@702af479`, `tests/test_optin_home.py#test_home_paths_costs_no_second_git_call_when_common_is_passed@28834cfa` | **Executed**: the case replaces `optin.subprocess.run` with a raiser and passes the common directory in. **Read**: the rider on `repo_root` counts five `git` calls per gated command, three of them `rev-parse --show-toplevel` | 2026-09-03 | The old fast path looked like it skipped `git_common_dir` when the shared root existed and did not: the throwaway opt-out needed the common directory anyway. Hoisting it is what let the two places be produced in one place |

#### What leaves the clone, and what never does

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S2 · no session state beside the root reaches the zip, and the exclusion is structural rather than a maintained list | `skills/implement/scripts/seal.py#root_files@f77dd55b`, `tests/test_the_records_can_be_carried_out_and_in.py#test_no_session_state_beside_the_root_is_in_the_zip@7762309b` | **Executed**: the case builds all six markers plus the worktree-choice directory and asserts none appears in the namelist | 2026-09-03 | The list in the case is a fixture, not the mechanism. The mechanism is that the export walks the root — which is why `docs/one-root-by-lifetime.md` requires the root to be its own directory |
| S3 · a symbolic link inside the root is skipped and named, which is the one way out of that structure | `skills/implement/scripts/seal.py#root_files@f77dd55b`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_link_out_of_the_root_is_skipped_and_named@4aa9f96b` | **Executed**: a link to `specseal-implementer` is planted, the export reports the skip, and no member of the zip holds the target's bytes | 2026-09-03 | Silently dropping a file from a copy is how a copy is found incomplete on the machine that has nothing else, so the skip is reported |
| S4 · one walk feeds both the zip's members and the digests the manifest records | `skills/implement/scripts/seal.py#root_files@f77dd55b`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_zips_members_are_exactly_what_the_walk_yields@8cb48441`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_link_leaves_the_digest_alone_too@32db5a8c` | **Executed**: the namelist is compared against the walk, and a link planted inside a work item leaves its digest equal | 2026-09-03 | Two enumerations drift at the seam: the manifest would record what one saw while the reminder compares against what the other sees, and the difference reads as a work item somebody changed. Pinned from both sides |

#### The import, which never overwrites

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S6 · a differing file lands beside the existing one as `<name>.incoming<ext>` and the existing bytes are unchanged | `skills/implement/scripts/seal.py#place@73dada70`, `tests/test_the_records_can_be_carried_out_and_in.py#test_an_id_already_here_lands_beside_it_and_is_reported@5e60b111` | **Executed**: the case reads the existing file's bytes after the import, not only the report — an import that overwrote would still print a perfectly good report | 2026-09-03 | The design's own spelling, `ledger/<id>.incoming.md` beside `ledger/<id>.md`, is what the case asserts |
| S7, S8 · identical bytes are not a collision, and a second differing copy is numbered rather than overwritten | `skills/implement/scripts/seal.py#place@73dada70`, `tests/test_the_records_can_be_carried_out_and_in.py#test_identical_bytes_are_not_a_collision@573a2308`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_second_differing_copy_is_numbered_never_overwritten@80c36c9e` | **Executed**: re-importing the same zip leaves the file list identical; a third differing copy lands at `.incoming-2.md` with the first two untouched | 2026-09-03 | Without the identical case, re-importing one zip would pile `.incoming` copies of files that are already there — the noise that would teach people not to import twice |

#### `extractall`, and what refusing it actually buys

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S19 · `extractall` is NOT a path-traversal sink on the CPython this ships on — the claim this work was planned around is false | `skills/implement/scripts/seal.py#linked_path@0ebc1dda`, `seal/specs/1788398967-local-modes-records-never-leave-the-clone/spec.md#"## Fail directions / ### What `extractall` does and does not do"@2cc3c762` | **Executed** on 2026-09-03 under 3.13.9 and 3.12.11: a zip of `seal/../../escaped.md` extracted into `<dest>/seal` wrote `<dest>/seal/escaped.md` and nothing outside; `/abs.md` lost its leading slash; a link entry was written as an ordinary file | 2026-09-03 | `spec.md` and `plan.md` both asserted the sink before the probe and are corrected in place, with the measurement beside the correction. What disqualifies `extractall` is that it OVERWRITES |
| S19 · the escape that does exist is a symbolic link already in the destination — a directory above the member OR the member's own name — and this module's own writer had both | `skills/implement/scripts/seal.py#linked_path@0ebc1dda`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_linked_directory_in_the_destination_refuses_the_zip@35dd2446`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_broken_link_where_a_record_goes_refuses_the_zip@c6a5e6fa`, `tests/test_the_records_can_be_carried_out_and_in.py#test_extractall_would_have_written_through_that_link@45499c82` | **Executed**: the directory case was seen open before the smith closed it. Round 1 then measured the LEAF still open — a broken link named `ledger/w1.md` in the destination took the record outside the root at exit 0 with nothing printed, because `os.path.exists` follows a link and calls a broken one absent, so the member was ADDED and `open()` followed it out. Reproduced by the orchestrator before and after the one-line fix: exit 0 with the file outside, then exit 1 with nothing written | 2026-09-03 | The check walked `range(1, len(parts))`, which is every directory above the member and never the member. Refusing the whole import rather than the member: the link is this clone's own state, so removing it gives a complete copy on the next run instead of a partial one now |
| S1b · the export's temporary name is opened with `O_EXCL`, and the zip's own name is read as a link rather than as a file | `skills/implement/scripts/seal.py#write_zip@4fc691c2`, `skills/implement/scripts/seal.py#unused@eb454662`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_link_at_the_partial_name_refuses_the_export@d343217d`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_broken_link_at_the_zips_own_name_is_not_a_free_name@119382e7` | **Executed** 2026-09-03 at ca96676: a broken link at `seal-<repo>-<date>.zip.partial` beside the clone put the manifest and every record outside it at exit 0, printing `wrote <path>` for what was the link. Reproduced by the orchestrator before and after: exit 1, nothing outside. A broken link at the zip's own name read as free and `os.replace` removed it | 2026-09-03 | The third escape in three rounds, and the first on the export side — two rounds had only opened the import. `unused`'s docstring claimed `place`'s `.incoming` chain went through it, so the fix that moved `place` to `lexists` had no reason to visit `unused` |
| S1b · `O_EXCL` refuses to open through a symbolic link on POSIX and not on Windows, so the `lexists` check is the defence and the flag narrows the race on one platform | `skills/implement/scripts/seal.py#write_zip@4fc691c2`, `skills/implement/scripts/seal.py#write_members@7b13771a`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_export_refuses_the_link_where_o_excl_does_not_catch_it@2ad00c5c` | **Executed by CI, not here**: the windows leg wrote the manifest and every record through a broken link at `<stem>.zip.partial` at exit 0, with the flag set and nothing else in front of it (run 33715420379, 2026-09-03). Seven review rounds and the broad gate all ran on macOS. Pinned locally by stripping the flag at the call, which is that platform's effective behaviour for this shape; reverting either check reddens its own case | 2026-09-03 | The race stays open on Windows and is recorded rather than closed: there is no portable open that refuses a reparse point |
| S1b · the refusal leaves what it found. The cleanup that removes a half-written archive runs only for a name this call created | `skills/implement/scripts/seal.py#write_zip@4fc691c2`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_file_at_the_partial_name_survives_the_refusal@b17c67ee`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_link_at_the_partial_name_refuses_the_export@d343217d` | **Executed** 2026-09-03: `os.open` sat inside the try, so `O_EXCL`'s refusal ran the cleanup and removed the link, or the file, that was already there. A concurrent export's `.partial` in flight went the same way, losing that export the finished zip it was about to rename. Reproduced before and after: gone, then still there | 2026-09-03 | Round 3 graded `os.replace` removing a link at the zip's own name; this is the same removal one name over, added by the commit that fixed it |
| S19e · `os.path.isfile` is not the question `os.makedirs` answers | `skills/implement/scripts/seal.py#blocked_path@1ae38bcf`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_named_pipe_where_a_directory_goes_refuses_the_zip@224f1c75` | **Executed**: `makedirs(exist_ok=True)` raises when the name exists and is not a directory. `isfile` is False for a FIFO, a socket and a device node, so all three walked past the check — a FIFO at `seal/a` left one record on disk and lost the rest | 2026-09-03 | `lexists and not isdir` is the condition `makedirs` itself checks |
| S17d · the write loop is the one failure that cannot happen before the first byte, and it now says so | `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_directory_the_copy_cannot_write_into_stops_with_a_line_of_its_own@03d0ecf4` | **Executed**: a directory in the root with its write bit cleared raised `PermissionError` out of `write_members`, leaving a partial copy and a traceback. A full disk ends the same way. The message names what the filesystem said and that a second run finishes the copy | 2026-09-03 | Every other refusal here runs first, which is what made this the only one left uncaught |
| S20 · the format is read before the names, because a later format is what moves the names | `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_zip_whose_format_moved_the_names_says_so@fd57f89b` | **Executed**: a zip declaring format 2 with its records under `records/` answered `'records/ledger.md' is not under seal/`, which reads as a malformed zip where the truth is a build too old. The case first asserted `"format" in out` and passed against the defect, because the message prints the path and the fixture was named `format-2.zip` — caught by reverting the order | 2026-09-03 | The manifest's size moved out of `unsafe` into `import_` for this, since the bound has to be known before the manifest is read |
| S17c · a manifest another machine wrote is read for its type, not only for its presence | `skills/implement/scripts/seal.py#normalise_remote@3c973d8a`, `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_manifest_missing_exported_at_still_ends_with_a_line_of_its_own@c82a6f65`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_manifest_field_of_the_wrong_type_does_not_raise@f202e146` | **Executed** 2026-09-03 at 3f2a7b1: `head` present without `exported_at` raised `KeyError`, and `head` as a number or a bool raised `TypeError` — both at the closing line, AFTER every record was written, so the person saw exit 1 and a traceback for a copy that had succeeded. `remote` as a list or a dict raised `AttributeError` before any write | 2026-09-03 | `read_manifest` checks that the manifest is an object and that its `format` is one this build reads; every other field was trusted for its type. Exit 1 reads as *nothing happened*, which is the one thing that was not true |
| S19b · `manifest.json/` is the same member as `manifest.json` to both the exemption and the bound | `skills/implement/scripts/seal.py#unsafe@357fdfa7`, `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_manifests_other_spelling_gets_the_size_bound_too@b27e812f` | **Executed**: `unsafe` exempted `name.rstrip("/")` and the size bound matched `info.filename` exactly, so that spelling declaring 400 MB passed both. `writestr` recomputes `file_size`, so the case writes the declared size into the central directory afterwards | 2026-09-03 | One question answered by two tests, which is `unused` and `place` again: the fix that moved the bound out of `unsafe` visited one of the pair |
| S17c · a member this build cannot read at all answers with a line of its own, not a traceback | `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_member_this_build_cannot_decompress_refuses_the_zip@48118c6a`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_data_is_read_before_the_manifest_is@22874c07` | **Executed**: an encrypted member left `testzip` as `RuntimeError` and compression method 99 as `NotImplementedError`, both tracebacks. A bad CRC on `manifest.json` left `read_manifest` as `BadZipFile`, whose docstring said it never raises. `testzip` catches `BadZipFile` itself, so the clause written around it could not fire | 2026-09-03 | The order is the fix, and an order is what no case pinned: moving `read_manifest` back above `testzip` reddened nothing until these two cases existed |
| S19e · a name that has to be a directory for the zip and is a file refuses the whole zip, from either side | `skills/implement/scripts/seal.py#blocked_path@1ae38bcf`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_member_under_a_member_refuses_the_zip@24c925c0`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_file_the_root_already_holds_blocks_a_member_under_it@555f940a` | **Executed**: a zip holding `seal/a` and `seal/a/b.md` left two records on disk, lost the one after them, and raised `FileExistsError` out of `os.makedirs` — which sits outside the try the corrupt-member fix added | 2026-09-03 | The sender corrupts nothing, only names two members that way; and the root's own contents raise it too, so a check over the zip's names alone would miss half of it |
| S19f · the member count is bounded, because neither byte bound sees a zip that is only a count | `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_zip_of_more_members_than_a_root_holds_refuses_it@d0412ebc` | **Executed**: a 31 MB zip of 300,000 zero-byte members wrote 300,002 files into the root at exit 0 in 34.5 s. A member declaring zero bytes passes the member bound and adds nothing to the total | 2026-09-03 | The class the two limits were added to close, entering on the axis neither measures |
| S19g · a record `O_EXCL` turns down is named rather than dropped | `skills/implement/scripts/seal.py#write_members@7b13771a`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_write_itself_refuses_a_name_that_became_a_link@fecb9953` | **Executed**: the pin hands `write_members` the name a raced check would hand it and asserts the name comes back in `refused`. Reverting the report reddens it | 2026-09-03 | The report said `0 files added`, which a person reads as an empty zip rather than as a name something took in between |
| S19c · the escape reaches past the member's own name to the `.incoming` sibling `place` falls back to, and both are refused | `skills/implement/scripts/seal.py#place@73dada70`, `skills/implement/scripts/seal.py#write_members@7b13771a`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_broken_link_at_the_fallback_name_refuses_to_be_written_through@53d30fe1`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_write_itself_refuses_a_name_that_became_a_link@fecb9953` | **Executed** 2026-09-03 at ee97c8c: a real `ledger.md` with differing bytes plus a broken link at `ledger.incoming.md` put the record outside the root at exit 0, printed as an ordinary collision. Reproduced by the orchestrator before and after. Two layers: `place` reads a candidate with `lexists` so a broken link counts as taken, and the write opens with `O_EXCL` so no write follows a link even if the name became one after the check | 2026-09-03 | Round 1 closed the member's own name. `place` writes to a name it falls back to, and `linked_path` never sees the fallbacks — the same escape one candidate over |
| S19b · the count, the total and the manifest's own size are read before the manifest is decoded; the member bound is read after it, because the names move with the format | `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_an_archive_declaring_more_than_a_root_refuses_the_zip@e6d236c3` | **Executed**: 20 members of 31 MB, each passing `unsafe` on its own, refused at 650117147 bytes declared with the root unchanged. The manifest is exempt from the member limit and `read_manifest` reads it whole, so a 400 MB `manifest.json` took 400 MB of memory before anything refused it — the total is now summed first | 2026-09-03 | Round 1 added the member limit and round 2 measured what it does not cover |
| S17b · a member whose data is corrupt refuses the whole zip rather than writing the records before it | `skills/implement/scripts/seal.py#import_@7f984dcb`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_member_whose_data_is_corrupt_writes_none_of_the_others@673b6fd0` | **Executed** before the fix: a bad CRC on the second of two records left the first on disk and raised `BadZipFile` with no line of this command's own printed. A zip truncated on disk was already refused; corruption inside a well-formed central directory was not | 2026-09-03 | `testzip` streams and holds nothing, and the archive bound above it is what makes reading every member affordable |
| S19b · a member's declared size is the sender's choice, and each member is read whole | `skills/implement/scripts/seal.py#unsafe@357fdfa7`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_member_declaring_more_than_a_record_refuses_the_zip@b0d0d392` | **Executed** on 2026-09-03: a 408 KB zip declaring 400 MB in one member wrote 419 MB and added as much to memory in 0.2 s, at exit 0. The case reverts the limit and reddens | 2026-09-03 | The smith recorded this as a limit rather than a defect, on the grounds that a record is markdown. The size is chosen by the machine that sent the zip, not by this root, and both READMEs tell a person to accept one |
| S4 · a repository with no commit records an empty `head`, because the helper reads git's exit code and not its output alone | `skills/implement/scripts/seal.py#git@15fb1306`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_repository_with_no_commit_records_no_head@e4ee0ffd` | **Executed** on 2026-09-03: `git rev-parse HEAD` on an unborn branch exits 128 and prints `HEAD` on stdout, which the manifest recorded as the SHA and the import printed back as one. Empty after the fix | 2026-09-03 | A string that is not a SHA is one a reader cannot tell from a SHA, which is why the spec says empty |
| S19 · every member's name is validated anyway, and one bad member refuses the archive before a byte is written | `skills/implement/scripts/seal.py#unsafe@357fdfa7`, `tests/test_the_records_can_be_carried_out_and_in.py#test_a_member_that_climbs_out_refuses_the_whole_zip@cfe71efd` | **Executed**: six member shapes, each asserting the root's file list is unchanged and that no file appeared outside it | 2026-09-03 | Kept although `extractall` would sanitise most of them: a defence that holds only while a standard-library sanitiser keeps its shape is not one this file can claim, and a refusal says what happened where a silent sanitise leaves a name nobody chose |

#### The reminder, and the gap it does not paper over

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S13 · the reminder is exactly one line, and writes nothing | `skills/implement/scripts/seal.py#check@abf017d7`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_reminder_is_one_line_and_writes_nothing@95111a80` | **Executed**: the case compares the WHOLE of stdout rather than searching it, because *and nothing else* is half the requirement, and asserts the root's file list is unchanged | 2026-09-03 | At N=1 it reads `1 work items`, which is the design's own wording. Reading a quoted line as a template with a grammar rule attached would be changing an acceptance criterion; `overview.md` records it for the owner |
| S13 · a root-level file changing reports 0, and that is a known gap rather than an oversight | `skills/implement/scripts/seal.py#work_item_of@85506870`, `tests/test_the_records_can_be_carried_out_and_in.py#test_the_reminder_ignores_a_root_level_file@c955db4e` | **Executed**: `follow-up.md` edited after an export still reports `0 work items changed since the last export` | 2026-09-03 | Q1 in `questions.md`, with the owner named. The line the design fixes says *work items*, and counting more would need the wording changed — which is the owner's call, not this command's |

<!-- specs/1788411058-the-mode-is-two-shell-lines-in-a-readme -->
### 1788411058-the-mode-is-two-shell-lines-in-a-readme

<!-- This work item's ledger rows: one file per work item so that two branches
never queue at seal/ledger.md, folded into it at the release.

The rows split in two. The first table is about a rule that is PROSE — the
row is declared state and nothing at runtime may read it — so its code
grounds are the module that does the resolving and the case that pins the
absence. The rest are behaviour, and "Executed" there means the case was run
against the shipped code.

Two rows rest on measurements rather than on reading: what `git rm -r
--cached` does to a staged edit, and what each of the two shipped checkers
does in a repository with no `seal/`. Both were run on 2026-09-03 and both
changed what the code does. -->

###### The row is declared state, and the folder still decides

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S-row · `seal/config.md`'s `Mode` row says where the root SHOULD be; `hooks/optin.py#home_at` says where it IS, and stays the only reader of that question | `skills/implement/scripts/seal.py#declared@1c170f62`, `hooks/optin.py#home_at@702af479`, `tests/test_the_mode_is_a_row_and_a_command.py#test_the_report_names_the_folder_the_row_and_whether_they_agree@997faec8` | **Executed**: the report names both and the cases assert what the FOLDER holds after every switch, never the row's word for it. **Read**: that no hook opens `config.md` — `grep` over `hooks/` finds no reader, and this row is what a later one would have to overturn | 2026-09-03 | A gate that trusted the row would open a place with no folder, and `optin` is documented to fail toward "not opted in" rather than toward a guess |
| S2 · the row has NO default. An absent one is written from where the folder is, which is the state of every repository that has a `config.md` today | `skills/implement/scripts/seal.py#mode_report@94f925b8`, `templates/config.md#"## Mode"@325a82df`, `tests/test_the_mode_is_a_row_and_a_command.py#test_an_absent_row_is_written_from_where_the_folder_is@113b9e72` | **Executed**: eight cases, four absent shapes × both modes. Running it in one mode only would pass for a fixed default of `shared`; the mutation that writes `shared` regardless reddens four of them | 2026-09-03 | The first-setup question's default (shared, offered first) is a different thing: it applies where the folder exists at NEITHER place, and it is an answer a person gives rather than a value code falls back to |
| S5 · a value that is neither mode is a claim nobody can act on — reported, fails `--check`, refuses `--apply`, and never overwritten | `skills/implement/scripts/seal.py#declared@1c170f62`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_value_that_is_not_a_mode_is_a_claim_nobody_can_act_on@570b5fff` | **Executed**: the report leaves the row as it found it, which is the half that matters — treating it as "absent" would write over what a person meant | 2026-09-03 | The four ways of NOT declaring (absent, empty, unparseable, no file) all pass; this fifth shape is not one of them |

###### What the switch touches, as a class

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S15 · the guard refuses what the INDEX can lose and names an untracked file as travelling. `git rm -r --cached` drops a staged edit out of the index and prints nothing | `skills/implement/scripts/seal.py#indexed@1ff97a21`, `skills/implement/scripts/seal.py#porcelain@a9cc6d66`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_change_the_index_carries_refuses@7343ce5d`, `tests/test_the_mode_is_a_row_and_a_command.py#test_an_untracked_file_travels_with_the_folder@beeacc70` | **Executed** 2026-09-03, four tree states against `git rm -r --cached seal`: clean, a staged modification, a worktree modification, and an untracked extra. All four exit 0 and leave every file on disk; the staged one loses its index content silently. That measurement is the guard's whole grounds, and it is also why untracked does not refuse | 2026-09-03 | Refusing over an untracked file made the ordinary first run refuse: the bare command writes an absent row, and the switch met the file it had just written |
| S10 · a pure deletion is allowed once the move has already happened, and only then — the shape a stopped run or the README's by-hand `mv` leaves | `skills/implement/scripts/seal.py#indexed@1ff97a21`, `skills/implement/scripts/seal.py#refusals@495488d6`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_stopped_run_is_finished_by_a_second@7bd5d983` | **Executed**: the case reproduces the by-hand state (`os.rename`, index untouched) and the second run finishes it. `MD` and `AD` still refuse | 2026-09-03 | `hooks/root-migrate.py#dirty` makes the same exception in the same words: a resume has to see past its own earlier steps |
| S18 · the guard watches the workflow path as well as the root, because the switch stages both | `skills/implement/scripts/seal.py#refusals@495488d6`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_dirty_workflow_path_refuses@f15f66ff` | **Executed**: the mutation that watches only the root reddens exactly this case | 2026-09-03 | #81 closed "a path this command writes through" three times, one name apart each time. The five paths are enumerated in `spec.md` §"What the switch touches" rather than fixed where a finding points |
| S16 / S17 · `lexists` at the destination and `islink` at the source, so a link is never moved and never written through | `skills/implement/scripts/seal.py#refusals@495488d6`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_destination_that_already_exists_refuses@62a9068e`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_source_that_is_a_symbolic_link_refuses@6d81a616` | **Executed** on macOS, three destination shapes including a broken link, which `exists` reads as absent. **Not run** on Windows, where the link cases skip for want of the privilege | 2026-09-03 | The same pair `write_zip` and `place` were fixed for, one command over |

###### The workflow file, which is the half a `mv` leaves behind

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S21 · a workflow left behind after a switch to local is not untidiness. The two shipped checkers fail in OPPOSITE directions in a repository with no `seal/` | `skills/implement/scripts/seal.py#remove_workflow@dcb7ea33`, `tests/test_the_mode_is_a_row_and_a_command.py#test_shared_to_local_removes_the_workflow_this_plugin_wrote@707ee74e` | **Executed** 2026-09-03 in a repository with no root: `unverified_check.py --baseline <ref> seal/specs/` exits **2** ("no such path"), `chain_check.py --baseline <ref>` exits **0** having examined nothing. So the leftover is a build red forever beside a check reporting a pass it never earned | 2026-09-03 | `templates/hygiene.yml`'s header already stated both exit codes correctly; nothing acted on them. A handoff to this work item asserted both pass, and the measurement is what corrected it |
| S22 / S23 / S25 · the file is removed only when it is this plugin's, written only when the path is free, and not written at all when the version cannot be read | `skills/implement/scripts/seal.py#install_workflow@b16c89ba`, `skills/implement/scripts/seal.py#remove_workflow@dcb7ea33`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_workflow_this_plugin_did_not_write_is_left_alone@e1b458a8`, `tests/test_the_mode_is_a_row_and_a_command.py#test_a_version_that_cannot_be_read_writes_no_workflow@b5144173` | **Executed**: the marker is the plugin's clone URL, and a case pins that `templates/hygiene.yml` still carries it, so the two cannot drift apart. The version case removes `plugin_version` alone — removing the whole plugin root took the template with it and passed through the wrong branch | 2026-09-03 | A workflow written with `v<version>` still in it fails CI's `git clone --branch` at the first pull request, which is worse than no workflow |

###### What a person is told

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S12 · local → shared is said to be the door before it acts, and the door is the COMMIT rather than the move | `skills/implement/scripts/seal.py#switch@0b1b6da7`, `docs/one-root-by-lifetime.md#"### Shared or local"@a47b3694`, `tests/test_the_mode_is_a_row_and_a_command.py#test_going_to_shared_says_the_commit_is_the_door@e5ff9e66` | **Executed**: the line is asserted to precede the instruction to commit, and to name the way back. The command stages and never commits, which is what keeps that moment the person's | 2026-09-03 | The direction local mode exists to prevent is the one that cannot be walked back. `questions.md` Q3 records that this is a line rather than a flag, and what would overturn it |
| S26 · a clone with more than one worktree is named rather than refused | `skills/implement/scripts/seal.py#other_worktrees@862f0a67`, `tests/test_the_mode_is_a_row_and_a_command.py#test_another_worktree_is_named@ce0815d1` | **Executed** 2026-09-03: switching shared → local from a linked worktree leaves the main worktree holding the committed root on its own branch, so the two read different roots until the commit reaches both. Nothing is lost — the other copy is the committed one — and it heals at checkout | 2026-09-03 | Refusals here are for states where something would be lost or half-moved. `questions.md` Q7 has the alternative |
| S12 · every string this command prints for a person to paste works from any directory, and under the failure its own message diagnoses | `skills/implement/scripts/seal.py#switch@0b1b6da7`, `skills/implement/scripts/seal.py#install_workflow@b16c89ba`, `tests/test_the_mode_is_a_row_and_a_command.py#test_the_recovery_commands_work_where_they_are_printed@a75063a7`, `tests/test_the_mode_is_a_row_and_a_command.py#test_the_way_back_it_names_works_from_a_subdirectory@ec9fd6fd` | **Executed** 2026-09-03 on git 2.50.1: all eight printed commands enumerated and run from a subdirectory. Before the fixes, five exited 128 with `did not match any files` or exited 0 having done nothing; one failed at the repository root too, under the ignore rule its own message diagnoses, for want of `-f`. `:/` resolves to the top of the worktree it is run in — inside a linked worktree it means that worktree's top and leaves the main one's index alone | 2026-09-03 | Four rounds fixed one member of this at a time: two sentences, then three commands. The enumeration is what closed it |

<!-- specs/1788420760-a-language-row-that-governs-four-things -->
### 1788420760-a-language-row-that-governs-four-things

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`. -->

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| S1 · the commit row is renamed and widened, and the skill reads it before writing any of the five surfaces | `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_skill_names_the_file_and_the_row@cbca742e` | **Executed** 2026-09-03: the 66-case file passes against the renamed row, and the case reads the skill as text rather than trusting it | 2026-09-03 | The name understated the row from the day it shipped — it has governed the commit subject and body since #82 |
| S3 · the two rows are independent, and the template says why | `templates/config.md#"## Record language"@ad683369` | **Read** — no code reads either row, so what can be checked is that the file says it. The rule it rests on is the file's own: an absent row's default is what every repository had before the row existed | 2026-09-03 | A row inheriting another's value would break that sentence, and somebody who set one did not ask for the second |
| S4 · every way of not naming a language lands on English, per row | `tests/test_the_pull_request_language_is_the_repositorys.py#configured_language@4e389a05` | **Executed**: four shapes — no file, no row, an empty value, a file that does not parse — each answering English | 2026-09-03 | Unchanged from #82 and now asked of two rows |
| S6 · what a checker reads literally stays English, and the list is in three places a session actually opens | `templates/config.md#"## What no row governs"@0d626d6d`, `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269` | **Executed**: the prefix and ASCII exclusions are asserted in both the skill and the template, because whoever writes a repository's config reads the second and never the first | 2026-09-04 | A translated `Fixes checked by` is not a translation; it is `chain_check.py` no longer reading the record. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the `deferred` in `deferred <home>` to the section's list of verdict words; the sentence this row pins is untouched |
| Q1 · the posted review report follows the commit row, not the record row | `seal/specs/1788420760-a-language-row-that-governs-four-things/spec.md#"## Two rows, and why not one and not seven"@6750ffbc` | **Read**: `skills/code-review/SKILL.md` writes the three files *"right after posting the report"*, and says elsewhere that the user does the posting — two acts, two texts. The first draft of #106 assumed one text and put the report with the records | 2026-09-03 | Recorded because the corrected sentence alone would not tell the next reader that the premise was tested |

<!-- specs/1788420761-the-settings-live-in-a-file-nobody-opens -->
### 1788420761-the-settings-live-in-a-file-nobody-opens

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`. -->

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| S1 · the skill names every row the template ships, present or absent, with its default | `skills/config/SKILL.md#"## Procedure"@9d22da3e`, `tests/test_the_settings_have_a_front_door.py#test_the_skill_names_every_row_the_template_ships@e4f22d43` | **Executed**: the case reads both the skill and the template as text and fails if either stops carrying a row, so adding a fourth row to one and not the other is caught | 2026-09-03 | A row a repository never set is the most likely one somebody wants to change |
| S2 · the mode row is routed to `seal mode` rather than read by the skill | `skills/config/SKILL.md#"## Procedure"@9d22da3e`, `tests/test_the_settings_have_a_front_door.py#test_the_mode_row_is_routed_and_not_read@637d3925` | **Executed**: the case asserts the skill names `seal mode`, says why it routes, and spells no `mv` of its own | 2026-09-03 | `seal mode --check` is what the pull-request checks run; a second reader of the row is a second answer |
| S5 · the direction that cannot be undone is named before the command runs, not after | `skills/config/SKILL.md#"## Before switching to shared, say what cannot be undone"@3a38d6a0`, `tests/test_the_settings_have_a_front_door.py#test_the_one_way_door_is_named_before_it_is_walked_through@2d20a6f6` | **Executed**: the case asserts the sentence, the timing, and that the way back is spelled with `:/` | 2026-09-03 | The commit is the point of no return, not the move — and local mode exists for exactly the repository this direction hurts |
| S6 · a repository with no root is told so and nothing is created | `skills/config/SKILL.md#"## Where the file is"@3fefcc1c`, `tests/test_the_settings_have_a_front_door.py#test_a_repository_with_no_root_is_not_opted_in_by_looking@8fcdc9dd` | **Executed** | 2026-09-03 | The root's presence at one of two places is the opt-in, so creating the file here would opt somebody in from a command they ran to look |
| S7 · the bootstrap names this skill as the way back to the questions it asks once | `skills/implement/SKILL.md#"### Bootstrap — create what's missing"@5fd3b434`, `tests/test_the_settings_have_a_front_door.py#test_the_bootstrap_names_the_way_back_to_its_own_questions@ca656c5c` | **Executed** | 2026-09-03 | A person answering a batch has no reason to expect a way back to it |

## 0.6.0 — 2026-09-03

<!-- specs/1788433011-every-spawn-prompt-is-retyped-from-memory -->
### 1788433011-every-spawn-prompt-is-retyped-from-memory

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Five rows, and the last of them is a defect this work item produced and its
own last phase found. Recorded rather than repaired quietly, because the
mechanism that let it through is the general lesson and the fix is one line. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| L1 · `skills/agent-contract/SKILL.md` is the single home for every rule true of all agents, and a definition that carries a section's own sentences fails rather than passing unnoticed | `tests/test_a_moved_rule_leaves_its_definition.py#test_no_definition_carries_a_sections_own_sentences@546722ba`, `skills/agent-contract/SKILL.md#"## How the sections are numbered"@838b3c7d` | **Executed**: the case is parametrised on both globs, so it is one case per section per definition and a §17 or a fourth agent is checked on the day it lands. Its mutation — a section's body pasted back under a new heading — is red for every pair | 2026-09-03 | The half of Q2 that says *the definition keeps only its own application* had nothing behind it until this module. Measured by the orchestrator at `e51fd0b`: §9 pasted into `agents/warden.md` left `test_broad_gate_rule`, `test_edits_go_through_the_edit_tool`, `test_every_agent_reads_the_contract` and `test_the_agent_contract_holds_the_universal_rules` all green, 81 passed, because every assertion in them is a presence check |
| L2 · the duplication check is a 15-word verbatim window, and both ends of its margin are measured rather than chosen | `tests/test_a_moved_rule_leaves_its_definition.py#test_the_window_sits_between_what_was_measured@412468cc` | **Executed**: the longest run any KEPT application shares with any section is **10** words — `agents/smith.md`'s *a bare word is a pathspec and git rejects it*, which §8 and the design gate's waiver paragraph both spell because both are about one git fact. The smallest run a REAL duplication produced, measured on the same files before phase 4 removed them, is **25** (§10), then 31 (§9), 37 (§16), 59 (§11) and 66 (§3) | 2026-09-03 | This is the number a later session would otherwise re-derive, and re-deriving it needs the pre-phase-4 files. The case prints both bounds and fails when either end of the margin closes, so raising the window to quiet a failure is red rather than silently mis-tuned. Verbatim is deliberate: a paraphrase check would have to decide when two sentences say the same thing, and it would refuse the application prose the rule exists to permit |
| L3 · the protocol is a pointer, and the sentence a prompt is actually governed by is what stands where the rules stood | `docs/review-handoff-protocol.md#"## What every spawn prompt used to carry / ### What a prompt is left holding"@2de7af10`, `docs/review-handoff-protocol.md#"## What every spawn prompt used to carry / ### Where each half went"@4fe0b4fb`, `tests/test_the_handoff_before_round_one.py#test_the_protocol_no_longer_states_the_rules_it_points_at@7f94150d` | **Executed**: the presence case asserts the section names the contract path, names `agents/<name>.md`, and still says *a prompt carries what is specific to the round and nothing else*; the absence case asserts five phrases verbatim from what phase 5 removed, so a paste-back is red rather than invisible | 2026-09-03 | The section title moved with the content — *What every spawn prompt carries, until the definitions carry it* asserted an interim state that stopped being true in the same commit. A heading is what a reader scanning the document acts on, and one document named the old title |
| L4 · the orchestrator is bound by the same contract, and the two skills it reads are where that is written | `skills/code-review/SKILL.md#"## Orchestrator: a fix pass resumes the implementer"@c0dc63a1`, `skills/implement/SKILL.md#"load it.** A session driving the work directly reads this skill and never"@21ad749e` | **Read**, both paragraphs, this session. Nothing at runtime reads either — the binding is prose that a session obeys — so what can be checked is that the two files say it and that neither restates a rule the contract holds. **Executed** 2026-09-03, re-measuring what phase 5 recorded: each added paragraph's longest run against any section is **4** words, not the 8 that work item's memo claimed, and both are far under L2's 15-word window | 2026-09-03 | `agents/*.md` cannot reach this party, and the headline failure was an orchestrator breaking a rule it had put into every prompt it sent. `user-invocable: false` permits the load and does not oblige it, so the obligation has to be written where the orchestrator reads. The `implement` coordinate is a line anchor rather than a heading path because the paragraph sits above the first `##` and the enclosing heading is the whole 741-line file — a hash over that would drift on every unrelated edit, which is the failure a coordinate exists to avoid. **What the re-measurement also found, and this work item did not decide:** measured over the WHOLE of `skills/implement/SKILL.md` rather than the added paragraph, the file shares a 30-word sentence with §5 — *is not a coordinate: the number can be checked while the claim it stands for cannot, and one such fact reached five documents before a review round found it false*. That prose predates the contract and Q2's answer scoped the single-home rule to `agents/*.md`, so nothing here is red; it is recorded because the next reader measuring the file rather than the paragraph will find the same 30 and have no way to tell it was seen. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added the fix-table paragraph to the review skill's section; the sentence binding the orchestrator to the contract is untouched, and the `implement` line anchor did not move |
| L5 · a rule with one written home is deleted by the move that was built to stop rules going missing, and an absence check alone cannot see it | `agents/smith.md#"## Phases"@194f9b4a`, `tests/test_the_handoff_before_round_one.py#test_the_smiths_definition_mandates_mutating_every_unit_it_added@6a971c8f` | **Executed** 2026-09-03: at `dcdf4e4` a search for *mutation-test every unit added* across `agents/`, `skills/`, `docs/`, `templates/`, `CLAUDE.md` and `CONTRIBUTING.md` returned nothing. The new presence case is shown red four ways — the rule deleted, the cadence changed, the timing dropped, the observation dropped — and the first draft's `one at a time` assertion PASSED under the second, because the design gate holds that phrase in an unrelated sentence a hundred lines up | 2026-09-04 | The row above it in the same module asserts the phrase has LEFT the protocol, and it went green over a rule that had left the repository altogether. That is the general claim: an absence check with no presence check beside it cannot tell a rule that moved from a rule that was deleted. The other four rows of that table point at contract sections, which `test_the_agent_contract_holds_the_universal_rules.py` pins from the other side; this one pointed at a definition and nothing pinned it. Contract §15 is the neighbour and not the rule — it asks that one case be seen red on the day it is planted, where this asks that every unit a branch added be broken before the branch leaves. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**: the region gained the fix-pass paragraph at step 3 — the `## Fixes` table, one row per OPEN finding, a correction closing `answered — corrected at <sha>` — the reach-back sentence naming `round_record.py new`, and the depth measured by `close` rather than declared. The mutation rule at step 4 is untouched |
| L6 · the record-language instruction has two homes and neither is a definition — the whole list in `implement`, the short form as contract §11 | `skills/agent-contract/SKILL.md#"## §11 The language the records are written in"@bdd6d99f`, `skills/implement/SKILL.md#"## The language the records are written in"@f307fad8` | **Executed**: the suite over the modules that read the definitions, 604 passed at `5e610a3` | 2026-09-03 | Replaces row S2 of work item 1788420760, removed from `seal/ledger.md` by this branch. That row's grounds named `agents/smith.md` and `agents/warden.md`, and phases 3 and 4 moved the section out of both — the coordinates read BROKEN, which is CI's failing exit rather than its warning |
| L7 · the probe rules and the git-from-Python rule are contract §7 and §8, and the definitions point rather than restate | `` `skills/agent-contract/SKILL.md#"## §7 A probe is named `test_tmp_*`, one file, run once, deleted"@9ac6eed4` ``, `tests/test_a_probe_that_commits_says_so.py#test_the_contract_carries_all_three_shapes@287934dc`, `tests/test_a_probe_that_commits_says_so.py#test_the_fact_finder_points_at_the_rule_it_no_longer_restates@9a67764a` | **Executed**: the module's cases now read the contract for the shapes and the definition for the pointer, which is the re-pointing Q2 answered `full` to | 2026-09-03 | Replaces the row under *An agent can write a probe that commits*, which claimed BOTH agent files carry the three shapes. `spec.md` records why that claim was always half wrong: the smith writes probes too and nothing said so anywhere, so the rule reached two agents of three |
| L8 · the contract-override refusal stands ONCE, as §3, with the 28-minute case named in it | `skills/agent-contract/SKILL.md#"## §3 A spawn prompt cannot widen the verification scope"@207e4d1d`, `tests/test_broad_gate_rule.py#test_the_refusal_is_stated_once_and_names_both_directions@174490ef`, `tests/test_broad_gate_rule.py#test_the_prohibition_itself_has_one_home_and_it_is_the_contract@321739a9` | **Executed**: both cases green at `5e610a3`, and the second is the one that would go red if a definition took a copy back | 2026-09-03 | Replaces the row claiming the refusal *stands in both agent contracts*, whose five smith coordinates and one warden coordinate all drifted when phases 3 and 4 removed the prose. The word that changed is *both* → *once*, and it is the whole point of #107 |
| L9 · §10 holds the batching rule and the serial-loop caveat; each definition holds only its own number | `skills/agent-contract/SKILL.md#"## §10 Batch independent reads and runs"@fdc474d5`, `tests/test_the_handoff_before_round_one.py#test_the_smiths_contract_does_not_demand_what_a_serial_loop_cannot_give@f69a6137` | **Executed**: the case asserts the caveat against the CONTRACT and the number against the DEFINITION, which is where each can go missing without the other noticing | 2026-09-03 | Replaces the row claiming the expectation is *stated where it pays and caveated where it cannot be met* in the two definitions. The split is deliberate: 1.89 judges a reviewer and 1.08–1.17 is what an edit-test loop gives, so the figure stays with the agent it measures |
| L10 · the `seal/…` root sentence is contract §16 and the skill's layout section, and the definitions gave it up | `` `skills/agent-contract/SKILL.md#"## §16 Where a `seal/…` path resolves"@bd939022` ``, `skills/implement/SKILL.md#"## Document layout — two roots, three lifetimes"@0ac22785`, `tests/test_first_setup_asks_once.py#test_the_session_rule_appears_once@1b472f16` | **Executed**: the parametrised case asserts the sentence appears exactly once per file it belongs in | 2026-09-03 | Replaces row Q1 of work item 1788398967, which claimed the sentence sits *once at the top of each agent*. Phase 1's divergence row holds why §16 exists at all: §11 sends an agent to `config.md` under the `seal/` root and never says where the root is, so the contract could not be followed as it stood |

## 0.7.0 — 2026-09-03

<!-- specs/1788445862-a-phase-hands-the-next-one-a-record -->
### 1788445862-a-phase-hands-the-next-one-a-record

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Five rows. This work item is templates, agent instructions, skill wiring and
tests — no hook, no gate, no checker — so most of what it built is already
pinned by the two new test modules and needs no coordinate of its own. What
earns a row here is what a future session could not cheaply re-derive from
those tests alone: a naming split that reads as an inconsistency until you
open the reasoning behind it (P3), and a testing pitfall this branch caught
in itself before it shipped (P5). -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| P2 · `agents/smith.md` and `skills/implement/SKILL.md` instruct writing `phases/phase-N.md` from `templates/sdd-phase.md` at each phase's close, and the SDD file-set table carries the row | `` agents/smith.md#"**At each phase's close, also write `seal/specs/<work-item-id>/phases/phase-N.md`"@aec5b02f ``, `` skills/implement/SKILL.md#"**What goes into `phases/phase-N.md`'s `## What this phase was asked`"@a1748882 ``, `tests/test_a_phase_hands_the_next_one_a_record.py#test_smith_is_told_to_write_the_phase_record@49d908c6`, `tests/test_a_phase_hands_the_next_one_a_record.py#test_skill_file_set_table_carries_the_phase_row@7689192c` | **Executed**: all four cases pass at this branch's HEAD | 2026-09-04 | Phase 2's own record notes the instruction was read cold by its own author but not tested cold — this branch's phase 3 is the first spawn that reached "write `phases/phase-N.md`" without the extra reminder already in its own task, and it did (phase-3.md's field table, `Commit: c5ad90e`). **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**: the anchored paragraph's own text is unchanged — `git diff` removes no line of it — and the fix-pass paragraph inserted directly after it is what moved the hash. The instruction to write `phases/phase-N.md` stands, and the new paragraph says a fix pass writes none |
| P3 · `templates/sdd-phase.md`'s own path comment keeps the bracketed `phase-<N>.md` form, matching `templates/sdd-round.md`'s own `round-<N>.md`; every file that only points at either record from prose uses the bare `phase-N.md`/`round-N.md` form instead — a deliberate split, not an inconsistency to flatten | `` templates/sdd-phase.md#"<!-- seal/specs/<unix-epoch-seconds>-<slug>/phases/phase-<N>.md — what this phase"@c215c92d ``, `tests/test_a_phase_hands_the_next_one_a_record.py#test_the_phase_templates_own_path_comment_matches_the_round_templates@ad73b8a4`, `tests/test_a_phase_hands_the_next_one_a_record.py#test_the_phase_record_path_is_spelled_the_same_in_every_prose_carrier@91fa7d0c` | **Executed**: the parametrised carrier case and the template's-own-comment case both pass at this branch's HEAD | 2026-09-04 | Read literally, `plan.md`'s Phase 2 row asks for one spelling across "all four files" and would fail against the real templates. `templates/sdd-round.md` already spelled its own path bracketed before this branch touched the file, so matching it exactly is what phase 2 and phase 3 both record as the reasoning — the closest thing this branch has to a defect a future session could "fix" wrongly by making the four files byte-identical |
| P5 · a cross-file consistency test that compares two hard-coded Python string constants to each other, instead of reading each template's actual heading text through a shared regex, cannot be shown red | `tests/test_a_segments_record_says_what_it_was_asked.py#SKELETON_RE@a7072e40`, `tests/test_a_segments_record_says_what_it_was_asked.py#test_the_heading_skeleton_matches_across_round_and_phase@9af431bc` | **Executed**: the case passes at this branch's HEAD. The first draft compared `ROUND_HEADING`/`PHASE_HEADING` constants to each other and stayed green even with both templates reverted to their pre-phase-3 wording, because nothing in it opened a file; fixed by matching `SKELETON_RE` against the real template text (phase-3.md, "What this phase found") | 2026-09-04 | The general form is the `verify` skill's "a check that cannot fail is a counterfeit seal" — the same shape as agent-contract §15's rule that a new case must be seen red before it is planted, applied here to the test's own construction rather than to the production code it covers |

## 0.8.0 — 2026-09-05

<!-- specs/1788472135-the-run-outlives-its-last-finding -->
### 1788472135-the-run-outlives-its-last-finding

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Twelve rows. Most of what this branch built is prose in six files, and two new
test modules read it sentence by sentence, so a coordinate for each of those
would be an inventory of the diff rather than a claim a later change can
break. What earns a row is inside `chain_check.py`: a grandfathering that is
three different excusals under one name (F1), a cutoff that is deliberately
not the cutoff beside it and a case that pins the three together anyway (F2),
and narrow readings a later tidy-up would widen in good faith, each of which
only mutation or a review round found (F3, F6, F8, F9, F10, F11). F12 is the
class round 2's 🔴 1 belongs to, and the case that would have caught it. F5 is the one
that is not about this checker at all — it is why the floor's answer words had
to be constants. F7 is not about this branch's own code: it is the claim that
replaces a count this branch moved in a row `seal/ledger.md` already carried.

F4 was `floor_answer`, and round 1's fix pass renamed that function to
`yes_or_no` when a second row started using it. The anchor is gone, so the row
is REMOVED rather than re-pointed and its claim is carried forward by F8 —
which says more than F4 did, because the reason for the rename is part of the
claim now. -->

<!-- What deliberately has no row. The floor and the depth are stated in
`docs/review-chain-spec.md`, `skills/code-review/SKILL.md`, `agents/warden.md`,
`agents/smith.md`, `templates/sdd-round.md` and `templates/config.md`, and
`tests/test_the_run_stops_at_the_last_finding.py` and
`tests/test_a_fix_pass_may_add_a_unit.py` assert each sentence in the file it
belongs in, in both directions where a sentence replaced another. A coordinate
per file would say what those two modules already say, and the sibling work
item 1788445862 made the same judgment for the same reason. `listed` has no row
either: it joins entry names for a message, and nothing about it is a claim.

Round 2's 🟡 3 — an empty `Needs a fix` cell getting the sentence an empty cell
gets — has no row either. It is a message a case pins verbatim, which is what
`agent-contract` §14 asks of it, and there is no reading of the code a later
change could widen in good faith. -->

<!-- What the real-records case cannot check about itself, recorded because
nothing else can hold it. `test_this_repositorys_own_round_records_pass_the_
per_record_checks` runs the checker's own per-record functions over every
committed `round-N.md`. A change that skips the walk and keeps the loop leaves
it green and saying nothing, and no case catches that — nothing tests a test.
Two things stand behind it instead: the `assert records` guard, which is red
when the glob stops finding records (executed), and the fact that it was run
against `4adbb86` and named the refused entry there. Whoever loosens that loop
has to meet this paragraph. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| F1 · the floor's grandfathering excuses four different things, and only the first is the shape the rows above it use | `skills/code-review/scripts/chain_check.py#stopping_floor@ae898553`, `` `docs/review-chain-spec.md#"##### The floor — `Loses a record or crashes`, and what may follow it"@34c84aac` ``, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_run_past_the_floor_prints_for_an_item_begun_before_it@0292e054` | **Executed** 2026-09-04 at `46a70f0`: 154 cases across the five modules that read this checker, exit 0. The grandfathering is covered from both sides — an absent row printing for an older item, and a run already past its floor printing for one | 2026-09-04 | `STRICT_FROM` and `SURFACE_FROM` excuse an absent row for one reason: the round is over and a merged record has no honest repair. `stopping_floor` excuses that **and** a run that already went past its floor **and** a `Needs a fix` row that is present and malformed, and none of the three reasons is the other. The repair for the second is a round that was never spawned, which nobody can write now; the third is excused whole rather than only when absent, because that row carried free text from draft 0.5 of the handoff protocol until round 1 of this work item first read it, so a value written earlier was held to no vocabulary and malformed there does not mean careless. A later change that folds them into one shared grandfathering helper keeps all three behaviours and loses two of the reasons, because the only place they are written is the document this row points at. **Re-read 2026-09-05 in work item 1788501054's round 7 fix pass**, which moved both anchors: `stopping_floor`'s walk gained a second stop condition and the spec's floor subsection gained the paragraph stating it. The grandfathering — `item_began` against `FLOOR_FROM`, reaching the absent row and the run that ran on — is untouched, and the three excused things are still three; what changed is what counts as the run stopping, not what is excused from the count. **Re-read again 2026-09-05 in that item's round 8 fix pass**: the anchor moved by docstring alone — the bold sentence above the walk now states both stops — and the executable body is identical, AST-compared with docstrings stripped. The grandfathering is untouched a second time. **Re-read a third time 2026-09-05 in that item's round 9 fix pass**, and this time the anchor moved by executable text: the refusal's message — a string inside the error tuple `stopping_floor` returns — now names both stops and tells the reader not to rewrite `Needs a fix` to make it pass, where it named one stop and offered exactly that as the way out (round 9's 🔴 2). The spec's floor subsection moved with it: its exits-table row for two quiet records now says *and none whose verdicts closed on a fix*. The three excused things are still three. **Re-read a fourth time 2026-09-05 in that item's round 10 fix pass**: the refusal's message moved again — it had said *none of them saying the run reopened and none closing on a fix* where the walk stops AT such a record, that record included, so with two counted the second IS one; it now says what the count reached — and, after round 11 found *the record before the stop* to be the verifying round the floor allows, it names the stopping record and every counted record after the first as the ones too many, by file. The spec's exits row moved the same way twice. The walk itself is the one this clause describes; the grandfathering is untouched a third time. **Re-read a fifth time 2026-09-05 in that item's round 11 fix pass**: the message names the excess records by file now — the first counted is the allowed verifying round, every counted record after it is one too many — where it had blamed the record before the stop, which is the allowed one. The walk is unchanged; the grandfathering is untouched a fourth time. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, and the count above is corrected from three to four: that work item's phase 3 added the reopening walk inside `stopping_floor` — a second walk over `later` that refuses the second fix-closing record — with a notice branch of its own, keyed to `REOPEN_FROM` rather than `FLOOR_FROM`, that prints instead of failing for a work item begun before it. Its reason is the second's, a round that was never spawned, but its cutoff is separate, so a helper folding the grandfatherings loses a third reason now. The three earlier excused things are untouched (round 1's ⬜ 9 of that item) |
| F2 · `DEPTH_FROM` is later than `SURFACE_FROM` on purpose, so a work item begun between them owes the `New units` row and not the depth in it — and a case pins the floor's, the depth's and `Needs a fix`'s cutoffs equal, which is what a release moving one of them meets first | `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68`, `skills/code-review/scripts/chain_check.py#DEPTH_FROM@923cb79f`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_records_predating_the_depth_print_rather_than_fail@9cfb8cc7`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_the_two_cutoffs_are_the_id_of_the_item_that_wrote_them@98ce34ae` | **Executed** 2026-09-04 at `46a70f0`: both cases green. The first builds a record under an id between the two cutoffs and asserts the depth prints while the row is still required; the second asserts `FLOOR_FROM == DEPTH_FROM == NEEDS_FROM` and that a directory of that name is in the tree | 2026-09-04 | The constants carry a comment saying they are separate names for one number **because the rules can move apart**. The case asserts they are equal, so moving one is red today. Nothing here is wrong — the rules shipped in one work item, and a difference now would mean one cutoff naming an item that did not write its rule — but a later release raising `DEPTH_FROM` alone has to change that case deliberately rather than discover it. One of the three may never move in one direction at all: `NEEDS_FROM` later than `FLOOR_FROM` puts the run-length bound on a row no record was required to carry, and the case's own message is where that is written. Re-read 2026-09-04 by work item 1788501054, which changed `fix_surface` and drifted this anchor: it added a THIRD cutoff to the same function (`ORDER_FROM`, for a fix surface left provisional), and the claim here is about `DEPTH_FROM` against `SURFACE_FROM`, which is untouched. **Re-read again 2026-09-04 in that item's round 3 fix pass**, which drifted this anchor a second time: what moved is prose in the docstring naming what the `ORDER_FROM` arm does not reach, no cutoff was added or moved, and the `deep_excused` branch this row is about reads `DEPTH_FROM` exactly as before. |
| F3 · `DEPTH_RE` matches the literal word `depth`, and a pattern reading a bare number instead fails in the wrong direction as well as passing what it should refuse | `skills/code-review/scripts/chain_check.py#DEPTH_RE@4a08b228`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_digit_in_the_unit_name_is_not_a_depth@b4f6a13f` | **Executed**: the case is green at `46a70f0`, and it exists because the mutation that produced it killed nothing. `DEPTH_RE` degraded to a bare `(\d+)` left all twenty-nine cases that predated it passing — no fixture had a digit in a unit name (phase-3.md, "What this phase found") | 2026-09-04 | Under the loose pattern `sha256_of` written with no depth at all reads as depth 256, which is over the bound, so the entry is refused for a reason that is not the one the author has to fix. That is the half a reader re-deriving this from the regex alone would miss: the loose pattern is not merely permissive, it also refuses correctly-shaped rows with the wrong message |
| F5 · the floor's two answer words and the `Needs a fix` label are module constants rather than literals, because `templates/config.md`'s *What no row governs* list is derived from what the module names | `skills/code-review/scripts/chain_check.py#FLOOR_NO@4dc550ce`, `skills/code-review/scripts/chain_check.py#FLOOR_YES@5b1a7b4c`, `tests/test_the_pull_request_language_is_the_repositorys.py#_literal_strings@f49e163f` | **Executed** 2026-09-04 at `46a70f0`: the module's own suite green, exit 0. `test_the_exclusion_list_holds_every_string_a_checker_matches` was seen red for exactly `no` and `yes` and green for `depth` and `Loses a record or crashes`, which phases 1 and 2 had already listed (phase-3.md). Re-read 2026-09-04 during round 1's fix pass, when `NEEDS` joined the derivation: green, because `Needs a fix` was already on the list for the row's own sake | 2026-09-04 | Writing `"no"` and `"yes"` inline would have passed that case in silence, because the derivation only sees what a constant names. What it costs when it goes wrong is not a red build: a repository that sets `Record language` is simply never told that these two words stay English, and a translated cell then reads as a word the check cannot parse |
| F6 · `depth_problems` returns four lists rather than one, and a depth below the first level and a crowded entry are each refused with a message of their own | `skills/code-review/scripts/chain_check.py#depth_problems@9d21ab97`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_depth_below_one_is_not_a_depth@d36982c5` | **Executed**: green at `46a70f0`, beside the parametrised case that asserts a depth of 2 and above names the exit | 2026-09-04 | `(depth 0)` parses, and read permissively it sits **under** the bound and passes. The third list is not a third refusal of the same thing: the exits differ, because a second-level unit is deferred with a named answerer or becomes an issue, while a unit at a level the rule does not define has nowhere to be sent at all. The fourth, added by round 1's 🟡 5, is the same argument once more — a crowded entry's author has to split the entry, which is neither of the other two answers. Merging the lists keeps the refusals and loses the messages, which is the whole of what the entry's author acts on (overview.md, "How many shapes the depth refuses") |
| F7 · `ROUND_RECORD_FIELDS` is hand-copied from `templates/sdd-round.md` and carries **no count in this row at all**, and the case that reads it runs in one direction only: a name in the list and absent from the template fails, and a row in the template and absent from the list does not | `tests/test_the_pull_request_language_is_the_repositorys.py#ROUND_RECORD_FIELDS@1e6ac0cf`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_round_template_carries_the_fields_it_is_expected_to@a270cfb5` | **Executed**: twelve parametrised cases green at `46a70f0`. The one direction is what phase 1 measured rather than reasoned about — the plan asked for the case to be seen red for the new row before the row was in the list, and it cannot be, because the list is what the case is parametrised over. The red it did produce is the opposite order: the field added to the LIST while the template still lacked it, `1 failed, 11 passed`, exit 1, on the `Loses a record or crashes` parameter (phase-1.md) | 2026-09-04 | Carries the count that row *r3 3 / r4 2* under `## 0.5.0` of `seal/ledger.md` used to carry. Phase 4 left that row reading *the eleven fields* and DRIFTED on purpose, so the checker would keep pointing at it; round 1's 🔴 3 overturned that, because `evidence-check --strict` is the release-preparation step and a deliberately drifted row exits 2 there. The repair the owner chose is the one phase 4 declined to take blind: the claim is corrected so it stops carrying a number that goes stale, and the census sentence beside it is dated to the day it was taken and to the eleven entries it counted, so it stays true under a longer list. **That repair was described and only half applied**: the row went on carrying `twelve`, and work item `1788491830` added `Ran by` and made it thirteen — the same failure a third time, in the row whose own Notes name it. The number is gone from the claim now rather than corrected again, which is what the sentence above asked for. Counted 2026-09-04: thirteen entries, and the count is written here in the Notes where a stale one is a dated observation rather than a false claim |
| F8 · one reader answers both `no`/`yes` rows, and the only cell that tells a correct implementation of it from a lax one is `yes` with a separator and nothing after it | `skills/code-review/scripts/chain_check.py#yes_or_no@949118ef`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_yes_with_a_separator_and_nothing_after_fails@3891b8d4`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_needs_a_fix_takes_a_reason_after_either_answer@50bc8ccf` | **Executed** 2026-09-04 at the fix pass: both cases green, and the second is what says a reason after `no` is an answer — 30 of this repository's own 77 round records are written that way, so a reader that refused it would fail real history | 2026-09-04 | Replaces F4, whose anchor round 1's fix pass renamed. The rename is part of the claim: `Loses a record or crashes` and `Needs a fix` are one vocabulary, and a second parser for the second row would be two hand-kept spellings of it — the drift this file closes everywhere else. The tail after `yes` is required by the caller that wants it rather than by this function, which is why the floor refuses a bare `yes` and `Needs a fix` does not: the floor's reason is what the next round inherits, and this one's is the verdict table directly below the cell. `yes` alone never reaches the reason arm at all, because the equality arm catches it first |
| F9 · the run-length bound counts later records only up to the FIRST one whose `Needs a fix` says the run reopened, that record included | `skills/code-review/scripts/chain_check.py#stopping_floor@ae898553`, `skills/code-review/scripts/chain_check.py#run_reopened@26824ff6`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_the_verifying_round_may_reopen_the_run_and_its_fixes_get_a_reader@18511b0f`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_reopening_further_down_does_not_excuse_the_two_before_it@fd9eeb90`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_an_unreadable_needs_a_fix_does_not_stop_the_count@f0504e8c` | **Executed** 2026-09-04 at the fix pass: the permitting case green and the two refusing cases exit 1. The three are the set — the first is the sequence a blind count made unwritable, the second is the over-permissive fix that would license the #81 shape by finding a reopening anywhere later, and the third is the one mutation found, where a row the checker cannot read quiets the bound | 2026-09-04 | Round 1's 🔴 1. A verifying round that opens something is a finding round, so its own fixes need a reader and a third record is the run behaving correctly. The first instance was this work item's own `round-1.md`, which answers the floor `no` and `Needs a fix: yes` — the wall was hit at the cheapest possible moment. Two readings a later change would take in good faith are refused here: counting the records after a reopening (they answer to that round, not to the one that met the floor) and stopping at ANY later reopening rather than the first (which lets two quiet rounds through on the strength of a third that reopened). `run_reopened` answering None — row absent or unreadable — counts as NOT reopening, because the declared failure direction is *blocks more* and a row the checker cannot read must never be what quiets a refusal. **Re-read 2026-09-05 in work item 1788501054's round 7 fix pass, and the claim is now the FIRST of two stop conditions rather than the only one.** The walk also stops at the first later record whose verdicts closed on a fix, whatever its `Needs a fix` says — `wrote_fixes`, R11 of that item's fragment — because the row is the reviewer's answer to what it opened and the bound needs whether fixes were written, and the two came apart when an orchestrator fixed a 🟡 over a `no`. Everything this row refuses is still refused: the three cited cases are green under the new walk, three quiet rounds are still red because `answered` writes nothing, and `run_reopened`'s None still counts as not reopening. **Re-read again 2026-09-05 in that item's round 8 fix pass**: the anchor moved by docstring alone, the bold sentence now naming both stops, and the walk is AST-identical to the one this clause describes; what this row refuses it still refuses. **Re-read a third time 2026-09-05 in that item's round 9 fix pass**: the anchor moved by the refusal's own message, which now states both stops — round 9's 🔴 2 found it stating one and naming a false `Needs a fix: yes` as the way out. The walk is the one this clause describes; the three cited cases are green and three quiet rounds are still refused, and the case pinning the new message sits beside them. **Re-read a fourth time 2026-09-05 in that item's round 10 fix pass**: the message moved again, this time to stop denying the very stop that fired — round 10's 🔴 1 — and a second case beside the first pinned that it names the record before the stop as the fault — which round 11 found to be the verifying round the floor allows; the case now pins that the stopping record and every counted record after the first are named, by file, and that the allowed one is not. **Re-read a fifth time 2026-09-05 in that item's round 11 fix pass**, which is the pass that made the previous sentence true; the anchor moved with it. The walk is unchanged; what this row refuses it still refuses. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved `stopping_floor` by the reopening walk — a second walk over the whole of `later`, never stopping, that refuses the second record whose verdicts closed on a fix. The count walk this row is about, its first stop at `run_reopened` and the None-counts-as-not-reopening direction, is untouched |
| F10 · a refusal's own instruction must not produce a cell that means something else, and `none;` is where that was measured | `skills/code-review/scripts/chain_check.py#says_none@f2a20d93`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_none_with_a_trailing_semicolon_is_still_none@7f0411cd`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_comma_inside_a_reason_after_none_is_not_a_list@daaabf4e` | **Executed** 2026-09-04 at the fix pass: `none;` passes, and `none, nothing added` still passes beside the new crowded-entry refusal that a comma otherwise triggers | 2026-09-04 | Round 1's 🟡 6. `none;` was refused as separators-with-nothing-between, and following that message produced `none (depth 1)` — which `says_none` read as NO units at all, so the fix for a refused cell was a cell that lied. The `;` joins the `.` in the rstrip, one character. The pair of cases is the point rather than either alone: `says_none` short-circuits BEFORE the depth walk, so widening the walk's comma reading without the second case would take `none, nothing added` — a value the sibling fix-surface module already pins as an answer |
| F11 · `says_none` refuses a cell carrying a parenthesised depth marker, and keys on the marker rather than on the word | `skills/code-review/scripts/chain_check.py#says_none@f2a20d93`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_unit_named_none_carrying_a_depth_is_a_unit@6ed8fca4`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_reason_that_merely_says_the_word_depth_is_still_none@bfd5807f` | **Executed** 2026-09-04 at round 2's fix pass: `` `NONE` (depth 2) `` and `none (depth 2)` both refused, and `none — the depth was not recorded` still passes. Before the guard, `says_none` answered True for the first two (round 2 executed it) | 2026-09-04 | Round 2's 🟡 2, the other half of round 1's 🟡 6. `EMPHASIS` strips backticks and `.lower()` strips case, so a unit really named `NONE` is the word `none`, and the space before its marker is a separator — the cell parsed as *none with a reason* and the depth walk never ran on a unit the rule refuses. What the guard does NOT close is in its docstring: a unit named `none` with no marker beside it still reads as the word, and nothing in the cell can tell them apart. A later change keying the guard on the word `depth` instead of the marker takes the ordinary reason with it, which is what the second case holds |
| F12 · the separator's limit is the third of three, it runs before the other two, and the entry that recorded it is the entry that met it | `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68`, `` `docs/review-chain-spec.md#"##### The depth in `New units`"@73bf8f51` ``, `tests/test_the_fixes_name_their_surface.py#test_an_entry_carrying_a_literal_separator_is_refused@702e0793`, `tests/test_chain_check_at_the_pull_request.py#test_this_repositorys_own_round_records_pass_the_per_record_checks@bc960675` | **Executed** 2026-09-04: the fixture case exits 1, and the real-records case is green at HEAD and red at `4adbb86` — where it names `round-1.md` and the truncated entry, which is round 2's 🔴 1 reproduced | 2026-09-04 | The branch recorded the arrow's limit and the comma's and said nothing about the separator that splits both rows before either applies, so a record describing the separator was cut in half by the row it was describing. That is why the class matters rather than the coordinate: three limits, one cause, and only the third one bites the person writing the record. The real-records case is the other half — nothing had ever run these checks against `seal/specs/*/rounds/*.md`, so the first reader of a record this repository writes was the pull request it was written for. Re-read 2026-09-04 by work item 1788501054, which changed `fix_surface` and drifted this anchor: the new arm sits inside the `says_none` branch and reads no separator, so the ordering of the three limits is unchanged. **Re-read again 2026-09-04 in that item's round 3 fix pass**, which drifted this anchor a second time: nothing executable moved — the docstring gained a paragraph about the arm's key, and the `value.split(";")` that runs before the arrow and the comma is still the first thing either row meets. |

<!-- specs/1788486395-the-roll-opens-the-next-log-with-no-body -->
### 1788486395-the-roll-opens-the-next-log-with-no-body

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Six rows. Phase 1 is a section of prose in one shipped skill, and
`tests/test_a_segment_feeds_the_flow_log.py` reads it sentence by sentence —
twelve cases, six of them new. A row per sentence would be an inventory of the
diff rather than a claim a later change can break silently, which is the
judgment `seal/ledger/1788472135-the-run-outlives-its-last-finding.md` made for
the same shape. F5 is the exception, because it is about what the prose does
NOT say and no reader reconstructs an absence from the text in front of them.

The other five are inside `roll_flow_measurement_issue.py`, and each is a
narrow reading a later tidy-up would widen in good faith: a guard whose
exclusion and whose sleep both read as noise (F1), a rung that looks
inconsistent with the two above it (F2), guard clauses that look defensive
(F3), a sentence split that looks like an f-string waiting to be joined (F4),
and a message whose hedge reads as vagueness (F6). Three were found by writing
the mutation rather than by reading the code, and three more by round 1.

F1, F3 and F6 were rewritten by round 1's fix pass rather than added. The
first two carried claims that were true of the code before the round and
narrower than the code after it, and F6 replaces nothing — the message it is
about was correct until the ladder arrived and made it a promise the code
could no longer keep. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| F1 · the ladder's guard asks whether a failed create landed by looking for an open issue that is **not** the one `main` just closed, and it gives an empty reading the same one retry every other empty reading in this module gets | `.github/scripts/roll_flow_measurement_issue.py#landed_create@9deecaf4`, `.github/scripts/roll_flow_measurement_issue.py#open_issue@bbb63d01`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_issue_main_just_closed_does_not_count_as_the_create_landing@d89d0be1`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_landed_create_guard_retries_an_empty_reading@5e81444a` | **Executed** 2026-09-04 at round 1's fix pass: 18 cases in that module, exit 0. Both cases were seen red at `b1f7340` before the guard existed, and four mutations killed — the exclusion removed, the exclusion inverted, the retry removed, and the sleep removed | 2026-09-04 | Round 1's 🔴 1 and 🟡 2. The first version of this guard read the raw open-issue list, and `main` closes the current log seconds before the ladder runs — so a `gh issue list` lagging that close answers with the closed issue, the guard reads it as the create landing, and the release ends with no rolling log open and the workflow green. This module's docstring is what the guard leaned on, and it says a lag can only ever produce a reading short a result. That was written about a reading taken after a label **add**. A reading taken after a **close** is the case it does not cover, and it produces exactly the extra issue the sentence rules out. The path is not exceptional either: whenever the milestone cannot be resolved, which is the case the ladder exists for, the first rung always fails and the guard always runs. The retry's sleep is the half a later reader drops as noise — an immediate second read hits the same lagged index, and removing it left every case here green until the case pinned it |
| F2 · the ladder's last rung calls `run` while the two above it call `try_run`, and the difference is what keeps `main`'s recovery message reachable | `.github/scripts/roll_flow_measurement_issue.py#open_issue@bbb63d01`, `.github/scripts/roll_flow_measurement_issue.py#try_run@7183318c`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_every_attempt_failing_still_exits_loudly@65158066` | **Executed** 2026-09-04 at `463f41d`: the last rung mutated to `try_run` and the case exits 1, naming the recovery message it lost | 2026-09-04 | The three rungs look like one loop with an odd tail, and making them uniform is the tidy-up a reader reaches for. What that takes away is the only path by which a create failing all the way down still names the already-closed issue and the title to open by hand — after which the release ends with zero open issues, silently, and the next one fails on a state nobody was told about. `try_run` exists for calls whose failure must not stop a release, and the create is not one of them: only the two arguments hanging off it are |
| F3 · `find_baseline_issue` answers `(number, note)` and separates **four** readings, not two: three silent empties, and two-or-more open, which is that label's invariant broken and is named rather than guessed between | `.github/scripts/roll_flow_measurement_issue.py#find_baseline_issue@1d701be6`, `.github/scripts/roll_flow_measurement_issue.py#BASELINE_AMBIGUOUS_NOTE@6dc9db35`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_durable_logs_lookup_answers_none_rather_than_failing@5343e13d`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_two_open_durable_logs_are_named_rather_than_guessed_between@60a20106`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_ambiguous_ledger_note_reaches_the_issue_it_is_about@1393ccbf` | **Executed** 2026-09-04 at round 1's fix pass: the lookup answers `None` with no note for `None`, `[]` and `not json`, and `None` with a note for two open. Four mutations killed — the two-or-more branch removed, its note dropped, a note attached to the ordinary empty, and the note set aside before it reached the body | 2026-09-04 | The three empties are the call failing, the label existing with nothing open, and the output not being JSON at all. Only the first two look like the same branch. `main` catches `SystemExit` and nothing else, so a `ValueError` raised out of `json.loads` after `close_issue` has already run ends the release with the old log closed, no new one open, and no recovery message printed. The fourth reading is round 1's note, and it is the same class as 🟡 5 one file over: the skill gives both labels the same exactly-one-open invariant and says a broken one is named rather than guessed at, and `issues[0]` was the guess. Two silences had to be told apart to say it — no durable ledger is the ordinary case and must stay silent, because a note about it would appear on every rolling log every repository ever opens |
| F4 · the durable ledger's clause is a sentence split rather than a conditional inside one f-string, because an absent ledger interpolated renders as an issue link that goes nowhere | `.github/scripts/roll_flow_measurement_issue.py#issue_body@9ed9ce29`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_body_drops_the_ledger_clause_where_no_durable_log_exists@2bf52760` | **Executed** 2026-09-04 at `463f41d`: both bodies printed and read — with a ledger, and without. The guard mutated to `if True` and the case exits 1 on the word it then carries | 2026-09-04 | Joining the two halves back into one f-string is the obvious simplification and it is invisible until a repository has no `flow-baseline` issue, which is every repository except this one. What it writes then is `live in #None`, and on GitHub a `#` followed by a word is prose rather than a broken link, so nothing renders as an error — a reader simply learns that the durable ledger is issue None. The case asserts the absence of `live in #` and the absence of the word, because asserting only the first passes for the mutation that matters |
| F6 · `main`'s recovery message tells an operator to check the label before opening anything, because the create that failed may have landed and the message cannot know | `.github/scripts/roll_flow_measurement_issue.py#main@4e197a3d`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_recovery_message_does_not_promise_the_log_is_empty@a9551ab1` | **Executed** 2026-09-04 at round 1's fix pass: the case was seen red at `b1f7340` against the old wording, and the mutation restoring that wording exits 1 | 2026-09-04 | Round 1's 🟡 3. The message used to assert *the flow-measurement log now has zero open issues*, which was true while `open_issue` was one create and stopped being true when the ladder arrived: `list_open_issues` raises `SystemExit` through `run`, so a lookup that stumbles mid-ladder reaches this handler with a create that may already have landed. Somebody following the old instruction opens the second, and the next release fails on two-or-more — the invariant broken by the recovery for the invariant. The case pins the new sentence and refuses the old one by name, so a later edit tightening the prose back into a promise is red rather than plausible |
| F5 · the shipped skill may name `flow-measurement` and `flow-baseline` and no tracker state that exists in this repository alone, and the case holding that is the only one in its module that reads for an absence | `` `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@961df00e` ``, `tests/test_a_segment_feeds_the_flow_log.py#test_the_shipped_skill_names_no_repository_specific_tracker_state@e9095a8b`, `.github/scripts/roll_flow_measurement_issue.py#INDEX_LABEL@848e20b3` | **Executed** 2026-09-04 at `97f1519`: the case is green, and it was seen red three times — `log: measurement` inserted into the section, `#51` inserted, and a bare backticked `measurement` inserted, each exiting 1 and each restored from bytes the probe kept | 2026-09-06 | Every other case in that module asserts a phrase is present, so every one of them is satisfied by a section that also says something it should not. This is the half that is not. The two layers meet here: `.github/` stays home by `tests/test_the_release_check_watches_what_ships.py`'s own list, which is what lets the roll script name `measurement` and `log: measurement` as constants while the skill may not name them at all. A later edit that moves a sentence from the script's docstring into the skill for symmetry is exactly what this refuses, and nothing in the skill's own prose would tell that reader they had crossed a line. **Re-read 2026-09-05 in work item 1788613827's phase 4**, after phase 2 added the run-level comparison table and its three sentences to this section. The absence this row is about is the one the enlarged section had to be written against: nine new rows and four new paragraphs name no issue number and no milestone, and `test_the_shipped_skill_names_no_repository_specific_tracker_state` reads the section as it now stands. **Re-read 2026-09-05 in the same work item's round 1 fix pass**, which corrected two of those rows and added a paragraph naming which file of a project directory is a run's main transcript. The deferral it opened is `#174`, and it is named in a commit message rather than in the skill — the absence this row is about survives because the section still names no issue number and no milestone. **Re-read 2026-09-06 in the same work item's round 2 fix pass**, which rewrote the `Location` row to enumerate `seal/specs/`, `seal/ledger/` and `seal/ledger.md` rather than the whole `seal/` directory, and named the three files at the root that fall outside it. Those are paths this plugin creates in every repository it is installed in, so the absence this row is about is untouched: no issue number, no milestone, no label that exists here alone |

<!-- specs/1788491830-a-segments-record-says-what-it-cost -->
### 1788491830-a-segments-record-says-what-it-cost

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Four rows, and each is a narrow reading a later tidy-up would widen in good
faith: a branch order that looks arbitrary (R1), a regex whose whitespace
looks like slack (R2), two needles in one parametrised list that look
inconsistent with each other (R3), and two templates saying one rule at two
lengths where the shorter one reads better and is wrong (R4).

R2 and R3 came out of the mutation loop rather than out of reading the code,
and both were SURVIVORS — the case existed, looked like it pinned the thing,
and stayed green against the mutation of the thing it named. That is the class
this work item produced twice, and the rows are here so the next reader meets
it before the loop does. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `runner_problem` tries the `unknown` branch **before** the `on` split, and that order settles what a cell is read AS and never whether it is accepted | `skills/code-review/scripts/chain_check.py#runner_problem@44124ee0`, `tests/test_a_record_says_what_ran_it.py#test_the_arm_order_changes_the_reading_and_never_the_verdict@e8b243f6` | **Executed** 2026-09-04 at `6bd730c`: the cited case now BUILDS the other arm order from the module's constants and compares the two on the verdict across 756 constructed cells, zero disagreements. Verified three ways — green unmutated, green with the real arms reversed (which is the claim, not a hole in it), and red naming the cell when an arm's separator set is changed so the orders come apart | 2026-09-04 | Round 1 wrote this row after disproving the load-bearing claim, and cited a case that did not run it — round 2's 🟡 6. That case asserted three values each asserted verbatim elsewhere plus `ON_RE.search("unknown") is None`, which cannot fail, since `unknown` holds no `on` substring at all. Reversing the real arms left it green. **A ledger row pointing at a case that does not execute its claim is the mistake this row's own Notes named one sentence later**, which is why the row is worth keeping rather than deleting: the claim is right, the grounds were a story, and the gap between those two is what a reader has to be able to see. What the order buys is the READING a message quotes back — `unknown — the model was not recorded on this run` is an unknown with a reason here and a pair whose model is `this run` under the other order, and both pass. A later reader may reverse it without breaking a verdict; what they would break is the sentence the refusal prints |
| R2 · `ON_RE` requires whitespace on **both** sides, and the word that pins it has to split into two NON-empty halves — `monitor`, not `carbon` | `skills/code-review/scripts/chain_check.py#ON_RE@17c6f7c5`, `tests/test_a_record_says_what_ran_it.py#test_the_on_must_stand_alone@a76f5757` | **Executed** 2026-09-04 at `2a36737`: the pattern mutated to a bare `on` and the case exits 1 on `monitor`. Against `carbon` alone the same mutation left every case green, which is how the gap was found | 2026-09-04 | The refusal has two clauses in sequence — *not two halves* and *a half is empty* — and `carbon` trips the second, so a case carrying only that word is satisfied by a clause other than the one it names. Stripping the `\s+` from the pattern is the obvious simplification (the row already says `on`, and the whitespace reads as slack), and it silently makes the `on` inside a name a separator. The general form is worth more than the instance: **a case whose input, needle or fixture is satisfied by something other than the thing it means to pin is green against its own mutation.** This work item has now produced six, and the shape moved each time — an input tripping an earlier clause (this row), a needle satisfied by prose around the row (R3), an exit code satisfied by an unreachable commit (round 1's 🟡 5), a fixture with one record where the claim needed two (round 2's 🟡 7), an assertion that cannot fail at all (round 2's 🟡 6), and a needle satisfied by the same phrase in a neighbouring subsection (found in the mutation loop for round 2's own fix, three times in one run). None was found by reading; every one was found by mutating |
| R3 · `INSTRUCTORS` pins the three row-carrying files on the ROW and the two skills on the NAME, and the asymmetry is deliberate | `tests/test_a_record_says_what_ran_it.py#INSTRUCTORS@addb97b1`, `tests/test_a_record_says_what_ran_it.py#test_every_instructing_file_names_the_row@a2fe73e9` | **Executed** 2026-09-04 at `bf2eddc`: `docs/review-handoff-protocol.md`'s `| Ran by |` renamed to `| Run by |` and the case exits 1. Against the bare-name needle the same mutation left it green | 2026-09-04 | Making the five needles uniform is the tidy-up a reader reaches for, and it is what the case looked like before the mutation caught it. `docs/review-handoff-protocol.md` names `Ran by` in its own `####` section and in the drafts log as well as in the table, so the bare name survives the row being renamed out of existence — the document still says the word while the row a session copies is gone. The two skills carry no table at all, so the row spelling is not available to pin there, which is why the split is asymmetric rather than an oversight |
| R4 · `templates/sdd-phase.md` separates whose the value is from whose keystrokes fill it, and the round template's sentence copied across unchanged makes the row unfillable by anyone present | `` `templates/sdd-phase.md#"# <work-item-id> — phase <N>"@9146ea0e` ``, `tests/test_a_phase_hands_the_next_one_a_record.py#test_the_template_resolves_who_types_a_phase_records_row@460d1dbc`, `tests/test_a_record_says_what_ran_it.py#test_verify_says_how_a_phase_record_comes_to_carry_the_row@ebdd6741` | **Executed** 2026-09-04 at `473765d`: three mutations killed — the spawn-prompt half removed, the transcribe/decide distinction removed, and the sentence naming who writes a phase record removed | 2026-09-04 | The rule is one act for `rounds/round-N.md`, because the orchestrator knows what ran the round and runs `round_record.py new`, which writes that file from the `--ran-by` value it is handed. A phase record is written by the segment that just ran, so *the spawning session fills it, never the segment itself* — true of the round record and copied here verbatim in the first draft — tells the only party holding the pen not to write the row. The correction is not a softening: the value is still the orchestrator's to SOURCE, and what stays refused is the segment sourcing it from its own idea of what it is. A later editor tightening the two templates back into one sentence for symmetry re-opens exactly this, and the shorter sentence is the one that reads better. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved the template's one sentence about the round record: the orchestrator runs `round_record.py new`, which writes that file from `--ran-by`. The distinction this row is about — whose the value is against whose keystrokes fill it — is untouched, and the sentence at the head of these Notes is corrected the same way (round 1's ⬜ 9 of that item) |
| R5 · a phase record's field table carries `Phase`, `Commit` and `Ran by` — three rows, not two — and its three sections still ship outside HTML comments as placeholders rather than filled claims | `templates/sdd-phase.md#"\| Field \| Value \|"@d59fb237`, `tests/test_a_phase_hands_the_next_one_a_record.py#test_the_field_table_is_phase_commit_and_what_ran_it@67075610`, `tests/test_a_phase_hands_the_next_one_a_record.py#test_all_three_sections_exist_outside_comments@f2f6b8d9` | **Executed** 2026-09-04 at `a8d899d`: the case asserts the exact list and goes red on any other, and the sections case is unchanged and green | 2026-09-04 | **Replaces P1 in `seal/ledger.md`, which this branch removed rather than re-pointed.** P1 claimed the table carries ONLY `Phase`/`Commit`, and adding `Ran by` made that false rather than stale — the claim went with the code, which is the case `CLAUDE.md` says is a removal and not a re-anchor. Its second anchor was BROKEN as well, because the case it named was renamed to say what the table now holds. The pin is an exact-list equality rather than a membership test, so it goes red whichever direction the template and the list move apart — worth knowing before a fourth row is added, because the equivalent round-record pin is a membership test and only goes red in one |

<!-- specs/1788501054-a-check-reports-clean-while-something-is-missing -->
### 1788501054-a-check-reports-clean-while-something-is-missing

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Twelve rows — R11, R12 and R13 are the last three. R11 and R12 are what the
fixed rows of R6 and R10 are about turned on the ledger's own reader: a bound
that read one of two cells answering the same question, the two came apart on
this branch's seventh round, and the rule that fixed it had eight copies of
which each fix pass corrected the ones it could count. R13 is R3's identity
arriving one function up, after a repair that composed a weaker one where the
copy was thirty lines away. Six are narrow readings a later tidy-up would
widen in good faith:
a pair of git decisions that each look redundant beside the other (R1), a
filter that looks like belt-and-braces (R2), an inode fold that looks like
ceremony beside a path comparison (R3), a helper extracted from four lines
that read fine inline (R4), a refusal whose `allow` direction reads as an
oversight (R7), and a set comprehension that reads as tidiness (R8). R5 and R6
are the opposite shape — claims about what a check
cannot reach, recorded so the next reader meets the rejected mechanisms
instead of writing one, and R6 is itself a correction: its first version
claimed a limit that the same commit had already disproved four times. R10 is
a third shape again — a rule about how a recorded limit is READ, written after
two of the four limits recorded on this branch turned out to be false. It has
been unaccounted for since it was written: this paragraph said *Eight* over
nine rows, and over ten before R9 left with the code it cited.

**Not one of the six was found by reading.** R1, R3 and R8 came out of the
mutation loop, R2 out of asking what the second round of every run looks like,
R7 out of a round asking what the rule this branch added had made of the row
beside it, and R4 out of the same pass asking what a moved default would leave
behind. R6, which is not one of the six, came out of a review round reading
dates — a sentence this paragraph used to list beside the other five as if it
were, until round 7 counted the names. R4 was Read-only for two
rounds and is executed now, because the mutation that separates it is not a
change to the code but a change to the DEFAULTS the code reads. R1 has now been a
survivor twice: eleven mutations before a twelfth case caught
`--diff-filter=A`, and twenty cases green over an index (`found[-1]`) that
round 1 mutated in one line. Both halves of that pair are in one row for that
reason. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `added_on_branch` reads the LATEST add, and `--diff-filter=A` is what keeps *added* from collapsing into *touched* — the two together, because each is undefended by the cases the other one needs | `skills/code-review/scripts/chain_check.py#added_on_branch@5461cc08`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_added_before_the_base_and_updated_on_the_branch_passes@53889300`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_deleted_and_re_added_after_the_fix_is_judged_on_the_later_add@4dee6e90` | **Executed** 2026-09-04 after round 1: `found[0]` → `found[-1]` turns the delete-and-re-add case red and nothing else; dropping `--diff-filter=A` turns TWO cases red, the base-moved one and the updated-in-place one. Nineteen mutations across both checkers, one survivor and it is C12 below | 2026-09-04 | **The index was `found[-1]` until round 1's 🟡 8 and nothing held it** — mutating it left all twenty cases green, because `--diff-filter=A` returns one commit for a file that was added and then only modified, which every case then in the file built. The earliest add is the permissive direction: a stub committed on time, deleted, and the real record written after the fixes passes on the strength of a commit that no longer holds any of its content. Note what changed when the index flipped: under `found[-1]` dropping the flag was separable only by a base that MOVES under a long branch, and under `found[0]` it also refuses every correctly updated record, because the newest commit that TOUCHED a record is its verdict update. **A row written for the old index would now be false**, which is why this row names the pair rather than the flag alone |
| R2 · a fix commit that is an ancestor of the record's own `Target SHA` is dropped, and without that the check fails the SECOND round of every run | `skills/code-review/scripts/chain_check.py#written_late@1e82498f`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_fix_this_round_did_not_commission_passes@d61a2df0` | **Executed** 2026-09-04 at `55ae839`: the filter replaced by `commissioned = True` and the cited case exits 1; every other case in the file stays green | 2026-09-04 | It reads as belt-and-braces beside the ancestry test that follows it, and it is the opposite: round N+1 reviews round N's fixes, so its verdict table answers round N's findings and its cells legitimately name commits that already existed. Round N+1's record is committed after those commits BY CONSTRUCTION. Without the filter the refusal is not a rare false positive, it is red on the second record of every well-run chain — including this repository's own |
| R3 · two names are one ledger when they reach one **inode**, so the skipped set folds by `st_dev`/`st_ino` and not by any spelling of the path | `skills/evidence-check/scripts/evidence_check.py#file_identity@e8a4f000`, `skills/evidence-check/scripts/evidence_check.py#skipped_by_narrowing@91696b61`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_an_inode_of_zero_does_not_fold_two_files_into_one@65e2fc82`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_hard_link_to_the_ledger_is_not_reported_as_skipped@b1ef0ac4`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_second_spelling_of_the_same_file_is_not_reported_as_skipped@2ac929ab`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_two_devices_that_share_an_inode_number_are_two_ledgers@52ab1e1d` | **Executed** 2026-09-04 after round 1: the fold replaced by `normcase(realpath(...))` and two cases exit 1 — the hard link on every platform, the `SEAL/ledger.md` spelling where the filesystem folds case. Measured on the build machine: `normcase('SEAL/ledger.md')` is the identity, `realpath` keeps the case, the filesystem folds it, and a hard link has one inode under two realpaths | 2026-09-04 | **It was `normcase(realpath(...))` until round 1's 🟡 9, and the platform was inside it**: `os.path.normcase` folds case on Windows alone and `realpath` canonicalises case nowhere, so on a case-insensitive filesystem the run read the ledger and then listed it as unread. That is `agent-contract` §13 exactly — a defence resting on the platform where nobody removed the guarantee — and the three obvious cases all pass under either spelling, because `--ledger seal/ledger.md` and the resolver's answer are string-equal. Three spellings break the old rule and one is platform-free: a leading `./`, a case variant, and a hard link. The inode fold answers all three plus symlinks with one rule. **Two ways out of the fold, and round 2's 🟡 8 is that only one of them existed**: `os.stat` raising reaches the fallback, and a `st_ino` of 0 did not — Python's contract makes a zero no identity at all, and taken at face value it gave every unstattable file ONE identity, so a ledger that was read swallowed every ledger that was not. Silence, which is the reverse of what this notice is for. Both ways out now key by path. **Re-read 2026-09-04 in round 3's fix pass, which found two things and moved the anchor** (🟡 6 and the enumeration around it). The `st_ino` test was undefended — swapping it for `st_dev` left the whole suite green, because zeroing BOTH fields is the only state any case built and a zero device falls back exactly as a zero inode does; a real device with a zero inode separates them and now runs in the same case. And the fallback's own claim was backwards: it was written as over-reporting, and it is a WEAKER identity than the inode — `abspath` folds `./seal/ledger.md` into `seal/ledger.md`. **Re-read 2026-09-05 in round 4's fix pass, which found the repair of that sentence false in the same place it had just been fixed** (round 4's 🟡 5). It read *`normcase` folds case where the platform does, so two spellings of one unstattable file read as one wherever the platform says they are one*, and `normcase` folds on WINDOWS alone rather than wherever the filesystem does — which is round 1's 🟡 9 restated one function over, arriving inside its own correction. Executed on a case-insensitive macOS volume: one file, one inode, TWO fallback identities. So the fallback OVER-reports off Windows, which is the declared direction, and the same holds for the two spellings the platform keeps apart anywhere. The paragraph declaring `normcase` unreachable was false too: it tied reachability to the zeroed inode, and `OSError` reaches the fallback on every platform — what is Windows-only is `normcase` differing from the identity, so it is unkillable off Windows and load-bearing on it. Nothing goes unread either way, and the conclusion is unchanged; only both reasons moved |
| R4 · `default_patterns` exists so the skipped set is subtracted from the same list the defaults are read from, not from a second copy of it | `skills/evidence-check/scripts/evidence_check.py#default_patterns@8d3a5c4c`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_the_skipped_set_is_subtracted_from_the_list_the_defaults_come_from@db1c7d23` | **Executed** 2026-09-04 in round 3's fix pass: the call replaced by an inlined copy of the three locations, and the case exits 1. It was Read-only until then, and the reason it stayed that way is worth keeping — no ORDINARY case can distinguish one spelling from two, because the two are identical on the day they are written. What separates them is MOVING the defaults, so the case adds a fourth location and asks whether the skipped set followed | 2026-09-04 | Inlining it back into `main` is the tidy-up, and the four lines do read fine there. What it costs is invisible until the defaults next change: the copy that decides which ledgers get NAMED as skipped is the quiet one, so a repository that adds a fourth default location would keep checking it and stop reporting it. That is the same failure the whole work item is about — a check reporting clean while something is missing — reintroduced by a refactor that looks like cleanup |
| R6 · the `Checked` DATE cannot show a re-read that lands on the day already in the cell, and the `Notes` column can — so the trace goes in the Notes, on every row a re-stamp touched | `skills/evidence-check/SKILL.md#"## Re-verifying is recomputing the hash"@8a596600`, `` `CLAUDE.md#"## Repo rule — commit early; on a declared branch it costs nothing"@143784bb` `` | **Executed** 2026-09-04: this branch's unscoped `--reverify` re-stamped eight rows in `seal/ledger.md`. Three carried `2026-09-02` and were visibly impossible against content written on the 4th; five already carried `2026-09-04` and were re-stamped without a re-read exactly like the other three, with nothing in the file to tell them apart. Round 1 found the three and could not have found the five | 2026-09-04 | The rule (`CLAUDE.md`) is that the column holds the date somebody read the code, and the failure it cannot catch is a re-stamp on the day of the last read. **This is the work item's own shape one level in** — a check reporting `517 ok` while the trace that would show the gap is missing. **The first version of this row said no column can distinguish a re-read that happened from one that did not, and the same commit disproved it four times** (round 2, 🟡 10): four of the eight rows carried a `Re-read <date>` clause in Notes and four did not, and the four that did are exactly the ones a reader can check. The repair is therefore a convention rather than a limit — the Notes clause names what the re-read found, and all eight rows now carry one. What no column can do is make a re-read HAPPEN, and what made the six visible at all was the unscoped read this branch adds; a scoped one would have re-stamped nothing and reported clean |
| R7 · the pending fix-surface refusal keys on `Fixes checked by`, so it needs no new source of truth — and its direction for a reason it does not recognise is **allow**, which is this file's only deliberate exception to *blocks more* | `skills/code-review/scripts/chain_check.py#says_not_yet@61e75f01`, `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_honest_mid_run_state_is_not_refused@0d3dbd79`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_reason_the_checker_does_not_recognise_passes@b6b5843a` | **Executed** 2026-09-04, rounds 2 and 3: eighteen mutations across the two checkers. Firing the arm regardless of `Fixes checked by` turns the honest mid-run case red, never firing it turns five red, keying it to `SURFACE_FROM` turns one red, and firing it on any `none` with a reason turns eight red. **Three survive and all three are inside `says_not_yet`**: substituting `NOT_YET in rest` for the prefix test (round 3's 🟡 7, closed by a case in the same file), and its own two guards — the `none` prefix and the separator after it — which duplicate `says_none`'s and cannot change the outcome, because `fix_surface` is the only call site and it calls this behind `says_none` | 2026-09-04 | **The state was this branch's own doing** and the repair had to avoid two traps. Widening to *any reason* refuses `none — the fixes deleted a line`, which is honest; keying on prose is the enumeration over an unbounded domain the arrow's and comma's limits already decline. What makes the narrow key defensible is that the phrase is a CONSTANT the template prints, tied by a case, so the pair cannot drift. **Re-read 2026-09-04 in round 3's fix pass, which measured the cost wider than a rewording** (🟡 5). *Escapable by rewording* was what this row first claimed; three spellings escape with the template's words unchanged — a dash outside `SEPARATORS`, a doubled space inside the phrase, and any clause in front of it. Only the first is punctuation, so widening `SEPARATORS` closes one of three, leaves the claim false about the other two, and moves a constant four other readers in this file share; the limit is written down instead. **The key is a second cost of the same shape** (🟡 1): the arm reads `Fixes checked by`, so it reaches the session that filled that cell and stopped, and a session that left all three cells behind meets `checked_by`'s notice rather than this refusal. *This record passed* therefore means *its cell does not carry the template's own pending words*, never *its fix surface is complete*. **Re-read 2026-09-05 in round 4's fix pass, which found the sentence about the two guards false twice** (round 4's 🟡 3). They do NOT both duplicate `says_none`'s: `says_none` answers True for a bare `none` by `s == NONE_WORD` — the value `templates/sdd-round.md` itself prints — so a value reaches `says_not_yet` with nothing after the word, and the `not rest` conjunct is what stops `rest[0]` raising on it. Measured, cache purged between runs: dropping that conjunct alone turns **20** cases red; deleting either guard WHOLE leaves the suites green, because an empty tail cannot start with `NOT_YET` either. So the survivors are three only as whole-guard deletions, the prefix half of the claim stands and the separator half never did, and the docstring now says which is which. The verdict cell above keeps round 3's wording as what that round measured; this clause is the correction |
| R5 · *does the record carry what it says it verified* is NOT machine-checkable, and the three checks somebody would write are named with the reason each fails | `` `docs/review-chain-spec.md#"##### What the record carries — a declaration, and why no check reads it"@c99fda37` ``, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_spec_answers_whether_carrying_is_checkable@a0d67224` | Read 2026-09-04 at `0e9aa27`; the case seen red under each of its three needles stashed in turn | 2026-09-05 | Issue #150's own comment asks for this to be *stated before it is assumed*, and a document that simply omits it reads as an oversight rather than as a decision — which is how somebody writes the keyword match over free prose that the arrow's and the comma's limits already decline elsewhere in the same file. The rule that remains is a declaration: a probe row whose subject was a proposed replacement carries the replacement, in a fenced block, and the reader who looks at it is the fix pass the record is the agenda for. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which added two paragraphs under the subsection: the record is written by `round_record.py` now and `new` copies the reviewer's probes table row for row, so the declaration is the reviewer's, made in the report; and the 0.8.x moratorium on parsed fields. The three checks somebody would write, and the reason each fails, are untouched. **Re-read 2026-09-05 at the 0.8.1 release preparation**, after the round 2 fix pass of the same work item rewrote the generator paragraph: `new` carries every fenced block under the probes table and nothing else of the section, which is the declaration's shape and not a check of it. The three checks somebody would write, and the reason each fails, are untouched |
| R8 · `resolve_patterns` deduplicates, and the run's TOTALS are the only place a duplicate shows — each pass over one file prints its own `1 ok` | `skills/evidence-check/scripts/evidence_check.py#resolve_patterns@4ecfd731`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_one_file_matched_by_two_patterns_is_read_once@7bc3c60b`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_one_file_matched_under_two_spellings_is_read_once@5193a084` | **Executed** 2026-09-04 in round 3's fix pass: `sorted({...})` replaced by a plain list comprehension and the case exits 1, reading `total: 2 ok` for one ledger with one row | 2026-09-04 | `sorted({...})` over a comprehension reads as tidiness, and the three DEFAULT patterns are disjoint, so no run through them can ever produce the same file twice — which is why nothing held this for three releases. `--ledger` is where it bites: it takes any number of globs, and two overlapping ones are the ordinary way somebody widens a narrowed run. **The case was written against the per-ledger line first and stayed green under the mutation**, because each pass prints its own count; only the total adds them. That is `agent-contract` §15 catching a case that would have proved nothing. **Re-read 2026-09-05 after the windows CI leg at pull request #162 turned this row's own case red** (round 12's 🔴 8): the deduplication was on the string `glob.glob` returns, and `glob` keeps a literal pattern's spelling while joining a wildcard's matches with `os.sep` — so on Windows one file arrived as `seal/ledger.md` and `seal\ledger.md`, the set kept both, and *executed* here meant executed on the platforms whose `os.sep` is the one `glob` was handed. The fold is `os.path.normpath` now, which folds separators and `.` segments and no case, and a second case pins the class off Windows with `./seal/ledger.md` against `seal/ledger.md` — seen red on macOS before the change. This row's claim stands; what moved is the identity it deduplicates on, and where it had been executed. **Re-read again 2026-09-05 after round 13 measured that repair as half a repair and a regression**: `normpath` folded separators and dot segments alone, so on this case-insensitive volume `seal/Ledger.md` beside `seal/ledger.md` still read `total: 2 ok` and a symlink and a hard link made `3 ok`; and it collapses `lnk/..` lexically, so a pattern crossing a symlink before a `..` returned a path naming a different file than `glob` matched, `main` opened that file, and a ledger with a BROKEN row went unread at exit 0. The identity is `st_dev`/`st_ino` now with the `normcase(abspath())` fallback — the rule R3 records for `skipped_by_narrowing`, copied — and the path returned is the pattern's own spelling. R13 carries it with the three cases that were seen red first; this row's claim stands a third time |
| R10 · a recorded limit is re-derived rather than inherited, because a wrong one tells the next mutation battery that a live line is dead | `skills/code-review/scripts/chain_check.py#says_not_yet@61e75f01`, `skills/evidence-check/scripts/evidence_check.py#skipped_by_narrowing@91696b61`, `skills/evidence-check/scripts/evidence_check.py#seal_home@15082932`, `tests/test_evidence_check.py#test_a_copy_under_a_plugin_tree_without_a_skill_beside_it_is_still_vendored@46f93249` | **Executed** 2026-09-05 in round 4's fix pass: all four limits round 3 recorded as unreachable were re-measured, five mutations plus direct predicate probes. One stands (`says_not_yet`'s prefix guard); one keeps its conclusion with a new reason (`normcase`); one was false and half its guard is load-bearing at **20** red; one was not a limit at all and is now closed by a case | 2026-09-05 | **Three of four reasons were false, and the two that survived a round were the two nobody re-derived.** Round 4 reopened two of the four; the fourth — `seal_home`'s `SKILL.md` conjunct — was reopened only by enumerating the class (`agent-contract` §12), and it was the one real hole. The shape they share is that each explained a guard through a platform or a caller fact that is NOT what makes it unreachable, and a mutation battery cannot tell an unreachable guard from an unheld decision, which is why the prose is what the next battery reads. Both `# RIDER:` comments this branch left are gone: each stood in for a case, and both cases now exist |
| R11 · the floor's count stops at a record that WROTE fixes as well as at one that reopened, because `Needs a fix` and the verdict column answer different questions and come apart when the orchestrator fixes over a `no` | `skills/code-review/scripts/chain_check.py#wrote_fixes@2b2cc367`, `skills/code-review/scripts/chain_check.py#stopping_floor@ae898553`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_round_that_fixed_over_a_no_is_where_the_count_stops@475e733d`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_wrote_fixes_reads_the_verdicts_and_not_the_needs_a_fix_row@25542128` | **Executed** 2026-09-05 in round 7's fix pass, seen red twice: the planted case fails against the old walk with the floor's own sentence, and this repository's own records fail the same way the moment `round-7.md` exists — `test_this_repositorys_own_round_records_pass_the_per_record_checks` red at `9bf9584`, green at `9efd314` with nothing but the walk changed. Every earlier floor case stays green, and three quiet rounds are still refused because `answered` writes nothing | 2026-09-05 | **The record already carried the fact and one reader looked at it.** `closed_with_a_fix` is what refuses `no fixes to check` beside a `fixed` verdict, and the walk three functions down never called it, so a run whose verifying round answered `no` and whose orchestrator fixed anyway had NO terminal record any exit accepted — a reader after the `no` was a second uncounted record, and stopping at the `no` was refused both ways. `wrote_fixes` wraps the existing predicate; no second source of truth. The direction is ALLOW, one record wider in one sequence, and it is the cheaper mistake because the only way to satisfy the old walk was rewriting `fixed` to `answered` over fixes that exist — a false record, which is this work item's subject. The fixture needed three commits where every other floor case uses one, because a record naming a fix in its verdicts has to be ADDED before that fix exists; that is the sequence `round-6.md` itself went through. **Re-read 2026-09-05 in round 8's fix pass, which moved the anchor by docstring alone**: `stopping_floor`'s bold sentence now names both stops where it named one, four lines above a walk that already had both — round 8's 🔴 1, three of the rule's six copies left stating the old rule as the whole rule. The walk is unchanged: an AST comparison of `293a761` against `cd4fec2` with docstrings stripped finds the module identical, and `test_every_carrier_of_the_count_rule_states_both_stops` now pins all five carriers. **Re-read again 2026-09-05 in round 9's fix pass, and two things in this clause were false a round later**: the anchor moved by executable text this time — the refusal's message inside the error tuple `stopping_floor` returns now names both stops and refuses the false way out (round 9's 🔴 2, R12) — and the carrier pin was live for the protocol alone, so *pins all five carriers* was true of one; it is narrowed to that one and R12 says why. **Re-read a third time 2026-09-05 in round 10's fix pass**: the anchor moved by the refusal's message once more — it had denied the very stop that fired, and now names the record before the stop as the fault (round 10's 🔴 1). The walk this row is about is unchanged, AST-compared. **Re-read a fourth time 2026-09-05 in round 11's fix pass**: the refusal moved once more, to name the excess records by file instead of blaming the allowed verifying round (round 11's 🔴 1); the walk is still the one this row describes. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which moved `stopping_floor` by the reopening walk after the count; `wrote_fixes` is now read by both walks, and the count's second stop this row is about is unchanged |
| R12 · the floor's count rule has EIGHT carriers from one commit, the refusal's own message is the one a person reads, and a whole-file substring pin over a sentence is live only where the sentence's words occur nowhere else | `skills/code-review/scripts/chain_check.py#stopping_floor@ae898553`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_the_floors_refusal_names_both_stops_and_refuses_the_false_way_out@aff44eae`, `tests/test_the_run_stops_at_the_last_finding.py#test_every_carrier_of_the_count_rule_states_both_stops@7b6050c5`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_the_floors_refusal_does_not_deny_the_stop_that_fired@33c0e60b` | **Executed** 2026-09-05 in round 9's fix pass: the message case seen red against the old refusal, then green; the carrier pin reverted one carrier at a time by round 9 — green for the spec, template, skill and `chain_check.py`, red for the protocol alone, and green on an empty tuple | 2026-09-05 | `f187b39` wrote the count rule in eight places — the spec twice (`:743` exits table, `:760`), the template, the skill's own paragraph, the protocol, and `chain_check.py` three times (module docstring, `stopping_floor`'s docstring, the refusal message). Rounds 7, 8 and 9 corrected them three, three and two at a time, each round's fix pass believing its count was the whole set; the eighth is the one a person reads at the moment of failure, and it named a false `Needs a fix: yes` as the way out. **The pin round 8 wrote was hollow for four carriers of five**: `flat()` is the whole file, and *closed on a fix* / *`fixed` verdict* occur 3·3·1·6 times in those files for other reasons, so reverting the rule left the case green, and an emptied tuple passed vacuously. The repository owner narrowed it to the protocol — the one carrier where the words occur once — with a guard against an emptied tuple, rather than build an anchored window search from a fix pass, which is issue #161's rule 1 applied to the branch that opened it. The other four carriers keep corrected sentences and no pin, and that is written where a reader meets the case. **Re-read 2026-09-05 in round 10's fix pass, which found two more things in this row's own subject.** The eight carriers split two ways: six state the rule exactly — *stops at the first later record …, that record included* — and two, the spec's exits row and the refusal message, stated it as *none of them …*, which is false whenever the stopping record is counted, and it always is: the walk breaks AT that record. Round 9's fix added a second false clause of that shape to the message while repairing the first; both now state the count and the stop. *A third case was then written to pin that the message names the record before the stop as the fault — and round 11 found that blame wrong*: the record before the stop is the verifying round the floor allows, as the same message says three clauses later and every other carrier agrees; the one too many is the stopping record and every counted record after the first. The message names those by file now, and the case's floor record answers `yes` so that a verifying round is actually warranted in the fixture — the shape the earlier fixture avoided, which is how a false clause came to be pinned as true. And the narrowing left the pin's other assertion — `Needs a fix` in the file — a whole-file search over a phrase the protocol uses eleven times; it anchors on the rule's own sentence now. Three anchors here moved with those changes. **Re-read 2026-09-05 in round 11's fix pass**, the pass that corrected the blame the sentence above records; the three anchors moved again with it — the refusal's text, and both cases' fixtures and assertions. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**: `stopping_floor` moved by the reopening walk, whose refusal is a ninth message carrying `CAPPED_EXIT`; the count rule's refusal message and the two cases pinning it are untouched |
| R13 · `resolve_patterns` deduplicates by `st_dev`/`st_ino` — the identity R3 records one function down — and returns the pattern's own spelling, because the caller opens it | `skills/evidence-check/scripts/evidence_check.py#file_identity@e8a4f000`, `skills/evidence-check/scripts/evidence_check.py#resolve_patterns@4ecfd731`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_one_file_matched_under_two_cases_is_read_once@3c219f5f`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_symlink_and_a_hard_link_to_a_ledger_are_that_ledger@5f2cabbd`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_the_path_returned_is_the_one_the_pattern_named@3a33c35a` | **Executed** 2026-09-05 in round 13's fix pass, three cases seen red against the `normpath` fold first: two case spellings on a case-insensitive volume read `total: 2 ok`, a symlink and a hard link beside the ledger read `3 ok`, and a pattern crossing a symlink before a `..` exited 0 where exit 2 and one BROKEN row is the truth. All three green after the change, with R8's two cases and both suites | 2026-09-05 | **The first repair was composed where a copy existed.** The Windows CI leg found R8's case red at pull request #162 — `glob` keeps a literal pattern's spelling and joins a wildcard's matches with `os.sep`, so one file arrived as `seal/ledger.md` and `seal\ledger.md` — and the repair was `os.path.normpath`, thirty lines above a docstring that already states the identity this branch chose for this class in round 1. `normpath` closed the separator half and no more, and it collapses `lnk/..` lexically: where `x/lnk -> y`, the pattern `x/lnk/../ledger.md` names `<root>/ledger.md` and `normpath` returned `x/ledger.md`, so `main` opened a clean file where the operator named one with a broken row — a run reporting clean while something was missing, produced by this work item a third time. **The path returned is never normalized**, because the caller opens it; the inode is only the fold. `agent-contract` §13 stated: the earlier fix rested on *every alias differs only in separators and dot segments*, and the two-spellings case removed the `os.sep` guarantee alone; these three remove the case, link and symlink-traversal guarantees, and the Windows leg's green at the pull request is the platform's own answer. **The rule has one spelling, `file_identity`, and both folds read it** — round 14 found the repair had copied the nine lines from `skipped_by_narrowing` rather than extracting them, against that docstring's own closing sentence; with the guard dropped in the one copy, `test_an_inode_of_zero_does_not_fold_two_files_into_one` alone goes red. The third case states each platform's answer for where the pattern names: POSIX follows the symlink before the `..`, Win32 collapses the `..` first, and the windows leg went red on the version that asserted the POSIX answer everywhere (round 14's 🔴 1) |

## 0.8.1 — 2026-09-05

<!-- specs/1788597030-a-runs-rounds-come-mostly-from-the-tools-own-fixes-and-records -->
### 1788597030-a-runs-rounds-come-mostly-from-the-tools-own-fixes-and-records

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Nine rows. Most of what this branch built is either a script whose every
cell has a case reading it back, or prose in eight carriers that
`tests/test_the_rules_have_one_owner.py` reads sentence by sentence, so a
coordinate for each of those would be an inventory of the diff. What earns a
row is a judgment a later tidy-up would undo in good faith: the generator's
refusal to write anything a person typed (R1), the surface it measures and
the depth it refuses before writing (R2), a second walk beside the count that
looks redundant and decides a different question (R3), a bare word that
counts open while its homed form counts closed (R4), and four rule owners
whose links are what keep the count of carriers from growing again (R5–R8).
R9 is a claim that lived in `seal/ledger.md` as P4 until phase 4a rewrote
the sentence it was anchored on: the row was REMOVED there, as `CLAUDE.md`
says of a row whose anchor a change removes, and the claim is written here
against the coordinate that carries it now.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run; the
unscoped READ form ran afterwards and its lines are in `phases/phase-5.md`. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `new` derives every field row from something a person did not type into a cell, refuses a value it cannot write, and commits nothing | `skills/code-review/scripts/round_record.py#new@b9343b59`, `skills/code-review/scripts/round_record.py#build@355eabb6`, `skills/code-review/scripts/round_record.py#cell@ddd2e129`, `tests/test_the_record_is_generated.py#test_the_two_record_run_reads_back_through_chain_check@729f5bfe`, `tests/test_the_record_is_generated.py#test_a_flag_carrying_a_pipe_writes_no_record@48fe88b9` | **Executed** 2026-09-05 in phase 1: one case per field derivation on a fixture repository, the reach-back read back through the next round, the two-record run exiting 0 through `chain_check --worktree`; 29 mutations, 28 dead on the first pass and the survivor (`cell`'s empty-value guard) given its case and killed | 2026-09-05 | The refusal in `cell` is what keeps the record free of prose, and it is the line a later session would loosen first — a `--note` flag, or a cell that accepts a pipe because a finding's title had one. The last branch's 🔴 1 of round 2 was a semicolon inside a code span splitting a `New units` cell, and its 🔴 1 of round 4 was prose in an entry; neither can be written here. **`Fixes checked by` lands on one of two values, not one**: the spawn prompt named `nobody — the fixes are not yet written` alone, and `chain_check.fix_surface`'s own docstring says that value is false of a record that commissioned no fixes, so `landing_values` chooses between the two by the verdict words (`overview.md`, first divergence). The check runs told the pull request is a draft unless `gh pr view` says it is ready, because outside a workflow `pull_request_state` judges as READY and every mid-run record has an unchecked `Pass`. **Re-read 2026-09-05 in round 1's fix pass**, which moved `new` by one thing: `reach_back` returns the line to print, and a previous record reading `no fixes to check` is left as it is and said so (round 1's 🟡 6). Every field derivation and the refusal in `cell` are untouched. **Re-read 2026-09-05 at the 0.8.1 release preparation**, after the round 2 fix pass moved `build` by three lines: it calls `fenced_after` and appends the blocks under the probes table. Every field row is still derived, `cell` still refuses, nothing is committed, so the claim holds |
| R2 · `close` measures the fix surface from the range's two ends, refuses a depth-2 unit before any cell is written, and re-derives `Fixes checked by` from the verdicts the table left — `no fixes to check` when nothing closed on a fix word | `skills/code-review/scripts/round_record.py#close@5fa86549`, `skills/code-review/scripts/round_record.py#depth_two@04eeaf0b`, `skills/code-review/scripts/round_record.py#landing_values@87e329a1`, `tests/test_the_fixes_close_the_record.py#test_a_unit_added_beside_a_finding_inside_an_earlier_units_is_refused@516ed266`, `tests/test_the_fixes_close_the_record.py#test_a_capped_runs_last_record_reads_no_fixes_to_check_and_the_check_exits_zero@946d78c4`, `tests/test_the_fixes_close_the_record.py#test_a_table_with_a_fix_leaves_the_checker_cell_for_the_next_round@871d4a72` | **Executed** 2026-09-05: phase 2's depth-2 refusal seen red on a fixture whose `round-1.md` names `helper` and whose range adds `helper_guard` in the same file, 39 mutations then 8 re-run, all dead after four cases were sharpened; phase 5's capped case seen red at `3320945` — the cell read `nobody — the fixes are not yet written` and the check exited 1 refusing `Pass` beside it — and green at `edda427`; six mutations over `landing_values` and the write, each killed by a named case (the one that wrote the pending value back unconditionally was answer-neutral by construction and was re-run writing `no fixes to check`) | 2026-09-05 | The derivation has one spelling, `landing_values`, and both subcommands call it; a second copy in `close` is the tidy-up that lets the two disagree about which words commission a fix. The surface rows keep the measurement on every path — a bare `none` is what an empty range measures to, and a unit the range did add beside a table of deferrals is still a unit nobody has reviewed. `depth_two` raises before `raw` is touched, and the case reads the record back to show no cell moved. What the AST comparison over-reports is recorded in `phases/phase-2.md` rather than parsed away: a docstring mentioning `name(` counts as a call site, and a renamed file's units read as new. **Re-read 2026-09-05 in round 1's fix pass**, which moved all three anchors: `close` refuses a row for a finding the reviewer already closed and lists a name added in two files once (🟡 3, ⬜ 7); `depth_two` resolves a Location's path against the tree at the range's start, requires the unit to exist in that file, and reads a bare name against every file the range touched (🟡 4); the depth case now runs nine Location forms. `measure` skips prose (🟡 5). What this row claims still holds, and each change was seen red first in the same module. **Re-read 2026-09-05 at the 0.8.1 release preparation**, after the round 2 fix pass widened `test_a_unit_added_beside_a_finding_inside_an_earlier_units_is_refused` by one parametrize entry, `fragment-after-path`, for the `path#a` and `#b` form `FRAGMENT_RE` now reads. The refusal is still before any cell is written, so the claim holds and is one form wider |
| R3 · the floor's reopening is one — a second later record whose verdicts closed on a fix, after a record that met the floor, is refused for work items begun on or after `REOPEN_FROM`, by a walk that never stops | `skills/code-review/scripts/chain_check.py#stopping_floor@ae898553`, `skills/code-review/scripts/chain_check.py#wrote_fixes@2b2cc367`, `tests/test_the_reopening_is_one.py#test_a_second_fix_closing_record_after_the_floor_is_refused@6a084c1e`, `tests/test_the_reopening_is_one.py#test_the_records_between_do_not_matter@531d0696`, `tests/test_the_reopening_is_one.py#test_a_second_reopening_prints_for_an_item_begun_a_second_before@099dc086` | **Executed** 2026-09-05 in phase 3: each refusal seen red at a named fixture before the walk existed, the cutoff at the boundary second, the two-record run kept green, this repository's own records passing under the arm; nine mutations, nine killed — the walk refusing at three, the walk reading `Needs a fix`, the cutoff inclusive the wrong way, the message naming the first record, the message naming no exit | 2026-09-05 | **Two walks over the same `later`, and the second is not the first restated.** The count stops at the first later record that reopened or closed on a fix and refuses a quiet round before that point (#81's shape); it was unbounded by construction because every record it stops at met the floor, so it restarted there. The reopening walk counts `wrote_fixes` across the whole of `later` and refuses the second — `wrote_fixes` and not `run_reopened`, because what a reader is owed is fixes and a `deferred` verdict wrote none, which is what `test_the_records_between_do_not_matter` separates. Failure direction blocks more; what it lets through is a defect the second reopening would have found, shipped as an issue (Q2) |
| R4 · `deferred <home>` is a closing word and not a fix word, and a bare `deferred` comes back as `deferred (no home)` — in neither set, so it counts OPEN | `skills/code-review/scripts/chain_check.py#verdict_of@3fd16b52`, `skills/code-review/scripts/chain_check.py#HOME_WORDS@f479adf0`, `tests/test_the_reopening_is_one.py#test_bare_deferred_reads_as_open@d0aa23d4`, `tests/test_the_reopening_is_one.py#test_deferred_with_a_home_reads_as_closed_and_not_as_a_fix@fab8a4b4`, `tests/test_the_reopening_is_one.py#test_the_word_is_a_closing_word_and_not_a_fix_word@b02a8bd9` | **Executed** 2026-09-05 in phase 3: the four bare readings and the ready-pull-request refusal of the bare word passed before the word existed (open before, open after) and were shown red afterwards by mutation, the home guard removed; `deferred #170`, `**deferred** #170` and `deferred seal/follow-up.md` read closed through the ready-pull-request path | 2026-09-05 | Every unreadable verdict comes back as the normalized cell, which counts open because it is in neither set — and the bare word IS the closing word, so returning the cell would count it closed. `HOME_WORDS` names the closing words that close nothing without a home, and `open_blocking`'s message then reads *this 🔴 row reads `deferred (no home)`*, a line a person reads and a pin holds. A separator after the word counts as nothing: `deferred —` is bare. `templates/config.md`'s exclusion list is derived from `CLOSED_WORDS` by a pin, so the word is a config-template edit too |
| R5 · the spec owns the reopening: after a record that met the floor at most one later record may close on a fix, the exit is `capped` with four cells and a pull-request line, the direction is *blocks more* with what it lets through stated | `` `docs/review-chain-spec.md#"##### The reopening — one, and then the run is capped"@d8a14583` ``, `tests/test_the_reopening_is_one.py#test_the_subsection_states_the_rule_the_refusal_the_cutoff_the_direction_and_the_exit@0268625f`, `tests/test_the_rules_have_one_owner.py#test_the_count_rules_sentence_is_the_owners_and_its_links@52d1da56` | Read 2026-09-05 at `3320945`; the pin seen red with each needle stashed in phase 3, and the carrier walk seen red with a second *at most one more round record* inserted in phase 4a | 2026-09-05 | The count rule had eight carriers from one commit on the last branch and three fix passes corrected them three, three and two at a time (`seal/ledger.md` R12). This subsection is the one carrier that states the bound; `skills/code-review/SKILL.md`, `templates/sdd-round.md`, `docs/review-handoff-protocol.md` and `agents/warden.md` link to it in one sentence each, and the walk in `test_the_rules_have_one_owner.py` holds the phrase to those files at a ceiling of four. A fifth carrier stating the rule is the state R12 records, arriving again |
| R6 · 🟡 has a threshold — *a defect the release would ship* — and ⬜ exists beside it for a sentence that reads badly while the behaviour and the fact stay right, which `Needs a fix` never counts | `` `skills/code-review/SKILL.md#"## Findings format"@063f14ca` ``, `tests/test_the_rules_have_one_owner.py#test_a_note_severity_exists_and_needs_a_fix_does_not_count_it@67e8fa6f` | Read 2026-09-05 at `3320945`; the pin seen red with the ⬜ line stashed in phase 4a | 2026-09-05 | Half of the last branch's 53 🟡 were true sentences about prose, and each cost a fix pass and a reader. The threshold-less line, *🟡 fix or justify — divergence with grounds*, was the one the reviewer read; `agents/warden.md` §Report links here with the one question per finding, *would the release ship a defect if this stands?*, and owns nothing. `templates/sdd-round.md`'s severity comment under the verdicts table still reads without ⬜ — short rather than wrong, since a generated record carries no comment, and left as it stands |
| R7 · the warden's report carries the three tables in the record's own headers under the headings `round_record.py new` parses, plus the two terminal lines, and the headers are one constant across both files | `` `agents/warden.md#"## Report"@8a7c9959` ``, `` `agents/warden.md#"## Verdicts"@933ffe83` ``, `tests/test_the_record_is_generated.py#test_the_wardens_report_headers_are_the_generators_constants@2f5cd4b7`, `tests/test_the_rules_have_one_owner.py#test_the_warden_hands_both_terminal_lines_to_the_generator@80d80d6c` | Read 2026-09-05 at `3320945`; the header pin compares by value across two loads of `chain_check.py` (an `is` comparison is false there, `phases/phase-1.md`), and was seen red with a header word changed in phase 1; the terminal-lines pin seen red with each copy of the generator sentence stashed in phase 4b, after a prefix pin stayed green over one copy | 2026-09-05 | *What breaks in six months* in `plan.md`: the report headers and the parser are two carriers of one shape. The mitigation is that the generator's headers are `chain_check`'s constants and this pin holds the warden's text to them, so a header reworded in the agent file goes red here rather than producing a record with an empty verdict table. The sentence *`round_record.py new` writes the record from this report* appears twice in the file and each copy is pinned by its own tail, because a pin on the shared prefix stayed green when either copy was stashed |
| R8 · a fix pass hands over a fix table under `## Fixes` and writes no `phases/phase-N.md` and no `plan.md` row; `close` applies the table and measures the depth rather than taking it from the hand-back | `` `agents/smith.md#"## Phases"@194f9b4a` ``, `tests/test_the_fixes_close_the_record.py#test_the_smith_hands_over_the_fix_table_and_writes_no_phase_record@6310b1f8`, `tests/test_the_fixes_close_the_record.py#test_the_implement_skill_says_the_same_in_section_five@9cf517a7` | Read 2026-09-05 at `3320945`; both pins seen red with the sentence stashed in phase 2, and the `smith.md` pin sharpened after a mutation was satisfied by the neighbouring paragraph | 2026-09-05 | On the last branch phases 5–17 were fix passes and each wrote a phase record, which is where the 12.8 h went. The rule is owned here and linked from `skills/code-review/SKILL.md` §*a fix pass resumes the implementer* and `skills/implement/SKILL.md` §5. `## Phases` is the whole numbered procedure, so this row drifts on any edit to it; the alternative, a minor anchor on the sentence, is the escape hatch `templates/ledger.md` reserves for a unit measured to drift on unrelated edits, and one release is not that measurement — re-read the claim when it drifts and re-stamp. **Re-read 2026-09-05 in round 1's fix pass**: `## Phases` gained two sentences in the fix-pass paragraph — one row per OPEN finding, and a correction closing `answered — corrected at <sha>` rather than `fixed` (🟡 3, 🟡 2) — and both pins gained the sentences, each seen red with the sentence stashed. The claim holds |
| R9 · `templates/sdd-round.md` carries `## What this round was asked` between the field table and `## Verdicts`, and the round paragraph of the spawn prompt is copied into it right after the report posts — by `round_record.py new` from the `--asked` file, the one thing the orchestrator still writes by hand | `templates/sdd-round.md#"## What this round was asked"@22af8462`, `` `skills/code-review/SKILL.md#"## Cross-session records — `seal/specs/<work-item-id>/`"@986af880` ``, `skills/code-review/scripts/round_record.py#build@355eabb6`, `tests/test_a_segments_record_says_what_it_was_asked.py#test_the_round_section_is_between_the_field_table_and_verdicts@94f24e70`, `tests/test_a_segments_record_says_what_it_was_asked.py#test_the_copy_instruction_sits_beside_the_round_record_starts_from_line@19bd16d3`, `tests/test_the_record_is_generated.py#test_the_asked_section_is_the_file_verbatim@20e553ba` | **Executed** 2026-09-05 at `edda427`: the nine cases of `test_a_segments_record_says_what_it_was_asked.py` pass with the sentence rewritten, and `test_the_asked_section_is_the_file_verbatim` reads the paragraph back from a generated record; the empty-paragraph refusal was shown red by mutation in phase 1 | 2026-09-05 | Was `seal/ledger.md` P4, anchored on *A round record starts from `templates/sdd-round.md`* in the skill's §Cross-session records. Phase 4a rewrote that sentence — a record is generated by `new` and the template is its shape rather than its source — so the anchor went BROKEN on this branch's unscoped read and the row was removed there rather than re-pointed. The claim that survives is the copy instruction and its moment: *right after posting* is still read raw on one line by `test_skill_carries_the_copy_instruction`, which is why phase 4a kept the phrase unwrapped (`phases/phase-4a.md`). What moved is WHO copies: the section's own words now name `new`, and `build` refuses an empty paragraph because the section is the durable home of what the round was told to attack (#119). **Re-read 2026-09-05 at the 0.8.1 release preparation**, after the round 2 fix pass moved `build` by the `fenced_after` call; the round paragraph is still copied from the `--asked` file into the section between the field table and `## Verdicts`, so the claim holds |
