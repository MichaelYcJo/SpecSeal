<!-- specs/1788749195-the-record-drops-the-fix-and-a-pipe-truncates-the-row -->

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Two rows over the two tickets that share this branch. R1 is #189: not the
escaping, which a reader can see, but WHICH reading of a row is taken and on
what evidence — the measurement over this repository's own records is what
ruled out the obvious repair, and it is not recoverable from the diff. R2 is
#187's empty arm, which is a decision about what a generator may claim rather
than a behaviour anyone would guess from the code.

Neither row records the fact that a pipe used to truncate a row or that the
fenced blocks used to reach no file. Those are in the issues, in
`plan.md`'s Technical context, and in the two phase records; a ledger row
carries the claim the code grounds today. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · a report row is re-serialised into the record rather than copied, in three readings: the plain one wherever it fits, then the code-span reading CAPPED at the header's width, then the plain cap, which leaves the surplus in the LAST cell. Every reading goes through `raw_cells`, so a `\|` inside an HTML comment is never a break. Not a fold of already-split cells, which cannot restore the reviewer's spacing | `skills/code-review/scripts/round_record.py#split_cells@a3412574`, `skills/code-review/scripts/round_record.py#row_cells@a20d32f9`, `skills/code-review/scripts/round_record.py#copied_row@793dbca0`, `tests/test_the_record_is_generated.py#test_the_plain_reading_is_the_readers_own@2b22ee7a`, `tests/test_the_record_is_generated.py#test_a_pipe_inside_a_code_span_stays_in_its_own_column@68ff63b3`, `tests/test_the_record_is_generated.py#test_a_pipe_in_a_column_that_is_not_the_last_stays_there@92b188ea`, `tests/test_the_record_is_generated.py#test_a_bare_pipe_before_the_last_column_keeps_its_text@6a08a411` | **Executed** 2026-09-07. The sweep that chose the shape: every body row of all 125 committed `seal/specs/*/rounds/round-*.md` records split and compared against its own header — eight are over-wide, and all eight have their pipe inside a code span. Replayed through `copied_row` at `007436a`, all eight come out at header width and seven recover the reviewer's own column. Nine mutations, one per site and one per branch, all red | 2026-09-07 | **The measurement is the row's real content.** The obvious repair — fold a row's surplus cells into the last column, because the last column is the free-text one in every table the record copies — is refused by two of the eight. One of them (work item `1788433011`, round 2) has its pipe in the probes table's FIRST column, which is a command, and folding moves half the command into `Result`. Another (work item `1788501054`, round 9) has it in the Finding column, where folding shifts every later column left and the verdict cell — which `chain_check` reads — comes out holding the location. **The cap is a guess and it is the only one available**: nothing in a flattened row says which column a bare `\|` came from, so a bare one before the last column lands the rest of the row in the last cell. That is never worse than the record's earlier behaviour, because the leading cells land at the same indices either way, and it makes the text visible rather than dropped. **`split_cells` with both knobs off has to BE `reader.split_row`**, which is why `row_cells` takes the reader's own answer wherever it fits and reaches for its own spelling only for the over-wide row: the record is read back through that function at the pull request, so a cell composed by one spelling and read by another is a cell CI reads differently from the record. **Corrected 2026-09-07 by review round 1, findings 1 and 2.** Two things were wrong and both were about a text this row did not name. `table_body` counts a row's columns on `strip_comments(report)` and this function is handed `raw`, so a `\|` inside an HTML comment was a break to one and not the other: the record lost a column at exactly header width, nothing downstream complained, and the Location stood in the Verdict cell `chain_check` reads — the placement `plan.md` refuses the fold for, reached by the fix that refused it. And a row carrying BOTH a code-span pipe and a bare one was over-wide under either reading alone, so it fell to the plain cap, which re-split the reviewer's code span with the same result. The comment rule now lives in `raw_cells` rather than as a flag at each call site: written per site, removing any ONE left every case green, because a later reading recovers what an earlier one splits wrongly. The uncapped span reading that stood first is REMOVED — a reading landing exactly on the width is unchanged by a cap of width minus one, and it is reached only where the plain reading is over — and it was measured identical over all 4128 body rows of this repository's committed records and 432 generated rows before it went; while it stood, dropping its `spans=True` left the module green. Re-run after the repair: `copied_row` changes exactly the eight over-wide rows and no other of the 4128. **The eighth row is the stated limit made visible** — its backtick runs are unbalanced, so the code-span reading swallows every break and lands at 2 against a header of 5, the plain reading takes it, and its tail lands in the last column. That is why the code-span reading is CHECKED against the width rather than trusted. The eight rows themselves belong to other work items and are untouched here |
| R2 · the record's `## Paste-ready fixes` says what the generator OBSERVED — `no paste-ready fix in the report` — and never that none was needed, and a report with no fence under the heading is written rather than refused | `skills/code-review/scripts/round_record.py#NO_PASTE_READY@5a1e9728`, `skills/code-review/scripts/round_record.py#PASTE_READY@754a5748`, `skills/code-review/scripts/round_record.py#READ_HEADINGS@3f93dc12`, `tests/test_the_record_is_generated.py#test_a_report_with_no_paste_ready_fix_is_still_a_record@db2d5d71`, `tests/test_the_record_is_generated.py#test_an_empty_paste_ready_section_says_the_report_carried_none@976a01eb`, `tests/test_the_record_is_generated.py#test_prose_under_the_paste_ready_heading_stays_in_the_report@726d0673` | **Executed** 2026-09-07. A report whose verdicts all close and whose heading is absent writes the record with that sentence; so does a heading with only prose under it. Six mutations of the section's wiring, all red | 2026-09-07 | **A generator cannot tell a round that needed no fix from a reviewer who wrote none**, and either claim would be the record asserting something nobody checked. The sentence names the report because the report is what was read. Beside an open row in the verdict table two sections up, that line IS the gap written down — which is the whole of what `plan.md` offered against a reviewer omitting the heading, so it is a load-bearing wording rather than a default string. **The empty arm is a scenario and not an edge case**: a verifying round that opens nothing writes no fix, and `spec.md` has it as a row. A refusal there would stop an unattended run over a report that is correct. **The section carries the fences and no prose**, which is what keeps a file `chain_check.py` reads free of text nobody parses — the reason `plan.md` refused keeping the report verbatim below the tables. **`READ_HEADINGS` is why the guard covers it**: `swallowed`'s heading loop reads that constant and its row loop still reads `REPORT_TABLES`, because only a table can lose its rows to a fence while its heading stands |
| R3 · `swallowed`'s two loops read two lists. The heading loop reads ~~`READ_HEADINGS`~~ (**corrected by R4 below — `REQUIRED_HEADINGS`**) and the row loop reads `REPORT_TABLES`, because only a table can lose its rows to a fence while its heading stands | `skills/code-review/scripts/round_record.py#READ_HEADINGS@3f93dc12`, `skills/code-review/scripts/round_record.py#swallowed@71c71886`, `tests/test_the_record_is_generated.py#SECTION_TEXT@a6f8fb79`, `tests/test_the_record_is_generated.py#test_a_fence_that_takes_a_section_the_generator_reads_is_refused@29053fab` | **Executed** 2026-09-07. Mutating the heading loop back to `REPORT_TABLES`, and `READ_HEADINGS` back to the table headings alone, each turn the parametrized case red on its new member | 2026-09-07 | **This narrows `seal/ledger.md`'s F1, which said the derivation was from two constants**, and F1 is corrected there rather than removed: its anchor still resolves and what moved is the content inside it. The derivation itself is unchanged in kind — a section added later is guarded, and gets a case, by being added to a constant, and the parametrized case still reads its own parametrization back out of one. What the split records is WHY there are two lists, which is the sentence a later reader would otherwise have to rediscover by asking why the loops differ. **`PASTE_READY` is the instance that made the constant earn itself**: it arrived after `READ_HEADINGS`' predecessor was introduced against a hypothetical, and it was guarded by one line changing |
| R4 · the swallow guard refuses a report over the REQUIRED headings only, in BOTH of its loops. Its premise — a heading hidden by a fence and absent outside it was swallowed — is an inference that holds for a section the report must carry and not for one it may leave out, and the row loop answers the same question vacuously for a section with no table | `skills/code-review/scripts/round_record.py#REQUIRED_HEADINGS@7e722eb8`, `skills/code-review/scripts/round_record.py#swallowed@71c71886`, `tests/test_the_record_is_generated.py#test_a_fence_quoting_the_optional_heading_is_kept_when_it_is_absent@f2d4a118`, `tests/test_the_record_is_generated.py#test_a_fence_that_takes_a_section_the_generator_reads_is_refused@29053fab` | **Executed** 2026-09-07. A report omitting `## Paste-ready fixes` while its probes fence quoted it: exit 2 and no record before, exit 0 and the record written after, with the quoted block still carried inside the probes fence. Four mutations — the loop widened back, the constant widened, the loop emptied, the second half of the condition dropped — all red | 2026-09-07 | **This corrects `seal/ledger.md` F3 and R3 above, and the scenario that falsified it is the one F3's own Notes describe**: a reviewer of this generator pastes record-shaped blocks, headings and all, and the round that quotes the empty section's own sentence is exactly the round that wrote no fixes. F3's two-half condition still holds; what was wrong is the set it ran over. **The false negative is accepted rather than unnoticed**: a report that DOES carry fixes and whose heading a closed fence swallows now writes a record saying it carried none. Nothing can tell that from a round that wrote none — outside the fence the two texts are identical — and the record shows the gap beside the open rows in the verdict table above. A refusal would stop an unattended run over the legitimate case to catch the unlikely one, which is the trade `CLAUDE.md`'s first goal decides. **The two constants differing by exactly the optional section is what the pair of cases asserts**, so a section added to one and not the other is a red case rather than a silent widening of what may be refused. **Extended 2026-09-07 while closing round 2's 🟡 1, by assuming a further instance of its class rather than waiting for one.** The ROW loop's exclusion of the optional section was pinned by nothing: widening it to `READ_HEADINGS` left all 78 cases green, and it is not a dormant branch — a section with no table of its own answers *do its rows stand only inside a fence* vacuously, so any fenced fix carrying a markdown table would read as hidden rows and be refused. This repository's own paste-ready fixes carry tables. Executed: with the loop widened, a report whose fix is a fenced Deferred table is refused; the new case is red there and green here |
| R5 · the pre-merge reminder decides a record is closed by reading it through the shared reader, so a closing word counts only where a reader would read it — ~~inside a fenced block~~ (**both arms: `readable` is `blank_fences(strip_comments(...))`, and it is the COMMENT arm that changes the verdict on every real record, corrected 2026-09-07 by round 2's 🟡 1**). Without the reader it falls back to the raw text rather than raising | `hooks/review-history-guard.py#is_closed@24b27511`, `hooks/review-history-guard.py#reader@20048595`, `tests/test_chain_hooks.py#test_a_pasted_fix_does_not_read_as_a_closing_note@de709750`, `tests/test_chain_hooks.py#test_a_real_closing_note_still_silences_the_merge_reminder@21068743`, `tests/test_chain_hooks.py#test_the_guard_falls_back_to_the_raw_text_without_the_reader@df85505e`, `tests/test_chain_hooks.py#test_the_reader_is_what_makes_a_fenced_closing_word_not_count@062d781a` | **Executed** 2026-09-07, round 1's 🟡 8. A record whose only closing word stands inside a pasted fix, beside a live Deferred row: the reminder was silent before and fires now. Four mutations — the reader never consulted, `reader()` always None, an unreadable record made to nag, the drain phrase dropped from the pattern — all red | 2026-09-07 | **The section is what made a pre-existing defect reachable everywhere.** A probes fence could already carry a closing word, so the shape predates this branch; putting the reviewer's code into EVERY record is what turned an unlikely coincidence into the ordinary case, in a run whose own fixes are fixes to this vocabulary — the record that found it carries `closed` and `drained` inside a fence. **`reader()` returns None rather than raising**, and §13 is why the fallback is executed rather than argued: the reader is reached by a relative path from `hooks/` into `skills/`, and a copy of the plugin without that directory must leave the hook printing a possibly-wrong reminder rather than stopping a session's Bash call. The case removes the guarantee by pointing the constant at a file that does not exist. **What the fallback gives up is stated**: without the reader the hidden word counts again, which is the pre-fix behaviour. **`reader()` has three reachable failure paths and each takes a different input** (**corrected 2026-09-07 by round 3's finding 1, which measured a fourth path with no constructible input and one reachable path no case watches — the ticket carries it**), which one case could not see: an absent path raises `FileNotFoundError` into the `except`, while a file Python has no loader for makes `spec_from_file_location` return None and never raises. Executed both; with a single parameter, deleting the `spec is None` arm left the module green. **Corrected 2026-09-07 by round 2's 🟡 1, and the correction is which arm does the work.** Every sentence written for this row named the fence, and the fence is the synthetic half: swapping `readable` for `blank_fences` left all 24 cases of the module green, while swapping it for `strip_comments` turned two red — so the arm nothing watched was the one carrying the behaviour. Re-executed over the 127 committed round records: the reader changes the verdict on three, **all three on the comment arm and none on the fence arm**, each of them an HTML comment the round wrote to narrate itself, beside live `Deferred` rows — **corrected 2026-09-07 by round 3's finding 4**: two are the record's header comment (*It closed all five*) and the third is a note beside the field table, seventeen lines in, saying the loop is *not closed by one more small fix*. The load-bearing half is the comment, not its position. The case is parametrized over both passes rather than written for one, and each parameter turns red on its own arm |
