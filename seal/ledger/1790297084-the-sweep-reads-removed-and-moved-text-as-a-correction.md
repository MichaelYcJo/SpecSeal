| P1 · where one key left two paths and arrived at a third, the arrival pairs with the departure whose path shares the most keys with its own (the distinct keys that left the one and arrived at the other), then with the one whose path is gone at the range's tip, then with the first in path order; so a move pairs with its own origin, and the report's `corrected` line names the correction and never the path that only moved, with the places and scores unchanged | `skills/code-review/scripts/survivor_check.py#paired_across_paths@53cabf4b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_move_pairs_with_its_own_origin_and_the_correction_is_named@178d519a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_split_pairs_with_its_own_origin_while_both_files_remain@9c499a46`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_path_order_does_not_decide_which_departure_is_the_source@9e1642f2`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_one_sentence_move_pairs_with_the_path_gone_at_the_tip@26faea18` | **Executed** 2026-09-25. At `2e0e2fa7` the whole-file move, the split and the one-sentence move each named the moved file, `docs/m.md`, as the correction; the case where the moved file sorts first was green there by design. Green after. Mutated from Python, the file restored from a copy each time: affinity out of the order, the split red; gone-at-`b` out, or the paths at `b` not passed, the one-sentence move red; the order reduced to reversed path order, the sorts-first case red; the one-for-one pop replaced by pairing whole paths, M6, M7 and all four red | 2026-09-25 | #592. Counted per path rather than per sentence pair, so a fold moving hundreds of rows that share a date cell costs one entry per path pair. The verdict and score cannot move, because the key and so its n-grams are the same for every departure it could pair with. `RELEASE_RANGES`' four pinned ranges report the same coordinates at the phase's commit |
| P2 · a work item directory the range retired is read at the range's left end, and its sentences take part in the pairing across paths before they leave the removed wording; so a sentence a fold carries verbatim from the retired `spec.md` into `docs/` pairs with its own origin and is held, and a correction the same range made elsewhere stays a source and its other copies are reported | `skills/code-review/scripts/survivor_check.py#corrected@e3c7e3f3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_verbatim_fold_hides_no_correction_the_same_range_made@739e30ce`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_retired_directory_is_not_corrected_wording@e1f6690a` | **Executed** 2026-09-25. The fold case red at `4665def0` (phase 1's tip, whose retirement handling is the base's): exit 0, `against 0 sentence(s)`. Green after. Mutated from Python, the file restored from a copy each time: the retired paths dropped before the pairing again, the fold case red; the drop after the pairing removed, the fold case and #517's A8 red. #581's real fold, `f673e3b1..8e13036c` and its squash `3c3f2342^..3c3f2342`, at `2e0e2fa7` and at `4910e445`: exit 0, 350 files, 44 removed, 0 places under both | 2026-09-25 | #591. The fold case's tie is one shared key each way, so the pairing's gone-at-`b` rank (P1) is what sends the arrival to the retired `spec.md`: a retired directory is always gone at the tip. Failure direction: toward reporting, because retired departures can only take pairings away from live ones, which keeps those as sources and their arrivals out of `written` **Re-read 2026-09-25 by work item 1790297084 (#603), phase 3**: after the pairing across paths `corrected` now also drops the sentences of a ledger row the range removed because an anchor of the row left the code (`removed_ledger_rows`), beside the retired-directory drop; nothing this row claims reads a removed ledger row, and the claim holds |
| P3 · after the pairing across paths, the sweep drops from the removed wording the sentences of a ledger row the range removed, where the row was a live table row of a ledger file at the range's left end, its line is not in that file at the tip, at least one of its anchors resolves at the left end and not at the tip (`evidence_check.py#resolve_unit`, loaded by path), and no live row of that file at the tip cites every anchor of it that still resolves there; so a row removed because its code was is no correction, and a row corrected in place, re-pointed, quoted in a fence or comment, or removed with every anchor standing stays measured | `skills/code-review/scripts/survivor_check.py#removed_ledger_rows@a2bb5fe2`, `skills/code-review/scripts/survivor_check.py#corrected@e3c7e3f3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_removed_with_its_anchor_is_not_a_correction@4fc0057a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_corrected_in_place_is_still_a_correction@4b149e9f`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_removed_while_its_anchors_resolve_stays_measured@e2e14d33`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_one_anchor_leaving_a_file_that_remains_is_enough@e4b7abb1`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_quoted_in_a_fence_or_a_comment_stays_measured@0272a0d8`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_rows_claim_carried_to_a_new_row_is_held_not_written@df409bbe`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_corrected_in_place_while_an_anchor_is_renamed_is_a_correction@87f69a04`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_row_sharing_one_live_anchor_with_another_row_takes_the_exit@0f180074`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_whose_anchor_never_resolved_here_stays_measured@f0171ead`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_whose_line_still_stands_takes_no_exit@511e6863`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_rows_cells_are_the_lines_the_row_stood_on@08b450cb`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_measured_range_that_removed_three_rows_reports_nothing@318c0eb4` | **Executed** 2026-09-25. At `4910e445` the removed-row case and the one-anchor case exited 1, and `58629718^..58629718` exited 1 with 8 places against 98 removed sentences; the re-pointed case exited 0 at `afb03a4c`, before the still-standing condition existed. Green after; `58629718` exits 0 against 52. Mutated from Python, the file restored from a copy each time, thirteen mutations over the sixteen cases, each red at least once: every removed row line exiting; the standing-line test, the live-line test, either half of the anchor condition, the still-standing test, "every" read as "any", the empty-set guard, a 0-based line count, the drop moved before the pairing, the releases shape, and the refusal each taken out | 2026-09-25 | #603. The still-standing condition was not in `plan.md`: Q2 found it (`phases/phase-3.md`). On #589's squash `seal/releases/0.15.1.md` R1 was corrected in place while the same commit renamed one of its tests, so its corrected claim took the exit under the plan's three conditions. It only keeps rows measured, so it moves toward reporting. What stays silent: a row retracted as false in a range that also removes one of its anchors, which the ledger's rules route to correction in place instead (`plan.md` §*What breaks in six months*) |
| P4 · the sweep's four ledger locations, `LEDGER_SHAPES`, are the tails of `evidence_check.py#default_patterns` in shared mode, and a copy of the sweep without `evidence_check.py` beside it refuses with exit 2 and a sentence naming the file, rather than reading every removed row as a correction | `skills/code-review/scripts/survivor_check.py#LEDGER_SHAPES@ff90ea38`, `skills/code-review/scripts/survivor_check.py#evidence@e8efd32b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_ledger_shapes_are_the_ones_the_ledger_checker_reads@9c20f2d6`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_missing_ledger_checker_refuses_rather_than_measuring@39a87e69` | **Executed** 2026-09-25. Both cases red before the units existed (AttributeError at `4910e445`). Mutated: the releases shape dropped, the shapes case and the real-range case red; the missing-file test disabled, the refusal case red | 2026-09-25 | Loaded only when the range's left end holds a ledger file, as `retired_directories` loads its reader only when it has a candidate. Work item E of this release edits `evidence_check.py`; this work reads `ANCHOR_RE`, `resolve_unit` and `default_patterns` and edits nothing there |
