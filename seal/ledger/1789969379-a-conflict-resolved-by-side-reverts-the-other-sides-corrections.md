| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| C1 · A correction marker is the verb and the date — a rewording of the sentence after it is not a loss, and a short qualifier before it is part of the same marker | `skills/evidence-check/scripts/correction_check.py#markers@4f42d0e2`, `skills/evidence-check/scripts/correction_check.py#MARKER@17486986` | **Executed** 2026-09-21. `test_a_marker_is_the_leading_verb_and_date_and_nothing_after_it`, `test_a_reworded_sentence_around_a_standing_marker_is_not_a_loss`, `test_a_qualifier_between_the_verb_and_the_date_is_the_same_marker`, `test_a_resolution_that_rewords_the_qualifier_is_not_a_loss`, `test_a_qualified_marker_reverted_at_a_merge_is_reported`, `test_a_run_long_enough_to_be_a_sentence_is_not_a_qualifier` and `test_a_capital_word_after_the_verb_is_not_a_qualifier` and `test_the_longest_qualifier_the_tree_carries_is_seen`. Three mutations, each red on its own case: the qualifier narrowed back, the bound widened to twelve words, and the lowercase gate opened | 2026-09-21 | **Corrected 2026-09-21 by review round 1, finding 1.** This cell read *at least three spellings of the `Corrected` sentence stand in `seal/ledger.md` today*, which was true and was not the measurement the claim needed: all three vary AFTER the date. Measured over the whole file: **403 marker occurrences on 190 rows**, of which a `<verb> <date>` pattern sees 365. The other 38 put a qualifier between the verb and the date in nine spellings — `again` 19 times, `and re-executed` 5, `a third time` 4, `a fourth time` 3, `and re-stamped` 2, `a fifth time` 2, and one each of `and re-stamped again`, `and widened` and `and re-measured` — and one row carried no other spelling, so it was invisible to the check entirely. **Corrected again 2026-09-21 by review round 2, finding 6.** This cell said *the check now sees 403 of 403*, and it was 403 of **404**: the count came from the pattern itself, so the spelling it could not see was missing from the census that justified its bound — the same circle one layer down, since the sentence above it was written to correct exactly that error in the frame. line 1172 of `seal/ledger.md` spells a marker `Re-read and re-stamped a third time <date>`, five lowercase words where the bound was four. The figures here are now taken by an instrument that is NOT the pattern and has no bound — for every verb, the next date on the same line, counting the words between — and they read **404 occurrences on 190 rows**, 401 in cells and 3 in prose, of which a bare `<verb> <date>` sees 365 and 39 carry a qualifier in ten spellings. The run lengths are 0, 1, 2, 3 and 5 words and there is no run of four, so the old bound matched exactly what a bound of three would have. The check sees 404 of 404 and invents no qualifier the file does not carry |
| C2 · Both verbs count, and `Re-read` is the common one — a check watching `Corrected` alone would watch 10 rows and ignore 185 | `skills/evidence-check/scripts/correction_check.py#VERBS@0257b870` | **Executed** 2026-09-21. `test_both_verbs_count_because_re_read_is_the_common_one`, and two merge-level cases that go red separately so a check watching one verb cannot pass both | 2026-09-21 | Counted in the tree: `grep -c` over `seal/ledger.md` gives 10 rows for `Corrected` and 185 for `Re-read`; the occurrence counts are 12 and 353, because a row can carry a marker in more than one cell. The work item merged immediately before this one re-read and widened four rows of the shared file and wrote `Re-read` on every one |
| C3 · A marker that vanishes WITH its row is `REMOVED` and correct; one that vanishes while its row stands is the defect. The check reports only the second | `skills/evidence-check/scripts/correction_check.py#losses@47f747b5`, `skills/evidence-check/scripts/correction_check.py#standing@c0609bbb` | **Executed** 2026-09-21. `test_a_marker_that_went_with_its_whole_row_is_not_a_loss` (A3) and `test_a_merge_that_removes_the_whole_row_reports_nothing`, both red against a `standing` that accepts any row of the result | 2026-09-21 | `CLAUDE.md` §*A row whose anchor a change removes is REMOVED, not re-pointed* is the rule, so this is the repository's own distinction rather than one the check invents. The permissive mutation is the one that matters: the first mutation tried returned the PARENT's row, the marker counts cancelled, and A3 stayed green over a survival test that had been switched off. **Re-read 2026-09-21** after round 1's fixes moved `losses` and `standing`: the claim stands unchanged, and `test_a_marker_that_went_with_its_whole_row_is_not_a_loss` is still red against a permissive `standing`. |
| C4 · A row is identified by its first cell and by its content anchors, and NEITHER identity answers where it holds two answers | `skills/evidence-check/scripts/correction_check.py#Row@d249f5f5`, `skills/evidence-check/scripts/correction_check.py#_index@7933d9ac`, `skills/evidence-check/scripts/correction_check.py#standing@c0609bbb` | **Executed** 2026-09-21. `test_a_row_whose_own_claim_cell_was_corrected_is_found_by_its_anchors`, `test_an_anchor_two_rows_share_does_not_decide_survival`, `test_two_result_rows_sharing_one_anchor_decide_nothing_either`, `test_two_rows_sharing_a_first_cell_decide_nothing_either`, `test_a_row_ambiguous_on_both_identities_is_not_identified` and `test_a_key_two_result_rows_share_falls_through_to_the_anchors`. Four mutations, each red on its own case: either side's guard removed, on either identity | 2026-09-21 | **Corrected 2026-09-21 by review round 1, finding 3.** This row claimed the ambiguity guard for the anchor route alone, and the cheap identity needed the same argument: removing one of two rows that share a first cell reported a loss because its twin still stood, which is A3 broken by the identity that runs FIRST rather than by the fallback. Latent when it was measured — 0 of 189 marked rows share a key — and not unreachable: `seal/ledger.md` repeats its section table headers, 123 rows carrying one of two first cells. A row ambiguous on one identity falls through to the other, and one ambiguous on both is not identified at all |
| C5 · A marker a parent deleted relative to the merge base is that parent's decision rather than the merge's, and the check stays silent there | `skills/evidence-check/scripts/correction_check.py#honoured@db6170d0`, `skills/evidence-check/scripts/correction_check.py#marker_counts@8de78ff3` | **Executed** 2026-09-21. `test_a_marker_a_parent_deleted_is_not_a_loss_at_the_merge`, red when `honoured` always answers False. Measured over history: the rule without the base reports 1 merge and 5 markers over 37 reachable merges, and with it reports 0 | 2026-09-21 | This diverges from `spec.md` §Scope 1, which reads *either parent*. M1 is the grounds: the one merge that rule reports is `87eced1`, where `release/v0.9.3` re-anchored a row whose section had moved file and rewrote the cell holding four historical `Re-read` sentences. The merge decided nothing. `overview.md` carries the divergence row |
| C6 · A blob the check could not read is named under `not judged` rather than passing — a ledger over the size cap used to read as a ledger with no rows | `skills/evidence-check/scripts/correction_check.py#ledger_listing@fd067e80`, `skills/evidence-check/scripts/correction_check.py#examine@73c871f1` | **Executed** 2026-09-21. `test_a_ledger_too_big_to_read_is_named_rather_than_passed`, red when the unreadable branch is skipped | 2026-09-21 | Found by the mutation sweep rather than by reading: `read_blobs` had always returned an unreadable blob as absent and said why in its docstring, and nothing downstream acted on the difference. The listing is per rev for exactly this — a path a rev does not carry and a path whose blob did not come back are different facts, and the union collapses them. **Re-read 2026-09-21** after round 1, finding 2 rewrote `examine`'s reporting loop: the unreadable-blob branch is untouched and its case is still red when the branch is skipped. |
| C7 · The merges the check reads are reachable from the merge ref a pull-request job checks out, and gone after the squash | `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_m2_the_branchs_own_merge_is_reachable_where_the_leg_runs@907129a4`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_m2_the_squash_is_what_puts_the_merge_out_of_reach@f08b3bf9` | **Executed** 2026-09-21. Both cases build a release branch, a feature branch that merges it in and resolves by taking a side — #424's incident — and read the check's answer at the merge ref and after the squash | 2026-09-21 | This is M2, and it is what the leg's placement rests on. `spec.md` §*What this repair cannot see* claimed it in prose; these two execute it. A negative answer would have moved the leg rather than dropped it |
| C8 · `CLAUDE.md` and `CONTRIBUTING.md` both say what to do when the shared ledger conflicts, and the two are held against each other | `` `CLAUDE.md#"## Repo rule — a change writes fragments, never the shared file"@1cabcc01` ``, `` `CONTRIBUTING.md#"## House rules"@98b2191f` `` | **Executed** 2026-09-21. `test_a8_both_rule_documents_say_what_to_do_at_the_conflict`, driven red from each side in turn by replacing the instruction in one document only | 2026-09-21 | Those two have already disagreed about this rule once — one forbade editing the shared file at all while the other forbade appending to it, which left a branch that had falsified a row with no reading that permits the only correct act. The needles are the instruction and its argument rather than whole paragraphs, so a rewording that still says it stays green |
| C9 · One marker gone from one row is ONE report, whichever parents carried it, and the closing count says one | `skills/evidence-check/scripts/correction_check.py#examine@73c871f1` | **Executed** 2026-09-21. `test_a_marker_both_parents_carried_is_reported_once`, red when the losses are appended per parent again | 2026-09-21 | Every marker older than the fork is carried by BOTH parents, so the per-parent loop printed one row's one loss twice and ended on `2 correction marker(s)` — a reader sent to open two hunks when there is one. Found by review round 1, finding 2. The parent named is the one that lost the most occurrences, which is the side whose text most needs reading; ties fall to the first parent, the side the person resolving had checked out. A row that carried one marker in two cells and carries it in none is still two entries, which is why the grouping is by `(row key, marker)` rather than a deduplication |
