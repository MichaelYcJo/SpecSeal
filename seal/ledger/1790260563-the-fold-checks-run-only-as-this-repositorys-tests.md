| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `settle` labels an anchored ledger row by its first cell, and what stands before the row's first pipe is dropped when it is container syntax only — any run of `>`, `-`, `*`, `+`, `1.`/`1)` and `<!--`, in any combination and spaced or not — and kept as the label otherwise, so `> - \| claim \|` is named `claim` and `see this row \| claim \|` keeps `see this row` | `skills/settle/scripts/settle.py#first_cell@b785771c`, `skills/settle/scripts/settle.py#CONTAINER_RE@890dc433`, `tests/test_settle_reads_before_it_removes.py#test_a_row_inside_container_syntax_is_named_by_its_claim@351744d3`, `tests/test_settle_reads_before_it_removes.py#test_a_quoted_list_row_is_named_by_its_claim_in_the_report@078abc9f`, `tests/test_settle_reads_before_it_removes.py#test_a_prefix_that_is_not_container_syntax_keeps_todays_label@e4ba9a94` | **Executed** 2026-09-24. At `9ff20d3f` the eight container cases were red, `<!-- > \|` labelled `>`, and `settle --retire` printed the row's label as `> -`; the four keep-today cases were green before and after. Mutated at `2e7ed5c7`: bullets out of the pattern 4 red, ordered markers out 2 red, `>` out 6 red, the old blank-cell arm back 9 red | 2026-09-24 | #530. The comment-opener strip above the split is kept, so `<!-- prose \| claim \|` is still labelled `prose` as before; folding the opener into the pattern alone would have changed that label |
| F1 · `fold-check`'s shape check: a fold marker group holding a work-item id at or above the cutoff heads a statement that opens bold and carries exactly one live `Enforced by:` line whose targets each resolve to a file inside the repository and, with `::name`, to a `def` or `class` found by `ast`, or which says `nothing — <why>` with a reason; stacked markers share one statement, which ends at the next marker, heading or end of file; the cutoff is a parameter, and `0` binds every statement | `skills/settle/scripts/fold_check.py#numbered_statements@35e27650`, `skills/settle/scripts/fold_check.py#statements@8f4adc99`, `skills/settle/scripts/fold_check.py#bound@0bbf13a2`, `skills/settle/scripts/fold_check.py#names_targets@ab1a8520`, `skills/settle/scripts/fold_check.py#target_problem@02164c80`, `skills/settle/scripts/fold_check.py#shape_problems@dce99d1e`, `skills/settle/scripts/fold_check.py#HEADING@5040ce15`, `skills/settle/scripts/fold_check.py#BOLD_OPENING@dc7810e2`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_shape_from_0_binds_a_statement_folded_before_any_cutoff@92f3d8ee` | **Executed** 2026-09-24. Moved from `tests/test_a_folded_statement_names_what_enforces_it.py` as they were (0.14.0's S1 carried their mutation record), and the planted cases now call the shipped functions, unchanged in what they assert. Mutated at `0a739888`, each red in the pins: `bound` inverted 17, `names_targets` always true 4, the live filter off 1 | 2026-09-25 | Replaces 0.14.0's S1, removed there because every unit it anchored left `tests/`. An empty `Enforced by:` value is still read as targets and reported as `an empty target`, as before the move. On this tree `--shape-from 0` reads 136 statements in 14 documents and names 115, each carrying no `Enforced by:` line; 21 are bound at `1790154761` **Re-read 2026-09-25 in round 1's fix pass**: a `::name` target whose file will not parse or decode is no longer a traceback; it stops the run at exit 2 naming the statement, the target and the reason (row G1). The shape rules this row states are unchanged |
| F2 · `fold-check`'s ceiling: a top-level `docs/*.md` not listed stays at or under the ceiling; a listed one must still exceed it, must exist, and must carry exactly its frozen marker count and, at that count, the marker ids whose digest the listing holds | `skills/settle/scripts/fold_check.py#ceiling_problems@aa408cfa`, `skills/settle/scripts/fold_check.py#markers@4238e018`, `skills/settle/scripts/fold_check.py#marker_digest@ba89003d`, `skills/settle/scripts/fold_check.py#documents@e87539f1` | **Executed** 2026-09-24. Moved from `tests/test_a_document_has_room_for_the_next_fold.py` as they were (0.14.0's P1 carried their mutation record); the planted cases call the shipped functions. Mutated at `0a739888`: the ceiling compared with `>=` 2 red. `marker_digest` without its `sorted` stays green — no fixture reorders markers — which the overview records as not done | 2026-09-25 | Replaces 0.14.0's P1. **Re-read 2026-09-25 in phase 3**: the two messages that named the `FROZEN_IDS_DIGEST` constant now name the `Over the ceiling` row and print the digest the file has now; each pinned case asserts the new sentence with the digest in it, and printing the entry's stale digest instead reddens 3 and 1 of them **Re-read 2026-09-25 in round 1's fix pass**: a listed path that exists below the top level is named as not a top-level `docs/*.md` rather than as missing (row G1); a document that is not UTF-8 stops the run at exit 2. The ceiling rules this row states are unchanged |
| F3 · `fold-check` exits 0 with nothing found, 1 with each problem on a line of its own, and 2 with nothing checked for a root that is not a directory, a root with no `docs/` when a check is declared, or a ceiling or cutoff that is not a number in range; it prints how many statements it read and how many the cutoff binds, and says when a check was not declared | `skills/settle/scripts/fold_check.py#main@ae412dc3`, `skills/settle/scripts/fold_check.py#run@a5e4b710`, `skills/settle/scripts/fold_check.py#positive@5c1f3dd3`, `skills/settle/scripts/fold_check.py#not_negative@1b005db9`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_the_command_names_a_bound_statement_with_no_line_and_exits_1@eb8931fb`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_the_command_exits_2_with_nothing_checked_on_an_unusable_root@d18f0ec4`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_command_names_a_document_over_the_ceiling_and_exits_1@fa81bebe`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_command_refuses_a_ceiling_that_is_not_a_positive_integer@c07616fe` | **Executed** 2026-09-24. `bin/fold-check --shape-from 1790154761 --ceiling 1000; echo $?` printed 136 statements, 21 bound, 14 documents, and 0. **Re-read 2026-09-25 in phase 3**: `main` now reads the rows first and a flag overrides its row; the not-declared lines name the row and the file (row F5), and the exit codes are unchanged. Mutated at `0a739888`, each red: exit 0 on problems 3, the binds count inverted 1, the `docs/` refusal off 1, the not-a-directory refusal off 1, a ceiling of 0 accepted 1 | 2026-09-25 | **Re-read 2026-09-25 in round 1's fix pass**: with no `--root` the command resolves the repository through `hooks/optin.py#repo_root`, and exit 2 now also covers a file the run had to read and could not, with the run's lines held so nothing printed reads as a result (row G1). Exits 0 and 1 are unchanged |
| F4 · `fold_check.py` refuses below the interpreter floor before `HERE` is computed or any argument is read, with the block copied from `round_record.py` naming `fold-check` | `skills/settle/scripts/fold_check.py#below_floor@5a9dd75e`, `tests/test_a_script_says_which_interpreter_it_needs.py#test_fold_check_refuses_before_anything_is_read_below_a_raised_floor@66f78e56`, `tests/test_a_script_says_which_interpreter_it_needs.py#test_fold_checks_guard_block_is_the_one_round_record_says_to_copy@45dd630f` | **Executed** 2026-09-24: the sentence changed to `settle:` at `0a739888` reddens both cases | 2026-09-24 | |
| F5 · `fold-check` reads its values from three optional `seal/config.md` rows through `hooks/config.py#config_rows`, under the root `hooks/optin.py#home_at` resolves: `Fold shape from` (a whole number), `Document line ceiling` (a positive whole number) and `Over the ceiling` (`none`, or `;`-separated entries `<path> frozen at <n> markers <12-hex digest> until <home>`); an absent or empty row is not declared, skips its check and says so naming the row and the file, neither declared checks nothing at exit 0, a value that will not parse exits 2 naming the row and the value with nothing checked, and `--shape-from` or `--ceiling` overrides its row for one run | `skills/settle/scripts/fold_check.py#declared@4aac689d`, `skills/settle/scripts/fold_check.py#parse_over@b17ea93a`, `skills/settle/scripts/fold_check.py#row_value@d7071d60`, `skills/settle/scripts/fold_check.py#config_rows@bfc22bab`, `skills/settle/scripts/fold_check.py#seal_home@682452e3`, `skills/settle/scripts/fold_check.py#OVER_ENTRY@7c3527b7`, `skills/settle/scripts/fold_check.py#main@ae412dc3`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_root_that_declares_neither_value_checks_nothing_and_says_so@575d2e39`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_row_with_an_empty_value_is_not_declared@bec9415a`, `tests/test_a_document_has_room_for_the_next_fold.py#test_one_absent_row_skips_its_check_and_says_which@01b3c2ef`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_row_that_will_not_parse_is_named_and_nothing_is_checked@fee7ce86`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_flag_overrides_its_row_for_one_run@f0ce8070` | **Executed** 2026-09-25. `bin/fold-check; echo $?` with no flags on this tree: 136 statements, 21 bound at `1790154761`, 14 documents held to 1000 lines, 0 listed, and 0. In a bare directory with `docs/` and no root it printed the neither-declared line and 0. Mutated at `e79c64fa`, each red: the cutoff parse off 5, the ceiling parse off 1, the entry parse off 1, the flag override off 2, no root resolved 11, the empty-value arm off 1 (red only after `test_a_row_with_an_empty_value_is_not_declared` was added, which the mutation first showed was missing) | 2026-09-25 | The root is `home_at`'s rather than a copy of `broad_gate.py#seal_home`, so a repository that opted out with the scratch marker reads as having no rows. A digit is ASCII `[0-9]`, so a Unicode digit `isdigit` would take is refused **Re-read 2026-09-25 in round 1's fix pass**: `seal_home` is now `located`'s first half, and a repository that opted out is told so rather than that it has no root (row G1); the rows and their parsing are unchanged |
| F6 · this repository declares `Fold shape from` `1790154761`, `Document line ceiling` `1000` and `Over the ceiling` `none`, and no top-level document under `docs/` is over the ceiling; `docs/the-evidence-ledger.md` states the same cutoff, ceiling and empty listing, and the prose pin reads the rows through the command's own reader and fails when either side moves alone | `tests/test_a_document_has_room_for_the_next_fold.py#prose_disagreements@f4a1db8e`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_evidence_ledger_states_the_values_the_config_rows_hold@cd561d1d`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_prose_pin_fails_when_either_side_moves_alone@47a1d760`, `tests/test_a_document_has_room_for_the_next_fold.py#test_every_document_in_docs_is_under_the_ceiling_or_frozen@fcc757d5`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_every_bound_statement_in_docs_has_the_shape@7f6071fc` | **Executed** 2026-09-25 at `e79c64fa`, one real file edited at a time and restored: the config cutoff alone, the config ceiling alone, the config listing a document alone, the prose cutoff alone and the prose ceiling alone each reddened the prose pin | 2026-09-25 | Replaces 0.15.1's S1, removed there because its anchors `OVER_CEILING`, `FROZEN_IDS_DIGEST` and the old prose pin left `tests/`. `docs/review-chain-spec.md`'s split along its own headings by MichaelYcJo/SpecSeal#526, which that row recorded, stands in the evidence-ledger's ceiling statement. The pin case was renamed from `…_these_constants_hold` because no constant is left to hold **Re-read 2026-09-25 in round 1's fix pass**: the real-tree case walks `fold_check.documents` rather than a second listing, and the evidence-ledger's cutoff statement now names it beside `fold_check.py::bound`; the values and the pin are unchanged |
| F7 · the wrap limit over the covered documents skips a line exactly when `fold_check.enforced_lines` names it: a folded statement's `Enforced by:` line of targets, whatever the cutoff and whatever its width; a `nothing — <why>` line, an `Enforced by:` line outside any statement (after a heading, or with its marker in a fence) and the line after a skipped one are still prose | `skills/settle/scripts/fold_check.py#enforced_lines@ac34d277`, `skills/settle/scripts/fold_check.py#names_targets@ab1a8520`, `tests/test_docs_line_wrap.py#prose_lines@321aec00`, `tests/test_docs_line_wrap.py#test_an_enforced_by_line_with_targets_is_skipped_and_the_next_line_is_not@5a93a127`, `tests/test_docs_line_wrap.py#test_an_enforced_by_line_the_reader_does_not_read_is_still_prose@85e2afc6` | **Executed** 2026-09-25. The skip case red at `a34fd12d` against the old `prose_lines` (line 5 yielded as prose), the not-read case green before and after, which is what it pins: the width of the skip. Mutated at `5c6fdb71`, each red: every statement line skipped 2, `nothing` lines skipped too 1, the skip off 1, the skip one line wide 1 | 2026-09-25 | #583. It adds no file to `COVERED`. Measured 2026-09-24 by the framer: 3,232 of the 3,490 `path::test` pairs under `tests/` exceed 88 columns as a single target after the prefix, so wrapping between targets could not have fit one target, and a join would add a continuation grammar to settle §2's *exactly one line* |
| F8 · the documents that describe the fold say the plugin ships `fold-check` and sets no value: settle §2 says it reads the shape and the ceiling and that a repository states the cutoff and the ceiling as `seal/config.md` rows, a row it does not write being a check it does not run, and no longer says the plugin ships no checker; settle's fold-branch section and the release checklist's step 2b run it after the prose is written and before `settle --retire`; the evidence-ledger's ceiling statement says the values are rows the command reads and names the prose pin among what enforces it | `` skills/settle/SKILL.md#"### 2. Write one standing statement per segment"@1185a277 ``, `` skills/settle/SKILL.md#"## What a fold branch owes"@9473f3df ``, `` docs/release-checklist.md#"## 2b. Settle what the release leaves behind — by hand, and not in that commit"@f314f1db ``, `tests/test_a_folded_statement_names_what_enforces_it.py#test_settle_owns_the_shape_rule@b3a92024` | **Executed** 2026-09-25. `test_settle_owns_the_shape_rule` red against the old §2 (the new phrase absent), and red again with the old sentence put back beside the new one; green with the edit. The 53 test modules that read a document this phase edited were run and are green, after two corrections: this case's phrase took the line break the document has, and the records naming units this work item removed were marked `NAME NOT IN TREE` | 2026-09-25 | Nothing pins the checklist's `fold-check` step or the fold-branch paragraph by text; review reads them. The README cheat sheet carries one row per edition, and `tests/test_docs_line_wrap.py` skips table rows |
| G1 · `fold-check` reads the repository the current directory is in when `--root` is absent; a `::name` target whose file will not parse or decode, and a `docs/*.md` that is not UTF-8, stop the run at exit 2 with one line naming the file and the reason (`SyntaxError at line N`, `not UTF-8`) and nothing on stdout, never a traceback and never exit 1; a run with no rows says which of two states it is in — no `seal/` root at either place, or a repository that opted out with `specseal-scratch`; a listed path that exists below the top level is named as not a top-level `docs/*.md`; a missing sibling script is refused naming what it is for | `skills/settle/scripts/fold_check.py#located@2d473952`, `skills/settle/scripts/fold_check.py#optin@2a3829e7`, `skills/settle/scripts/fold_check.py#why@2bfb273a`, `skills/settle/scripts/fold_check.py#read@550c34f7`, `skills/settle/scripts/fold_check.py#load@9b6d4b98`, `skills/settle/scripts/fold_check.py#target_problem@02164c80`, `skills/settle/scripts/fold_check.py#main@ae412dc3`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_command_typed_in_a_subdirectory_reads_the_repository@80dd2f12`, `tests/test_a_document_has_room_for_the_next_fold.py#test_an_opted_out_repository_is_told_it_opted_out@38bd13b7`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_repository_with_no_root_is_told_it_has_none@3fb02c2d`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_listed_document_below_the_top_level_is_named_as_outside_it@58aa70ab`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_a_target_file_that_will_not_parse_exits_2_naming_it@9fd5aa05`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_a_document_that_is_not_utf8_exits_2_naming_it@69942cc4`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_a_script_copied_on_its_own_says_which_sibling_it_misses@ec931689` | **Executed** 2026-09-25. Every new case red at the unfixed code, whose `fold_check.py` is the target SHA `e9dfe623`'s (8 red; the no-root pin green before and after, since it holds the state the split must leave). Mutated at `0bbf219f`, each red: the `repo_root` arm off 1, the opt-out arm off 1, the target's except narrowed to `OSError` 2, the document's except narrowed 2, `main`'s catch off 4, the lines not held 1, `why` without its decode arm 3 and without its syntax arm 1, the exists arm off 1, the statement prefix dropped 2. `bin/fold-check` typed in this repository's `docs/` read 136 statements and exited 0 | 2026-09-25 | Round 1's 🟡 1–4 and ⬜ 7 (MichaelYcJo/SpecSeal#587). The coordinator decided the unparseable target is exit 2 rather than a named problem, so a target file this Python cannot parse stops a run rather than counting as a shape problem |
