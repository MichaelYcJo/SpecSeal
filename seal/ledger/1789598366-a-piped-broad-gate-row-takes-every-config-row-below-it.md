<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Five rows over a branch that teaches one regex markdown's escape and gives
the gate a second sentence for a line it cannot read.

The first row replaces one this branch REMOVED from
`seal/ledger/1789445605-the-broad-gate-row-runs-unchecked-and-is-never-asked-for.md`,
a fragment of this same unreleased cycle. That row claimed a pipe *cannot
reach the row at all … and it takes every row below it*, and cited a test
unit whose NAME stated the same thing. The claim went with the code and the
unit was renamed, so the row was removed where it stood rather than
re-pointed (`CLAUDE.md` §*a change writes fragments, never the shared file*).
-->

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| A cell of `seal/config.md`'s table carries a literal pipe when it is written with markdown's own escape, `\|`, and every row below it still arrives. The reduction is **exactly those two characters**: a value holding Windows path separators reads back unchanged, and a bare pipe is still where a cell ends | `hooks/config.py#CONFIG_ROW@86b247d8`, `hooks/config.py#unescaped@4144edc6`, `hooks/config.py#config_rows@aa73cfd5`, `tests/test_the_mode_question_is_asked_once.py#test_an_escaped_pipe_is_one_row_and_the_rows_below_it_still_arrive@577285c8`, `tests/test_the_mode_question_is_asked_once.py#test_a_windows_path_survives_the_reader_exactly_as_written@5faff715`, `tests/test_the_mode_question_is_asked_once.py#test_a_three_column_row_still_ends_the_table@f456b01a` | **Executed** 2026-09-17. Red first against three separate mutations, each its own case: the value cell reverted to `[^\|]*?` returns **one row of four** and the count is what fails; a general `re.sub(r"\\(.)", r"\1", value)` returns `C:UsersxPythonpython.exe -m pytest` for a row holding `C:\Users\x\Python\python.exe -m pytest`; a greedy last cell reads `\| x \| y \| z \|` as the row `('x', 'y \| z')` and swallows the two-cell rows after it. Restored: 136 passed | 2026-09-17 | The count assertion is the case, not the value. A case reading only the `Broad gate` value passes just as well when every row under it has vanished, and vanishing is the defect. **The defect needs a row that parses ABOVE the piped one** — `config_rows` breaks on an unparseable line only once it has found a row — so *takes every row below it* is true of the second row of a table and not of the first. Measured both ways round |
| The reader and the writer still agree about which line is the `Mode` row, so `seal mode` no longer writes a second one into a file whose `Mode` row sits below a piped `Broad gate` row | `skills/implement/scripts/seal.py#table_span@10e55baa`, `tests/test_the_mode_question_is_asked_once.py#test_seal_mode_writes_one_row_where_the_mode_row_sits_below_a_piped_row@54d6631c` | **Executed** 2026-09-17, and it duplicated before the repair: `write_row` over such a file left it **two `Mode` rows deep**, with `declared_mode` answering `('none', '')` for a file whose own row says `shared` and `table_span` returning `(-1, 5)`. `questions.md` M1 carried this as *read, not executed*; this is the measurement | 2026-09-17 | `table_span`'s own comment says no command brings such a file back into agreement, which is why this is the worst outcome in the work item. The writer inherits the repair through the alias — it reads `CONFIG_ROW` from `hooks/config.py` rather than keeping a copy |
| `broad-gate` tells a `Broad gate` row that is ABSENT from one that is THERE and will not parse: the second quotes the line as the person wrote it, names `\|` as the way to write the pipe, says every row below it is lost too, and no longer says the row is absent | `hooks/config.py#refused_row@a623717f`, `skills/verify/scripts/broad_gate.py#refused_broad_row@d49c0e5a`, `skills/verify/scripts/broad_gate.py#missing_row@70797ed5`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_an_unescaped_pipe_is_named_as_a_line_that_will_not_parse@de5da900`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_refused_row_of_some_other_item_is_not_read_as_this_one@ea0ba366` | **Executed** 2026-09-17 end to end over a fixture repository whose row carries a bare pipe: exit 2, nothing run, the line quoted back. Red with the new branch in `missing_row` made unreachable — the old *has no `Broad gate` row* came back. Red a second way with the first-cell reading dropped, where a refused line naming another item was reported as this row. The absent-row case asserts the new sentence is NOT in its message, so the branch cannot have swallowed the old one | 2026-09-17 | Both halves of `agent-contract` §14 are asserted: the new sentence present and the old one gone. The whole line is quoted rather than its first cell (`questions.md` W1) — a refusal that does not show the line sends the reader back to the file to guess which one it meant |
| `hooks/mode-gate.py` gains no message and no refusal. A config whose `Mode` row is hidden below an unparseable line still reads as undeclared, and the gate still simply asks the mode question again | `hooks/mode-gate.py#undeclared@1a042be4`, `hooks/config.py#refused_row@a623717f`, `tests/test_the_mode_question_is_asked_once.py#test_a_line_that_will_not_parse_is_named_and_the_hook_still_says_nothing@f6352dad` | **Executed** 2026-09-17: `undeclared` over such a config answers the home, never a message, exactly as before the branch | 2026-09-17 | Deliberate, and it is the cost of keeping a `PreToolUse` hook silent: a wrong refusal there stops a session with nobody able to get past it. `refused_row` reports and refuses nothing, so importing it makes no caller able to deny |
| The three documents that promised the old behaviour say how a pipe IS written, and each still says what a BARE one costs: the allowed list a person reads before writing the row, the bootstrap paragraph that derives a candidate off a CI `run:` step, and the front door that tells them to edit the value in place | `templates/config.md#"## Broad gate"@cdd8826d`, `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@14994072`, `skills/config/SKILL.md#"## Procedure"@f143f876`, `tests/test_the_broad_gate_row_is_asked_for_and_runs_as_written.py#test_the_allowed_list_says_how_a_pipe_is_written@dbae8f44`, `tests/test_first_setup_asks_once.py#test_a_candidate_carrying_a_pipe_is_escaped_where_candidates_are_derived@3cc5ec0a`, `tests/test_the_settings_have_a_front_door.py#test_the_skill_says_how_a_value_carrying_a_pipe_is_written@68804915` | **Executed** 2026-09-17, one mutation per document with its own case red: the template's *cannot reach this row at all* put back, the bootstrap's *cannot be written into the row* put back, and the front door's `\|` sentence replaced by a sentence that does not name it. Restored: 81 passed across the three modules | 2026-09-17 | Each case asserts both halves of `agent-contract` §14 — the new sentence present and the old one absent — because a document can gain the correction and keep the old sentence two paragraphs down. The criterion and the two lists stay pointed at from the skill rather than copied into it; what the skill gained is how to write the CELL, which is not one of the three rules and cannot be read off either list |
