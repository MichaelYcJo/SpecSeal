| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Across both sweeps of `tests/test_one_word_one_meaning.py`, **five** members are `.py` and not four. That number is the whole of the closure argument for the seam case's phrase set — `flat` folds the seam for `.py` members only, so every phrase searched in a markdown member takes a code path the fold never runs on | `tests/test_one_word_one_meaning.py#test_folding_the_seam_cannot_hide_an_instance_it_would_have_found@612ae34f` | **Executed** 2026-09-16 against the module's own `SEAL_SWEPT` and `SEGMENT_SWEPT`: `seal_stamp.py`, `round_record.py` and `broad_gate.py` in the first, `session_cost.py` and `tests/test_session_cost.py` in the second | 2026-09-16 | #418 states four, and `spec.md` scope item 2 and `plan.md` §*Technical context* both carry it — one aggregate reaching three documents unopened, which is `agent-contract` §5's own example. The case pins the LIST now, not a number beside it, so the next reader cannot restate it wrongly |
| Anchoring the back of an asking stem is not one change. A bare `\b` at the end stops `persona` and `users` **and also** `questions`, `asked` and `answers`, which the guard is meant to match — so the stems split by whether the plural names the party at the keyboard or a population | `tests/test_chain_hooks_hardening.py#ASKING_BARE@8da3739d`, `tests/test_chain_hooks_hardening.py#test_the_asking_stems_are_anchored_at_both_ends@fa904ec6` | **Executed** 2026-09-16, both mutations: with the back anchor removed the case is red on `persona`; with the inflection group removed it is red on `questions`. The `user` stem stays and is measured — no `in one batch` occurrence stands in any `agents/*.md` today, so no occurrence of the word is within any window | 2026-09-16 | #422's own patch drops the `user` stem to stop `users`, in the same breath as adding the anchor that already stops it. `questions.md` Q1 records the reasoning and the measurement that closed it |
| The guard finds an occurrence by the CLAIM and not by one spelling: emphasis comes out before the search and the preposition and article are read as a set, so `in **one batch**`, `in a single batch` and `as a single batch` all reach the window | `tests/test_chain_hooks_hardening.py#BATCH_PHRASE@13307bae`, `tests/test_chain_hooks_hardening.py#test_the_batch_phrase_is_found_by_its_claim_not_by_one_spelling@fdb07b14` | **Executed** 2026-09-16, each spelling planted beside a person answering in `agents/smith.md` and the module run twice: exit 0 against the finder as of `7d1bf5bf`, exit 1 after. `agents/*.md` untouched stays green, so no definition had to be reworded | 2026-09-16 | **Corrected 2026-09-16 after round 1's 🟡 2.** The build stripped `*` alone, on the grounds that `_` is load-bearing in every identifier these definitions name. Measured: stripping `_` cannot change WHAT IS FOUND — no stem in `ASKING` and no word in `BATCH_PHRASE` carries one — so the whole cost is the printed window, which `skills/code-review/scripts/chain_check.py`'s normaliser of this same name already pays. And `*` alone left out the marker these files use most: backticks outnumber asterisks in four of the five definitions (warden 250/159, smith 202/142), so `` in `one batch` `` reached no window at all. The strip is `[*_`]+` now and the preposition set gained `into`, which round 1 measured missed |
| **Nothing in `agents/*.md` contains a batch phrase**, so the #422 sweep's loop body never executes and every part of the composition inside it is unreachable on anything committed. The guard is therefore held only by a function the sweep CALLS and a case calls too — never by a case that pins the pieces beside it | `tests/test_chain_hooks_hardening.py#batch_instructions@2eba501e`, `tests/test_chain_hooks_hardening.py#test_the_sweep_refuses_a_planted_instruction_in_every_spelling@7dffe175` | **Executed** 2026-09-16 by round 1 and reproduced at the fix pass: zero batch-phrase occurrences in all five definitions, and five mutations of the finder, the emphasis strip, the stem set and the window each left the module at exit 0, 50 passed. After the extraction all five are exit 1, each on `test_the_sweep_refuses_a_planted_instruction_in_every_spelling` | 2026-09-16 | Round 1's 🔴 1, and the fifth instance of this release's class — the third produced by a repair FOR it. `seal/specs/1789455558-…/overview.md` states the rule the build broke one layer down: pin the function the production path calls, never the helper beside it. The two pattern cases are kept, because a piece going red says which piece |
| The two documented prose maxima in `tests/test_docs_line_wrap.py` that had drifted are `agents/smith.md` (148 documented, 109 measured) and `skills/implement/SKILL.md` (99 documented, 90 measured), and the first is restated a second time inside `COVERED`'s own comment | `tests/test_docs_line_wrap.py#COVERED@b9362cc3`, `tests/test_docs_line_wrap.py#"    agents/smith.md                109"@ff61fcc5`, `tests/test_docs_line_wrap.py#"    skills/implement/SKILL.md       90"@ff61fcc5` | **Executed** 2026-09-16 with the module's own `prose_lines` and `display_width`, before and after: all four documented numbers now equal what those helpers report | 2026-09-16 | #422 reports the first. **Round 1's 🟡 4**: this row first carried `#<module>@00000000`, which matches neither `ANCHOR_RE` nor `OLD_COORD_RE`, so the checker counted it as nothing and `#<module>@deadbeef` read `ok` — the one outcome the comment under `ANCHOR_RE` says it will not accept. It is anchored on three real locators now: `COVERED`, whose comment carries the restated 109, and the two docstring lines that state the numbers. Each number is therefore held by the line that states it, and corrupting one takes the row BROKEN rather than leaving it silent. That a re-derived number breaks rather than drifts the row is the intended cost: the row claims WHICH two had drifted and to what, so a third number moving is a claim to re-read. Nothing in the tree reads a docstring, and planting a reader is mechanism a fix pass may not add |
| The two `&` cells of `templates/config.md` state which shell each behaviour belongs to, and the case asserts the ATTRIBUTION rather than the behaviour — the `cmd.exe` half stays unmeasured | `tests/test_the_broad_gate_row_is_asked_for_and_runs_as_written.py#test_both_ampersand_cells_name_both_shells@47e9a54d` | **Executed** 2026-09-16, both of #413's measured swaps separately: `/bin/sh` and `cmd.exe` exchanged in the refused row is exit 1, and in the allowed row is exit 1. 18 passed at exit 0 untouched | 2026-09-16 | Work item `1789445605-…` records the `cmd.exe` claim as unmeasured with the `windows-latest` job named. If it is ever measured false the cells change, and this case is what makes the change visible instead of silent |
