| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · the document that owns the cap states that three and five count ROUNDS, that a run the round cap stopped may still write a fix, and that the test is *who owns the unit now* — with each record's `New units` row named as where the evidence already sits | `docs/review-chain-spec.md#"### The cap bounds rounds, and not the fixes of the round it stopped"@cd25543f`, `tests/test_the_rules_have_one_owner.py#RULES@6be9f0b8` | **Executed** 2026-09-22: rule 13's two assertions seen red one mutation at a time, eight in all — the owner's sentence and each of the seven links — each run exiting 1 and naming the case it was meant to redden, with the module 58 green before the first mutation and 58 green after the last restore | 2026-09-22 | The substitution the measured run made was *when did the defect start*, and the subsection names it as the wrong question rather than leaving the next reader to avoid it. The rule has more link carriers than any other row of that registry, because the sentence it corrects had already been copied into six of them |
| C2 · two bounds exit a run `capped` and the permission to write a fix is the round cap's alone: `chain_check.py`'s reopening walk refuses a SECOND fix-closing record after a floor `no`, so the reopening bound's terminal record commissions nothing | `docs/review-chain-spec.md#"##### The reopening — one, and then the run is capped"@9f09b40e`, `skills/code-review/scripts/chain_check.py#stopping_floor@db4e9292` | **Read** 2026-09-22, and **executed** as an absence: `git diff 6d410023..HEAD -- skills/code-review/scripts/chain_check.py` touches the module docstring and nothing else, so the walk that refuses the second record is byte-identical to the base. `tests/test_the_reopening_is_one.py` is green over the corrected subsection | 2026-09-22 | Writing the permission into the reopening subsection would have named a state its own checker refuses, which is #341 arriving again. The two are named apart instead, and each subsection says which of them it is |
| C3 · a capped record that wrote fixes reads `round-N` and is NOT the last record; the verifying round that read those fixes is, and `round_record.py seal` is what requires it by refusing `Broad gate` on a last record whose cell reads anything but `no fixes to check` | `docs/review-chain-spec.md#"### The cap bounds rounds, and not the fixes of the round it stopped"@cd25543f`, `skills/code-review/scripts/round_record.py#seal@2e29fbe1` | **Read** 2026-09-22 against the shipped pair: `seal/specs/1790039346-settle-reads-a-marker-inside-a-commented-out-draft/rounds/round-6.md` is a capped record with five findings closed on fixes and `Fixes checked by: round-7`, and `round-7.md` reads `no fixes to check` beside `Broad gate: 5e2ede92 against 3cdfd8ad`. That branch shipped green, so the shape the sentence permits is one the checkers already accept | 2026-09-22 | The sentence this corrects said a capped record's cell reads `no fixes to check`, which is true of a record that closed nothing on a fix and false of one that did. Both shapes are in the tree, one work item apart |
| C4 · a finding is filed where somebody will act on it: an open finding takes the first rung that fits, and a new issue is the third rather than the default. The test is read off the reviewer's `## Deferred` table, whose `Who answers it` column already existed — so no parsed field, verdict word or template column was added | `docs/review-chain-spec.md#"### Where a leftover goes — the ladder, and why a new issue is not the default"@0973dd22`, `skills/code-review/scripts/round_record.py#DEFERRED_HEADER@ab8ad6ea`, `tests/test_the_rules_have_one_owner.py#RULES@6be9f0b8` | **Executed** 2026-09-22: rule 14's two assertions seen red one mutation at a time, six in all — the owner's sentence and each of the five links — the module 60 green before the first and 60 green after the last restore. **Read**: `DEFERRED_HEADER` is unchanged from the base by the same diff C2 cites | 2026-09-22 | What the fourth rung costs is written into the rule: a real defect that can name nobody stops being visible in the tracker and lives in the round record and the pull request body. `seal/follow-up.md` sits on the third rung and takes the same test, which is what keeps every sentence elsewhere about that file true |
| C5 · the `from-review` label is documented where the tracker's conventions are owned — what it marks and what it is counted for — so the share behind the ladder is one query rather than a reconstruction | `docs/issues-and-milestones.md#"## A label answers *what it is about*, and survives the move"@a4763174` | **Executed** 2026-09-22: `grep -c "from-review" docs/issues-and-milestones.md` returns 2 where it returned 0 before this branch | 2026-09-22 | 89 issues carried the label and no document in the tree named it. The number the ladder rests on — 43 of them closed, 48%, on 2026-09-22 — is not re-derivable from a label nobody documented, which is why documenting it was in scope for a work item that adds no mechanism. **Corrected 2026-09-22** in round 1's fix pass: this row and the two documents first said 47% closed with 47 open, which is arithmetic the tracker does not hold, and carried no as-of date. Round 1 finding 4 re-ran the query and the fix pass re-ran it a third time; all four carriers now state 89 / 43 closed / 46 open, dated, with this run's own issue excluded **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold's side of this section is `<!-- specs/<id> -->` marker comments and nothing else; no sentence changed, so the claim holds and the hash is both sides' edits together. |
| C6 · after round 1's fix pass no LIVE document tells a reader that an open finding at a capped exit becomes a new issue. The class was enumerated by construction over `docs`, `skills`, `agents`, `templates`, `CLAUDE.md`, `CONTRIBUTING.md` and both READMEs, on fifteen independent spellings of the destination, and the one remaining site is a runtime message left standing on purpose | `docs/review-chain-spec.md#"##### The reopening — one, and then the run is capped"@9f09b40e`, `skills/code-review/scripts/chain_check.py#CAPPED_EXIT@8bd0d100` | **Executed** 2026-09-22: the sweep returns four hits, and all four are outside the class — `chain_check.py`'s `CAPPED_EXIT` (the runtime message, disclosed in `overview.md`) and three carriers of *deferred with a named answerer, or becomes an issue*, which names two homes generically and is the depth exit rather than the capped one. Before the pass the same sweep returned seven, three of which the reviewer had not named | 2026-09-22 | The reviewer enumerated four live sites; the sweep found three more — `skills/code-review/orchestration.md`'s and `skills/implement/SKILL.md`'s copies of the no-mechanism destination, and `agents/warden.md`'s `deferred #N` candidates. That is the third time in one work item that a carrier list was shorter than the class, which is why this row records the sweep rather than the list. Two pins require `deferred #N` to survive — `test_the_reopening_is_one` on the spec's subsection and rule 4's link count over the review skill's two halves — so the phrase is kept in both places as the spelling for the issue home, and the paste-ready fixes were adapted rather than applied verbatim |
