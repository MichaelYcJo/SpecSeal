| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A1 · the person's `automation` answer counts as creation consent only when all four hold: it is in this session's own main transcript (the payload's `transcript_path` when its basename is `<session-id>.jsonl`, otherwise the single `<projects>/*/<session-id>.jsonl`); it is a `user` entry, not marked `isSidechain: true`, whose `tool_result` names an earlier `AskUserQuestion` call by a string `tool_use_id`, read from `toolUseResult`; its single-select question's options have exactly the leading phrases `automation`, `per axis` and `no work item`, and the answer is one of those option labels verbatim with the leading phrase `automation`; and the entry's `cwd` is in the same clone. Every other shape is no consent, and nothing raises: the reader handles the two shapes round 1 found raising, and `consent` answers "" for any exception the read throws | `hooks/worktree_consent.py#automation_answered@d9bf2ddf`, `hooks/worktree_consent.py#transcript_for@ce2dac5b`, `hooks/worktree_consent.py#_routing_preset@85b623f0`, `hooks/worktree_consent.py#leading_phrase@a173a76c`, `tests/test_the_guard_asks_once_per_session.py#test_a_result_not_linked_to_an_ask_is_not_consent@bd487897`, `tests/test_the_guard_asks_once_per_session.py#test_automation_on_another_question_is_not_consent@fd953438`, `tests/test_the_guard_asks_once_per_session.py#test_an_answer_given_in_another_clone_is_not_consent@a81739f1`, `tests/test_the_guard_asks_once_per_session.py#test_an_unreadable_transcript_is_no_consent@16049d13`, `hooks/worktree_consent.py#consent@754e5d51`, `tests/test_the_guard_asks_once_per_session.py#test_a_typed_answer_that_qualifies_the_preset_is_not_consent@ef82fe95`, `tests/test_the_guard_asks_once_per_session.py#test_a_sidechain_entry_is_not_consent@53a6a594`, `tests/test_the_guard_asks_once_per_session.py#test_the_reader_itself_answers_both_shapes_without_raising@7fab5ba6`, `tests/test_the_guard_asks_once_per_session.py#test_a_shape_the_reader_did_not_expect_keeps_the_guards_deny@42a886e8`, `tests/test_the_guard_asks_once_per_session.py#test_consent_fails_closed_on_any_exception@9c0afe67` | **Executed** 2026-09-26. All 17 reader cases seen red against the pre-change module, then eight mutations, one at a time, each turning exactly its case red, the module restored from bytes kept before the first write | 2026-09-26 | Any matching answer counts, not the latest: a later `per axis` does not revoke an earlier `automation`, which is the record's standing too. `multiSelect` must be `False`, not absent. The order of question and result is enforced by the single pass, so it has no mutation of its own. Measured transcript shape: `spec.md` §*Data & interfaces*. Corrected 2026-09-26 after round 1: the row said *answered `automation`* and *nothing raises*, and both were false. A typed `Other` answer such as *automation - but ask me before each worktree* was cut at ` -` and read as the preset, and a list-valued `tool_use_id` or a NUL in `transcript_path` raised, which `hooks/dispatch.py` turned from the guard's deny into no decision. Both are fixed, a sidechain entry is refused as well, and the clause now says what the reader requires. The harness writes an `Other` answer into `answers` verbatim, and a pressed option's answer equals its label: round 1 counted 184 pressed single-select answers matching a label and 16 free-text answers matching none, over 67 local transcripts. The three new cases and the dispatch case were seen red against the unfixed reader, and each repair was then undone alone and turned its own case red. Re-read 2026-09-26 against the fix |
| A2 · the reader's labels are the routing question's: `ROUTING_LABELS` equals the three labels of `skills/implement/orchestration.md`'s Question 1 table, and `PRESET` is the first | `hooks/worktree_consent.py#ROUTING_LABELS@d00258b4`, `skills/implement/orchestration.md#"## Orchestrator: how the work is routed — two questions, one call, one file"@ec41c96f`, `tests/test_the_guard_asks_once_per_session.py#test_the_labels_match_the_routing_question_the_orchestrator_asks@993c2733` | **Executed** 2026-09-26, seen red by changing `per axis` to `per-axis` in the constant | 2026-09-26 | What breaks in six months and turns nothing red is the harness renaming `toolUseResult` or `answers`; this pin catches only the drift the tree can see, the question being relabelled here |
| A3 · row 0 of the Bash creation ladder reads the record or the answer, record first, with the record's effect: `allow` for a command that is worktree creation and nothing else, `silent` for anything more, above every other row including ACTIVE; the `allow` reason names which consent it read. `consented` takes `allow` or `silent` only | `hooks/worktree-guard.py#guard_worktree_creation@7aa6ef85`, `hooks/worktree_consent.py#consent@754e5d51`, `tests/test_the_guard_asks_once_per_session.py#test_the_measured_automation_run_is_not_stopped@20cb14b7`, `tests/test_the_guard_asks_once_per_session.py#test_a_bare_creation_after_the_automation_answer_is_allowed@761adf88`, `tests/test_the_guard_asks_once_per_session.py#test_the_answer_outranks_every_row_below_it@499ffd23`, `tests/test_the_guard_asks_once_per_session.py#test_the_allow_says_which_consent_it_read@47175467` | **Executed** 2026-09-26 through `wg.main()`. Seen red against the phase-1 tree (six of eight cases; the other two pin what must not move and were seen red by mutation), and by reading the record alone, by dropping the path argument, and by giving the answer the record's reason | 2026-09-26 | S1 is the measured run of #604: two `git worktree add` lines and a `git switch -c … # [worktree-ok]` in one call, raw stdout empty. Re-read 2026-09-26 after round 1's fixes: `consent` now catches any exception from the read and answers "", and the function's only other change is the `[worktree-ok]` row's text, which reaches no consent verdict |
| A4 · the switch direction reads neither consent: with the answer present, a switch over an ACTIVE session still denies | `hooks/worktree-guard.py#main@e59a08e1`, `tests/test_the_guard_asks_once_per_session.py#test_the_answer_does_not_reach_the_switch_direction@a742d3fd` | **Executed** 2026-09-26, seen red by making the switch ladder exit on consent | 2026-09-26 | A switch written after a creation in one command is never judged when consent is present; that predates this work and is #620 |
| A5 · the Agent/Task `isolation: "worktree"` path counts no sessions and reaches no choice site: silent under consent, otherwise one `ask` whose reason says the agent runs beside this session and names neither *single-stream* nor calling the Agent again without isolation | `hooks/worktree-guard.py#main@e59a08e1`, `tests/test_worktree_guard.py#test_an_isolated_agent_asks_without_counting_sessions@d32c27a0`, `tests/test_worktree_guard.py#test_the_two_reworded_reasons_are_pinned_in_both_languages@6e5b2ee1`, `tests/test_the_guard_asks_once_per_session.py#test_the_agent_path_is_silent_after_the_automation_answer@0909fb77` | **Executed** 2026-09-26 over four tree states, `sessions_in_tree` replaced by a recorder that must stay empty. Seen red against the phase-2 tree, by calling `sessions_in_tree` again, and by ignoring consent | 2026-09-26 | #8. #237's floor is unmoved: the first isolated agent of a session without consent is still one confirmation. Re-read 2026-09-26 after round 1: the S10 case's docstring changed and its assertions did not |
