<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Six rows. Four are narrow readings a later tidy-up would widen in good faith:
a pair of git decisions that each look redundant beside the other (R1), a
filter that looks like belt-and-braces (R2), an inode fold that looks like
ceremony beside a path comparison (R3), and a helper extracted from four lines
that read fine inline (R4). R5 and R6 are the opposite shape — claims about
what CANNOT be checked, recorded so the next reader meets the rejected
mechanisms instead of writing one.

**None of the four was found by reading.** R1 and R3 came out of the mutation
loop, R2 out of asking what the second round of every run looks like, and R6
out of a review round reading dates. R1 has now been a survivor twice: eleven
mutations before a twelfth case caught `--diff-filter=A`, and twenty cases
green over an index (`found[-1]`) that round 1 mutated in one line. Both
halves of that pair are in one row for that reason. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `added_on_branch` reads the LATEST add, and `--diff-filter=A` is what keeps *added* from collapsing into *touched* — the two together, because each is undefended by the cases the other one needs | `skills/code-review/scripts/chain_check.py#added_on_branch@adf58230`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_added_before_the_base_and_updated_on_the_branch_passes@1662f140`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_deleted_and_re_added_after_the_fix_is_judged_on_the_later_add@4dee6e90` | **Executed** 2026-09-04 after round 1: `found[0]` → `found[-1]` turns the delete-and-re-add case red and nothing else; dropping `--diff-filter=A` turns TWO cases red, the base-moved one and the updated-in-place one. Nineteen mutations across both checkers, one survivor and it is C12 below | 2026-09-04 | **The index was `found[-1]` until round 1's 🟡 8 and nothing held it** — mutating it left all twenty cases green, because `--diff-filter=A` returns one commit for a file that was added and then only modified, which every case then in the file built. The earliest add is the permissive direction: a stub committed on time, deleted, and the real record written after the fixes passes on the strength of a commit that no longer holds any of its content. Note what changed when the index flipped: under `found[-1]` dropping the flag was separable only by a base that MOVES under a long branch, and under `found[0]` it also refuses every correctly updated record, because the newest commit that TOUCHED a record is its verdict update. **A row written for the old index would now be false**, which is why this row names the pair rather than the flag alone |
| R2 · a fix commit that is an ancestor of the record's own `Target SHA` is dropped, and without that the check fails the SECOND round of every run | `skills/code-review/scripts/chain_check.py#written_late@1e82498f`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_fix_this_round_did_not_commission_passes@d61a2df0` | **Executed** 2026-09-04 at `55ae839`: the filter replaced by `commissioned = True` and the cited case exits 1; every other case in the file stays green | 2026-09-04 | It reads as belt-and-braces beside the ancestry test that follows it, and it is the opposite: round N+1 reviews round N's fixes, so its verdict table answers round N's findings and its cells legitimately name commits that already existed. Round N+1's record is committed after those commits BY CONSTRUCTION. Without the filter the refusal is not a rare false positive, it is red on the second record of every well-run chain — including this repository's own |
| R3 · two names are one ledger when they reach one **inode**, so the skipped set folds by `st_dev`/`st_ino` and not by any spelling of the path | `skills/evidence-check/scripts/evidence_check.py#skipped_by_narrowing@e2396342`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_hard_link_to_the_ledger_is_not_reported_as_skipped@b1ef0ac4`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_second_spelling_of_the_same_file_is_not_reported_as_skipped@2ac929ab` | **Executed** 2026-09-04 after round 1: the fold replaced by `normcase(realpath(...))` and two cases exit 1 — the hard link on every platform, the `SEAL/ledger.md` spelling where the filesystem folds case. Measured on the build machine: `normcase('SEAL/ledger.md')` is the identity, `realpath` keeps the case, the filesystem folds it, and a hard link has one inode under two realpaths | 2026-09-04 | **It was `normcase(realpath(...))` until round 1's 🟡 9, and the platform was inside it**: `os.path.normcase` folds case on Windows alone and `realpath` canonicalises case nowhere, so on a case-insensitive filesystem the run read the ledger and then listed it as unread. That is `agent-contract` §13 exactly — a defence resting on the platform where nobody removed the guarantee — and the three obvious cases all pass under either spelling, because `--ledger seal/ledger.md` and the resolver's answer are string-equal. Three spellings break the old rule and one is platform-free: a leading `./`, a case variant, and a hard link. The inode fold answers all three plus symlinks with one rule, and `os.stat` failing falls back to the normalized absolute path — over-reporting, so nothing goes unread |
| R4 · `default_patterns` exists so the skipped set is subtracted from the same list the defaults are read from, not from a second copy of it | `skills/evidence-check/scripts/evidence_check.py#default_patterns@8d3a5c4c` | Read 2026-09-04 at `93c8b89`, with the mutation loop's coverage beside it: no case distinguishes one spelling from two today, because the two would be identical on the day they were written | 2026-09-04 | Inlining it back into `main` is the tidy-up, and the four lines do read fine there. What it costs is invisible until the defaults next change: the copy that decides which ledgers get NAMED as skipped is the quiet one, so a repository that adds a fourth default location would keep checking it and stop reporting it. That is the same failure the whole work item is about — a check reporting clean while something is missing — reintroduced by a refactor that looks like cleanup |
| R6 · a `--reverify` that lands on the same DAY as the row's existing `Checked` date leaves no trace at all, so the date is a necessary and insufficient signal that somebody re-read | `skills/evidence-check/SKILL.md#"## Re-verifying is recomputing the hash"@8a596600`, `` `CLAUDE.md#"## Repo rule — commit early; on a declared branch it costs nothing"@143784bb` `` | **Executed** 2026-09-04: this branch's unscoped `--reverify` re-stamped eight rows in `seal/ledger.md`. Three carried `2026-09-02` and were visibly impossible against content written on the 4th; five already carried `2026-09-04` and were re-stamped without a re-read exactly like the other three, with nothing in the file to tell them apart. Round 1 found the three and could not have found the five | 2026-09-04 | The rule (`CLAUDE.md`) is that the column holds the date somebody read the code, and the failure it cannot catch is a re-stamp on the day of the last read. **This is the work item's own shape one level in** — a check reporting `517 ok` while the trace that would show the gap is missing — so it is recorded rather than closed: the repair is a re-read per row, which is a person's act, and no column can distinguish one that happened from one that did not. What made the six visible at all was the unscoped read this branch adds; a scoped one would have re-stamped nothing and reported clean |
| R5 · *does the record carry what it says it verified* is NOT machine-checkable, and the three checks somebody would write are named with the reason each fails | `` `docs/review-chain-spec.md#"##### What the record carries — a declaration, and why no check reads it"@56a6089a` ``, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_spec_answers_whether_carrying_is_checkable@a0d67224` | Read 2026-09-04 at `0e9aa27`; the case seen red under each of its three needles stashed in turn | 2026-09-04 | Issue #150's own comment asks for this to be *stated before it is assumed*, and a document that simply omits it reads as an oversight rather than as a decision — which is how somebody writes the keyword match over free prose that the arrow's and the comma's limits already decline elsewhere in the same file. The rule that remains is a declaration: a probe row whose subject was a proposed replacement carries the replacement, in a fenced block, and the reader who looks at it is the fix pass the record is the agenda for |
