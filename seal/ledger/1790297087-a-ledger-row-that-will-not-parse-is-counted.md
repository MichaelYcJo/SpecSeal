| Every `# RIDER:` carries a `Verified <date> against <anchor>@<hash>` stamp, and no stamp names a commit | `tests/test_a_rider_reaches_its_file.py#test_every_rider_carries_a_verification_stamp@62106019`, `tests/test_a_rider_reaches_its_file.py#test_no_rider_stamp_names_a_commit@946811d6` | **Executed** 2026-09-25, both cases: 2 passed | 2026-09-25 | Replaces the `seal/ledger.md` row *Every `# RIDER:` carries the date and SHA it was verified at, and the SHA is an ancestor of HEAD*, which phase 1 of this work item removed. Its coordinate quoted a `STAMP = re.compile(…)` line with bare `"` inside, so it never parsed and was never checked (#299); the line it quoted is now `OLD_STAMP`, kept only so a case can name the pre-#239 form, and the claim that a stamp names a commit has been false since #239. `seal/releases/0.9.1.md`'s S1 row states the checker's side of the same rule |
| A `Code grounds` cell holding a coordinate neither `ANCHOR_RE` nor `OLD_COORD_RE` parses, or citing none while the row claims something, is `MALFORMED`: named with its remedy, counted on the per-ledger and `total:` lines at zero as well, and exit 2 with or without `--strict` | `skills/evidence-check/scripts/evidence_check.py#malformed_rows@86494828`, `skills/evidence-check/scripts/evidence_check.py#refused_coordinate@41b030fc`, `tests/test_a_row_points_by_content.py#test_prose_marks_beside_a_good_anchor_are_not_refused@9d7a6932`, `tests/test_a_row_points_by_content.py#test_an_unticked_coordinate_with_one_mark_is_named@9af32b6e`, `skills/evidence-check/scripts/evidence_check.py#grounds_cells@5507b31b`, `skills/evidence-check/scripts/evidence_check.py#exit_code@317c80c8`, `tests/test_a_row_points_by_content.py#test_a_coordinate_that_will_not_parse_is_malformed_under_both_readings@d66d736b`, `tests/test_a_row_points_by_content.py#test_a_claim_whose_grounds_cite_nothing_is_malformed@ebe294a4`, `tests/test_a_row_points_by_content.py#test_malformed_is_on_both_totals_lines_even_at_zero@87b356c5` | **Executed** 2026-09-25: every S1–S10 case red against the phase-1 checker (`e1679906`), where each shape read `0 ok · 0 drifted · 0 broken` at exit 0, and green after; each added unit mutated one at a time with a case going red for every mutation | 2026-09-25 | `questions.md` Q1 default (a); the `exit_code` branch is its own so a later answer moves one branch **Re-read 2026-09-25 by round 1's fix pass (#299):** rule (a) now counts a leftover, in a code span or a bare word alike, only when it holds both marks or a `#` opening a locator, and never in a URL (🟡 1): `#299`, `@cache`, `@Transactional` and an address beside a good anchor are prose, and a bare `src/service.py#Box` or `b.py#g>h` is named. Both halves red before the fix; this tree still reads 0 malformed. The claim holds **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| Only the `Code grounds` cell is read — the column headed so, or the second cell of a row under no header — so the template's notation row, a scope-decisions table, shorthand in a Notes cell, notation in a Clause cell and a row in a fence that closes are not refused; a fence that does not close is read | `skills/evidence-check/scripts/evidence_check.py#grounds_cells@5507b31b`, `tests/test_a_row_points_by_content.py#test_what_is_not_a_claim_is_not_refused@36b09d0c`, `tests/test_a_row_points_by_content.py#test_a_fragment_row_after_a_headed_table_is_still_read@0fbfee30` | **Executed** 2026-09-25: over this repository's ledgers the arm names nothing; over `seal/ledger.md`, `seal/releases/0.4.0.md` and `seal/releases/0.12.0.md` as they stood at `ca2afdb9` it names exactly the five rows `spec.md` lists | 2026-09-25 | A renamed column takes its table out of the arm, which `skills/evidence-check/SKILL.md` §*Known limits* states **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| `--reverify` names each `MALFORMED` row with a `LEFT` line and the remedy, writes nothing to it, and exits 1 | `skills/evidence-check/scripts/evidence_check.py#reverify@cc41254a`, `tests/test_a_row_points_by_content.py#test_reverify_names_a_malformed_row_and_leaves_it@f7a01d9d` | **Executed** 2026-09-25: red against the phase-1 checker, which printed `0 rows re-verified` at exit 0 | 2026-09-25 | **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| A bare `"` inside a quoted locator is named with the escape `\"`, and the escaped coordinate resolves | `skills/evidence-check/scripts/evidence_check.py#malformed_remedy@5414b35d`, `skills/evidence-check/scripts/evidence_check.py#BARE_QUOTE_RE@0a3ecaca`, `tests/test_a_row_points_by_content.py#test_a_quoted_locator_with_no_hash_is_not_told_about_a_bare_quote@60764afa`, `tests/test_a_row_points_by_content.py#test_a_bare_quote_in_a_quoted_locator_is_malformed_and_the_escape_repairs_it@21c56b97` | **Executed** 2026-09-25: red against the phase-1 checker; the remedy's quote branch removed turns it red again | 2026-09-25 | The repair phase 1 applied to four live rows **Re-read 2026-09-25 by round 1's fix pass (#299):** `BARE_QUOTE_RE` no longer matches at the end of the text, so a quoted locator with no hash (`a.py#"line"`, `a.py#f>"g"`) gets the missing-hash remedy rather than the bare-quote one (🟡 2), red before the fix. The claim holds **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| The plugin's copy splits a row with the shared reader's `split_row`, and a copy `evidence-ci` vendors alone uses its own, held in step | `skills/evidence-check/scripts/evidence_check.py#cell_rule@fdce4ca0`, `skills/evidence-check/scripts/evidence_check.py#vendored_split_row@75592b17`, `tests/test_evidence_check.py#test_the_vendored_cell_rule_agrees_with_the_shared_one@9a0d3f3a` | **Executed** 2026-09-25: red before `cell_rule` existed; red with the vendored split's escape lookbehind removed, and with `cell_rule` returning the vendored split | 2026-09-25 | |
| The commit advisor keeps `MALFORMED` rows beside `BROKEN` and `OLD-FORMAT` and prints a block naming each row with the checker's remedy, and its docstring says so | `hooks/evidence-advisor.py#failing_rows@4ddc821f`, `hooks/evidence-advisor.py#main@d43eea30`, `tests/test_dispatch.py#test_a_commit_with_a_malformed_ledger_row_is_told_so@9da2850e`, `tests/test_a_row_points_by_content.py#test_the_advisory_docstring_names_what_it_prints@c911c4c2` | **Executed** 2026-09-25: both cases red first — the commit printed nothing, and the docstring named no `MALFORMED` — and green after; the filter entry, the block and its row lines each removed in turn turn the dispatch case red | 2026-09-25 | The filter `OLD-FORMAT` joined in round 4 (🟡 6), for the same reason **Re-read 2026-09-25 by round 1's fix pass (#299):** the block's header now counts Code grounds texts, which is what the checker counts, says nothing a row citing nothing would make false, and the closing line that repeated each text's own remedy is gone (⬜ 3); S11 and the docstring pin assert all three, red before the fix. The claim holds **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
