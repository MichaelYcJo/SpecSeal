| Every `# RIDER:` carries a `Verified <date> against <anchor>@<hash>` stamp, and no stamp names a commit | `tests/test_a_rider_reaches_its_file.py#test_every_rider_carries_a_verification_stamp@62106019`, `tests/test_a_rider_reaches_its_file.py#test_no_rider_stamp_names_a_commit@946811d6` | **Executed** 2026-09-25, both cases: 2 passed | 2026-09-25 | Replaces the `seal/ledger.md` row *Every `# RIDER:` carries the date and SHA it was verified at, and the SHA is an ancestor of HEAD*, which phase 1 of this work item removed. Its coordinate quoted a `STAMP = re.compile(…)` line with bare `"` inside, so it never parsed and was never checked (#299); the line it quoted is now `OLD_STAMP`, kept only so a case can name the pre-#239 form, and the claim that a stamp names a commit has been false since #239. `seal/releases/0.9.1.md`'s S1 row states the checker's side of the same rule |
| A `Code grounds` cell holding a coordinate neither `ANCHOR_RE` nor `OLD_COORD_RE` parses, or citing none while the row claims something, is `MALFORMED`: named with its remedy, counted on the per-ledger and `total:` lines at zero as well, and exit 2 with or without `--strict` | `skills/evidence-check/scripts/evidence_check.py#malformed_rows@a052f1a0`, `skills/evidence-check/scripts/evidence_check.py#grounds_cells@5507b31b`, `skills/evidence-check/scripts/evidence_check.py#exit_code@317c80c8`, `tests/test_a_row_points_by_content.py#test_a_coordinate_that_will_not_parse_is_malformed_under_both_readings@e6172dfa`, `tests/test_a_row_points_by_content.py#test_a_claim_whose_grounds_cite_nothing_is_malformed@f371941f`, `tests/test_a_row_points_by_content.py#test_malformed_is_on_both_totals_lines_even_at_zero@87b356c5` | **Executed** 2026-09-25: every S1–S10 case red against the phase-1 checker (`e1679906`), where each shape read `0 ok · 0 drifted · 0 broken` at exit 0, and green after; each added unit mutated one at a time with a case going red for every mutation | 2026-09-25 | `questions.md` Q1 default (a); the `exit_code` branch is its own so a later answer moves one branch |
| Only the `Code grounds` cell is read — the column headed so, or the second cell of a row under no header — so the template's notation row, a scope-decisions table, shorthand in a Notes cell, notation in a Clause cell and a row in a fence that closes are not refused; a fence that does not close is read | `skills/evidence-check/scripts/evidence_check.py#grounds_cells@5507b31b`, `tests/test_a_row_points_by_content.py#test_what_is_not_a_claim_is_not_refused@59145301`, `tests/test_a_row_points_by_content.py#test_a_fragment_row_after_a_headed_table_is_still_read@5f8351a3` | **Executed** 2026-09-25: over this repository's ledgers the arm names nothing; over `seal/ledger.md`, `seal/releases/0.4.0.md` and `seal/releases/0.12.0.md` as they stood at `ca2afdb9` it names exactly the five rows `spec.md` lists | 2026-09-25 | A renamed column takes its table out of the arm, which `skills/evidence-check/SKILL.md` §*Known limits* states |
| `--reverify` names each `MALFORMED` row with a `LEFT` line and the remedy, writes nothing to it, and exits 1 | `skills/evidence-check/scripts/evidence_check.py#reverify@cc41254a`, `tests/test_a_row_points_by_content.py#test_reverify_names_a_malformed_row_and_leaves_it@6903b423` | **Executed** 2026-09-25: red against the phase-1 checker, which printed `0 rows re-verified` at exit 0 | 2026-09-25 | |
| A bare `"` inside a quoted locator is named with the escape `\"`, and the escaped coordinate resolves | `skills/evidence-check/scripts/evidence_check.py#malformed_remedy@5414b35d`, `tests/test_a_row_points_by_content.py#test_a_bare_quote_in_a_quoted_locator_is_malformed_and_the_escape_repairs_it@7720d072` | **Executed** 2026-09-25: red against the phase-1 checker; the remedy's quote branch removed turns it red again | 2026-09-25 | The repair phase 1 applied to four live rows |
| The plugin's copy splits a row with the shared reader's `split_row`, and a copy `evidence-ci` vendors alone uses its own, held in step | `skills/evidence-check/scripts/evidence_check.py#cell_rule@fdce4ca0`, `skills/evidence-check/scripts/evidence_check.py#vendored_split_row@75592b17`, `tests/test_evidence_check.py#test_the_vendored_cell_rule_agrees_with_the_shared_one@9a0d3f3a` | **Executed** 2026-09-25: red before `cell_rule` existed; red with the vendored split's escape lookbehind removed, and with `cell_rule` returning the vendored split | 2026-09-25 | |
