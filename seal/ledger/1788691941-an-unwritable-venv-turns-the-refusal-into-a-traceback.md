<!-- specs/1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback -->

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Two rows over a branch of one guard, one sentence and one case. Both earn a
row for the same reason: each is a claim whose repair looks obvious from the
outside, and in both the obvious repair is DELETION. R1 is a `try` around a
one-line write that reads as defensive programming beside a guarded read
thirty lines up; R2 is a second case beside one that already reads the
generator's constants, which reads as a duplicate of it.

Neither row is a rewrite of a row in `seal/ledger.md`. The two this branch
touched there — R2 and R8 of the `1788632199` fragment's section — keep their
claims and gained a caveat each, because what this work found was a
precondition each had left unstated rather than code either of them cites
being removed. The reasoning is in those rows' own Notes, where a reader who
opens the coordinate will be.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run: two anchors
in `seal/ledger.md` drifted for edits this branch never opened
(`templates/config.md` and `round_record.py#swallowed`), and a blanket run
would re-stamp them on behalf of a session that never read them. The unscoped
READ form ran before and after, and its output is in `phases/phase-2.md`. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · the one write this runner makes to the working tree is guarded, so a `.venv` the operator has made read-only ends the run in a sentence rather than in a traceback under a refusal that had already printed. The sentence names the ignore it could not write, the reason, and that the virtualenv stays visible to `git status` until the reader removes it or makes it writable. The refusals above it keep their wording and their exit codes, and the runner does not try to make the write succeed. The class is one member by construction rather than by a list: everything else this module puts in the tree is a builder subprocess's, whose failure is already a return code the caller reads | `.github/scripts/run_tests.py#hide_from_git@08f18fc6`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_unwritable_venv_leaves_the_refusal_a_sentence@6729bec5`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_unwritable_sentence_is_the_same_on_every_platform@e6fc997d` | **Executed** 2026-09-06 in phase 1, both cases seen red first against the unguarded write at `aabb30c`: `PermissionError: [Errno 13] Permission denied: '<tmp>/.venv/.gitignore'` escaping `ensure`'s `finally` after the floor refusal had reached stderr. Green at `8be39a9` with the six `hide_from_git` cases beside them. **Executed** against the real fixture through a probe deleted after it ran — a `.venv` holding an interpreter and a `pytest` script, `pyvenv.cfg` at `version = 3.11.9`, the directory `chmod 555` — two sentences, exit code `2`, no traceback, and `git status --porcelain` reading `?? .venv/`, which is the directory the second sentence promises the reader will see | 2026-09-06 | **The tidy-up is deleting the `try`**, which reads as defensive programming around a one-line write. `venv_version` guards its read thirty lines up, so the unguarded write was the odd one out rather than the house style — and the write sits in a `finally` that runs on the two exits whose entire product is a sentence, which is what turns one `OSError` into a traceback printed underneath a refusal the reader has already been given. **The second is catching the error and saying nothing.** That leaves the directory in `git status` with nobody told why, which is the silent half of the very defect `seal/ledger.md`'s R2 exists against — the guarantee would then fail invisibly instead of loudly. **The third is testing the directory for writability before writing**: a check that can pass and then fail, and one more thing to keep true, where the write already answers the question. **A `chmod 555` fixture is not a guarantee**, so the case built on one is skipped as root and on Windows with the reason written into the `skipif` rather than beside it, and a second case makes the WRITE refuse by monkeypatching `Path.write_text` so the wording is pinned on all three platforms CI runs. Asserting only *no traceback* would have passed silently wherever `chmod` does not stop a write; asserting the sentence converts that into a case that cannot pass on a write that succeeded |
| R2 · the reach vocabulary `docs/review-chain-spec.md` §*The fix surface* names is derived from `call_sites`' own `return` statements — the module-level string constants named in one, and the string literals written into one — so a value the function GAINS has to reach the document before the suite is green again. What the derivation does not reach is recorded in `reach_values`' docstring rather than closed: a value another function hands back, one formatted at run time, and the two the section names as categories rather than as words, which are the reviewed repository's own names and have no finite set to derive | `tests/test_the_fixes_name_their_surface.py#reach_values@874ccde9`, `tests/test_the_fixes_name_their_surface.py#test_the_section_names_every_reach_value_the_generator_fixes@c288651c`, `skills/code-review/scripts/round_record.py#call_sites@2aad80bc` | **Executed** 2026-09-06 in phase 2, seen red first: with `NO_TRACKED_FILE = "not a tracked file"` added to `call_sites`' returnable set, the run reads `1 failed, 1 passed` — the derived case exits 1 naming the value the section does not carry, and the case that reads the three constants by name passes, which is the gap measured rather than argued. Restored from kept bytes and verified byte for byte, both green; the whole module reads `35 passed`, exit `0` | 2026-09-06 | **The tidy-up is deleting one of the two cases as a duplicate of the other**, and neither direction is safe. A constant renamed with its value untouched is still the same word in the document, so the derivation cannot see that edit and only the named form fails on it; a constant ADDED is invisible to the named form and only the derivation fails on it. Their docstrings say which is which where the deletion would be typed. **The second tidy-up is listing the fourth value beside the case** when one is added, which is the defect this row exists against moved one level up: the list would be the thing going stale, and nothing would be reading the generator at all. **The third is the vacuous pass**: a derivation that stops finding anything — `call_sites` renamed, the `return` shapes changed — leaves the assertion loop with nothing to iterate and the case green, so the count is asserted before the loop runs |
