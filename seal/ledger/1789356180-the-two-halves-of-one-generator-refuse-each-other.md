<!-- Seven rows over four phases, cut the way `spec.md` cuts the tickets — by
the column of `rounds/round-N.md` each claim is about. The first three are the
`#` column and the verdict vocabulary that reads it, the next two are the
Grounds cell, and the last is the `Fixes checked by` row. The sixth is a
correction to a standing `# RIDER:` whose stated reason measurement did not
support, recorded as a row because the rider it replaces is gone. -->

#### The two halves of one generator refuse each other

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A `#` cell with no digit is a verdict row that commissions nothing — copied into the record as written, never keyed, never asked for a fix-table row, never counted toward `Pass` — **unless the cell is empty or carries 🔴 or 🟡**, the two severities that mean somebody owes the row an answer, which are refused. A cell carrying digits that is not a bare integer is still refused | `skills/code-review/scripts/round_record.py#finding_number@ca377616`, `skills/code-review/scripts/round_record.py#DIGIT_RE@2dc5ff00`, `skills/code-review/scripts/round_record.py#verdict_rows@6bce3c46`, `tests/test_a_finding_id_is_a_bare_integer.py#test_six_rows_with_no_id_stand_beside_one_finding_and_commission_nothing@a1f0d95e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_every_no_digit_shape_the_corpus_holds_is_admitted@828cca68` | **Executed** 2026-09-14, every case seen red against `e387bff` first — the six confirmation rows produced `the verdict table has a row whose `#` reads '✅'` and exit 2. The discriminator is the corpus's and was re-derived rather than read: of the 253 cells the old rule refuses, **199 carry digits and 51 carry none**, and the two populations do not overlap on anything a reviewer writes. Three mutations, each killed: admitting any malformed cell, admitting none, and dropping a malformed cell silently. **Round 1's 🔴 1 reopened this and the severity arm is its repair**, seen red 2026-09-14 over `🔴 A`, `🟡 A`, a bare `🔴`, `🟡 fix-surface` and an empty cell — before it, `| 🟡 A | … | open |` came through `new` at exit 0 and the record was written with `- [x] Pass` beside an open finding in its own verdict table | 2026-09-14 | **The grounds first written for this row read the corpus backwards, and round 1's 🟡 2 is the correction.** The count 51 is right; the reading of it was not. Re-derived through the module's own `table_body`: **44 of the 51 are a severity marker and a single LETTER** — `🔴 A` through `🟢 O`, a finding id in the wrong alphabet — and only **7** are the shape this rule admits (`carried` twice, `🟢 fix-surface` twice, `🟢 fragment`, `🟢 grep`, `🟢 overview`). Neither `✅` nor a bare em dash — the two exemplars five documents led with — occurs in a committed record at all; the 21 bare em dashes are in reviewers' REPORTS, a different corpus. All 44 predate `round_record.py` by two days, so the shape is not what a reviewer reaches for today either. That is why the severity is read as well: it is 44 rows of a mistake the admission would otherwise make silent, and all 26 no-digit cells carrying 🔴 or 🟡 are genuine findings (11 `fixed`, 4 `answered`, 11 `open`). **What still fails, stated:** a reviewer who writes 🟢, ❓ or ⬜ on a row that IS an open finding is not caught — the check reaches the two markers that owe an answer and not a wrongly chosen one. `spec.md` §6 read this population as 54 and 1,992 verdict rows; executed here it is **51 and 1,989** over the 207 records that parse |
| The refusal for a malformed id names EVERY offending row of the table, and it lands at `new` as well as at `close` | `skills/code-review/scripts/round_record.py#id_refusal@1eb57437`, `skills/code-review/scripts/round_record.py#build@3ff64476`, `tests/test_a_finding_id_is_a_bare_integer.py#test_one_refusal_names_every_malformed_row@cf44943e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_malformed_id_is_refused_at_new_where_the_author_is@f1ca789e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_hand_edited_record_still_meets_the_rule_at_close@ac175599` | **Executed** 2026-09-14. Five rows `R2-1` … `R2-5` produce one refusal quoting all five and the phrase `bare integer` exactly once; #303 measured the old message at two round trips per repair. The `new` half goes through `verdict_rows` on the report's own lines rather than through a new call site, so `test_the_rule_is_one_constant_both_tables_read`'s counts — one `FINDING_ID_RE.match`, three `finding_number(` — are unchanged and were re-run. Three mutations killed: never refusing, not naming the format, quoting only the first row | 2026-09-14 | `new` refusing does not retire the reading at `close`: a record is a file somebody can edit, and the hand-edit case holds that half open. Reading the report's verdict table at `new` also closed a disagreement nobody had listed — `build` derived `Pass` from every copied row, so a confirmation row reading `verified` counted OPEN and `new` would tick no box where `close` ticked one |
| `❓ out of verified scope` closes a finding without commissioning anything, and a severity marker leads a verdict cell without being part of it | `skills/code-review/scripts/chain_check.py#OUT_OF_SCOPE@dd4e3e65`, `skills/code-review/scripts/chain_check.py#MARKER@4be749dc`, `skills/code-review/scripts/chain_check.py#verdict_of@5b8a6124`, `tests/test_the_fixes_close_the_record.py#test_a_scope_marker_keeps_its_own_word@b8cb102d`, `tests/test_the_fixes_close_the_record.py#test_a_fix_row_for_a_scope_marker_is_refused_as_already_closed@d4120899` | **Executed** 2026-09-14, both cases seen red first. The marker strip was measured BEFORE it was taken: applied to all **1,989** committed verdict rows it changes the reading of exactly **one** cell, and that cell is `❓ out of verified scope` in `1789081272-…/rounds/round-1.md` — the row #353 is about. Two mutations killed: removing the word from `CLOSED_WORDS`, and dropping the marker strip | 2026-09-14 | It is in `CLOSED_WORDS` and in neither `FIX_WORDS` nor `HOME_WORDS`, so it closes without commissioning a reader — the three import-time assertions that hold those sets in step are unaffected. The `#` column could not reach this at all: #84's finding 15 carries the id `15`, so the row that #353 is about is keyed and the repair had to be in the Verdict column |
| The fix pass's Verdict cell holds the word alone and everything after it goes in `Commit or grounds`; `deferred <home>` is the one word carrying its own suffix, and a cell that begins with `fixed` or `answered` and carries more is refused naming the row to write | `skills/code-review/scripts/round_record.py#fix_table@21156979`, `` `docs/review-chain-spec.md#"### The last round verifies, and what it verifies is a diff"@f05861ca` ``, `tests/test_the_rules_have_one_owner.py#test_a_correction_row_closes_answered_and_never_fixed@ef136e85`, `tests/test_the_fixes_close_the_record.py#test_the_suffixed_verdict_cell_is_refused_naming_the_two_cell_shape@a4233f32` | **Executed** 2026-09-14: `close` over `| 1 | answered — corrected at 6883bfb | x |` — the exact spelling `docs/review-chain-spec.md` prescribed — exits 2 with `is none of `fixed`, `answered`, `deferred <home>``, naming three words without saying the cell had begun with one of them. The new arm was seen red on that message. One mutation killed: removing the arm. **Round 1's 🟡 3 found the arm advising a row it then refuses** — `head` was cut at the first `SEPARATORS` character found ANYWHERE in the cell, and `SEPARATORS` begins with a space, so `answered, corrected at <sha>` printed `| 1 | answered, | … |`. `head` now comes from the arm's own test. Seen red 2026-09-14 by pasting the printed row back into `close`, over four separators — every one that touches the word was wrong, not the comma alone | 2026-09-14 | Both halves of #341's comment hold, and the four cases beside this one were GREEN before any edit: they pin the spelling `agents/smith.md` already prescribed and `fix_table` has always accepted. The live defect was the message a reader met when they followed the owner instead. The owner's own sentence still NAMES the one-cell spelling, to say what it cost — so the case asserts the prescribing clause is gone rather than the string, which is a distinction a blunter pin erases **Re-read 2026-09-14 in the post-gate fix pass, and the drift is `8d8a753`'s rather than the revert's** — `1ff0a6c` changed only lines under the section on a verdict row that commissions nothing, outside this unit, while `8d8a753` rewrote *until 0.11.4* to *until the release that corrected it* inside it. Both halves of the claim stand: the section still reads *the Verdict cell holds the word alone and the correcting commit goes in `Commit or grounds` beside it*, and `already deferred` is still grounds with the verdict cell reading `deferred <home>`. The prescribing clause the case asserts is gone is still gone; only the release name in the sentence recording what it cost changed. |
| No verdict word discards the Grounds cell it was not asked to change: all three join the reviewer's grounds after `; `, and a `deferred` row keeps the fix pass's prose beside its home | `skills/code-review/scripts/round_record.py#close@f7819302`, `tests/test_the_fixes_close_the_record.py#test_no_verdict_word_discards_a_cell_it_was_not_asked_to_change@f83cc267`, `tests/test_the_fixes_close_the_record.py#test_a_deferred_row_keeps_the_fix_passs_reasoning@124cce46` | **Executed** 2026-09-14: the parametrised case red on exactly two of three words before the fix — `fixed` was already the only one that preserved what stood — which is the enumeration §12 asks for, since #391 names one row and the class is three wide. Two mutations killed: dropping the join, and discarding a deferred row's third cell again. **Round 1's 🟡 4 found the home printed twice** where the third cell BEGINS with it: the guard was exact equality, so `| 1 | deferred #309 | #309 — the parity arm is out of scope |` — #391's own worked example — landed as `#309 — #309 — the parity arm …`. The home now comes off the front of the note; seen red on that exact row | 2026-09-14 | Q6 is answered **no migration arm**, measured rather than argued: 66 committed rows read as their home alone, and only **15** have a committed `round-N-fixes.md` beside them, of which **11** carried prose the record does not hold. For the other 51 the prose is not recoverable from anything in the tree, because a fix table is an input file and is usually not committed — so a migration arm could not run over them even if one were wanted. Rewriting the 11 would assert a state untrue at the record's own `Target SHA`. Nothing machine-reads a verdict row's Grounds cell; `round_record.py` is its only reader |
| The cut that removes a fix commit from the third cell takes the commit's own code span, so no empty code span is left beside it — and `chain.SEPARATORS`, shared with the `deferred` home reader and with `chain_check`'s readers, is not widened | `skills/code-review/scripts/round_record.py#fix_table@21156979`, `tests/test_the_fixes_close_the_record.py#test_a_fix_commit_carries_no_empty_code_span@aaf49808`, `tests/test_the_fixes_close_the_record.py#test_the_empty_span_repair_did_not_widen_the_shared_separators@72f99e94` | **Executed** 2026-09-14. The shape is on **210** committed verdict rows, not the 103 `spec.md` §6 read. Probed after the repair: `| 1 | fixed | `511d6b2` — `token_totals` is summed before the guard |` lands as `fixed at 511d6b2 — `token_totals` is summed before the guard`, with the second code span intact. One mutation killed: narrowing the cut back to the hex | 2026-09-14 | **The standing `# RIDER:` at this coordinate was right about WHERE and wrong about WHY, and the correction is the reason this is a row.** It said widening `chain.SEPARATORS` would strip the backticks off a home deliberately written as a code span. Measured 2026-09-14: `chain.EMPHASIS` is ``[*_`]+`` and `fix_table` applies it to the verdict cell one line before `SEPARATORS` is reached, so `` deferred `seal/follow-up.md` `` already arrives stripped. The conclusion still holds for the constant's other two callers, which nothing here measured, so it is left alone — and the case pins the constant and the cut site rather than the consequence that turned out not to exist |
| `close` writes both of `landing_values`' answers: `no fixes to check` where nothing closed on a fix word, and a reason saying the fixes are written and no round has opened them where one did. It corrects only the landing value, never a cell naming a `round-N` | `skills/code-review/scripts/round_record.py#WRITTEN_CHECKER@5afc4524`, `skills/code-review/scripts/round_record.py#landing_values@db2f01fc`, `tests/test_the_fixes_close_the_record.py#test_the_checker_cell_stops_saying_the_fixes_are_not_yet_written@60a45a0a`, `tests/test_the_fixes_close_the_record.py#test_neither_chain_check_arm_turns_red_on_the_new_reason@2c174bd4` | **Executed** 2026-09-14, seen red twice — against `e387bff` and again against the committed phase-3 tree after the two phases were split back apart. Q5 measured before the wording was written: `chain_check.checked_by` never mentions `NOT_YET` and splits the word from its reason, and `fix_surface`'s pending arm gates on `CHECKER_RE`, which matches `round-N` and neither spelling of `nobody — <why>` — so **no arm turns red**. Two mutations killed: leaving the cell alone, and rewriting it over any standing value | 2026-09-14 | `nobody` is right and only the reason was wrong, which is narrower than #273 part 1 reads: the ordering rule requires a checker to be a LATER round and at that moment none exists. What makes it worth correcting is the capped run's last record, where no next `new` exists and whatever stands there stands permanently. `landing_values`' docstring carried the old behaviour as deliberate — *the cell stays at the landing value for `new` of the next round to set* — which is true of WHO and false of WHAT, and it is rewritten rather than contradicted |
