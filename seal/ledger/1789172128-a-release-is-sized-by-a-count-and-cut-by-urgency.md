| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · what decides a release's size is stated in exactly one document, and what it states is a **criterion** — what has to be in effect before the next work item starts — with three or four named as a ceiling rather than a target | `docs/issues-and-milestones.md#"## A milestone answers *when*, and takes three shapes"@1b69d154`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_rule_states_the_criterion_and_not_the_count@6b1ead4c`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_count_survives_as_a_ceiling_in_the_wording_the_cap_already_uses@06f26b32`, `tests/test_a_release_is_sized_by_a_criterion.py#test_one_document_states_a_releases_size@7bd4eb41`, `tests/test_a_release_is_sized_by_a_criterion.py#hits@d323992a` | **Executed** 2026-09-12. The sweep, `git grep -niE "three or four\|sized in work items\|release is sized"` over `docs`, `skills`, `agents`, `templates`, `tests` and the three root documents, returns lines in this one file both before and after the edit, and `git grep -n "is the size"` over the same set exits **0** after it, with every hit inside `tests/test_a_release_is_sized_by_a_criterion.py` — the module that names the replaced wording in its own docstring and constants, and the one file the sweep excludes — **by basename rather than by path**, so a copy of it under another directory is excluded with it. **This row said exit 1 and round 1 measured 0**; the recorded claim was the intent of the check rather than what the command answered, which is the one thing a ledger row may not be. The four cases were seen **red** before the wording landed — the document reverted to `6de3f54` and the module run, 5 failed 2 passed — and green after, 7 passed. The two that stayed green are the sweep and its own can-fail case, and each was then broken on its own: a second statement of a size added to `docs/release-checklist.md` failed the sweep, and a dead pattern failed the can-fail case. **Round 2 found the sweep's own reach unpinned and it is STILL unpinned**: each of round 1's three widenings reverts with the module green. Two cases were added and each was seen red under exactly its own mutation and was the only failure — the three noun forms against the pattern, and `CLAUDE.md` against `tracked()` — and then both were **reverted**, because `round_record.py close` refused them at depth 2 and the repository owner's answer was to take that gate's exit. The reach ships unpinned and the pinning is **#366**. Round 3 found this cell claiming the opposite of its own `Notes`, which is the third time this row has carried a sentence the tree contradicts. `hits()` is anchored here but **not pinned by a case**: it was created by round 1's fix pass, so a case for it is depth 2 and refused, and reverting its joined-line branch leaves the module green. That gap is a row in `seal/follow-up.md` | 2026-09-12 | **Two coordinates were removed from this row rather than re-pointed**, on 2026-09-12: the two cases pinning the sweep's pattern and its scanned set were reverted when `round_record.py close` refused them at depth 2, and a row whose anchor a change removes is REMOVED at that anchor rather than aimed somewhere else. The claim is unchanged and still rests on the five coordinates above, the sweep among them; what went with those two is the pinning of the sweep's REACH, which is **#366**. **This row replaces S3 of `seal/ledger/1789100139-…md`**, removed there rather than re-pointed, per `CONTRIBUTING.md` §*Changing cited code is the case the rule has to answer*. That claim named the rule's own wording — *sized in work items, three or four* — so the claim went with the sentence even though the heading it was anchored to survived. What carried over is its substance, which is the one-owner half, and that is what the sweep case now pins. The row lived in **another work item's** fragment, and nothing said whether a later work item may edit one; both fragments already sit in `release/v0.11.1`, the fragment rule is about two concurrent branches not sharing a file, and leaving a false row to be folded into `seal/ledger.md` at the release was the alternative |
| R2 · `size: now` is defined where labels are defined, in the prefix form, in two states, and **nothing reads it** | `docs/issues-and-milestones.md#"## A label answers *what it is about*, and survives the move"@7d9851f7`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_label_is_two_states_and_nothing_reads_it@f5c24924` | **Executed** 2026-09-12, the case seen red against the unedited section (in the same reverted-document run as R1) and green after. **Read** 2026-09-12: `gh label list` shows `chain: capped` as the tracker's existing `<subject>: <state>` label, which is the shape the name was taken from, and no label named `size: now` exists — Q3 leaves creating and applying it to the repository owner, after this merges | 2026-09-12 | The prefix is what keeps the label inside this section's own rule rather than beside it. §*A label answers what it is about* gives a label to a concern that outlives a schedule; the **subject** `size:` does, since every release is sized, and only the **value** `now` is spent when the release ships. So the section states no exception and `flow-measurement` stays the one label here that is not a topic at all. This diverges from `spec.md`, which expected a second acknowledged non-topic label with the exception written out. **Nothing pins the label against the tracker**: a label that is never created, or created under a different spelling, leaves the document describing something absent, and no check can see that. Q3 accepted that state |
| R3 · the two releases the rule cites are named in prose because the check refuses both numbers in a loaded file, and one of the two has already shipped | `tests/test_release_hygiene.py#timers_in@a31fd794`, `docs/issues-and-milestones.md#"## A milestone answers *when*, and takes three shapes"@1b69d154`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_two_releases_are_cited_without_a_version_number@b62f22f5` | **Executed** 2026-09-12 in phase 1, by a `test_tmp_*` probe calling `timers_in("docs/issues-and-milestones.md", text, version())` with one token at a time and deleted afterwards. At the running `0.11.0`: `0.11.0`, `v0.11.0`, `0.11.1` and `v0.11.1` are each refused; `0.8.3` and the illustrative `1.2.3` are kept. `CHANGELOG.md` records `0.11.0` as released and `git tag --list v0.11.*` answers `['v0.11.0']`, so one of the two refused numbers names a release this repository has demonstrably shipped | 2026-09-12 | **The refusal outlives its own reason by one release, and that is #363 rather than this work item.** `timers_in`'s docstring argues the ceiling from *below `running` is history and is kept*, and `plugin.json` is bumped at the release-preparation commit, so from that commit until the next bump the running version is the one that already shipped. Q1 chose the prose citation and filed the off-by-one instead, on the ground that widening a checker so a document may say something is the easiest way to green. The paragraph in the document says why the numbers are not written, because once 0.11.2 ships both become history and a later author would otherwise replace the descriptions with numbers and be right to |
