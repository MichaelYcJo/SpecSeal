| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A fold marker is matched whole, so it cannot be wrapped — and the wrap limit skips it rather than the document leaving the covered set | `tests/test_docs_line_wrap.py#prose_lines@76df356d`, `tests/test_docs_line_wrap.py#test_a_fold_marker_is_skipped_and_the_line_beside_it_is_not@bbed20be`, `tests/test_docs_line_wrap.py#test_a_marker_that_is_not_on_a_line_of_its_own_is_still_prose@7144339a` | **Executed**: with the skip arm deleted, the marker case yields `[1, 2]` where it asserts `[2]` and the parametrised case on `docs/the-evidence-ledger.md` fails at 89 columns; both green with it. Measured over this fold's 88 markers: one reaches 89 columns, 87 fit | 2026-09-22 | The length is not the document author's to control — it is `<!-- specs/ -->` plus a work item id, and an id is `<unix-epoch-seconds>-<slug>` with nothing bounding the slug. 87 of 88 fitting is luck, which is what makes this a class rather than one long directory name. **Re-read 2026-09-23 for #497's round 1 ⬜ 7.** The skip now matches the raw line rather than the stripped one, because the reader anchors the marker at column 0; an indented marker is prose again, and the marker-in-a-sentence case gained that arm, seen red with the skip matching the stripped line. The claim holds: a marker on a line of its own is still skipped and the line beside it is not |
| A `routing.md` this pull request RETIRED is not one it declared, and the marker is the only thing that can tell a retirement from a deletion | `skills/code-review/scripts/chain_check.py#main@1fb2f596`, `tests/test_chain_check_at_the_pull_request.py#test_a_declaration_this_branch_retired_is_not_one_it_made@aa5a5b74`, `tests/test_chain_check_at_the_pull_request.py#test_a_deleted_declaration_with_no_marker_is_still_refused@dc635c09` | **Executed**: `chain_check --baseline origin/release/v0.13.1` failed all 88 retired declarations before the arm and prints 88 `retired:` notices after it, leaving one error — this work item's own absent round record. With the arm removed the excusing case fails and the refusing case stays green, so the pair is not checking one thing twice | 2026-09-22 | `unverified_check.folded_items` grew this distinction when `settle` shipped and this reader did not, so two readers disagreed about one tree. `changed_routing` already excludes a rename for the same reason; this is the other way a declaration leaves a diff |
| Five permanent ledger rows anchored into a work item's own `spec.md`, where `spec.md` G3 enumerated one anchored at a `rounds/` record | `seal/ledger.md#"### 1788331011-two-roots-hold-three-lifetimes"@f9548f96`, `seal/ledger.md#"### 1788398967-local-modes-records-never-leave-the-clone"@e6b5771f` | **Executed**: `evidence_check.py --strict .` after the retirement reported `5 broken`, each `file not found` on a `seal/specs/<retired-id>/spec.md#"<heading>"` anchor — work items 1788331011, 1788354065, 1788398967, 1788420760 and 1788789330. After the repair, `1435 ok · 0 drifted · 0 broken`, exit 0 | 2026-09-22 | G3 called line 78 "the one permanent row that reads one", and that row was safe because its directory is kept. It counted rows anchored at a `rounds/` record and missed five anchored at a `spec.md`. The two anchored here keep a live code anchor and lose only the dead one; three had no other anchor and are REMOVED per `CLAUDE.md`. **The multi-anchor case is the owner's** — `seal/ledger.md` §1788354065's S12 row already named it as such, and this branch is its second instance **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The release side added re-read notes to the review-arm row under this heading and moved its hash; no row here gained an anchor into a retired `spec.md`, and `evidence-check` reports 0 broken. |
| A range that retires 88 work items reports 12,100 survivors, and one range row excuses every one of them | `skills/code-review/scripts/survivor_check.py#whole_range@aeae8cd7`, `skills/code-review/scripts/survivor_check.py#report@e959af99` | **Executed**: `survivor-check --range origin/release/v0.13.1...HEAD --exempt <this item>/survivors.md` exits 0 with `every survivor is excused by a row above (12100)` | 2026-09-23 | The range-row mechanism was built for a shape whose largest measured instance was 153. At 12,100 the per-sentence alternative is not an escape anybody takes, which is the argument the row was shipped on. **Re-anchored 2026-09-23 for #497's round 1 🟡 6.** The row used to anchor at this work item's own `survivors.md`, which the next fold retires with the directory, and that is the class phase 11 repaired in five `seal/ledger.md` rows. It now anchors at the checker that lets one range row excuse a range: `whole_range` returns the first declaration that resolves onto the run's range and belongs to a work item the range touches, and read on this date it still does **Widened 2026-09-23 after round 2's second note**: `whole_range` selects the declaration and `report` is where a matched range becomes every candidate's grounds, so the row anchors both and drifts when either changes. |
