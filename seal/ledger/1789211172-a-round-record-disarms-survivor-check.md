| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · a work item's `rounds/` records are outside the **range** as well as outside the corpus, on both sides of the range's path list | `skills/code-review/scripts/survivor_check.py#corrected@2d2ad445`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_round_record_the_range_added_does_not_subtract_the_survivor_it_quotes@03a5e1ab`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_round_record_the_range_edited_does_not_become_a_source@863fc756` | **Executed** 2026-09-12. Both cases seen red against the unedited module first, and red in opposite directions: the added side at exit **0** — the record's quotation subtracted the survivor and the gate reported success having measured nothing — and the removed side at exit **1**, naming a coordinate inside the constructed round record as the place a claim was corrected. Green after. Re-measured on the real range `7e17f5e..941dab5`: 894 files and 16 removed sentences before and after, exit 0 → 1, and the survivor in `seal/specs/1789100139-…/changelog.md` back at **2.00** | 2026-09-12 | **This is `seal/ledger.md` S6's claim, widened by the work rather than in place.** S6 says the records are outside the corpus and that row stays exactly true; what it became is an understatement, because the exclusion had held on one of the two functions since the module shipped. The two counts are the load-bearing half of the real-range run: `corrected` returns the removed sentences from the same list the filter narrows, so a filter reaching too far would show as a smaller number, and a record the range only ADDS removes nothing — 16 is what proves the filter took nothing it was not meant to |
| S2 · every function in the module that derives a path list from git is filtered by the predicate, filtered by its only caller, or named with grounds — and the three buckets are checked against the source, not declared | `tests/test_a_corrected_sentence_survives_elsewhere.py#test_every_path_list_this_module_derives_from_git_is_filtered_or_named@030cc07e` | **Executed** 2026-09-12, seen red two ways with the module restored byte-identical after each: the filter removed from `corrected` (``corrected derives a path list and no longer applies `records_a_past_round` ``), and a fourth unfiltered path-list call added (``the module derives a path list from git in ['corrected', 'since', 'tracked', 'whole_range'] and this case accounts for ['corrected', 'tracked', 'whole_range']``). The second message is the proof the walk measures rather than restates: it names the three functions found in the source | 2026-09-12 | **Why three buckets and not one.** `tracked`'s bucket also asserts `corpus` is its ONLY caller — a second, unfiltered caller would be a fourth path list reached through a third, which set equality alone cannot see because `tracked` stays accounted for. `whole_range`'s bucket asserts the grounds and the function both still rest on `foreign`, a declaration refused and PRINTED rather than silently dropped; if that mechanism leaves the function the grounds are an argument about code that is gone. **The walk keys on the path-listing subcommand words, not on the `git` helper's name**, which removes `plan.md`'s stated failure scenario for this case — a rename turning it red for no defect — and finds a fourth site written as a direct `subprocess.run(["git", …])` |
| S3 · the module docstring names which side each exclusion holds on, and the sentence is pinned by what it must SAY rather than by its text | `skills/code-review/scripts/survivor_check.py#"## What is excluded, by construction rather than by list"@57f0406a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_docstring_names_both_sides_of_the_round_record_exclusion@0bfea2b6` | **Executed** 2026-09-12, seen red two ways with the module restored byte-identical: the paragraph deleted (``**A record of a past round.** is no longer the first exclusion stated``), and the pre-#365 one-sided sentence put back (``the paragraph does not say the exclusion applies to the pool``) | 2026-09-12 | `agent-contract` §14. What this replaces read *Everything under a work item's `rounds/` is out.* — it stated the intent and named neither function it had to be true of, which is how the asymmetry survived five releases without the module's own account of itself being wrong. Three of the four assertions are positive and about the claim (`pool`, `range`, `both sides`), so a rewrite that keeps the claim passes; the one literal assertion is negative, refusing the old sentence's return |
