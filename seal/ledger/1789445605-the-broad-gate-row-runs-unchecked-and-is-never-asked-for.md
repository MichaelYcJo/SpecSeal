<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Eight rows over a branch that adds one refusal to `broad-gate`, gives the
`Broad gate` row the criterion it never had, and moves the question that
fills it in from the gate's refusal to the bootstrap.

The last row replaces one this branch REMOVED from `seal/ledger.md`: S14 of
work item 1788354065 said the bootstrap asks the mode question and then the
parity question **and nothing else**, and it now asks a third. The claim went
with the code, so the row was removed there rather than re-pointed, and the
new claim is here (`CONTRIBUTING.md` §*House rules*). **S14 bound four claims
and round 1's replacement covered one**, which round 1's own review located and
round 2 confirmed still standing; the last row below is the second of the four,
written now rather than left as a claim nobody records having read. Sixteen other anchor
citations drifted and every one of them was re-read and `--reverify`'d: the
claims held, only the hashes moved. -->

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| The gate looks at the `Broad gate` value before a shell gets it, and refuses three forms — the whole value wrapped in backticks, the whole value wrapped in `$(…)`, and a trailing `&` that is not part of `&&` — with exit 2 and nothing run | `skills/verify/scripts/broad_gate.py#not_as_written@559eb969`, `skills/verify/scripts/broad_gate.py#wholly_substituted@1a067633`, `skills/verify/scripts/broad_gate.py#gate@67fcdd99`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_row_wrapped_in_backticks_is_refused_and_shown_rewritten@2dd077ab`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_dollar_spelling_of_the_same_substitution_is_refused@a2994c81`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_row_ending_in_a_single_ampersand_is_refused@46ee147f` | **Executed** 2026-09-15. Red first with the call in `gate()` reverted: A1, A2, A3, A4 and A6 failed, and the wrapped row of A2 came back **exit 0 with `SEALED` drawn and `row exit 0` in the panel** over a check that had failed — #402's measured shape, reproduced. Restored: 88 passed | 2026-09-15 | The refusal is raised between the row being read and the first `run`, which is what closes the second surface (`compare_at_base` → `first_command`) by reachability rather than by a second guard |
| A matched pair that closes early wraps a part and not the whole, so `$(a) && $(b)`, `pytest -n $(nproc)` and a trailing `&&` are not refused | `skills/verify/scripts/broad_gate.py#wholly_substituted@1a067633`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_pair_that_closes_early_wraps_a_part_and_not_the_whole@7548680a`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_forms_that_stay_allowed_are_sealed_exactly_as_today@93fa055f` | **Executed** 2026-09-15: eight values read off the unit, and three allowed forms sealed end to end over a fixture repository — a `;`, a `$(…)` inside a longer line, and a `&&` chain. **Narrowed 2026-09-16** after the `windows-latest` job: the first two are POSIX syntax that `cmd.exe` does not have, so they run where `subprocess(shell=True)` is a POSIX shell and skip where it is not — asked by attempting, never from `os.name`. The `&&` chain runs everywhere, and all three now assert the panel's `suite 1 passed` rather than the verdict alone | 2026-09-15 | The row stays an arbitrary shell command line by design (#402 §*Not this*). Telling a status-discarding `;` from a quoted one needs a shell parser, which is the over-reach the ticket steers away from |
| A pipe is allowed by the criterion and cannot reach the row at all: `hooks/config.py#CONFIG_ROW` matches a cell as `[^\|]*?`, so a value containing `\|` — escaped or not — stops the line being a row of that table and `broad-gate` refuses it as ABSENT. **And it takes every row below it**: `config_rows` stops reading at the first line that does not parse, so a `Record language` written under a piped `Broad gate` row is invisible and falls back to its default with no message anywhere | `hooks/config.py#config_rows@2e0e32f1`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_piped_row_is_refused_by_the_table_and_not_by_this_refusal@daefe453`, `templates/config.md#"## Broad gate"@8b0444c6` | **Executed** 2026-09-15, three values through `config_rows`: `bin/test -q \| tee out.txt` → `[]`, the backslash-escaped spelling → `[]`, `bin/test -q && ruff check .` → the row. Then end to end: the gate refuses a piped row with *has no `Broad gate` row*. **Re-measured by round 1** on a four-row table — a piped `Broad gate` row above `Record language` returns `[('Mode', 'shared')]` alone | 2026-09-15 | Found by building phase 1 — `spec.md`'s allowed list said a pipe stays legal and stopped there, which is true about the refusal and false about the row. `config_rows` is NOT repaired here: it is the shared reader for three callers, one of them a `PreToolUse` hook, and `spec.md` §*Data & interfaces* lists it Unchanged. The template now carries the measurement beside the promise |
| `templates/config.md` §*Broad gate* is the one owner of the criterion for choosing a value — three rules, each with its reason — and of both lists; `skills/config/SKILL.md` and `skills/implement/orchestration.md` name that section and restate nothing | `templates/config.md#"## Broad gate"@8b0444c6`, `tests/test_the_rules_have_one_owner.py#RULES@8980d6be`, `tests/test_the_broad_gate_row_is_asked_for_and_runs_as_written.py#test_rule_three_was_folded_rather_than_copied_into_a_third_place@597626b2` | **Executed** 2026-09-15: each rule deleted in turn and each pinned case went red, including rule 3's second copy put back in `skills/config/SKILL.md`, which fails the fold case | 2026-09-15 | Rules 1 and 2 existed in no document at all — the session that met the refusal derived them under pressure (#401). Rule 3 stood in two places and is FOLDED into the owner rather than copied into a third |
| The absent-row refusal says the row is a person's and names where they answer it, instead of telling the reader to write one | `skills/verify/scripts/broad_gate.py#missing_row@63a0c556`, `agents/sealer.md#"## The command"@d9c5fd31`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_absent_row_refusal_sends_the_question_to_a_person@36e70f8c` | **Executed** 2026-09-15 against a fixture repository with no row: exit 2, nothing run, and the message carries *not this session's to do*, *a row is a thing a person wrote* and `/specseal:config`. Red with the old sentence put back | 2026-09-15 | #401 is a session that met the old message, ran four candidate commands, chose one, wrote the row, and told the owner afterwards. The only reader standing in front of that message is a session, which is the one party that may not write the row |
| The bootstrap asks for the `Broad gate` row in the same `AskUserQuestion` as the mode — candidates read off the repository and offered with the file each came from, the criterion named by pointer, and a decline that says what it costs | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@35f719f7`, `tests/test_first_setup_asks_once.py#test_the_broad_command_rides_the_question_that_already_stops_the_session@86d58f78`, `tests/test_first_setup_asks_once.py#test_the_question_proposes_candidates_read_off_the_repository@0072b68b`, `tests/test_first_setup_asks_once.py#test_the_decline_is_offered_and_says_what_it_costs@89aa7599` | **Executed** 2026-09-15: ten mutations of the Bootstrap section, each pinned case red, including a second `AskUserQuestion` opened beside the first. The prompt budget is unchanged — the call already stopped the session, once per repository | 2026-09-15 | This replaces the row removed from `seal/ledger.md`, whose claim ended *and nothing else*. A decline writes nothing at all: unlike the mode row, whose absence #151 measured, a declined row and a never-asked row meet the same refusal and are told the same correct thing |
| A backgrounding `&` is refused only where it is LAST, and the mid-line form stays legal with its cost named rather than being refused by a heuristic | `skills/verify/scripts/broad_gate.py#not_as_written@559eb969`, `templates/config.md#"## Broad gate"@8b0444c6`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_an_ampersand_that_is_not_last_stays_allowed@81af4642` | **Executed** by round 1 and again here: `(a failing check) & echo second` exits 0 under `/bin/sh` and `not_as_written` returns None, while the trailing form is refused. Four boundary values pinned, including `2>&1` and a quoted `&` | 2026-09-15 | Round 1's 🟡 1. The form breaks the criterion's second half wherever it stands, so it had to be in ONE of the two lists; it is in the allowed one because telling an operator `&` from a `2>&1` or a quoted one needs the shell parser `spec.md` §Scope refuses, and a false deny makes a legitimate row unwritable. A fix pass may not add mechanism (`skills/code-review/orchestration.md`), so the code route is `questions.md` Q6 rather than this branch's |
| The once-per-repo bootstrap says in three lines what it created, that the root's presence at that place is the opt-in, and what each part of the root is for | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@35f719f7` | **Read** 2026-09-15: step 2 of the Bootstrap carries all three, unchanged by this branch — what this branch changed is step 1, which now asks two questions in one `AskUserQuestion`. **No case pins this claim**, and this row says so rather than citing one that pins a neighbouring paragraph: round 3 grepped `tests/` for *three lines* and for *what each part* and found neither, and the case this row used to cite asserts three strings in the *Ask only here* paragraph instead | 2026-09-15 | Replaces the second of the four claims S14 bound, which left `seal/ledger.md` with that row. Round 1 removed the row because its *and nothing else* went with the code and wrote one replacement; round 1's own review found the other three unreplaced, and round 2 confirmed it. The remaining two were correct to leave — they are about the mode question and the parity question, which rows 1885 and the parity rows already carry |
