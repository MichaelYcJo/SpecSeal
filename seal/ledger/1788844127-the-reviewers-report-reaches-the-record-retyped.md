| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · `round_record.py new` reads the reviewer's report from `<item>/rounds/round-N-report.md` when `--report` is absent, derived from `--item` and `--round` — the same pair `round-N.md` itself is derived from, one function away, so the reviewer and the generator cannot spell the path differently from each other. The flag stays and it wins | `skills/code-review/scripts/round_record.py#report_path@5d2fdf66`, `skills/code-review/scripts/round_record.py#REPORT_NAME@14962cbc`, `tests/test_the_reviewers_report_reaches_the_record.py#test_the_default_reads_the_report_the_reviewer_left@2d61c9bf`, `tests/test_the_reviewers_report_reaches_the_record.py#test_the_flag_still_wins_over_the_conventional_path@bc11d937` | **Executed** 2026-09-08. Both cases red against `--report required=True` first, where the run exits 2 on argparse before it reaches anything they assert. Four mutations of the two added units, all killed — including `report_path` ignoring `given`, which is the one mutation the flag case exists for and the one thing no run against the old code could have shown, because the flag's behaviour is unchanged | 2026-09-08 | **The flag has two live callers and both are real**, which is why removing it was never on the table: a round whose report predates the convention, and a round that ran more than one reviewer, where the orchestrator hands each its own path rather than letting the second overwrite the first. That second case is also what keeps `skills/code-review/SKILL.md`'s *parallel writers overwrite each other* true of the report as well as of the record |
| S2 · the absence of both the flag and the file refuses, and the message names the path it looked at AND the convention that fills it | `skills/code-review/scripts/round_record.py#report_path@5d2fdf66`, `tests/test_the_reviewers_report_reaches_the_record.py#test_the_absence_names_the_path_and_the_convention@916ad62c` | **Executed** 2026-09-08, red against the old code first (argparse exits 2 with a different message). The mutation dropping the `agents/warden.md` clause from the sentence is killed | 2026-09-08 | **`read_text`'s own refusal names the path and would have been enough for a typo.** It is not enough here, because nobody typed this path — it was derived — so a bare path reads as a mistyped argument, which is the one thing it cannot be. The sentence has to say where the reviewer was told to leave the report, or the reader's next act is to check an argument that does not exist |
| S3 · the reviewer writes the report and the orchestrator writes the record, and the documents that carried the prohibition now carry the distinction. `agents/warden.md` names the path, says it is returned, and names the write as the second of §6's two exceptions — beside the clone rule it excepts, not in the contract | `agents/warden.md#"## Where you work"@f6393b78`, `agents/warden.md#"## Report"@2b8e66f7`, `skills/code-review/SKILL.md#"**The reviewer does write one file, and it is not a record.**"@4ac8efe4`, `tests/test_the_reviewers_report_reaches_the_record.py#test_the_warden_is_told_where_to_leave_the_report@3bf56450`, `tests/test_the_reviewers_report_reaches_the_record.py#test_the_record_and_the_report_are_told_apart@8b5fdb35` | **Read**, and the two cases **executed** 2026-09-08, both red against the documents as they stood at `760c293`. `skills/agent-contract/SKILL.md` is unchanged and was verified unchanged: `git diff --stat` names it nowhere on this branch | 2026-09-08 | **The constraint that forbids this is one section above the one the handoff named.** §6's instance is about the record, and the record/report distinction answers it. `agents/warden.md` §*Where you work* — *"a `git clone --no-local` … and only there … you never write in it"* — is about the tree, and no distinction gets past it. §6 itself prescribes the way out: *"An exception is one agent's, and it is named in that agent's definition — never here."* **Which checkout, and why not the clone.** Writing in the clone leaves the rule untouched and costs the default firing at all, plus a returned path into a directory whose lifetime nothing in this repository states — the ticket's own failure shape one step along. `plan.md` §*Alternatives considered* carries both |
| S4 · a report beside a record is not a record: every reader of `rounds/` selects by name through `routing.round_number`, whose `ROUND_RE` is `round-(\d+)\.md` under `fullmatch`, and `docs/review-handoff-protocol.md` §*Layout* now says so at protocol level rather than leaving its diagram to read as an inventory | `hooks/routing.py#round_number@4fd2d884`, `hooks/routing.py#ROUND_RE@db69235a`, `docs/review-handoff-protocol.md#"## Layout"@2c2f524e`, `tests/test_the_reviewers_report_reaches_the_record.py#test_a_report_beside_a_record_is_not_a_record@ff491bd7` | **Executed** 2026-09-08: `round_number("round-1.md")` is `1` and `round_number("round-1-report.md")` and `round_number("round-12-fixes.md")` are both `None`; and a round-2 run whose `rounds/` holds round 1's record and round 1's report reaches round 1's record for the reach-back and inherits its `Location` cell. `tests/test_the_record_is_generated.py`, `test_the_reopening_is_one.py`, `test_chain_check_at_the_pull_request.py`, `test_the_fixes_close_the_record.py`, `test_a_finding_id_is_a_bare_integer.py`, `test_a_new_returnable_value_is_a_contract_change.py` and `test_a_runner_reached_unit_reads_pytest_only.py` together: 280 passed, 1 skipped, exit 0 | 2026-09-08 | **The filter already existed and this change did not add it**, which is the useful half: `round-N-report.md` is named in a comment in `tests/test_the_reopening_is_one.py` and in the same comment copied into `tests/test_chain_check_at_the_pull_request.py`, both written because a reader took directory membership for record-ness and raised `TypeError` on sorting two `None`s rather than failing an assertion. So the layout anticipated this file and nothing outside two test comments said so. **`round-N-asked.md` and `round-N-fixes.md` are still in that state** — executed: `grep -rn 'asked\.md\|fixes\.md' agents/ skills/ docs/ templates/ CONTRIBUTING.md` returns nothing. `questions.md` Q3 puts naming them to the orchestrator |
