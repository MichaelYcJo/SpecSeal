<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Four rows over a branch that is one new function and its cases. What earns a
row is a judgment whose repair looks obvious from the outside, and this
change has four because the guard is built almost entirely out of refusals
to do the direct thing: it does not list the sections it protects (F1), it
does not live in the function that used to hold half of it (F2), it does not
refuse a fence for carrying a heading (F3), and it does not read the `#`
character at all (F4).

Three of the four are the same failure seen from three sides — a report
losing a whole section with the generator exiting 0 — which is the bug being
fixed, and each tidy-up below puts one of those sides back.

The units and cases were opened at `5721a31` in phase 2, before each row was
written; the module was re-run at the same commit. What phase 1 executed is
labelled as phase 1's, and the mutations named in the Notes are its.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, never by an unscoped run; the unscoped
READ form ran afterwards, its output in `phases/phase-2.md`. The scoped form
cannot narrow below a file, so a blanket run would re-stamp `seal/ledger.md`
on behalf of a session that never read it. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| F1 · what a fence may not cross is **derived** from `REPORT_TABLES`' headings and `TERMINAL_LINES` and never restated. Those two constants are the generator's own statement of what it looks up in the report, and nothing in the module read either one before this change — both were defined and referenced only from the tests. So a section added to `REPORT_TABLES` is guarded by that addition alone, and the parametrized case reads its own parametrization back out of the constant, so the case list cannot fall behind it | `skills/code-review/scripts/round_record.py#swallowed@db03c7a8`, `skills/code-review/scripts/round_record.py#REPORT_TABLES@d761cf9b`, `skills/code-review/scripts/round_record.py#TERMINAL_LINES@bfc50233`, `tests/test_the_record_is_generated.py#SECTION_TEXT@a6f8fb79`, `tests/test_the_record_is_generated.py#test_a_fence_that_takes_a_section_the_generator_reads_is_refused@d2e6cc20` | Read 2026-09-06 in phase 2 at `5721a31`: the guard's two loops opened against both constants, and the base commit `774e76b` grepped for either name to confirm the definitions were the only occurrences in the module. **Executed** 2026-09-06 in phase 1, all three parameters seen red at `c4d7077` — `## Deferred` and `## Executed probes` exiting 0, `## Verdicts` exiting 2 on the wrong message — and re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-06 | **The tidy-up is typing the three section names into `swallowed`**, which is #169's own proposal and reads simpler than two loops over constants. It goes stale the day a section is added, and nothing goes red when it does: the new section is unguarded and has no case, so the silence looks exactly like correctness. **The second tidy-up is deleting the parametrization assert as a tautology** — it compares a list in the test file with the constant it was copied from, which reads as testing the copy rather than the code. It is the only thing tying the case list to the constant, and without it `REPORT_TABLES` can grow while the parametrization does not, which is the first tidy-up arriving through the test file instead |
| F2 · the rule is **one sentence read over the whole report** — a fence must close, and its span must not cross a line the generator reads the report by — so both halves live in `swallowed`, called from `build` before anything is looked up, and `fenced_after`'s `never closed` raise is gone rather than kept as a second line of defence. A fence can open in one section and destroy another, and the section it destroys is not the one it opened in | `skills/code-review/scripts/round_record.py#swallowed@db03c7a8`, `skills/code-review/scripts/round_record.py#build@c4ef9d43`, `skills/code-review/scripts/round_record.py#fenced_after@db8108a7`, `skills/code-review/scripts/round_record.py#NEVER_CLOSED@963e4998`, `skills/code-review/scripts/round_record.py#SENTINEL@30b6ad77`, `tests/test_the_record_is_generated.py#test_an_unclosed_fence_above_the_probes_table_names_the_fence@80ab7d3f`, `tests/test_the_record_is_generated.py#test_a_fence_that_swallows_a_terminal_line_names_the_fence@29fb0368` | Read 2026-09-06 in phase 2 at `5721a31`: `build`'s call site, the comment left where the old raise stood, and both cases opened together. **Executed** 2026-09-06 in phase 1, both cases seen red at `c4d7077` — the unclosed fence exiting 2 on `the report has 0 Needs a fix: lines`, which sends the writer to look for a line they wrote — and re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-06 | **The tidy-up is putting the never-closed refusal back in `fenced_after`**, where it reads as belonging: that function is the one walking fences. `build` calls it for the probes section alone, so a fence opened under the verdict table takes `## Executed probes` with it, the call never happens for a section the report no longer has, and the record gets the empty template. That is the shape the raise was supposed to catch. **The second tidy-up is dropping `SENTINEL` and asking whether the report's last line survived `blank_fences`** — a report's last line is very often blank for reasons that have nothing to do with a fence, so the question has to be asked of a line that cannot be blank for any other reason. **The third is dropping `strict=True` from the `zip`**: the two reader passes are asserted to be the same file line for line, and a length that differed would truncate the hidden set silently, which is this guard reporting clean because it read less |
| F3 · what is refused is a report **losing** a section, never a fence **mentioning** one. Both halves of the condition are load-bearing: the heading is among the lines a fence hid, **and** no heading of that name still stands outside a fence | `skills/code-review/scripts/round_record.py#swallowed@db03c7a8`, `tests/test_the_record_is_generated.py#test_a_fence_quoting_a_heading_is_kept_while_the_real_one_stands@37e0181f` | Read 2026-09-06 in phase 2 at `5721a31`: the `and not reader.sections(lines, heading)` clause opened against the case, which asserts exit 0, the quoted block copied into the record, and the real Deferred table read as written. **Executed** 2026-09-06 in phase 1 and re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-06 | **The tidy-up is dropping the second half of the condition as redundant** — a heading among the hidden lines reads as sufficient on its own, and every refusal case still passes without it. It is what would stop this tool during its own review rounds: a reviewer of the record generator pastes record-shaped blocks, headings and all, so the branch that added the guard would be the first thing the guard refused. The condition is what makes the rule about a loss rather than about a word |
| F4 · nothing in the guard reads the `#` character. The lines a fence hid are read off the reader's own two passes — what survives `strip_comments` and is blank after `blank_fences` — so a Python comment at column 0 inside a fence is not a heading, and a Markdown heading inside one is | `skills/code-review/scripts/round_record.py#swallowed@db03c7a8`, `tests/test_the_record_is_generated.py#test_a_comment_at_column_zero_inside_a_fence_is_not_a_heading@8f378370` | Read 2026-09-06 in phase 2 at `5721a31`: the `hidden` comprehension opened against the case, which asserts exit 0 and the comment line arriving in the record. **Executed** 2026-09-06 in phase 1, where mutating the comprehension to a `startswith("#")` test turned both acceptance cases red; re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-06 | **The tidy-up is testing `startswith("#")` on the hidden lines**, which reads as asking the question directly and is faster than pairing two passes. A `#` at column 0 is a Markdown heading and a Python comment both, and only the fence tells them apart; the fenced block a probes row owes is usually code, so the direct question refuses the ordinary case. The mutation is named here because it is the argument for the pairing rather than an argument against the constant-keyed shape — F1's tidy-up and this one look like the same simplification from outside and fail differently |
