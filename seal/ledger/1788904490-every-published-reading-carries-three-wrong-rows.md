<!-- specs/1788904490-every-published-reading-carries-three-wrong-rows -->

<!-- One work item's rows, three tickets. No header — `fold_ledger.py` writes
the `###` at the release and moves this file into `seal/ledger.md`.

S1 and S2 are #200, S3 and S4 are #202, S5 is #193. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · a test runner named by PATH is a test run, and the shapes added are only those that mean the same thing in any repository — a script called `test` under `bin/` or `scripts/`, a `test.sh`, and the runners a language's own convention names. The row still means something: a directory called `testdata`, a `git log` naming the word, and this repository's other `bin/` scripts stay out | `skills/verify/scripts/session_cost.py#FAMILIES@eaaaa17b`, `tests/test_session_cost.py#test_a_runner_named_by_path_is_a_test_run@8765de5e` | **Executed** 2026-09-09 over the 180 transcripts on this machine: calls whose FIRST WORD is this repository's own runner go from **0 of 157** charged to `test` to 157 of 157. Nineteen positive and six negative shapes asserted one at a time; deleting the path-invoked branch turns the case red | 2026-09-09 | The config-row shape was the more faithful one and was rejected on cost — it cannot help a repository on the day it installs, and it puts a `PreToolUse` parser inside a script whose argument is a transcript that need not belong to the repository the command runs in. `questions.md` Q2 holds it with the condition under which it becomes right |
| S2 · a runner named inside a heredoc body is not a run of it, and what the table still cannot name is printed rather than left silent. The classifier stops at the heredoc operator, which answers the question for every family at once instead of one word at a time; and where `other` leads the table by seconds the report says so and names the slowest command charged there | `skills/verify/scripts/session_cost.py#family@413fcc85`, `skills/verify/scripts/session_cost.py#HEREDOC@96b8d9af`, `skills/verify/scripts/session_cost.py#analyse@3f56a33a`, `skills/verify/scripts/session_cost.py#report@48a15e90`, `tests/test_session_cost.py#test_a_runner_named_inside_a_heredoc_is_not_a_run_of_it@b1c38cbc`, `tests/test_session_cost.py#test_the_report_names_the_command_the_table_could_not@9ae97e0c` | **Executed** 2026-09-09: **644** calls the old family charged to `test` are document writes, and `lint/type` (361 → 175) and `build` (177 → 47) lose the same kind. Seven mutations one at a time, all caught — including the three the first fixture let live, which is what forced the fixture to carry a first-but-cheap command, two pipes of one command, and a named family larger than every unnamed one | 2026-09-09 | **Three mutants survived the first fixture and the fixture was rebuilt rather than the claim narrowed.** Taking the arrival-order entry, taking the un-grouped entry, and collecting over every family rather than over `other` all passed against a fixture whose first, largest and only-unnamed command were the same call. The bound on the heredoc cut is stated in the pattern's own comment: a lowercase unquoted delimiter is left classified as it is today, because cutting on `echo 'a << b' && pytest` would charge a real run to `other` — the error this change exists to remove |
| S3 · each token field is summed at the LARGEST count its message reached, not the first row carrying it. The dedup itself is unchanged: one message is one turn however many rows it is split across | `skills/verify/scripts/session_cost.py#token_totals@d6239cb8`, `skills/verify/scripts/session_cost.py#FIELDS@6bc4f293`, `tests/test_session_cost.py#test_a_streamed_message_is_counted_at_its_largest_row@b4d4d2f4`, `tests/test_session_cost.py#test_a_message_whose_rows_arrive_out_of_order_keeps_the_completed_count@7bebe96c`, `tests/test_session_cost.py#test_a_split_message_is_still_one_turn_and_not_one_per_row@a59e48e0` | **Executed** 2026-09-09 over the same 180 transcripts: 9,098 of 13,425 messages are split, and `output` reads 4,976,637 against a real 8,683,844. Four mutations — first-row-wins, last-row-wins, per-row summing, and the field list — all caught. `cache_write` and `cache_read` are asserted beside `output` as the control, since a change to per-row summing would move all three | 2026-09-09 | **The maximum rather than the last row, and the two agree everywhere they were measured** — all 13,425 messages, 0 rows out of order. The case that separates them had to be constructed, and it is here rather than left as *no observed difference*: `output_tokens` grows within a message, so the largest IS the completed count under any row order, where last-row-wins is right only under an ordering the transcript format does not promise |
| S4 · `load`'s per-turn tuple carries the stamp and the input count and nothing else. The third element it used to carry, that message's `output_tokens`, was read by nothing in the file | `skills/verify/scripts/session_cost.py#load@cd36b266`, `tests/test_session_cost.py#test_load_returns_input_counts_only@a9a6d3fd` | **Executed** 2026-09-09. `token_thirds` takes index 1 and `analyse` takes `len(turns)`; no other index appears in the module. Restoring the element turns the case red | 2026-09-09 | **Removed rather than repaired, and that is the argument #202 asked for.** Its *Not verified* row asks whether the second reader takes the same first row: it does, and for `output_tokens` it would be wrong — a first partial count sitting in a field with no consumer, which is how the defect in `token_totals` came to be written. The input-side fields this reader does use are fixed when the request is made and repeat unchanged, measured identical first-row and last-row over 13,425 messages, which is why this reader was never affected. The shape a future consumer should take is written where the element was |
| S5 · a third the file could not compute is charged 0, and 0 is not a baseline a multiple can be taken of; and the filter deciding which turns enter the mean is strictly positive rather than truthy | `skills/verify/scripts/session_cost.py#report@48a15e90`, `skills/verify/scripts/session_cost.py#token_thirds@25a4cd22`, `tests/test_session_cost.py#test_a_charged_third_is_not_a_baseline_the_context_line_multiplies@2a990121`, `tests/test_session_cost.py#test_the_context_line_still_prints_where_the_baseline_is_real@725f2344`, `tests/test_session_cost.py#test_a_negative_input_count_is_dropped_and_a_zero_is_dropped_with_it@69e4c80f` | **Executed** 2026-09-09, both directions in one case: a transcript whose FIRST third overflows printed the growth line and now does not, and one whose LAST third overflows was already silent and stays silent. Two mutations, both caught. The line is asserted still printing on a real 10 → 100 growth, so the conjunct did not buy its silence by silencing the line | 2026-09-09 | Zero goes on being dropped from the mean and that is deliberate, not a half-fix: `count` answers 0 both for a field a harness never wrote and for one it wrote as 0, so this file cannot tell a turn that spent nothing from a turn nobody measured, and a mean is the wrong place to guess. What changed is that a count below zero leaves with it |
