<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Ten rows over a branch that adds an agent, two commands and a subcommand.
S10 and S11 arrived with phase 5 and the enumeration below runs to S8; the
last three are the capped run, the rule that one seal is final, and the
subcommand's own refusals.

**S8 is gone, and work item 1789034970 (#120) is what removed it.** Its claim
was that `agents/sealer.md` names its one write as §6's own exception and
states that §2 as it stands forbids its one act — and its second coordinate
was the section written to be deleted, `## §2 as it stands, and #120`. #120
deleted that section, rewrote §6 so that a named write is the rule rather than
an exception, and renamed both cases the row cited. A row whose anchor a
change removes is REMOVED rather than re-pointed, so the successor claim is a
new row in that work item's own fragment,
`seal/ledger/1789034970-the-contract-is-settled-against-the-agents-that-exist.md`.
The enumeration above still names S8 because it is a record of what the branch
judged, and that judgment happened.
What earns a row here is a judgment whose repair looks obvious from the
outside, and a gate that draws a picture has an unusual number of them: a
sampling offset that reads as an off-by-one nobody would defend (S2), a
question asked before a call rather than after it, which reads as the wrong
order (S3), a refusal where a default would be friendlier (S4), a comparison
that runs late rather than always (S5), a second subcommand where one exists
(S6), a draft judgment inside a command that is about a ready pull request
(S7), and a paragraph written to be deleted (S8).

S1 is not one of those. It is the claim R4 of `seal/ledger.md` used to carry a
coordinate for: this work item renamed the case that reads the sentence, and a
renamed unit is a REMOVED anchor rather than a drifted one, so the claim moves
here rather than being re-pointed there.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form. `seal/ledger.md`'s eighteen rows that this
branch drifted were re-read one by one and re-stamped by an UNSCOPED
`--reverify`, which the scoped form cannot reach — and the measurement that
made it safe is in `phases/phase-4.md`: the base commit reads 1055 ok · 0
drifted, so there was no row on that file another session would have to judge.
The unscoped READ ran afterwards and is quoted in the same record. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · `CONTRIBUTING.md` §*Running the checks* says the full run is the **sealer's**, run once after the review rounds settle, and names both the section that forbids it to a segment and the definition that assigns it — and the case that reads the sentence is named for what the sentence says | `` `CONTRIBUTING.md#"## Running the checks"@bb771075` ``, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_section_says_the_full_run_is_the_sealers@f1147757` | Read 2026-09-10 in phase 4, against the section as phase 3 left it; **executed** 2026-09-10, the module green after the rename with the case reading the same two words it read before | 2026-09-10 | **This row exists because a rename is not a re-point.** `seal/ledger.md`'s R4 cited this case under its old name, `test_the_section_says_the_full_run_is_the_orchestrators`, and #30 made that name assert the opposite of what the case checks. Changing a unit's BODY drifts an anchor and changing its NAME removes one, so R4 lost the coordinate and the claim was written here — which is the rule `CLAUDE.md` and `templates/ledger.md` both state and which has, until now, had no worked instance in this repository. **The tidy-up is re-pointing R4 at the new name and deleting this row.** It reads tidier and it is the thing the rule forbids: a row's claim is bound to the code it cited, and a coordinate that resolves is not the test — R4's own claim was ALSO stale (it said the run was the orchestrator's), so a re-point would have carried a false claim onto a true coordinate. Both halves were repaired in the same pass, and only one of them stayed in that file |
| S2 · the disc is **computed** from the 29×32 chart rather than drawn, and it is sampled at each cell's CENTRE — `x + 0.5 - ox` — so the top and bottom rope rows come out equal and the figure cannot be off centre. The letter twin has the same width and height as the block form at every accepted scale, and every accepted scale draws the SAME BYTES in every process — the tie between two chart colours breaks on the count and then on position in the chart, never on set iteration order (round 1's 🟡 6) | `skills/verify/scripts/seal_stamp.py#build@dd47eca8`, `skills/verify/scripts/seal_stamp.py#shrink@e8e6e102`, `skills/verify/scripts/seal_stamp.py#letter_row@626cc1bb`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_disc_is_symmetric_because_it_is_computed@26ba07fc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_twin_and_the_block_form_have_equal_width_and_height@d3c72bf2` | **Executed** 2026-09-06 in phase 1: the source outside the tree sampled each cell at its corner, and the symmetry case is what found it — the top rope row measured 18 letters against the bottom row's 12, with the first line blank. After the fix both read `OooOOOooO`, and the disc is 43×22 at 1.0 and 33×17 at 0.75. Re-read 2026-09-10 in phase 4; the module is green at this branch's head | 2026-09-10 | **The tidy-up is `int()` instead of `math.floor()` in the chart lookup**, which reads as the same thing and is not: `int` truncates toward zero, so it reads stitch 0 twice at the top-left edge and the figure loses a row of symmetry at exactly the place the case measures. **The second is sampling at the corner again**, which is what the source did and what any reader writing `dx = x - ox` from scratch would write. #30's claim is that *a circle that is calculated cannot be off centre*, and that holds only when the sample point is the cell's centre — the case is the whole of what keeps the claim true |
| S3 · the shape is picked from the stream **before** the console is reconfigured to UTF-8, and a pipe gets letters rather than blocks | `skills/verify/scripts/seal_stamp.py#pick_shape@027a2979`, `skills/verify/scripts/seal_stamp.py#main@4b8233a8`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_pick_shape_is_letters_off_a_utf8_terminal@dfa34011`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_command_piped_prints_the_twin@66d0411b` | **Executed** 2026-09-06 in phase 1: `sys.stdout` and `sys.__stdout__` are one object, and `reconfigure(encoding="utf-8")` moves both — under `PYTHONIOENCODING=cp949` the encoding read `cp949` before the call and `utf-8` after it. Asked afterwards, a cp949 console is handed blocks it cannot draw. Simulated under `script` on that codepage: letters. Re-read 2026-09-10 in phase 4 | 2026-09-10 | **The tidy-up is asking after the reconfigure**, which is where every other reader of an encoding in this repository sits and which `plan.md`'s own Technical context prescribed. It is wrong here for one reason: `hooks/console.py#to_utf8` exists to make the stream lie about what the terminal can render, and this is the one question that has to be asked of the truth. **The second is treating a UTF-8 PIPE as a terminal** — `plan.md` said a pipe gets blocks and `spec.md` said the twin is chosen when stdout is not a UTF-8 *terminal*; the spec won, because an agent's stdout is a pipe and §Out says the sealer's returned text carries the twin |
| S4 · a missing `Broad gate` row is a **refusal** — the row is named, exit 2, nothing run — and never a default | `skills/verify/scripts/broad_gate.py#broad_command@cff39c72`, `skills/verify/scripts/broad_gate.py#missing_row@4be18a64`, `` `templates/config.md#"## Broad gate"@0102348f` ``, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_without_the_row_the_gate_names_it_and_runs_nothing@54dd2dd0` | **Executed** 2026-09-06 in phase 2 on a fixture repository with no row: the message names the row to write and the file to write it in, and nothing ran. Re-read 2026-09-10 in phase 4 | 2026-09-10 | **The tidy-up is a default of `pytest`**, which makes the command work out of the box and is the counterfeit `verify` names: it seals a repository that runs `npm test`, and it seals it silently. `true` is the same mistake with the argument made honestly. **The second is asking a person for the command**, which is the other thing this cannot be — the gate runs unattended, so a refusal naming what to write is answered by the next reader, where a question stops a session that may have nobody at the keyboard. `CLAUDE.md`'s first goal is what decides between them |
| S5 · the base comparison is **reactive**: it happens only when a test fails, only on the failing files the base actually carries — the absent ones read `new` without a run, because pytest handed a path that does not exist prints no `FAILED` line and would lose the measurement for every other file in the same run (round 1's 🟡 4) — at the base in a scratch worktree the gate removes, and each failure comes back `new` or `failing on base too`. What it re-runs is what stands before the row's first `&&` | `skills/verify/scripts/broad_gate.py#compare_at_base@efeccb32`, `skills/verify/scripts/broad_gate.py#first_command@8623ea0c`, `skills/verify/scripts/broad_gate.py#failing_files@6d1dc82c`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_failing_test_is_not_sealed_and_is_new_when_the_base_passes@49706137`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_failure_the_base_shares_is_labelled_failing_on_base_too@b5babfda` | **Executed** 2026-09-06 in phase 2, and once on the real repository at `0c04bc3`: the scratch worktree's `bin/test` built its own `.venv` with `uv` in about ten seconds out of a 367-second run, because `run_tests.py` resolves the root relative to the worktree's own copy. A file the base does not carry cannot fail there, so it reads `new`. Re-read 2026-09-10 in phase 4 | 2026-09-10 | **The tidy-up is running the whole suite at the base as well**, which is symmetric and is the waste `skills/verify/SKILL.md` §*The broad gate* already measured — a 19,000-test suite run serially for a baseline before any failure existed. **The second is splitting the row on something cleverer than the first `&&`.** The row is one shell line and nothing in it says which part is the suite runner; `templates/config.md` and the config skill both say to put the runner first for exactly this reason, which is a convention the reader keeps rather than a fact the parser can check |
| S6 · `round_record.py seal` sets the LAST record's `Broad gate` cell and touches nothing else, and the number of `raise Refused` sites in it is **six**, counted rather than described: no `Pass` box or more than one, `Pass` unchecked, a `Fixes checked by` that is neither a later round nor `no fixes to check`, a cell with no SHA in it, a SHA the repository cannot see, and a `Target SHA` that descends from the run's own commit. It is a subcommand of its own because `close` cannot make this write twice | `skills/code-review/scripts/round_record.py#seal@e0ca2f8f`, `skills/code-review/scripts/round_record.py#last_record@5e446de7`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_writes_the_last_records_cell_and_nothing_else@30b3531f`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_while_pass_is_unchecked@8aa4c246`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_a_sha_the_target_descends_from@f9794302`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_while_the_fixes_have_been_read_by_nobody@8ce6f088`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_a_cell_with_no_sha_in_it@11fab069` | **Executed** 2026-09-06 in phase 2: the write is compared byte for byte against every other line of the record, and each refusal names its reason and writes nothing. The failure it removes was **executed** on the other work item of this release — `close --broad-gate` on a record whose fix table had been applied refused with *the fix table has a row for finding 17 (`answered`), which the reviewer already closed … no cell was written*, and the only way through was a fix table with a header and no rows. Re-read 2026-09-10 in phase 4 | 2026-09-10 | **The tidy-up is folding this back into `close` as a flag**, which is where the cell was written until #30 and which looks like one subcommand too few. `close` applies a fix table and refuses a row for a finding the reviewer already closed — correctly — so on a settled record every route through `close` is refused for the table it has to be handed. **The second is having the sealer return the value for the orchestrator to write** (`questions.md` Q3): five such reach-backs were forgotten on one branch, and a sealer with no durable write is #30's *nobody is assigned it* one step later. **A refusal here is exit 2 and no stamp**, never a stamp followed by a note: the alternative prints *sealed* over a record that says the run came too early. **Phase 5 removed the `Needs a fix` refusal and this row lost a coordinate with it**: the case that pinned it was renamed, which removes an anchor rather than drifting one, and the claim it carried moved to S10 below. The claim above was corrected by re-reading `#seal` rather than by re-stamping it — a re-stamp recomputes a hash and reads nothing, so a row enumerating three refusals over a function that now has two would have passed silently |
| S7 · the gate runs `chain_check.py` judged as a **draft** pull request, on purpose, and leaves a payload GitHub itself wrote alone | `skills/verify/scripts/broad_gate.py#draft_env@61c9c8af`, `skills/verify/scripts/broad_gate.py#gate@b7709fff` | **Executed** 2026-09-06 in phase 2 on the `--record` fixture: judged as READY the same run exits 1 with *`Broad gate` is `not yet` on the last round record, and this is a ready pull request* — the cell the gate is about to write | 2026-09-10 | **The tidy-up is judging it ready, since a ready pull request is what the gate is for.** The gate runs BEFORE `seal` writes the cell, so at that moment the last record honestly reads `not yet`, and a ready judgment fails the run on the one row the run exists to fill. **The second is writing the payload unconditionally**, which would overwrite the event GitHub hands a workflow; `draft_env` writes one only when the environment carries none, which is the shape `round_record.run_check` already uses |
| S9 · the class *what names the owner of the broad run* includes a printed failure MESSAGE, not only a document. `chain_check.py`'s **three** fatal refusals name the sealer's spawn and `broad-gate`, the second keeps `close --broad-gate` for the one pass where fixes and the gate land together, and the third — the premature SHA — says to re-take the run rather than telling the reader to take it by hand | `skills/code-review/scripts/chain_check.py#broad_gate@48254b45`, `tests/test_chain_check_at_the_pull_request.py#test_a_broad_gate_that_never_ran_fails_a_ready_pull_request@0810f067`, `tests/test_chain_check_at_the_pull_request.py#test_a_one_word_cell_is_not_a_way_past_the_arm@0c06ba74` | **Executed** 2026-09-06 in phase 3: three mutation arms — the agent unnamed in each message, and the one-pass route dropped from the second — all red. The two messages were found by `survivor-check`, not by the spec: `spec.md` §Scope 6 lists seven documents and not this file, because it is a script | 2026-09-10 | **The tidy-up is reading `spec.md`'s list as the class.** It is a list of documents, and these are the two places a person actually reads the instruction — at the moment a pull request is refused. Contract §12 is the rule and this is its instance: the finding named a coordinate and the fix was owed to every place the same cause produces. **Two code comments inside `chain_check.py`'s own gate arm were deliberately left**, because they explain why an unparseable cell above the cutoff is a cell somebody chose, and that reasoning survives `seal` joining `close` — both are subcommands, neither is free text. The survivor check does not reach them, and the docstring of the case that pins that arm was corrected to name both writers |
| S10 · a CAPPED run reaches its seal. `seal` asks the verdict table — the `Pass` box — and never the reviewer's `Needs a fix`, so a record whose findings all closed `deferred <home>` seals while that row still reads `yes`, and the reviewer's row is left exactly as written | `skills/code-review/scripts/round_record.py#seal@e0ca2f8f`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#capped_item@afa7f6bc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_writes_over_a_capped_runs_needs_a_fix@9203d9a9` | **Executed** 2026-09-10 in phase 5, seen red against the old code first — the refused run printed *round-1.md's `Needs a fix` reads `yes — 🔴 1`, and the broad gate runs after the rounds settle … no cell was written*, which is phase 4's measurement reproduced by the case. Green after the removal, and `seal` ends with `chain_check`, so exit 0 is the whole chain end to end | 2026-09-10 | **The tidy-up is keeping the refusal and teaching the capped run to rewrite `Needs a fix`.** That row is the reviewer's answer from while the round ran, and nothing else in the workflow rewrites what a reviewer wrote; editing it to unblock a gate is the review certifying itself one step removed. **The second is leaving both refusals and calling the capped run an exception at the pull request**, which puts a documented way past `chain_check` in front of every branch. What the removed refusal was reaching for is *is a finding still open*, and `Pass` answers exactly that, one row down, from the table rather than from prose |
| S11 · the rule that one seal is final has ONE owner and two linking carriers, and it is held by the same table that holds the other nine — `skills/verify/SKILL.md` states *every agent seals what it verified, and the one seal over the whole project is the sealer's*, and `agents/sealer.md` and `agents/warden.md` each carry a sentence naming that section rather than restating the rule — the warden's said it verbatim before linking until round 1's correction, which is the drift the table exists to catch arriving in the row that added it | `` `skills/verify/SKILL.md#"## Every agent seals what it verified, and one of them is final"@13e77c7f` ``, `tests/test_the_rules_have_one_owner.py#test_every_link_names_the_owner@314ad3a3`, `tests/test_one_word_one_meaning.py#test_the_owner_states_that_one_seal_is_final@cdc94327` | **Executed** 2026-09-10 in phase 5: both parametrised cases red against the definitions as they stood — the owner case on the missing sentence, the link case naming `agents/sealer.md` — and green after. The sweep case that forbids an anonymous instance was red on four files at the same commit | 2026-09-10 | **The tidy-up is renaming the sealer so one agent owns the word.** The product's name is built on the noun and the whole stamp design rests on it, and the ambiguity was never that several seals exist — a smith's proof block over its own slice is a legitimate seal. What was missing is that one of them is final. **The second is writing the rule into each definition rather than linking**, which is the drift `tests/test_the_rules_have_one_owner.py` exists to catch and which its own docstring counts: one rule reached eight carriers and three consecutive rounds corrected them three at a time |
| S12 · `broad-gate` reads BOTH of `seal`'s non-zero exits as unsealed. A refusal raised before the write exits 2; what `seal` returns after the write is `chain_check.main`'s `1 if errors else 0`, and the gate's message tells the two apart by the word `sealed` in the `round-record:` line above it — **round 1 wrote this row saying the line's PRESENCE was the discriminator and round 2 measured that both endings print one**, so what the row recorded as verified was the thing that was wrong; the correction is the word, not the line | `skills/verify/scripts/broad_gate.py#gate@b7709fff`, `skills/verify/scripts/broad_gate.py#seal_record@4d582ed3`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_seal_exit_that_is_not_two_leaves_the_tree_unsealed@6a8b01ee` | **Executed** 2026-09-10, round 1's fix pass: the case returned exit 0 against the branch as reviewed, with the disc printed over a failing chain check | 2026-09-10 | **The tidy-up is reading `== 2`**, which is what shipped and which is the natural reading of a subcommand documented as *exit 2 on a refusal*. The second exit is not a refusal at all — it is the chain check `seal` runs after the cell is written, and it comes back as 1 — so the seal that fell through was over a tree the gate's own check refuses. **The second is treating that 1 as a failure of the checks**: the checks passed, the cell exists, and the message has to say so or the reader hunts a check that is green |
| S13 · a run that ended at the round cap seals and a run whose fixes nobody has read does not, and the two are told apart by `Fixes checked by` rather than by `Pass`. `close` ticks the box the moment a fix table applies, which is one row earlier than the run ending; it re-derives the row to `no fixes to check` only where nothing commissioned a fix, which is the capped case. **The row holds three values and everything outside two of them is refused**, not `nobody` alone: reading `nobody` alone let a name, a word outside the vocabulary and an empty cell reach the write, and the chain check run AFTER the write then refused on that same row | `skills/code-review/scripts/round_record.py#seal@e0ca2f8f`, `skills/code-review/scripts/round_record.py#landing_values@87e329a1`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_writes_over_a_capped_runs_needs_a_fix@9203d9a9`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_capped_run_still_seals_beside_the_third_refusal@c2dc3011` | **Executed** 2026-09-10, round 1's fix pass: the new refusal red against the code as reviewed; and the plausible wrong fix — refusing anything but a named checker — turns both capped-run cases red, which is what says the two rows are not one rule | 2026-09-10 | **The tidy-up is replacing the `Pass` refusal with this one**, since this is the stricter test. They answer different questions: `Pass` says nothing in the verdict TABLE is open, and this says the fixes that closed it were read. A record can fail either without failing the other. **The second is the whole history of this cell**: three rows were tried in order — `Needs a fix`, which refused a capped run; `Pass`, which lets an unread fix pass through; and this one. Anything that reads like the obvious single answer has already been one of the three |
| S14 · the panel's `suite` row carries pytest's own counts and never a linter's, and the discriminator is the WALL CLOCK rather than a list of words. A run in which every test was skipped reports its counts instead of coming back empty, which the panel would have rendered `exit 0` | `skills/verify/scripts/broad_gate.py#suite_counts@2febe15d`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_suite_row_reads_pytests_counts_and_not_a_linters@f9362575` | **Executed** 2026-09-10 over round 1's and round 2's shapes: `2 warnings emitted`, `Found 2 errors.`, `3 skipped in 0.10s`, and a decorated `768 passed in 63.21s (0:01:03)` | 2026-09-10 | **The tidy-up is a list of pytest's summary words**, which is what round 1 shipped and what round 2 reopened. It closes the finding on the instance and not the class: the list held `errors` and not `warnings`, so one linter's line still landed on the row — and ADDING the missing words is worse, because `warnings` back in the list revives round 1's original defect. The two halves fight, and a word list is the wrong axis. **The second is reading the LAST line rather than the last match**, which breaks on a runner that prints anything after its summary. **This row lost a coordinate rather than re-pointing one**: the clock pattern stood as a module constant `SUMMARY_TAIL` until `close` refused the fix table for it — a top-level name added by a fix answering a finding inside a unit `round-1.md`'s `New units` names is at depth 2, and a fix pass may not add one. The pattern is inline in `#suite_counts` now, so there is no second anchor and the claim is whole in the first |
| S15 · `quote` takes the platform as an ARGUMENT rather than reading `os.name`, so both branches can be turned red from either machine; and the Windows branch uses double quotes rather than `subprocess.list2cmdline`, because `run(..., shell=True)` there goes through `cmd.exe` and `list2cmdline` builds `CreateProcess` quoting | `skills/verify/scripts/broad_gate.py#quote@b6b2caf6`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_path_is_quoted_for_the_shell_of_either_platform@e99855e3` | **Executed** 2026-09-10: `list2cmdline` with the platform as an argument returns `tests/x&y.py` unescaped, and `return path` — which was green before this case existed — fails all three parametrisations. The `cmd.exe` behaviour itself is **read**, from Python's own documentation; there is no Windows here | 2026-09-10 | **The tidy-up is reading `os.name` inside the body**, which is what shipped and which is exactly `docs/flow.md` #103's class made out of the fix for it: the branch runs on CI's `windows-latest` leg and nothing asks what it produced, so the whole unit could be replaced by `return path` with every case green on every leg. **The second is claiming the quoting is complete.** `cmd.exe` expands `%VAR%` inside double quotes, so a path holding percent signs is still mangled; the docstring names it rather than closing it, because the fix for that is not quoting |
