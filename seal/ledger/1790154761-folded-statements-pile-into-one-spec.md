<!-- specs/1790154761-folded-statements-pile-into-one-spec -->

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows, one per rule the next fold meets: E1 the editions are paired, S1
a folded statement names what enforces it, P1 a document over the ceiling
takes no new fold. Nothing in `seal/ledger.md` moved, because nothing
anchored there was removed. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| E1 · every top-level `docs/X.ko.md` beside a `docs/X.md` has the same sequence of live-heading levels, and under each heading position the same multiset of fold-marker ids; `CONTRIBUTING.md` owns the rule and names the check; the Korean one-root edition carries the section and ten markers it lacked | `tests/test_both_editions_carry_the_same_folds.py#outline@950b0cd6`, `tests/test_both_editions_carry_the_same_folds.py#disagreements@d1792e2e`, `CONTRIBUTING.md#"## House rules">"heading levels and, under each heading position, the same fold markers. A"@fe81ef3b`, `docs/one-root-by-lifetime.ko.md#"## 저장소가 스스로 정하는 것, 그리고 그것을 읽는 방법"@9aeb18ea` | **Executed** 2026-09-23. At the base the real-tree case was red on the heading levels (23 positions against 22); with the Korean heading added and no markers yet it was red on the missing ids position by position; green with the ten markers placed. Mutations, one at a time: a Korean marker moved to another section, the per-position comparison off, the level comparison off, the heading pattern without its space — each red. The guide pin was red with `CONTRIBUTING.md` at `HEAD` | 2026-09-23 | Markers are read with `unverified_check.py#live_lines` and `#FOLD_MARKER`, so the check counts exactly what the fold counts. The two READMEs are outside `docs/` and not read by it |
| S1 · a fold marker whose work-item id is at or above `SHAPE_CUTOFF` heads a statement that opens bold and carries exactly one live `Enforced by:` line, whose targets each resolve to a file and, with `::name`, to a `def` or `class` found by `ast`, or which says `nothing — <why>` with a reason; stacked markers share one statement, which ends at the next marker, heading or end of file; settle §2 owns the rule | `tests/test_a_folded_statement_names_what_enforces_it.py#SHAPE_CUTOFF@dd68a93b`, `tests/test_a_folded_statement_names_what_enforces_it.py#statements@834d3ec9`, `tests/test_a_folded_statement_names_what_enforces_it.py#target_problem@98013f16`, `tests/test_a_folded_statement_names_what_enforces_it.py#shape_problems@264f736c`, `skills/settle/SKILL.md#"### 2. Write one standing statement per segment">"**A standing statement has one shape: the rule, its grounds, and what enforces"@fbcf0ce5` | **Executed** 2026-09-23. Ten mutations of the reader, one at a time, each red: bold check off, count relaxed, targets not resolved, `::name` lookup off, empty reason accepted, cutoff lowered to 0 (the real `docs/` goes red), cutoff raised, stacking off, headings not ending a statement, live filter off. The settle pin was red with settle at `HEAD`. **Read**: `docs/` holds no marker at or above the cutoff yet, so the real-tree case binds none and the fixtures carry the bound side | 2026-09-23 | What it cannot read is whether a target really enforces the rule, or whether two statements contradict; settle §2 says both |
| P1 · a top-level `docs/*.md` outside `OVER_CEILING` stays at or under `LINE_CEILING` lines; a listed one must still exceed it, must exist, and must carry exactly its frozen marker count; settle §2 owns the placement rule and `docs/the-evidence-ledger.md` states the three values, pinned against the constants | `tests/test_a_document_has_room_for_the_next_fold.py#LINE_CEILING@122ed4fe`, `tests/test_a_document_has_room_for_the_next_fold.py#OVER_CEILING@6ced4306`, `tests/test_a_document_has_room_for_the_next_fold.py#ceiling_problems@f206dc07`, `skills/settle/SKILL.md#"### 2. Write one standing statement per segment">"**One subject, one document, and a document over its ceiling takes no new"@ab3c0c05`, `docs/the-evidence-ledger.md#"## The fold, and what tells it from a deletion">"**This repository holds the fold's shape and placement rules with three"@4dc7f4f4` | **Executed** 2026-09-23. Red: the evidence ledger at `HEAD`, each of its four stated values edited by one digit, settle without the placement rule, and five reader mutations (ceiling off, growth-only comparison, outlived entry, missing listed file, live filter off). The real tree is green: `docs/review-chain-spec.md` at 2,159 lines and 29 markers | 2026-09-23 | The count is compared for equality, so a removed marker lowers the frozen count rather than leaving room to refill. Home of the entry: MichaelYcJo/SpecSeal#526 |
