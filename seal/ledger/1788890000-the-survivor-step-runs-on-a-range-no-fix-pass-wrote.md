| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| W1 · the survivor step passes on a pull request whose base is `main`, and prints which of *skipped* and *found nothing* it did | `.github/workflows/hygiene.yml#"wording this branch removed is not still standing elsewhere"`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_workflow_step_skips_a_release_range_and_says_why` | **Executed** 2026-09-08. The release pull request for 0.9.3 (#283) failed this step at `9e46d1c` with `72 place(s) still carry wording this range removed`, over a range of five squashed work items. Mutated three ways, one at a time, restored from kept bytes between: the guard block removed, its `exit 0` removed, and the reason clause removed — each turns the case red, and the restored file is green | 2026-09-08 | The check is not wrong and its floor is not raised. Its unit is a fix pass's range — 77 of them, and the 1.6 floor chosen against that curve — and a release's range is the union of every work item it carries, so one item's removed wording is scored against four other items' prose. Each of those items was already checked at its own pull request, so the union asks a different question rather than a bigger one. The guard prints rather than being a job-level `if:`, matching the two steps above it: a skipped step reads as *did not run* |
