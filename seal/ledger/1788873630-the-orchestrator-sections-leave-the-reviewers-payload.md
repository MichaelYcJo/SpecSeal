<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Six rows. The first three are claims that already stood in `seal/ledger.md`
and whose anchors this change removed: #265 moved the five sections
`skills/code-review/SKILL.md` prefixed `Orchestrator:` into
`skills/code-review/orchestration.md`, so a row anchored on one of those
heading paths had its anchor removed rather than moved. The rule is that such
a row is removed where it stood and its claim re-stated here, so the three
rows below carry the same claims against the file that now holds the prose.
Their verification history is in the released sections of `seal/ledger.md`
under `1788229400`-era and `1788277657` and `1788433011`.

The last three are this work item's own. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The verifying round treats what `New units` names as a finding surface, not a verification surface | `skills/code-review/orchestration.md#"## Orchestrator: the run ends with a verifying round"@b1368831`, `agents/warden.md#"## Role">"judge them as code — *is this correct* — never as fixes. A finding there is"@98a430c2` | Read, and pinned by `tests/test_the_fixes_name_their_surface.py::test_new_units_are_a_finding_surface_not_a_verification_surface` with the inverted spellings refused beside the sentences. **Executed** on this branch: that case is parametrized over the two carriers and the review-skill leg was red against the old path and green against the new one | 2026-09-08 | Re-anchored, not re-verified: the section moved file and its prose is byte-identical to what the removed row cited. Without the exemption, *answers rather than new findings* tells the reviewer to skip the one set of units nobody has reviewed — round 4's fix commit created eight, four defective. The removed row carried six re-reads between 2026-09-04 and 2026-09-05, every one of them recording that the exemption's own sentence was untouched; this row starts that count again against the new coordinate |
| A fix pass is obtained by resuming the implementing session; the fresh spawn is the fallback for a session that no longer exists, and it costs the handoff before round 1 | `skills/code-review/orchestration.md#"## Orchestrator: a fix pass resumes the implementer"@12414490` | **Executed** — pinned by `tests/test_review_axes.py::test_the_fix_pass_resumes_the_implementer` (the sentence, the 282-call measurement, the fallback clause), which was red against the old path on this branch and green against the new one | 2026-09-08 | Re-anchored, not re-verified. Three measurements, no counterexample: 282 calls / 45 minutes as a fresh spawn (#33) against 30 / 3.9 (#29) and 26 / 5.2 (the #57 chain). The claim is unchanged and the prose is unchanged; only the file holding it moved **Re-read 2026-09-08 by work item 1788873640 (#180)**, which added a paragraph to this section — the fix pass asks what its range left standing, after the `close` paragraph. The resume-first sentence, its three measurements and the fallback clause are untouched, and the claim holds. |
| The orchestrator is bound by the same contract, and the two skills it reads are where that is written | `skills/code-review/orchestration.md#"## Orchestrator: a fix pass resumes the implementer"@12414490`, `skills/implement/SKILL.md#"load it.** A session driving the work directly reads this skill and never"@21ad749e` | **Read**, both paragraphs, this session. Nothing at runtime reads either — the binding is prose a session obeys — so what can be checked is that the two files say it and that neither restates a rule the contract holds | 2026-09-08 | Re-anchored, not re-verified. `agents/*.md` cannot reach an orchestrating session, and #107's headline failure was an orchestrator breaking a rule it had put into every prompt it sent. The paragraph binding the orchestrator travelled with its section into `orchestration.md`, which is the file an orchestrator is now sent to — so the claim is if anything sharper than it was: the binding sentence and the orchestrator's procedure are in one file **Re-read 2026-09-08 by work item 1788873640 (#180)**: the paragraph it added sits above the contract paragraph and changes nothing in it. The claim holds. |
| The seam is the one the file's own headings drew, and it is contiguous | `skills/code-review/orchestration.md#"# code-review — the orchestrator's half"@1b376aea`, `skills/code-review/SKILL.md#"## The orchestrator's half is a file of its own"@053c4d7a` | **Executed** — the five sections were lines 236–653 of the pre-split file, one unbroken block; the 418 lines the diff removes are byte-identical to the block the new file holds, checked line by line against `git diff -U0`, with the one extra removed line being the blank separator. 24,947 characters as cut — the block without the blank line that separated it from `## Findings format`, and 24,948 by the section-table method every other figure here uses, 53.1% of the 46,986-character file | 2026-09-08 | Contiguity is the evidence that the seam is the author's rather than this change's: every heading in the run carries the prefix and nothing outside the run does. #255 §4 proposed a different seam — before-first-read against when-writing-the-record — which recovers 12,140 characters and carries the risk it names, that a reviewer has to know the record's shape while reading **Re-read 2026-09-08 by work item 1788873640 (#180)**, which inserted a paragraph into the moved block. The byte-identity above is a record of the split as executed — it says the 418 lines that diff removed equalled the block that diff created — and a later commit adding a paragraph does not reach back into it. Contiguity, which is what the row claims, is unaffected: the seam was the author's heading run and still is. What is no longer true is any present-tense reading of byte-identity, and that reading was never the claim. |
| A reference to a moved heading is not found by grepping the heading's own prefix | `tests/test_the_rules_have_one_owner.py#ORCH@de7911e1` | **Executed** — the ticket's enumeration greped the literal `Orchestrator:` and reported that no reference crosses the seam. Re-taken by construction over every heading inside the five sections, `##` and `###` alike, against every tracked file with whitespace collapsed: eight of the twelve headings are named outside the file, four of them by a `###` subsection reference the prefix grep could not see — `agents/smith.md` and `skills/implement/SKILL.md` name one, `tests/test_a_record_precedes_the_fixes_it_commissions.py` names another, and `docs/review-handoff-protocol.md` names a `##` one twice | 2026-09-08 | This is `agent-contract` §12 met in the enumeration rather than in the fix: the class is *a reference to a section that moved*, and a grep for the section's naming convention finds only the sections, never the references to their children. The same hole caught this session's own first check — a path grep over `tests/` reported the ticket's eleven modules and missed ten more that name the file as the tuple `("skills", "code-review", "SKILL.md")` |
| The eleven modules that pin the path are not the modules the split breaks | `tests/test_docs_line_wrap.py#COVERED@f81d0929` | **Executed** — 21 test modules name the review skill (11 by path, 10 by tuple only). Of the ticket's eleven, six pass untouched because what they pin stayed in the reviewer's half; five modules it never named fail, and a twelfth affected module names a moved `###` heading without naming any file. `test_docs_line_wrap.py` is the case with no failure and a real loss: the 418 moved lines were wrap-covered inside `SKILL.md` and the new file had to be added to `COVERED` or the coverage went silently | 2026-09-08 | A list of what a change costs, taken by reading, is a list of what somebody remembered. The wrap-coverage instance is the one a test run could never have reported: the check reads a fixed list of paths, so material leaving a listed file loses its guard with nothing going red |
