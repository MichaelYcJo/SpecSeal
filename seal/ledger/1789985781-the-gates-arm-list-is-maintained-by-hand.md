| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| G1 · Every step of `hygiene.yml`'s `release` job is mirrored by a named arm or excluded with a written reason, and the partition is held against the workflow from BOTH sides — a step added there and a row naming a step renamed away each go red on their own | `skills/verify/scripts/broad_gate.py#PARTITION@53860d6c`, `tests/test_the_gate_names_every_step_ci_runs.py#test_every_step_the_workflow_runs_is_classified@14575fb1`, `tests/test_the_gate_names_every_step_ci_runs.py#test_every_entry_of_the_partition_names_a_step_the_workflow_has@c11b6549` | **Executed** 2026-09-21. Both cases red against `PARTITION`'s absence (`AttributeError: module … has no attribute 'PARTITION'`, 10 failed), green after. Three mutations of the workflow, applied alone and restored from bytes kept outside git: a fourteenth step added reddens A1 alone; a step deleted whose row stands reddens A2 alone with A1 green; a step renamed reddens both | 2026-09-21 | Half a pin is #423's finding 4, and the deletion mutation is what shows this one is not half: the two cases fail on different edits rather than on one edit each way. `plan.md` §*The failure scenario at six months* names the hole this does NOT close — an exclusion written to make the case green rather than to state a truth |
| G2 · The step names are read out of `hygiene.yml` as text, with no YAML parser, and the reader takes text so it can be driven over shapes the file does not have | `skills/verify/scripts/broad_gate.py#job_steps@afc42ebb` | **Executed** 2026-09-21. Six cases drive it over fragments this repository's workflow does not carry: two jobs, an unnamed step, a commented-out step, a job that does not exist, a file with no `jobs:` at all, a quoted name, and a name carrying a colon. Four mutations each red on their own case — the job filter removed, `unquote` neutered, `STEP_RE` splitting a name at its first colon, `STEP_RE` unanchored from the dash | 2026-09-21 | #424's finding 4 is the precedent and its repair is the shape copied here: a reader nothing drives is a reader nobody can tell is partial. Driving it is what found the first spelling partial — splitting on `"\njobs:\n"` cannot see a `jobs:` on a file's first line, and no test over `hygiene.yml` could have said so. A fifth mutation left the module green at 10 passed and took a unit out of the tree with it: the line dropping comment lines is unreachable, because both patterns already require the first non-blank character to be the dash or a job key's letter. **Re-read 2026-09-21, round 1's fix pass.** The claim is unchanged and the docstring now carries the one gap round 1 found by driving the reader further: a TRAILING comment joins the step name, where YAML ends an unquoted scalar at ` #`. Not live, fails loudly through A1 rather than silently, and shared with `strip_comments` in `tests/test_ci_gives_the_checks_what_they_need.py`, which is why round 1 recorded it as a correction rather than a fix |
| G3 · Six steps of the `release` job had a local answer and no arm; two of the six run a check the plugin ships, and those two are now arms | `skills/verify/scripts/broad_gate.py#gate@084b7e9f` | **Executed** 2026-09-21. `test_every_arm_the_partition_names_is_an_arm_the_gate_runs` and its opposite, red when either arm is deleted from `gate()` while its row stands. The classification itself was taken by construction over all thirteen steps rather than by reading the ones that looked relevant | 2026-09-21 | This is `questions.md` M1, and the frame refused to guess it for a reason: the answer is six and not the one the issue's title suggests. The line between the six and the two is whether the plugin ships the check — `correction_check.py` and `seal.py` are under `skills/`, while `gather_changelog.py`, `fold_ledger.py` and `claude_block.py` are under `.github/scripts/`, which exists in this repository and in no other clone. `seal/config.md`'s `Broad gate` row is where a repository names checks of its own, and whether these three go there is Q2, for the owner |
| G4 · A branch whose merge dropped a correction the ledger had made is refused by the gate, and the same branch with the correction kept is sealed | `tests/test_the_gate_names_every_step_ci_runs.py#test_a_merge_that_dropped_a_correction_is_not_sealed@ec36cfa3` | **Executed** 2026-09-21. Red against the gate as it stood, and the red is the defect itself: **exit 0 with the stamp drawn**, every other arm green and the ledger row reading `1 ok . 0 broken`, over a branch CI would have refused. Green after at exit 1 with `corrections` named and `Corrected 2026-09-15` quoted. A second mutation, the arm asking about `HEAD..HEAD`, reddens it alone | 2026-09-21 | This is the instance #468 was opened for. The paired case — the same fixture resolved the other way, sealed — is what keeps the first from passing against a gate that refuses everything. The arm takes the survivor arm's range spelling and inherits its known non-equivalence with the step: the workflow skips both when the base is `main` and the gate skips neither, which predates this work. **Corrected 2026-09-21 by review round 1, finding 2.** This cell sent that non-equivalence to #423, which ships in this release, so the fact would have had no open home the moment 0.12.2 reached `main` — and #423 is about the base the gate resolves rather than about a guard. Its home is **#473** |
| G5 · A `Mode` row that disagrees with where the folder actually is stops the seal | `tests/test_the_gate_names_every_step_ci_runs.py#test_a_mode_row_that_disagrees_with_the_folder_is_not_sealed@c50c742f` | **Executed** 2026-09-21. Red against the gate as it stood — the fixture sealed green — and red again when the arm is deleted while its row stands | 2026-09-21 | A3 is structural and a structural pin alone can only be broken by a reader of the source. The behavioural half drives the one direction of that disagreement CI can ever reach: a row saying `local` beside a root committed in the tree. `seal.py mode --check` takes no `--root` and resolves from the working directory, which `run` already sets to the tree being gated |
| G6 · A run says what it did not answer — the count on the panel, the names on the stream beside the command — naming a step the partition EXCLUDES with a reason apart from one it has no row for at all, and a run that answers every step says so rather than going quiet | `skills/verify/scripts/broad_gate.py#coverage_line@f3dd1189`, `skills/verify/scripts/broad_gate.py#panel@e2c844e8`, `tests/test_the_gate_names_every_step_ci_runs.py#test_the_stamp_says_how_many_steps_the_seal_did_not_answer@b4f25713` | **Executed** 2026-09-21. Five mutations, each red on its own case: the stderr line never written, the panel row removed, `unanswered` answering nothing, `workflow_text` never finding a workflow, and `coverage_line` going quiet when nothing is unanswered | 2026-09-21 | `questions.md` W1 asked a count or the names and the answer is both, on different streams: a panel value is 23 columns (`PANEL_VALUE_WIDTH`) and `2 of 3 not answered` fits where a step name does not, while A6 asks that a reader can tell WHICH. The rendered assertion and the width assertion cannot be one assertion — `seal_stamp.letter` cuts at the frame with no marker, so a value one column too wide reads on the drawing as a shorter true statement, and the width is measured off the panel data before the frame touches it. **Re-read 2026-09-21, round 1's fix pass (finding 3), which widened the claim.** The line used to tell every unanswered step's reader that a reason for it stands in `PARTITION`, which is false for a step no row classifies — executed in a scratch repository with a `release` job of its own, where it named `deploy to staging; notify the channel` that way. The two populations are now named apart, and two cases hold both halves: one red when they are merged again, one red when the pointer to `PARTITION` is deleted from the half that has one. **Re-read 2026-09-21, round 2's fix pass**, where the Claim cell was widened to say what the Notes had been carrying alone — a Claim a re-reader audits has to reach the case that pins it. A third case renders the two clauses TOGETHER, which neither earlier fixture did: `UNCLASSIFIED_WORKFLOW` leaves the excluded half empty and this repository's workflow leaves the unknown half empty, so the joined sentence a mixed repository's reader parses was rendered by nothing. It is red when either clause learns to claim every unanswered step |
| G7 · A repository with no workflow this gate can read — absent, or present and not UTF-8 — is sealed exactly as it was: no panel row, no line, the same rows in the same order, and never a traceback | `skills/verify/scripts/broad_gate.py#workflow_text@1f3fd87c`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_repository_with_no_hygiene_workflow_is_sealed_exactly_as_before@22ad989b` | **Executed** 2026-09-21. Two mutations, each red: the panel row filled from `PARTITION` when no workflow is there, and the coverage line written from `PARTITION` for the same case | 2026-09-21 | A7 is what `questions.md` Q1's answer (a) rests on — the partition lives in the gate, and the gate ships to repositories whose CI it describes nothing of. Holding it required the gate to read the workflow of the tree it was pointed at rather than to print from the table: a repository with a differently-shaped `hygiene.yml` is then told, truthfully, that this gate answers none of its steps. The case asserts the panel's row labels against the exact set they were before the row existed, so an added row is red whatever its content. **Re-read 2026-09-21, round 1's fix pass (finding 5), which widened the claim to the shape that ENDED the run rather than changing it.** `workflow_text` promised *or None* and caught `OSError` alone; `UnicodeDecodeError` is a `ValueError` and `main` catches only `Refused`, so a workflow file that is not UTF-8 came out as a traceback — reproduced at `'utf-8' codec can't decode byte 0xe9 in position 45`. A third case now runs the gate over a latin-1 workflow and asserts the seal, no traceback and no panel row; it is red against `except OSError` alone, measured with the mutation anchored on this unit's own body after a first attempt matched an unrelated handler earlier in the file. **Re-read 2026-09-21, round 2's fix pass**: the Claim cell read *a repository with no `.github/workflows/hygiene.yml`*, which a latin-1 workflow is not, so a re-reader auditing this row never reached the case that pins the decode path. An under-claim rather than a false one, and the Claim column is the checkable half |
| G8 · A reason is prose a person wrote and names the thing out of the gate's reach; a category word is refused | `tests/test_the_gate_names_every_step_ci_runs.py#test_every_exclusion_carries_prose_rather_than_a_category@1ba9f807` | **Executed** 2026-09-21. Red against a reason cut to `EXCLUDED`, and the second case red against a reason that is prose and names nothing out of reach — *this one is not worth the trouble right now* | 2026-09-21 | Two structural rules stand in for a judgment no checker makes: several words, and not a marker. What neither catches is a category in prose clothing — *not applicable here* passes both and says as little as `EXCLUDED` — and that is written into the case rather than left for a reader to discover. The vocabulary the second case looks for is what the kinds of unreachable thing are called here, so a fifth kind is a word to add rather than a case to delete |
