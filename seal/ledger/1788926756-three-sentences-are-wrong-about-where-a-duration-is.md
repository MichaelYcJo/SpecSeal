| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A window's span runs to the last call to END, so it can never be shorter than a single call the window counts | `skills/verify/scripts/session_cost.py#analyse@c8423846`, pinned at `tests/test_session_cost.py#test_a_span_covers_every_call_it_counts@f1584e66` | **Executed.** On a background command 0-1000s beside calls at 10-12s and 990-995s the span read **995s** — five seconds shorter than the single `Bash` call the window holds — and now reads 1000s, printing `span 16.7m` where it printed 16.6m. The case was seen red against the module before the change, exit 1 on `assert 995.0 == 1000`. Three mutations one at a time, each restored from bytes the probe kept: back to `calls[-1]["end"]`, `min(c["end"])` instead of `max`, and measured from `calls[-1]["start"]` — each exit 1 | 2026-09-09 | **The old rule read the end of the last element of a list `load` sorts by START**, so the arithmetic and the sentence describing it were both about the last call to BEGIN. `command_s` and `model_s` are untouched: the sum over calls and the turn walk read the same values as before, which is what keeps every other published number comparable. **The tidy-up this row exists against is taking `command_s` to the union of the call intervals** so the share cannot pass 100% — that changes what the number MEANS, from command time to wall clock with a command running, and breaks the series `analyse`'s own docstring exists to protect. `questions.md` Q3 holds it with the owner |
| No printed figure on this machine moves under the new span rule, at run level or at row level | `skills/verify/scripts/session_cost.py#analyse@c8423846` | **Executed**, both rules computed over every transcript under `~/.claude/projects`. Run level: 169 transcripts with calls, **one span moves, by 0.006s**, printed span 10.3m either way and `command` share 0% either way. Row level, which the run-level sweep does not cover and which `reading.md` publishes: 599 spawn-cycle rows, **8 move at all** and **2 by enough to change a printed figure** (368.5m → 368.7m, 195.7m → 195.8m). **Every one of the 8 is in another project's transcript**; this project's directory has 16 transcripts and not one moving row | 2026-09-09 | **The row-level axis is the one a run-level sweep misses, and it is the one the published readings are on.** A cycle row's span comes from the same `analyse`, so a row whose last call to begin is not its last to end moves whether or not the run does — and the two printed moves prove it happens. What makes the published readings safe is not the rule being harmless but this project's transcripts having no such row, which is a measurement and not a structural guarantee. A transcript with a genuinely long-lived call inside a cycle would move, and `skills/verify/SKILL.md` says so where a person taking a reading meets it |
| The between-the-rows refusal names the cut a row ends at rather than a spawn's result, and prints the two sums rather than their difference | `skills/verify/scripts/session_cost.py#report_spawns@da507a52`, disclosed at `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@f197c507`, pinned at `tests/test_session_cost.py#test_a_head_call_outlives_the_cut_without_outliving_a_spawns_result@c86f2eb3` | **Executed.** Head call 0-8s, spawn 5-9s, calls 6-9s and 9-10s: row spans 8s + 4s + 1s against a run span of 10s, and the line printed *a call outlived a spawn's result* while no call in the transcript ends after the spawn's result at 9s. `spawn_cuts` opens its cut list at the first spawn's START (`#spawn_cuts@570c32bb`), which is why the head row's cut is not a result. The difference on that shape is three seconds, so `minutes` printed *by 0.0m* as the grounds for withholding a figure, beside a span column reading 0.1m/0.0m/0.0m. Both cases seen red against the module before the change, exit 1. Two mutations — the result named again, the difference printed again — each exit 1 | 2026-09-09 | **Neither printed figure is the rows' overlap, and after the span rule changed that holds for a new reason.** A row's span and the run's both end at the last call to end, so every row's interval sits inside the run's, and the difference is the gaps between the rows MINUS their overlap. On the pinned fixture the head row covers the whole run, so there are no gaps and the two coincide — which is exactly why naming it the overlap would be a claim that holds there and fails in general. The old grounds, that an outliving call shortened the RUN's span too, are gone with the rule. **One consequence nothing had named: the refusal can no longer fire on ONE row.** It used to — the head row's span reached 1000s against a run of 995s — and a sum past the run now requires two rows covering the same seconds |
| An exact cover reads as the partition agreeing rather than as a refusal | `skills/verify/scripts/session_cost.py#report_spawns@da507a52`, pinned at `tests/test_session_cost.py#test_an_exact_cover_reads_as_the_partition_agreeing@ee75e690` | **Executed.** Head 0-8s, spawn 5-7s, call 10-12s: run span 12s against row spans of 8s, 2s and 2s, so `outside` is 0 and the line prints *0.0m of the run's 0.2m is BETWEEN the rows — mostly the wait*. Seen red by tightening the guard to `> 0`, exit 1, which is the mutation this case exists to kill; a second mutation reworded the sentence, also exit 1 | 2026-09-09 | **The `>= 0` choice was unpinned until now, and `spec.md` cited a case that did not exist** — the only existing boundary case covers the positive branch at `outside == 304`. #145's round 3 probed the exact cover and planted nothing. Under `> 0` a run whose rows cover it exactly would print *no between-the-rows figure*, which is the one shape where a reader can watch the arithmetic work. **`mostly` also carries the other direction**: where a small overlap is netted off against larger gaps, what prints is the gaps minus the overlap, so the figure is a floor on what is between the rows and never an overstatement |
| A negative span means no call ended after the first call began, which is strictly narrower than the last call to begin having ended before it | `skills/verify/scripts/session_cost.py#report@ae4d5c2d`, pinned at `tests/test_session_cost.py#test_a_negative_span_says_what_it_actually_saw@15c2cc26` | **Executed**, and one arm of the existing case flipped rather than being re-worded. A transcript with call `a` from 10:00 to 12:00 and call `b` from 10:05 to 09:00 read **minus sixty minutes** for a run that plainly lasted two hours, and now reads `span 120.0m` with neither non-positive sentence printed. The genuinely negative arms — every call ending before the first call begins — still print, now as *no call ended after the first call began*. One mutation on the sentence's wording, exit 1 | 2026-09-09 | **The sentence says what the arithmetic computes, and the arithmetic moved.** The sibling work item settled the old wording as *exactly the proposition* the old span stated, checked by construction; that reasoning was correct about the old rule and is why the sentence had to move with it. The old sentence is still TRUE under the new rule — every call ending before the first call began implies the last to begin did — but it is no longer equivalent, and a weaker sentence beside a stronger arithmetic is how a page stops describing its own code. **The branch is also rarer on purpose**, and the flipped arm is the shape it stopped claiming |
