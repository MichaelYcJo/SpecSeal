| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| S1 · A removed released work item is told from a deleted one by the fold record in `docs/`, and nothing in the removal itself carries the distinction — both shapes are a directory present at the fork point and absent here | `skills/verify/scripts/unverified_check.py#folded_items@5de85738`, `tests/test_unverified_rows_close.py#test_a_folded_work_item_is_not_read_as_this_branchs_deletion@b41aa507` | **Executed** 2026-09-22. Both behaviour cases were red against the shipped code before the change — `1 failed` each, on the assertion that the folded directory is not reported. Green after. Three mutations each turned one case red alone: `folded_items` returning an empty set, `FOLD_MARKER` losing its line anchors, and `work_item_of` taking the file's basename instead of its directory's. **Re-read and strengthened 2026-09-22 in round 1's fix pass**, which found the line anchor insufficient twice over: a marker inside a fenced block and a marker below the top level of `docs/` both satisfied it, and each removed a directory at exit 0. The scan now goes through `blank_fences` and reads the top level only; seven fence shapes are parametrised, the shipped skill pasted into `docs/` records nothing, and both fixes were shown red by reverting them. **Round 2 found the other half of the same rule still open and it is closed here**: a marker inside a commented-out draft block satisfied the guard too, and `settle --retire` removed the directory at exit 0. A line stops being live two ways, so the scan now asks `opens_outside_a_comment` as well as `blank_fences`, and both views come out of one `comment_scan` rather than a second copy of the walk. Six comment shapes are parametrised beside the seven fence shapes, and the count in this sentence was wrong at eight before round 2 read it | 2026-09-22 | The scan is `docs/` and nothing else. Widening it to the whole tree would let a marker in a round record, a changelog fragment, or the removed directory's own files at the base excuse a removal nothing absorbed |
| S2 · `gather_changelog.py --check` passed having examined nothing once the fragment glob went empty, which is the state a fold produces; it now counts the markers `CHANGELOG.md` carries and refuses a corpus with neither | `.github/scripts/gather_changelog.py#main@cf0640aa`, `.github/scripts/gather_changelog.py#MARKER_LINE_RE@95d471a3`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_corpus_with_no_fragment_and_no_marker_is_not_a_pass@ffdce19b` | **Executed** 2026-09-22, twice. Q3's probe against the SHIPPED script over a scratch root holding a `CHANGELOG.md` with one entry and no marker and a `seal/specs/<id>/` with no fragment: `0 changelog fragments, all gathered`, exit 0, read with `echo $?` and not through a pipe. Then four cases red first and green after, and two mutations each turning one red alone: `MARKER_LINE_RE` unanchored, and the empty-corpus refusal never firing | 2026-09-22 | The read in `spec.md` §*The two readers* was right, so this row records a confirmation rather than a correction. The repair is a count and not a new population: what was wrong was that an empty glob and *everything reached the file* printed the same sentence |
| S3 · A work item's segment is the file the majority of its non-`tests/` ledger coordinates anchor in, ties broken by path order; a `tests/` anchor rolls up to the code the case pins, and an item anchored only under `tests/` or carrying no ledger row is named rather than guessed at | `skills/settle/scripts/settle.py#segment_of@3e97cac3`, `skills/settle/scripts/settle.py#coordinates@73b46b7e`, `tests/test_settle_reads_before_it_removes.py#test_the_segment_rule@d6db5aa2` | **Executed** 2026-09-22 against this repository's own corpus, which is what the rule was decided on: 97 released work items, **81 grouped into 37 segments, 16 named, 0 skipped**, one unreleased and untouched. Of the 16, fourteen wrote no ledger row and **two** are anchored only under `tests/`. Two mutations each turned one case red alone: dropping the `tests/` filter, and taking the first anchor instead of the majority | 2026-09-22 | Q2. 852 of 1,772 coordinates sit under `tests/`, and the majority rule is why that costs two named items rather than a segment called `tests` holding half the corpus: nearly every work item that writes a case also writes a row about the code the case pins. The largest segment is `round_record.py` at 13 work items and 21 of the 37 segments hold exactly one **Re-read 2026-09-22 in round 2's fix pass**, which gave `coordinates` fence tracking: a fenced example in `seal/ledger.md` used to open a section and attribute every coordinate after it to the id in the quotation. The file carries no fence today, so no figure above moves, and the reader is the `blank_fences` this module already loads rather than a third copy of the rule. |
| S4 · `settle --retire` removes the directory of a released work item only where `docs/` records its fold, and refuses one the evidence-todo guard is holding even when the marker is there | `skills/settle/scripts/settle.py#retire@5e3f09fa`, `skills/settle/scripts/settle.py#open_rows@f3779a2f`, `tests/test_settle_reads_before_it_removes.py#test_retire_refuses_an_item_the_guard_is_holding@70daf9a0` | **Executed** 2026-09-22 over a fixture root: with one item's fold recorded, that directory goes and the unfolded sibling stays; with the held item's fold recorded, the run exits 1, the directory is still there and the report names it under `kept`. Over this repository the same arm reports `nothing to retire` at exit 1 and removes nothing, read with `echo $?` and not through a pipe. Three mutations each turned one case red alone: `retire` no longer refusing a held item, `open_rows` no longer honouring `drained`, and `survey` no longer skipping a recorded fold. **Re-read and strengthened 2026-09-22 in round 1's fix pass.** Finding 4 moved the guard ahead of the fold record in `survey`, which would have emptied `retire`'s candidate list of every held item and left this refusal unreachable — so the candidate set is read from the tree (markers ∩ directories ∩ released) rather than from `survey`'s classification, and a mutation putting it back turns the case red | 2026-09-22 | The marker being the condition is what makes the ordering enforceable rather than remembered. The worst outcome the arrangement can produce is a thin policy document, which a reader can see, instead of a directory removed with nothing absorbing it, which nobody can **Re-read 2026-09-22 in round 2's fix pass.** What changed is the empty-candidate report, not the removal rule: *the fold is complete* used to print whenever `docs/` recorded any fold at all, while asserting that no marked item still had a directory — so a marked item sitting on disk and absent at the release ref was reported as finished. Three states now, each told apart by what its own sentence claims. |
| S5 · The prerequisites of a fold are not the two readers `docs/one-root-by-lifetime.md` §*The dependency rule* names: they are those two, the survivor sweep, and every check in the repository carrying a population floor over `seal/specs/` | `skills/settle/SKILL.md#"### 3. Answer every check that reads the corpus"@33612685`, `tests/test_settle_reads_before_it_removes.py#test_the_skill_names_the_floors_a_fold_has_to_answer@905c4485` | **Read**, not executed, 2026-09-22. `spec.md` §*The ticket's headline claim is false* establishes the figures — fourteen test modules reading the corpus and at least six carrying a floor, three re-opened by the framer — and nothing here re-measured them, because measuring them is what the fold branch does and no fold ran. What was executed is the case: it was red with *is not the whole list* removed from the skill. Who answers whether the six are still six is the fold's own work item | 2026-09-22 | The rule is in the skill rather than in this repository's documents because it is general: any project running this methodology accumulates checks over its own records, and a floor is exactly the shape a check takes when its author worried about a vacuous pass. `docs/one-root-by-lifetime.md` §*The dependency rule* was written when this repository had 13 work items |
