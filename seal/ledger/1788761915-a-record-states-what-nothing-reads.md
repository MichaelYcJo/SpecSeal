<!-- specs/1788761915-a-record-states-what-nothing-reads -->

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows over two tickets that share a branch. R1 and R2 are #190, split at
the line the two arms are actually different: R1 is the boundary and the
identifier arm, whose whole content is the narrowing a measurement forced; R2
is the stamp arm, which is a refactor rather than a new reader and whose real
content is the two things that must NOT carry across from the ledger. R3 is
#207.

**R2 re-points five `seal/ledger.md` rows rather than removing them.** The
anchor loop moved out of `check_ledger` into `check_text`, so five rows whose
claim is about that loop now cite the unit that holds it. Nothing was removed
and every claim is unchanged — this is `CLAUDE.md`'s one exception to the
fragment rule, for rows an edit falsifies by moving the content out from
under them. The two rows whose claim is about `check_ledger` ITSELF — a
ledger nobody can read is reported, and every ledger path a person reads goes
through `display_name` — were left where they are. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · a record of a work item that has **not shipped** may not state a compound backticked identifier nothing outside `seal/specs/` carries; the boundary is the presence of `seal/ledger/<id>.md`, and a line carrying `NAME NOT IN TREE` is not read at all | `skills/evidence-check/scripts/evidence_check.py#unshipped@6f1e7a1e`, `skills/evidence-check/scripts/evidence_check.py#record_files@38aa6967`, `skills/evidence-check/scripts/evidence_check.py#compound@ac3a4be3`, `skills/evidence-check/scripts/evidence_check.py#stated_names@4471753c`, `skills/evidence-check/scripts/evidence_check.py#tree_names@b1d41cff`, `skills/evidence-check/scripts/evidence_check.py#check_records@fb024fb6`, `tests/test_a_record_states_what_the_tree_has.py#test_this_repositorys_own_records_state_nothing_the_tree_lacks@3509a181` | **Executed** 2026-09-07. The arm was seen RED on this repository's own tree before anything was corrected, naming exactly the four occurrences of `chain_module` in work item `1788749195`'s `rounds/round-2.md` and `rounds/round-3.md` — the instance that work item's own round 3 found by reading, three weeks of records later — and green at `6de1bca` once those three lines carried the marker. 176 names read to find them, 169 after the correction. Eleven mutations of the unit, one at a time with `tests/__pycache__` cleared between: every one caught, and the binary sniff was caught only after a case was written for it | 2026-09-07 | **The narrowing is the row's real content.** A backticked name is read as a claim only when it is COMPOUND — it carries an underscore — and that was forced by measuring rather than chosen. Of the 55 distinct names that appear nowhere outside `seal/specs/`, the 19 without an underscore are a shell command, six stdlib names, an errno, an environment variable, a lint code, a probe value, five words of ordinary prose in backticks, and three verdict words a checker used to emit. Not one is a claim about a unit; all 36 compound ones are. The narrowing removes 56 of 146 occurrences and loses no true positive, which is `plan.md`'s own repair for the false positive it predicts — *narrow the pattern, do not widen the exemption* — taken up front. What it gives up is a one-word unit renamed away, and that is the cheaper mistake: the alternative asks an author to write `NAME NOT IN TREE` beside `rpartition`, which is marker noise on a real name. **The corpus excludes exactly two directories, and they are the two a work item writes about itself** — `seal/specs/` and `seal/ledger/`. `seal/ledger.md` is IN: its S15 note keeps a renamed unit's old name beside the new one on purpose, so a reader coming from an older record can follow it, and excluding the whole of `seal/` refuses five occurrences of that name in work item `1788735085`'s records, every one of them that work item narrating its own rename. The FRAGMENTS are out, and that was found by writing this work item's own rows: doing so put four names into the corpus and silenced four refusals in its own `phase-2.md`, so a work item could clear the check on its records by naming the unit in its own ledger file. The fold moves a fragment into `seal/ledger.md` at the release, which is the same moment the work item stops being live, so nothing changes hands at the boundary. **The 129 occurrences in shipped work items are history and were spot-checked, not assumed.** Six were opened at their coordinates: `header_of`, `row_baseline`, `_correction_traces` and `test_ledger_stamps_resolve` are units that existed when the record was written and have been renamed or removed since; `cmp` and `rpartition` are the false-positive class the narrowing removes |
| R2 · a `path#unit@hash` a record wrote down is resolved by the ledger's own reader, one line at a time, and graded the way a ledger row is — an anchor that does not resolve fails, drift reports | `skills/evidence-check/scripts/evidence_check.py#check_text@2328e19a`, `skills/evidence-check/scripts/evidence_check.py#stated_stamps@94ec6ba3`, `tests/test_a_record_states_what_the_tree_has.py#test_a_location_column_is_not_an_unmigrated_coordinate@ca2fc7b1`, `tests/test_a_record_states_what_the_tree_has.py#test_a_records_drift_does_not_fail_the_run_and_a_broken_anchor_does@8263fb70` | **Executed** 2026-09-07. Seven mutations of the arm, all caught. The decisive one is the alternative design — `check_ledger` over the whole record file instead of `check_text` line by line — which turns five cases red, the Location one included. The three real stamps in this tree were resolved directly to measure what the boundary is doing: the two real ones both read DRIFTED and the fixture one reads BROKEN, and all three sit in work items that have shipped, so the arm never opens them | 2026-09-07 | **Two things must not carry across from the ledger, and both were found by writing a case.** `old_format_rows` refuses `path:line` and tells the author to run the migrator, which is right for a ledger row and wrong for a record: a verdict table's `Location` column is `path:line` by design, prescribed by `templates/sdd-round.md`, and a checker calling every one of those an unmigrated coordinate would refuse the format it asks for. And **drift in a record is exit 1 the way drift in a ledger is** — a live work item's branch is editing the very units its records stamp, so failing on drift is red by construction, which is the state `.github/workflows/test.yml`'s `ledger` job already refuses in its own comment. **The arm reads line by line because the refusal has to name the line**, the way the identifier arm does; a fresh `seen` per line follows from that, since two lines stamping one unit are two claims, and the repo-wide scan cache is shared across them so a broken anchor's scan is paid once. **There are no stamps at all in the two unreleased work items** — `0 stamps read` — so this arm's proof on this tree is its cases and its mutations rather than a live occurrence |
| R3 · `round_record.py new` prints which of three states the run's bound is in as it writes the record — nothing, `one reopening remains`, or `this record ends the run` — keyed to the **earliest** earlier record whose floor row reads `no` | `skills/code-review/scripts/round_record.py#floor_and_fixes@0fcedb9d`, `skills/code-review/scripts/round_record.py#bound_line@025aa14f`, `skills/code-review/scripts/round_record.py#new@38501892`, `tests/test_the_record_is_generated.py#test_the_floor_record_is_the_earliest_and_not_the_latest@e2860f38`, `tests/test_the_record_is_generated.py#test_a_first_round_says_nothing_about_the_bound@bf05cd4f` | **Executed** 2026-09-07. Eight mutations of the two units, all caught: nothing printed at all, the latest floor record taken instead of the earliest, a floor of `yes` counting as met, an unreadable value counting as met, the fix walk never finding one, every later record counting as a fix, the reopening line printed where no record met the floor, and the capped exit dropped from the message | 2026-09-07 | **Which floor record the bound is keyed to is the whole of the design, and `plan.md` did not name it.** Keyed to the LATEST, the walk restarts at every record it stops at — because every record it stops at is itself a record that met the floor — which is the *unbounded by construction* failure `chain_check.stopping_floor` records in its own docstring for the count. The discriminating case is round 1 met the floor · round 2 closed on a fix · round 3 met the floor again: from the earliest, writing round 4 ends the run; from the latest, it reads as having a reopening left. **Three states and no fourth.** A run whose floor has not been met is the same silence as round 1 — the cap governs there and the cap is not this line's subject — and so are a record that cannot be read and a floor row outside the vocabulary, both of which `stopping_floor` already reports at the gate. A second reader inventing a sentence about them here would be #207's own failure one file over. **The bound is read from disk where `chain_check` reads from `HEAD`**, and the two are not inconsistent: `read_record` asks git because it is enforcing at a pull request, where the working tree is exactly what CI cannot see, and this line is printed to whoever just ran the command. **The message carries `chain.CAPPED_EXIT` verbatim** — a line saying the run ends and not what to do with what is still open is the wall the refusal at the gate already refuses to be |
