<!-- Rows cut the way `spec.md` cuts the tickets — by what each claim is about
rather than by the phase that wrote it. No header: every row carries its own
anchor, so there is nothing for one to declare. -->

#### The checker is wrong about itself, and nothing goes red

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Every reader of this repository's own round records lists from **HEAD** (`git ls-tree -r --name-only HEAD -- seal/specs`) and narrows the path with a `fullmatch`, never from the index and never through a git pathspec — because git's `*` crosses a slash and `ls-files` reads the index, where a record `git add`-ed and not committed is listed and has no content at HEAD | `tests/test_chain_check_at_the_pull_request.py#_real_records@6481defa`, `tests/test_the_reopening_is_one.py#_real_records@11190fe1`, `tests/test_a_finding_id_is_a_bare_integer.py#committed_records@725a4df5`, `tests/test_chain_check_at_the_pull_request.py#test_the_real_records_lister_reads_head_and_not_the_index@e5ff198e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_an_uncommitted_record_is_not_in_the_committed_corpus@4b846bb1` | **Executed** 2026-09-15, every case seen red by putting its own reader back on `git ls-files` — three mutations, three kills, one at a time with `tests/__pycache__` cleared between them and the files restored from bytes kept in memory | 2026-09-15 | The three readers are not three copies of one defect, and the difference decides what the repair buys. The two walkers take their CONTENT from HEAD, so a staged record was listed and then **silently skipped** — every per-record check answers `([], [])`. `committed_records` takes its content from the working tree, so nothing was skipped; the record was read and counted, and a function named for the committed corpus measured a population that is not it. That third one is why the first mutation run had a survivor: with its listing put back, all 47 cases in its module stayed green. Its residual is kept deliberately and stated in its docstring — content still from disk, which is the right reading for a population measurement over a tree somebody is editing and the wrong one for a check on what CI will see |
| A walk over the real records that collects failures has a **positive control**: the same loop, over a record known to be refused, asserting a failure comes back. An assertion that reads the collected list cannot tell an empty list from a loop that never collected anything | `tests/test_chain_check_at_the_pull_request.py#_record_walk@a8366d0d`, `tests/test_chain_check_at_the_pull_request.py#test_the_walk_over_the_real_records_can_fail@51ca0ff5`, `tests/test_the_reopening_is_one.py#_capped_failures@acac6292`, `tests/test_the_reopening_is_one.py#test_the_reopening_walk_over_the_real_records_can_fail@7f42d09c` | **Executed** 2026-09-15. Three mutations, each killed: `failures.extend(errors)` after `fix_surface` replaced with `pass`; the same after `stopping_floor`; and the reopening walk's own `failures.extend(...)`. The fixture for the first control carries an empty `New units` cell **and** an empty floor row, so **either** stub alone turns it red — the plan's acceptance asked only that both stubbed together do | 2026-09-15 | The extraction is what makes it the same call path: the control calls `_record_walk` rather than a copy of it, so a loop that stops collecting stops collecting for both. #142's own words are *not another assertion on the same loop*, and an assertion on `failures` is exactly that. The reopening control reuses the sequence `test_a_second_fix_closing_record_after_the_floor_is_refused` already runs through the whole script — two reopenings after a floor record, in a work item begun at `REOPEN_FROM` — run through the walk instead |
| #142's `TypeError` clause is **stale in all three readers**: the crash is absent by a conjunction rather than by one repair, and the sort the ticket names is not the sort that could raise | `tests/test_chain_check_at_the_pull_request.py#_real_records@6481defa`, `tests/test_chain_check_at_the_pull_request.py#_record_walk@a8366d0d`, `tests/test_a_finding_id_is_a_bare_integer.py#committed_records@725a4df5` | **Executed** 2026-09-15 in a `git clone --no-local` of this repository: two `round-N-draft.md` siblings committed beside a real record, then both walkers and both corpus cases run in the clone — 4 passed, exit 0, no `TypeError`. The clone and the probe were deleted | 2026-09-15 | The sort inside a lister is `sorted(p for p in out.split("\0") if p)`, a plain path-string sort that cannot raise; the keyed numeric sort is downstream in the caller, behind a `round_number is not None` filter that landed 2026-09-08 in `14c2a57` (#234) — four days after #142 was opened. The third reader never sorts numerically at all. So the clause is stale about the outcome and imprecise about the mechanism, and **no case was written for a crash that cannot happen**. This row exists so the ticket's line is answered rather than left looking unanswered |
| A verdict row whose `#` cell says it commissions nothing and whose `Verdict` cell says `open` is **refused**, because the two cells contradict each other and `Pass` would be ticked over the row. The verdict arm composes with the severity arm rather than replacing it, and a row failing both is quoted once | `skills/code-review/scripts/round_record.py#verdict_rows@1fa83cac`, `skills/code-review/scripts/round_record.py#OPEN_WORD@774a9a3b`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_row_that_commissions_nothing_cannot_read_open@98137818`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_row_failing_both_arms_is_named_once@987f4eff` | **Executed** 2026-09-15, all six shapes seen red with the arm removed — the state `1ff0a6c` left — and the both-arms case seen red with the `flagged` guard dropped. This repository's own parsed records are unmoved: the two walkers are green at this phase's boundary, 312 passed across the four modules. The corpus is **211 of 212** records that parse, measured here rather than carried — `plan.md` and `spec.md` say 176, which was round 4's count at `151792e`, before the 0.11.4 merge brought 35 more records in | 2026-09-15 | Composing is what makes it free, and the distinction is what the grounds for not reading the verdict missed: `verified` is in no vocabulary and therefore OPEN, so a VOCABULARY test would refuse every confirmation row — true of REPLACING the marker check, false of composing with it. What is matched is the one word. **What still fails, stated:** a row now takes two mistakes in two different cells to slip through — 🟢, ❓ or ⬜ on a row that IS open, and a verdict worded as something other than `open` |
| The open verdict's boundary is **a space or a comma**, spelled in `OPEN_BOUNDARY` and not borrowed from `chain.SEPARATORS`. Widening the shared constant for any of its five other readers does not widen what counts as the open verdict | `skills/code-review/scripts/round_record.py#says_open@4fd87bcd`, `skills/code-review/scripts/round_record.py#OPEN_BOUNDARY@c0d1f7dc`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_word_that_merely_begins_with_open_is_not_the_open_verdict@d9307c10`, `tests/test_a_finding_id_is_a_bare_integer.py#test_widening_the_shared_separators_does_not_widen_the_open_verdict@9fae9b6e` | **Executed** 2026-09-15, three mutations killed: equality restored (3 of 4 spellings red, the bare `open` green because equality already caught it), the boundary borrowed from `chain.SEPARATORS` (3 red), and the boundary dropped altogether (4 red) | 2026-09-15 | Both directions of the coupling are the finding, and each is half of it. Borrowing over-refused: `-` and `:` are already in the shared set, so `open-ended question` and `open: see 5` read as the open verdict and the refusal named a word the cell does not carry. And it tied what counts as open to a constant `chain_check.py`'s own prose weighs widening — the direction a boundary change alone leaves open, which is why `spec.md` gives it a scenario of its own (S5) |
| The bound on a verdict row is `VERDICT_COL`, not the header width: a numbered short row is refused at exit 2 by `new` **and** by `close`, and a four-cell row that merely lacks its `Grounds` is admitted and written short at exit 0 | `skills/code-review/scripts/round_record.py#verdict_rows@1fa83cac`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_numbered_short_row_is_refused_rather_than_raising@be415269`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_numbered_short_row_is_refused_at_close_too@d1c16a9f`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_row_missing_only_its_grounds_is_still_written_short@6d9ebc93` | **Executed** 2026-09-15, two mutations killed in opposite directions: the bound back to `NUMBER_COL` turns all three short-row cases red, and the bound widened to the header width turns the four-cell case red **and** `test_a_short_row_with_a_comment_pipe_is_not_padded_into_a_full_one` in `tests/test_the_record_is_generated.py` | 2026-09-15 | A digit in the `#` cell KEYS the row, so a short row reached four callers that index the Verdict column by position — `build`'s `words`, `close`'s `open_now`, its `already` message and its write pass — as an `IndexError` at exit 1 where the base refused it by name. The wider bound the round proposed refuses a shape nothing indexes past, and one a shipped case deliberately admits so the record shows the column the reviewer left out rather than inventing one. Refusing exactly what crashes leaves that decision standing. The `close` half was verified by probe in round 4 and carried by no case; it has one now |
| **Committed `round-N.md` records that parse — 211 of the 212 this repository carries, 2,044 verdict rows, none short — hold 123 verdict cells beginning `open`, 9 of them wider than the bare word.** Equality reached 114 of 123; the space-or-comma boundary reaches all 123; none of the 123 is in `CLOSED_WORDS`; of the 25 no-digit `#` cells the rule admits, 15 carry a verdict outside `CLOSED_WORDS` and **0** are newly refused | `skills/code-review/scripts/round_record.py#says_open@4fd87bcd`, `tests/test_a_finding_id_is_a_bare_integer.py#test_every_spelling_of_open_the_records_hold_is_refused@88db77dc` | **Executed** 2026-09-15 at `dd6af99`, one pass, one probe, deleted after: the corpus listed with `git ls-tree -r --name-only HEAD -- seal/specs` and narrowed by `routing.round_number`, each record read through `round_record.table_body` under `VERDICT_HEADER`, each verdict cell through `chain.verdict_of`, each result through `round_record.says_open`. All five figures come off that one walk | 2026-09-15 | §5: an aggregate is not a coordinate — the number can be checked while the claim it stands for cannot — so this row exists to give the figure one. **It is the sixth wrong measurement in the release that makes the method the claim.** `127 / 9 / 118` did not reproduce under any of the six populations round 4 tried, and round 4's own replacement, `95 / 7 / 88 over 1,704 rows`, was taken at `151792e` against a corpus this branch has since grown by 35 records — so re-running the reader is not the repair. What the row carries instead is the population named exactly, the date, the reader by name, and every figure of the sentence re-derived together rather than three of different ages. The two neighbouring claims reproduce exactly, which is what makes the triple one measurement |
| The overturned ruling and the borrowed boundary are **pinned by cases**, because `survivor-check` is structurally blind to both: the range removes the old wording nowhere, so there is nothing for it to match | `` `docs/review-chain-spec.md#"##### A verdict row that commissions nothing"@411f28f0` ``, `tests/test_the_rules_have_one_owner.py#test_the_verdict_ruling_is_against_a_vocabulary_test_not_against_reading@1868cc73`, `tests/test_the_rules_have_one_owner.py#test_the_open_verdicts_boundary_is_not_the_shared_separators@5dc2f95b` | **Executed** 2026-09-15, three mutations killed: the ruling put back as it stood, the boundary sentence back to *the literal word*, and the paragraph naming the constant it is NOT removed altogether | 2026-09-15 | *The verdict word cannot do this job* argues against a VOCABULARY test and was written as an argument against reading the cell at all; it survived one correction pass bolded, twenty lines under the paragraph overturning it, because nothing pinned it. The boundary sentence is the same class one round later — four live coordinates said the arm borrows `verdict_of`'s boundary while it borrowed `chain.SEPARATORS`. Both are now sentences a case reads |
| On the **last** record, `no fixes to check` is the only value `seal` accepts in `Fixes checked by`. A `round-N` is refused there before the write, because a named checker has to be a LATER round and the last record has none — `CHECKER_RE` tests the cell's shape and cannot test its position | `skills/code-review/scripts/round_record.py#seal@626b7dfa`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_a_round_n_on_the_last_record@63daa9b2`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_refusal_says_which_value_the_last_record_may_hold@44b07904`, `` `agents/sealer.md#"## The one write, and why it is yours"@8ebf209f` `` | **Executed** 2026-09-15, all four measured values seen red against the shape-only test — `round-1`, `round-9`, `round-2.md`, `ROUND-1`, each of which passed the refusal, wrote the cell, printed `round-record: sealed` and then came back non-zero from the check afterwards. Three mutations killed: the test back to shape-only, the refusal back to naming three values, and the sealer's definition back to counting two refusals | 2026-09-15 | **`seal` does not DERIVE lastness; it selects by it.** `n, path = last_record(routing, rounds)` is the line above the predicate, and a subcommand whose docstring reads *Set the LAST record's `Broad gate` cell* is not importing a fact when it uses the one it chose the file with. The rule is stated about the last record in ratified policy three times — `docs/review-handoff-protocol.md` §*The `Fixes checked by` field*, `docs/review-chain-spec.md` §*What the record carries* twice, and `skills/code-review/orchestration.md`'s three-value table. **What it gives up, stated:** the refusal's own sentence *The row holds one of three values* stops being true where it is printed, so §14 applies and the new text is pinned in the same commit; `agents/sealer.md` listed two refusals where the subcommand has raised on three since #30's round 1, and that is corrected with it. **Failure direction: blocks more, prompt budget zero** — the refusal replaces a refusal, one step earlier and without a written cell |
| `broad_gate` and `round_record` are bound by a case over the **real pair**, in both endings, so changing the word the generator prints turns a case red | `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_gate_reads_the_real_seals_two_endings_apart@c6e23bdb`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#a_record_the_chain_check_refuses_after_the_write@932ab26f` | **Executed** 2026-09-15: with `round-record: sealed` printed as `round-record: stamped`, this one case fails and the other 70 in its module pass. The route is asserted as the route inside the case, so it cannot quietly stop reaching the state it was written for | 2026-09-15 | Q3's answer, measured against the real `seal` before the case was written. Four candidates: an earlier record's `New units` emptied (reached — chosen), the last record's `Target SHA` unresolvable (reached), an earlier record's floor row emptied (reached), a stray `round-draft.md` under `rounds/` (NOT reached, exit 0). **And the route has to be UNCOMMITTED**, which is the half a case going through the gate has to get right: `broad_gate` runs `chain_check` itself, at HEAD, before it calls `seal`, and `seal` runs `chain_check --worktree` after the write. A committed refusal fails the gate's own check first and never reaches `seal` — a third ending, and not one of the two the pair is about |
| The depth-2 refusal names **the finding whose fix commit added the unit**, resolved from the commits `close` already holds rather than from the file the unit landed in. Where the range cannot resolve one, it still refuses and the message says the attribution is FILE-LEVEL and names every candidate finding rather than asserting one | `skills/code-review/scripts/round_record.py#depth_two@8c7bfa47`, `skills/code-review/scripts/round_record.py#unit_adders@fae2cccc`, `` `docs/review-chain-spec.md#"##### The depth in `New units`"@63d6ae03` ``, `tests/test_the_fixes_close_the_record.py#test_a_depth_two_refusal_names_the_finding_whose_fix_added_the_unit@3a13b3a8`, `tests/test_the_fixes_close_the_record.py#test_a_depth_two_refusal_it_cannot_attribute_says_so_and_names_every_candidate@1ef7a119` | **Executed** 2026-09-15, four mutations killed: the attribution back to the file, the second pass removed so nothing resolves an adder, the fallback stopping short of a refusal, and the owner's two sentences removed. Both cases are built on #30's measured shape — two findings in one file, each inside a different unit an earlier record names, each fix adding a unit — and the fallback case on a single-commit range | 2026-09-15 | **Q5, measured rather than assumed.** The per-commit answer is NOT derivable from what `close` holds: `measure` compares the range's two ends and never asks which commit introduced a unit, so it takes a second pass. Cost on a two-commit, one-file fix range: **128 ms against 36 ms** for the range pass, 3.5× — dominated by git subprocesses rather than by AST work, and bounded by the fix range because each pass parses only the files ONE commit touched. **Why the fallback still refuses:** it is the direction every verdict the checker cannot read takes, and `CONTRIBUTING.md` supplies the asymmetry — a wrong deny costs a prompt, a wrong allow ships a unit read by nobody. **Why it stops asserting:** the depth goes per entry, so a per-file answer is structurally unable to state what the record is required to state. **A fixture constraint that is a finding of its own** is recorded as a `# RIDER:` at `units_named_earlier`: `chain.EMPHASIS` is `[*_`]+` applied to the whole entry, so a snake_case parent is read back as one long word and the depth walk reaches no Python unit with an underscore in its name. Both cases use underscore-free parent units for that reason. Outside this work item's six tickets; the answerer is the repository owner |
| `close --round N` reaches **forward** into `round-{N+1}.md`'s `## Inherited coordinates` and brings every row inherited from round N to the word that round's verdict cell now carries, printing how many it filled. It refuses rather than guesses where the table is unreadable or a coordinate names no row, and says nothing where round N+1 does not exist | `skills/code-review/scripts/round_record.py#reach_forward@37893beb`, `skills/code-review/scripts/round_record.py#close@e9777007`, `` `templates/sdd-round.md#"## Inherited coordinates"@bb442a02` ``, `tests/test_the_fixes_close_the_record.py#test_two_records_committed_together_do_not_contradict_each_other@a82b2916`, `tests/test_the_fixes_close_the_record.py#test_the_reach_forward_says_nothing_where_the_next_round_does_not_exist@9c8c6533` | **Executed** 2026-09-15, six mutations killed: the reach removed, the second write dropped so the row is computed and not saved, an absent table read as nothing-to-do, an unknown coordinate skipped, the reach refusing where round N+1 does not exist, and the template's sentences removed | 2026-09-15 | **Why not the spawn order.** Running `close --round N` before `new --round N+1` costs no code and is not the repair: it makes correctness depend on an order nothing enforces, a run that spawns the verifying round first reproduces the defect silently, and `docs/review-chain-spec.md`'s ordering rule requires the record committed BEFORE its fixes exist — so both orders are reachable by design. **Why not drop the word from the `Why` cell**, which the template's own *coordinates carry; conclusions do not* invites: the word is what tells a fix pass what it is reopening, and rounds older than N-1 carry words that are already final and correct. **Symmetric with `reach_back`**, which already reaches the other way into round N-1's `Fixes checked by` and refuses rather than guessing — so the shape is not new to this generator, only its direction. Both refusals are built before either write |
| The forward reach's coordinate map is keyed from **every** verdict row, not only the numbered ones, and a round whose coordinates an earlier round already claimed is **silence** rather than a refusal. `inherited_rows` writes one row per `Location` cell of every row and is first-seen-wins across rounds, so both shapes are ones the generator itself writes | `skills/code-review/scripts/round_record.py#close@e9777007`, `skills/code-review/scripts/round_record.py#reach_forward@37893beb`, `` `templates/sdd-round.md#"## Inherited coordinates"@bb442a02` ``, `tests/test_the_fixes_close_the_record.py#test_a_row_that_commissions_nothing_does_not_stop_the_reach@67779ded`, `tests/test_the_fixes_close_the_record.py#test_a_round_whose_coordinates_an_earlier_round_claimed_is_not_refused@a17d66ae` | **Executed** 2026-09-15: both cases red against the generator at `53259ab` — exit 2 with *round-2.md inherits ``README.md``…* and exit 2 with *round-3.md's `## Inherited coordinates` names no row from round-2* — and green at `df404e2`. The module's 75 cases pass, and 485 across eight generator modules | 2026-09-15 | **Round 1's 🔴 1 and 🔴 2, both inside #342's own repair.** The map is built beside `words` rather than after the field writes, because `rows`' indices are the original file's and the heuristic comment may already have been inserted above the verdict table — and building it from the REWRITTEN text instead pre-empts `write_record`'s own hider guard, which turned `test_close_reads_back_the_record_it_writes` red |
| A unit whose adder the range resolves to **no candidate row** is at depth 1 and the walk says nothing. Resolving to SEVERAL candidate rows is a different state and still takes the file-level fallback: it is a resolution that cannot say which fix added the unit | `skills/code-review/scripts/round_record.py#depth_two@8c7bfa47`, `` `docs/review-chain-spec.md#"##### The depth in `New units`"@63d6ae03` ``, `tests/test_the_fixes_close_the_record.py#test_a_unit_added_by_a_fix_outside_every_earlier_unit_is_depth_one@24af71c4`, `tests/test_the_fixes_close_the_record.py#test_a_depth_two_refusal_it_cannot_attribute_says_so_and_names_every_candidate@1ef7a119` | **Executed** 2026-09-15: the new case red against the generator at `53259ab` — a depth-1 unit refused with *the range does not resolve which fix added it* — and green at `df404e2`. The multi-owner half was measured rather than read: round 1's paste-ready `if resolved` turned S12's own case red, and `if resolved and not owners` is what keeps it green | 2026-09-15 | **Round 1's 🟡 3**, and the one place the round's own repair was wrong. `unit_adders` resolves every `fixed` commit's units, including units added by findings that sit inside no unit an earlier record names; the intersection with the candidate rows is then empty and the fallback fired on a unit the depth-2 definition does not reach |
| `seal`'s last-record refusal names the subcommand rather than reading `the seal`, in both of the sentences that left the instance anonymous, and the case that pins it asserts the whole output holds no bare `the seal` | `skills/code-review/scripts/round_record.py#seal@626b7dfa`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_refusal_says_which_value_the_last_record_may_hold@44b07904`, `tests/test_one_word_one_meaning.py#test_no_instructing_document_leaves_an_instance_anonymous@981c6b9c` | **Executed** 2026-09-15: the sweep red at `9c728a8` naming `round_record.py` and green after; the moved pin red against the old literal — `assert 'the only value \|`seal\|` accepts' in …` fails when the message is put back — and `bin/test tests/test_the_seal_is_taken_once_by_the_sealer.py tests/test_one_word_one_meaning.py -q` exit 0, 86 passed | 2026-09-15 | **The message and its pin moved in one commit**, which is §14's shape rather than an exception to it: a person stopped by this refusal reads the sentence the case holds, so repairing one without the other either reverts the wording or leaves the pin asserting text nobody prints. The class was enumerated by the sweep's own list rather than site by site — `round_record.py` now holds no bare `the seal`, and the three swept files that still do are the rule's owner and two the allowlist permits. `seal/ledger.md` row S13 carries the phrase too and is not swept: it is a record rather than an instructing document, and the sweep's list is the boundary the owner chose |
