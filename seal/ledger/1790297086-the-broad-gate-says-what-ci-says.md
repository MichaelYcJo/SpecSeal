| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · where the shell is `cmd.exe`, a command name has its `/` written `\` only where the part before its first `/` (its `"` and `^` removed, one leading `@` dropped) names a directory where the row runs; an empty part is the drive's root and counts as one without asking. One decision per name covers every `/` in it, `CMD_BUILTINS` is still asked first, and every other name — `xcopy/e/i`, `findstr/s`, `timeout/t`, `ipconfig/all` — reaches `cmd.exe` as written. The scan takes the question as a predicate and does no I/O; `handed_to_shell` asks `os.path.isdir(os.path.join(root, part))`, with `None` meaning the current directory, and `run` passes its own `root`, so `compare_at_base` asks its scratch worktree | `skills/verify/scripts/broad_gate.py#command_names_backslashed@0bd3c768`, `skills/verify/scripts/broad_gate.py#handed_to_shell@90dc3bd1`, `skills/verify/scripts/broad_gate.py#run@d70c6fb0`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_switch_against_another_program_reaches_cmd_exe_as_written@474f15aa`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_name_that_starts_in_no_directory_is_handed_over_as_written@44c14123`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_scan_asks_about_a_name_and_reads_nothing_itself@2fec1a85`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_run_judges_the_directory_where_the_row_runs@0466847c` | **Executed** 2026-09-25 on macOS. The four switch rows were red against the frame commit `cf5428b1` (`xcopy\e\i`, `findstr\s`, `timeout\t`, `ipconfig\all`), and the scan there took one argument (`TypeError` for the predicate case). Each unit mutated in turn went red: every name turned (29), the empty part asked (2), `@` kept (6), quotes kept (7), carets kept (3), the decision not reset per name (7), decided at every `/` (9), a quoted `/` ignoring the decision (3), a quoted name's start not set (1), a `^`-opened name's start not set (1), `root` ignored (6), `run` not passing `root` (1). The last two start-not-set mutations survived until two rows with a quoted and an escaped name after a separator were added | 2026-09-25 | **Read, not executed:** what real `cmd.exe` does with a glued switch on a non-built-in. `questions.md` M1 holds it, answered only by CI's `windows-latest` leg |
| S1 · `templates/config.md` §*Broad gate* states the directory rule, that a glued switch on any other program reaches `cmd.exe` as written, that a blank before it works too, and both bounds — a directory made or entered earlier in the row is not seen, and a root directory named like a program makes its glued switch read as a path — and no longer names #596 as open. `handed_line` says a command name that starts in a directory has its `/` handed over as `\`, pinned verbatim | `templates/config.md#"## Broad gate"@0c8cb1a3`, `skills/verify/scripts/broad_gate.py#handed_line@8bcead5c`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_template_says_which_positions_are_rewritten@e0a74ea9`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_rewritten_row_is_kept_and_said_beside_the_row_as_written@32a0d966` | **Executed** 2026-09-25: each new template sentence cut in turn and the template case went red (7 of 7), and putting back "Telling the two apart is #596" went red too; the old `handed_line` text put back turned the kept-row case red | 2026-09-25 | |
| S1 · on `cmd.exe`, `where/q cmd` run through `run(..., shell=True)` from a root with no `where` directory is handed over as written and exits 0 | `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_switch_against_a_program_runs_on_the_real_platform@efc95139` | **Unverified** here: skipped on macOS, where the shell is not `cmd.exe` | 2026-09-25 | Only CI's `windows-latest` leg executes it (`questions.md` M1). A red there means a glued switch never ran on `cmd.exe`, and goes back through review as a finding |
