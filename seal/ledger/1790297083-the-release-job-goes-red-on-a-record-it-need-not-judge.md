| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A1 · `added_on_branch` passes `--full-history --topo-order` to its `git log --diff-filter=A`, so its first line is the LATEST add across a merge and under a skewed clock; a merge is never itself the add | `skills/code-review/scripts/chain_check.py#added_on_branch@de3ddaed`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_re_add_merged_back_from_a_side_branch_is_the_latest_add@c3ac2d4e`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_re_add_on_a_side_branch_with_an_older_clock_is_the_latest_add@40195b0a`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_added_on_a_merged_side_branch_is_read_at_its_own_add@bd88ccb2` | **Executed** 2026-09-25: the merged re-add and the older-clock cases red on the pre-phase line (exit 0, the early add taken); the older-clock case red with `--full-history` alone while the merged re-add is green there; the side-branch add case green on both and red under an added `--first-parent`, which names the merge | 2026-09-25 | #529. Two members of one class: history simplification follows one parent of a merge that matches it at the path, and the date order lists the early add first when the side's clock is behind. The test clock puts every commit in one second, which is a date tie, so the merged re-add case dates its side branch ahead to isolate the first member (`phases/phase-1.md`) |
| A2 · `restored_from(root, fork, rel)` names a commit in the merge base's history that carries `rel`'s current bytes (HEAD's, or the working tree's under `--worktree`) at `rel`, found by `git log --topo-order --find-object=<blob> <fork> -- <rel>` and taken as the last line; None when `fork` is None or git fails | `skills/code-review/scripts/chain_check.py#restored_from@3abcc872`, `tests/test_chain_check_at_the_pull_request.py#test_a_record_restored_from_the_bases_history_makes_no_reachability_claim@26b2ff42`, `tests/test_chain_check_at_the_pull_request.py#test_a_restored_record_the_pull_request_then_edits_is_its_own_claim@b05ef81e`, `tests/test_chain_check_at_the_pull_request.py#test_bytes_the_base_held_only_on_a_merged_side_line_are_found@93c3b512`, `tests/test_chain_check_at_the_pull_request.py#test_the_named_commit_is_where_the_bytes_entered_whatever_the_clock@40679114`, `tests/test_chain_check_at_the_pull_request.py#test_a_restore_not_yet_committed_is_read_from_the_working_tree@8202e656` | **Executed** 2026-09-25, one mutation at a time: the predicate answering None (six cases red), the working-tree branch reading HEAD (the uncommitted-restore case), the first line named instead of the last (three), no `--topo-order` (the clock case), and `--find-object` dropped so any commit touching the path counts (the edited-after-restore case) | 2026-09-25 | #598 instance 1. **No `--full-history`, by measurement**: every case stayed green without it, and a probe showed `--find-object` lists the commits of a side line that changed the record and changed it back before a `--no-ff` merge, which a plain path-limited `git log` simplifies away. `--topo-order` matters only where the retirement is a side line merged back and the add's clock ran ahead; in date order the line then names the retiring commit, which does not carry the bytes |
| A3 · a restored record is not the pull request's claim in the two arms keyed on its own add: `main` sets `refs` to None for a restored last record and prints that it was restored from the named commit, and `written_late` returns nothing for one after finding its fixes and before reading its add. Everything else is still read on it | `skills/code-review/scripts/chain_check.py#main@2bb97fac`, `skills/code-review/scripts/chain_check.py#written_late@29a5295d`, `tests/test_chain_check_at_the_pull_request.py#test_a_restored_record_is_still_read_for_everything_else@8921d85f`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_restored_from_the_bases_history_makes_no_claim@3e287475` | **Executed** 2026-09-25: the reachability case and the `written_late` case red on the pre-phase code (`not an ancestor of`; `this record was ADDED by <restoring commit>`). `main` not asking the predicate turns five cases red; `written_late` not asking it turns its own case red | 2026-09-25 | `questions.md` Q2: a `fork` argument, one definition, two call sites in `main`. Measured against PR #597 in framing: all four records it restored are found at `a211a5d7` (`spec.md` Scope 2) |
| A4 · in a draft pull request `checked_by` prints `Pass` beside `nobody — <why>` on the last record of a work item at or after `STRICT_FROM`, naming the verifying round and saying *Ready for review* re-runs the check and fails if the cell still says `nobody`; at ready and at an unreadable state it fails as before, and no other refusal in the row reads `strict` | `skills/code-review/scripts/chain_check.py#checked_by@7fa1deb4`, `skills/code-review/scripts/chain_check.py#main@2bb97fac`, `tests/test_the_last_rounds_fixes_are_checked.py#test_pass_beside_nobody_prints_on_a_draft_and_names_what_re_arms_it@7217e63a`, `tests/test_the_last_rounds_fixes_are_checked.py#test_pass_beside_nobody_still_fails_a_ready_pull_request@a4898234`, `tests/test_the_last_rounds_fixes_are_checked.py#test_a_draft_is_excused_the_pass_half_and_no_vocabulary_refusal@04227905` | **Executed** 2026-09-25: the draft case red on the pre-phase code. The draft arm firing at ready too turns the two ready-side cases red; `main` not passing `strict` turns the draft case red; a draft excusing every refusal in the row turns five cases red | 2026-09-25 | #598 instance 4. `round_record.py#run_check` tells the check `draft` unless `gh` says the pull request is ready, so `close` in the window now prints this notice and exits 0; `tests/test_the_fixes_close_the_record.py::test_pass_is_ticked_when_nothing_is_open_and_the_gate_is_the_flag` asserts that (`phases/phase-3.md`) |
| A5 · `skills/code-review/orchestration.md` says the window from `close` ticking `Pass` until the verifying round's record commits is not red on a draft, and that *Ready for review* fails the pull request if the cell still says `nobody` | `skills/code-review/orchestration.md#"## Orchestrator: the pull request opens before round 1, and a phase is re-run"@3fbfe924`, `tests/test_the_rules_have_one_owner.py#test_the_release_leg_is_not_red_until_the_verifying_rounds_record_commits@79d92f2d` | **Executed** 2026-09-25: the pin red with the sentence deleted | 2026-09-25 | Replaces *"It is red once more … and that window is expected"*, whose pin was renamed with it |
