| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · `seal.py` copied without `hooks/config.py` and `hooks/optin.py` beside the plugin layout exits 2 before its `import` lines, with one stderr sentence per missing file naming its path and what it is for (`HOOK_PURPOSES`), and no traceback; its module docstring's exit-code line names 2 | `skills/implement/scripts/seal.py#refuse_without_hooks@f95f693a`, `skills/implement/scripts/seal.py#HOOK_PURPOSES@0456aea5`, `tests/test_a_script_copied_alone_exits_2.py#CASES@e85ce449` | **Executed** 2026-09-26: the `seal.py` row of the class case red at `09034979` (exit 1, `ModuleNotFoundError: No module named 'config'` traceback), green after. Mutated one at a time, each red: the missing-file test forced false, the exit forced to 1, the `optin.py` purpose reworded | 2026-09-26 | #610. `mode --check` is the invocation because the load is at import and argparse's own usage error is also exit 2 (`questions.md` Q4). `seal.py` is the `sys.path.insert` + `import` shape, which a search for by-path loaders does not find |
| C2 · `payload_meter.py --calibrate <transcript>` copied without `session_cost.py` exits 2 from `_session_cost`, before the transcript is opened, with one stderr sentence naming the path and that it reads a transcript's spawns, and no traceback; the module docstring lists 2 for both causes, the interpreter floor and the missing sibling | `skills/verify/scripts/payload_meter.py#_session_cost@ec73910e`, `tests/test_a_script_copied_alone_exits_2.py#CASES@e85ce449` | **Executed** 2026-09-26: the `payload_meter.py` row of the class case red at `09034979` (exit 1, traceback), green after. Mutated, each red: the file check forced false, the exit forced to 1 | 2026-09-26 | #610. 2 and not 1, because the meter's 1 is *this input could not be measured* and its 2 already meant *nothing ran* (the floor) |
