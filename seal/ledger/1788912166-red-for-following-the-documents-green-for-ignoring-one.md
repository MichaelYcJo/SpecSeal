<!-- One work item's rows, three tickets. No header — `fold_ledger.py` writes
the `###` at the release and moves this file into `seal/ledger.md`.

G1 is #296, G2–G4 are #295, G5–G6 are #297. G4 is a row about where a
constant lives rather than about a behaviour, and it is a row because the
failure it prevents is silent: the writer would keep writing a row the reader
no longer finds, and the reader reads a missing row as `no run was named`. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| G1 · the arm counting round records reads the draft state, so a draft pull request may lack the record the rounds have not written yet; and `unknown` — no payload, an unparseable payload, one naming no pull request, one whose `draft` is a string — is judged as READY at this arm exactly as it is at `Pass`, so the excuse cannot be reached by a harness that stops writing the flag | `skills/code-review/scripts/chain_check.py#main>"which is the state a draft pull request opens in"@29864f39`, `tests/test_chain_check_at_the_pull_request.py#test_a_draft_pull_request_has_not_had_its_rounds_yet@00b57f50`, `tests/test_chain_check_at_the_pull_request.py#test_an_unknown_state_is_not_a_draft_at_this_arm_either@0e761056` | **Executed** 2026-09-09. Seen red before the fix: exit 1 on a draft, the failure being the very error the fix removes, printed under a header reading `judged as a draft pull request`. The six sibling cases were green before and after — they pin the direction the fix must not move. Mutated: `if not strict` forced to `if False` turns the draft case red alone | 2026-09-09 | The value was already computed for every run and reached `check_round` alone, a hundred lines down the same walk; the fix is that value reaching one more arm, with no new input and nothing threaded through anything. What made it worth a ticket is that `orchestration.md` documented the resulting red window as *the window's expected state, not a failure to chase* — a check red for obedience to the document beside it. That sentence is corrected in the same change |
| G2 · at a ready pull request the LAST round record's `Broad gate` cell is read, and three states are told apart: `not yet` or no row at all is the run that never happened, a SHA the record's own `Target SHA` descends from is the run spent before the round it sealed, and anything else is reported rather than failed | `skills/code-review/scripts/chain_check.py#broad_gate@5155468f`, `skills/code-review/scripts/chain_check.py#says_gate_not_yet@70eef030`, `tests/test_chain_check_at_the_pull_request.py#test_a_broad_gate_spent_before_the_round_it_was_meant_to_seal_fails@b5253337`, `tests/test_chain_check_at_the_pull_request.py#test_a_broad_gate_cell_nobody_can_parse_is_reported_rather_than_failed@7c81a567` | **Executed** 2026-09-09. Eleven cases seen red against the unfixed script, the first with `AttributeError: module has no attribute 'BROAD_GATE'` — the constant did not exist because nothing had ever read the cell. Eight mutations of this arm, all killed: the reason-after-separator path dropped, the draft excuse removed, an absent row read as nothing to check, ancestry without the equality guard, an unresolvable gate SHA treated as a real run, and an unparseable cell failed instead of reported | 2026-09-09 | **Equal is not premature**, and `merge-base --is-ancestor X X` exits 0, so an arm resting on ancestry alone fails the exactly-correct case — the round reviewed a commit and the gate ran at that commit. The resolved oids are compared first, which is also what makes an abbreviated cell and a full-length `Target SHA` comparable. Reading an ABSENT row as the same state as `not yet` is a judgment beyond the specification's letter: it is silent on the case, and reading it as *nothing to check* would make deleting one line the way past the whole arm. `round_record.py new` writes the row on every record it generates, so above the cutoff an absent row cannot arise honestly |
| G3 · the arm reaches work items begun on or after `GATE_FROM` and no others, `>=` the way the seven cutoffs before it are keyed, and a work item whose id is not a unix second is grandfathered whole | `skills/code-review/scripts/chain_check.py#GATE_FROM@b20640c7`, `tests/test_chain_check_at_the_pull_request.py#test_a_work_item_one_second_below_the_cutoff_is_not_failed_for_it@f0073b98`, `tests/test_chain_check_at_the_pull_request.py#test_a_work_item_at_the_cutoff_is_held_to_it@e1926d2a` | **Executed** 2026-09-09. The two boundary cases sit one second either side of the constant, and the test module states the number as a literal rather than importing it, so a typo in the constant cannot make every case agree with it; a third case pins the script's value against that literal. Mutated both ways: dropping the cutoff turns the below case red, and `<=` in place of `<` turns the at-the-cutoff case red | 2026-09-09 | **This cutoff is not optional in the way the seven before it were, and the difference is worth the row.** Those grandfather records that could not have met a rule which did not exist. This one grandfathers records that MET the rule and had nowhere to say so: `round_record.py` has written `not yet` into every record it ever generated. The blast radius of getting it wrong is every work item in the tree and every one in flight — one was mid-review in another checkout of the same clone while this was built. `GATE_FROM` is this work item's own id, so with `>=` the first pull request the arm applies to is this one |
| G4 · `Broad gate` and its `not yet` sentinel are defined once, in the script that READS them, and the script that writes the row imports both | `skills/code-review/scripts/chain_check.py#BROAD_GATE@b37b2aa9`, `tests/test_chain_check_at_the_pull_request.py#test_the_broad_gate_label_has_one_spelling_both_scripts_read@e664be54` | **Executed** 2026-09-09. The case asserts the quoted literal appears exactly once in the reader and never in the writer, and that the writer names the reader for both values | 2026-09-09 | They lived in the writer under a comment reading *"Field labels the checker has no constant for, because it never reads them"* — true when written, false from this change on. **An `is` comparison cannot state this claim**: the two scripts load each other as separate module objects and `"Broad gate"` is not an identifier, so the literal is not interned and identity fails between two correct copies. Equal values prove nothing either — two copies agree until one is edited, which is the whole failure. So the claim is asserted against the source text, and the direction it guards is the silent one: rename the row in the writer alone and it keeps writing a row the reader no longer finds, which G2's arm reads as *no run was named* |
| G5 · `survivors.md` takes a second row shape, `\| Range \| Grounds \|`, told apart from a per-survivor row by the first cell alone; the RANGE is the anchor, so a declaration stops holding the moment the check runs over a different range, and a row with no grounds is not a row | `skills/code-review/scripts/survivor_check.py#read_exemptions@eb97cb3a`, `skills/code-review/scripts/survivor_check.py#RANGE_CELL@4dc41537`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_whole_range_row_does_not_reach_a_different_range@e22bce12`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_path_row_and_a_range_row_are_told_apart@ed780c04` | **Executed** 2026-09-09 over a built repository whose range corrects one statement and leaves its twin standing. Six cases seen red first, and the two that passed had to: the undeclared-deletion floor, and a range row with empty grounds at exit 2 for the old reason that a two-cell row was not a row at all. Three mutations killed: the pattern widened to match anything reads a path row as a range, empty grounds accepted, and a declaration excusing any range | 2026-09-09 | A path can never match `^[^\s\|]+\.\.\.?[^\s\|]+$` because the dots need a non-space word on BOTH sides, so `../notes.md` is a path and `A..B` is a range. That is why no new file, no new flag and no header convention was needed: a two-cell range row sits inside the same three-column table, which is how it will be written. The old parser skipped two-cell rows in silence, so nothing that used to be read changes meaning. The grounds are not optional because they are the entire content of the escape — what a reviewer reads is the written sentence, and a row without one silences 153 places on the strength of nothing |
| G6 · a declared range is RESOLVED rather than compared as text, and a spec that no longer resolves silences nothing and is printed rather than refusing the run | `skills/code-review/scripts/survivor_check.py#whole_range@c7541c3e`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_whole_range_row_is_resolved_rather_than_string_matched@73729189`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_range_row_that_does_not_resolve_silences_nothing_and_says_so@72f6af26` | **Executed** 2026-09-09: a declaration spelled `HEAD^..HEAD` silences a run given the same range as two oids, and `origin/gone..HEAD` silences nothing and is named in the report. Two mutations killed: matching on the spec's text, and raising instead of collecting the unresolvable ones | 2026-09-09 | Both halves are about the same fact — a range has two honest spellings and a `survivors.md` outlives the refs its own names. CI spells the range `origin/<base>...HEAD`, which is what a session copies into the declaration; a person types two commits. Refusing an unresolvable spec at exit 2 would be the landmine: the file lives from the work item's first row until the release that ships it, the release branch its range names gets deleted, and every later range's check would then refuse over a row that has nothing to do with it |
