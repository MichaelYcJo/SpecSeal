| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · S2 · S3 · the session-start notice names `/reload-plugins` before the restart, sources the reload's reach as measured, and names hooks and agent definitions as unmeasured | `hooks/version-check.py#notice@6f2cfd5a`, `tests/test_version_check.py#test_the_warning_names_the_cheap_move_before_the_expensive_one@81b2a4de` | **Executed** 2026-09-07. The case seen red against the old text before the hook was touched (`1 failed, 16 passed`, exit 1, the failure printing the old message verbatim), green after (`17 passed`, exit 0). Six mutations of the notice, six killed: the reload unnamed, the restart named first, the reload's reach asserted without the source label, each half of the gap dropped, and the gap turned into a claim that the reload covers everything | 2026-09-07 | **The first draft of this case proved nothing and the mutation is how that surfaced.** `"measured" in msg` over the whole message was satisfied by the *gap* sentence, so the reload's own clause could drop its source label and stay green. A substring test over a multi-sentence message pins the message's vocabulary, not any of its claims; the case now splits on sentences and holds each claim to its own |
| S4 · S5 · S7 · the update procedure gives both moves with their evidence labels, names the run that would settle the open row, and says it will type neither | `skills/update/SKILL.md#"## Procedure"@bda7566e`, `skills/update/SKILL.md#"## What this does not do"@8f48c0f7`, `skills/update/SKILL.md#"## Output"@1639f87b` | Read, not run. Nothing pins this file's wording | 2026-09-07 | **The scope statement had to widen with the change or the change backfires.** *"It does not restart anything"* was about one move; a procedure that names `/reload-plugins` and never says it will not type it is offering the act the ticket's own *Not this* rules out. The reason is durable rather than incidental — a built-in CLI command is not a skill, so no procedure can invoke one |
| S6 · both READMEs carry the same three-part fact under *Updating*, English and Korean, in one commit | `README.md#"### Updating"@c4177fe0`, `README.ko.md#"### 업데이트"@bedad8a5` | Read, not run. `tests/test_docs_line_wrap.py` holds both editions to 88 display columns and passed | 2026-09-07 | The Korean paragraph is split rather than edited in place: three facts inside `적용은 재시작 후이고` would put a long insertion between a subject and its particle, which `skills/writing-style/SKILL.md` §영어를 옮길 때는 구조를 버리고 뜻만 가져온다 names as the first failure of a structure-preserving translation |
| The measurement's boundary is one axis wider than the experiment's own *What it did not establish* section says | `docs/experiments/2026-09-03-skill-preload-and-the-copy-in-force.md#"## Results"@b1d499df` | Read, not run. Run 6's row reads `version cache, flag on` with the sentinel placed in the directory of the version that was already loaded | 2026-09-07 | **Inferred during implementation.** The experiment names one gap (whether the `/` menu omits a flagged skill) and this is a second one it never claims to have covered: a re-read of the copy in force is not evidence about resolving to a *new* copy. It is the axis the ticket assumed when it wrote *"a full restart is not required"*. A planner may overturn this the moment somebody runs the experiment named in `skills/update/SKILL.md` §5 |
