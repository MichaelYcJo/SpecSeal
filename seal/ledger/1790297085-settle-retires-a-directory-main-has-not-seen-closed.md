| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| M1 · a shipped script copied without the sibling it loads by path exits 2 with one stderr sentence naming the missing path and what that file is for, and no traceback: `fold_check.py`, `settle.py` and `round_record.py` refuse in their loaders, before anything is read, and `chain_check.py` already did in `main`; `settle.py` and `round_record.py` look each sibling's purpose up by path, so `hooks/optin.py` is named as what finds the `seal/` root and not as the fold record's reader | `skills/settle/scripts/fold_check.py#load@7bfcabf5`, `skills/settle/scripts/settle.py#load@cb0e9197`, `skills/settle/scripts/settle.py#PURPOSES@cfe9eac9`, `skills/code-review/scripts/round_record.py#load@4c6d397b`, `skills/code-review/scripts/round_record.py#PURPOSES@bfe7848a`, `tests/test_a_script_copied_alone_exits_2.py#test_a_script_copied_alone_exits_2_and_names_what_it_misses@05fcace7`, `tests/test_settle_reads_before_it_removes.py#test_a_missing_sibling_reader_is_a_sentence_and_not_a_traceback@6596501d` | **Executed** 2026-09-25. The class case, four scripts each copied alone into an empty directory and run as a subprocess, was red for three of them at `1880cf92` (exit 1: two sentences and one `FileNotFoundError` traceback) and green for `chain_check.py`; green for all four at `3c1be03a`. Mutated at `3c1be03a`, each red: `fold_check.py#load`'s exit back to 1, `settle.py`'s `hooks/optin.py` purpose back to the fold record's, `settle.py#load`'s exit back to 1 (2 red), `round_record.py`'s purpose for `chain_check.py` reworded, `round_record.py#load`'s file check removed | 2026-09-25 | #590. The `# RIDER:` that stood on `round_record.py#load` since 2026-09-08 asked for exactly this and is removed. `chain_check.py` is not edited by this work item; its exit 2 is pinned by the class case alone |
| M2 · `settle` asks the rule arm's predicate, `unverified_check.py#retired_by_rule`, of the working tree and of the merge base of `--released-at` and `HEAD`, the revision both CI readers ask it of; a released spec-less directory that passes the tree and fails the base is listed under *kept until the closure reaches <base>* with every row open there, never under *retired by the rule*, and `settle --retire` keeps it at exit 1; a directory with a row open on disk stays under *kept by the rule*; a `--released-at` sharing no commit with `HEAD` is exit 2 in both arms with nothing read, and `retire` handed a survey with no base refuses at 2 by itself | `skills/settle/scripts/settle.py#survey@73b7df61`, `skills/settle/scripts/settle.py#retire@744eadbf`, `skills/settle/scripts/settle.py#main@c945557b`, `skills/settle/scripts/settle.py#write_rule_kept@df37f57d`, `skills/settle/scripts/settle.py#RULE_BASE_HEADING@918358e4`, `tests/test_settle_reads_before_it_removes.py#test_a_closure_the_base_has_not_seen_keeps_the_directory@c5f0515f`, `tests/test_settle_reads_before_it_removes.py#test_an_uncommitted_closure_keeps_the_directory@803415c6`, `tests/test_settle_reads_before_it_removes.py#test_a_closure_the_base_has_seen_lets_the_directory_go@07fc6e8c`, `tests/test_settle_reads_before_it_removes.py#test_the_report_does_not_promise_what_the_retirement_refuses@9e0cc6ea`, `tests/test_settle_reads_before_it_removes.py#test_no_merge_base_is_refused_and_nothing_is_removed@e9a09a67`, `tests/test_settle_reads_before_it_removes.py#test_the_retirement_refuses_a_survey_with_no_base@d31e7fb4` | **Executed** 2026-09-25. Against the code at `ee3afefc`: S4 and S5 red with the directory removed and exit 0, S7 red with the directory under *retired by the rule*, S8 red at exit 0 and 1, the heading pin red; S6, the counter-case, green before and after. Green at `4f77ecdc`, 118 cases in the module, 111 of them the cases that stood before (none of which encoded a working-tree-only closure as retirable, `questions.md` Q2). Mutated at `4f77ecdc`, each red: `survey` asking the tree where it asks the base, `retire` doing the same, `retire` not counting what it kept there, the report omitting the heading, `main`'s refusal off, `survey` walking on with no base, the rows' text dropped, the heading's remedy dropped; `retire`'s own no-base guard off turned its case red at `cac3341f`. `./bin/settle` on this repository at `4f77ecdc`, read and not `--retire`: exit 0, `3 to retire by the rule, 0 kept by it, 0 kept until the closure reaches origin/main` | 2026-09-25 | #602, the 0.15.3 run's #598 instance 2. The predicate is asked of the tree first, so a directory with a row still open on disk keeps its old heading and remedy. A stale `origin/main` keeps more than it must, never less, and the heading names the ref so a reader can see it |
| M3 · the skill, the evidence-ledger policy and both README editions say the rule arm is asked at the merge base of `--released-at` and `HEAD`, and that a closure has to reach the branch the release merges to before the directory goes; `RULE_KEPT_HEADING` says the same | `skills/settle/SKILL.md#"### 1. Read what is waiting"@6c708fea`, `skills/settle/scripts/settle.py#RULE_KEPT_HEADING@259eff1a`, `tests/test_settle_reads_before_it_removes.py#test_the_documents_say_the_closure_has_to_reach_the_base@05093a52` | **Executed** 2026-09-25: the case red for each of five deletions made one at a time at `4f77ecdc` — the skill's paragraph, the policy's sentence, each README's clause, the kept heading's clause — and green with all of them | 2026-09-25 | The policy statement's `Enforced by:` line names `test_a_closure_the_base_has_not_seen_keeps_the_directory` beside the rule arm's case, and `fold-check`'s real-tree case reads it |
