<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows over a branch that adds an agent definition, moves two skills and
opens a fourth routing axis. Most of what this work item claims is claimed by
a pin instead — `tests/test_every_agent_reads_the_contract.py`,
`tests/test_a_moved_rule_leaves_its_definition.py`,
`tests/test_broad_gate_rule.py` and
`tests/test_chain_hooks_hardening.py#definitions_naming` all read the
definitions out of the `agents/*.md` glob, so the fifth file is covered on the
day it lands and no row is needed to say so.

What earns a row here is the half of the mark gate that is new. `seal/ledger.md`
carries three rows about that gate and every one of them speaks for ONE axis,
which phase 4b re-read on 2026-09-11 and left narrow on purpose: each is still
true of the axis it names, and the `Planning` half is a NEW claim rather than a
widening of an old one. This is where it goes.

P3 is the judgment the two pins cannot carry — why an unreadable answer on this
axis is unanswered rather than fatal, and why the cross-file pin had to be
built differently from the third axis's. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| P1 · Spawning `framer` (`specseal:framer`, or a project-local `framer`) through the `pre-agent` group writes the checked-out branch name to `<git-dir>/specseal-planner` and prints nothing, and **a mark for one axis does not answer for the other**: the planning mark standing leaves `stands` False for the implementation mark on the same branch, and the reverse. One comparison over `AGENT_MARKS` serves both, so the qualifier is dropped with `rsplit(":", 1)[-1]` in one place and `framer-helper` is refused for the same reason `smith-helper` is | `hooks/implementer.py#mark_for@0b5b14c2`, `hooks/implementer.py#AGENT_MARKS@197da685`, `hooks/implementer.py#PLANNING_MARK@d2ce0baa`, `hooks/implementer-mark.py#main@3ab7a0f2`, `tests/test_the_implementer_is_recorded.py#test_spawning_the_framer_leaves_the_planning_mark@8b423c1e`, `tests/test_the_implementer_is_recorded.py#test_a_mark_for_one_axis_does_not_answer_for_the_other@696ee877`, `tests/test_the_implementer_is_recorded.py#test_spawning_any_other_agent_leaves_none@d9adc84e` | **Executed** 2026-09-11 in phase 4b, each case shown red first and mutation-tested one at a time; **re-executed** 2026-09-11 in phase 6, 72 cases green across the three modules that read this axis | 2026-09-11 | **Why a second constant and not a second module.** `hooks/implementer.py`'s own docstring says it is named for the AXIS rather than for the agent, and a mark written by one hook and read by another in two spellings is a mark that is written and never found — which is its whole reason for being a module. `plan.md`'s Alternatives table records the rejection, with contract §11 and §16 as the precedent: a rule that sat in two definitions in near-identical words while a third carried none. **The file name under the git dir is what finds an existing mark**, so `specseal-implementer` did not change when the constant holding it became `IMPLEMENTATION_MARK`. This row does not restate the smith half — `seal/ledger.md` carries it, narrow and still true |
| P2 · After a command that invokes `git commit`, where this branch's declaration answers `Planning` with `framer` and no planning mark stands, one line names `seal/specs/<item>/routing.md`. Where **both** axes are unfulfilled the session is told **once, in one line naming both**, never one line per axis; where one axis's mark stands and the other's does not, the line speaks only for the one that is missing. Nothing is said for an axis answering `the session`, whose row is absent, or whose row is unreadable. The once-per-repository-per-session grain is the existing marker and did not change, and the notice still carries no permission decision | `hooks/implementer-notice.py#unfulfilled@16fd0e9a`, `hooks/implementer-notice.py#line@1ef727af`, `hooks/implementer-notice.py#AXES@8b80f2ae`, `hooks/implementer-notice.py#main@3b7473f2`, `tests/test_the_implementer_is_recorded.py#test_a_declared_framer_with_no_mark_is_noticed_after_a_commit@ddd5d4b5`, `tests/test_the_implementer_is_recorded.py#test_two_unfulfilled_axes_produce_one_line_not_two@e7929bb2`, `tests/test_the_implementer_is_recorded.py#test_a_mark_for_one_axis_does_not_silence_the_other_at_the_notice@6b16f73f`, `tests/test_the_implementer_is_recorded.py#test_with_the_planning_mark_present_nothing_is_said@e865ad2e` | **Executed** 2026-09-11 in phase 4b through `dispatch.py post-bash`, each case shown red first; **re-executed** 2026-09-11 in phase 6 with the modules above | 2026-09-11 | **One line for two axes is the claim, and the reason is the budget rather than the wording.** A notice is spent against this project's first goal every time it reaches a person, so two axes arriving as two lines would double the cost of a session that forgot both answers at once — which is exactly the session most likely to forget. The singular wording is byte-identical to what 0.7.0 shipped, because it is what nearly every notice will say; the plural branch is the new one. The sentence is in `line()` rather than inline in `main` so that one unit holds both forms. **`seal/ledger.md`'s notice row still reads `smith` alone** and is still true of the axis it names; phase 4b's 2026-09-11 re-read note there says this half lives here |
| P3 · The fourth axis is read on the third axis's terms: `parse()` returns `planning` beside `implementation`, absent reads as unanswered, and an answer outside the vocabulary reads as unanswered **rather than as *this file is not a declaration***. Every declaration already committed in this repository parses unchanged and reads `planning: None`, and the commit gate's output is byte-identical with the row and without it. `table_rows()` needed no change, which is the promise its own docstring made | `hooks/routing.py#parse@556b3824`, `hooks/routing.py#PLANNING_ANSWERS@86500d55`, `hooks/routing.py#BY_FRAMER@b06622cf`, `tests/test_routing_is_recorded.py#test_a_declaration_without_a_planning_row_is_still_a_declaration@8d72cac4`, `tests/test_routing_is_recorded.py#test_the_commit_gate_decides_the_same_with_the_planning_row_and_without@ba5b537b`, `tests/test_waiver_decided_at_start.py#test_the_template_PARSES_into_the_FOURTH_axis_it_ships@5904916f` | **Executed** 2026-09-11 in phase 4, over every declaration in the tree and a case per answer. Two of the cases could not be shown red by the code alone and are recorded that way: the gate-decides-the-same case already held before the axis existed, and was shown red by making `parse()` reject an unreadable planning answer; the every-declaration case asserts a premise rather than a behaviour, and was shown red by adding a `Planning` row to this work item's own declaration and restoring it. **Re-executed** 2026-09-11 in phase 6 | 2026-09-11 | **Why a typo here must not take the declaration down.** Nothing decides a commit on this axis, so reading the whole file as *not a declaration* would re-ask the review question at every commit of a branch whose review answer is committed — a stop, for a misspelling in a row that is a record. **The cross-file pin had to be built differently from the third axis's.** Substituting the constants into the template's own row is self-consistent by construction: rename `BY_FRAMER` and the substitution renames with it, so the case stays green while the template goes on offering a word the parser no longer accepts — measured, not argued. The fourth axis's case instead READS the two answers out of the comment a person follows and compares them to `PLANNING_ANSWERS`, which is red from either side, and both directions were mutated. **The third axis's own case still has the gap**; widening it is a sweep this work item has no row for, and `seal/follow-up.md` carries it for the repository owner |
