## Who asks the routing question, and what checks the answer

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The framer's mark is read at the **foot** of `spec.md` and never by a search across it. A spec that DOCUMENTS the mark carries it three times — quoted in a fenced block, named in a table of who writes what, and stated as an acceptance row — so a search anywhere in the file passes the one work item in this tree that has never been framed by anybody | `skills/code-review/scripts/chain_check.py#frame_mark@95039234`, `tests/test_chain_check_at_the_pull_request.py#test_a_mark_that_is_only_QUOTED_does_not_count@21f7139f` | **Executed** 2026-09-16 before the arm was written: `grep -rln "^Framed .*, before the build\.$" seal/specs/*/spec.md` matched exactly one file, this work item's own `spec.md`, which carries no mark. The arm reads the last non-empty line, so all three quoting shapes are refused | 2026-09-16 | This is the third work item of the release about checks that could not fail, and the instance was already in the tree when the arm was designed. The template's line is the same text, which is why an unfilled `<date>`/`<who>` is refused separately |
| `item_began` answers **None** for a path one level shallower than a round record, and None is the GRANDFATHERED answer — so a cutoff keyed on it is off, silently, for the arm that needs it | `skills/code-review/scripts/chain_check.py#item_began_at@39742e68`, `skills/code-review/scripts/chain_check.py#item_began@f1539c2d` | **Executed** 2026-09-16: the first run of the `not yet` case on a work item begun at 1789518345 exited 0 and its notice quoted `1788912166`, the chain path's constant. `item_began_at` is the rule one level up and `item_began` delegates to it | 2026-09-16 | The sentinel for "derive it yourself" is `False`, not `None`, because `None` is a legal value that MEANS grandfathered — a repository naming its work items some other way has no date to compare |
| `chain_check.broad_gate` needs **no second reader** for a `broad-gate.md`. The only thing it asks of the record around the cell is `Target SHA`, which that file does not carry, so the premature comparison makes no claim — and that is the right answer rather than a gap tolerated: with no round there is no reviewed commit for a run to have been spent before | `skills/code-review/scripts/chain_check.py#broad_gate@e22933ea`, `skills/code-review/scripts/chain_check.py#direct_seal@65d1ae0d` | **Read** 2026-09-16, then **executed** through `test_a_direct_declaration_with_a_seal_passes` and `test_a_direct_declaration_whose_seal_never_ran_fails`. What the one reader cannot answer is the file being ABSENT, because `read_record` returning None is its "no claim" value | 2026-09-16 | `questions.md` Q4. No field forced a split, so one cell has one spelling |
| The `CLAUDE.md` block's one source is `templates/claude-md-block.md`, not the repository's own `CLAUDE.md`, which carries a generated copy | `.github/scripts/claude_block.py#<module>@00000000` | **Executed** 2026-09-16: `claude_block.py --write` then `--check` exits 0. Editing `CLAUDE.md` directly is overwritten by the next regeneration and ships the old paragraph to every installer | 2026-09-16 | #292 measured the two copies and found `## Git` 95 % identical with one sentence moved in one and not the other. A correction handed to this work item said the source was `CLAUDE.md`; it is not |
| The word `automation` enters this repository's machine-read vocabulary with **no collision**: every occurrence in the tree is the ordinary English noun | `hooks/routing.py#AUTOMATION@5b2bfac6`, `tests/test_routing_is_recorded.py#test_the_row_name_and_the_option_label_are_one_word_two_cases@0e774d5d` | **Executed** 2026-09-16: 40 occurrences, all of the form *this repository's own automation* or *costs no automation anything*, none a named concept. `tests/test_one_word_one_meaning.py` pins six words and this is not one of them | 2026-09-16 | The row name `Automation` and question 1's option label `automation` are deliberately one word. The two vocabularies must not cross, and both crossings read as unanswered rather than as a confident wrong answer |
| The design gate had a **second copy** in `agents/smith.md`, sixty lines below the span `plan.md` located it at, and a removal keyed on that span left the file saying *phase 2 is your caller's spawn* and *wait for an explicit go* in one section | `` `agents/smith.md#"## Phases"@61d693d7` ``, `tests/test_a_moved_rule_leaves_its_definition.py#test_the_smith_claims_no_act_another_definition_owns@9e5d6595` | **Read** 2026-09-16 at the phase boundary. The rung CONDITION stays — `test_the_top_rung_names_behaviour_rather_than_a_count` requires `observable behaviour` in this file — and only the asking clause moved | 2026-09-16 | The span was named by content on purpose, because the waiver example and its `# RIDER:` sit inside the same line range and are a kept application. A line-keyed removal takes both |
| The assertion that pinned the declaration's path inside `agents/smith.md` went with the act. That file no longer names `seal/specs/<work-item-id>/routing.md` at all, because the write is the framer's — so the claim *every assertion that pinned the path by substring names the full path* keeps four of its five coordinates and loses the fifth | `tests/test_waiver_decided_at_start.py#test_the_smith_carries_its_own_half_and_not_the_questions@a0da734a`, `agents/framer.md#"## The four writes, and why they are yours"@8838d537` | **Executed** 2026-09-16: the renamed case requires the ABSENCE of `three axes`, `two axes` and `three checkboxes` in `agents/smith.md`, and goes red when the design-gate paragraph is pasted back. The path assertion now lives where the act does | 2026-09-16 | The shared ledger's row kept its other four coordinates rather than being removed whole: one anchor of five went, and the claim still holds for the rest. `seal/ledger.md` carries the correction |
