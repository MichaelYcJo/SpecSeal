| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A spawn is an `Agent` `tool_use` block whose `input` carries `subagent_type` and `description`, and a cycle is bounded by when its REPORT arrived rather than by when the spawn went out | `skills/verify/scripts/session_cost.py#spawn_cuts@a3d80e4f`, reading the labels captured at `skills/verify/scripts/session_cost.py#spawn_labels@daa3eba6` | **Executed**, on this project's own transcripts and then by mutation. 132 such blocks across the transcripts here, every one carrying both fields; sorting the spawns by `start` instead of `end` swaps the two names on a batch whose first-sent agent reported last, which `test_a_batch_of_two_spawns_is_bounded_by_when_each_report_arrived` catches and nothing else in the module can | 2026-09-09 | The handoff for this work item said 131 and the count read 132 — this session's own spawn. An aggregate is not a coordinate: the number is checkable and the claim behind it is not, which is why the shape is pinned at the coordinate and not by the total. `plan.md` answers a renamed spawn tool with disclosure rather than with a second signal, so `DELEGATING` matches the name alone |
| Every paired call in the transcript lands in exactly one row: the head before the first spawn, one row per cycle, the tail after the last report | `skills/verify/scripts/session_cost.py#in_windows@a74f65bd`, over the cuts from `skills/verify/scripts/session_cost.py#spawn_cycles@6ff1b124` | **Executed**, by mutation. Assignment is by one instant per item — a call's `start` — so `bisect_right` → `bisect_left` moves the spawn call sitting exactly on a cut out of its cycle and into the head, and three cases go red. The sum over the rows equals the run's own call count on the fixture and on a transcript whose report is stamped before its call | 2026-09-09 | A row with no call keeps its place with `numbers` at null. Dropping it would leave the rows not partitioning the run, which is the one property the sum rests on, and an empty head is a real reading — the run's first act was a spawn |
| The `Agent` call's own interval is absent from the cycle's model time and command time, and present as `delegated_s` | `skills/verify/scripts/session_cost.py#analyse@e52dee1b`, switched on by `measure_cycles` at `skills/verify/scripts/session_cost.py#measure_cycles@8194a30c` | **Executed**, on a fabricated 1200-second spawn. `delegated_s` is 1200, `command_s` is the 4 seconds of the two real checks, and `model_s` is 16 — the 7s and 9s gaps either side of the spawn, neither swallowed. The call stays in the walk that bounds those gaps, so removing it from the list instead collapses them into one and reads 7 | 2026-09-09 | Three wrong implementations are separated by that one case: charging the interval gives 1266 or 1204, dropping the call gives 7. The exclusion is an argument the whole-run path does not pass, so the plain reading is unchanged value for value — `command_s` still 1827 on the same fixture, with `delegated_s` 0.0 meaning nothing was removed |
| Where no spawn is found the count and the transcript path are printed and no cycle table is | `skills/verify/scripts/session_cost.py#report_spawns@b4355caf` | **Executed**, both arms. A spawn-free transcript prints `0 spawns found in <path>` with no table header; replacing that line with a bare `no spawns` turns `test_a_run_with_no_spawn_names_the_count_rather_than_printing_a_table` red | 2026-09-09 | #200's failure shape, repaid the way #200 was. An empty table reads as a run that spawned nothing, and a run that DID spawn reads the same way the moment a harness stops writing a spawn as an `Agent` block |
| The orchestrator's boundary is stated where a session measuring a segment reads it, not only in this work item | `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@3ea77257` | **Read**, and the surrounding cases run. The paragraph sits inside step 1, beside the resumed-agent split it parallels, and says the boundary is not a user line | 2026-09-09 | It says **spawn cycle** rather than `cycle`, because `docs/review-chain-spec.md` owns that word for the mark's own unit and `tests/test_one_word_one_meaning.py` is the file that keeps one word to one meaning. It also names no issue number: the skill ships, and `test_the_shipped_skill_names_no_repository_specific_tracker_state` refuses this repository's own tracker state in it — which is what caught the first draft |
