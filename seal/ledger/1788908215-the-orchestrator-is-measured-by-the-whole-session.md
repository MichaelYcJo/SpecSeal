| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A spawn is an `Agent` `tool_use` block whose `input` carries `subagent_type` and `description`, and a cycle is bounded by when the spawn call's RESULT arrived rather than by when the call went out | `skills/verify/scripts/session_cost.py#spawn_cuts@570c32bb`, reading the labels captured at `skills/verify/scripts/session_cost.py#spawn_labels@daa3eba6` | **Executed**, on this project's own transcripts and then by mutation. 132 such blocks across the transcripts here, every one carrying both fields; sorting the spawns by `start` instead of `end` swaps the two names on a batch whose first-sent agent reported last, which `test_a_batch_of_two_spawns_is_bounded_by_when_each_report_arrived` catches and nothing else in the module can | 2026-09-09 | The handoff for this work item said 131 and the count read 132 — this session's own spawn. An aggregate is not a coordinate: the number is checkable and the claim behind it is not, which is why the shape is pinned at the coordinate and not by the total. `plan.md` answers a renamed spawn tool with disclosure rather than with a second signal, so `DELEGATING` matches the name alone |
| Every paired call in the transcript lands in exactly one row: the head before the first spawn, one row per cycle, the tail after the last spawn's result | `skills/verify/scripts/session_cost.py#in_windows@a74f65bd`, over the cuts from `skills/verify/scripts/session_cost.py#spawn_cycles@c449f364` | **Executed**, by mutation. Assignment is by one instant per item — a call's `start` — so `bisect_right` → `bisect_left` moves the spawn call sitting exactly on a cut out of its cycle and into the head, and three cases go red. The sum over the rows equals the run's own call count on the fixture and on a transcript whose spawn result is stamped before its call | 2026-09-09 | A row with no call keeps its place with `numbers` at null. Dropping it would leave the rows not partitioning the run, which is the one property the sum rests on, and an empty head is a real reading — the run's first act was a spawn |
| The `Agent` call's own interval is absent from the cycle's model time and command time, and present as `delegated_s` | `skills/verify/scripts/session_cost.py#analyse@e52dee1b`, switched on by `measure_cycles` at `skills/verify/scripts/session_cost.py#measure_cycles@7b49dfca` | **Executed**, on a fabricated 1200-second spawn. `delegated_s` is 1200, `command_s` is the 4 seconds of the two real checks, and `model_s` is 16 — the 7s and 9s gaps either side of the spawn, neither swallowed. The call stays in the walk that bounds those gaps, so removing it from the list instead collapses them into one and reads 7 | 2026-09-09 | Three wrong implementations are separated by that one case: charging the interval gives 1266 or 1204, dropping the call gives 7. The exclusion is an argument the whole-run path does not pass, so the plain reading is unchanged value for value — `command_s` still 1827 on the same fixture, with `delegated_s` 0.0 meaning nothing was removed |
| Where no spawn is found the count and the transcript path are printed and no cycle table is | `skills/verify/scripts/session_cost.py#report_spawns@0ff5ad93` | **Executed**, both arms. A spawn-free transcript prints `0 spawns found in <path>` with no table header; replacing that line with a bare `no spawns` turns `test_a_run_with_no_spawn_names_the_count_rather_than_printing_a_table` red | 2026-09-09 | #200's failure shape, repaid the way #200 was. An empty table reads as a run that spawned nothing, and a run that DID spawn reads the same way the moment a harness stops writing a spawn as an `Agent` block |
| The orchestrator's boundary is stated where a session measuring a segment reads it, not only in this work item | `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@1b459b88` | **Read**, and the surrounding cases run. The paragraph sits inside step 1, beside the resumed-agent split it parallels, and says the boundary is not a user line | 2026-09-09 | It says **spawn cycle** rather than `cycle`, because `docs/review-chain-spec.md` owns that word for the mark's own unit and `tests/test_one_word_one_meaning.py` is the file that keeps one word to one meaning. It also names no issue number: the skill ships, and `test_the_shipped_skill_names_no_repository_specific_tracker_state` refuses this repository's own tracker state in it — which is what caught the first draft |
| The `Agent` call's own interval is the delegated wall clock only where the harness writes its result at completion. On the harness measured here it is written when the spawn is ACCEPTED, so the agent's run falls BETWEEN the cycle that spawned it and the row after, in no column of either | `skills/verify/scripts/session_cost.py#DELEGATING@18f7c732`, disclosed at `skills/verify/scripts/session_cost.py#report_spawns@0ff5ad93` | **Executed**, over three orchestrator transcripts of the 0.9.x line. An `Agent` call pairs in 1.5-3.7s across 67 spawns; each subagent transcript's FIRST stamp equals its spawn's result stamp within one second in 61 of the 67, the six misses being subagents of subagents, which have no call in the main transcript. Subagent spans median about 1,000s. The wait then shows in NO column of any cycle row -- 351s, 768s and 962s are the three largest opening gaps of one 18-gap run, at cycle 10, cycle 14 and the tail, and `model_s` holds none of them. 17 of that run's 18 are under the 900s ceiling and total 1,507s. `analyse` starts a window's `span_s` at its own first call (`calls[-1]["end"] - calls[0]["start"]`) and its model walk adds no gap before that call, so the interval between one row's last call and the next row's first is in no column at all: 12-31% of a run's wall clock over the three runs, of which the wait after a spawn's result is 98% | 2026-09-09 | **This contradicts `plan.md`, which defines a spawn's result arriving as *the report*.** The exclusion built to `plan.md` is therefore right and nearly free here rather than wrong, and the double count #145 set out to remove is in no column of any row -- it is the interval between two of them. Moving `delegated_s` onto the wait is a decision about what it measures and is `questions.md` Q4, not a defect in what it measures now. The join a future answer needs is already exact and is the row's own measurement: a subagent transcript's first stamp IS its spawn's result stamp |
| The rows partition the run's CALLS and not its time, and the report prints how much of the run is between them | `skills/verify/scripts/session_cost.py#report_spawns@0ff5ad93` | **Executed**, over the three orchestrator runs of the 0.9.x line. The sum of the rows' spans is short of the run's span by 167.6m of 546.2m (30.7%), 53.7m of 448.0m (12.0%) and 41.9m of 164.0m (25.6%). The opening gaps -- each row's cut to its own first call -- are 167.1m, 53.1m and 41.2m of those, so the wait after a spawn's result is 98% of the interval and the remainder is each row's last call to the next cut. The printed line reads `41.9m of the run's 164.0m is BETWEEN the rows` on the third of them | 2026-09-09 | The call tally beside it cannot disagree -- both sides come from one list through `in_windows`, which assigns by construction -- so the time figure is the one that carries information. `test_the_delegated_wait_is_in_no_column_of_any_row` pins both parts on a synthetic run whose 304s outside is 300s of wait plus 4s of the head's own tail, which is why the printed line says MOSTLY the wait |
| A cycle row whose span exceeds its own parts by an hour is measuring the orchestrator's own idle time, never the agent it spawned | `skills/verify/scripts/session_cost.py#analyse@e52dee1b` | **Executed**, on the row `reading.md` publishes as the top of the smith span range: span 6,979s (116.3m) against 11.5m of command and model, one internal gap of 6,285s (104.8m) before a `cd` shell call, and a delegated wait of 580s -- under the 900s ceiling. Every row over 5,000s in the three runs decomposes the same way: four such rows, opening gaps of 6s to 580s, internal gaps of 1,038s to 6,285s | 2026-09-09 | The 900s ceiling in the model walk is what drops the internal gap, so the residue is span minus parts and it belongs to the orchestrator. Round 1 found four pages crediting this row to a delegated wait passing the ceiling, which would have made a later reading's smaller *in no column* share read as the delegated question having been answered |
