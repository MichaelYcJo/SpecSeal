<!-- specs/1790134781-a-label-description-past-100-characters-fails-every-release -->

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

One row, because the work item makes one new claim. The existing row it also
touched, `seal/ledger.md`'s T2, was re-read rather than replaced: its claim —
the reconcile creates a declared label carrying the document's own words —
still holds, so it carries a `Re-read` marker there and no row here. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · every label description this repository sends to `gh label create` fits GitHub's cap, which is named once as `LABEL_DESCRIPTION_LIMIT` with where the number comes from, and a case holds each description under it — the declared labels one by one, and the merged-label description at a version far longer than any release will carry | `.github/scripts/label_merged_on_release_branch.py#LABEL_DESCRIPTION_LIMIT@2ec877be`, `.github/scripts/label_merged_on_release_branch.py#label_description@134b3579`, `.github/scripts/tracker_labels.py#LABELS@a6833af9`, `tests/test_a_declared_label_reaches_the_tracker.py#test_every_declared_description_fits_the_trackers_cap@061f30bd`, `tests/test_a_merged_ticket_says_so_on_the_tracker.py#test_the_label_description_fits_the_trackers_cap_for_a_long_version@9a3cb393` | **Executed** 2026-09-23. The declared-labels case was red on the 119-character description before it was shortened (`[('size: now', 119)]`) and green at 95. Each unit was then broken on its own: the 119-character value back reds the cap case and the A9 case; the limit tightened to 50 reds both cap cases, which is what shows they read the constant rather than a literal; the merged-label template grown past 100 at the long version reds its case alone; and the removal half dropped from the description reds A9 alone. **Unverified here**: the cap's value. It is GitHub's, quoted from the 422 in #515's body (*description is too long (maximum is 100 characters)*); the run that body cites, `35796513013`, returns 404 on this repository (it ran before the 2026-09-23 migration, as far as this work item can tell), so nobody in this work item opened the log | 2026-09-23 | **The number is not this repository's to raise.** A description that does not fit is shortened; bumping the constant turns the suite green and moves the 422 back to the workflow after the merge, where it skipped the step behind it — the flow-measurement roll — and nobody was looking. The merged-label description is the same class because it goes to the same call, and it had 20 characters to spare at `9999.9999.9999` when this was written; pinning it costs one case and removes the question of whether a longer template would still fit |
