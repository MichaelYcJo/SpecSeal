| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| T1 · `reader()` has FIVE arms — `spec is None`, `spec.loader is None`, and an except tuple of three — of which FOUR are reachable and each takes a different input, and each reachable one has a parameter of its own. The fifth, `spec.loader is None`, has no constructible input and is defence in depth | `hooks/review-history-guard.py#reader@20048595`, `tests/test_chain_hooks.py#test_the_guard_falls_back_to_the_raw_text_without_the_reader@f0ad0bf1` | **Executed** 2026-09-08 at `8aedf11`: 29 passed. Both new parameters seen red on their own arm — deleting `SyntaxError` from the except tuple fails `[a reader that does not parse]` alone, deleting `ImportError` fails `[a reader whose own import is missing]` alone, each leaving the other 28 green. `spec.loader is None` executed against nine inputs: a directory, a `.txt`, an extensionless file, an empty string and a missing `.txt` all return `spec is None`; a `.py`, a `.pyc`, a `.so`, a missing `.py` and a DIRECTORY named `x.py` all return a spec with a real loader. Nothing makes `spec` truthy while its loader is falsy | 2026-09-08 | **The arms were enumerated out of the function's own source, and that is what found the second one.** #209 named `SyntaxError` and tabled four arms; walking `reader`'s AST for `ExceptHandler` and `If` nodes gives five, because the except tuple has THREE members and every prose reading of it had counted two. `ImportError` is reached by a reader that parses and imports something this interpreter does not have — the same truncated copy of the plugin as `SyntaxError`, one line further in — and no case, no ledger row and no ticket had named it. Without its arm the input raises `ModuleNotFoundError` out of `reader()`, through `is_closed`, into the hook's `main()`, which is the one thing R5's own argument says must never happen. **The honest close differs by constructibility**: an arm with a reachable input gets a parameter, an arm with none gets a sentence in the docstring saying so and naming the nine inputs that proved it. Writing a case for `spec.loader is None` would mean reaching past `spec_from_file_location` to build a spec by hand, which pins the test's own construction rather than the hook's behaviour |
| T2 · what a closing word can be hidden by is **derived** from `readable`'s own source and never restated: the parametrization of the hidden-closing-word case is compared with the passes `readable` composes, so a pass added to the reader fails the case rather than arriving unguarded | `skills/verify/scripts/unverified_check.py#readable@5ef11fb3`, `tests/test_chain_hooks.py#reader_blanking_passes@4f7be63f`, `tests/test_chain_hooks.py#HIDDEN_CLOSING_WORD@f9440b6e`, `tests/test_chain_hooks.py#test_a_closing_word_a_reader_blanks_is_not_a_closing_note@64444658` | **Executed** 2026-09-08 at `8aedf11`. The mutation is the deliverable: a third blanking pass added to `readable` — inline code spans blanked — turns both parameters red with the tie in place, and leaves **29 passed, exit 0** with the tie assertion deleted and nothing else changed. That control is #210's finding reproduced in this tree | 2026-09-08 | This is `seal/ledger.md` F1's standard applied one module over — *the parametrized case reads its own parametrization back out of the constant, so the case list cannot fall behind it* — and F1's Notes name the failure this removes: *nothing goes red when it does, so the silence looks exactly like correctness*. The constant is keyed by the pass's own NAME rather than by prose, which is what makes the comparison possible at all; the prose the keys used to carry (*a fenced block*, *an HTML comment*) is in the comment above them. **The derivation is by name and by definition**: the passes are the calls `readable` makes to `ast.Name` targets that are functions of the reader module, so `text.splitlines()` drops out as an attribute call and a builtin like `list` drops out as having no function on the module. **The tidy-up is deleting the tie as a tautology** — it compares a dict in the test file with something read out of another module, which reads as testing the copy. It is the only thing tying the case list to the reader, and without it the reader can grow a pass while the parametrization does not, which is exactly the state #210 was filed about. **The failure direction is safe either way**: a further blanking pass can only make `is_closed` return `False` more often, which is the reminder firing rather than staying silent, so this is case-strength and not a defect |
| T3 · `is_closed` with no records at all answers closed, so a work item whose `rounds/` directory holds no record is not read as one whose rows were never drained | `hooks/review-history-guard.py#is_closed@24b27511`, `tests/test_chain_hooks.py#test_no_records_at_all_is_not_an_unclosed_directory@99c904e6` | **Executed** 2026-09-08 at `57e9603`: 30 passed. Seen red on its own arm — mutating `if not records: return True` to `return False` fails this case alone, 29 green. The same mutation before the case existed left all 29 green | 2026-09-08 | **Neither ticket named this arm; enumerating `is_closed` from its source did.** Its four arms were mutated one at a time: `except OSError` kills four cases, `if seen is not None` kills four, `CLOSED_RE` kills two, and `if not records` killed nothing. It is unreachable from `main()`, which calls `is_closed` only behind `if records` — so it is defence in depth like T1's fifth arm. It parts from that one on constructibility: `is_closed([])` is a one-line call, and where an arm can be reached directly the honest close is a case rather than a sentence. **The direction is why it is worth pinning.** Mutated to `return False`, the pre-merge reminder fires at every merge for a work item with an empty `rounds/` directory — the state most work items are in, and the state this hook's own docstring says must stay quiet |
