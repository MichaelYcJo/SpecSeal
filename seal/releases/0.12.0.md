## 0.12.0 — 2026-09-17

<!-- specs/1789445605-the-broad-gate-row-runs-unchecked-and-is-never-asked-for -->
### 1789445605-the-broad-gate-row-runs-unchecked-and-is-never-asked-for

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Seven rows over a branch that adds one refusal to `broad-gate`, gives the
`Broad gate` row the criterion it never had, and moves the question that
fills it in from the gate's refusal to the bootstrap.

The last row replaces one this branch REMOVED from `seal/ledger.md`: S14 of
work item 1788354065 said the bootstrap asks the mode question and then the
parity question **and nothing else**, and it now asks a third. The claim went
with the code, so the row was removed there rather than re-pointed, and the
new claim is here (`CONTRIBUTING.md` §*House rules*). **S14 bound four claims
and round 1's replacement covered one**, which round 1's own review located and
round 2 confirmed still standing; the last row below is the second of the four,
written now rather than left as a claim nobody records having read. Sixteen other anchor
citations drifted and every one of them was re-read and `--reverify`'d: the
claims held, only the hashes moved. -->

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| The gate looks at the `Broad gate` value before a shell gets it, and refuses three forms — the whole value wrapped in backticks, the whole value wrapped in `$(…)`, and a trailing `&` that is not part of `&&` — with exit 2 and nothing run | `skills/verify/scripts/broad_gate.py#not_as_written@559eb969`, `skills/verify/scripts/broad_gate.py#wholly_substituted@1a067633`, `skills/verify/scripts/broad_gate.py#gate@79c16119`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_row_wrapped_in_backticks_is_refused_and_shown_rewritten@2dd077ab`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_dollar_spelling_of_the_same_substitution_is_refused@a2994c81`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_row_ending_in_a_single_ampersand_is_refused@46ee147f` | **Executed** 2026-09-15. Red first with the call in `gate()` reverted: A1, A2, A3, A4 and A6 failed, and the wrapped row of A2 came back **exit 0 with `SEALED` drawn and `row exit 0` in the panel** over a check that had failed — #402's measured shape, reproduced. Restored: 88 passed | 2026-09-21 · 2026-09-24 | The refusal is raised between the row being read and the first `run`, which is what closes the second surface (`compare_at_base` → `first_command`) by reachability rather than by a second guard **Re-read 2026-09-21 by work item 1789956662 (#423), which drifted this anchor.** The gate now resolves `--base` once, before any check runs, and hands every consumer the resolved commit; the claim above is untouched by that — the `not_as_written` refusal still stands between the row being read and the first `run`, with the resolution inserted BELOW it. **Re-read again 2026-09-21 by work item 1789985781 (#468), which drifted this anchor.** That work item's two new arms run AFTER the first `run`, so the refusal is still between the row being read and any shell, and the reachability argument is unchanged. Re-stamped by the same `--reverify` run as S12 and read for the same reason. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** one argument added to the `panel` call, `gate_copy(root)`, so the stamp says which copy of the gate ran (#475); nothing else in the body moved, and `args.base` is still read once. The claim stands. |
| A matched pair that closes early wraps a part and not the whole, so `$(a) && $(b)`, `pytest -n $(nproc)` and a trailing `&&` are not refused | `skills/verify/scripts/broad_gate.py#wholly_substituted@1a067633`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_pair_that_closes_early_wraps_a_part_and_not_the_whole@7548680a`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_forms_that_stay_allowed_are_sealed_exactly_as_today@93fa055f` | **Executed** 2026-09-15: eight values read off the unit, and three allowed forms sealed end to end over a fixture repository — a `;`, a `$(…)` inside a longer line, and a `&&` chain. **Narrowed 2026-09-16** after the `windows-latest` job: the first two are POSIX syntax that `cmd.exe` does not have, so they run where `subprocess(shell=True)` is a POSIX shell and skip where it is not — asked by attempting, never from `os.name`. The `&&` chain runs everywhere, and all three now assert the panel's `suite 1 passed` rather than the verdict alone | 2026-09-15 | The row stays an arbitrary shell command line by design (#402 §*Not this*). Telling a status-discarding `;` from a quoted one needs a shell parser, which is the over-reach the ticket steers away from |
| `templates/config.md` §*Broad gate* is the one owner of the criterion for choosing a value — three rules, each with its reason — and of both lists; `skills/config/SKILL.md` and `skills/implement/orchestration.md` name that section and restate nothing | `templates/config.md#"## Broad gate"@5b968902`, `tests/test_the_rules_have_one_owner.py#RULES@6be9f0b8`, `tests/test_the_broad_gate_row_is_asked_for_and_runs_as_written.py#test_rule_three_was_folded_rather_than_copied_into_a_third_place@597626b2` | **Executed** 2026-09-15: each rule deleted in turn and each pinned case went red, including rule 3's second copy put back in `skills/config/SKILL.md`, which fails the fold case | 2026-09-25 | Rules 1 and 2 existed in no document at all — the session that met the refusal derived them under pressure (#401). Rule 3 stood in two places and is FOLDED into the owner rather than copied into a third **Re-read 2026-09-17 by work item 1789598366 (#415)**, which rewrote one cell of the allowed list. The section still owns the criterion's three rules and both lists, and no other document restates them. **Re-read 2026-09-18 by work item 1789721571 (#429)**, which added the fence rule to this document. The section this row anchors on gained a paragraph and lost none; the claim holds and the cases it cites are green. **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** `RULES` gained rules 13 and 14, one owner and their links each. Rule 12's owner sentence and its two links are unchanged, and both pinned cases are green, so the claim holds. **Re-read again 2026-09-22**, in round 1's fix pass. Comments were added above rules 13 and 14 recording what each row pins and what the link assertion cannot see. No row, needle or assertion changed. **Re-read 2026-09-25 by work item 1790260567 (#448), phase 1**, which added one paragraph to `## Broad gate` saying which shell runs the row on Windows and that `cmd.exe` is handed each command name with `/` written `\`. The paragraph states no rule of the criterion and no entry of either list, and no other document restates them; the claim holds. |
| The absent-row refusal says the row is a person's and names where they answer it, instead of telling the reader to write one | `skills/verify/scripts/broad_gate.py#missing_row@85b31017`, `agents/sealer.md#"## The command"@dabbe5b2`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_absent_row_refusal_sends_the_question_to_a_person@927b1796` | **Executed** 2026-09-15 against a fixture repository with no row: exit 2, nothing run, and the message carries *not this session's to do*, *a row is a thing a person wrote* and `/specseal:config`. Red with the old sentence put back | 2026-09-21 · 2026-09-24 | #401 is a session that met the old message, ran four candidate commands, chose one, wrote the row, and told the owner afterwards. The only reader standing in front of that message is a session, which is the one party that may not write the row. **Re-read 2026-09-17 by work item 1789598366 (#415), which drifted this anchor**: the absent-row message is unchanged word for word. What the branch added is a SECOND message, for a `Broad gate` line that is there and will not parse, and the case grounding this row now also asserts that second sentence is absent from the absent-row refusal. **Re-read 2026-09-17 again, in that work item's round 2 fix pass**, which drifted the anchor a second time: the absent-row message is still unchanged word for word, and what moved is which line the second message is built from. Executed over a config with no `Broad gate` row anywhere — the absent-row refusal is what comes back, and `test_a_refused_row_of_some_other_item_is_not_read_as_this_one` is the case that keeps it **Re-read 2026-09-18 by work item 1789721571 (#429)**, which put one fence rule in front of every walk of this table and left both halves of the stop rule where they were. The unit this row anchors on gained that filter and nothing else; the claim holds and the cases it cites are green. **Re-read 2026-09-21 by work item 1789956662 (#423), which drifted this anchor.** The gate now resolves `--base` once, before any check runs, and hands every consumer the resolved commit; the claim above is untouched by that — `agents/sealer.md` §*The command* gained three paragraphs about which base the gate resolves, above the absent-row paragraph this row is about, which is unchanged word for word. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** gained the paragraph on which copy of the gate runs and the capture-file sentence (#475, #544); the command, the base resolution and every refusal paragraph are unchanged. The claim stands. Re-stamped again in round 1's fix pass: the capture-file sentence now says to make the directory first (`mkdir -p`, ⬜ 3); nothing else in the section moved. |
| The bootstrap asks for the `Broad gate` row in the same `AskUserQuestion` as the mode — candidates read off the repository and offered with the file each came from, the criterion named by pointer, and a decline that says what it costs | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `tests/test_first_setup_asks_once.py#test_the_broad_command_rides_the_question_that_already_stops_the_session@86d58f78`, `tests/test_first_setup_asks_once.py#test_the_question_proposes_candidates_read_off_the_repository@0072b68b`, `tests/test_first_setup_asks_once.py#test_the_decline_is_offered_and_says_what_it_costs@89aa7599` | **Executed** 2026-09-15: ten mutations of the Bootstrap section, each pinned case red, including a second `AskUserQuestion` opened beside the first. The prompt budget is unchanged — the call already stopped the session, once per repository | 2026-09-15 | This replaces the row removed from `seal/ledger.md`, whose claim ended *and nothing else*. A decline writes nothing at all: unlike the mode row, whose absence #151 measured, a declined row and a never-asked row meet the same refusal and are told the same correct thing **Re-read 2026-09-17 by work item 1789598366 (#415)**, which rewrote the pipe warning beside this question. The candidates, the file each came from, the criterion by pointer and the decline are untouched, and all three cited cases are green. |
| A backgrounding `&` is refused only where it is LAST, and the mid-line form stays legal with its cost named rather than being refused by a heuristic | `skills/verify/scripts/broad_gate.py#not_as_written@559eb969`, `templates/config.md#"## Broad gate"@5b968902`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_an_ampersand_that_is_not_last_stays_allowed@81af4642` | **Executed** by round 1 and again here: `(a failing check) & echo second` exits 0 under `/bin/sh` and `not_as_written` returns None, while the trailing form is refused. Four boundary values pinned, including `2>&1` and a quoted `&` | 2026-09-25 | Round 1's 🟡 1. The form breaks the criterion's second half wherever it stands, so it had to be in ONE of the two lists; it is in the allowed one because telling an operator `&` from a `2>&1` or a quoted one needs the shell parser `spec.md` §Scope refuses, and a false deny makes a legitimate row unwritable. A fix pass may not add mechanism (`skills/code-review/orchestration.md`), so the code route is `questions.md` Q6 rather than this branch's **Re-read 2026-09-17 by work item 1789598366 (#415)**, which rewrote the pipe cell two rows above the `&` one. Both `&` cells are untouched. **Re-read 2026-09-18 by work item 1789721571 (#429)**, which put one fence rule in front of every walk of this table and left both halves of the stop rule where they were. The unit this row anchors on gained that filter and nothing else; the claim holds and the cases it cites are green. **Re-read 2026-09-25 by work item 1790260567 (#448), phase 1**, which added one paragraph to `## Broad gate` saying which shell runs the row on Windows and that `cmd.exe` is handed each command name with `/` written `\`. The paragraph names `&` only as a separator after which a command name starts, and both `&` cells of the lists are untouched; the claim holds. |
| The once-per-repo bootstrap says in three lines what it created, that the root's presence at that place is the opt-in, and what each part of the root is for | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b` | **Read** 2026-09-15: step 2 of the Bootstrap carries all three, unchanged by this branch — what this branch changed is step 1, which now asks two questions in one `AskUserQuestion`. **No case pins this claim**, and this row says so rather than citing one that pins a neighbouring paragraph: round 3 grepped `tests/` for *three lines* and for *what each part* and found neither, and the case this row used to cite asserts three strings in the *Ask only here* paragraph instead | 2026-09-15 | Replaces the second of the four claims S14 bound, which left `seal/ledger.md` with that row. Round 1 removed the row because its *and nothing else* went with the code and wrote one replacement; round 1's own review found the other three unreplaced, and round 2 confirmed it. The remaining two were correct to leave — they are about the mode question and the parity question, which rows 1885 and the parity rows already carry **Re-read 2026-09-17 by work item 1789598366 (#415)**, which rewrote the pipe warning in step 1. The three lines of step 2 are untouched. |

<!-- specs/1789455558-the-record-chain-disagrees-with-itself-in-five-places -->
### 1789455558-the-record-chain-disagrees-with-itself-in-five-places

<!-- Rows cut the way `spec.md` cuts the tickets — by what each claim is about
rather than by the phase that wrote it. No header: every row carries its own
anchor, so there is nothing for one to declare. -->

###### The record chain disagrees with itself in five places

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A repeated `Location` resolves to the **first** verdict row carrying it on both sides: `inherited_rows` skips a coordinate it has already emitted, and the map `close` hands the forward reach is built with `setdefault` rather than a plain assignment. The two agree by construction rather than by both being right — whichever row a repeat resolves to, both resolve to the same one | `skills/code-review/scripts/round_record.py#close@b91242ae`, `tests/test_the_fixes_close_the_record.py#test_the_reach_takes_the_row_the_inherited_table_attributed_it_to@d8073517`, `tests/test_the_fixes_close_the_record.py#test_a_repeated_coordinate_resolves_to_one_row_on_both_sides@a17cafc1` | **Executed** 2026-09-15, both cases seen red with the plain assignment restored and failing on different halves — the first on the value (`round 1's 🟢 — verified` where `round 1's 🔴 1 — fixed` was owed), the second on the agreement (`round 1's ⬜ ` against `round 1's 🟢 `). 77 passed in the module with the repair, exit 0 | 2026-09-24 | **Refusing the repeat instead is foreclosed by the corpus, measured here rather than inherited.** Of the 366 `round-N.md` under `seal/specs/*/rounds/` at `c36e0fe0`, 247 parse under `verdict_rows` — the keying reader `close` itself uses — and **71 of those repeat a `Location`, over 105 coordinates**. A refusal would refuse records this repository has already written. **The defect is still latent: 0 of the 105 pair a numbered row with an unnumbered one**, the pairing the two sides can actually disagree about. The issue measured 0 of 74 on a corpus half this size and the figure has not moved. What makes it worth the repair is that `agents/warden.md` asks a reviewer for exactly the shape that produces the pairing. The class is one member — `close`'s other two maps are keyed by finding number, and `finding_number` refuses a duplicate id before either is built. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1), which drifted `close`.** `--broad-gate` now writes the cell through `kept_broad_gate`, the one path `seal` takes, so a run the cell already holds is kept; the table, the range, the surface and the ordering this row is about are untouched. Re-read 2026-09-24 — #556 brought the comment above `close`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| An empty fill has **two** causes and the accounting tells them apart. `close --round N` refuses when round N+1's `## Inherited coordinates` names no row from round N **and** does not account for round N's coordinates under any earlier round either, naming the ones it could not account for, with nothing written to either record. It stays silent when the table accounts for them — the re-review round the silence exists for | `skills/code-review/scripts/round_record.py#reach_forward@c5fe1344`, `tests/test_the_fixes_close_the_record.py#test_a_truncated_inherited_table_is_refused_naming_what_it_no_longer_holds@78803909`, `tests/test_the_fixes_close_the_record.py#test_a_section_that_accounts_for_the_coordinates_under_an_earlier_round_is_silent@db36973a` | **Executed** 2026-09-15: the truncation case red at **exit 0** with the accounting deleted, where exit 2 is owed, and `test_a_round_whose_coordinates_an_earlier_round_claimed_is_not_refused` green in the opposite direction with the accounting in place. Both records asserted byte-identical after the refused run. 79 passed in the module, exit 0 | 2026-09-15 | **The narrowing to `filled == 0` is a measurement, not a preference.** Applied to every run, the accounting refuses **65 of the 139 committed round-N/round-N+1 pairs**. Setting aside the 15 the standing *coordinate the verdict table lacks* refusal already stops: 74 pairs are silent under both rules, 48 refused under both, and **2 refused by the unconditional rule alone** — `1788272986` round 2 and `1788433011` round 2, whose next round's section was written **by hand**, one correct row where the round before carried three coordinates. Refusing a hand-written section for filling only what it knew about is the reader sent to correct a table that is not wrong. **The fourth combination is zero**: `filled == 0` with every coordinate accounted appears in no committed pair at all, so the shape the silence exists for survives only in its own case, and the corpus could not have caught a regression there. The accounting reads the whole `Coordinate` column rather than the `round-N` rows, because `inherited_rows` is first-seen-wins across rounds |
| The two cuts that take a span **out of** a cell strip a full stop at the boundary, and `chain.SEPARATORS` is not widened. The class is **two** members, not the one the report named: the fix-table note's commit-span cut, and the `deferred` row's cut of the home off the front of the third cell | `skills/code-review/scripts/round_record.py#fix_table@d2c83250`, `tests/test_the_fixes_close_the_record.py#test_a_fix_note_carries_no_stray_period@fa9341e7`, `tests/test_the_fixes_close_the_record.py#test_a_deferred_note_carries_no_stray_period@9fba7b64`, `tests/test_the_fixes_close_the_record.py#test_the_stray_period_repair_did_not_widen_the_shared_separators@7de7f0a9` | **Executed** 2026-09-15. The second member was reproduced through `fix_table` directly before it was repaired: a third cell of `#309. the parity arm is out of scope` came back with a note of `. the parity arm is out of scope` and rendered `#309 — . the parity arm is out of scope`. Both cases red with the strip back to `chain.SEPARATORS` alone, and the direction guard red at `assert 0 == 2`. 201 passed across both record modules, exit 0 | 2026-09-15 | **What makes a site a member is not the direction of the cut**, which is what the frame's default reasoned from. It is whether the character at the cut boundary has already been tested. Five `strip(chain.SEPARATORS)` sites exist, not four: at the `home` line and at the suffixed-verdict refusal the arm above matched on `word[len(w)] in chain.SEPARATORS`, so the remainder begins with a separator run the strip takes whole, and `written_late_cell` reads an entire flag value where a trailing stop is a sentence's. **What the symmetric strip costs, stated:** a note that ends in a full stop loses it too. A one-sided strip would leave the two members spelled differently from their three neighbours for a stop nobody has yet written, and `#414` prescribed the symmetric form verbatim |
| At the pull request, the severity in a refusal's **message** is read from the row's `#` cell, and a row whose verdict the vocabulary does not close is refused by its own arm with its own sentence — naming **both** of the things that put it there, a blocking marker in a cell other than the `#` cell and a verdict that is not one of the words that close a row, with those words rendered from `CLOSED_WORDS`, and naming no 🔴 itself. The set of refused rows is unchanged | `skills/code-review/scripts/chain_check.py#open_blocking@8facae38`, `skills/code-review/scripts/chain_check.py#open_row_reason@f520777e`, `tests/test_chain_check_at_the_pull_request.py#test_a_confirmation_quoting_an_earlier_blocker_is_not_called_a_blocking_finding@06dfc0b2`, `tests/test_chain_check_at_the_pull_request.py#test_an_unrecognised_verdict_is_refused_by_its_own_name@ecb46252`, `tests/test_chain_check_at_the_pull_request.py#test_a_row_that_carries_a_blocker_keeps_the_blocking_sentence@8b29624b` | **Executed** 2026-09-15, two mutations killed. With `BLOCKING in "".join(seen)` restored the run printed the defect verbatim — `` `Pass` is checked, and this 🔴 row reads `verified` — a blocking finding that is not fixed, answered or withdrawn: 🟢 `` — one sentence naming two severities for one row; with the vocabulary clause deleted the §14 pin went red on `` `fixed` `` missing. 94 passed in the module, exit 0, including the walk over this repository's own committed last records | 2026-09-17 | **The repair is a message split and the selection is deliberately untouched**, which is not what the ticket's title suggests and is what its body asks: *failing the row is correct*, because an unrecognised verdict counting as closed is the tolerant read the file exists to refuse. Reading the SELECTION from the `#` cell is the rejected alternative — it would let a row whose verdict word the vocabulary does not hold stop being refused at all. **Round 1's 🟡 1 corrected the sentence, not the split.** The arm first said the verdict was *outside the vocabulary*, which reached `open` — the word `agents/warden.md` prescribes and the first arm prints back — and stated a rule the file does not hold, since a 🟢/`verified` row quoting no 🔴 is not selected at all. Two conditions select the row and either is a way out, so the message names both. **Round 2's ⬜ 3 and ⬜ 4 narrowed each clause to what the arm can check**: the selection is a 🔴 in any cell but the first, which is usually an earlier round's finding quoted in the Grounds and is not always — three shapes were measured where it is this round's own marker, or quotes no finding at all — so the sentence says *carries a blocking marker*; and it stopped calling the author's prescribed word *an unrecognised verdict* one clause after dropping *outside the vocabulary* for saying the same thing. **Two neighbouring checks say the selection could not widen either way:** `test_a_checked_pass_beside_an_open_NON_blocking_finding_passes` pins a 🟡 row reading `open` as a pass, and the corpus walk at the foot of the same module runs every committed last record through these functions |
| The rule that a reference to a seal names whose is held by **one** check, `tests/test_one_word_one_meaning.py`'s sweep, which skips a hit whose next character is a letter and whose `SEAL_SWEPT` lists `round_record.py`. The module-local pins that remain are the two positive ones, and the seal refusal's exit sentence names the sealer | `skills/code-review/scripts/round_record.py#seal@ad6b9461`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_refusal_says_which_value_the_last_record_may_hold@d784246a`, `tests/test_one_word_one_meaning.py#test_no_instructing_document_leaves_an_instance_anonymous@408c4af0` | **Executed** 2026-09-15: with the exit reworded to *before the seal runs*, the sweep went red naming `skills/code-review/scripts/round_record.py` and quoting the span — which is what proves the sweep, and not the deleted assertion, holds the rule. 86 passed across the two modules with `before the sealer runs` in place, exit 0 | 2026-09-24 | **The `seal/ledger.md` row this branch removed is restored**, with only the clause that went false corrected in place (round 1's ⬜ 6). The deleted assertion had no next-character guard, so it refused `the sealer` — the correct way to name the agent — and it is what #406 removed. **Deleting it was NOT coverage-neutral, and this row said it was** (round 1's 🟡 2): the sweep reads flattened SOURCE while the assertion read the run's OUTPUT, and Python joins adjacent string literals where a flattened read does not, so an anonymous instance split across two literals was invisible to the sweep and visible in the refusal. The coverage is restored by folding the literal seam inside `flat`, which keeps one implementation of the rule reading two kinds of input — the shape the alternatives table asked for. **And the fold was itself held by nothing until round 2's 🟡 1**: both cases round 1 planted called `LITERAL_SEAM.sub` directly, so reverting `flat` left all seventeen cases green and the repair open again with the suite silent — the third time on this branch that a repair for *a check that cannot fail* was not held. The case that holds it goes through `flat`, asserts the seam it reads is really there, and pins the `.py` guard on a markdown file whose quoted shell command a fold would cut. What went is still a second, stricter reading of one rule, held by a check that is not the rule's owner; `CLAUDE.md` names the sweep as the check for that rule, and a second stricter one is a disagreement rather than a second check. §14 is still met inside the module a reader of the refusal opens, by the two positive pins and by the new one on `before the sealer runs` **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** `seal`'s docstring and one comment in its body were corrected: they said a capped run reads `no fixes to check` here, which holds of a capped run's LAST record and not of a capped record that wrote fixes. No arm, constant or refusal moved. The sweep is still the one check holding the rule and `SEAL_SWEPT` still lists `round_record.py`; the same 18 green cases cover it, so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `seal`** at the write alone; `SEAL_SWEPT` and the sweep are untouched. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1, ⬜ 8), which drifted `seal`.** The keep moved into `kept_broad_gate`, shared with `close --broad-gate`, and a run at the commit the newest entry already names replaces that entry rather than duplicating it; the refusals are unchanged. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| A fixture that rewrites a record by substitution **asserts the substitution landed**, and that guard is what closes the vacuity. The case it feeds carries a positive assertion beside its negatives which is false in the same state, and it is a **duplicate** of the guard rather than an independent reader: the fixture asserts the same equality on the same cell and fires first | `tests/test_the_fixes_close_the_record.py#one_finding_inside_one_earlier_unit@4743ea9b`, `tests/test_the_fixes_close_the_record.py#test_a_unit_added_by_a_fix_outside_every_earlier_unit_is_depth_one@ce138205`, `tests/test_the_fixes_close_the_record.py#two_rounds@bb49d6c5`, `tests/test_the_fixes_close_the_record.py#two_findings_inside_two_earlier_units@1ce763a4`, `tests/test_the_record_is_generated.py#test_the_two_record_run_reads_back_through_chain_check@7f980adc` | **Executed** 2026-09-15, and the vacuity measured rather than argued: with the `New units` pattern broken so the substitution misses, and with neither the guard nor the positive assertion present, the case **passes** — 1 passed, exit 0. Two mutations killed against the rebuilt tree: the broken pattern turns the fixture red at `assert 'none — the fixes are not yet written' == 'alpha (depth 1)'`, and round 1 renamed to `none` **after the fixture returns** turns the case red at `assert 'none' == 'alpha (depth 1)'` with both negatives above it holding. 201 passed across both modules, exit 0 | 2026-09-24 | **Round 1's ⬜ 4 is why the clause says duplicate.** In the defect state #407 names, the fixture guard fires before the case is entered, so the case-level assertion cannot be what catches it; it is kept because it states inside the case what the case depends on. **Nothing in `close`'s output discriminates the two states**, which is why the assertion the case already carried is not the one the ticket asks for: `New units \| beta_guard (depth 1)` is written from `measure` and `added` whether or not the depth walk ran. What is false in exactly the guard state is round 1's own `New units` cell read back from disk. **The second mutation only proves anything applied inside the case** — inside the fixture, the guard fires first and the case is never reached. The class is five substitution sites and one was already guarded, `test_a_previous_record_whose_checker_cell_does_not_parse_is_refused`, which is the house shape the ticket says the author knew and left off three of four. **Re-read 2026-09-24 by work item 1790174138 (#436), which drifted `test_the_two_record_run_reads_back_through_chain_check`:** the case gained a sixth substitution, the `Fix range` row, and the guard for it in the same shape — an assertion that the range landed — so the claim holds one site wider. Re-read 2026-09-24 — #526's split re-pointed the anchored case to the document that now holds the section it reads, and widened any absence it asserts of the spec to all three documents; the claim unchanged. |

<!-- specs/1789518345-who-asks-the-routing-question-and-what-checks-the-answer -->
### 1789518345-who-asks-the-routing-question-and-what-checks-the-answer

#### Who asks the routing question, and what checks the answer

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The framer's mark is read at the **foot** of `spec.md` and never by a search across it. A spec that DOCUMENTS the mark carries it three times — quoted in a fenced block, named in a table of who writes what, and stated as an acceptance row — so a search anywhere in the file passes the one work item in this tree that has never been framed by anybody | `skills/code-review/scripts/chain_check.py#frame_mark@95039234`, `tests/test_chain_check_at_the_pull_request.py#test_a_mark_that_is_only_QUOTED_does_not_count@21f7139f` | **Executed** 2026-09-16 before the arm was written: `grep -rln "^Framed .*, before the build\.$" seal/specs/*/spec.md` matched exactly one file, this work item's own `spec.md`, which carries no mark. The arm reads the last non-empty line, so all three quoting shapes are refused | 2026-09-16 | This is the third work item of the release about checks that could not fail, and the instance was already in the tree when the arm was designed. The template's line is the same text, which is why an unfilled `<date>`/`<who>` is refused separately |
| `item_began` answers **None** for a path one level shallower than a round record, and None is the GRANDFATHERED answer — so a cutoff keyed on it is off, silently, for the arm that needs it | `skills/code-review/scripts/chain_check.py#item_began_at@39742e68`, `skills/code-review/scripts/chain_check.py#item_began@f1539c2d` | **Executed** 2026-09-16: the first run of the `not yet` case on a work item begun at 1789518345 exited 0 and its notice quoted `1788912166`, the chain path's constant. `item_began_at` is the rule one level up and `item_began` delegates to it | 2026-09-16 | The sentinel for "derive it yourself" is `False`, not `None`, because `None` is a legal value that MEANS grandfathered — a repository naming its work items some other way has no date to compare |
| `chain_check.broad_gate` needs **no second reader** for a `broad-gate.md`. The only thing it asks of the record around the cell is `Target SHA`, which that file does not carry, so the premature comparison makes no claim — and that is the right answer rather than a gap tolerated: with no round there is no reviewed commit for a run to have been spent before | `skills/code-review/scripts/chain_check.py#broad_gate@e13776dc`, `skills/code-review/scripts/chain_check.py#direct_seal@65d1ae0d` | **Read** 2026-09-16, then **executed** through `test_a_direct_declaration_with_a_seal_passes` and `test_a_direct_declaration_whose_seal_never_ran_fails`. What the one reader cannot answer is the file being ABSENT, because `read_record` returning None is its "no claim" value | 2026-09-16 | `questions.md` Q4. No field forced a split, so one cell has one spelling. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `broad_gate`.** One reader still serves both homes: `seal` writes the newest-first cell into `broad-gate.md` and the last record alike, and `direct_seal` passes on the newest entry (`test_the_direct_home_takes_the_same_shape_on_a_re_seal`). **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1), which drifted `broad_gate`** in its no-SHA message alone — it now says `close --broad-gate` takes the same newest-first path; the states and the refusals are as they were. **Re-read 2026-09-24 by work item 1790206437's round-1 fix pass (⬜ 3, the sweep):** `broad_gate`'s docstring now says a re-seal at a new commit records a second run and the same comparison as the newest entry replaces that entry, which is `same_run`'s rule — asked of `entries[0]` alone (narrowed to the newest entry by round 2's fix pass, ⬜ 6); the states and the refusals are as they were and the claim holds |
| The `CLAUDE.md` block's one source is `templates/claude-md-block.md`, not the repository's own `CLAUDE.md`, which carries a generated copy | `.github/scripts/claude_block.py#<module>@00000000` | **Executed** 2026-09-16: `claude_block.py --write` then `--check` exits 0. Editing `CLAUDE.md` directly is overwritten by the next regeneration and ships the old paragraph to every installer | 2026-09-16 | #292 measured the two copies and found `## Git` 95 % identical with one sentence moved in one and not the other. A correction handed to this work item said the source was `CLAUDE.md`; it is not |
| The word `automation` enters this repository's machine-read vocabulary with **no collision**: every occurrence in the tree is the ordinary English noun | `hooks/routing.py#AUTOMATION@5b2bfac6`, `tests/test_routing_is_recorded.py#test_the_row_name_and_the_option_label_are_one_word_two_cases@0e774d5d` | **Executed** 2026-09-16: 40 occurrences, all of the form *this repository's own automation* or *costs no automation anything*, none a named concept. `tests/test_one_word_one_meaning.py` pins six words and this is not one of them | 2026-09-16 | The row name `Automation` and question 1's option label `automation` are deliberately one word. The two vocabularies must not cross, and both crossings read as unanswered rather than as a confident wrong answer |
| The design gate had a **second copy** in `agents/smith.md`, sixty lines below the span `plan.md` located it at, and a removal keyed on that span left the file saying *phase 2 is your caller's spawn* and *wait for an explicit go* in one section | `` `agents/smith.md#"## Phases"@c0667ce2` ``, `tests/test_a_moved_rule_leaves_its_definition.py#test_the_smith_claims_no_act_another_definition_owns@9e5d6595` | **Read** 2026-09-16 at the phase boundary. The rung CONDITION stays — `test_the_top_rung_names_behaviour_rather_than_a_count` requires `observable behaviour` in this file — and only the asking clause moved | 2026-09-24 | The span was named by content on purpose, because the waiver example and its `# RIDER:` sit inside the same line range and are a kept application. A line-keyed removal takes both **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The section gained two paragraphs: that a capped run does not end the smith's fixes, and that the first home a leftover takes is a rung rather than a default. Neither new paragraph mentions waiting for a go or asking a question, so no second copy of the design gate returned and the claim holds. **Re-read again 2026-09-22**, in round 1's fix pass. The no-mechanism destination sentence changed from *is an issue* to *takes the ladder*; nothing this row is about moved. **Re-read a third time 2026-09-22**, when the broad gate's refusal fired the `# RIDER:` anchored on this section: its stamp was re-read, its measured half re-measured and corrected, and the rider re-stamped. The rider is about the `[no-review]` waiver example at the head of the section and nothing this row claims is in it. Re-read 2026-09-24 — #55's sentence joined the design gate's waiver paragraph (the waiver is the last shape, and contract §8 names the two before it); the claim unchanged. |
| The assertion that pinned the declaration's path inside `agents/smith.md` went with the act, and round 1 moved the act a second time. `agents/smith.md` no longer names `seal/specs/<work-item-id>/routing.md`, and neither does `agents/framer.md`: the write is the SESSION's, because no agent this plugin spawns has `AskUserQuestion` | `tests/test_waiver_decided_at_start.py#test_the_smith_carries_its_own_half_and_not_the_questions@f1f662e7`, `tests/test_waiver_decided_at_start.py#test_no_agent_definition_tells_an_agent_to_ask_a_person@a11ed083`, `` `agents/framer.md#"## You have no interactive phase, and you ask nobody anything"@559ee97c` `` | **Executed** 2026-09-16, twice. The build: the renamed case requires the ABSENCE of `three axes`, `two axes` and `three checkboxes` in `agents/smith.md`. Round 1's fix pass: `ToolSearch` for `AskUserQuestion` returns `No matching deferred tools found` from `smith` as it did from `warden`, and the new case refuses any `agents/*.md` line naming the tool outside the sentence that says no agent has it | 2026-09-16 | The shared ledger's row kept its other four coordinates rather than being removed whole: one anchor of five went, and the claim still holds for the rest. This row's own anchor moved once more when the framer's writes table went from four back to three |
| **No agent this plugin spawns has `AskUserQuestion` or any equivalent**, so the routing batch cannot be a subagent's act. The home of the asking is the session that spawns the work | `` `agents/framer.md#"## You have no interactive phase, and you ask nobody anything"@559ee97c` ``, `tests/test_waiver_decided_at_start.py#test_no_agent_definition_tells_an_agent_to_ask_a_person@a11ed083`, `` `skills/implement/orchestration.md#"## Orchestrator: how the work is routed — two questions, one call, one file"@ec41c96f` `` | **Executed** 2026-09-16 from two agents independently: `warden` during round 1 and `smith` during its fix pass. Each declares no `tools:` key and inherits the full set; the tool is absent from the list and `ToolSearch select:AskUserQuestion` returns `No matching deferred tools found` | 2026-09-16 | The case holds ONE name over the `agents/*.md` glob, not a vocabulary. A sweep for every tool an agent cannot reach needs a per-agent list kept in step with the harness, which is mechanism and is handed over as a ticket. **Re-read 2026-09-23 by work item 1790154759 (#518).** The anchored section's one change is the wake/quiet table's review-arm *Wakes when* cell, which now says a commit confined to `docs/` and `seal/` wakes that arm too; the section still homes the asking in the session that spawns the work, so the claim holds **Re-read 2026-09-23 by work item 1790173106 (#241), which drifted this `##` region.** The four-combinations table's two direct rows and the routing section's closing paragraph now name the sealer's `broad-gate.md` where they said *nothing required* and *by the token in every command*; the two questions, their rows and the one spelling of the decision are unchanged |
| `seal_home` picks the cell's home from the `Review` row, never from what is on disk, because `chain_check` reads `broad-gate.md` on its direct arm alone and the last round record on its chain arm — so a home chosen from the disk disagrees with the reader in BOTH directions | `skills/code-review/scripts/round_record.py#seal_home@93804610`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_a_chain_declaration_with_no_round_record@a3bc9e74`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_direct_declaration_seals_into_its_own_home_even_with_rounds@f9294350` | **Executed** 2026-09-16: with the disk-based version restored, the chain case reports `sealed …/broad-gate.md` at exit 0 and the direct case reports `sealed …/rounds/round-2.md`; both go red. Round 1 reproduced the first direction in a throwaway clone before the fix | 2026-09-16 | The refusal `last_record` used to raise comes back for the one state it was always right about — a chain declaration whose first record is not written yet. An unreadable or absent declaration falls back to the disk, which is the module's standing direction |
| `skills/implement/orchestration.md` and the `CLAUDE.md` block state the routing decision in ONE spelling, `two questions in ONE ``AskUserQuestion`` call`, because a case pairs the two documents on nine shared phrases — and two spellings of one decision is the drift that pair exists to catch | `tests/test_review_axes.py#test_implement_and_the_preset_block_do_not_drift@cb5fc88d`, `` `skills/implement/orchestration.md#"## Orchestrator: how the work is routed — two questions, one call, one file"@ec41c96f` `` | **Executed** 2026-09-16 during round 1's fix pass: the case had been red since the build's phase 2, which rewrote both documents and dropped two of the nine phrases. Neither the build's narrow set nor round 1's six changed modules opened it | 2026-09-16 | The lesson is about the SET rather than the phrase: both runs chose their modules by which files they had edited, and this module reads two edited files while naming neither. A module that reads a file is in that file's set. **Re-read 2026-09-23 by work item 1790154759 (#518).** The anchored section's one change is the wake/quiet table's review-arm *Wakes when* cell; the routing decision's spelling is untouched, and `tests/test_review_axes.py` passed in that work item's phase 2 run, so the claim holds **Re-read 2026-09-23 by work item 1790173106 (#241), which drifted this `##` region.** The four-combinations table's two direct rows and the routing section's closing paragraph now name the sealer's `broad-gate.md` where they said *nothing required* and *by the token in every command*; the two questions, their rows and the one spelling of the decision are unchanged |
| A check over `agents/*.md` decides by what a sentence CLAIMS, not by the phrase it uses: **a batching instruction in any of its spellings** is refused only where the window around it names a person answering, because `agent-contract` §10 gives those phrases a second and legitimate meaning | `tests/test_chain_hooks_hardening.py#test_the_questions_are_collected_before_the_work_not_during_it@e305a1b6` | **Executed** 2026-09-16, both directions on a throwaway edit to an agent definition: *open every coordinate a task names in one batch* passes (exit 0), and *collect everything a person has to answer in one batch* is refused naming `['answer', 'person']` (exit 1). **Re-verified 2026-09-16** by work item `1789540097-…`, which made the second half true: the three spellings #422 measured passing — `in **one batch**`, *in a single batch*, *as a single batch* — are each exit 1 now, and each was exit 0 against this row's own version of the unit | 2026-09-17 | The first draft of this repair carried the same defect one layer down — a bare `"ask" in window` fires on `task`, and the probe meant to show the legitimate wording passing went red on that word. Matched at a word boundary now, with stem prefixes so `asks` matches and `task` does not. **The row as written was half true and is corrected in place rather than superseded** (`questions.md` Q2 of `1789540097-…`): the refusal decided by the claim from the start, the FINDER decided by one literal until #422 was repaired, and the anchor never moved. What it still cannot tell apart is *you collect the batch* from *your caller collects the batch*, which is written in the case rather than left to be found |
| `survivor-check` bounds what a range REMOVED, verbatim — not what the branch wrote ABOUT the removal. Three paraphrases written earlier in this branch said the routing act was the framer's and were never in the removed set | `tests/test_waiver_decided_at_start.py#test_the_smith_carries_its_own_half_and_not_the_questions@f1f662e7`, `tests/test_chain_hooks_hardening.py#test_the_smith_says_whether_the_frame_holds_before_building_to_it@d77ff556` | **Read** 2026-09-16 by round 2, reached by a paraphrase sweep after `survivor-check` reported exit 0 over the same range. Confirmed in the fix pass by a six-pattern sweep of `tests/ agents/ skills/ docs/ templates/ hooks/ .github/ CLAUDE.md CONTRIBUTING.md`: one remaining hit, about the file-set table rather than the routing batch | 2026-09-16 | The claim in `survivors.md`'s range row was true of everything the tool reported and false of the tree. It now says which of the two it is about |

<!-- specs/1789540097-three-checks-that-do-not-see-what-they-are-named-for -->
### 1789540097-three-checks-that-do-not-see-what-they-are-named-for

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| Across both sweeps of `tests/test_one_word_one_meaning.py`, **five** members are `.py` and not four. That number is the whole of the closure argument for the seam case's phrase set — `flat` folds the seam for `.py` members only, so every phrase searched in a markdown member takes a code path the fold never runs on | `tests/test_one_word_one_meaning.py#test_folding_the_seam_cannot_hide_an_instance_it_would_have_found@612ae34f` | **Executed** 2026-09-16 against the module's own `SEAL_SWEPT` and `SEGMENT_SWEPT`: `seal_stamp.py`, `round_record.py` and `broad_gate.py` in the first, `session_cost.py` and `tests/test_session_cost.py` in the second | 2026-09-16 | #418 states four, and `spec.md` scope item 2 and `plan.md` §*Technical context* both carry it — one aggregate reaching three documents unopened, which is `agent-contract` §5's own example. The case pins the LIST now, not a number beside it, so the next reader cannot restate it wrongly |
| Anchoring the back of an asking stem is not one change. A bare `\b` at the end stops `persona` and `users` **and also** `questions`, `asked` and `answers`, which the guard is meant to match — so the stems split by whether the plural names the party at the keyboard or a population | `tests/test_chain_hooks_hardening.py#ASKING_BARE@8da3739d`, `tests/test_chain_hooks_hardening.py#test_the_asking_stems_are_anchored_at_both_ends@fa904ec6` | **Executed** 2026-09-16, both mutations: with the back anchor removed the case is red on `persona`; with the inflection group removed it is red on `questions`. The `user` stem stays and is measured — no `in one batch` occurrence stands in any `agents/*.md` today, so no occurrence of the word is within any window | 2026-09-16 | #422's own patch drops the `user` stem to stop `users`, in the same breath as adding the anchor that already stops it. `questions.md` Q1 records the reasoning and the measurement that closed it |
| The guard finds an occurrence by the CLAIM and not by one spelling: emphasis comes out before the search and the preposition and article are read as a set, so `in **one batch**`, `in a single batch` and `as a single batch` all reach the window | `tests/test_chain_hooks_hardening.py#BATCH_PHRASE@13307bae`, `tests/test_chain_hooks_hardening.py#test_the_batch_phrase_is_found_by_its_claim_not_by_one_spelling@fdb07b14` | **Executed** 2026-09-16, each spelling planted beside a person answering in `agents/smith.md` and the module run twice: exit 0 against the finder as of `7d1bf5bf`, exit 1 after. `agents/*.md` untouched stays green, so no definition had to be reworded | 2026-09-16 | **Corrected 2026-09-16 after round 1's 🟡 2.** The build stripped `*` alone, on the grounds that `_` is load-bearing in every identifier these definitions name. Measured: stripping `_` cannot change WHAT IS FOUND — no stem in `ASKING` and no word in `BATCH_PHRASE` carries one — so the whole cost is the printed window, which `skills/code-review/scripts/chain_check.py`'s normaliser of this same name already pays. And `*` alone left out the marker these files use most: backticks outnumber asterisks in four of the five definitions (warden 250/159, smith 202/142), so `` in `one batch` `` reached no window at all. The strip is `[*_`]+` now and the preposition set gained `into`, which round 1 measured missed |
| **Nothing in `agents/*.md` contains a batch phrase**, so the #422 sweep's loop body never executes and every part of the composition inside it is unreachable on anything committed. The guard is therefore held only by a function the sweep CALLS and a case calls too — never by a case that pins the pieces beside it | `tests/test_chain_hooks_hardening.py#batch_instructions@2eba501e`, `tests/test_chain_hooks_hardening.py#test_the_sweep_refuses_a_planted_instruction_in_every_spelling@ab802c66` | **Executed** 2026-09-16 by round 1 and reproduced at the fix pass: zero batch-phrase occurrences in all five definitions, and five mutations of the finder, the emphasis strip, the stem set and the window each left the module at exit 0, 50 passed. After the extraction all five are exit 1, each on `test_the_sweep_refuses_a_planted_instruction_in_every_spelling` | 2026-09-16 | Round 1's 🔴 1, and the fifth instance of this release's class — the third produced by a repair FOR it. `seal/specs/1789455558-…/overview.md` states the rule the build broke one layer down: pin the function the production path calls, never the helper beside it. The two pattern cases are kept, because a piece going red says which piece |
| The sweep's own **read and call** are a second half of the guard, and extracting `batch_instructions` held only the first. With the function and the constants untouched, the read truncated to ten bytes and the loop's iterable replaced by `()` both left the module green | `tests/test_chain_hooks_hardening.py#test_the_questions_are_collected_before_the_work_not_during_it@e305a1b6` | **Executed** 2026-09-16 by round 2 and reproduced at the fix pass: three mutations, each exit 0, 51 passed. After the walk reports what it read and the report is asserted, the truncation and the cut loop are exit 1; `batch_instructions("")` is still exit 0 | 2026-09-17 | Round 2's 🟡 7. `assert len(definitions) >= 3` pins the glob and nothing after it, which is why two of three mutations passed a case that walks five files. The third needs a walk a case can drive with a file it plants — mechanism a fix pass may not add on top of a unit an earlier fix pass created — and it is open in `overview.md` §*Not verified* |
| The two documented prose maxima in `tests/test_docs_line_wrap.py` that had drifted are `agents/smith.md` (148 documented, 109 measured) and `skills/implement/SKILL.md` (99 documented, 90 measured), and the first is restated a second time inside `COVERED`'s own comment | `tests/test_docs_line_wrap.py#COVERED@0dd80b64`, `tests/test_docs_line_wrap.py#"    agents/smith.md                109"@ff61fcc5`, `tests/test_docs_line_wrap.py#"    skills/implement/SKILL.md       90"@ff61fcc5` | **Executed** 2026-09-16 with the module's own `prose_lines` and `display_width`, before and after: all four documented numbers now equal what those helpers report | 2026-09-24 | #422 reports the first. **Round 1's 🟡 4**: this row first carried `#<module>@00000000`, which matches neither `ANCHOR_RE` nor `OLD_COORD_RE`, so the checker counted it as nothing and `#<module>@deadbeef` read `ok` — the one outcome the comment under `ANCHOR_RE` says it will not accept. It is anchored on three real locators now: `COVERED`, whose comment carries the restated 109, and the two docstring lines that state the numbers. Each number is therefore held by the line that states it, and corrupting one takes the row to exit 2 rather than leaving it silent. **Measured again by round 2, each corruption alone**: `109`→`999` is exit 2, 1 drifted and 1 broken; `90`→`999` is exit 2, 1 drifted and 1 broken; the restatement inside `COVERED`'s comment is exit 1, 2 drifted. Each of the two docstring corruptions produces BOTH a broken locator and a drifted unit, because the quoted line is the locator and the four-line block is the unit — round 1's fix table split the two verdicts between them, which is a record this pass may not edit. That a re-derived number breaks rather than drifts the row is the intended cost: the row claims WHICH two had drifted and to what, so a third number moving is a claim to re-read. Nothing in the tree reads a docstring, and planting a reader is mechanism a fix pass may not add **Re-read 2026-09-22** by work item 1790027178 (#458), which added one entry and one comment to `COVERED`. Neither maximum moved: the entry is a path and the comment is prose inside the list, and the two documented figures are still the ones measured. Re-read 2026-09-24 — #526's split re-pointed a citation inside the anchored unit to the document that now holds the section; the claim unchanged. |
| The two `&` cells of `templates/config.md` state which shell each behaviour belongs to, and the case asserts the ATTRIBUTION rather than the behaviour — the `cmd.exe` half stays unmeasured | `tests/test_the_broad_gate_row_is_asked_for_and_runs_as_written.py#test_both_ampersand_cells_name_both_shells@47e9a54d` | **Executed** 2026-09-16, both of #413's measured swaps separately: `/bin/sh` and `cmd.exe` exchanged in the refused row is exit 1, and in the allowed row is exit 1. 18 passed at exit 0 untouched | 2026-09-16 | Work item `1789445605-…` records the `cmd.exe` claim as unmeasured with the `windows-latest` job named. If it is ever measured false the cells change, and this case is what makes the change visible instead of silent |

<!-- specs/1789598366-a-piped-broad-gate-row-takes-every-config-row-below-it -->
### 1789598366-a-piped-broad-gate-row-takes-every-config-row-below-it

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Five rows over a branch that teaches one regex markdown's escape and gives
the gate a second sentence for a line it cannot read.

The first row replaces one this branch REMOVED from
`seal/ledger/1789445605-the-broad-gate-row-runs-unchecked-and-is-never-asked-for.md`,
a fragment of this same unreleased cycle. That row claimed a pipe *cannot
reach the row at all … and it takes every row below it*, and cited a test
unit whose NAME stated the same thing. The claim went with the code and the
unit was renamed, so the row was removed where it stood rather than
re-pointed (`CLAUDE.md` §*a change writes fragments, never the shared file*).
-->

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| A cell of `seal/config.md`'s table carries a literal pipe when it is written with markdown's own escape, `\|`, and every row below it still arrives. The reduction is **exactly those two characters**: a value holding Windows path separators reads back unchanged, and a bare pipe is still where a cell ends. **It narrows in one shape and only one**: a backslash standing immediately against a cell-ending pipe is that escape now, so `\| Broad gate \| C:\Users\x\tools\\|` stopped being a row — and a space before the closing pipe returns the same value byte for byte | `hooks/config.py#CONFIG_ROW@86b247d8`, `tests/test_the_mode_question_is_asked_once.py#test_a_backslash_against_a_pipe_is_the_one_shape_the_escape_narrows@402f8ec5`, `hooks/config.py#unescaped@4144edc6`, `hooks/config.py#config_rows@c173b5c2`, `tests/test_the_mode_question_is_asked_once.py#test_an_escaped_pipe_is_one_row_and_the_rows_below_it_still_arrive@577285c8`, `tests/test_the_mode_question_is_asked_once.py#test_a_windows_path_survives_the_reader_exactly_as_written@5faff715`, `tests/test_the_mode_question_is_asked_once.py#test_a_three_column_row_still_ends_the_table@f456b01a` | **Executed** 2026-09-17. Red first against three separate mutations, each its own case: the value cell reverted to `[^\|]*?` returns **one row of four** and the count is what fails; a general `re.sub(r"\\(.)", r"\1", value)` returns `C:UsersxPythonpython.exe -m pytest` for a row holding `C:\Users\x\Python\python.exe -m pytest`; a greedy last cell reads `\| x \| y \| z \|` as the row `('x', 'y \| z')` and swallows the two-cell rows after it. Restored: 136 passed. **Re-read and re-measured 2026-09-17 in round 1's fix pass** for the direction nobody had looked in: over 1,460 tracked files, 84 lines read differently through the old cell pattern and the new, 83 of them lines that were not rows before and are now, and one that was a row and is not — a fixture in a review report, quoting this very shape. The narrowing case is red under the pre-escape pattern `[^\|]` | 2026-09-18 | The count assertion is the case, not the value. A case reading only the `Broad gate` value passes just as well when every row under it has vanished, and vanishing is the defect. **The defect needs a row that parses ABOVE the piped one** — `config_rows` breaks on an unparseable line only once it has found a row — so *takes every row below it* is true of the second row of a table and not of the first. Measured both ways round **Re-read 2026-09-18 by work item 1789721571 (#429)**, which put one fence rule in front of every walk of this table and left both halves of the stop rule where they were. The unit this row anchors on gained that filter and nothing else; the claim holds and the cases it cites are green. |
| The reader and the writer still agree about which line is the `Mode` row, so `seal mode` no longer writes a second one into a file whose `Mode` row sits below an ESCAPED piped `Broad gate` row. **Written with a BARE pipe it still writes one**, because the writer stops where the reader stops — and a file already two rows deep is repaired by nothing: the next `seal mode` sets the first row and leaves the second, so the file states two modes and every reader takes the first | `skills/implement/scripts/seal.py#table_span@872d300e`, `tests/test_the_mode_question_is_asked_once.py#test_seal_mode_writes_one_row_where_the_mode_row_sits_below_a_piped_row@54d6631c`, `tests/test_the_mode_question_is_asked_once.py#test_seal_mode_still_writes_a_second_mode_row_for_a_bare_pipe@53787ed6` | **Executed** 2026-09-17, and it duplicated before the repair: `write_row` over such a file left it **two `Mode` rows deep**, with `declared_mode` answering `('none', '')` for a file whose own row says `shared` and `table_span` returning `(-1, 5)`. `questions.md` M1 carried this as *read, not executed*; this is the measurement. **Re-read and re-executed 2026-09-17 in round 1's fix pass, for the spelling the branch did NOT close**: over a bare-piped config `write_row` still leaves the file two `Mode` rows deep, and running it a second time sets the first row to `local` while the person's own row two lines down goes on reading `shared`. The case is red under a greedy last cell, where the bare pipe parses and the limitation is gone | 2026-09-18 | `table_span`'s own comment says no command brings such a file back into agreement, which is why this is the worst outcome in the work item. **Round 1's 🟡 4 is that three records read as if the bare spelling were closed too**; each says what it is now, and the case above is what keeps the sentence and the tree in step. The writer inherits the repair through the alias — it reads `CONFIG_ROW` from `hooks/config.py` rather than keeping a copy. **Re-read 2026-09-17 in round 2's fix pass**, which found the limitation case naming one of the four records that have to change when the limit is closed and pointing vaguely at the rest. All four are named in its message now. The assertions are untouched: a message's text is not what pytest reads, so this is a correction to what the case hands whoever reddens it rather than a change to what it pins **Re-read 2026-09-18 by work item 1789721571 (#429)**, which put one fence rule in front of every walk of this table and left both halves of the stop rule where they were. The unit this row anchors on gained that filter and nothing else; the claim holds and the cases it cites are green. |
| `broad-gate` tells a `Broad gate` row that is ABSENT from one that is THERE and will not parse: the second quotes the line as the person wrote it, names `\|` as the way to write the pipe, and no longer says the row is absent. **What that line cost is read off the file rather than stated flat, and off the line that ACTUALLY stopped the reader** — which in a file holding two lines the reader will not take is not the line being quoted. Four cases: nothing was lost where no line stopped the reader; the rows below the quoted line where it is the one that stopped it; the rows below a line LOWER DOWN where the quoted line was read and something further on stopped the reader; and everything from the stopping line down, this line included, where the quoted line sits below it. **The row is told absent only when it is absent**: a `Broad gate` row sitting BELOW a refused line that names some other item gets a refusal naming that line, and a file with no such row anywhere still gets the absent-row refusal — and the gate looks for its own row among EVERY line the reader would not take rather than the first of them, so a `Broad gate` line that is the second such line is quoted back instead of being called absent | `hooks/config.py#refusal@e93d93cb`, `skills/verify/scripts/broad_gate.py#hides_this_row@2c2be78c`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_broad_gate_row_below_a_refused_line_is_not_reported_absent@81e60b5e`, `hooks/config.py#refused_row@3b513f55`, `skills/verify/scripts/broad_gate.py#refused_broad_row@798b3efe`, `skills/verify/scripts/broad_gate.py#missing_row@85b31017`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_an_unescaped_pipe_is_named_as_a_line_that_will_not_parse@de5da900`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_refused_row_of_some_other_item_is_not_read_as_this_one@6a551f07`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_what_a_refused_line_cost_is_read_off_the_file_and_not_stated_flat@b12532b9` | **Executed** 2026-09-17 end to end over a fixture repository whose row carries a bare pipe: exit 2, nothing run, the line quoted back. Red with the new branch in `missing_row` made unreachable — the old *has no `Broad gate` row* came back. Red a second way with the first-cell reading dropped, where a refused line naming another item was reported as this row. The absent-row case asserts the new sentence is NOT in its message, so the branch cannot have swallowed the old one. **Re-read and re-executed 2026-09-17 in round 1's fix pass**, where the cost sentence became conditional: red with the chooser wired to `True`, which is the sentence the build shipped, and red again with the two sentences swapped — so the CONDITION is what the case pins and not the presence of a string. The hidden-row branch is red twice the same way: made unreachable, the person's row is called absent again; widened to fire on every other-item refusal, the file that genuinely has no such row is sent looking for one | 2026-09-18 | Both halves of `agent-contract` §14 are asserted: the new sentence present and the old one gone. The whole line is quoted rather than its first cell (`questions.md` W1) — a refusal that does not show the line sends the reader back to the file to guess which one it meant. **Re-read and re-executed 2026-09-17 in round 2's fix pass**, where the walk stopped answering about the FIRST refused line and started answering about the table. `rows_under` was folded into it and its coordinate removed from this row's grounds — the unit went, the claim did not. Red eight ways, each file restored from bytes the mutation script held: the cost chosen by whether any row was lost rather than by the stopping line — which is the shape round 2's own paste-ready fix proposed — reddens the one-bad-line half; the cost collapsed back to two cases reddens the two-bad-line half; the hidden-row branch gated on the first refused line, and the gate's row looked for in the first refused line only, each redden their own case; the walk giving up at prose above the table's first row, and every refused line reported as reached, redden the third case; the branch widened past `hides_this_row` reddens the guard case; and both units put back to the commit before the fix redden both new cases. **Re-read 2026-09-18 for #430**, which made three of the four cost sentences conditional on what was actually written below the line: the four-way choice this row is about is untouched, every arm is still chosen off the stopping line, and `bin/test tests/test_the_seal_is_taken_once_by_the_sealer.py -q` is 109 passed, 3 skipped at exit 0 |
| `hooks/mode-gate.py` gains no message and no refusal. A config whose `Mode` row is hidden below an unparseable line still reads as undeclared, and the gate still simply asks the mode question again | `hooks/mode-gate.py#undeclared@1a042be4`, `hooks/config.py#refused_row@3b513f55`, `tests/test_the_mode_question_is_asked_once.py#test_a_line_that_will_not_parse_is_named_and_the_hook_still_says_nothing@f6352dad` | **Executed** 2026-09-17: `undeclared` over such a config answers the home, never a message, exactly as before the branch | 2026-09-17 | Deliberate, and it is the cost of keeping a `PreToolUse` hook silent: a wrong refusal there stops a session with nobody able to get past it. `refused_row` reports and refuses nothing, so importing it makes no caller able to deny. **Re-read 2026-09-17 in round 2's fix pass**: `refusal` answers with a list of refused lines now, so `refused_row` hands back the first of them. It still reports and still refuses nothing, `hooks/mode-gate.py` is untouched, and the case above is green |
| The three documents that promised the old behaviour say how a pipe IS written, and each still says what a BARE one costs: the allowed list a person reads before writing the row, the bootstrap paragraph that derives a candidate off a CI `run:` step, and the front door that tells them to edit the value in place | `templates/config.md#"## Broad gate"@5b968902`, `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `skills/config/SKILL.md#"## Procedure"@144ac155`, `tests/test_the_broad_gate_row_is_asked_for_and_runs_as_written.py#test_the_allowed_list_says_how_a_pipe_is_written@dbae8f44`, `tests/test_first_setup_asks_once.py#test_a_candidate_carrying_a_pipe_is_escaped_where_candidates_are_derived@0af2642d`, `tests/test_the_settings_have_a_front_door.py#test_the_skill_says_how_a_value_carrying_a_pipe_is_written@5f3c3757` | **Executed** 2026-09-17, one mutation per document with its own case red: the template's *cannot reach this row at all* put back, the bootstrap's *cannot be written into the row* put back, and the front door's `\|` sentence replaced by a sentence that does not name it. Restored: 81 passed across the three modules | 2026-09-25 | Each case asserts both halves of `agent-contract` §14 — the new sentence present and the old one absent — because a document can gain the correction and keep the old sentence two paragraphs down. The criterion and the two lists stay pointed at from the skill rather than copied into it; what the skill gained is how to write the CELL, which is not one of the three rules and cannot be read off either list. **Re-read 2026-09-17 in round 1's fix pass**: two of the three said the cost fell on every row below the bare pipe without the condition the branch had measured, and both now carry it. Each was put back to its flat sentence and its own case went red. **Re-read 2026-09-18 for #430**: the front door's paragraph gained the LAST-row case beside the first-row one, so all three documents still say what a bare pipe costs and one of them now says it for every position the line can be in **Re-read 2026-09-25 by work item 1790260567 (#448), phase 1**, which added one paragraph to `## Broad gate` saying which shell runs the row on Windows and that `cmd.exe` is handed each command name with `/` written `\`. The template's pipe sentences are untouched, and the paragraph names `\|` only as a separator; the claim holds. |
