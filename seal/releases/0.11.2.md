## 0.11.2 — 2026-09-13

<!-- specs/1789211172-a-round-record-disarms-survivor-check -->
### 1789211172-a-round-record-disarms-survivor-check

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · a work item's `rounds/` records are outside the **range** as well as outside the corpus, on both sides of the range's path list | `skills/code-review/scripts/survivor_check.py#corrected@ea893a8f`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_round_record_the_range_added_does_not_subtract_the_survivor_it_quotes@03a5e1ab`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_round_record_the_range_edited_does_not_become_a_source@863fc756` | **Executed** 2026-09-12. Both cases seen red against the unedited module first, and red in opposite directions: the added side at exit **0** — the record's quotation subtracted the survivor and the gate reported success having measured nothing — and the removed side at exit **1**, naming a coordinate inside the constructed round record as the place a claim was corrected. Green after. Re-measured on the real range `7e17f5e..941dab5`: 894 files and 16 removed sentences before and after, exit 0 → 1, and the survivor in `seal/specs/1789100139-…/changelog.md` back at **2.00** | 2026-09-24 | **This is `seal/ledger.md` S6's claim, widened by the work rather than in place.** S6 says the records are outside the corpus and that row stays exactly true; what it became is an understatement, because the exclusion had held on one of the two functions since the module shipped. The two counts are the load-bearing half of the real-range run: `corrected` returns the removed sentences from the same list the filter narrows, so a filter reaching too far would show as a smaller number, and a record the range only ADDS removes nothing — 16 is what proves the filter took nothing it was not meant to **Re-read 2026-09-23 by work item 1790138190 (#517), phase 4**: the `records_a_past_round` filter still narrows the path list on both sides; a second filter now leaves out a directory the range retired, by either arm, after it. The claim holds. **Re-read 2026-09-24 by work item 1790174139 (#507, #308, #460, #304), phase 4**: the filter on `corrected`'s path list is `records_a_past_state`, which keeps `records_a_past_round` as its first arm; a round record is out of the range on both sides exactly as before, and the claim holds. Re-run green. **Re-read 2026-09-24 by work item 1790206435 (#543, #307, #439), phase 4**: a third filter on the same list, for a changelog fragment the tip's `CHANGELOG.md` has gathered, sits beside `records_a_past_state`; the round-record arm is untouched and the two anchored cases re-run green. The claim holds. **Re-read 2026-09-24 by the same work item, phase 5 (#551)**: the list is read with `--no-renames`, so a record moved between paths is listed at both; the filter on it is unchanged, and the claim holds. **Re-read 2026-09-24 in round 1's fix pass (🟡 1)**: `corrected` gained a held count for the tip's released changelog sections after the list is built; the round-record filter on the list is unchanged, and the claim holds. **Re-read 2026-09-24 in round 2's fix pass**: the held count is now what this range put under a version heading, not every released section at the tip; it still sits after the list is built, the filter is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790221963 (#557, #555), phase 4**: the gathered fragments are now read at `a` from a list spelled from `CHANGELOG.md`'s markers, never from git, and only to drop their sentences from what a release writes; the path list and its round-record filter are unchanged, the two anchored cases re-run green, and the claim holds. **Re-read 2026-09-24 in that work item's round 1 fix pass**: the held sentences' shared n-grams are written after the list is read; the filter is unchanged, and the claim holds. **Re-read 2026-09-24 in that work item's round 2 fix pass**: those n-grams are returned apart from `written` and scored against `CHANGELOG.md` alone; the round-record filter is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790260564 (#564), phase 1**: `corrected` now also lists the tree at `a` and keeps the gathered fragments `a_gathered_fragment` accepts there, only to hold their text; the round-record filter on the diff's path list is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790260564 (#563), phase 2**: `corrected` pairs sentences across paths after every path in the filtered list is counted, so a round record is still in neither side to pair; the filter is unchanged, the two anchored cases re-run green, and the claim holds. **Re-read 2026-09-25 by work item 1790297084 (#592), phase 1**: `paired_across_paths` now takes each key's pairs by the departure path's affinity to the arrival path, then by the departure's path being gone at `b`, then by path order, and `corrected` passes it the paths read at `b`; the pairing is still one for one after every path is counted, and only which departure stays the source can change, so the claim holds |
| S2 · every place in the module that derives a path list from git is filtered by the predicate, filtered by each caller that reaches it — each named with the filter it applies — or named with grounds — counted by **call site**, module scope included, and checked against the source rather than declared | `tests/test_a_corrected_sentence_survives_elsewhere.py#test_every_path_list_this_module_derives_from_git_is_filtered_or_named@3838478c` | **Executed** 2026-09-12, seen red two ways with the module restored byte-identical after each: the filter removed from `corrected` (``corrected derives a path list and no longer applies `records_a_past_round` ``), and a fourth unfiltered path-list call added (``the module derives a path list from git in ['corrected', 'since', 'tracked', 'whole_range'] and this case accounts for ['corrected', 'tracked', 'whole_range']``). The second message is the proof the walk measures rather than restates: it names the three scopes found in the source. **Re-verified 2026-09-12 in round 1's fix pass**, which moved the unit from the function to the call site and re-ran six mutations, six red: the filter removed; a fourth unfiltered path-list function; a second unfiltered list inside `corrected` itself; a path list at module scope; two path lists in one outer call's arguments; and a second unfiltered caller of `tracked`. The third and fourth were **green** before this pass and are the two `plan.md` names in its own six-month failure scenario, *a second range* and *a cache of changed files* | 2026-09-12 | **The unit is the call site, and round 1 is why.** The first shipped walk keyed by function NAME, so two sites in one scope collapsed to one entry and a list at module scope was found by nothing — while `spec.md`'s class table is headed `Call site` and gives `corrected` and `whole_range` separate rows for the same spelling of the same command. The case rose to meet the frame rather than the frame being narrowed. `_path_list_words` reads the words a call names ITSELF and stops at a nested call, which is the reviewer's paste-ready shape corrected: measured against a mutation putting `corrected`'s one declared site inside an outer call beside a second, the subtree-reading shape counts `corrected: 1` and passes, and this one counts 2 and fails. **Why three buckets and not one.** `tracked`'s bucket also asserts `corpus` is its ONLY caller — a second, unfiltered caller would be a fourth path list reached through a third, which set equality alone cannot see because `tracked` stays accounted for. `whole_range`'s bucket asserts the grounds and the function both still rest on `foreign`, a declaration refused and PRINTED rather than silently dropped; if that mechanism leaves the function the grounds are an argument about code that is gone. **The walk keys on the path-listing subcommand words, not on the `git` helper's name**, which removes `plan.md`'s stated failure scenario for this case — a rename turning it red for no defect — and finds a fourth site written as a direct `subprocess.run(["git", …])` **Corrected 2026-09-24 by work item 1790260564 (#564), phase 1**: the claim said *filtered by its only caller*, and the note above says `tracked`'s bucket asserts `corpus` is its ONLY caller. `corrected` now reads `tracked` at the range's left end for the gathered fragments, keeping only what `a_gathered_fragment` accepts, so `tracked` has two callers. The bucket names each caller with the filter it applies and still checks the set of callers against the source, so a third, unfiltered caller is still red. |
| S3 · the module docstring names which side each exclusion holds on, and the sentence is pinned by what it must SAY rather than by its text | `skills/code-review/scripts/survivor_check.py#"## What is excluded, by construction rather than by list"@57f0406a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_docstring_names_both_sides_of_the_round_record_exclusion@1abe4cd8` | **Executed** 2026-09-12, seen red two ways with the module restored byte-identical: the paragraph deleted (``**A record of a past round.** is no longer the first exclusion stated``), and the pre-#365 one-sided sentence put back (``the paragraph does not say the exclusion applies to the pool``) | 2026-09-12 | `agent-contract` §14. What this replaces read *Everything under a work item's `rounds/` is out.* — it stated the intent and named neither function it had to be true of, which is how the asymmetry survived five releases without the module's own account of itself being wrong. Three of the four assertions are positive and about the claim (`pool`, `range`, `both sides`), so a rewrite that keeps the claim passes; the one literal assertion is negative, refusing the old sentence's return **Re-read 2026-09-24 by work item 1790174139 (#507, #308, #460, #304), phase 4**: the pinning case now reads every exclusion in an `EXCLUSIONS` list, the round record first, with the same three positive assertions per paragraph and the negative one on the round record's alone; the red message for a missing opener reads `is no longer an exclusion stated`. The section anchor's hash did not move because a quoted heading in a `.py` file owns its own line alone (`text_regions`, the markdown-only heading rule), so the case's hash is what pins the content. The claim holds. |
