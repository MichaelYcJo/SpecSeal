## 0.11.1 — 2026-09-12

<!-- specs/1789100139-the-file-said-to-delete-it-when-the-last-box-was-ticked -->
### 1789100139-the-file-said-to-delete-it-when-the-last-box-was-ticked

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · the order a ticket runs in has one owner, and it is the orchestrator's half of the implement skill. The draft pull request opens between the smith and the warden rounds, and step 2 names the framer with no interval clause deferring it to a ticket | `skills/implement/orchestration.md#"## Orchestrator: the order inside a ticket"@ba4a34f9`, `tests/test_the_rules_have_one_owner.py#test_the_order_opens_the_draft_between_the_build_and_the_rounds@fcba761b`, `tests/test_the_rules_have_one_owner.py#test_the_order_no_longer_defers_the_framer_to_a_ticket@8557ce6c` | **Executed** 2026-09-11. Both cases were seen RED against the pre-move tree — the constants repointed at `skills/implement/orchestration.md` before the section existed, `ValueError: substring not found` on `text.index("2. spec · plan")`, 3 failed 42 passed, exit 1 — and green once the section landed, 45 passed. The section is the only carrier: `git grep -l "order inside a ticket"` over `docs`, `skills`, `agents`, `templates` and `tests` returns that one file | 2026-09-11 | **The constant had a third usage no path grep could see.** `FLOW` was also rule 6's link carrier in the `RULES` table, spelled as the name rather than as the path, so the module failed to COLLECT rather than failing a case — `NameError` before any test ran. A live reference to a file is a path or a name bound to one, and the work item's enumeration saw only the first kind. The other eight files in the frame's table were re-checked for the same shape and none has one |
| S5b · `RECORDS_OF_A_MOMENT` holds two exact paths and one prefix, and dropping the third exact path left no loaded file needing it | `tests/test_release_hygiene.py#RECORDS_OF_A_MOMENT@9f037b57` | **Executed** 2026-09-11, and this is Q5's answer. The check was recomputed over every tracked loaded file with that one entry dropped and every other exemption in place: seven offending lines, all seven in the file being deleted (`:43`, `:67`, `:76`, `:77`, `:78`, `:102` twice). No other loaded file needs it | 2026-09-11 | **The entry and the deletion had to be one commit.** With the file still tracked, a commit dropping the entry leaves `test_no_loaded_file_names_a_version_at_or_above_the_running_one` red on those seven lines, so `plan.md`'s phase-3 placement was moved to phase 4. **Removing the entry also made the comment beside it wrong** — it said *the three exact paths above* where there are two — and nothing would have caught that: no case reads the comment and no check counts the entries |

<!-- specs/1789108681-a-merged-ticket-looks-unstarted-until-the-release-ships -->
### 1789108681-a-merged-ticket-looks-unstarted-until-the-release-ships

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S4 · a release pull request is refused while its milestone claims an open issue the release branch does not carry, and the refusal is computed from the branch's own commits rather than from the labels | `.github/scripts/release_completeness_check.py#judge@8f94fcdf`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_an_open_issue_the_release_does_not_carry_refuses_it@e70b91e7` | **Executed** 2026-09-11, against the live tracker rather than a fixture. `HEAD_BRANCH=release/v0.11.1 BASE=origin/main REPO=<this repo> python3 .github/scripts/release_completeness_check.py` exits **1** and prints `M \ D — the milestone 'release: 0.11.1' claims 1 open issue(s) this release branch does not carry: #359`, with `M: #351, #359` and `D: #351` below it. That is the correct answer: #359 is this work item and the branch has not merged it | 2026-09-11 | Computing the refusal as `M \ L` gives the same answer by transitivity and blames the wrong party — a release blocked by a failed label write is repaired by a person adding a label by hand, which is the act this work item removes. `plan.md`'s alternatives table records the rejection |
| S6 · the two directions of a label disagreement are named, and only one of them fails | `.github/scripts/release_completeness_check.py#judge@8f94fcdf`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_a_label_naming_a_release_the_issue_is_not_in_fails@0ac207b4`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_the_signal_having_missed_one_reports_and_passes@4dc675fc` | **Executed** 2026-09-11. Mutating `judge` so that `L \ D` stops failing turns the first case red; adding `failed = True` to the `D \ L` branch turns the second red. Both directions print their own name — the literals `L \ D` and `D \ L` — so a reader is never left to work out which set the finding is about | 2026-09-11 | `questions.md` Q1, answered (c) by the repository owner. `L \ D` is always a hand-edit or a squash subject that lost its `(#N)` and one command repairs it; `D \ L` is the signal failing, and a release is not held for the signal's outage |
| S3 · re-running the signal writes nothing a second time, and that holds by construction rather than on an assumption about GitHub | `.github/scripts/label_merged_on_release_branch.py#main@17582ab9`, `.github/scripts/label_merged_on_release_branch.py#issue_labels@7afd8565`, `tests/test_a_merged_ticket_says_so_on_the_tracker.py#test_an_issue_that_already_carries_the_label_is_left_alone@331abc95` | **Executed** 2026-09-11. Deleting the `if label in carried` guard turns that case red. Whether a duplicate add errors or is a no-op is **unmeasured and does not matter**: the path is never reached. GitHub's REST reference for *Add labels to an issue* says only that it "adds labels to an issue's existing labels" and lists no response for the duplicate case (read, fetched 2026-09-11) | 2026-09-11 | `questions.md` Q6's default — *it is a no-op; add unconditionally* — was overturned rather than measured, because measuring it needs a write to the tracker. The label's own creation is the opposite case and the answer there IS known: `gh label create` on an existing name errors, so an unconditional create would fail every push after a release's first squash. **Re-read 2026-09-22 for #450**, which moved `issue_labels`: the body is now a one-line delegation to `close_issues_on_release.py#issue_labels`, because the closing script gained a second caller for the same read and a copied reader is what this script's own docstring says it exists not to have. The reader is byte-for-byte the same logic on the same 404-tolerant `_issue_api`, `main`'s `if label in carried` guard is untouched, and `test_an_issue_that_already_carries_the_label_is_left_alone` was re-run green. The claim — that re-running writes nothing a second time, by construction — is unaltered |
| Q5 · `gh issue list` excludes pull requests and the REST `/issues` endpoint does not, which is why M is read with the former | `.github/scripts/release_completeness_check.py#open_in_milestone@ff50b326`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_M_is_read_with_gh_issue_list_and_not_the_rest_endpoint@65472540` | **Executed** 2026-09-11 on this repository. `gh api "repos/<repo>/issues?state=all&per_page=6"` returns #359, #358, #357, #356, #355, #354 with #358, #356 and #355 carrying a `pull_request` key; `gh issue list --state all --limit 6` returns #359, #357, #354, #353, #351, #350 — none of the three — and `gh pr list --state all --limit 3` returns exactly those three. All three commands exit 0 | 2026-09-11 | It matters because a pull request carrying the milestone could never carry `merged: X.Y.Z`, so it would sit in `M \ D` and block the release with nothing anybody could do about it. No pull request in this repository carries a milestone today, so the property was measured in general rather than on an instance |
| The gate refuses to measure a milestone that is not there, instead of passing on an empty set | `.github/scripts/release_completeness_check.py#milestone_titles@b9fea961`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_a_milestone_that_does_not_exist_says_it_verified_nothing@dcc35502` | **Executed** 2026-09-11. `gh issue list --milestone "release: 9.9.9" --state open --json number` returns `[]` and **exits 0** against the live tracker — so a mistyped or absent title would otherwise make the whole check pass by measuring nothing. With the existence read in place, `HEAD_BRANCH=release/v9.9.9` prints `no milestone 'release: 9.9.9' exists — this run verified NOTHING` and exits 0 | 2026-09-11 | It passes rather than fails, on the same grounds `D \ M` was rejected on: a release nobody scheduled is a person's call and not this gate's. What it must not be is silent, and the sentence is pinned. This hole is not named anywhere in the frame. **Re-verified 2026-09-11 in round 1's fix pass, and the claim got stronger.** Round 1 found the same fail-open one level down: the read took one unpaginated page, `per_page` caps at 100, and a repository with more milestones than that would have the title fall off the page and be reported absent. The guard's presence was pinned and its query was not — the round's probe gutted the query and all 25 cases stayed green. `--paginate` is the fix and `test_the_milestone_list_is_read_past_the_first_page` is the case; three mutations (flag dropped, page size lowered, query gutted) each turn it red. The round's own paste-ready fix also added `--jq .[].title` on the grounds that `--paginate` concatenates arrays into something `json.loads` refuses; **measured on gh 2.92, that is false** — `per_page=2`, seventeen pages, `json.loads` returns one list of 33 — so the flag was not added |
| Q8 · what the release carries is measured from where the branch left its base, not from the base's tip | `.github/scripts/release_completeness_check.py#merge_base@821e8e4b`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_the_range_is_measured_from_where_the_branch_left_main@041efe89` | **Executed** 2026-09-11. The case pins the two commands — `git merge-base origin/main HEAD`, then `git log --format=%s <that>..HEAD` — and replacing `merge_base` with a function returning the base turns it red | 2026-09-11 | The two spellings give the same set under every history this repository has today and come apart the moment a hotfix that was also merged into the release branch moves `main` mid-release. The case pins the commands rather than the resulting set, because the resulting set cannot tell them apart here. **Re-verified 2026-09-11 in round 1's fix pass, and the claim was TRUE OF THE SCRIPT AND FALSE OF THE JOB.** Round 1 read what the workflow hands the script: `actions/checkout` takes no `ref:`, so on a `pull_request` event the job holds the merge ref — the head already merged into the base — and `git merge-base origin/main HEAD` answers the base tip. The range collapsed to exactly the `base..HEAD` spelling Q8 rejected, arriving through the checkout rather than through the code, which is why the case pinning the two commands passed throughout. The step now passes `HEAD_SHA: ${{ github.event.pull_request.head.sha }}` and the range is measured to it; by hand there is no such commit and the fallback is `HEAD`, the branch's own tip. Four cases and seven mutations cover it, including one that threads the head into only one of the two commands |
| S10 · the document that owns tracker conventions says what reads a milestone and what a wrong one now costs | `docs/issues-and-milestones.md#"## One thing reads a milestone, and it can stop a release"@9d4046d6` | **Executed** 2026-09-11. `grep -rn "Nothing automated reads a milestone" docs/ skills/ agents/` returns nothing (exit 1); before this work it returned the heading of the section that has been rewritten. The phrase survives only in earlier work items' `seal/specs/*/spec.md` and `overview.md`, which are records of a moment and are not rewritten | 2026-09-11 | The heading changed as well as the body, because the old one was the claim. `survivor_check.py --range origin/release/v0.11.1...HEAD` reports no removed wording still standing, over 874 files and 8 removed sentences. **That sentence is qualified, 2026-09-12, by round 2 of #361**: `corrected` counts a review record's verbatim quotation of removed wording as wording the range wrote, so a range carrying its own round records can report zero survivors it would otherwise report. The claim above does not rest on it — it rests on the grep, which is unaffected — but the survivor reading is not evidence until **#365** lands AND the added-side exclusion beside it does, which round 3 of #361 measured are two changes and not one. **Re-verified 2026-09-11 in round 1's fix pass; the claim is unchanged and one sentence in the section is not.** The section pointed a reader at `docs/release-checklist.md` step 0's *first box on the list*, and the milestone box is the third of four — the boxes stand at lines 14, 18, 40 and 50, counted. The repair is content-addressed rather than positional: it names the box by what it asks, so the next box added to step 0 cannot make it wrong again. The order was left alone on its own grounds — boxes 1 and 2 confirm what arrived, and a milestone cannot be checked against a release until you know what is in it |
| S9 · `docs/release-checklist.md` names the gate where the deleted bullet stood, and §6 states the trigger and the input the tree actually has | `docs/release-checklist.md#"## 0. Before starting"@f3cc2de3`, `docs/release-checklist.md#"## 6. After the merge"@761f59ff` | **Executed** 2026-09-11. `grep -n "on the tag" docs/release-checklist.md` returns nothing (exit 1). The sentence it removes claimed the close-issues workflow "runs on the tag and closes every issue the changelog section names"; both halves are false against the tree — `close-issues-on-release.yml` triggers on `push: branches: [main]` and `close_issues_on_release.py` reads pull request bodies. **Re-read 2026-09-22 for #386 and #417**, which added two boxes to §6 — the release note exists, the plugin directory carries the version — and moved this unit's hash. The claim holds unchanged and its own executed check reproduces: `grep -n "on the tag" docs/release-checklist.md` still returns nothing (exit 1), and the paragraph stating the trigger and the input is untouched word for word — the edit was 36 pure insertions and altered no existing line, which is why §0's anchor beside this one did not drift at all. The one thing worth naming, because it is the shape this claim exists to refuse: the new release-note box says *the tag push fires* a workflow. That is `publish-release.yml`, a different workflow, named in the box and distinguished from the close-issues one by the paragraph immediately below it, which still says that fires when `main` moves | 2026-09-24 | The replacement says the paragraph used to say the opposite, because a reader who acted on the old sentence has no other way to learn they were wrong. Step 0's new box is the first in the list on purpose: it is the act that keeps the gate from firing at the worst moment **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold added the update-notice paragraph under its marker, after the two new boxes. It states no trigger, `grep -c "on the tag" docs/release-checklist.md` still returns 0, and the close-issues paragraph is untouched, so the claim holds. **Re-read 2026-09-23 by work item 1790119502 (#514)**, which rewrote one clause of the directory box: the open question it names is now answered by the repository owner instead of pointing at the work item that built the box, whose directory this fold retires. No trigger or input moved, `grep -c "on the tag" docs/release-checklist.md` still returns 0, and the claim holds. **Re-read again 2026-09-23** in round 2's fix pass of the same work item, which rewrote the release-note box's failure sentence to name every direction the job fails in — a missing changelog section, a `v*` tag that is not `vX.Y.Z`, a failed `gh` call. No trigger or input moved, `grep -c "on the tag"` still returns 0, and the claim holds. Re-read 2026-09-24 by work item 1790208643 (#526), phase 3: the squash step's merge sentence gained one sentence naming `docs/the-evidence-ledger.md` §*A correction a merge dropped* for a ledger conflict; the claim holds. **Re-read 2026-09-24 for #572**, which added a sentence to §6's release-note box — the note's summary and `### 🙌 Thanks to` line, and the log line when the pull requests cannot be listed — and moved this unit's hash. The claim holds unchanged; nothing it cites was edited. Re-read 2026-09-25 by work item 1790263216 (#565), phase 5: §6's closing statement gained an `Enforced by:` line after its last box, and the trigger and input sentences are unchanged; the claim holds. |

<!-- specs/1789172128-a-release-is-sized-by-a-count-and-cut-by-urgency -->
### 1789172128-a-release-is-sized-by-a-count-and-cut-by-urgency

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · what decides a release's size is stated in exactly one document, and what it states is a **criterion** — what has to be in effect before the next work item starts — with three or four named as a ceiling rather than a target | `docs/issues-and-milestones.md#"## A milestone answers *when*, and takes three shapes"@771925cf`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_rule_states_the_criterion_and_not_the_count@6b1ead4c`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_count_survives_as_a_ceiling_in_the_wording_the_cap_already_uses@06f26b32`, `tests/test_a_release_is_sized_by_a_criterion.py#test_one_document_states_a_releases_size@1b41716f`, `tests/test_a_release_is_sized_by_a_criterion.py#hits@d323992a` | **Executed** 2026-09-12. The sweep, `git grep -niE "three or four\|sized in work items\|release is sized"` over `docs`, `skills`, `agents`, `templates`, `tests` and the three root documents, returns lines in this one file both before and after the edit, and `git grep -n "is the size"` over the same set exits **0** after it, with every hit inside `tests/test_a_release_is_sized_by_a_criterion.py` — the module that names the replaced wording in its own docstring and constants, and the one file the sweep excludes — **by basename rather than by path**, so a copy of it under another directory is excluded with it. **This row said exit 1 and round 1 measured 0**; the recorded claim was the intent of the check rather than what the command answered, which is the one thing a ledger row may not be. The four cases were seen **red** before the wording landed — the document reverted to `6de3f54` and the module run, 5 failed 2 passed — and green after, 7 passed. The two that stayed green are the sweep and its own can-fail case, and each was then broken on its own: a second statement of a size added to `docs/release-checklist.md` failed the sweep, and a dead pattern failed the can-fail case. **Round 2 found the sweep's own reach unpinned and it is STILL unpinned**: each of round 1's three widenings reverts with the module green. Two cases were added and each was seen red under exactly its own mutation and was the only failure — the three noun forms against the pattern, and `CLAUDE.md` against `tracked()` — and then both were **reverted**, because `round_record.py close` refused them at depth 2 and the repository owner's answer was to take that gate's exit. The reach ships unpinned and the pinning is **#366**. Round 3 found this cell claiming the opposite of its own `Notes`, which is the third time this row has carried a sentence the tree contradicts. `hits()` is anchored here but **not pinned by a case**: it was created by round 1's fix pass, so a case for it is depth 2 and refused, and reverting its joined-line branch leaves the module green. That gap is a row in `seal/follow-up.md` | 2026-09-22 | **Two coordinates were removed from this row rather than re-pointed**, on 2026-09-12: the two cases pinning the sweep's pattern and its scanned set were reverted when `round_record.py close` refused them at depth 2, and a row whose anchor a change removes is REMOVED at that anchor rather than aimed somewhere else. The claim is unchanged and still rests on the five coordinates above, the sweep among them; what went with those two is the pinning of the sweep's REACH, which is **#366**. **This row replaces S3 of `seal/ledger/1789100139-…md`**, removed there rather than re-pointed, per `CONTRIBUTING.md` §*Changing cited code is the case the rule has to answer*. That claim named the rule's own wording — *sized in work items, three or four* — so the claim went with the sentence even though the heading it was anchored to survived. What carried over is its substance, which is the one-owner half, and that is what the sweep case now pins. The row lived in **another work item's** fragment, and nothing said whether a later work item may edit one; both fragments already sit in `release/v0.11.1`, the fragment rule is about two concurrent branches not sharing a file, and leaving a false row to be folded into `seal/ledger.md` at the release was the alternative **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The sizing paragraph's clause about the reopening bound turning every open finding into an issue was corrected to the ladder, and one line was rewrapped to the 88-column limit. The criterion and *three or four is a ceiling* are both untouched — what the correction removed was an aside quoting the old filing rule — so the claim holds. **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold's side of this section is one `<!-- specs/<id> -->` marker comment; no sentence changed, so the claim holds and the hash is both sides' edits together. **Re-read 2026-09-24 by work item 1790173209 (#363).** The paragraph under this heading about why the two releases are named rather than numbered was reworded for the repaired off-by-one; the criterion and the one document stating it are untouched, so the claim holds. |
| R3 · the two releases the rule cites are named in prose because the check refuses both numbers in a loaded file, and one of the two has already shipped | `tests/test_release_hygiene.py#timers_in@608ccc2e`, `docs/issues-and-milestones.md#"## A milestone answers *when*, and takes three shapes"@771925cf`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_two_releases_are_cited_without_a_version_number@b62f22f5` | **Executed** 2026-09-12 in phase 1, by a `test_tmp_*` probe calling `timers_in("docs/issues-and-milestones.md", text, version())` with one token at a time and deleted afterwards. At the running `0.11.0`: `0.11.0`, `v0.11.0`, `0.11.1` and `v0.11.1` are each refused; `0.8.3` and the illustrative `1.2.3` are kept. `CHANGELOG.md` records `0.11.0` as released and `git tag --list v0.11.*` answers `['v0.11.0']`, so one of the two refused numbers names a release this repository has demonstrably shipped | 2026-09-22 | **The refusal outlives its own reason by one release, and that is #363 rather than this work item.** `timers_in`'s docstring argues the ceiling from *below `running` is history and is kept*, and `plugin.json` is bumped at the release-preparation commit, so from that commit until the next bump the running version is the one that already shipped. Q1 chose the prose citation and filed the off-by-one instead, on the ground that widening a checker so a document may say something is the easiest way to green. The paragraph in the document says why the numbers are not written, because once 0.11.2 ships both become history and a later author would otherwise replace the descriptions with numbers and be right to **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The sizing paragraph's clause about the reopening bound turning every open finding into an issue was corrected to the ladder, and one line was rewrapped to the 88-column limit. Both releases are still named in prose and the correction added no version number, so the claim holds. **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold's side of this section is one `<!-- specs/<id> -->` marker comment; no sentence changed, so the claim holds and the hash is both sides' edits together. **Re-read 2026-09-23 by work item 1790173209 (#363).** The off-by-one this row filed is repaired: a tagged version is history to `timers_in` now, so the release that had shipped could be numbered. The paragraph was reworded to say so and the citations stay prose — a description reads the same on every release — so the claim's first half holds and its grounds moved from *the check refuses both* to *a number would be one more line to re-read*. |
