## 0.15.3 — 2026-09-24

<!-- specs/1790260563-the-fold-checks-run-only-as-this-repositorys-tests -->
### 1790260563-the-fold-checks-run-only-as-this-repositorys-tests

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `settle` labels an anchored ledger row by its first cell, and what stands before the row's first pipe is dropped when it is container syntax only — any run of `>`, `-`, `*`, `+`, `1.`/`1)` and `<!--`, in any combination and spaced or not — and kept as the label otherwise, so `> - \| claim \|` is named `claim` and `see this row \| claim \|` keeps `see this row` | `skills/settle/scripts/settle.py#first_cell@b785771c`, `skills/settle/scripts/settle.py#CONTAINER_RE@890dc433`, `tests/test_settle_reads_before_it_removes.py#test_a_row_inside_container_syntax_is_named_by_its_claim@351744d3`, `tests/test_settle_reads_before_it_removes.py#test_a_quoted_list_row_is_named_by_its_claim_in_the_report@078abc9f`, `tests/test_settle_reads_before_it_removes.py#test_a_prefix_that_is_not_container_syntax_keeps_todays_label@e4ba9a94` | **Executed** 2026-09-24. At `9ff20d3f` the eight container cases were red, `<!-- > \|` labelled `>`, and `settle --retire` printed the row's label as `> -`; the four keep-today cases were green before and after. Mutated at `2e7ed5c7`: bullets out of the pattern 4 red, ordered markers out 2 red, `>` out 6 red, the old blank-cell arm back 9 red | 2026-09-24 | #530. The comment-opener strip above the split is kept, so `<!-- prose \| claim \|` is still labelled `prose` as before; folding the opener into the pattern alone would have changed that label |
| F1 · `fold-check`'s shape check: a fold marker group holding a work-item id at or above the cutoff heads a statement that opens bold and carries exactly one live `Enforced by:` line whose targets each resolve to a file inside the repository and, with `::name`, to a `def` or `class` found by `ast`, or which says `nothing — <why>` with a reason; stacked markers share one statement, which ends at the next marker, heading or end of file; the cutoff is a parameter, and `0` binds every statement | `skills/settle/scripts/fold_check.py#numbered_statements@35e27650`, `skills/settle/scripts/fold_check.py#statements@8f4adc99`, `skills/settle/scripts/fold_check.py#bound@0bbf13a2`, `skills/settle/scripts/fold_check.py#names_targets@ab1a8520`, `skills/settle/scripts/fold_check.py#target_problem@02164c80`, `skills/settle/scripts/fold_check.py#shape_problems@dce99d1e`, `skills/settle/scripts/fold_check.py#HEADING@5040ce15`, `skills/settle/scripts/fold_check.py#BOLD_OPENING@dc7810e2`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_shape_from_0_binds_a_statement_folded_before_any_cutoff@92f3d8ee` | **Executed** 2026-09-24. Moved from `tests/test_a_folded_statement_names_what_enforces_it.py` as they were (0.14.0's S1 carried their mutation record), and the planted cases now call the shipped functions, unchanged in what they assert. Mutated at `0a739888`, each red in the pins: `bound` inverted 17, `names_targets` always true 4, the live filter off 1 | 2026-09-25 | Replaces 0.14.0's S1, removed there because every unit it anchored left `tests/`. An empty `Enforced by:` value is still read as targets and reported as `an empty target`, as before the move. On this tree `--shape-from 0` reads 136 statements in 14 documents and names 115, each carrying no `Enforced by:` line; 21 are bound at `1790154761` **Re-read 2026-09-25 in round 1's fix pass**: a `::name` target whose file will not parse or decode is no longer a traceback; it stops the run at exit 2 naming the statement, the target and the reason (row G1). The shape rules this row states are unchanged |
| F2 · `fold-check`'s ceiling: a top-level `docs/*.md` not listed stays at or under the ceiling; a listed one must still exceed it, must exist, and must carry exactly its frozen marker count and, at that count, the marker ids whose digest the listing holds | `skills/settle/scripts/fold_check.py#ceiling_problems@aa408cfa`, `skills/settle/scripts/fold_check.py#markers@4238e018`, `skills/settle/scripts/fold_check.py#marker_digest@ba89003d`, `skills/settle/scripts/fold_check.py#documents@e87539f1` | **Executed** 2026-09-24. Moved from `tests/test_a_document_has_room_for_the_next_fold.py` as they were (0.14.0's P1 carried their mutation record); the planted cases call the shipped functions. Mutated at `0a739888`: the ceiling compared with `>=` 2 red. `marker_digest` without its `sorted` stays green — no fixture reorders markers — which the overview records as not done | 2026-09-25 | Replaces 0.14.0's P1. **Re-read 2026-09-25 in phase 3**: the two messages that named the `FROZEN_IDS_DIGEST` constant now name the `Over the ceiling` row and print the digest the file has now; each pinned case asserts the new sentence with the digest in it, and printing the entry's stale digest instead reddens 3 and 1 of them **Re-read 2026-09-25 in round 1's fix pass**: a listed path that exists below the top level is named as not a top-level `docs/*.md` rather than as missing (row G1); a document that is not UTF-8 stops the run at exit 2. The ceiling rules this row states are unchanged |
| F3 · `fold-check` exits 0 with nothing found, 1 with each problem on a line of its own, and 2 with nothing checked for a root that is not a directory, a root with no `docs/` when a check is declared, or a ceiling or cutoff that is not a number in range; it prints how many statements it read and how many the cutoff binds, and says when a check was not declared | `skills/settle/scripts/fold_check.py#main@ae412dc3`, `skills/settle/scripts/fold_check.py#run@a5e4b710`, `skills/settle/scripts/fold_check.py#positive@5c1f3dd3`, `skills/settle/scripts/fold_check.py#not_negative@1b005db9`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_the_command_names_a_bound_statement_with_no_line_and_exits_1@eb8931fb`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_the_command_exits_2_with_nothing_checked_on_an_unusable_root@d18f0ec4`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_command_names_a_document_over_the_ceiling_and_exits_1@fa81bebe`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_command_refuses_a_ceiling_that_is_not_a_positive_integer@c07616fe` | **Executed** 2026-09-24. `bin/fold-check --shape-from 1790154761 --ceiling 1000; echo $?` printed 136 statements, 21 bound, 14 documents, and 0. **Re-read 2026-09-25 in phase 3**: `main` now reads the rows first and a flag overrides its row; the not-declared lines name the row and the file (row F5), and the exit codes are unchanged. Mutated at `0a739888`, each red: exit 0 on problems 3, the binds count inverted 1, the `docs/` refusal off 1, the not-a-directory refusal off 1, a ceiling of 0 accepted 1 | 2026-09-25 | **Re-read 2026-09-25 in round 1's fix pass**: with no `--root` the command resolves the repository through `hooks/optin.py#repo_root`, and exit 2 now also covers a file the run had to read and could not, with the run's lines held so nothing printed reads as a result (row G1). Exits 0 and 1 are unchanged |
| F4 · `fold_check.py` refuses below the interpreter floor before `HERE` is computed or any argument is read, with the block copied from `round_record.py` naming `fold-check` | `skills/settle/scripts/fold_check.py#below_floor@5a9dd75e`, `tests/test_a_script_says_which_interpreter_it_needs.py#test_fold_check_refuses_before_anything_is_read_below_a_raised_floor@66f78e56`, `tests/test_a_script_says_which_interpreter_it_needs.py#test_fold_checks_guard_block_is_the_one_round_record_says_to_copy@45dd630f` | **Executed** 2026-09-24: the sentence changed to `settle:` at `0a739888` reddens both cases | 2026-09-24 | |
| F5 · `fold-check` reads its values from three optional `seal/config.md` rows through `hooks/config.py#config_rows`, under the root `hooks/optin.py#home_at` resolves: `Fold shape from` (a whole number), `Document line ceiling` (a positive whole number) and `Over the ceiling` (`none`, or `;`-separated entries `<path> frozen at <n> markers <12-hex digest> until <home>`); an absent or empty row is not declared, skips its check and says so naming the row and the file, neither declared checks nothing at exit 0, a value that will not parse exits 2 naming the row and the value with nothing checked, and `--shape-from` or `--ceiling` overrides its row for one run | `skills/settle/scripts/fold_check.py#declared@4aac689d`, `skills/settle/scripts/fold_check.py#parse_over@b17ea93a`, `skills/settle/scripts/fold_check.py#row_value@d7071d60`, `skills/settle/scripts/fold_check.py#config_rows@bfc22bab`, `skills/settle/scripts/fold_check.py#seal_home@682452e3`, `skills/settle/scripts/fold_check.py#OVER_ENTRY@7c3527b7`, `skills/settle/scripts/fold_check.py#main@ae412dc3`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_root_that_declares_neither_value_checks_nothing_and_says_so@575d2e39`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_row_with_an_empty_value_is_not_declared@bec9415a`, `tests/test_a_document_has_room_for_the_next_fold.py#test_one_absent_row_skips_its_check_and_says_which@01b3c2ef`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_row_that_will_not_parse_is_named_and_nothing_is_checked@fee7ce86`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_flag_overrides_its_row_for_one_run@f0ce8070` | **Executed** 2026-09-25. `bin/fold-check; echo $?` with no flags on this tree: 136 statements, 21 bound at `1790154761`, 14 documents held to 1000 lines, 0 listed, and 0. In a bare directory with `docs/` and no root it printed the neither-declared line and 0. Mutated at `e79c64fa`, each red: the cutoff parse off 5, the ceiling parse off 1, the entry parse off 1, the flag override off 2, no root resolved 11, the empty-value arm off 1 (red only after `test_a_row_with_an_empty_value_is_not_declared` was added, which the mutation first showed was missing) | 2026-09-25 | The root is `home_at`'s rather than a copy of `broad_gate.py#seal_home`, so a repository that opted out with the scratch marker reads as having no rows. A digit is ASCII `[0-9]`, so a Unicode digit `isdigit` would take is refused **Re-read 2026-09-25 in round 1's fix pass**: `seal_home` is now `located`'s first half, and a repository that opted out is told so rather than that it has no root (row G1); the rows and their parsing are unchanged |
| F6 · this repository declares `Fold shape from` `0` (`1790154761` until #565), `Document line ceiling` `1000` and `Over the ceiling` `none`, and no top-level document under `docs/` is over the ceiling; `docs/the-evidence-ledger.md` states the same cutoff, ceiling and empty listing, and the prose pin reads the rows through the command's own reader and fails when either side moves alone | `tests/test_a_document_has_room_for_the_next_fold.py#prose_disagreements@f4a1db8e`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_evidence_ledger_states_the_values_the_config_rows_hold@cd561d1d`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_prose_pin_fails_when_either_side_moves_alone@47a1d760`, `tests/test_a_document_has_room_for_the_next_fold.py#test_every_document_in_docs_is_under_the_ceiling_or_frozen@fcc757d5`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_every_bound_statement_in_docs_has_the_shape@7f6071fc` | **Executed** 2026-09-25 at `e79c64fa`, one real file edited at a time and restored: the config cutoff alone, the config ceiling alone, the config listing a document alone, the prose cutoff alone and the prose ceiling alone each reddened the prose pin | 2026-09-25 | Replaces 0.15.1's S1, removed there because its anchors `OVER_CEILING`, `FROZEN_IDS_DIGEST` and the old prose pin left `tests/`. `docs/review-chain-spec.md`'s split along its own headings by MichaelYcJo/SpecSeal#526, which that row recorded, stands in the evidence-ledger's ceiling statement. The pin case was renamed from `…_these_constants_hold` because no constant is left to hold. **Corrected 2026-09-25 by work item 1790263216 (#565), phase 7:** the claim said the row declares `1790154761`; #565 lowered it to `0` once every statement under `docs/` carried its line, and `docs/the-evidence-ledger.md` states `0` with it, so the pin still holds the two to one number. The ceiling and the empty listing are unchanged **Re-read 2026-09-25 in round 1's fix pass**: the real-tree case walks `fold_check.documents` rather than a second listing, and the evidence-ledger's cutoff statement now names it beside `fold_check.py::bound`; the values and the pin are unchanged Re-read 2026-09-25 at the merge of #587's final tip into #565: the unit carries both sides' edits, each note above describes one, and they touch different sentences, so the claim holds; re-stamped after the merge. |
| F7 · the wrap limit over the covered documents skips a line exactly when `fold_check.enforced_lines` names it: a folded statement's `Enforced by:` line of targets, whatever the cutoff and whatever its width; a `nothing — <why>` line, an `Enforced by:` line outside any statement (after a heading, or with its marker in a fence) and the line after a skipped one are still prose | `skills/settle/scripts/fold_check.py#enforced_lines@ac34d277`, `skills/settle/scripts/fold_check.py#names_targets@ab1a8520`, `tests/test_docs_line_wrap.py#prose_lines@321aec00`, `tests/test_docs_line_wrap.py#test_an_enforced_by_line_with_targets_is_skipped_and_the_next_line_is_not@5a93a127`, `tests/test_docs_line_wrap.py#test_an_enforced_by_line_the_reader_does_not_read_is_still_prose@85e2afc6` | **Executed** 2026-09-25. The skip case red at `a34fd12d` against the old `prose_lines` (line 5 yielded as prose), the not-read case green before and after, which is what it pins: the width of the skip. Mutated at `5c6fdb71`, each red: every statement line skipped 2, `nothing` lines skipped too 1, the skip off 1, the skip one line wide 1 | 2026-09-25 | #583. It adds no file to `COVERED`. Measured 2026-09-24 by the framer: 3,232 of the 3,490 `path::test` pairs under `tests/` exceed 88 columns as a single target after the prefix, so wrapping between targets could not have fit one target, and a join would add a continuation grammar to settle §2's *exactly one line* |
| F8 · the documents that describe the fold say the plugin ships `fold-check` and sets no value: settle §2 says it reads the shape and the ceiling and that a repository states the cutoff and the ceiling as `seal/config.md` rows, a row it does not write being a check it does not run, and no longer says the plugin ships no checker; settle's fold-branch section and the release checklist's step 2b run it after the prose is written and before `settle --retire`; the evidence-ledger's ceiling statement says the values are rows the command reads and names the prose pin among what enforces it | `` skills/settle/SKILL.md#"### 2. Write one standing statement per segment"@1185a277 ``, `` skills/settle/SKILL.md#"## What a fold branch owes"@9473f3df ``, `` docs/release-checklist.md#"## 2b. Settle what the release leaves behind — by hand, and not in that commit"@f314f1db ``, `tests/test_a_folded_statement_names_what_enforces_it.py#test_settle_owns_the_shape_rule@b3a92024` | **Executed** 2026-09-25. `test_settle_owns_the_shape_rule` red against the old §2 (the new phrase absent), and red again with the old sentence put back beside the new one; green with the edit. The 53 test modules that read a document this phase edited were run and are green, after two corrections: this case's phrase took the line break the document has, and the records naming units this work item removed were marked `NAME NOT IN TREE` | 2026-09-25 | Nothing pins the checklist's `fold-check` step or the fold-branch paragraph by text; review reads them. The README cheat sheet carries one row per edition, and `tests/test_docs_line_wrap.py` skips table rows |
| G1 · `fold-check` reads the repository the current directory is in when `--root` is absent; a `::name` target whose file will not parse or decode, and a `docs/*.md` that is not UTF-8, stop the run at exit 2 with one line naming the file and the reason (`SyntaxError at line N`, `not UTF-8`) and nothing on stdout, never a traceback and never exit 1; a run with no rows says which of two states it is in — no `seal/` root at either place, or a repository that opted out with `specseal-scratch`; a listed path that exists below the top level is named as not a top-level `docs/*.md`; a missing sibling script is refused naming what it is for | `skills/settle/scripts/fold_check.py#located@2d473952`, `skills/settle/scripts/fold_check.py#optin@2a3829e7`, `skills/settle/scripts/fold_check.py#why@2bfb273a`, `skills/settle/scripts/fold_check.py#read@550c34f7`, `skills/settle/scripts/fold_check.py#load@9b6d4b98`, `skills/settle/scripts/fold_check.py#target_problem@02164c80`, `skills/settle/scripts/fold_check.py#main@ae412dc3`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_command_typed_in_a_subdirectory_reads_the_repository@80dd2f12`, `tests/test_a_document_has_room_for_the_next_fold.py#test_an_opted_out_repository_is_told_it_opted_out@38bd13b7`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_repository_with_no_root_is_told_it_has_none@3fb02c2d`, `tests/test_a_document_has_room_for_the_next_fold.py#test_a_listed_document_below_the_top_level_is_named_as_outside_it@58aa70ab`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_a_target_file_that_will_not_parse_exits_2_naming_it@9fd5aa05`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_a_document_that_is_not_utf8_exits_2_naming_it@69942cc4`, `tests/test_a_folded_statement_names_what_enforces_it.py#test_a_script_copied_on_its_own_says_which_sibling_it_misses@ec931689` | **Executed** 2026-09-25. Every new case red at the unfixed code, whose `fold_check.py` is the target SHA `e9dfe623`'s (8 red; the no-root pin green before and after, since it holds the state the split must leave). Mutated at `0bbf219f`, each red: the `repo_root` arm off 1, the opt-out arm off 1, the target's except narrowed to `OSError` 2, the document's except narrowed 2, `main`'s catch off 4, the lines not held 1, `why` without its decode arm 3 and without its syntax arm 1, the exists arm off 1, the statement prefix dropped 2. `bin/fold-check` typed in this repository's `docs/` read 136 statements and exited 0 | 2026-09-25 | Round 1's 🟡 1–4 and ⬜ 7 (MichaelYcJo/SpecSeal#587). The coordinator decided the unparseable target is exit 2 rather than a named problem, so a target file this Python cannot parse stops a run rather than counting as a shape problem |

<!-- specs/1790260564-a-moved-file-counts-as-written -->
### 1790260564-a-moved-file-counts-as-written

| G7 · the reader in `corrected` that holds a gathered fragment's text reads every path `a_gathered_fragment` accepts for a gathered `<id>` in the tree at the range's left end, so the fragment the predicate keeps out of the range is the fragment whose text is held; the path has one spelling, the predicate's | `skills/code-review/scripts/survivor_check.py#corrected@e3c7e3f3`, `skills/code-review/scripts/survivor_check.py#a_gathered_fragment@17739bce`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_fragment_at_the_other_spelling_is_held@94290961`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_every_path_list_this_module_derives_from_git_is_filtered_or_named@3838478c` | **Executed** 2026-09-24. G7 seen red at `c52e8350`, exit 0 with `against 2 sentence(s)`, a fragment at `specs/<id>/changelog.md`; green after. The path-list case went red on the change, naming `tracked`'s second caller, and was reclassified: `tracked` maps each caller to the filter it applies | 2026-09-24 | One `ls-tree` more per run, taken only when the tip's `CHANGELOG.md` carries a marker. What the predicate accepts is unchanged (`<anything>/specs/<id>/changelog.md`); narrowing it is `spec.md` §*Out* **Re-read 2026-09-25 by work item 1790297084 (#592), phase 1**: `paired_across_paths` now takes each key's pairs by the departure path's affinity to the arrival path, then by the departure's path being gone at `b`, then by path order, and `corrected` passes it the paths read at `b`; the pairing is still one for one after every path is counted, and only which departure stays the source can change, so the claim holds **Re-read 2026-09-25 by work item 1790297084 (#591), phase 2**: `corrected` now reads a retired directory at the left end, lets its sentences take part in the pairing across paths, and drops them from the removed wording after it rather than from the path list before it; nothing this row claims depends on where that drop sits, and the claim holds **Re-read 2026-09-25 by work item 1790297084 (#603), phase 3**: after the pairing across paths `corrected` now also drops the sentences of a ledger row the range removed because an anchor of the row left the code (`removed_ledger_rows`), beside the retired-directory drop; nothing this row claims reads a removed ledger row, and the claim holds |
| G8 · in the root `CHANGELOG.md` a version heading opens a released region and, after it, only another version heading or an `Unreleased` heading (`## Unreleased`, `## [Unreleased]`, any case) changes the region; a `## ` line of any other kind inside a released section stays released, and `## Unreleased` below a release stays live. `blank_released` and `only_released` both read the one rule, so they stay exact complements | `skills/code-review/scripts/survivor_check.py#released_lines@a98c0509`, `skills/code-review/scripts/survivor_check.py#UNRELEASED_HEADING@1a012446`, `skills/code-review/scripts/survivor_check.py#blank_released@4a6ce6aa`, `skills/code-review/scripts/survivor_check.py#only_released@417ace0e`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_fragment_carrying_a_heading_stays_released@f058968c`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_unreleased_section_below_a_release_is_still_a_carrier@049314b6` | **Executed** 2026-09-24. G8 seen red at `c52e8350`, exit 0 with `against 2 sentence(s)`: the fragment's text after its `## Notes` line was live and written. G9 green there, and red with the `Unreleased` arm deleted from `released_lines` (the file restored from a copy afterwards) | 2026-09-24 | The rule reads headings and never the gather's marker, so a misspelled marker cannot reopen the region. Its failure scenario: a changelog keeping live prose under another name below a version heading has it read as released, and a survivor there goes unreported. No convention this plugin names puts live prose there |
| G10 · `read_blobs` returns every committed text with `\r\n` turned into `\n`, so every reader after it — `MARKER` under `re.M`, the region rule's `split("\n")` — reads a CRLF file as it reads an LF one; line counts are unchanged, and a lone `\r` is left alone | `skills/code-review/scripts/survivor_check.py#read_blobs@7c70d4b4`, `skills/code-review/scripts/survivor_check.py#MARKER@1ed4fe02`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_crlf_changelog_still_has_its_gathered_ids@af663330` | **Executed** 2026-09-24. G10 seen red at `c52e8350`, exit 0 with `against 2 sentence(s)`: the CRLF marker line gave no gathered id. The case sets `core.autocrlf=false` in its fixture repository and asserts the committed blob holds `\r\n` after the marker, so a runner that converts line endings cannot turn it into an LF case that measures nothing (`agent-contract` §13) | 2026-09-24 | One boundary rather than `\r?$` in `MARKER`, so the next `$`-anchored reader inherits it (§12). `read_exemptions` opens its file in text mode, which already applies universal newlines |
| M1 · a sentence the range removed at one path and added verbatim at another — the same key, paired one for one after every path is counted — is neither removed nor written, so text a range moved (a file moved whole, a rename, a document split into two that both remain) is held the way gathered text is, and a pure move removes nothing and reports `against 0 sentence(s)` | `skills/code-review/scripts/survivor_check.py#paired_across_paths@53cabf4b`, `skills/code-review/scripts/survivor_check.py#corrected@e3c7e3f3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_file_moved_whole_still_reports_the_quote_it_carried@4e76d171`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_split_document_still_reports_the_quote_it_moved@6434ce80`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_file_moved_verbatim_is_silent_because_it_removes_nothing@c4423f1a` | **Executed** 2026-09-24. M1 and M2 seen red at `fe4573d7` (phase 1's tip, whose `corrected` counts moves as the base did), each exit 0 with `against 3 sentence(s)`; the rewritten S18 red there at `against 43 sentence(s)`. Green after. Mutated from Python over the whole module, the file restored from a copy: the pairing call removed, M1, M2 and S18 red; the paired fresh copies still written, M1 and M2 red | 2026-09-24 | Pairing leaves a sentence out of `gone` only when all its n-grams are written anyway, so `wanted` can only grow; a larger `wanted` can join two runs, and one run scores by its rarest n-gram — the score the text gets had it not moved (`plan.md`'s failure scenario). Measured: #526's split `82928fc6` removes 245 sentences where it removed 1291 and reports the same three places at the same scores; the four `RELEASE_RANGES` commits remove 48, 60, 35 and 120 where they removed 60, 61, 35 and 156, and report the same coordinates at the same scores **Re-read 2026-09-25 by work item 1790297084 (#592), phase 1**: `paired_across_paths` now takes each key's pairs by the departure path's affinity to the arrival path, then by the departure's path being gone at `b`, then by path order, and `corrected` passes it the paths read at `b`; the pairing is still one for one after every path is counted, and only which departure stays the source can change, so the claim holds **Re-read 2026-09-25 by work item 1790297084 (#591), phase 2**: `corrected` now reads a retired directory at the left end, lets its sentences take part in the pairing across paths, and drops them from the removed wording after it rather than from the path list before it; nothing this row claims depends on where that drop sits, and the claim holds **Re-read 2026-09-25 by work item 1790297084 (#603), phase 3**: after the pairing across paths `corrected` now also drops the sentences of a ledger row the range removed because an anchor of the row left the code (`removed_ledger_rows`), beside the retired-directory drop; nothing this row claims reads a removed ledger row, and the claim holds |
| M6 · the pairing is one for one in both directions: where a range moves one copy of a sentence and removes another, one is held and the other is still removed; where it moves one copy and writes a second, one is held and the other is written as the range's own | `skills/code-review/scripts/survivor_check.py#paired_across_paths@53cabf4b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_move_holds_only_as_many_copies_as_it_carried@5b73e34d`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_copy_written_beyond_the_ones_moved_is_the_ranges_writing@5f3ba793` | **Executed** 2026-09-24. Both counts in `paired_across_paths` were first mutated with M1–M3 in place and every case of the module stayed green, so each was unpinned. M6 then red with the removal count left undecremented (exit 0) and at `c52e8350`; M7 red with the arrival count left undecremented (exit 1, both copies reported). The file restored from a copy after each | 2026-09-24 | Found by mutating the unit rather than planned: `spec.md` names M1–M5 and no case for either count **Re-read 2026-09-25 by work item 1790297084 (#592), phase 1**: `paired_across_paths` now takes each key's pairs by the departure path's affinity to the arrival path, then by the departure's path being gone at `b`, then by path order, and `corrected` passes it the paths read at `b`; the pairing is still one for one after every path is counted, and only which departure stays the source can change, so the claim holds |
| O1 · a `survivors.md` whose real path is under the real path of `<git-common-dir>/seal/specs/` (the common directory read by `hooks/optin.py#git_common_dir`) is a local-mode declaration, and it is its work item's when the range's tip `b` is `refs/heads/<Branch>` or an ancestor of it and is on no other local branch whose head is an ancestor of `<Branch>` — a branch the declared one was cut from — `<Branch>` read from that work item's `routing.md` through `hooks/routing.py#parse`; the question is asked only of a declaration that would have matched, and shared mode keeps the diff test | `skills/code-review/scripts/survivor_check.py#whole_range@cc77852a`, `skills/code-review/scripts/survivor_check.py#local_specs@3b0fc79a`, `skills/code-review/scripts/survivor_check.py#hook@2b603cc5`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_stacked_childs_declaration_does_not_reach_its_parents_range@7dfb861d`, `skills/code-review/scripts/survivor_check.py#local_item@cd229ab7`, `skills/code-review/scripts/survivor_check.py#on_its_branch@537eaffb`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_local_mode_work_item_owns_its_own_range_row@22038dd7`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_local_mode_declaration_does_not_reach_another_branch@e260f655` | **Executed** 2026-09-25. O1 seen red at `502952ce` (phase 2's tip, whose `whole_range` is the base's) in the main and the linked worktree, exit 1 with the row under `not yours`; green after in both and through a symlink. Mutated from Python, the file restored from a copy after each: ownership reduced to `True`, O2 alone red; `local_item` answering None, O1 main and linked red; the common directory not joined onto `root`, O1 main alone red (git answers `.git` there); `abspath` for `realpath` on the file, O1 symlinked alone red | 2026-09-25 | The failure scenario is a reused branch name: a second work item on the same branch name, with the first one's local `survivors.md` still on disk, claims that branch's ranges too. The first anchor still binds, so the exposure is a relation spelling re-resolved on a reused name. **Corrected 2026-09-25 in round 1's fix pass (🟡 1, ⬜ 6)**: the claim said the tip being on `<Branch>` or an ancestor of it is enough. Round 1 executed a stacked child's row excusing its parent branch's whole run, because the parent's tip is an ancestor of the child's branch; the claim now carries the cut, and O6 is its case (red at `6e48cb5f`, exit 0 excused). Once the cut is refused, two exposures remain: the reused name above, and a parent's run from a detached HEAD with no local head naming the parent, which a stacked child's row excuses because it is the same range as the child's own run at an older tip (round 2's ⬜ 4, executed). Corrected 2026-09-25 by the orchestrator after round 3's ⬜ 3. The common directory is now read by `hooks/optin.py#git_common_dir`, loaded by path through `hook`, where `local_specs` had its own copy of that reader |
| O4 · a local-mode declaration whose `routing.md` is missing or names no branch, or whose branch does not resolve, is not its work item's: it excuses nothing and prints under `not yours`, and the line's reason is the test that refused it, returned by `on_its_branch` in local mode — *it has no routing.md this run can read*, *its routing.md is not a declaration naming a branch*, *the branch its routing.md names, <Branch>, is not here*, *this range's tip is not on the branch its routing.md names*, *this range's tip is on a branch the one its routing.md names was cut from* — and *this range touches nothing in it* in shared mode; a copy of the script with no `hooks/routing.py` or no `hooks/optin.py` beside it refuses (exit 2) rather than placing nothing, naming what the missing file answers | `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_missing_common_dir_reader_refuses_rather_than_placing_nothing@c4f716f1`, `skills/code-review/scripts/survivor_check.py#on_its_branch@537eaffb`, `skills/code-review/scripts/survivor_check.py#report@74a8bff6`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_local_mode_declaration_nobody_can_place_is_not_yours@40796b37`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_missing_routing_reader_refuses_rather_than_placing_nothing@6eaa6631`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_declaration_one_directory_deeper_still_has_an_owner@bd9591fc` | **Executed** 2026-09-25. Each mutated from Python, restored from a copy: an unreadable `routing.md` answering True, O4 missing red; an unparsed one answering True, O4 no-branch red; the reason fixed to the shared-mode text, O2 and both O4 red; the reader guard removed, O5 red | 2026-09-25 | The reason clause is a divergence from `spec.md` §*Data & interfaces* (*no report line changes shape*): the line's shape is kept, and only the reason varies, because in local mode *this range touches nothing in it* is true of every range and names the wrong file to open (`overview.md`). **Corrected 2026-09-25 in round 1's fix pass (🟡 2)**: the claim said one local-mode sentence, *this range's tip is not on the branch its routing.md names*, was printed for every local refusal, and O4 pinned it where `routing.md` was missing or named no branch — a branch nobody named. `on_its_branch` now returns the reason for each refusal and `whole_range` prints it; O4's three parameters (a third, `unknown-branch`, added) each pin their own sentence and were red at `6e48cb5f`, and O6 pins the cut's. **Corrected 2026-09-25 in round 2's fix pass (⬜ 3)**: `hook` refused a missing `hooks/optin.py` with the routing reader's sentence, *which says whose a local-mode declaration is*; each caller now names what its file answers, and O5's twin pins the `optin.py` sentence (red at `cd5f2dcd`, the pattern unmatched) |

<!-- specs/1790260565-a-ledger-row-carries-two-readings-in-one -->
### 1790260565-a-ledger-row-carries-two-readings-in-one

| L1 · a ledger row with no header above it — every fragment row, and a released row a fold copied in without one — is counted against the width `templates/ledger.md` declares for a ledger row, read from its `\| Clause \|` header and not written as a literal; a table with its own header keeps its own width | `tests/test_release_hygiene.py#overwide_rows@d1f18a13`, `tests/test_release_hygiene.py#ledger_row_width@ae79bd0c`, `tests/test_release_hygiene.py#ledger_overwide@c22074ba`, `tests/test_release_hygiene.py#test_a_row_under_no_header_is_counted_against_the_width_it_is_given@1c308775`, `tests/test_release_hygiene.py#test_no_ledger_row_splits_into_more_cells_than_its_header@95b9631a` | **Executed** 2026-09-25: the new case red against the header-only `overwide_rows` (`assert [] == [(1, 5, 6)]`), green after; the corpus case green over 767 table body rows, 25 of them under no header and all five cells wide | 2026-09-25 | #501. Before this, the policy's *the case reads … every fragment* was true of reading and false of counting. Re-read 2026-09-25 in round 1's fix pass (⬜ 2): only the unit case's comment changed. It now says the case goes red when the width is dropped inside `ledger_overwide`, and that no case goes red when the corpus case calls `overwide_rows` bare. The claim is about what is counted, and it holds |
| X1 · the fix-surface cell's `allow` for an unrecognised *not yet* reason is stated as one exception with one referent in both places that state it: the spec's paragraph and `says_not_yet`'s docstring each name the direction every other refusal in `chain_check.py` takes, which treats what it cannot read as failing, and the spec no longer points at §*The reopening*'s `blocks more` | `` `docs/round-record-spec.md#"## The fix surface — `Contract changes` and `New units`"@40fa761a` ``, `skills/code-review/scripts/chain_check.py#says_not_yet@0e4e0de5`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_exception_names_the_direction_the_code_names@a22b45fc` | **Executed** 2026-09-25: the new case red against the paragraph as it stood (*does not name the direction chain_check.py's other refusals take*), green after the reviewer's sentence went in | 2026-09-25 | #569 ⬜ 2. §*The reopening*'s `blocks more` is the reopening check's own failure direction, a different rule. The behaviour of `says_not_yet` did not change Re-read 2026-09-25 at the merge of #587's final tip into #565: the anchored section carries #565's added `Enforced by:` lines beside this row's own edit, which change none of the sentences this claim is about; the claim holds. |

<!-- specs/1790260566-a-row-inside-a-fence-reads-as-live -->
### 1790260566-a-row-inside-a-fence-reads-as-live

| P1-1 · One fence-delimiter rule, to CommonMark 4.5, and every fence walk in `unverified_check.py` asks it: at most three spaces of indentation, three or more backticks or tildes, a backtick opener's info string holds no backtick, and a closer is the same character, at least as long, with nothing after it but whitespace. `blank_fences` and so `readable` no longer take a four-space run as a delimiter (#491), and neither `blank_fences` nor `_liveness` closes a block on a closer that carries an info string | `skills/verify/scripts/unverified_check.py#fence_opener@1b762321`, `skills/verify/scripts/unverified_check.py#fence_closes@fedc1bc8`, `skills/verify/scripts/unverified_check.py#fence_spans@4469f557`, `skills/verify/scripts/unverified_check.py#blank_fences@d8110a00`, `tests/test_unverified_rows_close.py#test_an_indented_delimiter_is_not_a_fence_for_the_gates_reader@d802d736`, `tests/test_unverified_rows_close.py#test_each_reader_asks_the_one_delimiter_rule@6f4cf0c3` | **Executed** 2026-09-24: S4 and both S5 shapes red against `c52e8350`'s reader, run from a saved copy — the four-space table disappeared from `readable`, and ```` ```python ```` closed the block in both readers while ```` ```x` ```` opened one. The tilde shape is the control and passes on both | 2026-09-24 | Phase 1 of `1790260566`. The rule lives in the leaf module because every reader in scope already loads it or can |
| P1-2 · `hooks/config.py#fence_map` is the one deliberate second copy of the delimiter rule, and a case holds the two in step shape by shape: which lines are fenced, and where an unclosed block opened | `hooks/config.py#fence_map@79dfca74`, `tests/test_unverified_rows_close.py#test_the_fence_rule_agrees_with_the_config_reader@0faaf14f`, `tests/test_unverified_rows_close.py#FENCE_SHAPES@919d6177` | **Executed** 2026-09-24: `c52e8350`'s `blank_fences` rule disagrees with `fence_map` on shapes 3, 4, 6, 12 and 13 of the nineteen, and the shared rule agrees on all nineteen | 2026-09-24 | `config.py` runs on the hook path, where loading a skill module would cost every hook call, which is why it keeps its own copy (questions.md D8) |
| P1-3 · `_paragraph_ends_at` ends a paragraph on an ATX heading only within three spaces of indentation, and on a fence delimiter only where the line is an opener | `skills/verify/scripts/unverified_check.py#_paragraph_ends_at@92b6c912`, `tests/test_unverified_rows_close.py#test_an_atx_heading_ends_a_paragraph_only_within_three_spaces@1928be86`, `tests/test_unverified_rows_close.py#a_reading_from_the_commonmark_rules@aec41270` | **Executed** 2026-09-24: the four-space case red against `c52e8350`. The oracle gained the same two info-string clauses as the reader, and `test_the_scan_never_reads_live_what_the_format_parks` is green over its 2,000 generated documents | 2026-09-24 | #491's round-7 comment named the unbounded ATX branch |
| P1-4 · Across the 124 tracked `.md` files, the phase-1 rule moves 10 lines of `readable` output in 3 files and no `live_lines` flag. Every moved line is a fenced block indented four to six spaces inside a list item — `docs/release-checklist.md`, `skills/evidence-ci/SKILL.md`, `skills/implement/orchestration.md` — now read rather than blanked, and no gate that reads through `readable` reads those three files | `skills/verify/scripts/unverified_check.py#readable@5ef11fb3`, `skills/verify/scripts/unverified_check.py#fence_opener@1b762321` | **Executed** 2026-09-24: one script comparing `c52e8350`'s `readable` and `live_lines` with this tree's over `git ls-files '*.md'` | 2026-09-24 | Q2. Under the format those lines ARE fenced, because a list item moves the indentation a fence is measured from, and the reader has no block model. Kept rather than reverted: the bound is #491 itself, and it makes the error loud (rows read) where the old spelling made it silent (a four-space run swallowing every row below it) |
| P2-1 · The checker, `--reverify` and `--migrate` skip every line of a fenced block that closes: an example row there is not reported, not re-stamped and not migrated, and the session-start hook's trigger does not count it. A row in a fence that never closes, or in an HTML comment, is read as before (#444) | `skills/evidence-check/scripts/evidence_check.py#quoted_lines@de284263`, `skills/evidence-check/scripts/evidence_check.py#unquoted@15eff07a`, `skills/evidence-check/scripts/evidence_check.py#check_ledger@6ef65e78`, `skills/evidence-check/scripts/evidence_check.py#old_format_rows@15ea65b9`, `skills/evidence-check/scripts/evidence_check.py#migrate@f0cdbbcf`, `skills/evidence-check/scripts/evidence_check.py#reverify@53d69854`, `tests/test_evidence_check.py#test_a_fenced_example_row_is_not_checked@ad6f018b`, `tests/test_evidence_check.py#test_the_writers_leave_a_fenced_example_byte_for_byte@e9646689`, `tests/test_evidence_check.py#test_an_unclosed_fence_still_holds_its_rows@8d481ab2` | **Executed** 2026-09-25: S1 and S2 red against `c52e8350`'s checker swapped in from a saved copy — `BROKEN nosuchfile.py#nothing file not found` at exit 2, and `--reverify` rewrote the fenced hash; a separate probe showed `c52e8350`'s `--migrate` rewriting a fenced old-format coordinate and this tree's leaving it. S3 passes on both, which is its job | 2026-09-25 | Phase 2 of `1790260566`. `unquoted` blanks to spaces and keeps every offset, because `reverify` splices by match position |
| P2-2 · The checker takes its delimiter rule from the shared reader where it is the plugin's copy, and from a vendored pair of the same two functions where `evidence-ci` put it alone in a user repository's `tools/`; a case holds the pair in step with the shared rule, and a vendored copy skips a fenced example too | `skills/evidence-check/scripts/evidence_check.py#fence_rule@21812ee9`, `skills/evidence-check/scripts/evidence_check.py#vendored_fence_opener@0a389b92`, `skills/evidence-check/scripts/evidence_check.py#vendored_fence_closes@918d399b`, `tests/test_evidence_check.py#test_the_vendored_fence_rule_agrees_with_the_shared_one@647ec6de`, `tests/test_evidence_check.py#test_a_vendored_copy_skips_a_fenced_example_too@88a7197b` | **Executed** 2026-09-25: the agreement case red with the vendored opener's info-string clause removed; the vendored-copy case red against `c52e8350`'s checker | 2026-09-25 | `plan.md` said the checker loads the reader and that the plugin layout ships it beside the checker. The vendored copy has no reader beside it, which the frame did not name; `questions.md` Q6 records the default taken |
| P2-3 · Over this repository's ledger no anchor sits inside a closed fence: `seal/ledger.md` and the 30 files under `seal/releases/` carry no fence line at all, so #444 moves no row's verdict here, and `evidence-check --strict .` counts the same 2,067 anchors before and after | `skills/evidence-check/scripts/evidence_check.py#quoted_lines@de284263` | **Executed** 2026-09-25: `quoted_lines` and `ANCHOR_RE` over every ledger file; a grep for a triple backtick or tilde anywhere in those files finds one file, `seal/releases/0.4.0.md`, and no line that opens a fence | 2026-09-25 | Q3. The frame counted fences in 11 files; the count that matches a fence line is 0 |
| P3-1 · An evidence-todo file's open rows are one rule, `todo_open_rows`: a LIVE line outside a table whose first word is `drained` closes the whole file, and one quoted in a fence, an HTML comment or a code span closes nothing; otherwise every table body row is open unless its first cell begins with ✅, a header and its separator are not rows, and a row inside a fenced block that closes is an example, while a row in an unclosed fence or a comment is still open. Lines are split on `\n` alone, so a cell holding U+2028, U+0085 or a form feed before `drained` does not close the file | `skills/verify/scripts/unverified_check.py#todo_open_rows@adee2d04`, `tests/test_settle_reads_before_it_removes.py#test_a_quoted_drained_line_closes_nothing@eff2cd1f`, `tests/test_settle_reads_before_it_removes.py#test_a_fenced_example_row_is_not_an_open_row@952f3f5e`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_quoted_drained_line_closes_nothing@8ed8d75d`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_fenced_example_row_does_not_refuse_the_fold@1fbc001a`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_line_separator_in_a_cell_does_not_close_the_file@922a28b8` | **Executed** 2026-09-25: the four new cases red with `c52e8350`'s body of `todo_open_rows` and `c52e8350`'s `fold_ledger.py` swapped in, both restored from saved copies afterwards; every earlier open and closed shape in the fold module stays green | 2026-09-25 | Replaces two rows of `seal/releases/0.4.0.md` REMOVED in this phase, whose anchor was the copy at `.github/scripts/fold_ledger.py#open_rows`: the rule with `drained` and ✅, and the split on `\n` alone. Both claims carry over, narrowed by #487's two halves |
| P3-2 · The fold's `open_rows` IS the shipped `unverified_check.py#todo_open_rows`, loaded by path the way `rider_check.py` loads the shipped checker; the copy and its "may not depend on this" clause are gone | `.github/scripts/fold_ledger.py#open_rows@896a007d`, `.github/scripts/fold_ledger.py#load_reader@577ec7e5`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_fold_asks_the_shipped_open_rows_rule@e627b0e2` | **Executed** 2026-09-25: the case red against `c52e8350`'s script, whose `open_rows` was its own; `tests/test_a_script_says_which_interpreter_it_needs.py` green with the new load, which answers Q4 | 2026-09-25 | #487. A moved or renamed reader stops the fold at load with a traceback at the release, the loud direction |
| P3-3 · The survivor sweep takes a gathered fragment's marker off a live line of `CHANGELOG.md` only, so a marker quoted in a fence, an HTML comment or a code span excuses nothing | `skills/code-review/scripts/survivor_check.py#gathered_fragments@5c6f3c28`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_marker_quoted_in_a_fence_gathers_nothing@93c784f1` | **Executed** 2026-09-25: red against `c52e8350`'s sweep, exit 0 with the fragment excused. Over this repository's `CHANGELOG.md` all 110 markers are live, so the gathered set does not move | 2026-09-25 | S9. The same reading `folded_items` gives `docs/` |
| P4-1 · In the records arm a comment's `-->` is a position: what follows it on the line is read by the line's own rules, so a claim after a closer is read and a `<!--` beginning that remainder opens an aside again; an aside opens only where a line, or such a remainder, begins with `<!--`, and a comment opening part-way along text is read with it (Q1's default). The text handed back is what lies outside the asides, and `NAME NOT IN TREE` is asked of the whole line | `skills/evidence-check/scripts/evidence_check.py#claim_lines@e5006cc3`, `tests/test_a_record_states_what_the_tree_has.py#test_a_claim_after_a_closing_delimiter_is_read@9eeb45fa`, `tests/test_a_record_states_what_the_tree_has.py#test_a_comment_reopened_on_its_closing_line_is_an_aside_again@3df43e97`, `tests/test_a_record_states_what_the_tree_has.py#test_a_claim_after_a_one_line_comment_is_read@ca1c4864`, `tests/test_a_record_states_what_the_tree_has.py#test_a_mid_line_comment_is_read_with_its_text@fd296b05` | **Executed** 2026-09-25: S10, S11 and the one-line case red with `c52e8350`'s body of `claim_lines` swapped in, restored from a saved copy; the mid-line case passes on both, as a pin of a default should | 2026-09-25 | #220. Q1 of `1790260566` holds the alternative, a positional scanner with code-span state |
| P4-2 · The records arm opens and closes a fence by the shared delimiter rule, through `fence_rule`: a ```` ``` ```` inside a ```` ```` ```` block, a ```` ```python ```` line inside a ```-block and a four-space run are not delimiters | `skills/evidence-check/scripts/evidence_check.py#claim_lines@e5006cc3`, `tests/test_a_record_states_what_the_tree_has.py#test_a_shorter_fence_does_not_close_a_longer_one@be5d6c24` | **Executed** 2026-09-25: S12 red against `c52e8350`'s `claim_lines`. Over every record file this repository's history holds, 1,711 of them, one file reads differently: a round report whose ```` ```` ```` block quoted two ```` ``` ```` blocks, which flipped every later fence in the file under the old rule. The new reading follows the format, reading 45 names of prose the flip had hidden and dropping one inside a `python` block | 2026-09-25 | Q5. On this tree the arm reads 158 names with 0 refused, before and after |
| R1-1 · The record generator's verbatim-fence walk closes a block only on a run of the opener's character, at least as long, with nothing after it, and opens none on a backtick run whose info string holds a backtick; its opener keeps any indentation, so a fix fenced inside a list item still reaches the record | `skills/code-review/scripts/round_record.py#fenced_after@f091142c`, `tests/test_the_record_is_generated.py#test_a_fence_line_with_an_info_string_inside_a_fix_does_not_end_it@e13cb1ce`, `tests/test_the_record_is_generated.py#test_a_backtick_line_whose_info_holds_a_backtick_opens_no_fix@b1918daf` | **Executed** 2026-09-25: the case red at `f754eafb` — the record lost fix lines and copied the prose between two fixes — and green with the fix. The opener half's case, planted in round 2's fix pass, red with that half removed | 2026-09-25 | Round 1's 🟡 1. The one fence walk on the record path the frame's class table had not named |
| R1-2 · The root migration's re-point leaves an example row inside a fenced block that closes byte for byte and still moves a live row, because it finds anchors in `evidence_check.py#unquoted`'s text and splices from the original | `hooks/root-migrate.py#repoint@6b3bfe22`, `tests/test_the_root_migrates_itself.py#test_a_fenced_example_row_is_left_byte_for_byte@58351288` | **Executed** 2026-09-25: the case red at `f754eafb`, where the fenced `.specseal/` path became `seal/` | 2026-09-25 | Round 1's 🟡 2. A third ledger writer beside `--reverify` and `--migrate` |

<!-- specs/1790260567-the-broad-gate-hands-cmd-a-forward-slash -->
### 1790260567-the-broad-gate-hands-cmd-a-forward-slash

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A1 · a shell string is rewritten only where the shell is `cmd.exe`: Windows, with `COMSPEC` naming `cmd.exe` by its basename in any case or quoting, or unset or empty. Any other `COMSPEC` and every POSIX machine get the row as written | `skills/verify/scripts/broad_gate.py#handed_to_shell@ea268102`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_only_cmd_exe_on_windows_is_handed_the_backslashed_row@f5428e90`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_an_unset_comspec_on_windows_is_cmd_exe@fdf72d2c` | **Executed** 2026-09-25 on macOS with `windows` and `comspec` passed in, 38 passed in the module. Mutated from a script restoring the bytes it read: the rewrite returning its input, 20 red; the `COMSPEC` test dropped, 3 red; `windows` defaulting to true, 1 red (the real-platform case) | 2026-09-25 | Keyed on the shell and not on `os.name`, because a Windows `COMSPEC` can name a POSIX shell, which runs `bin/test` as written |
| A2 · only `/` inside a word `cmd.exe` reads as a command name changes — at the start of the line and after `&&`, `\|\|`, `&`, `\|` or a block's `(` — except a `/` written straight after one of `cmd.exe`'s own commands (`CMD_BUILTINS`), which is that command's switch and stays as written; every other character stays where it was. The module has one `subprocess` call passing `shell=`, inside `run`, which hands the shell what `handed_to_shell` returned, and `gate` and `compare_at_base` are its only two `shell=True` callers. `templates/config.md` §*Broad gate* states which positions are rewritten and names examples of those that are not, without counting them | `skills/verify/scripts/broad_gate.py#CMD_BUILTINS@461f7f94`, `skills/verify/scripts/broad_gate.py#command_names_backslashed@5084295a`, `skills/verify/scripts/broad_gate.py#run@c165e6b5`, `templates/config.md#"## Broad gate"@77f505d4`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_only_the_command_names_have_their_slash_turned@4a94e8bd`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_one_shell_site_is_run_and_it_applies_the_rewrite@f1d7a21a`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_switch_against_another_program_is_rewritten_the_documented_bound_not_the_goal@a660e729`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_template_says_which_positions_are_rewritten@12094abe` | **Executed** 2026-09-25 on macOS. Every branch of the scan mutated in turn and each went red: a quoted `/` kept (1), a quote not opening a name (1), an escaped character not opening one (1), a separator not reopening command position (4), a redirection not closing it (1), a blank not ending a name (12), a `(` not ending one (1). The template case went red with its `call`/`start`/`if` sentence cut | 2026-09-25 | **Read, not executed:** what real `cmd.exe` does with each of these strings. The scan models its lexer from documentation, and `questions.md` Q1 holds the one resolution only CI's `windows-latest` leg executes **Corrected 2026-09-25** in round 1's fix pass (🟡 2, ⬜ 3): the claim said every `/` in a command word changes and that the template names two unrewritten positions. A built-in's switch is now left as written, and the template states the rule instead of a count. Executed: the four built-in cases and a built-in after a separator were red at `7dab2a17` and are green; the template case went red against the template at `7dab2a17`; mutated at `edfb96af`, the built-in branch gone (4 red), `@` not stripped (1), case kept (1), the switch keeping the name open (1), the name's start never moving (1), a `(` always opening command position (4) **Corrected 2026-09-25** in round 2's fix pass (round 2's 🟡 1): the built-ins are the only switch left as written. A `/` straight after any other program (`xcopy/e`) is read as part of a path and rewritten, which denies a row that ran before; the template, the scan's docstring and `spec.md` now say so and name the blank that avoids it (`xcopy /e`), and #596 holds the behaviour fix. Executed: the template case was red without the new sentences, and the bound case went red with `xcopy` and `findstr` added to `CMD_BUILTINS` (2 red) |
| A3 · where the rewrite changed the string, one stderr line says what `cmd.exe` was handed before the run, and the kept `<name>.txt` carries the row as written on its first line and `cmd.exe was handed: <line>` on its second. Where nothing changed, and for every argv check, nothing extra is printed or kept | `skills/verify/scripts/broad_gate.py#run@c165e6b5`, `skills/verify/scripts/broad_gate.py#handed_line@c0c1b8d0`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_rewritten_row_is_kept_and_said_beside_the_row_as_written@64be627a`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_row_handed_as_written_adds_nothing_to_the_record@e7e46913`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_an_argv_check_is_never_rewritten@15f8aee3` | **Executed** 2026-09-25 with the platform forced and `subprocess.run` stubbed. Mutated: `run` dropping the rewrite (2 red), the stderr line dropped (1), the kept line dropped (1), the stderr line reworded (1) | 2026-09-25 | The stderr line is pinned verbatim, because a person reads it and acts on it (`agent-contract` §14) |
| A4 · the row `bin/probe`, beside a POSIX `bin/probe` and a `bin/probe.cmd`, runs through the real shell of the machine and exits 0 with the probe's output in the kept file | `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_row_runs_on_the_real_platform@4db25c17` | **Executed** 2026-09-25 on macOS, where `sh` runs the POSIX half. **Unverified — CI windows leg**: `cmd.exe` resolving `bin\probe` to `bin\probe.cmd` is executed only on `windows-latest` at the pull request | 2026-09-25 | Its red on Windows without the fix is #448's own four-spellings table, the ticket's measurement and not a run in this work item |
| A5 · a failing `suite` whose output holds no pytest summary with a wall clock gets one line on the failure form saying its exit code is not a count of failing tests; where a summary is present the form shows the count instead, and no other check gets the line. The exit code stays 1, and `agents/sealer.md` says the line is the gate's words, handed on unedited | `skills/verify/scripts/broad_gate.py#failure_lines@bcb835e9`, `skills/verify/scripts/broad_gate.py#NO_SUMMARY@b8998837`, `agents/sealer.md#"## The command"@0dd54c8e`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_failing_suite_with_no_summary_says_it_is_not_a_count@98609087`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_failing_row_with_no_summary_says_so_on_the_form@1f290523` | **Executed** 2026-09-25 on macOS: the unit case over five outputs and the gate run end to end with the rows `exit 1` and `echo 1 failed in 0.01s && exit 1`, both of which read the same under `/bin/sh` and `cmd.exe`. Mutated at `017758c2`: the line dropped, which is the code before this change (3 red); the line on every check (1 red); the line beside a count (3 red); the sentence reworded (3 red) | 2026-09-25 | Keyed on the missing summary rather than on an exit code, because #448 measured `cmd.exe` exiting 1 for a command it could not find, and its message is in the machine's own language (`plan.md` Alternatives 4) |
| A6 · at import, `tests/conftest.py` points `GH_CONFIG_DIR` at an empty directory it removes at exit, removes `GITHUB_TOKEN` and `GITHUB_ENTERPRISE_TOKEN`, and sets `GH_TOKEN` and `GH_ENTERPRISE_TOKEN` to `NOT_A_GH_TOKEN`, a value no server accepts, so the token `gh` would send under the suite's environment is that placeholder and never a login from `hosts.yml` or the OS keyring; `CONTRIBUTING.md` §*Running the checks* says so and why | `tests/conftest.py#NOT_A_GH_TOKEN@0c8e339a`, `tests/conftest.py#GH_CREDENTIALS@293aea32`, `CONTRIBUTING.md#"## Running the checks"@048eae4c`, `tests/test_the_suite_runs_with_gh_logged_out.py#test_the_conftest_takes_every_gh_login_away@e8917318`, `tests/test_the_suite_runs_with_gh_logged_out.py#test_the_token_gh_would_send_is_not_a_login@2bb81a83`, `tests/test_the_suite_runs_with_gh_logged_out.py#test_contributing_says_the_suite_runs_logged_out@c404837f` | **Executed** 2026-09-25 on the builder's macOS machine, where `gh` 2.100.0 is logged in through `hosts.yml`: `gh auth status` exited 0 as it stood and 1 under the block. Mutated at `fb340ce0`: the whole block removed, 2 red, the behavioural case among them; `GH_CONFIG_DIR` not pointed, 2 red; the tokens not removed, 1 red; one token left in the list, 1 red; the cleanup dropped, 1 red. The `CONTRIBUTING.md` case went red with the paragraph's lead cut | 2026-09-25 | The behavioural case can go red only where `gh` is logged in, and on CI it is green either way. That is the block's point, and it is why the structural case runs the conftest in a child that carries every token and a config directory holding a login. `GH_CREDENTIALS` is the anchor because the conftest's block has no unit of its own; the four lines after it are held by the cases rather than by the hash **Corrected 2026-09-25** in round 1's fix pass (🟡 1): the claim said `gh auth status` reports no login, which holds on a `hosts.yml` machine and not on a keyring one, where `ActiveToken` falls back to the keyring (read from upstream `cli/cli`). Executed here: `gh auth token` under the suite's environment returns the placeholder, and `gh api user` fails `Bad credentials (HTTP 401)`. The structural case was red against the block at `7dab2a17`. Mutated at `554536af`: `GH_TOKEN` or `GH_ENTERPRISE_TOKEN` not set (1 red each, the structural case); the placeholder changed (2 red, the behavioural case among them); the whole block gone (2 red). The keyring path itself is `read`: this machine keeps no keyring login |

<!-- specs/1790263216-the-older-statements-name-what-enforces-them -->
### 1790263216-the-older-statements-name-what-enforces-them

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| E1 · each folded statement of a `docs/X.md` is paired with the statement under the same markers at the same heading position of its `docs/X.ko.md` edition, and the two `Enforced by:` lines name the same targets, or `nothing` in both; the reason after `nothing — ` is each edition's prose and is not compared, and a line one edition carries and the other lacks is named | `tests/test_both_editions_carry_the_same_folds.py#enforcement@edca6aa3`, `tests/test_both_editions_carry_the_same_folds.py#disagreements@a9663a09`, `tests/test_both_editions_carry_the_same_folds.py#test_an_enforced_by_line_naming_other_targets_is_named@6da4aba6`, `tests/test_both_editions_carry_the_same_folds.py#test_an_enforced_by_line_one_edition_lacks_is_named@ad6d1f8b`, `tests/test_both_editions_carry_the_same_folds.py#test_the_reason_after_nothing_is_each_editions_own_prose@bdb9b2cf` | **Executed** 2026-09-25. The three planted cases were red against `disagreements` before its comparison was written (3 failed, 8 passed) and green after. The real-tree case is green over `one-root-by-lifetime` with nine Korean lines whose targets are the English ones byte for byte, and red with one Korean line narrowed to its first target (phase 6's M19). `enforcement` made to skip every line reddens the first planted case (M20) | 2026-09-25 | #565, `spec.md` §*Scope* item 4. `enforcement` reads statements through `fold_check.py#numbered_statements` and the value through `#names_targets`, so the span and the targets-or-`nothing` reading are the shape check's own |
