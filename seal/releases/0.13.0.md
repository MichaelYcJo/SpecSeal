## 0.13.0 — 2026-09-22

<!-- specs/1790027178-a-shipped-spec-waits-for-a-settle-that-was-never-built -->
### 1790027178-a-shipped-spec-waits-for-a-settle-that-was-never-built

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| S1 · A removed released work item is told from a deleted one by the fold record in `docs/`, and nothing in the removal itself carries the distinction — both shapes are a directory present at the fork point and absent here | `skills/verify/scripts/unverified_check.py#folded_items@7a8d3c99`, `tests/test_unverified_rows_close.py#test_a_folded_work_item_is_not_read_as_this_branchs_deletion@b41aa507` | **Executed** 2026-09-22. Both behaviour cases were red against the shipped code before the change — `1 failed` each, on the assertion that the folded directory is not reported. Green after. Three mutations each turned one case red alone: `folded_items` returning an empty set, `FOLD_MARKER` losing its line anchors, and `work_item_of` taking the file's basename instead of its directory's. **Re-read and strengthened 2026-09-22 in round 1's fix pass**, which found the line anchor insufficient twice over: a marker inside a fenced block and a marker below the top level of `docs/` both satisfied it, and each removed a directory at exit 0. The scan now goes through `blank_fences` and reads the top level only; seven fence shapes are parametrised, the shipped skill pasted into `docs/` records nothing, and both fixes were shown red by reverting them. **Round 2 found the other half of the same rule still open and it is closed here**: a marker inside a commented-out draft block satisfied the guard too, and `settle --retire` removed the directory at exit 0. A line stops being live two ways, so the scan now asks `opens_outside_a_comment` as well as `blank_fences`, and both views come out of one `comment_scan` rather than a second copy of the walk. Six comment shapes are parametrised beside the seven fence shapes, and the count in this sentence was wrong at eight before round 2 read it | 2026-09-22 | The scan is `docs/` and nothing else. Widening it to the whole tree would let a marker in a round record, a changelog fragment, or the removed directory's own files at the base excuse a removal nothing absorbed. **Re-read 2026-09-22 in #489's build (work item `1790039346`)**, which moved the read from its two-call form — `blank_fences`, then `opens_outside_a_comment` — to the one `live_lines` helper that also blanks inline code spans before it asks the comment state. The claim stands: a folded item is still told from a deleted one by the marker in `docs/`, the seven fence shapes and six comment shapes stay green, and a seventh comment shape — an opener quoted inside backticks a line above a real marker — reads as a fold now rather than parking every marker below it. **Re-read again 2026-09-22 in that work item's round 4 fix pass**, which replaced the three passes behind `live_lines` with one scan carrying fence, comment and code-span state together. `folded_items` is byte-identical — only what it asks changed — and the claim stands: executed, all eleven corpus files derive the same marker sets as before, and a marker inside a multi-line code span is no longer read as a fold, which closes a door this row's work item left open |
| S2 · `gather_changelog.py --check` passed having examined nothing once the fragment glob went empty, which is the state a fold produces; it now counts the markers `CHANGELOG.md` carries and refuses a corpus with neither | `.github/scripts/gather_changelog.py#main@ed9590ec`, `.github/scripts/gather_changelog.py#MARKER_LINE_RE@95d471a3`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_corpus_with_no_fragment_and_no_marker_is_not_a_pass@ffdce19b` | **Executed** 2026-09-22, twice. Q3's probe against the SHIPPED script over a scratch root holding a `CHANGELOG.md` with one entry and no marker and a `seal/specs/<id>/` with no fragment: `0 changelog fragments, all gathered`, exit 0, read with `echo $?` and not through a pipe. Then four cases red first and green after, and two mutations each turning one red alone: `MARKER_LINE_RE` unanchored, and the empty-corpus refusal never firing | 2026-09-22 | The read in `spec.md` §*The two readers* was right, so this row records a confirmation rather than a correction. The repair is a count and not a new population: what was wrong was that an empty glob and *everything reached the file* printed the same sentence **Re-read 2026-09-23 by work item 1790173209 (#289).** `main` reads the section's existing date and says when it appended; the `--check` arm this row is about did not change, so the claim holds. **Re-read 2026-09-24 by round 1's fix pass of work item 1790173209.** `main` asks `section_heading` for existence and prints the file's own heading; the `--check` arm is untouched, so the claim holds. **Re-read 2026-09-25 by work item 1790297085 (#586), phase 3:** `main`'s `--version` arm now refuses an ungathered fragment carrying a line starting `## ` before it writes or prints anything; the `--check` arm this row is about is untouched, so the claim holds |
| S3 · A work item's segment is the file the majority of its non-`tests/` ledger coordinates anchor in, ties broken by path order; a `tests/` anchor rolls up to the code the case pins, and an item anchored only under `tests/` or carrying no ledger row is named rather than guessed at | `skills/settle/scripts/settle.py#segment_of@3e97cac3`, `skills/settle/scripts/settle.py#coordinates@8a11661a`, `tests/test_settle_reads_before_it_removes.py#test_the_segment_rule@d6db5aa2` | **Executed** 2026-09-22 against this repository's own corpus, which is what the rule was decided on: 97 released work items, **81 grouped into 37 segments, 16 named, 0 skipped**, one unreleased and untouched. Of the 16, fourteen wrote no ledger row and **two** are anchored only under `tests/`. Two mutations each turned one case red alone: dropping the `tests/` filter, and taking the first anchor instead of the majority | 2026-09-22 | Q2. 852 of 1,772 coordinates sit under `tests/`, and the majority rule is why that costs two named items rather than a segment called `tests` holding half the corpus: nearly every work item that writes a case also writes a row about the code the case pins. The largest segment is `round_record.py` at 13 work items and 21 of the 37 segments hold exactly one **Re-read 2026-09-22 in round 2's fix pass**, which gave `coordinates` fence tracking: a fenced example in `seal/ledger.md` used to open a section and attribute every coordinate after it to the id in the quotation. The file carries no fence today, so no figure above moves, and the reader is the `blank_fences` this module already loads rather than a third copy of the rule. **Re-read 2026-09-22 in #489's build (work item `1790039346`)**, which gave `coordinates` the comment half of the rule as well, through the same `live_lines` that `folded_items` reads: a marker inside a commented-out draft opens no section, in the shared ledger and in a fragment alike, and an anchor quoting `<!--` inside backticks parks nothing. No figure above moves — `bin/settle` at that build's tip prints `81 work items in 37 segments, 16 ungrouped, 0 skipped` at exit 0, and the ids it sections are the ids the fence-only reading sectioned. **Re-read again 2026-09-22 in that work item's round 1 fix pass**, which corrected the liveness rule in one shape — a span holding a lone closing delimiter is no longer blanked, so a closer quoted in prose still ends a parked draft — and added a paragraph naming the indented code block as a door the reader does not answer. The segment rule and every figure in this row are unmoved: executed, `81 work items in 37 segments, 16 ungrouped, 0 skipped` at exit 0, and 83 section ids against the fence-only reading's 83. **Re-read once more 2026-09-22 in that work item's round 2 fix pass**, which changed what the span pass blanks — the comment delimiter rather than the span around it — and corrected one ground in this function's docstring. Every figure in this row is unmoved and re-derived rather than carried: 761 not-live lines, 83 section ids, the three named markers live, 0 corpus lines answering differently, and `81 work items in 37 segments, 16 ungrouped, 0 skipped` at exit 0. **Re-read a third time 2026-09-22 in that work item's round 4 fix pass.** `coordinates` now reads through a single stateful scan rather than three passes in sequence; its two loops are unchanged, and every figure above is re-derived rather than carried — 761 not-live lines, 94 marker occurrences over 83 unique ids, and the same dry-run line at exit 0 **Re-read 2026-09-24 (#547)**: `coordinates` runs its section loop over `seal/ledger.md` and every `seal/releases/*.md`; the majority rule and the ties are untouched. **Re-read 2026-09-24 by work item 1790260566 (#491), phase 1:** only the docstring's indented-code-block paragraph changed, to name the shared delimiter rule `unverified_check.py#fence_opener` where it named `blank_fences`; both loops and every figure in this row are untouched **Re-read 2026-09-25 by work item 1790260566, round 1's fix pass:** one docstring phrase narrowed, from the rule every reader shares to the rule the ledger readers share; the loops are untouched |
| S4 · `settle --retire` removes the directory of a released work item only where `docs/` records its fold, or where the item wrote no `spec.md` and nothing in its record is open, and refuses one the evidence-todo guard is holding even when the marker is there | `skills/settle/scripts/settle.py#retire@c15f488a`, `skills/settle/scripts/settle.py#open_rows@f9472208`, `tests/test_settle_reads_before_it_removes.py#test_retire_refuses_an_item_the_guard_is_holding@70daf9a0` | **Executed** 2026-09-22 over a fixture root: with one item's fold recorded, that directory goes and the unfolded sibling stays; with the held item's fold recorded, the run exits 1, the directory is still there and the report names it under `kept`. Over this repository the same arm reports `nothing to retire` at exit 1 and removes nothing, read with `echo $?` and not through a pipe. Three mutations each turned one case red alone: `retire` no longer refusing a held item, `open_rows` no longer honouring `drained`, and `survey` no longer skipping a recorded fold. **Re-read and strengthened 2026-09-22 in round 1's fix pass.** Finding 4 moved the guard ahead of the fold record in `survey`, which would have emptied `retire`'s candidate list of every held item and left this refusal unreachable — so the candidate set is read from the tree (markers ∩ directories ∩ released) rather than from `survey`'s classification, and a mutation putting it back turns the case red | 2026-09-22 | The marker being the condition is what makes the ordering enforceable rather than remembered. The worst outcome the arrangement can produce is a thin policy document, which a reader can see, instead of a directory removed with nothing absorbing it, which nobody can **Re-read 2026-09-22 in round 2's fix pass.** What changed is the empty-candidate report, not the removal rule: *the fold is complete* used to print whenever `docs/` recorded any fold at all, while asserting that no marked item still had a directory — so a marked item sitting on disk and absent at the release ref was reported as finished. Three states now, each told apart by what its own sentence claims. **Re-read 2026-09-23 by work item 1790138190 (#511), phase 1**, which gave `retire` a second guard: a candidate a live ledger row anchors into is kept too, per directory, and the row is named. A removal still needs the marker and the evidence-todo refusal is unchanged, so the claim holds; what widened is what else is kept. **Corrected 2026-09-23 by the same work item's phase 3 (#517, D3).** The clause said *only where `docs/` records its fold*, which the rule arm made false: a released item with no `spec.md` whose `overview.md` and `evidence-todo.md` hold no open row is now removed with no marker, asked of `unverified_check.py#retired_by_rule`. The clause names that second condition; `open_rows` now delegates to `unverified_check.py#todo_open_rows` and still honours `drained`, which `test_a_drained_line_closes_the_file` executes. **Re-read 2026-09-25 by work item 1790260566 (#487), phase 3:** `open_rows`' docstring alone changed: `.github/scripts/fold_ledger.py#open_rows` is now the same function, loaded from the shipped reader. The rule it asks moved as well — `drained` counts only on a live line, and a row inside a fenced block that closes is not a row — and the guard's refusal is unchanged. **Re-read 2026-09-25 by work item 1790297085 (#602), phase 2:** the rule arm now asks `retired_by_rule` of the merge base of `--released-at` and `HEAD` as well as of the tree, and keeps a directory that passes the tree and fails the base; a `found` with no base is refused at exit 2. That narrows *nothing in its record is open* further and widens nothing, so *only where* still holds; the marker arm and the evidence-todo refusal are unchanged **Re-read 2026-09-25 in round 1's fix pass of work item 1790297085 (#602):** `retire` prints the kept-at-base heading through `base_heading`, which names the merge that moves a base the ref has moved past, and its no-base refusal is a shared sentence; what it removes is unchanged, so the claim holds |
| S5 · The prerequisites of a fold are not the two readers `docs/one-root-by-lifetime.md` §*The dependency rule* names: they are those two, the survivor sweep, and every check in the repository carrying a population floor over `seal/specs/` | `skills/settle/SKILL.md#"### 3. Answer every check that reads the corpus"@33612685`, `tests/test_settle_reads_before_it_removes.py#test_the_skill_names_the_floors_a_fold_has_to_answer@905c4485` | **Read**, not executed, 2026-09-22. `spec.md` §*The ticket's headline claim is false* establishes the figures — fourteen test modules reading the corpus and at least six carrying a floor, three re-opened by the framer — and nothing here re-measured them, because measuring them is what the fold branch does and no fold ran. What was executed is the case: it was red with *is not the whole list* removed from the skill. Who answers whether the six are still six is the fold's own work item | 2026-09-22 | The rule is in the skill rather than in this repository's documents because it is general: any project running this methodology accumulates checks over its own records, and a floor is exactly the shape a check takes when its author worried about a vacuous pass. `docs/one-root-by-lifetime.md` §*The dependency rule* was written when this repository had 13 work items |

<!-- specs/1790039346-settle-reads-a-marker-inside-a-commented-out-draft -->
### 1790039346-settle-reads-a-marker-inside-a-commented-out-draft

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| R1 · The opt-out refusal reads the scratch marker as a FILE, the way `hooks/optin.py#home_at` reads it, so a directory of that name is refused as no root rather than told to delete "that file" to turn the gates back on | `skills/settle/scripts/settle.py#main@b8fbc4bb`, `tests/test_settle_reads_before_it_removes.py#test_a_directory_of_the_markers_name_is_not_an_opt_out@f0699c3e` | **Executed** 2026-09-22. Red against `3cdfd8ad`: a repository with no `seal/` at either place and a DIRECTORY named `specseal-scratch` under `.git/` was told *has opted out … Delete that file to turn them back on*. Green once `exists` became `isfile`; the module's 54 cases pass at exit 0, read with `echo $?` and not through a pipe | 2026-09-22 | Round 3's finding 1. The class is three readers of one marker — `hooks/optin.py#home_at`, `skills/implement/scripts/seal.py#no_root` and this arm — and this arm was the one that disagreed. `hooks/root-migrate.py` reads the pre-#80 marker to refuse a move and is outside the class **Re-read 2026-09-23 by work item 1790138190 (#517), phase 3**, which changed `main` by the `--retire` help text alone, naming the rule arm and the anchored guard; the refusal paths this row is about are untouched **Re-read 2026-09-23 by work item 1790138190 (#517), phase 5**: `main` now exits 0 on a root with no work item; the opt-out refusal above it is untouched. **Re-read 2026-09-25 by work item 1790297085 (#602), phase 2**: `main` gained a refusal at exit 2 for a `--released-at` sharing no commit with `HEAD`, after `survey` and far below the opt-out arm, and a longer `--released-at` help text; the opt-out refusal is untouched. **Re-read 2026-09-25 in round 1's fix pass of work item 1790297085 (#602):** `main`'s no-merge-base refusal now names a clone too shallow to reach the shared commit, and its comment says when the local merge base is CI's; the opt-out refusal is untouched |
| R2 · The refusal path resolves the common git directory once and hands it to `home_at(root, common)`, the parameter `hooks/optin.py` documents for exactly this caller | `skills/settle/scripts/settle.py#main@b8fbc4bb`, `tests/test_settle_reads_before_it_removes.py#test_the_refusal_path_asks_for_the_common_directory_once@1ed54cbc` | **Executed** 2026-09-22. Counted through the `optin` module `settle.load` hands `main`, and counted as calls rather than processes because the resolver's `.git`-directory fast path costs none: two calls against `3cdfd8ad` with `home_at` receiving `None`, one call after with `home_at` receiving the value | 2026-09-22 | Round 3's finding 5, the ⬜ it left on the same twenty lines finding 1 opens **Re-read 2026-09-23 by work item 1790138190 (#517), phase 3**, which changed `main` by the `--retire` help text alone, naming the rule arm and the anchored guard; the refusal paths this row is about are untouched **Re-read 2026-09-23 by work item 1790138190 (#517), phase 5**: the settled-root arm comes after the common directory is resolved once; that resolution is untouched. **Re-read 2026-09-25 by work item 1790297085 (#602), phase 2**: `main`'s new no-merge-base refusal sits after `survey`; the common directory is still resolved once and handed to `home_at`, and the anchored case re-ran green. **Re-read 2026-09-25 in round 1's fix pass of work item 1790297085 (#602):** only `main`'s no-merge-base sentence and its comment changed; the common directory is still resolved once, and the anchored case re-ran green |
| R6 · `settle.py#coordinates` sections the shared ledger and reads a fragment by the same rule: a marker inside a commented-out draft opens no section, an opener quoted inside a code span parks nothing, a fragment's fenced or parked example row is not the fragment's own, and this repository's ledger sections the ids the fence-only reading sections | `skills/settle/scripts/settle.py#coordinates@8a11661a`, `tests/test_settle_reads_before_it_removes.py#test_a_parked_marker_in_the_ledger_opens_no_section@68b0d487`, `tests/test_settle_reads_before_it_removes.py#test_an_opener_quoted_inside_a_code_span_parks_nothing@eba644ad`, `tests/test_settle_reads_before_it_removes.py#test_the_rule_over_this_repositorys_ledger_loses_no_section@35b4fb60`, `tests/test_settle_reads_before_it_removes.py#test_a_fragments_quoted_coordinates_are_not_the_fragments_own@a666209f` | **Executed** 2026-09-22. A2 and A5 red against `3cdfd8ad`: the parked row was attributed to a section, and the fragment's parked row was the fragment's own. A3 and A4 red with the span pass removed from `live_lines`, A4 naming three ids missing from the real ledger's sectioning — `1788472135-the-run-outlives-its-last-finding`, `1788613827-a-runs-report-carries-one-comparison-table`, `1788844127-the-reviewers-report-reaches-the-record-retyped` — which are the three markers round 3 measured lost. A5 red again under round 3's M7, the fragments loop back to a whole-file `finditer`, which had turned nothing red before. `bin/settle` at the build's tip: `released and unfolded: 81 work items in 37 segments, 16 ungrouped, 0 skipped`, exit 0 read with `echo $?` | 2026-09-22 | A2's fixture puts the draft's row BEFORE its marker. HTML comments do not nest, so a marker inside a draft closes the draft with its own `-->`, and a row after it is live again and falls to the section that was open — `comment_scan`'s rule, which `seal/ledger.md` pins, and the case pins that reading with the reason beside it. **Re-read 2026-09-22 in round 1's fix pass**: the docstring now also names the THIRD quotation it does not answer, markdown's indented code block, with the reason it is answered by convention rather than by the reader. No loop changed. **Re-read again 2026-09-22 in round 2's fix pass**, which corrected one ground in that paragraph: the refusal a widened fence reader actually meets is `tests/test_the_record_is_generated.py#test_a_continuation_that_looks_like_an_opener_is_still_joined`, executed, and NOT `reader_blanking_passes`, which stays green because it reads the calls `readable` makes by name. The decision to leave the door open is unchanged and the loops are untouched **Re-read 2026-09-24 (#547)**: the same loop now runs over each release file as well, because a release file is the section byte for byte; a marked section in `seal/releases/0.2.0.md` is attributed by the same rule (case red first). **Re-read 2026-09-24 by work item 1790208593 (#547), phase 4:** the real-corpus case reads `seal/ledger.md` and every release file, so it holds after the one-time split; measured green in both shapes of this tree **Re-read 2026-09-24 by work item 1790260566 (#491), phase 1:** only the docstring's indented-code-block paragraph changed, to name the shared delimiter rule `unverified_check.py#fence_opener` where it named `blank_fences`; the decision and both loops are untouched **Re-read 2026-09-25 by work item 1790260566, round 1's fix pass:** one docstring phrase narrowed, from the rule every reader shares to the rule the ledger readers share; the loops are untouched |
| R7 · A closing delimiter quoted in prose still ends a parked draft, and an indented example row in a fragment is counted as the fragment's own — the first fixed, the second decided and pinned as it stands | `tests/test_settle_reads_before_it_removes.py#test_a_closer_quoted_in_prose_still_closes_a_parked_draft@fc05da6b`, `tests/test_settle_reads_before_it_removes.py#test_an_indented_example_row_is_counted_and_the_reader_says_so@2933d5fd`, `skills/verify/scripts/unverified_check.py#OPENER@d8752669` | **Executed** 2026-09-22 in round 1's fix pass. The first case was red against `adb5607d`'s parent on its first assertion, the row reported under `1700000003-gamma` instead of beta's section, and is red again under the mutation that drops `holding=OPENER`. The second is green by construction — it pins a door left open — and is red under a mutation that skips an indented line in the fragments loop, so it is not vacuous. A third mutation, the predicate blanking nothing, reddens seven cases including all five span shapes | 2026-09-22 | Round 1's findings 1 and 3. `folded_items` is unreachable through the indented door: `FOLD_MARKER` is line-anchored and an indented marker returns the empty set, executed the same day. Widening `blank_fences` would move `readable`, `check_text`, `round_record.py` and the review-history guard at once, which is why the third door is named in a docstring and a case rather than closed. **Re-read 2026-09-22 in round 2's fix pass**: the case docstring named the wrong guard and now names the measured one. What the case asserts is unchanged |
| R11 · A line is live only where BOTH span readings call it live — the one where an unclosed backtick run is literal text and the one where it is a span reaching to its partner — so nothing has to decide where markdown's block ends, and a disagreement parks the line | `skills/verify/scripts/unverified_check.py#live_lines@cee4ff98`, `skills/verify/scripts/unverified_check.py#_liveness@13b14119`, `skills/verify/scripts/unverified_check.py#_partner_ahead@00bac1d4`, `tests/test_unverified_rows_close.py#test_every_shape_five_review_rounds_named@8070335f`, `tests/test_unverified_rows_close.py#test_the_scan_never_reads_live_what_the_format_parks@b88a4350` | **Executed** 2026-09-22 in round 5's fix pass. Round 5's shape — a paragraph with an unclosed backtick, a draft opener on the next line, the partner after it, a marker below — read live at `62f58ea5` and parks here; verified at both ends, `release/v0.13.0` also parks it, so the defect was this branch's. All seven shapes the five rounds named answer as the design table says, and two of the seven are red against the shipped reader: round 5's shape and the safety fuzz. Five mutations, each applied alone and restored: the literal reading alone reddens the seven-shape family and the fuzz; the crossing reading alone reddens the family; dropping the table-row rule reddens the coordinate invariant, the fuzz and rounds 1 and 2's cases; dropping the block-quote rule reddens the fuzz alone; keeping backticks special inside a comment reddens eight. Four modules 249 passed exit 0, ruff clean, `/usr/bin/python3` 3.9.6 answers the round-5 shape correctly | 2026-09-22 | Rounds 1 to 4 guessed at the comment state from a stateless pass; round 4's scan fixed that and guessed instead at markdown's block structure, which round 5 caught. Computing both readings ends the class because there is no third thing left to be wrong about. **Two doors close together**: round 5's, which was ours, and the multi-line code span `overview.md` §*Not done* had carried since round 2 **Re-read 2026-09-24 by work item 1790260566 (#491), phase 1:** `_liveness` now asks `fence_opener` and `fence_closes`, so a closer carrying an info string no longer closes a block and a backtick opener whose info string holds a backtick no longer opens one. The two-readings rule is untouched, and the safety fuzz stays green against an oracle given the same two clauses of CommonMark 4.5 |
| R12 · The crossing reading is bounded by where a paragraph ends, and the bound is a list of block starts rather than a guess at the grammar — because an incomplete list here cannot delete a directory, only park a line | `skills/verify/scripts/unverified_check.py#_paragraph_ends_at@92b6c912`, `tests/test_settle_reads_before_it_removes.py#test_no_section_of_this_repositorys_ledger_loses_a_coordinate@fdf39e69`, `tests/test_unverified_rows_close.py#test_the_three_named_markers_are_live_in_this_repositorys_ledger@2fe71173` | **Executed** 2026-09-22, and the bound was chosen by measurement rather than assumed. Unbounded, the crossing reading takes `seal/ledger.md` from 761 not-live lines to 1,829 and from 94 markers over 83 ids to 20 over 20, because in a table block a stray backtick finds a partner rows later. Bounded by a blank line alone — the one rule CommonMark states unconditionally — it keeps all 94 markers and all 83 ids at 778 not-live, and still takes three work items from 12, 57 and 29 coordinates to 5, 40 and 19. With the table row added every section matches the fence-only reading coordinate for coordinate, and the figures return to 761 / 94 / 83 | 2026-09-22 | The first draft of this row argued that reaching further could only park more, so an incomplete list was safe. **That argument is false and the fuzz disproved it**: reaching further changes which runs PAIR, and consuming a partner early can leave a later line live — minimised to three lines, where the crossing reading pairing across a block quote frees the line below. So the list has to end the paragraph where the format does, and what watches it is `test_the_scan_never_reads_live_what_the_format_parks` against an oracle written from the specification rather than from this module **Re-read 2026-09-24 by work item 1790208593 (#553):** the case's docstring alone changed — the ledger no longer doubles a marker line, and the reading stays per occurrence regardless **Re-read 2026-09-24 by work item 1790208593 (#547), phase 4:** both real-corpus cases read every release file beside `seal/ledger.md`, per file, green in both shapes of the tree **Re-read 2026-09-24 by work item 1790260566 (#491), phase 1:** the ATX branch gained the three-space bound the oracle always had, and the fence stop asks `fence_opener`, so only an opener ends a paragraph. The bound is still a list of the format's block starts |
| R13 · The scan's precedence is fixed and pinned — a fence opener is decided before anything else on its line, and after that whichever of a comment opener and a backtick run comes first in the text wins — and `folded_items` asks `live_lines` for all of it | `skills/verify/scripts/unverified_check.py#folded_items@7a8d3c99`, `tests/test_unverified_rows_close.py#test_the_scan_decides_a_fence_before_a_comment_or_a_span@e61ea481`, `tests/test_unverified_rows_close.py#test_the_first_delimiter_on_the_line_wins@48805d40`, `tests/test_unverified_rows_close.py#test_a_code_span_closes_at_a_backtick_string_of_equal_length@2c7dc94b`, `tests/test_unverified_rows_close.py#test_strip_comments_reads_through_the_one_comment_scanner@5ad8cedb` | **Executed** 2026-09-22. Each is red under the mutation named for it, applied alone and restored: a scan that stops treating a fence opener as a fence, a comment opener that always beats a backtick run, a span closing at any run rather than an equal-length one, and a private copy of the comment walk inside `strip_comments`. The precedence pair and the span rule are unchanged by round 5's rewrite — both readings share them, and only the unclosed-run question differs | 2026-09-22 | Re-homed from rows this pass removed, whose anchors went with `is_block_boundary` and the renamed fuzz case. `comment_scan` has one reader left, so what the scanner case still pins is that the reader keeps no private copy of the walk, which is the shape #487 is open about |
| R14 · A fence delimiter is bounded to three spaces of indentation, in the reader AND in the oracle that judges it, because a deeper one is an indented code block and reading it as a delimiter inverts the fence state for the rest of the file | `skills/verify/scripts/unverified_check.py#FENCE_RE@e3109eb0`, `tests/test_unverified_rows_close.py#a_reading_from_the_commonmark_rules@aec41270`, `tests/test_unverified_rows_close.py#test_every_shape_five_review_rounds_named@8070335f` | **Executed** 2026-09-22 in round 6's fix pass, and the two bounds move together because moving one alone is a trap: with the reader bounded and the oracle still stripping the line, the safety case reports the CORRECTED reader unsafe on five lines of the fenced-example document. Both shapes were red against the shipped pair — a real fenced example under a phantom opener and a phantom pair straddling a draft, each taking `settle --retire` to exit 0 with the directory removed. Bounding `FENCE_RE` alone moves **0 lines in all 1,460 `.md` files** of the repository, re-derived rather than carried. `seal/ledger.md` stays at 761 not-live, 94 marker occurrences, 83 unique ids, the three named markers live at 767, 992 and 1619; the eleven-file corpus at 903 | 2026-09-22 | Round 6's findings 1 and 2, which are one fix. The laxness was inherited — `release/v0.13.0` deletes for this shape too — but `FENCE_RE` is a unit this branch created and the oracle is entirely this branch's, so the case that could not see the class it was written for is ours outright **Re-read 2026-09-24 by work item 1790260566 (#491), phase 1:** `FENCE_RE` gained its info-string group and is the first half of `fence_opener` and `fence_closes`, which `blank_fences` now asks too — the reader the bound had not reached. The oracle gained the same two info-string clauses. The claim holds and now covers every fence walk in the module |
| R15 · The paragraph rule stops where markdown stops and nowhere else, and the case that watches it can build the shapes it can be wrong about | `skills/verify/scripts/unverified_check.py#_paragraph_ends_at@92b6c912`, `tests/test_unverified_rows_close.py#documents_with_several_spans_on_a_line@05d2d9bb`, `tests/test_unverified_rows_close.py#test_the_three_named_markers_are_live_in_this_repositorys_ledger@2fe71173` | **Executed** 2026-09-22. Four over-stops removed or bounded — a hash with no space, seven hashes, an ordered marker that is not 1 — and one missing stop added, the setext underline (CommonMark 4.3). The rules disagree on 944 lines of the repository's own `.md` files, and liveness changes on **6 lines across 4 files, every one under `seal/specs/` and none in `docs/`, `seal/ledger.md` or `seal/ledger/`**: five toward parked and one toward live, all of them prose beginning with an issue reference. **Reverting any of the three paragraph corrections reddened nothing until the generator was widened**, which is round 6's finding 2 one level over; with `#nospace`, seven hashes, `3. item`, `===` and an indented delimiter as tokens, and a quarter of lines emitted as a single token, each revert reddens `test_the_scan_never_reads_live_what_the_format_parks` | 2026-09-22 | Round 6's findings 3, 4 and 5. The docstring had stated *being incomplete here is safe* as the design's whole safety argument, which round 5's own fuzz disproved and which reached the ledger and not the sentence the next editor reads; it now says incomplete is CHEAP rather than safe and why. The corpus floor counts OCCURRENCES rather than keying a dictionary on the marker line — `seal/ledger.md` carries eleven marker lines twice, so a regression parking the first of a pair was invisible, and a mutation that parks exactly that now reddens the case **Re-read 2026-09-24 by work item 1790208593 (#553):** docstring-only change to the cited case, as in R12; its assertions are untouched **Re-read 2026-09-24 by work item 1790208593 (#547), phase 4:** the cited case reads every release file beside `seal/ledger.md`, per file; its three markers stay required **Re-read 2026-09-24 by work item 1790260566 (#491), phase 1:** the ATX branch is bounded to three spaces and the fence stop is an opener only, both the format's own rules; the cited cases are unchanged and green |
