## 0.15.1 — 2026-09-24

<!-- specs/1790206435-the-sweep-reads-a-code-idiom-as-removed-wording -->
### 1790206435-the-sweep-reads-a-code-idiom-as-removed-wording

| P1 · a Python file's prose is its comments, docstrings and string literals: every other token is blanked with a sentence end where it stood, so a code token between two literals ends the sentence and two literals across a line break are one, line numbers are intact, and a file the tokenizer refuses is read whole as before | `skills/code-review/scripts/survivor_check.py#python_prose@8bdedcfe`, `skills/code-review/scripts/survivor_check.py#PROSE_TOKENS@412c0224`, `skills/code-review/scripts/survivor_check.py#STRUCTURE_TOKENS@ecdcec03`, `skills/code-review/scripts/survivor_check.py#sentences@52d4b903`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_loop_rewritten_in_one_file_is_not_reported_at_every_other_loop@449edc51`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_docstring_corrected_in_one_file_is_reported_where_prose_carries_it@b16e373c`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_code_token_between_two_literals_ends_the_sentence@2431f1b0`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_fstring_expression_ends_the_sentence_and_its_quotes_do_not@60326934`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_file_the_tokenizer_refuses_is_read_whole_as_before@ff2ce64a` | **Executed** 2026-09-24. S1 seen red at `a5c5cadd` at exit 1 naming the other module's loop at 3.00, S4 red with the key `first half name second half`; S2 (comment and docstring carriers) and S5 (an unterminated string) green before and after. Mutated at `476af109` from Python, the file restored byte-identical: the fallback returning nothing, a space where the `\|` goes, `sentences` never calling the reader, `COMMENT` out of the kept kinds, `FSTRING_END` out of the structure set — each red in its own cases; `INDENT` out of the structure set green, which the comment states (noise only) | 2026-09-24 | `STRUCTURE_TOKENS` began as ten names and `NL`, `NEWLINE`, `DEDENT`, `ENDMARKER` and `ENCODING` were measured to change no output — they stand past a line's text or on the next token's column, where the position guard writes nothing — so they were removed as members that cannot change the answer. The refusal set is `TokenError`, `SyntaxError` and `ValueError`. #269's pin now reports at the literal's own line (450) rather than at the head of its code block (447) |
| P2 · over the four squash commits of the 0.15.0 release, resolved at each tip, the sweep reports the prose and none of the code: `576fe39d` 4 places, `cc49ae64` 0, `3dd24073` 8, `d2f2c0dc` 1, pinned coordinate for coordinate and skipped when a commit does not resolve | `tests/test_a_corrected_sentence_survives_elsewhere.py#RELEASE_RANGES@cd748af5`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_four_real_ranges_report_their_prose_and_none_of_their_code@ffd41f8c` | **Executed** 2026-09-24 at `93acc277`: the four reports reproduce the framer's at `a5c5cadd` byte for byte before the build (5, 2, 9, 5), then 4, 1, 8, 1 after phase 1 and 4, 0, 8, 1 after phase 2. The six function-body places and the two released-changelog places are gone; the founding survivors are unchanged at 2.00 and 1.89 / 1.78 | 2026-09-24 | Two prose coordinates moved, both the weighting: the sweep's `seal/ledger.md` place at its line 2053 left B's range at phase 1 (2.47 against a docstring joined to three assert literals by code; one literal per sentence its best source is one run at 1.00), and its `skills/code-review/scripts/survivor_check.py` place at line 142 joined it at phase 2 (1.51 → 1.61 with nine gathered fragments out of the pool). So B is 8 where the frame expected 9 and then 7; `phases/phase-1.md` and `phases/phase-2.md` carry the scores. The case pins a set rather than a count so the next movement is seen |
| P3 · the module docstring states what a sentence is in a Python file, in a section of its own, and the case reads the section by its heading | `skills/code-review/scripts/survivor_check.py#"## What a sentence is in a Python file"@7a67a672`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_docstring_states_what_a_sentence_is_in_a_python_file@07ee2351` | **Executed** 2026-09-24: red at `a5c5cadd` with the heading absent, green at `476af109` | 2026-09-24 | `agent-contract` §14: the rule that changed the verdict is stated where a reader looks for the module's account of itself. Comment text keeps its `#`, so a comment block is still one sentence per line and the reading is subtractive — the section says so, and `spec.md` §*Out* names the wider reading as somebody else's change |
| C1 · a released changelog section — the heading naming a version in the root `CHANGELOG.md` and every line under it, up to the next `## ` heading — is out of the sweep on both sides, and `## Unreleased` stays in | `skills/code-review/scripts/survivor_check.py#blank_released@a7d38107`, `skills/code-review/scripts/survivor_check.py#VERSION_HEADING@5a4c644c`, `skills/code-review/scripts/survivor_check.py#CHANGELOG@0cad39a3`, `skills/code-review/scripts/survivor_check.py#sentences@52d4b903`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_released_changelog_section_is_not_a_carrier@985a3b0a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_unreleased_changelog_section_is_a_carrier@7fe7a7c8`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_range_that_edits_only_a_released_section_removes_no_sentence@327e8a17` | **Executed** 2026-09-24. S7 seen red at `476af109` under all three heading spellings (`## 1.0.0 — <date>`, `## [1.2.0] - <date>`, `## v1.2.0`) at exit 1 naming `CHANGELOG.md`, S9 red at `against 1 sentence(s)`; S8 green before and after. Mutated at `93acc277`: the blank never applied, 6 red; `VERSION_HEADING` matching `## Unreleased`, S8 red | 2026-09-24 | Read off the heading rather than the file left out by path, because a repository following `agents/smith.md`'s *let the entry accumulate unreleased* keeps live prose in the same file; in this repository the whole file is released sections and the two readings agree. On the real ranges the two `CHANGELOG.md` places (line 2252 on C, line 2090 on B) are gone and the `against N` counts fell where a range had edited the file |
| C2 · a `seal/specs/<id>/changelog.md` whose `<!-- specs/<id> -->` marker stands in `CHANGELOG.md` at the range's tip is out of the sweep on both sides, an ungathered one is in, and the marker is read by the sweep itself off one `read_blobs` call with no path list | `skills/code-review/scripts/survivor_check.py#gathered_fragments@f3e5ba56`, `skills/code-review/scripts/survivor_check.py#a_gathered_fragment@a9ab13f3`, `skills/code-review/scripts/survivor_check.py#MARKER@1ed4fe02`, `skills/code-review/scripts/survivor_check.py#corrected@3502a088`, `skills/code-review/scripts/survivor_check.py#corpus@672d8026`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_fragment_standing_in_the_pool_is_not_a_survivor@8dff8dee`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_fragment_the_range_edited_is_not_a_source@34a2ba07`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_ungathered_fragment_is_still_a_carrier@286d4b19` | **Executed** 2026-09-24. The range arm seen red at `476af109` at `against 1 sentence(s)`; the pool arm was green there for the wrong reason — the same sentence in the released body and the fragment halved each other's weight to 1.49 — so the fixture's released body carries an unrelated entry and the arm is shown red by mutation instead: at `93acc277` the predicate returning `False` reddens both gathered arms, ignoring the gathered set reddens the ungathered arm, and `corpus` or `corrected` dropping the predicate reddens its own arm. `test_every_path_list_this_module_derives_from_git_is_filtered_or_named` green with no new path list | 2026-09-24 | Q4's default: a sibling of `records_a_past_state` rather than a parameter on it, because that predicate is a pure function of the path and this one needs a file's contents. `MARKER` is spelled here in `unverified_check.py#FOLD_MARKER`'s shape rather than imported from `gather_changelog.py`, which is release automation a shipped script does not depend on. **Re-read 2026-09-24, phase 5 (#551)**: `corrected` and `corpus` re-anchored after `corrected` gained `--no-renames`; the predicate's application is unchanged. **Re-read 2026-09-24 in round 1's fix pass (🟡 1)**: `corrected` re-anchored again for the held count over the tip's released sections (row F1); the gathered-fragment predicate is unchanged. **Re-read 2026-09-24 in round 2's fix pass**: the held count is now what the range released (row F1, corrected); the predicate is unchanged. **Corrected 2026-09-24 by work item 1790221963 (#557)**: the note above ended by saying the `lost` guard is what keeps a gathered release's text out of `written`. That holds only where `CHANGELOG.md` loses no sentence: a release that renames `## Unreleased` or rewords an entry loses one, and round 3 of this work item executed the gathered text reaching `written` in both shapes. What keeps it out now is `corrected`'s held set: each gathered fragment is read at the range's left end by `seal/specs/<id>/changelog.md` and its sentences are dropped from what a release writes (row F1). `a_gathered_fragment` is re-anchored for a docstring that says the release leaves the file standing or deletes it, where it said the release retires it; the predicate's body is unchanged. **Re-read 2026-09-24 in work item 1790221963's round 1 fix pass**: a held gathered sentence now writes the n-grams it shares with a sentence `CHANGELOG.md` lost (row F1); the predicate and the marker reading are unchanged. **Re-read 2026-09-24 in that work item's round 2 fix pass**: those n-grams are now subtracted from `CHANGELOG.md`'s removed sentences alone (row F1, corrected); the predicate is unchanged |
| F1 · a release commit that moves `## Unreleased` under a version heading removes no sentence: for `CHANGELOG.md` the sentences **this range** put under a version heading — the released sentences at the tip beyond those at the range's left end (`newly_released`) — are added to the held count before the difference is taken, so a document restating an entry is not reported at the release; a sentence standing in an older release holds nothing; and where the file lost a sentence, the released sentences' wording is written as any file's is, so an entry reworded as it is released still splits the removed sentence into the runs it no longer shares — except a gathered fragment's wording, which is held and not written: the fragment's own branch wrote it, not this range, and the n-grams it shares with a sentence the file itself lost are subtracted from this file's removed sentences alone | `skills/code-review/scripts/survivor_check.py#newly_released@9f729ae3`, `skills/code-review/scripts/survivor_check.py#only_released@3d9efeb5`, `skills/code-review/scripts/survivor_check.py#corrected@3502a088`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_moves_the_unreleased_section_under_a_version_removes_nothing@dd5a023e`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_rewords_an_entry_still_reports_its_verbatim_copy@799f7a27`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_sentence_in_an_older_release_does_not_hold_the_unreleased_one@3f67aa7e` | **Executed** 2026-09-24. Round 1's 🟡 1: the pure-release case red at `64f42602` at exit 1, `against 3 sentence(s)`, `docs/a.md` at 2.00; green after. Round 2's 🟡 1 and 🟡 2: the reworded release red at `1f8cdcda` at exit 0 with `against 2 sentence(s)`, and the older-release case red there at exit 0 with `against 0 sentence(s)`; both green after, the pure-release case and S17/S18 still green, the module at 96. The four real ranges re-run after each fix: the same coordinates and removed-sentence counts (60, 61, 35, 156), because no squash range releases | 2026-09-24 | **Corrected 2026-09-24 in round 2's fix pass**: this row first said the held set was every sentence under a version heading at the tip, *counted and never written back*, with the wrong allow *a sentence a release both moved under a heading and removed from a live document in one commit — which no release commit does*. Round 2 executed both halves false: withholding the wording from `written` merged a reworded entry's removed sentence into one run at 1.00 and silenced its verbatim copy (a regression against the base), and holding every released section let an older release's copy hold any range's unreleased sentence. The bound the code has now: a live sentence counts as held only if this range wrote the identical sentence under a version heading, which is a move and not a correction. The `lost` guard writes nothing when the file lost nothing. `plan.md` §*Operational impact* carries the same bound. **Corrected 2026-09-24 by work item 1790221963 (#557, #555)**: the note went on to say the guard *keeps a gathered release's text out of `written`*. Round 3 of this work item executed that false wherever the file loses a sentence (H1, H1k, H2 and H2k at exit 0): a release that renames `## Unreleased` or rewords an entry reached the write-back with the gathered fragments' text in it, and a fragment quoting wording the same commit corrected subtracted the survivor. The claim now carries the gathered-fragment clause. That work item reads each gathered fragment at the range's left end and drops its sentences after the guard, blanks a marker line in `newly_released` so a fragment opening with prose keeps its own keys, and pins the guard and the filter apart; its rows are in `seal/ledger/1790221963-a-release-writes-the-gathered-text-back.md`. This work item's `plan.md` gate row is left as written, a record of what it planned; the corrected bound is in that work item's `plan.md` §*Operational impact*. **Corrected 2026-09-24 in round 1's fix pass of work item 1790221963**: the clause first said a gathered fragment's wording is *held and never written*; withheld whole, a gathered rewording of a lost live entry merged that entry's runs and silenced its verbatim copy, so the clause now says only the n-grams shared with a sentence the file lost are written (that work item's row H1). **Corrected 2026-09-24 in that work item's round 2 fix pass**: written for every file, those n-grams let a gathered fragment subtract another file's survivor again; they are now subtracted from `CHANGELOG.md`'s removed sentences alone, where `score` scores them, and the clause says so |
| C3 · the module docstring's exclusion section names the released section and the gathered fragment as its fourth member, with the pool, the range and both sides, and the pinning case reads it fourth in the list | `tests/test_a_corrected_sentence_survives_elsewhere.py#EXCLUSIONS@9e718481`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_docstring_names_both_sides_of_the_round_record_exclusion@1abe4cd8` | **Executed** 2026-09-24: red at `476af109` with the paragraph absent, red at `93acc277` under mutation with the opener's closing `.**` removed, green otherwise | 2026-09-24 | The paragraph also states the range-side reason nothing else does: a release's gathering commit writes each fragment's text under a heading that is blanked, so where that commit deletes or edits a gathered fragment, without the predicate its sentences would count as removed at the release and the work items' own `spec.md` and `overview.md` would be reported. **Corrected 2026-09-24 by work item 1790221963 (#557)**: this note said the gathering commit *deletes each fragment*. `.github/scripts/gather_changelog.py` deletes nothing, and the docstring paragraph now names both a gatherer that leaves the fragment and one that deletes it; the anchors are untouched |
| U1 · an unresolved `survivors.md` declaration is judged by the second anchor before it is printed: owned by a work item this range touches nothing of, it prints nothing and silences nothing; with no owner, or owned by a work item the range touches, it prints under `unresolved` as before | `skills/code-review/scripts/survivor_check.py#whole_range@5c8d9d1b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_unresolved_declaration_of_another_work_item_prints_nothing@6e08c85c`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_unresolved_declaration_of_the_work_item_the_range_touches_prints@4eb1e107`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_range_row_that_does_not_resolve_silences_nothing_and_says_so@72f6af26` | **Executed** 2026-09-24. S13 seen red at `93acc277` with `unresolved  origin/gone..HEAD …` printed; S14's two arms green before and after. Mutated at `78610546`: appending before ownership, S13 red; never appending, both S14 arms red; `owner` always `None`, four red. The branch's own sweep with every `seal/specs/*/survivors.md` over `origin/release/v0.15.1...HEAD` prints zero `unresolved` and zero `not yours` lines where the same command at `9f846733` printed three | 2026-09-24 | The wrong allow is empty because an unresolved row excuses nothing whether printed or not; the wrong deny was three lines on every pull request into a release branch and every seal of one release. The one `git diff --name-only` call site in the function is unchanged, so `NAMED_EXCEPTION`'s grounds still rest on `foreign` in the body. **Re-read 2026-09-24, phase 5 (#551)**: that call gained `--no-renames`; the ownership question is unchanged |
| R1 · a file moved and reworded in one commit — a rename to git — is read as a deletion plus an addition, so the reworded sentence is removed, not written back, and its copy standing in another file is reported; a file moved verbatim is silent because every removed sentence is written back and `wanted` subtracts it, with the old path read rather than never listed | `skills/code-review/scripts/survivor_check.py#corrected@3502a088`, `skills/code-review/scripts/survivor_check.py#whole_range@5c8d9d1b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_file_moved_as_a_rename_with_one_sentence_reworded_is_still_measured@00f31ae8`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_file_moved_verbatim_is_silent_because_its_wording_is_written_back@29362265`, `tests/test_a_corrected_sentence_survives_elsewhere.py#moved_section@14dfa6d0` | **Executed** 2026-09-24. Both cases seen red at `6a424d81`, each at `against 0 sentence(s)`: the reworded rename at exit 0 where 1 is owed, and the verbatim move silent with the old path never read. Green after `--no-renames`; each case asserts that git reports the move as `R`, so the fixture cannot quietly become the delete-plus-add shape. The four real ranges re-run: the same coordinates and the same removed-sentence counts (60, 61, 35, 156) as after phase 2, so no rename inside those squash ranges changed a number | 2026-09-24 | #551, added to this work item as phase 5 by the owner after phase 3's Q5 probe (row U2). The failure direction reverses: before, a rename silenced a real survivor; after, the wrong direction is noise on a pure move, which `wanted`'s subtraction of what is written back already answers. `whole_range`'s `changed` list takes the same flag so a file moved out of a work item's directory counts as touching it; no case pins that arm, and `phases/phase-5.md` says so. **Re-read 2026-09-24 in round 1's fix pass (🟡 1)**: `corrected` re-anchored for the held count (row F1); the rename reading is unchanged. **Re-read 2026-09-24 in round 2's fix pass**: re-anchored with row F1's correction; S17 and S18 re-run green, and the rename reading is unchanged. **Re-read 2026-09-24 by work item 1790221963 (#557, #555), phase 4**: `corrected` re-anchored for the gathered-text filter (row F1's second correction), which acts on what a release writes after the path list is read; the rename reading is unchanged. **Re-read 2026-09-24 in that work item's round 1 fix pass**: re-anchored for the held sentences' shared n-grams, `CHANGELOG.md`'s alone; the rename reading is unchanged. **Re-read 2026-09-24 in that work item's round 2 fix pass**: re-anchored for `corrected`'s third return; the rename reading is unchanged |
| U2 · a section moved whole between two files that both remain reports nothing, and a whole file moved as a rename reports nothing even when one sentence in it is reworded, because `git diff --name-only` runs with rename detection on and lists the new path alone | `skills/code-review/scripts/survivor_check.py#corrected@3502a088` | **Executed** 2026-09-24 from Python in throwaway repositories, removed afterwards: a two-sentence section moved verbatim to `b.md` exits 0 at `against 0 sentence(s)`; moved with its claim reworded it exits 1 naming `notes.md` at 2.00; a forty-paragraph section moved with one sentence reworded is `R096` to git and exits 0 at `against 0`, the reworded claim's copy in `notes.md` unreported | 2026-09-24 | The first two are what `spec.md` judgment 7 predicted; the third was a defect the sweep carried, found here and handed back, which the owner filed as #551 and added to this work item as phase 5. **Repaired 2026-09-24 in phase 5**: row R1 below is the fix and its two cases, and this row stays as the measurement that found it — its claim is what the sweep did BEFORE `--no-renames`, at `78610546`. **Re-read 2026-09-24 in round 1's fix pass**: anchor re-stamped with `corrected`; the claim is a dated measurement and is unchanged. **Re-read 2026-09-24 in round 2's fix pass**: re-stamped again with `corrected`; unchanged. **Re-read 2026-09-24 by work item 1790221963 (#557, #555), phase 4**: re-stamped with `corrected`'s gathered-text filter; the claim is a dated measurement and is unchanged. **Re-read 2026-09-24 in that work item's round 1 fix pass**: re-stamped again; unchanged. **Re-read 2026-09-24 in its round 2 fix pass**: re-stamped for `corrected`'s third return; unchanged |

<!-- specs/1790206436-the-runs-instruments-cost-wall-clock -->
### 1790206436-the-runs-instruments-cost-wall-clock

| P1 · `bin/test` builds and adopts an environment that carries `pytest-xdist`: `PACKAGES` names it beside `pytest` for both build strategies; an adopted `.venv` whose site-packages holds no `xdist` package directory takes exactly one install step, by the same tool order `build` uses (`uv pip install --python <venv python> pytest-xdist` where `uv` is on PATH, else `<venv python> -m pip install --quiet pytest-xdist`); and a warm call that finds the directory runs no install — the marker is read by the filesystem alone, `Lib/site-packages` on Windows and `lib/python*/site-packages` elsewhere, never a subprocess | `.github/scripts/run_tests.py#PACKAGES@ee94f257`, `.github/scripts/run_tests.py#site_packages@1942734d`, `.github/scripts/run_tests.py#has_xdist@c39eed21`, `.github/scripts/run_tests.py#add_xdist@ef1d5d5d`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#fake_venv@c21de5d3`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_fresh_build_installs_xdist_beside_pytest@329775a8`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_adopted_environment_without_xdist_is_given_it_once@96b289de`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_environment_with_xdist_takes_no_install_step@439f9161` | **Executed** 2026-09-24 in phase 1: the five cases red at `421b0c0e` (`8 failed, 67 passed` with the module copied into a scratch clone at that commit) and green at `adabb0f8` (`75 passed`); `PACKAGES` without `pytest-xdist` turned both build cases red, and `add_xdist` short-circuited turned both adopted-environment cases and the failed-install case red. **Executed** the same day in a scratch clone at `adabb0f8`, driven from Python and deleted after: the `uv` build printed `+ pytest-xdist==3.8.0`, `.venv/lib/python3.13/site-packages/xdist` was there afterwards, the cold call took 1.6 s wall and the warm call 0.9 s with no install step | 2026-09-24 | **The tidy-up this row is against is refusing an adopted `.venv` without xdist the way a below-floor one is refused.** A missing speed-up is not a wrong interpreter: the suite runs correctly without it, and every `.venv` the runner built before #337 is the adopted case — a worktree's, a clone's, a reviewer's — so refusing would turn a speed-up into a suite nobody can run. **The second is asking a subprocess whether xdist is installed**, which `test_a_built_environment_is_reused_and_never_rebuilt`'s rule forbids on the warm path; the package directory is what pytest imports, so it is the marker. |
| P2 · the command `bin/test` builds ends in `-n auto` unless the caller's own arguments settle the question — a worker count (`-n <k>`, `-n<k>`, `-nauto`, `--numprocesses…`), the plugin switched off (`-p no:xdist`, `-pno:xdist`) or `--pdb` — or the install step failed, in which case one sentence on stderr names the package, says the run is serial and names the remedy (`Remove that directory and run bin/test again`), the suite still runs without the flag, and the exit code is pytest's. The narrow form is the file the caller named and nothing wider, the flag appended after it | `.github/scripts/run_tests.py#caller_decided@9e2dabce`, `.github/scripts/run_tests.py#main@5c9e8019`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_whole_suite_runs_in_parallel_by_default@6f97de69`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_callers_own_choice_wins@c3ab7c95`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_failed_xdist_install_is_a_sentence_and_the_suite_still_runs@0efac6a2`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_arguments_pass_through@7868d44a` | **Executed** 2026-09-24 in phase 1: the inverted pin and the narrow-form tail red at `421b0c0e`, green at `adabb0f8`; the pass-through case shown red by mutation (`caller_decided` returning `False`, all eight spellings red) and the sentence by mutation (`-n auto` passed after a failed install, one red). **Executed** the same day against the built environment, pytest 9.1.1 and pytest-xdist 3.8.0: `-n 2 --pdb` exits 4 with xdist's `UsageError` and `-n auto --pdb` is collapsed by xdist to zero workers (a failing probe reached the `(Pdb)` prompt in-process); `--sw`, `--lf -x`, `-s`, `-x` and `--trace` were each accepted beside `-n auto`, so none joins the list. A probe with one failing case run through `bin/test` printed `FAILED tests/<probe>::test_tmp_one_fails - assert 1 == 2` and `1 failed, 1 passed in 0.55s` under `-n auto`, which is what `broad_gate.py#failing_files` and `#suite_counts` read | 2026-09-24 | **The list is what bounds the failure scenario of a default**, and a list rots: a flag pytest refuses beside `-n auto` would exit 4 with `unrecognized arguments`, the ticket's middle row arriving from the other side. The remedy, `-p no:xdist`, is on the list, and the runner's own `-n` pass-through is the fall-back as before. **`--pdb` stays on the list although `-n auto` alone would not break it**: withholding and passing reach the same serial run, withholding is the shorter route, and the arguments then pass through exactly as typed — the measurement is in `caller_decided`'s docstring. **Not a carve-out for the narrow form**: xdist's start-up on one module is a fraction of a second, and a second rule to remember costs more. |
| P3 · no loaded document states the suite's cost as a serial figure with no moment: `bin/test`, the runner's docstring and `CONTRIBUTING.md` §*Running the checks* no longer say *about five minutes*, the runner's comment no longer withholds `-n auto` and says why it used to, and `docs/release-checklist.md` §3's suite line is `bin/test -q` rather than a hand-run `uv run … -n auto` spelling of the same run. The measured before-and-after lives in the work item's changelog fragment with its date and machine | `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_no_loaded_document_states_the_serial_figure@a35474b9`, `` `CONTRIBUTING.md#"## Running the checks"@200cca51` ``, `` `docs/release-checklist.md#"## 3. Verify before committing — all of it, here"@35603f23` `` | **Executed** 2026-09-24 in phase 1: the case red at `421b0c0e` and green at `adabb0f8`; `survivor-check --range origin/release/v0.15.1...HEAD` at `8a234674` examined 398 files against the 29 sentences the range removed and found no removed wording standing, exit 0 | 2026-09-24 | **The figure is not moved to another loaded document.** `seal/follow-up.md`'s row on *a figure about a corpus, stated with no moment* names the class; the changelog fragment carries the numbers with the date (2026-09-24), the machine (macOS, 10 logical CPUs) and the run they came from, which is the shape that row asks for. **`seal/ledger.md`'s R6 row of `1788632199` still says *five-minute suite* in its clause cell**; it is a record's wording, a correction to the shared file is not a removal, and the claim it makes — the shell builtin is the guard — is untouched, so it stands as written. **Re-read 2026-09-24 by the orchestrating session at the merge of `release/v0.15.1` into work item 1790208593 (#547), which drifted this anchor.** That branch's §3 edits add the release reading — the table-line count and the strict exit code around `--split` — and state no cost of the suite, serial or otherwise; the suite line stays `bin/test -q`. The claim stands. Re-read 2026-09-24 by work item 1790208643 (#561): §3's table-line reading is one glob-free `find … -exec cat {} + 2>/dev/null \| grep -c '^\|'` in a fence, compared with step 2's before-reading; the claim holds. |
| G1 · `broad-gate` runs the copy of itself the gated tree ships: `main` resolves the root after parsing and, where `<root>/skills/verify/scripts/broad_gate.py` exists and is not the running file by `os.path.realpath`, writes one stderr line naming both copies and runs that file under `sys.executable` with the argument vector as given and inherited streams, returning its exit code — before `gate()` is entered and before any check runs. The tree's own copy finds itself under the root and takes `gate()` as before, so there is no loop; a repository shipping no such file runs the invoked copy exactly as before but for one row and one line | `skills/verify/scripts/broad_gate.py#shipped_gate@7f0117b8`, `skills/verify/scripts/broad_gate.py#redirect_line@45b54f0e`, `skills/verify/scripts/broad_gate.py#main@5feb1a6d`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_gate_runs_the_copy_the_tree_ships_with_the_same_arguments@3bd5157b`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_trees_own_copy_does_not_redirect_to_itself@11f14aa6`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_shipped_copy_that_is_this_file_by_realpath_is_not_a_redirect@59bc82d9` | **Executed** 2026-09-24 in phase 2: the stub case (a fixture shipping a gate that prints a marker and its `sys.argv[1:]` and exits 3) red at `2e5a4729` — the old gate ignored the stub and sealed the fixture — and green at `5d51b319` with exit 3, the marker once, `'--base', 'base'` and the fixture's absolute root in the handed vector, and both absolute paths on the stderr line; `shipped_gate` returning `None` always, the realpath check removed, the redirect line dropped and the argument vector dropped each turned the cases red. `args.base` is read once, in `gate`, and `test_the_gate_asks_the_range_ci_will_ask.py`'s structural count held | 2026-09-24 | **The redirect is decided from `parse_known_args`, so the vector reaches the tree's copy whatever this copy's parser knows** (round 1's 🟡 1): a branch whose gate grows an argument is inside #475's class, and a copy that predates the flag must not be the one that refuses it; `--base` stays required, and the full parse happens only on the path that runs here. **The redirect lives in the Python and not in `bin/broad-gate` / `bin/broad-gate.cmd`**: the pair is two files kept in step by a case, the script is the one place both exec, and the `.cmd` twin's `py -3` is not consulted twice because the child is spawned from `sys.executable`. **The released copy is not the default** (#475 left it open): CI runs the checkout's scripts, the gate exists to say what CI will say, and the released copy's one measured cost was a false refusal that stopped an unattended run. The symmetric cost — a tree that breaks an arm seals itself — is named on the stamp by `tree` (G2) and caught at the pull request by the same scripts. **A user repository that vendors a file at that path is a fork of this plugin**, whose CI runs its copy too. **Re-read 2026-09-24 after the seal, for CI's `windows-latest` leg on PR #549:** the stub prints `sys.argv[1:]` as a list, whose repr doubles every backslash, so the case now looks for `repr(str(repo))` — the stub's own spelling of the root — rather than the path in single quotes; the claim, the fixture's absolute root in the handed vector, is unchanged. |
| G2 · the stamp says which copy of the gate drew it: `panel` carries a `gate` row beside `from`, `tree <version>` where the running copy's realpath lies under the gated root and `plugin <version>` otherwise, the version read from the running copy's own `.claude-plugin/plugin.json` and `?` where that file cannot be read or does not say; the value is cut at `PANEL_VALUE_WIDTH` with `...` the way the `from` row is, so a long version never widens the row; and every run past root resolution — sealed, not sealed, refused — writes `broad-gate: gate <absolute path> (<value>)` to stderr, where the path the panel cannot hold lives | `skills/verify/scripts/broad_gate.py#gate_copy@09f2fa56`, `skills/verify/scripts/broad_gate.py#plugin_version@63f5ed96`, `skills/verify/scripts/broad_gate.py#under@7184719d`, `skills/verify/scripts/broad_gate.py#running_line@cef42121`, `skills/verify/scripts/broad_gate.py#panel@ab36a0ff`, `tests/test_the_gate_names_every_step_ci_runs.py#HISTORICAL_ROWS@23802d18`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_repository_shipping_no_gate_runs_the_invoked_copy@151bbf30`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_refusal_after_the_root_resolved_still_names_the_running_copy@4f399c92`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_gate_row_says_tree_under_the_gated_root_and_plugin_elsewhere@1c4611dc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_gate_row_fits_the_panel_for_a_nine_character_version@2ec29c89` | **Executed** 2026-09-24 in phase 2: the four cases and `test_a_repository_with_no_hygiene_workflow_is_sealed_exactly_as_before` (against the grown `HISTORICAL_ROWS`) red at `2e5a4729`, green at `5d51b319`; `gate_copy` fixed to `plugin`, the elision removed, the running line dropped, the panel row dropped and an unreadable version reading as blank each turned the cases red. Over this repository `gate_copy(ROOT)` reads `tree 0.15.0` and over a fixture root `plugin 0.15.0` | 2026-09-24 | **The row sits between `from` and the first blank** rather than after `chain`, because it is the same kind of fact as `base` and `from` — what this run was measured against, and by what. **`gate` is touched by one argument** (`gate_copy(root)` on its `panel` call), which `spec.md` §*Data & interfaces* said would not happen: `panel` has no other way to learn the gated root, and the alternative — a module global `main` sets — would print `plugin` for any caller that drives `gate()` in process. The five rows anchored on `gate` were re-read in phase 4 and re-stamped; none is about the panel call. **The `Broad gate` cell's format is out of scope**: the cell is parsed by `chain_check.py#broad_gate` and written by one path in `round_record.py`, and widening it is a change to what a gate reads. |
| G3 · `agents/sealer.md` §*The command* says the gate reports which copy of itself ran, that where the tree ships one that copy is the one that runs, that a `tree` value on the stamp means the branch was measured by the gate it ships, and that the gate line is quoted in the report the way the moved-base line is — not a warning, not a refusal | `` `agents/sealer.md#"## The command"@dabbe5b2` ``, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_sealer_is_told_the_gate_says_which_copy_ran@8a14ba35` | **Executed** 2026-09-24 in phase 2: the case red at `2e5a4729` against the unedited definition and green at `5d51b319`; the four prose pins over the definition (`test_docs_line_wrap.py`, `test_one_word_one_meaning.py`, `test_a_moved_rule_leaves_its_definition.py`, `test_the_rules_have_one_owner.py`) `275 passed`, and the seven further modules that read `agents/sealer.md` `395 passed` | 2026-09-24 | **The sealer is not told to type a path** (#475's *Not this*): a rule kept in prose goes missing, and the sealer opens no repository file by its own definition. The choice is in the gate, and the definition's sentence is about what the gate prints. **This branch's own seal is the one run that still needs the path by hand**: the installed 0.15.0 copy has no redirect, so `plan.md` §*Operational impact* has the orchestrator spawn the sealer with this tree's absolute `bin/broad-gate`, once. |
| N1 · two agents alive at once cannot share a scratch name: `agents/warden.md` §*Where you work* names the clone `<scratchpad>/<work-item-id>/round-<n>/clone` and puts every probe, capture and fixture of the round under `<scratchpad>/<work-item-id>/round-<n>/`, saying why (agents of one session share the scratchpad; the 0.15.0 run measured the collision three times); `agents/sealer.md` §*The command* names the capture file `<scratchpad>/<work-item-id>/broad-gate.log` and says to quote the gate's `outputs kept under broad-gate-<random>/` line; `skills/verify/SKILL.md`'s capture example redirects to `<scratchpad>/<work-item-id>/run.txt` and `/tmp/run.txt` is gone | `` `agents/warden.md#"## Where you work"@a68e1953` ``, `` `agents/sealer.md#"## The command"@dabbe5b2` ``, `` `skills/verify/SKILL.md#"**Capture once, filter locally.** A command re-run to see its output a"@fd85d868` ``, `tests/test_a_parallel_agent_names_its_scratch_after_the_work_item.py#test_the_wardens_clone_is_named_for_the_work_item_and_the_round@bcf01155`, `tests/test_a_parallel_agent_names_its_scratch_after_the_work_item.py#test_the_sealers_capture_file_carries_the_work_item_id@2e4449f3`, `tests/test_a_parallel_agent_names_its_scratch_after_the_work_item.py#test_the_capture_example_is_not_a_name_every_session_shares@4f478a3a` | **Executed** 2026-09-24 in phase 3: the module written first and run against the unedited documents at `5d51b319`, `3 failed`, one per absent sentence; green at `8a234674`, `3 passed`. The 43 modules that read any of the three documents ran as one batch, `1 failed, 2071 passed, 7 skipped` — the one failure being this work item's then-absent `overview.md`, written in phase 4 | 2026-09-24 | **What the pin buys is the sentence, not the obedience**: a spawn prompt that names a different directory still wins in the moment, and nothing in the tree reads a prompt. **A per-agent scratchpad is the harness's to build**, not the repository's; the definitions name the sub-directory so the orchestrator's hand stops being needed, and the named directory is still correct if the harness ever scopes it. **The spelling is one per definition**: #544's `r<N>-<id>` would satisfy the same pin; what the pin holds is that both the work item id and the round are in the name. **The gate's kept directory was never the collision**: `broad-gate-<random>/` is unique per run, and the file that collided was the sealer's own redirect, which is why the sentence names that file and not `--keep-output`. |

<!-- specs/1790206437-a-second-fold-writes-a-second-heading -->
### 1790206437-a-second-fold-writes-a-second-heading

<!-- One work item's rows. No header and no marker line — `fold_ledger.py`
writes the marker and the `###` at the release and moves this file into the
release's ledger file (`seal/releases/<X.Y.Z>.md` since #547); a fragment
carrying its own marker line was folded with it before #553, and the ledger
then counted that work item twice.

F1–F3 are #540 (phases 1 and 2), B1–B2 are #542 (phase 3), D1 is #366's
second half (phase 4). Every case named was seen red before it was planted;
the phase records carry the runs. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| F1 · a second fold for a version `seal/ledger.md` already heads joins that section: the work items go at the end of the section before the next `## `, one blank line each side, the section keeps the first fold's date over `--date` and over today, `--dry-run` prints the existing heading under *appending into the existing section:*, and the message ends *(appended into the existing section)* | `.github/scripts/fold_ledger.py#section_heading@23ead87d`, `.github/scripts/fold_ledger.py#insert@541f6773`, `.github/scripts/fold_ledger.py#main@b490214c`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_second_fold_for_the_same_version_joins_its_section@4c5e5ca1`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_kept_date_wins_over_today_as_well@554bee90`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_dry_run_of_a_second_fold_shows_the_section_it_appends_into@47469657` | **Executed** 2026-09-24, phase 2. The four cases seen red at `dcd060ef` against the unedited script (a second `0.4.0 — 2026-09-16` heading; `0.4.0 — <today>` with no `--date`; the dry run printing a fresh heading), then green with the fold, gather and hygiene modules (120 passed). Four mutations, restored from kept bytes: `insert` always appending kills the three join cases; the printed heading taken from the block kills the message and dry-run cases; the dry-run sentence dropped kills its case. A `date` override in `main` was measured dead — both kept-date cases green with it disabled — and removed: the kept date is `insert`'s and the printed heading's | 2026-09-24 | Q5 measured: no case in the module expected the second date. The `date = args.date or …` line is byte-identical — `1790173209`'s `survivors.md` quotes it. `insert` splits on `\n` alone, as `demote` does, so a row holding U+2028 that the first fold moved byte for byte is not two lines to the second; `gather_changelog.py#insert` uses `splitlines()` and is on the spec's unchanged list. **Re-read 2026-09-24 by the round-1 fix pass (🟡 1):** the join case's docstring states the measured count (seventeen ledger sections) in place of *six releases*; the case body is unchanged and the claim holds. **Re-read 2026-09-24 by the round-2 fix pass (🟡 5):** the same docstring states the measured gap — `bee7ae99` at 02:04, `4ac9bf35` at 02:35 on 2026-09-09 +0900, thirty-one minutes; the case body is unchanged and the claim holds. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 2:** the join is over the release file `seal/releases/X.Y.Z.md` now, `insert` and `section_heading` unchanged; the three cases are re-pointed to that file, and `test_the_joined_section_is_one_section_wherever_it_stands` is retired — its premise, an area heading after the version section, cannot occur in a release file, which `--check` refuses when it heads a second version — so its anchor is dropped here (narrowed, not re-pointed) **Re-read 2026-09-24 by work item 1790208593 (#553), phase 3:** `main` gained the doubled-marker arm; the join is unchanged **Re-read 2026-09-24 by 1790208593 (#547), phase 4:** `main` gained the `--split` branch; the join is unchanged |
| F2 · `--check` refuses a ledger that heads a version twice, naming the version and every line it stands at, after the fragment and open-row reports when those apply | `.github/scripts/fold_ledger.py#doubled_versions@e85cf261`, `.github/scripts/fold_ledger.py#main@b490214c` | **Executed** 2026-09-24, phase 2. Seen red at `dcd060ef`: exit 0 over the planted file. Real tree: `fold_ledger.py --check --root <a scratch root holding 9f846733:seal/ledger.md>` exit 0 before the arm and exit 1 after, `0.9.3  at lines 1673, 1764`; over this tree exit 0 with `118 work items marked` before and after. `doubled_versions` returning nothing kills the case | 2026-09-24 | Q2 decided: two readers of one regex, `tests/test_release_hygiene.py#duplicated_version_headings` on every pull request and this one at the release, so the hygiene module does not import a release script. `CONTRIBUTING.md`'s four answers: seen red against the real file; blocks more (a doubled heading fails the release pull request); prompt budget zero; a line-shaped read with no platform behind it. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 2:** `doubled_versions` is `version_headings` filtered, and the arm reads each release file rather than `seal/ledger.md` — any version heading left in the shared file is refused by an arm of its own naming `--split`. The case that pinned it is `test_check_refuses_a_release_file_that_heads_its_version_twice` now, cited from 1790208593's fragment; the old name's anchor is dropped here **Re-read 2026-09-24 by work item 1790208593 (#553), phase 3:** `main` gained the doubled-marker arm after this one; this arm is unchanged **Re-read 2026-09-24 by 1790208593 (#547), phase 4:** `main` gained the `--split` branch; this arm is unchanged |
| F3 · this repository's `seal/ledger.md` heads no version twice, and the hygiene module refuses one on every pull request | `tests/test_release_hygiene.py#test_no_version_heads_two_sections_of_this_ledger@f528aff7`, `tests/test_release_hygiene.py#duplicated_version_headings@a083a574` | **Executed** 2026-09-24, phase 1. Red against the tree at `9f846733`: `seal/ledger.md heads 0.9.3 twice, at lines [1673, 1764]`; green at `d08c671a` after the second `## 0.9.3 — 2026-09-08` heading and its blank line were removed. `evidence-check --strict .` read `1662 ok · 0 drifted · 0 broken` before and after; `fold_ledger.py --check` counted 118 work items marked both times | 2026-09-24 | The second heading was written by `4ac9bf35` at 02:35:35 on 2026-09-09 (+0900), thirty-one minutes after the 0.9.3 preparation commit `bee7ae99` at 02:04:21 — both 2026-09-08 in UTC (17:04:21 and 17:35:35), the date the heading carries — a fragment landing after the release pull request went red, #289's shape one file over (round 2's 🟡 5 measured the gap with `git log -1 --date=iso-strict` on both commits, where this row had written a day). #540's premise that nobody had run the fold twice was false, and the repair arrived seventeen ledger sections after the instance (`0.9.4` to `0.15.0`, counted after the second heading at `9f846733`; eighteen tags after `v0.9.3`, `0.13.2` having folded no section — round 1's 🟡 1, which measured what *six releases* had only asserted). **Re-read 2026-09-24 by the round-2 fix pass (🟡 5):** the case's docstring names `bee7ae99` as the preparation commit and states the thirty-one-minute gap with both times; the case body is unchanged and the claim holds. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 4:** the case also requires each `seal/releases/*.md` to head exactly the version its name says, once — green in both shapes, red on a planted misnamed file |
| B1 · every `broad-gate.md` that `seal` writes says a run the newest entry already records — the same commit against the same base — replaces it, and no longer says a run taken again is written in front | `skills/code-review/scripts/round_record.py#new_broad_gate_file@42eea32e`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_written_broad_gate_file_says_a_same_run_re_seal_replaces_its_entry@d310f6a0` | **Executed** 2026-09-24, phase 3. The stands half seen red at `20ede02c` against the unedited generator; the gone half by mutation, the old sentence written after the new one in lowercase, restored from kept bytes. Green with the seal and cell modules (130 passed) | 2026-09-24 | The round-3 report's block, verbatim (`1790174138`, ⬜ 1). The eleven `broad-gate.md` files already committed keep the old sentence: they are records of the runs that wrote them. The absence check is case-sensitive, as the `GATE_CARRIERS` pairs are — a first mutation restoring the sentence with a capital letter passed and was corrected rather than the case widened |
| B2 · `kept_broad_gate`'s docstring counts distinct comparisons — commit and base — and the two further carriers of the replaced rule, `docs/review-handoff-protocol.md`'s `Broad gate` row and `skills/code-review/orchestration.md`'s sealer paragraph, state the same-commit-same-base replace in one clause each, with `GATE_CARRIERS`' stands phrases intact | `skills/code-review/scripts/round_record.py#kept_broad_gate@43a33862`, `skills/code-review/scripts/round_record.py#same_run@c1b1316a`, `tests/test_the_broad_gate_cell_keeps_every_run.py#GATE_CARRIERS@247d1ff4`, `tests/test_the_broad_gate_cell_keeps_every_run.py#test_the_writers_are_named_as_one_path@57547c81` | **Read** 2026-09-24, phase 3: the two documents re-read against `templates/sdd-round.md`'s `Broad gate` row; every other `GATE_CARRIERS` file re-grepped for the replaced wording with no hit. **Executed**: the cell module green after the rewording (its stands and gone phrases unchanged), `tests/test_docs_line_wrap.py` green over both documents | 2026-09-24 | §12: the ticket named two carriers and the class had five — the two documents were not re-read by `1790174138`'s round 3 for `same_run`. The docstring sentence is not pinned by a test on purpose (`spec.md` §Out); A9 in §0.15.0 anchors the unit and carries this phase's re-read note |
| D1 · a finding whose coordinates sit at two depths — one inside a unit an earlier round's fixes created, another not — is written as two findings, so each verdict carries one depth and the fix of one does not refuse the units the other's fix adds; one text in the three places the reviewer copies from, pinned, and `depth_two` untouched | `agents/warden.md#"## Role"@466a9805`, `skills/code-review/SKILL.md#"## Findings format"@d010e4c6`, `` `docs/round-record-spec.md#"## The depth in `New units`"@85a39a9c` ``, `tests/test_the_report_standard_is_one_in_three_places.py#SPLIT_AT_TWO_DEPTHS@ca2acc0a`, `tests/test_the_report_standard_is_one_in_three_places.py#REVIEWER_CARRIERS@46895435`, `tests/test_the_report_standard_is_one_in_three_places.py#test_every_carrier_tells_the_reviewer_to_split_a_finding_that_sits_at_two_depths@89dd5c29`, `skills/code-review/scripts/round_record.py#depth_two@b4a81871` | **Executed** 2026-09-24, phase 4. Red at `b9cec50f` with no carrier holding the sentence; red with it deleted from each carrier in turn by mutation, restored from kept bytes; green with the report-standard, line-wrap and floor-and-depth modules (114 passed) and the one-owner, one-word, moved-rule and survives modules (314 passed). `depth_two`'s hash is the one §0.15.0's rows carry | 2026-09-24 | Q1 answered by the repository owner 2026-09-24: convention only, #366 closes here; the finer `depth_two` is not built and no ticket is opened for it. The template is not a carrier: its comment is the record's shape, not the reviewer's rule for a `Location` (judgment 7). `depth_two`'s refusal already names the finding whose fix added the unit, which is the one a reviewer who did not split reads next round. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. Re-read 2026-09-24 — #526's split re-pointed a citation inside the anchored unit to the document that now holds the section; the claim unchanged. Re-read 2026-09-24 — #526's split re-pointed the anchored case to the document that now holds the section it reads, and widened any absence it asserts of the spec to all three documents; the claim unchanged. Re-read 2026-09-24 — the docstring gained a paragraph saying the candidates are the units added in the finding's own file, which keeps the rule and contract §15 consistent. It is #559's, which closed #222; the paragraph #526's branch wrote for the same ticket was withdrawn at the merge. The walk is unchanged and the claim holds. **Re-read 2026-09-24 by 1790208593 (#547), phase 5:** `agents/warden.md` §Role changed only in the paperwork sentence's record paths, so the claim holds. Re-read 2026-09-24 at the merge of `release/v0.15.1` (#558, #559) into #526's branch, at `4a077852`: #559's paragraph on the file-scoped candidates is the one the docstring keeps, and this branch's for the same ticket (#222) was removed; the walk is unchanged and the claim holds. #558 widened the paperwork sentence's record paths and #526's split re-pointed one citation to the record document; the claim holds. |

<!-- specs/1790208593-the-fold-writes-each-release-to-its-own-file -->
### 1790208593-the-fold-writes-each-release-to-its-own-file

<!-- One work item's rows. No header and no marker line — the fold writes
both at the release. R1–R4 are phase 1, F1–F2 phase 2, M1 phase 3 (#553),
P1–P2 and L1 phase 4, D1 phase 5. Each case named was seen red before it
was planted or was shown red by a mutation; the phase records carry the runs. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · with no `--ledger`, the checker reads `<home>/releases/*.md` beside `<home>/ledger.md` and `<home>/ledger/*.md`, so a row in a release file is counted, re-stamped by `--reverify`, and named as skipped by a narrowed run | `skills/evidence-check/scripts/evidence_check.py#default_patterns@3b4720ba`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_row_in_a_release_file_is_read_by_the_default_run@077cf925`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_a_narrowed_run_names_the_release_file_it_did_not_read@4d8a1b26`, `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_reverify_re_stamps_a_drifted_row_in_a_release_file@5048cf57`, `tests/test_the_printed_ledger_name_is_the_file_that_was_read.py#test_the_defaults_render_the_names_they_have_always_rendered@c321cda5` | **Executed** phase 1: each case red at `9f5902e5` with the readers untouched (`1 ok` where two rows stood; the notice named nothing; the file kept `00000000`; the list had three names), green at `137bf447` | 2026-09-24 | `main`, `reverify`, `migrate`, `skipped_by_narrowing` and `settle.py#anchored_rows` all read this one list, so one line reached five readers |
| R2 · the commit-time advisor and the session-start migration read `releases/*.md` under the resolved root | `hooks/evidence-advisor.py#failing_rows@4837a604`, `hooks/ledger-migrate.py#HOME_GLOBS@66c4aef5`, `tests/test_local_mode_resolves_under_the_git_dir.py#test_the_evidence_advisor_reports_a_broken_row_from_the_local_ledger@461ad9cc`, `tests/test_the_ledger_migrates_itself.py#test_an_old_format_row_in_a_release_file_is_migrated_too@6d44521d` | **Executed** phase 1: the `releases/0.4.0.md` parameter printed no BROKEN line, and the migration reported `1 row` with the release file untouched, both before the globs widened | 2026-09-24 | Three spellings of the list outside `default_patterns` (these two and `correction_check.py`) is the accepted cost `plan.md` §*What breaks in six months* names |
| R3 · `correction-check` lists `seal/releases` beside the shared file and the fragments at every rev, so a marker a merge drops from a release file is reported; a row a merge moves between two paths is not identified on either, and the docstring says so | `skills/evidence-check/scripts/correction_check.py#ledger_listing@873f8f32`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_a_marker_lost_from_a_release_file_is_reported@7829e22d`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#ledger_corpus@c945dea9` | **Executed** phase 1: exit 0 naming nothing before `RELEASES` reached `ls-tree`; `test_the_bound_covers_every_candidate_marker_site_the_corpus_carries` red on the missing constant until it existed | 2026-09-24 | The cross-path silence matters once, at the split, which runs at a release-preparation commit with no merge in its range (`questions.md` Q6) **Re-read 2026-09-24 by round 2's fix pass of work item 1790208593 (#547):** `ledger_corpus`'s docstring says the fragments fold into the shared file until #547 and into the release's file since; the corpus it reads is unchanged |
| R4 · `settle` attributes a release file's rows to their work items by the same loop as `seal/ledger.md`'s, and its anchored-row guard reads the release files through the checker's list | `skills/settle/scripts/settle.py#coordinates@c8ec101f`, `tests/test_settle_reads_before_it_removes.py#test_a_release_file_is_read_as_well_as_the_gathered_ledger@07de5593`, `tests/test_settle_reads_before_it_removes.py#test_a_row_in_a_release_file_is_read_by_the_guard@eb1b020b` | **Executed** phase 1: beta stayed `tests only`, and the guard removed a directory a release-file row anchored into, before the loop widened | 2026-09-24 | A release file is the section byte for byte — one `## ` line, then markers — so the loop needed no second rule |
| F1 · the fold writes `seal/releases/<X.Y.Z>.md`: a new release is `section()`'s block as a new file, a second fold for one release joins that file through `insert` and keeps its date, `--dry-run` prints *appending into the existing file:*, and `seal/ledger.md` is never written; a file at the release's path that does not head that release is refused | `.github/scripts/fold_ledger.py#main@b490214c`, `.github/scripts/fold_ledger.py#release_files@edb86ba4`, `tests/test_the_ledger_fragments_fold_at_release.py#fold@03f4f6e8`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_release_file_is_the_section_and_the_shared_ledger_is_untouched@0e8aadd5`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_second_fold_for_the_same_version_joins_its_section@4c5e5ca1`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_fold_refuses_a_release_file_that_does_not_head_its_version@b61e0ceb` | **Executed** phase 2: `30 failed, 20 passed` with the module re-pointed and the script untouched; green at `8faae73e`. Mutation: the join replaced by a fresh block — 2 red | 2026-09-24 | `append` stays as `insert`'s no-section arm, unreachable from `main` (`questions.md` Q2). C's `test_the_joined_section_is_one_section_wherever_it_stands` is retired: its premise cannot occur in a release file |
| F2 · `--check` counts markers across `seal/ledger.md` and every release file, refuses any release heading left in `seal/ledger.md` naming `--split`, refuses a release file not named for the one version it heads once, and `folded` names the file a re-created fragment's marker stands in | `.github/scripts/fold_ledger.py#version_headings@cb9214c4`, `.github/scripts/fold_ledger.py#misnamed@1739d9f3`, `.github/scripts/fold_ledger.py#folded@a10e4565`, `tests/test_the_ledger_fragments_fold_at_release.py#test_check_refuses_a_release_left_in_the_shared_ledger@341f262f`, `tests/test_the_ledger_fragments_fold_at_release.py#test_check_refuses_a_release_file_named_for_another_version@0c85b5f5`, `tests/test_the_ledger_fragments_fold_at_release.py#test_check_refuses_a_release_file_that_heads_its_version_twice@334e4c68`, `tests/test_the_ledger_fragments_fold_at_release.py#test_check_passes_once_folded_and_says_what_it_counted@00615ae5` | **Executed** phase 2, red first; mutations: `misnamed` → None 2 red, the shared-ledger arm skipped 1 red, `folded` finding nothing 3 red, the count over one file 3 red | 2026-09-24 | `doubled_versions` is `version_headings` filtered and keeps its name (`questions.md` Q3) |
| M1 · a fragment's own leading `<!-- specs/<id> -->` line is dropped before the fold writes its marker, `--check` refuses a work item marked more than once anywhere in the ledger corpus naming each `path:line`, and this repository's `seal/ledger.md` marks each work item once | `.github/scripts/fold_ledger.py#own_marker_dropped@77e1b77f`, `.github/scripts/fold_ledger.py#doubled_markers@a6b7eb33`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_fragment_that_begins_with_its_own_marker_is_folded_with_one_marker@9d23a77d`, `tests/test_the_ledger_fragments_fold_at_release.py#test_check_refuses_a_marker_that_stands_twice@5742be32`, `tests/test_release_hygiene.py#test_no_marker_stands_twice_in_this_ledger@20ba708a`, `tests/test_release_hygiene.py#duplicated_markers@2d7f704e` | **Executed** phase 3 (#553): the fixture case and both `--check` shapes red first; the real-tree case red against the ledger at `f3ec093a` naming twenty work items, and `--check` over those bytes exit 1 naming 20 pairs; 40 lines removed, the sorted list of 862 table rows identical. Mutations: the drop an identity 1 red, the refusal empty 2 red | 2026-09-24 | The ticket counted two pairs; there were twenty, every one the same shape. Corpus-wide because a work item marked in two files over-counts exactly as one marked twice in one |
| P1 · `fold_ledger.py --split` cuts every `## X.Y.Z` section of `seal/ledger.md` into `seal/releases/<X.Y.Z>.md` byte for byte, rewrites each `seal/ledger.md#"<heading>"` anchor whose heading moved to the release file with the hash unchanged, names any anchor it cannot place, refuses a version headed twice, an existing target and a ledger with nothing to move, and `--dry-run` prints each section's line range and row count and each rewrite | `.github/scripts/fold_ledger.py#split@6890e040`, `.github/scripts/fold_ledger.py#release_sections@1de31f16`, `.github/scripts/fold_ledger.py#rewrite_self_anchors@1020b299`, `.github/scripts/fold_ledger.py#body_rows@a48173e5`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_split_moves_every_release_section_byte_for_byte@f9683514`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_split_rewrites_an_anchor_into_a_moved_section@46f2bc60`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_split_refuses_a_target_that_exists_and_writes_nothing@50c5f7ae`, `tests/test_the_ledger_fragments_fold_at_release.py#test_a_dry_run_of_the_split_says_what_would_move_and_writes_nothing@3be73f3e` | **Executed** phase 4: six cases red on the missing flag; mutations — no rewrite 3 red, the cut to EOF 4 red, targets not refused 1, doubled versions not refused 1, the row count counting headers 1 | 2026-09-24 | The hash survives the move because `evidence_check.py#normalise` drops blank lines, and a section cut at EOF loses only trailing blanks **Re-read 2026-09-24 by round 1's fix pass of work item 1790208593 (#547):** round 1's 🟡 1 fixed: a locator is read by the checker's rule (the `@hash` required, a heading path only when its first part is a heading, one whole line otherwise), `moved` and `kept` hold every non-blank line, and each rewrite prints its own line (⬜ 9); `test_the_split_names_only_the_anchors_it_cannot_place` and `test_each_identical_rewrite_prints_its_own_line` red at `577f1671`, green on the fix **Re-read 2026-09-24 by round 2's fix pass of work item 1790208593 (#547):** round 2's ⬜ 3: a line standing in more than one place — in the standing area and a moved section, or in two moved sections and the standing area — is left out of both maps and named, because only the row's hash could choose; ⬜ 4 pins the escaped-quote reading. Both cases red at `94e6d6cd` or on the mutant, green after; the real-tree dry run byte-identical |
| P2 · the split rehearsed over a copy of this repository's `seal/ledger.md` every run: one file per release it heads, every table row in exactly one file, the same `(status, coordinate)` set from `check_ledger` before and after, the two anchors of the §0.13.1 row rewritten to `0.4.0.md` and `0.5.0.md` and OK there; after the real split it asserts the result instead | `tests/test_the_ledger_fragments_fold_at_release.py#test_the_split_rehearsed_over_this_repositorys_ledger@e71f97b0` | **Executed** phase 4; red under the no-rewrite and cut-to-EOF mutations. `--split --dry-run` over this tree: 29 sections, lines 103–2694, 2 anchors | 2026-09-24 | 29 releases, not the 37 the frame stated (38 `## ` lines at `9f846733`, 8 of them standing areas and one C's doubled `0.9.3`). **Round 1 measured (2026-09-24):** on a copy at `577f1671` the split raises `evidence_check.py .`'s `ok` total from 1736 to 1932 with 0 drifted and 0 broken both times, and the distinct `(status, coordinate)` set is unchanged at 1662 — `check_text` de-duplicates per file, so a pair two releases shared is counted in each. The release session should read the rise as expected. **Re-read 2026-09-24 by round 2's fix pass (#547):** the distinct count was taken in process by importing the checker, and no command prints it; at `c7d64f2a` round 2 measured 1713 distinct both times and 1027 table lines (`grep -c '^\|'` over every ledger file) before and after, which is the comparison `docs/release-checklist.md` §3 now gives the release session |
| L1 · the layout and real-tree pins hold on both shapes of the tree — `seal/releases/` holds only `<X.Y.Z>.md`, each heads its own version once — and every real-tree reader of a folded section reads the release files too | `tests/test_release_hygiene.py#test_this_repository_has_one_root_laid_out_by_lifetime@25af0287`, `tests/test_release_hygiene.py#test_no_version_heads_two_sections_of_this_ledger@f528aff7`, `tests/test_settle_reads_before_it_removes.py#real_ledgers@1da90568`, `tests/test_unverified_rows_close.py#test_the_three_named_markers_are_live_in_this_repositorys_ledger@2fe71173`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_declared_limit_names_what_escapes_with_the_words_unchanged@f20c579e` | **Executed** phase 4: the 44 ledger-reading modules run with the tree as committed and again after a real `--split` in the worktree (restored from kept bytes): split shape 4 new failures, each a reader of `seal/ledger.md` alone; after widening, both shapes green. The two layout pins red on planted files | 2026-09-24 | `test_a_row_points_by_content.py`'s real-tree glob went blind rather than red and was widened too. Re-read 2026-09-24 at the merge of `release/v0.15.1` (#558, #559) into #526's branch, at `4a077852`: #558 made the case read a folded copy from `seal/releases/` as well as `seal/ledger.md`, and #526's split pointed its spec copy at the record document; the five copies and what each must say are unchanged, so the claim holds. |
| D1 · the documents name the release files: the house rules, the release checklist's once-only `--split` and the join into the release file, branch-and-release, the evidence-ledger policy, both READMEs' trees, three skills, the framer's reading list, the four paperwork-location carriers, and `seal/ledger.md`'s header | `docs/release-checklist.md#"## 2. Gather, fold, bump"@2b097b36`, `CONTRIBUTING.md#"## House rules"@c1fd7d64`, `docs/the-evidence-ledger.md#"## A row is a content anchor, and it names no commit"@c69d1fb2`, `seal/ledger.md#"> rows from before the fragments existed. **A work item writes"@6918b568` | **Read**, and the 94 modules reading an edited document executed: 3 real reds met and answered in phase 5 | 2026-09-25 | `CLAUDE.md` and `seal/README.md` are not edited: the first is the owner's, the second is the template verbatim (`overview.md`) **Re-read 2026-09-24 by round 1's fix pass of work item 1790208593 (#547):** round 1's 🟡 2, 4 and 5 widened the set: the checklist, the policy, two skills and the docstring say a move changes no row's status and can change the `ok` count, `correction-check`'s scope is named with the release files, and three code comments and one workflow comment name the release file **Re-read 2026-09-24 by round 2's fix pass of work item 1790208593 (#547):** round 2's 🟡 2 and ⬜ 5 to 7: the checklist's comparison is the table-line count, `CONTRIBUTING.md`'s conflict heading names fragments too, and three comments and two docstrings name the release files or the past tense. Re-read 2026-09-24 by work item 1790208643 (#526), phase 3: §House rules gained #488's sentence (a removal or an edit is kept true in the file the row is in, and neither is an append) and #509's halves paragraph (the notes are a union, the hash is the editing side's, run `evidence-check` after the resolution); the claim holds. Re-read 2026-09-24 by work item 1790208643 (#526), phase 3: the *Appended is the word* paragraph widened from a removal to a removal or an edit (#488); the claim holds. Re-read 2026-09-24 by work item 1790208643 (#561): step 2's second block takes the table-line count and the strict exit before `--split`; the claim holds. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: §House rules' ledger answers are three now (a claim the edit made false is corrected in place with a `Corrected <date>` note), and the halves paragraph gives the hash to neither side where both edited; the claim holds. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: the edit arm corrects in place a claim the edit made false before re-stamping; the claim holds. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the section gained 1790208643's marker and `Enforced by:` line over the edit-arm paragraph, whose text is unchanged, and a folded statement on escaped pipes; the claim holds. Re-read 2026-09-25 by work item 1790260565 (#501): the escaped-pipe paragraph now says a row under no header is counted against the template's five columns, and that the three split rows were two date-and-notes pairs and one split Notes cell. The sentence naming the `seal/releases/*.md` glob beside the shared file and the fragments is untouched; the claim holds. |

<!-- specs/1790208643-the-spec-is-split-and-its-sentences-are-settled -->
### 1790208643-the-spec-is-split-and-its-sentences-are-settled

<!-- One work item's rows. No marker line — `fold_ledger.py` writes the marker
and the `###` at the release and moves this file into the release's ledger
file (`seal/releases/<X.Y.Z>.md`). S1–S3 are #526's split (phases 1 and 2),
E1–E2 are #488 and #509 (phase 3), N1–N4 the settled sentences (phase 4),
P1 is #466 (phase 5), C1–C2 are #561 and #562. Every case named was seen red
before it was planted; the phase records carry the runs. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · no top-level document under `docs/` is over the 1,000-line ceiling, so none is listed: `docs/review-chain-spec.md` was split along its own headings into itself, `docs/commit-review-gate-spec.md` and `docs/round-record-spec.md`, every fold marker carried whole, and the evidence-ledger policy's bullet says so rather than naming a frozen count | `docs/the-evidence-ledger.md#"## The fold, and what tells it from a deletion"@9c38097b`, `tests/test_a_document_has_room_for_the_next_fold.py#OVER_CEILING@9510ca40`, `tests/test_a_document_has_room_for_the_next_fold.py#FROZEN_IDS_DIGEST@b1c9b377`, `tests/test_a_document_has_room_for_the_next_fold.py#test_the_evidence_ledger_states_the_values_these_constants_hold@91d516f7` | **Executed** 2026-09-24: the ceiling module red on A10 before the constants were emptied (*no longer over the ceiling … Remove its entry*), green after; 878, 523 and 885 lines by `wc -l`; 29 live markers, 26 ids, the base's digest, over the three files; `bin/settle` byte-identical before and after | 2026-09-24 | The three sizes leave the record document about 115 lines of room, the first a fold into it will meet. `phases/phase-1.md` carries the heading tree and the red run Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the values paragraph became three folded statements (the cutoff, the ceiling, the paired editions); the ceiling statement still says no document is over it and none is listed, so the claim holds. |
| S2 · each of the three documents opens saying what it is the authority for and naming the other two, and a section moved to a sibling is cited there by the file that holds it | `docs/review-chain-spec.md#"# review chain — behavior spec">"Authority for the cycle contract the"@4a0f466a`, `docs/commit-review-gate-spec.md#"# commit-review-gate — behavior spec">"Authority for"@b920d5fb`, `docs/round-record-spec.md#"# round record — behavior spec">"Authority for the rows of"@096a8355` | **Read** 2026-09-24, every `§*…*` citation of the three names in `agents/`, `skills/`, `hooks/`, `templates/`, `docs/`, both READMEs and `CONTRIBUTING.md` against the headings of the file it names; **executed**, a script resolving each citation: 36 resolved and one false negative (this branch's backticked `§`Fixes checked by``) | 2026-09-24 | 22 shipped lines re-pointed, two more than the frame counted (`phases/phase-1.md`). Nothing reads a citation against the tree, so a later drift is a reviewer's finding (`plan.md`'s failure scenario) |
| S3 · a sentence a case forbids in the review chain's specification is forbidden in all three documents, through one reader, so the split cannot hide a returned sentence in a sibling | `tests/conftest.py#REVIEW_CHAIN_DOCS@6e41915b`, `tests/conftest.py#review_chain_text@06494309` | **Executed** 2026-09-24: every forbidden sentence planted in the gate document (a sibling, not the file each case read before) turned the twelve widened absences red — `12 failed, 249 passed` over six modules — and restored from kept bytes; `review_chain_text` narrowed to one file and `REVIEW_CHAIN_DOCS` short of the record document each killed by mutation | 2026-09-24 | A widened absence the run-stops module would never have run was reverted (`phases/phase-2.md`) |
| E1 · a branch that removes or edits code an existing ledger row cites keeps that claim true in the file the row is in — re-stamped where it still holds, corrected in place first where the edit made it false — which is not appending; adding a claim is what goes in the fragment. The evidence-ledger policy owns it and `CONTRIBUTING.md` carries it | `docs/the-evidence-ledger.md#"## A row is a content anchor, and it names no commit"@c69d1fb2`, `CONTRIBUTING.md#"## House rules"@c1fd7d64`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#CONFLICT_SENTENCES@4e5f6382`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_the_policy_document_owns_the_exception_and_the_halves@b5565675`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#EDIT_OUTCOMES@69099dff` | **Executed** 2026-09-24: the owner's case red with each of the two sentences removed from the policy document, restored from kept bytes; the guides' case red on `CLAUDE.md` until the orchestrator pastes `phases/phase-3.md`'s text, and green on a scratch copy of `CLAUDE.md` with it applied | 2026-09-25 | #488. Written as *the file the row is in* so it holds for `seal/ledger.md`, a release file and a fragment alike. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: §House rules' ledger answers are three now (a claim the edit made false is corrected in place with a `Corrected <date>` note), and the halves paragraph gives the hash to neither side where both edited; the claim holds. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: the edit arm corrects in place a claim the edit made false before re-stamping; the claim holds. **Corrected 2026-09-24 (round 2's ⬜ 4):** the Result's *Executed* holds for the sentences phase 3 pinned; the clauses round 1's fix pass added are **read**, held by no case yet — MichaelYcJo/SpecSeal#569 carries the case that pins them. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the section gained 1790208643's marker and `Enforced by:` line over the edit-arm paragraph, whose text is unchanged, and a folded statement on escaped pipes; the claim holds. Re-read 2026-09-25 by work item 1790260565 (#501): the section's escaped-pipe paragraph changed; the edit-arm paragraph this claim is about is unchanged, so the claim holds. Re-read 2026-09-25 by work item 1790260565 (#569): the clauses round 1's fix pass added are held by a case now. `CONFLICT_SENTENCES` gained `` `Corrected <date>` note ``, and the owner's case gained its own assertion for *the edit made false is corrected there first*, because the halves paragraph's `` `Corrected <date>` notes `` would keep the shared needle green. Each clause was deleted from a scratch copy of each carrier, `CLAUDE.md` included, and the right case went red every time (`phases/phase-3.md`), so the Result's *Executed* now covers them; the claim holds. Re-read 2026-09-25 by work item 1790260565's round-1 fix pass (⬜ 1): the edit rule's other two outcomes, a claim re-stamped where it holds and a row removed to the fragment where the claim went with the code, are held now as well, by `EDIT_OUTCOMES`, one pair of needles per carrier in that carrier's own words. Each was deleted from a scratch copy of its carrier, `CLAUDE.md` included, and the right case went red (`rounds/round-1.md`); the claim holds. |
| E2 · resolving a conflicted ledger row has two halves: the notes are a union, the anchor's hash is the side's that edited the anchored unit and neither side's where both did, and `evidence-check` runs after the resolution, the row re-read against every edit the merged unit carries; the release checklist's squash step names the owner | `docs/the-evidence-ledger.md#"## A correction a merge dropped"@9824d9bc`, `docs/release-checklist.md#"## 0. Before starting"@f3cc2de3`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_the_policy_document_owns_the_exception_and_the_halves@b5565675` | **Executed** 2026-09-24: red with each of the two halves sentences removed from the owner; the checklist's link read by the same case | 2026-09-25 | #509. `correction-check` cannot see this defect, because no marker is dropped; the drift names the row, and names a side only where one side edited. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: the halves paragraph moved below the marker check and gives the hash to neither side where both edited; the claim holds. **Corrected 2026-09-24 (round 2's ⬜ 4):** the Result's *Executed* holds for the sentences phase 3 pinned; the clauses round 1's fix pass added are **read**, held by no case yet — MichaelYcJo/SpecSeal#569 carries the case that pins them. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the halves paragraph gained its marker and an `Enforced by:` line and no word of its text changed; the claim holds. Re-read 2026-09-25 by work item 1790260565 (#569): the clauses round 1's fix pass added are held by a case now. `CONFLICT_SENTENCES` gained *to neither side where both did* and *re-read against every edit the merged unit carries*, and the owner's case reads them as the last seven needles. Each was deleted from a scratch copy of each carrier, `CLAUDE.md` included, and the right case went red every time (`phases/phase-3.md`), so the Result's *Executed* now covers them; the claim holds. Re-read 2026-09-25 by work item 1790260565's round-1 fix pass (⬜ 1): the first half, *only the notes are a union*, is a needle now and the owner reads the last eight. The guides' two arguments, *Taking a side reverted three corrections* and *cannot see a union that kept a stale hash*, are needles for the guides alone, since the owner words them its own way. Each was deleted from a scratch copy of each carrier, `CLAUDE.md` included, and the right case went red (`rounds/round-1.md`); the claim holds. |
| N1 · the smith's waiver paragraph says the token is the last way past the gate, what it records untruly of a probe, and that contract §8 names the two shapes before it, in the smith's own words and not §8's | `agents/smith.md#"## Phases"@c0667ce2`, `tests/test_a_moved_rule_leaves_its_definition.py#test_a_citation_is_not_a_copy@7793beee` | **Executed** 2026-09-24: the kept-phrase case red with the sentence deleted (*lost its own application*), green restored; the window case still reads 10 as the longest kept application; the rider's four numbers re-measured unchanged and re-stamped | 2026-09-24 | #55 |
| N2 · the `arm-check` section says `--timeout` bounds the wait and not the work: 900 seconds per operator, twice that per arm, only the command's own process killed, the wrapper form the one that leaks, and the pytest command named directly against a suite near the bound | `` `skills/verify/SKILL.md#"#### `arm-check` asks condition 2 of a whole module, one arm at a time"@acdd5d7c` ``, `skills/verify/scripts/arm_check.py#run_arms@8995285a` | **Read** 2026-09-24 against `run_arms`' docstring and `main`'s `--timeout` handling. No case: `tests/test_arm_check.py` reads the script and no document, and #310 warns against pinning a clause by substring (W4) | 2026-09-24 | #316. Whether the bound reaches the process group is #313. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: the timeout paragraph describes a wrapper that starts pytest as a child, not SpecSeal's own `bin/test`; the claim holds. |
| N3 · the shipped verify skill names SpecSeal where it describes SpecSeal's own CI, so an installation reading it is not told its own `release` job has four unmirrored steps | `skills/verify/SKILL.md#"## The broad gate — after the rounds, then compare against the base"@c42b1782` | **Executed** 2026-09-24: a whitespace-collapsed search for *this repository* over the skill; the two left are addressed to the reader's own repository | 2026-09-24 | #474 item 2 |
| N4 · the comments above `kept_broad_gate`'s calls in `close` and `seal`, and the sealer's seal sentence, say the newest entry is replaced where it is the same commit against the same base | `skills/code-review/scripts/round_record.py#close@b91242ae`, `skills/code-review/scripts/round_record.py#seal@ad6b9461`, `agents/sealer.md#"## What a completion claim is worth"@c1c8c7f3` | **Read** 2026-09-24; **executed**: the broad-gate cell and seal modules green, and the edited `.py` equal to the base with docstrings stripped (AST) | 2026-09-24 | #556. The class, enumerated by grep, is in `phases/phase-4.md` |
| P1 · no file the hygiene timer rule reads names a concrete release branch outside a history sentence the allowlist names, and an allowance whose sentence is gone fails | `tests/test_the_contributor_has_a_procedure.py#concrete_release_branches@99258ad8`, `tests/test_the_contributor_has_a_procedure.py#RELEASE_BRANCH_HISTORY@2998364f`, `tests/test_the_contributor_has_a_procedure.py#test_no_loaded_file_names_a_concrete_release_branch@bb4f95d1`, `tests/test_the_contributor_has_a_procedure.py#test_the_widened_pin_reads_a_file_the_four_surfaces_never_held@40a33ed4`, `tests/test_the_contributor_has_a_procedure.py#test_each_history_sentence_still_stands@1f02e9b0` | **Executed** 2026-09-24: red over this tree before `correction_check.py`'s usage line changed (`release/v1.2.3`); six mutations of the added units, each killed | 2026-09-24 | #466. `hooks/` and `tests/` are outside the population by Q2's default |
| C1 · the release checklist takes the ledger's table-line count and the strict exit before `--split`, and counts after it with one glob-free command, so the comparison reads the same number in bash and zsh | `docs/release-checklist.md#"## 2. Gather, fold, bump"@2b097b36`, `docs/release-checklist.md#"## 3. Verify before committing — all of it, here"@35603f23` | **Executed** 2026-09-24 in zsh 5.9 on a scratch clone of `31937b9f`: 1047 table lines before `--split`, after it and after `--version`, strict exit 0 at each; the old after-form printed 0 with *no matches found* | 2026-09-24 | #561. It has to land before the 0.15.1 release runs step 2 |
| C2 · a `\|` inside a ledger cell is escaped, and a row with more cells than its table's header fails, over the shared file, every release file and every fragment | `tests/test_release_hygiene.py#overwide_rows@d1f18a13`, `tests/test_release_hygiene.py#test_no_ledger_row_splits_into_more_cells_than_its_header@95b9631a`, `tests/test_release_hygiene.py#test_an_unescaped_pipe_in_a_ledger_cell_is_named@94b311c3` | **Executed** 2026-09-24: red against the ledger at `31937b9f`, 22 rows named; green after the escapes; both files byte-identical to the base with the added escapes removed | 2026-09-25 | #562. Two of the 22 were a second date-and-notes pair written into a Notes cell, and one a Notes cell split in three; escaped, not merged, because which pair is the row's was not the escape's to decide. **Corrected 2026-09-25 by work item 1790260565 (#501, #568).** The claim's *every fragment* was true of reading and false of counting: `overwide_rows` skipped a row with no header above it, a fragment has no header by rule, so no fragment row was counted, and neither were the 25 released rows a fold had copied in without one. A row under no header is now counted against the five columns `templates/ledger.md` declares, so the claim holds as written from this commit on. The Notes said *three* date-and-notes pairs; the third row carried one date and a Notes cell split in three (seven cells at `31937b9f`), and the count is corrected in place. #568 joined all three into one reading. |

<!-- specs/1790221963-a-release-writes-the-gathered-text-back -->
### 1790221963-a-release-writes-the-gathered-text-back

| H1 · a gathered fragment's text is held at a release and not written as the range's own: for every `<id>` whose marker stands in `CHANGELOG.md` at the range's tip, `seal/specs/<id>/changelog.md` is read at the range's LEFT end, and its sentences are dropped from what the release writes after the `lost` guard; their n-grams that also occur in a sentence `CHANGELOG.md` itself lost are returned apart from `written` and subtracted, where `score` scores a source, from `CHANGELOG.md`'s removed sentences alone, so a gathered rewording of a lost entry still splits it, no gathered text subtracts another file's sentence, and a release that renames `## Unreleased` or rewords an entry does not let a fragment quoting wording the same commit corrected subtract the survivor in another file; the held count is untouched | `skills/code-review/scripts/survivor_check.py#corrected@3502a088`, `skills/code-review/scripts/survivor_check.py#a_gathered_fragment@a9ab13f3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_renames_unreleased_and_gathers_still_reports@dad4d1c8`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_gathers_and_deletes_the_fragment_still_reports@d154e922`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_rewords_an_entry_and_gathers_still_reports@0f8bd859`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_replaces_an_entry_with_a_gathered_rewording_reports@1172ddd8`, `skills/code-review/scripts/survivor_check.py#score@d07bfb3f`, `skills/code-review/scripts/survivor_check.py#examine@4ad680b7`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_fragment_cannot_subtract_a_survivor_through_a_lost_entry@a84cea29` | **Executed** 2026-09-24. G1 (H1k), G2 (H1) and G3 (H2k) each seen red at `61f0d0d8`, exit 0 with `against 2 sentence(s)`; green at `1d177b26`, the module 96 → 99. Mutated from Python, the file restored byte-identical: the held set read at the tip, G2 alone red of 15 release and fragment cases; the filter off, G1–G4 red and the guard's two pins green | 2026-09-24 | #557's paste-ready fix, applied as written except one comment sentence: it said the fragment is read at `a` *which the release does not delete*, false for the deleting gatherer G2 pins, and it now says the fragment stands at `a` whether the release leaves it or deletes it. Read at `a` because `gather_changelog.py` leaves the fragment in place, so it is not in the release commit's diff at all, and a gatherer that deletes it leaves nothing at the tip. The list is spelled from `MARKER`'s ids, never from a git path listing, so `test_every_path_list_this_module_derives_from_git_is_filtered_or_named` counts nothing new. Every marker at the tip is read, earlier releases' too, and what that holds is only sentences nobody in this range wrote. Not closed: a fragment copy-edited as it is gathered, or created and gathered in one range, is wording this range wrote and is written (`spec.md` §*Out*). **Corrected 2026-09-24 in round 1's fix pass (🟡 1, ⬜ 2)**: this row said the text is *held and never written*, and that holding it *only drops from `written` sentences nobody in this range wrote*, read as the noise direction. The score is not monotonic in `written`: withholding a sentence that shares runs with a removed one merges those runs, and a merged run is capped at 1.0 (`seal/ledger.md` S1). Round 1 executed it (X5): the live entry `FOUND` replaced at the release by a gathered fragment rewording it, `docs/b.md` quoting `FOUND`, exit 1 at `61f0d0d8` and exit 0 at `bf7ba905`. Fixed at `79c791fe` with round 1's paste-ready fix: a held sentence writes the n-grams it shares with a sentence `CHANGELOG.md` itself lost. X5 is the planted case, red at `bf7ba905` (exit 0, `against 2 sentence(s)`) and green after; the split removed by mutation turns X5 alone red of 25 release cases, and the split written whole turns G1–G4 red. One report-less shape stays, executed in the same fix pass from a deleted probe: a range that deletes a sentence from another file with no rewording of its own, where the only rewording is a gathered fragment's, exits 1 at `61f0d0d8` and 0 at the tip, because that rewording is the fragment's branch's wording and shares nothing with what `CHANGELOG.md` lost. **Corrected 2026-09-24 in round 2's fix pass (🟡 1)**: the claim and the note above said the shared n-grams are *written*, and the note's first correction called that bound safe for every other file. `written` is one set for the whole range and is subtracted from every file's removed sentences, so a gathered fragment quoting a claim the same commit corrected in `docs/a.md` subtracted the survivor in `docs/b.md` again whenever the lost entry shared the claim's wording: round 2 executed Z1 and Z2 silent at `e6c85df6`, and Z1n, the same release with no fragment, reporting in every state. Fixed at `d66b9632` with round 2's paste-ready fix, whole: the shared n-grams are `corrected`'s third return, `split`, and `score` subtracts them only from a `CHANGELOG.md` source; `examine` is the one caller. The planted case (Z1) is red at `e6c85df6`, exit 0 with `against 3 sentence(s)`, and green after, the module 104 passed. Mutated at `d66b9632` over the whole module, the file restored byte-identical: `mine -= split` removed, X5 alone red; the split put back into `written`, Z1 alone red; the `CHANGELOG.md` test in `score` made true for every source, Z1 alone red. Round 2 also judged the report-less shape above the rule working rather than a defect: a gathered rewording behaves like wording that stood before the range (its control Wc, exit 0 everywhere) and not like wording the range wrote (Ww, exit 1 everywhere) |
| H2 · a gather's marker line ends a block when `newly_released` reads a released section, on both ends of the range, so a fragment whose body opens with prose keeps its first sentence's own key rather than one fused with the marker's words | `skills/code-review/scripts/survivor_check.py#newly_released@9f729ae3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_fragment_that_opens_with_prose_is_still_held@8aaec801`, `tests/test_a_corrected_sentence_survives_elsewhere.py#gathered_section@1a64adc8` | **Executed** 2026-09-24. G4 red with H1 in place and the marker read as prose: exit 0, `against 2 sentence(s)`, the fused key `specs 1700000003 a shipped item the verdict cell …`. Green at `c90821fb`. The blank removed by mutation over the whole module: 1 failed (G4), 99 passed | 2026-09-24 | `agent-contract` §12's member of H1's class, answered by Q1. Blanked inside `newly_released` rather than by widening `BLOCK`, which `issue_claims_check.py` and `round_record.py` spell alike on purpose, and rather than by prepending the marker to the fragment's text, which would couple the key to the gatherer's exact layout. The fixture lays the section down with the gatherer's own `section`, loaded by path, and asserts the body still sits directly under the marker, so a changed gatherer turns it red with a sentence rather than green for the wrong reason |
| H3 · the `lost` guard and H1's filter are pinned apart: a release whose entry is written in place, with no marker, losing no sentence, is kept out of `written` by the guard alone; a gather that loses no sentence is kept out by either one, so its case goes red only with both removed | `skills/code-review/scripts/survivor_check.py#corrected@3502a088`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_release_that_writes_its_entry_directly_and_loses_nothing_reports@c59e27bd`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_gathered_release_that_loses_nothing_reports@e4a87e52` | **Executed** 2026-09-24 at `6ac7a028`, three mutations over the 24 release, changelog, fragment and prose cases, the file restored byte-identical: guard off, G5 alone red; filter off, G1–G4 red and G5, G6 green; both off, G1–G6 red. Unmutated, 24 passed | 2026-09-24 | #555 asked for P6 alone (G6), and after H1 that case pins neither unit, the §15 failure; so G5 (P6d) is the guard's pin and G6 the pair's, each docstring saying which. Q2's measurement agreed with `spec.md` judgment 4. The guard stays: without it G5 goes silent. Not closed: a release that renames `## Unreleased` and writes a new entry of its own quoting corrected wording (H1d) stays silent, because that wording is the range's and is written as any file's is (`spec.md` §*Out*, for the owner). **Re-read 2026-09-24 in round 1's fix pass**: re-run with row H1's split in place at `79c791fe`, the same three mutations give the same reds over 25 cases (guard off: G5 alone; filter off: G1–G4; both off: G1–G6), and the new case X5 is green under each, because each keeps the split. **Re-read 2026-09-24 in round 2's fix pass**: at `d66b9632`, over the whole module of 104, guard off reddens G5 alone; filter off reddens G1–G4 and round 2's case Z1, because Z1's fragment is gathered text the filter holds; both off redden G1–G6 and Z1. The guard and the filter are still pinned apart, and G6 still pins neither alone |
