## 0.11.3 — 2026-09-13

<!-- specs/1789296100-the-seal-and-ci-read-one-ledger-differently -->
### 1789296100-the-seal-and-ci-read-one-ledger-differently

<!-- One work item's rows. No `# <id>` title — `fold_ledger.py` writes the `###`
at the release and moves this file into `seal/ledger.md`; the `####` below is a
heading inside the body, and `demote` moves it two levels to `######` under that
`###`. `CLAUDE.md` says a fragment needs no header of its own, which is not the
same as forbidding one: what the fold drops is the title, and what it demotes is
everything else.

Four rows over a change that adds one printed line and moves no exit code. The
first two are the line and the condition it prints on; the third is the claim
that line makes about a file it does not live in, which is the only part of
this work that can go false without anybody editing it; the fourth is the
five documents that used to describe one reader of three. -->

###### The lenient ledger run says what the broad gate will say

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A run whose answer is exit 1 says that `broad-gate` runs this same check with `--strict`, where drift is exit 2, and that this tree would come back `NOT SEALED`. Every other exit code prints nothing new | `skills/evidence-check/scripts/evidence_check.py#LENIENT_NOTICE@dd5982ec`, `skills/evidence-check/scripts/evidence_check.py#main>"if code == 1:"@e236e784` | **Executed** over fixtures built without git: a drifted tree exits 1 and carries the sentence as its last line; the same tree under `--strict` exits 2 and is silent; a clean tree exits 0 and a BROKEN anchor and an OLD-FORMAT row both exit 2, all three silent. Shown red by mutation — deleting the `if code == 1:` guard so the line prints on every run turns all four silence cases red at once | 2026-09-13 | The sentence is stated twice on purpose, here and in `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py`, compared for equality, so a reworded line goes red rather than quiet. `NOT SEALED` is `seal_stamp`'s word and this is borrowing it |
| The condition for that line is the run's own answer and not a second spelling of the grading | `skills/evidence-check/scripts/evidence_check.py#exit_code@d54d06af` | **Executed** at the unit across the whole grading table — both ledger totals and both records-arm counts, strict and lenient. Shown red by mutation: making drift return 1 whether or not `strict` turns two cases red, the flag's effect and the line's silence under it | 2026-09-13 | The four `return` statements that used to end `main` inline. Extracted so the print reads `code == 1` instead of restating the rule beside it — a predicate that restates a rule is a predicate that can drift from it |
| `broad-gate` passes `--strict` at its ledger call site, which is the half of the printed sentence that is about a file the sentence does not live in | `skills/verify/scripts/broad_gate.py#gate>"checks[LEDGER] = run(LEDGER, [py, EVIDENCE, \"--strict\", root], root, keep)"@b0aafbe9` | **Executed**: deleting `--strict` from that call turns `test_the_gate_still_passes_the_flag_the_notice_names` red, and the file was restored byte-for-byte from a copy held in the mutating process rather than from HEAD | 2026-09-13 | **The call site is in `gate()`, not in `main()`.** `spec.md` §*Data & interfaces* named `broad_gate.py#main` as the coordinate this work builds on; `main` there is lines 644-678 and does not contain the call. Round 1's finding 5 carried that correction back into `spec.md` itself, so the contract and this row now name the same unit. The case walks from `checks[LEDGER]` until the parentheses balance rather than reading a line number |
| No document that tells a reader what drift exits describes the lenient reader alone | `skills/evidence-check/SKILL.md#"## Run / ### Which reader graded your tree"@7348c15c` | **Executed per document**, not once: after the edits, `broad-gate` was replaced with a bare `the gate` in each of the five files in turn and the case re-run — five for five red, each restored byte-for-byte | 2026-09-13 | The case asks for one BLOCK carrying `--strict` and `broad-gate` together, because both READMEs already named `broad-gate` in the sealer's row of the agent table a hundred lines from anything about drift. A file-level check would have passed them with the ledger paragraph gutted. **Re-read 2026-09-13 in round 1's fix pass**, which split the anchored table into four rows — one reader each — so the three that read the exit code are counted apart from the one that never reaches it. `broad-gate` and `--strict` still stand in that one block, and the case is green |

<!-- specs/1789296200-the-record-before-the-fix-sequence-has-no-arm -->
### 1789296200-the-record-before-the-fix-sequence-has-no-arm

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `round_record.py new` compares the resolved `--target` against the branch's HEAD and, where they differ and HEAD reaches commits the round did not, lists those commits with their subjects — and refuses nothing. R4 carries the case with nothing to list | `skills/code-review/scripts/round_record.py#head_moved@d27dd8c1`, `skills/code-review/scripts/round_record.py#head_moved_line@307b565c`, `tests/test_new_says_when_head_is_not_the_target.py#test_a_round_whose_fixes_already_landed_is_told_which_commits@da241fd7`, `tests/test_new_says_when_head_is_not_the_target.py#test_the_round_that_read_head_is_told_nothing_new@f765874e` | **Executed** 2026-09-13 in phase 2. Two mutations, both caught: the three calling lines deleted in `new` (5 of 6 red, A1 green), and `head_moved`'s equality arm deleted so it fires on every call (A1 red alone) | 2026-09-13 | **That it prints rather than refuses is a measurement, and it is the whole of the design.** Phase 1 took it against a criterion `questions.md` Q1 fixed before the number: 0 or 1 differing record and `new` refuses, 2 or more and it prints. **The number is 40 of 152.** Where it was taken matters as much: `main` cannot answer the question at all, because a feature branch squashes into its release branch and the commit the reviewer read goes with it — 0 of 328 records there have a `Target SHA` equal to their adding commit's first parent. The pre-squash refs (`refs/remotes/pull/N/head`) are the only place the moment survives, and commits adding more than one record are excluded by construction as bulk moves. Both differing records opened by hand differ because the round's own paperwork landed between the review and the record. **The listing and not a count** is the second decision: a fix pass and a round paragraph are one line apart in `git log --format='%h %s'` and identical in a number, and the orchestrator's whole judgment here is which kind landed. **The target is resolved for the line**, because `--target` legitimately takes a revision and *the round read HEAD~1* names nothing anybody can open later |
| R2 · a round record can say WHY it was written after its own fixes — `\| Written late \| yes — <why> \|`, written by `new --written-late`, and `chain_check.written_late` prints the refusal with the reason quoted instead of failing | `skills/code-review/scripts/round_record.py#written_late_cell@213ea56d`, `skills/code-review/scripts/chain_check.py#written_late_reason@060965c5`, `skills/code-review/scripts/chain_check.py#written_late@a606a261`, `tests/test_a_record_says_why_it_was_written_late.py#test_the_flag_writes_the_reason_into_the_record@08cf5bf4`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_late_record_that_says_why_prints_instead_of_failing@96a89e1f`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_cell_that_names_no_reason_buys_nothing@db2805d4` | **Executed** 2026-09-13 in phases 3 and 4. Four mutations, all caught: the flag writing nothing (8 red), the emptiness test dropped (5 red), `written_late` ignoring the cell (A5 red), and `close` filtering out a field row it does not recognise (1 red) | 2026-09-13 | **It is a documented relaxation of a gate, and the three exits it replaces are why.** A record refused here used to have rewriting history so the adding commit moves, merging over the red line, or inventing a waiver nobody wrote down; work item 1789034970 met all three, took none, and ended with a pull request red on a line no later commit could clear. **Four values buy nothing** and are judged exactly as before: the row absent — every record that exists today — the cell `no`, a bare `yes`, and a value outside the vocabulary. The bare `yes` is refused at the point of writing too, because a relaxation bought with an empty cell is a waiver with no author, which is the third bad exit under a flag. **It prints rather than passing in silence**, the shape the `ORDER_FROM` grandfathering already takes: the state is what the check exists to surface. **It owes no cutoff of the `ORDER_FROM` kind** — those exist because a check red on history nobody can fix is a check people learn to skip, and this is a relaxation, so no record that exists is judged more harshly. **The row is a row and not prose inside `Target SHA`** because a probe settled by execution that `close` preserves a field row it does not recognise: it assigns into the rows it knows by index and never rebuilds the block. **Re-read 2026-09-25 by work item 1790297083 (#598, #529), phase 4.** `written_late` gained a `fork` argument and returns nothing for a record `restored_from` finds in the base's history, before the add is read. The `Written late` reason is read below that exit and is unchanged, so the claim holds. |
| R4 · where HEAD differs from the commit the round read but reaches no commit the round did not, `new` says that instead of printing a count of nothing under two readings that are both false; and one commit between reads as *1 commit stands* | `skills/code-review/scripts/round_record.py#head_moved_line@307b565c`, `skills/code-review/scripts/round_record.py#head_moved@d27dd8c1`, `tests/test_new_says_when_head_is_not_the_target.py#test_a_tree_behind_the_reviewed_commit_gets_neither_false_reading@922efbd7`, `tests/test_new_says_when_head_is_not_the_target.py#test_one_commit_between_reads_as_one_commit@a47ccb04` | **Executed** 2026-09-13 in round 1's fix pass. The state was reproduced on a scratch repository with HEAD detached one commit behind the reviewed commit: the shipped line read *0 commits stand between them*, listed nothing under it, and offered both readings. Both cases were seen red on the shipped code before the branches they pin existed | 2026-09-13 | **`git log <reviewed>..<head>` is empty when HEAD is an ANCESTOR of the reviewed commit**, and the two are different commits all the same, so the ordinary line printed for a tree that is simply behind. Neither reading describes it — nothing landed after the review there — and the first sends the reader to `--written-late`, which writes a reason into a record nothing is late about. The state is reachable without anybody doing anything strange: `--target` naming the pull request's head ref while the local tree has not fast-forwarded, or a `git switch --detach` onto an earlier commit to re-read something mid-round. **The singular arm existed for the noun and not the verb**, and the one-commit case is the commonest real one, since the round's own paragraph is a single commit |
| R5 · the refusal `written_late` raises names the fourth exit — the `Written late` row, its cell shape, the flag that writes it, and the hand-edit route a record already on the branch needs | `skills/code-review/scripts/chain_check.py#written_late@a606a261`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_failure_names_the_fourth_exit@efcc1585` | **Executed** 2026-09-13 in round 1's fix pass. The shipped refusal was captured from a scratch repository and searched: `Written late`, `--written-late` and *added by hand* were all absent. The case is red on the first of those with the refusal itself still firing | 2026-09-13 | **The flag alone is insufficient by construction.** `new --written-late` writes the row as it writes the record, and every reader of this refusal already has a committed record — so naming only the flag names an exit that has gone. The exit was documented in the template, the spec, the orchestration half and the notice printed for a record that ALREADY carries the row: every place except the one text the person who needs it reads. A session met this exact refusal on 2026-09-13 and escaped only because its commit was unpushed. **Re-read 2026-09-25 by work item 1790297083 (#598, #529), phase 4.** `written_late` gained the restoration exit before the add is read. The refusal's text is untouched and still names the fourth exit, so the claim holds. |
| R3 · the row's label has one spelling — `chain_check.WRITTEN_LATE`, imported by the generator — and `templates/sdd-round.md`, `docs/review-chain-spec.md` and `skills/code-review/orchestration.md` each carry it with the reason a reader needs | `skills/code-review/scripts/chain_check.py#WRITTEN_LATE@f5afb2ac`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_row_has_one_spelling_and_the_generator_imports_it@43fcb6df`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_the_spec_carries_the_fourth_exit_and_its_states@8f8e066d` | **Executed** 2026-09-13 in phase 5, as part of the module's own run at 50 passed | 2026-09-13 | Equal values would not catch the drift this guards, because they are equal right up to the rename — so what is asserted is that the generator has no literal of its own to rename. The failure is silent in the direction that matters: rename the row in one file alone and the generator goes on writing a row the checker no longer finds, which the checker reads as a record that said nothing. Same reasoning as `BROAD_GATE`, which moved to the reader for the same failure |

<!-- specs/1789296300-a-segments-own-wall-clock-is-in-no-column -->
### 1789296300-a-segments-own-wall-clock-is-in-no-column

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A segment transcript's opening stamp names it: it is matched to the nearest unclaimed `Agent` result within one second, each spawn claimed at most once, and a segment matching nothing is named by nobody rather than by its nearest spawn | `skills/verify/scripts/session_cost.py#opening_stamp@897f323b`, `skills/verify/scripts/session_cost.py#join_segments@53882697` | **Executed**: a probe over all 43 runs with a `subagents/` directory on the machine that built this, applying the one-to-one rule itself — 296 of 349 segment transcripts named at 1.0s, 301 at 2.0s. Plus six cases in `tests/test_session_cost.py`, each red first on `KeyError: 'segments'`: the named pair, a segment moved three seconds out and shown unnamed against the same fixture named at zero, a nested transcript with no spawn to claim, two transcripts opening at one instant against one spawn, and a run with no `subagents/` directory | 2026-09-13 | The remainder is structural rather than a tolerance problem: those 43 runs hold 349 segment transcripts against 305 `Agent` calls, so 44 have no call in the parent to be named by at ANY tolerance — a subagent of a subagent is spawned from a transcript the parent never sees. Widening to two seconds buys five of 349 and doubles the window a batch of two spawns can match the wrong one in. `opening_stamp` reads the file's first row rather than its first tool call because the measurement was taken against the opening; joining on the first call would be late by the agent's whole startup read, about 110,000 characters (#265) |
| The harness writes every segment flat under `<session-id>/subagents/`, subagents of subagents included, so what makes a segment unnamable is the absence of an `Agent` call in the parent and never the directory it sits in | `skills/verify/scripts/session_cost.py#subagent_transcripts@469d2608` | **Executed**: the same probe reported zero transcripts at any depth below one, across all 349. The walk covers both shapes either way — it is `os.walk` rather than a listing — and `tests/test_a_segment_the_parent_cannot_name_is_counted_rather_than_dropped` keeps the nested shape covered | 2026-09-13 | `spec.md` and `plan.md` both describe the unnamable segment as *a nested transcript under another segment's directory*, which is the frame's one wrong picture. Nothing needed repairing: the join infers *unnamed* from the match failing, never from the path |
| A resumed segment is one row per stretch of work, cut at the coordinator's own message row — a `type=user` row with `isMeta` whose bare-string content begins `The coordinator sent a message while you were working:` | `skills/verify/scripts/session_cost.py#resume_cuts@a1569511`, `skills/verify/scripts/session_cost.py#segment_slices@b342b3fd` | **Executed**: a probe over all 350 segment transcripts on this machine — 41 hold an idle gap at or above `analyse`'s 900-second ceiling, 82 such gaps in all, and 61 of the 82 are followed by that row shape. Plus a two-slice fixture whose slices read 16s and 21s against the file's own 8,406s span, shown red first against the one-row reading of `140.1m` | 2026-09-13 | **The marker is the sentence and not the row shape, and the probe is what settled it.** The same `isMeta` bare-string shape also carries a response-cut-off-mid-stream notice and a background-task notification; splitting at those cuts one stretch of work in half, which is worse than not splitting because it invents a boundary rather than missing one. **Re-read 2026-09-13 in round 1's fix pass**, which stopped a window with no call in it from becoming a row: two coordinator messages arriving before the agent acts made an empty window, and printing it gave the file a stretch the agent never worked. The claim is strengthened rather than changed — one row per stretch of work is what it says, and an empty window is not one — and the file that paired no call anywhere still owes its single row. The cost is that a harness rewording the sentence stops every split — and that failure is loud, because the file falls to `widest_idle_gap`'s floor, which prints one row AND names the gap the span now covers |
| The first slice still opens at the spawn's result, so slicing changes what a row is without changing what is joined | `skills/verify/scripts/session_cost.py#measure_segments@b91ce824` | **Executed**: the same probe found exactly 350 bare-string `type=user` rows carrying no `isMeta` across 350 files — one per file, the spawn prompt, which is the row `opening_stamp` reads. `tests/test_a_later_slice_inherits_the_name_from_the_files_first` pins the consequence | 2026-09-13 | Every slice carries the file's name because only the file's opening can be joined: a resume has no `Agent` call of its own in any transcript — the coordinator sent a message, it did not spawn — so a second slice would otherwise read as a segment nobody spawned and the unnamed count, which is a reading about the harness, would climb with every resume. **Re-read 2026-09-13 in round 1's fix pass**, which made `unnamed` count TRANSCRIPTS rather than rows — the failure this sentence names, undone one function later and repaired there. What is joined is untouched: the first slice still opens at the spawn's result. |
| A segment's own `Agent` calls are counted per slice, and a non-zero count prints a line naming the agent, the count and §6 | `skills/verify/scripts/session_cost.py#report_breaches@fa29d967` | **Executed**: run across all 43 runs with a `subagents/` directory on this machine — exit 0 on every one, and 13 carry the line. **Re-measured 2026-09-13 in round 1's fix pass**, because the first reading counted runs and not what §6 reaches: of the 13, **12 name an agent this plugin spawns** — 40 `Agent` calls by wardens and 4 by smiths — and the thirteenth names `claude-preset:code-reviewer`, which no definition here governs and whose own procedure instructs the fan-out. The positive case was red first on the absence of `§6` from the report, and its negative twin — two segments that spawned nothing print no line and no mention of the section — is planted in the same commit | 2026-09-13 | **It notices and stops nothing, and that is the claim the evidence's location permits rather than a softer one chosen on taste.** The transcript is under the home directory of the machine that ran the agent, in no commit and on no CI runner, so a checker would run exactly where a person already is. The two counts of one breach — calls inside segments, and segments the parent could not name — are printed rather than reconciled, because where they part either a child's transcript is missing or a segment is unnamed for the other reason and nothing here can tell which. **Re-read 2026-09-13 in round 1's fix pass**, which added the sentence saying who §6 reaches: the walk sees an `Agent` call and cannot tell an agent this plugin spawns from one whose own procedure instructs the fan-out. Measured over the 43 runs on this machine, 13 carry the line and 12 of those name an agent this plugin spawns. The row is stated rather than filtered, which is the direction the two counts already take |
| A segment row's tokens cover that segment's own file, and a later slice of a resumed file carries none at all | `skills/verify/scripts/session_cost.py#segment_slices@b342b3fd` | **Read**, plus `tests/test_a_segments_tokens_are_its_own_file_and_not_the_runs` (two rows summing to 2 transcripts against the run's 3) and `tests/test_a_files_tokens_are_carried_by_its_first_slice_only` | 2026-09-13 | Re-deriving a token figure per slice would duplicate `token_totals`' keep-the-largest-count-per-message-id rule, and a streamed message whose rows straddled a slice boundary would be counted in both — #202's own failure shape rebuilt one reader over. So the figure stays the file's, rides its first slice, and the others print a dash rather than a zero a reader would add up. **Re-read 2026-09-13 in round 1's fix pass**: the figure now rides the first KEPT slice rather than window 0, which may have been dropped as a window holding no call. Both halves of the claim stand — the figure is the file's and a later slice carries none. **What this gives up is a per-slice token column**, stated in the docstring rather than left to be found; widening `token_totals` to take a time window would drift three ledger rows anchored on it to refine a column this work item is not about |
| Nothing the mode adds changes what any existing reading prints | `skills/verify/scripts/session_cost.py#analyse@c8423846`, `skills/verify/scripts/session_cost.py#report_spawns@15595f59` | **Executed**: `tests/test_no_existing_printed_line_moves_when_the_mode_is_not_asked_for`, plus the module's pre-existing 69 cases green throughout — including `test_the_plain_reading_is_the_number_it_was_before_the_mode_existed` and `test_a_cycle_carries_the_same_numbers_the_whole_run_does` | 2026-09-13 | `analyse`'s own docstring sets this condition and #200 and #202 are what breaking it cost. The one existing case that moved is `test_a_cycle_carries_the_same_numbers_the_whole_run_does`, which derives the run's `analyse` keys as `set(data) - {"tokens", "spawns"}` and caught the new top-level key on the commit that added it — its docstring says adding one is *a decision somebody makes here*, so the exclusion list gained `segments` and the reason it is not one of `analyse`'s keys is recorded beside it. That is the case working, not a regression |
| `segment` means one agent's own stretch of a chain, and a spawn cycle is not one | `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@70aef725` | **Executed**: `tests/test_one_word_one_meaning.py::test_the_owner_states_what_a_segment_is_and_what_it_is_not` and `::test_no_shipped_document_calls_a_spawn_cycle_a_segment`, both red first against the standing sentence, which the assertion quoted back | 2026-09-22 | The loose sentence had three shipped homes and the sweep found the third: `skills/verify/SKILL.md`, `session_cost.py`'s module docstring, and `tests/test_session_cost.py`'s own comment, which carries it verbatim and is the file a reader opens beside the module. Records were left alone — `CHANGELOG.md` and every `seal/specs/…` directory hold what was true when they were written — which is the boundary that module's seal sweep already draws **Re-read 2026-09-22 for the flow-log posting command**, which named `session-cost … --post` in place of the two by-hand lookups and the post step, and kept both `gh` forms in the prose beside it. Step 1, the run-level table and the orchestrator-boundary paragraph are untouched. This claim is about a different part of the section; the hash is the new content of the same anchor. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which changed the `Broad gate` row of the run-level table in this section to read the count off the cell — one entry per run, newest first; nothing this row is about moved **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** only the `Location` row's record paths changed, so the claim holds |
| The rule that a session measures a segment with the mode rather than one invocation per transcript, and takes the resume split by hand | `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@70aef725` | **Executed**: four cases in `tests/test_a_segment_feeds_the_flow_log.py`, three of them red first — the hand-split instruction still present, the mode's handling of a resume unstated, and `§6` absent from the section | 2026-09-22 | The fourth, `test_the_section_names_the_per_segment_mode`, could NOT be seen red: phase 2's word-pinning edit had already put `--segments` into that section as the contrast with `--spawns`, so the string was there before this phase ran. It is a guard against the name being dropped, not evidence for this phase **Re-read 2026-09-22 for the flow-log posting command**, which named `session-cost … --post` in place of the two by-hand lookups and the post step, and kept both `gh` forms in the prose beside it. Step 1, the run-level table and the orchestrator-boundary paragraph are untouched. This claim is about a different part of the section; the hash is the new content of the same anchor. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which changed the `Broad gate` row of the run-level table in this section to read the count off the cell — one entry per run, newest first; nothing this row is about moved **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** only the `Location` row's record paths changed, so the claim holds |
