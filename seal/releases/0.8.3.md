## 0.8.3 — 2026-09-06

<!-- specs/1788686494-the-printed-ledger-name-collapses-through-relpath -->
### 1788686494-the-printed-ledger-name-collapses-through-relpath

#### The printed ledger name is the file that was read

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A ledger path rendered for a person drops the root's own segments and folds no `..` | `skills/evidence-check/scripts/evidence_check.py#display_name@091b0e46` | **Executed**, by two batteries over the unit's eighteen cases. Against `flavour.relpath` in the unit's own position, eleven cases go red — `x/lnk/../ledger.md` renders as `x/ledger.md`, and on the symlink fixture the rendered name reopens `<root>/x/ledger.md` while the pattern opened `<root>/ledger.md`. Against the verbatim-always alternative, nine go red. Every case is red against at least one. By mutation, six decisions each turn a case red: the anchor comparison, the drive comparison, the `<=` at the tail length, the separator set's source, slicing rather than rejoining, and whole-segment rather than `str.startswith` | 2026-09-06 | `relpath` normalises `..` the way `normpath` does, lexically, so where a pattern crosses a symlink before a `..` the printed name is a different file that exists. Round 13 of work item `1788501054` closed the same rule for the file that is OPENED; round 14 found the printing half open, and issue #163 is that class. This is phase 1 — the unit alone; the five call sites are phase 2's |
| A miss prints the caller's own spelling, so it costs length and never correctness | `skills/evidence-check/scripts/evidence_check.py#display_name@091b0e46` | **Executed**: a path outside the root, a root spelled with a different anchor, a sibling whose name extends the root's, and a second Windows drive all come back verbatim. `ntpath.relpath` does not answer the last one at all — it raises `ValueError` on two drives, so the site that called it would traceback instead of printing a name | 2026-09-06 | The judgement `plan.md`'s accepted alternative makes, and the one the handoff asked to be written down for local mode: `seal_home` can answer `<git-common-dir>/seal`, which from a LINKED worktree is absolute and outside the worktree root (`optin.git_common_dir` normalises git's answer against the root). Such a header prints absolute — longer than `relpath`'s `../main/.git/seal/ledger.md`, and it names the file that was read from any directory |
| The separator set comes from the path flavour, so a Windows case runs from a POSIX machine | `skills/evidence-check/scripts/evidence_check.py#display_name@091b0e46` | **Executed**: five cases pass `ntpath` and assert the Windows answers on macOS, and one asserts that `\` is an ordinary character under `posixpath`. The mutation that hardcodes `("/", "\\")` survived the first battery and was caught only after a second assertion was added — `/tmp/proj\seal/ledger.md` is one segment on POSIX and is NOT under `/tmp/proj`, and the hardcoded set answered `seal/ledger.md` for it | 2026-09-06 | `agent-contract` §13: a case that skips off Windows is a defence resting on a guarantee nobody removed, and round 14 of `1788501054` was a Windows leg red at a reviewed head for exactly that reason. The one case that stays skipped on Windows is the symlink fixture, where the premise itself is gone — Win32 folds `..` before the filesystem is consulted, so the two renderings name one file |
| Every place a ledger path becomes a name a person reads calls `display_name`, and the class is closed by data flow rather than by a list of sites | `skills/evidence-check/scripts/evidence_check.py#check_ledger@7391f8db`, `skills/evidence-check/scripts/evidence_check.py#migrate@f0cdbbcf`, `skills/evidence-check/scripts/evidence_check.py#reverify@cc41254a`, `skills/evidence-check/scripts/evidence_check.py#main@ceb63d8d`, `tests/test_the_printed_ledger_name_is_the_file_that_was_read.py#test_no_ledger_path_reaches_relpath@11087e2c` | **Executed**: all five sites put back to `os.path.relpath` in one batch, and the source-reading case names each of the five with its function and line — `check_ledger` L1131, `migrate` L1389, `reverify` L1514, `main` L1741 and `main` L1708. Green with them routed. The 328 cases of every module that asserts checker output text pass unchanged, so no existing rendering moved | 2026-09-10 · 2026-09-13 · 2026-09-26 | The issue listed four sites, found by a grep for `relpath(ledger`. There are five: `main`'s `--ledger narrowed this run` loop names its variable `path`, and its paths come from `skipped_by_narrowing`'s OWN `resolve_patterns(default_patterns(root))` rather than from the `--ledger` arm — so their spelling is `seal_home(root)`'s, which in local mode from a linked worktree is absolute and outside the tree. The class was enumerated by data flow instead: a ledger path enters the program at three `resolve_patterns` calls and nowhere else, so every name bound to one of those lists, every loop variable over one, every parameter one is passed to and every assignment from a function returning one is the closure, and every statement such a name appears in is the sink set. Twenty-four statements, of which five render. **Round 1 measured the guard that holds this and found it under-reaching**: six spellings of a `relpath` on a carrier went unreported — an alias, a subscript, an inline wrapper, a tuple unpack, a bare `from os.path import relpath`, and `os.path.normpath`. Two rules were widened to close them, leaving the carrier sets byte-identical on the current source. What the guard covers is the spellings an ordinary edit reaches for; a carrier arriving at a rendering through a dict value or a `getattr` still passes it, and no version of this check makes that untrue  **Re-read 2026-09-10 in work item 1789034970 (#120), round 1's fix pass.** The same rider inside `reverify` moved that unit's hash. The call to `display_name` for an unreadable ledger is untouched, and the class is still closed by data flow.  **Re-read 2026-09-13 in work item 1789296100 (#354).** The line added to `main` prints a module constant and no path, so every ledger path a person reads still reaches them through `display_name`. **Re-read 2026-09-25 by work item 1790260566 (#444), phase 2:** `check_ledger`, `old_format_rows`, `migrate` and `reverify` now skip every line of a fenced block that closes, through `quoted_lines`; a row in an unclosed fence or an HTML comment is read as before. Every `display_name` call is where it was. **Re-read 2026-09-25 by work item 1790297087 (#299), phase 2:** the new output names coordinates, not ledger paths: `reverify`'s `LEFT` line names a malformed coordinate and `main`'s totals lines gained a count, so no new place turns a ledger path into a name and the claim holds. **Re-read 2026-09-26 by work item 1790381328 (#614), phase 3:** `main`'s change is `--strict`'s help text, which now says drift and malformed coordinates also fail; the grading itself moved in `exit_code`, which `main` calls as before. No ledger path is rendered anywhere new, and the claim holds |
| The header printed over a ledger opens the file that was read, not a lexical guess at it | `tests/test_a_narrowed_ledger_read_says_what_it_skipped.py#test_the_printed_header_names_the_ledger_that_was_read@e90471dd` | **Executed**, seen red first with `os.path.relpath(ledger, root)` back in the header's position: the header read `x/ledger.md` and stat-ed to inode 185959477 while the run had read 185959475 — `assert (16777232, 185959477) == (16777232, 185959475)`. Green with `display_name`, header `x/lnk/../ledger.md`, one inode | 2026-09-07 | The assertion is by inode before it is by spelling, so a future renderer that finds some third collapsing spelling fails too; the spelling is pinned second because a name that resolves correctly and reads as gibberish is still a bad name. The fixture's premise — that `ledger.md` and `x/ledger.md` are two files — is asserted rather than assumed, since were they one every assertion would pass against `relpath` as well. POSIX only, and what is missing on Win32 is the premise rather than the platform: it folds `..` before the filesystem is consulted, so there the pattern names `x/ledger.md` and no second file exists for a header to name by mistake |
| The scanned-source path is not a ledger name, keeps its `relpath`, and the class check does not reach it | `skills/evidence-check/scripts/evidence_check.py#scan_candidates@d2637ac4`, `tests/test_the_printed_ledger_name_is_the_file_that_was_read.py#test_the_scanned_source_path_is_not_a_ledger_and_keeps_its_relpath@f7685c46` | **Executed** in both arms: routing the site through `display_name` turns the case red on its first assertion, and dropping the detector's carrier guard so it flags every `relpath` turns it red on the second, reading `assert 'scan_candidates' not in ['scan_candidates']`. The data-flow enumeration does not reach the site at all — `full` and `repo` are not in the closure | 2026-09-07 | `spec.md` puts it out of scope and this is the judgement written down. Three things separate it from a ledger name: `os.walk` composes it downward from the root it was given, so it carries no `..` to collapse; it is compared against `rel`, a path spelled the way a ledger row spells one, so it must be normalised rather than preserved; and it appends `.replace(os.sep, "/")`, which `display_name` deliberately does not do, so routing it through the helper would change what Windows prints for every `(moved?)` hint. **Re-read 2026-09-23 by work item 1790154760 (#519)**, whose edit to this unit is its docstring alone: the stale "~114 ms" became "the clean path pays nothing for it". The path handling the claim is about is unchanged |
| The class check cannot pass vacuously | `tests/test_the_printed_ledger_name_is_the_file_that_was_read.py#test_the_refusal_above_can_actually_fail@d3aa66c9` | **Executed**, and this is the mutation that mattered: with the detector's seed changed from `resolve_patterns` to a name no call uses, the carrier set is empty, `test_no_ledger_path_reaches_relpath` PASSES on an empty offender list, and only this case goes red — `assert 'ledger' in set()` | 2026-09-06 | `verify`'s second condition, a check that cannot fail is a counterfeit seal, applied to a case whose green is an empty list. Two arms: the carrier set must still hold the six known names, and one site is put back in a copy of the source where the detector must name it. The `assert put_back != text` guard is what keeps the second arm from proving nothing after the header site is next respelled. **Re-read 2026-09-08 in work item 1788826000's round 2 fix pass.** The hash moved in that work item's round 1 fix pass with no note saying who read what, which is the one thing a re-stamp must not leave behind. Executed: across the whole branch, `git diff <base>..HEAD` over this file is ONE line — the rider's own stamp, `Verified 2026-09-06 at 4581fe1` becoming `Verified 2026-09-06 against test_the_refusal_above_can_actually_fail@8e0a246a`. The date is unchanged, the claim did not move at all, and a ledger hash covers comments, which is the whole reason the region moved (round 2, finding 14) |
<!-- specs/1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback -->
### 1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Two rows over a branch of one guard, one sentence and one case. Both earn a
row for the same reason: each is a claim whose repair looks obvious from the
outside, and in both the obvious repair is DELETION. R1 is a `try` around a
one-line write that reads as defensive programming beside a guarded read
thirty lines up; R2 is a second case beside one that already reads the
generator's constants, which reads as a duplicate of it.

Neither row is a rewrite of a row in `seal/ledger.md`. The two this branch
touched there — R2 and R8 of the `1788632199` fragment's section — keep their
claims and gained a caveat each, because what this work found was a
precondition each had left unstated rather than code either of them cites
being removed. The reasoning is in those rows' own Notes, where a reader who
opens the coordinate will be.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run: two anchors
in `seal/ledger.md` drifted for edits this branch never opened
(`templates/config.md` and `round_record.py#swallowed`), and a blanket run
would re-stamp them on behalf of a session that never read them. The unscoped
READ form ran before and after each time — phase 2's output is in
`phases/phase-2.md`, round 1's fix pass in `rounds/round-1.md` and round 2's
in `rounds/round-2.md`.

`seal/ledger.md`'s R2 cites `hide_from_git` too, and this branch's edits
drifted it twice. Both times the anchor there was re-stamped BY HAND rather
than by the scoped write, because `--reverify` narrows to a file and that file
holds two anchors nobody here has read. The hash was computed the way the tool
computes it and matched the tool's own value for the same anchor in this
fragment, on both occasions. Round 2's fix pass touched no file that row
cites, so it drifted nothing there and stamped nothing by hand. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · the one write this runner makes to the working tree is guarded, so a `.venv` the operator has made read-only ends the run in a sentence rather than in a traceback under a refusal that had already printed. The sentence names the ignore it could not write, the reason, and that the virtualenv stays visible to `git status` until that write can succeed or the reader removes the directory — **a remedy that names no cause**, because `except OSError` is four of them and `make it writable`, the wording this row carried first, is wrong advice on a full disk and on a read-only filesystem (round 1's finding 3); `strerror` carries which case it was. The refusals above it keep their wording and their exit codes, and the runner does not try to make the write succeed. The class is one member by construction rather than by a list: everything else this module puts in the tree is a builder subprocess's, whose failure is already a return code the caller reads | `.github/scripts/run_tests.py#hide_from_git@6182d405`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_unwritable_venv_leaves_the_refusal_a_sentence@6729bec5`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_unwritable_sentence_is_the_same_on_every_platform@421257f5` | **Executed** 2026-09-06 in phase 1, both cases seen red first against the unguarded write at `aabb30c`: `PermissionError: [Errno 13] Permission denied: '<tmp>/.venv/.gitignore'` escaping `ensure`'s `finally` after the floor refusal had reached stderr. Green at `8be39a9` with the six `hide_from_git` cases beside them. **Executed** against the real fixture through a probe deleted after it ran — a `.venv` holding an interpreter and a `pytest` script, `pyvenv.cfg` at `version = 3.11.9`, the directory `chmod 555` — two sentences, exit code `2`, no traceback, and `git status --porcelain` reading `?? .venv/`, which is the directory the second sentence promises the reader will see | 2026-09-06 | **The tidy-up is deleting the `try`**, which reads as defensive programming around a one-line write. `venv_version` guards its read thirty lines up, so the unguarded write was the odd one out rather than the house style — and the write sits in a `finally` that runs on the two exits whose entire product is a sentence, which is what turns one `OSError` into a traceback printed underneath a refusal the reader has already been given. **The second is catching the error and saying nothing.** That leaves the directory in `git status` with nobody told why, which is the silent half of the very defect `seal/ledger.md`'s R2 exists against — the guarantee would then fail invisibly instead of loudly. **The third is testing the directory for writability before writing**: a check that can pass and then fail, and one more thing to keep true, where the write already answers the question. **A `chmod 555` fixture is not a guarantee**, so the case built on one is skipped as root and on Windows with the reason written into the `skipif` rather than beside it, and a second case makes the WRITE refuse by monkeypatching `Path.write_text` so the wording is pinned on all three platforms CI runs. Asserting only *no traceback* would have passed silently wherever `chmod` does not stop a write; asserting the sentence converts that into a case that cannot pass on a write that succeeded |
| R2 · the reach vocabulary `docs/round-record-spec.md` §*The fix surface* names is derived from what `call_sites`' `return` statements can HAND BACK — not from the strings that appear inside one — so a value the function GAINS has to reach the document before the suite is green again, and a comparison operand or a keyword argument in the same `return` does not. `handed_back` has one arm per way an expression carries a value outward, and everything else arrives as nothing. **The returns it is asked about are the scope's OWN**: `own_returns` stops at every node that opens another function scope, so a `def` written inside `call_sites` no longer hands the derived set a word `call_sites` never returns. The boundary set is two members and not four — only a function body may hold a `return`, and a class or a lambda is reached through a boundary rather than past one, so a member for either is one no mutation can kill. What the derivation cannot see is recorded in `reach_values`' docstring in both directions: five shapes hand a value back across a statement or a call and read as nothing, six more are one `isinstance` arm away, and TWO shapes are read though the function may never hand them back — a call's positional arguments, and a local that shadows a module constant, which derives the module's value. That is why the refusal names two directions instead of only the word | `tests/test_the_fixes_name_their_surface.py#own_returns@d668f779`, `tests/test_the_fixes_name_their_surface.py#SCOPE_BOUNDARIES@ec701f53`, `tests/test_the_fixes_name_their_surface.py#handed_back@f14fa539`, `tests/test_the_fixes_name_their_surface.py#reach_values@27fbe2e5`, `tests/test_the_fixes_name_their_surface.py#test_the_section_names_every_reach_value_the_generator_fixes@44a43bc6`, `tests/test_the_fixes_name_their_surface.py#test_the_derivation_reads_both_shapes_a_return_can_fix@16d3aa4d`, `tests/test_the_fixes_name_their_surface.py#test_a_second_call_sites_is_refused_rather_than_picked_from@ce202481`, `tests/test_the_fixes_name_their_surface.py#test_the_refusal_names_the_other_direction_too@bf436dc5`, `skills/code-review/scripts/round_record.py#call_sites@61f9b386` | **Executed** 2026-09-06 in phase 2, seen red first: with `NO_TRACKED_FILE = "not a tracked file"` added to `call_sites`' returnable set, the run reads `1 failed, 1 passed` — the derived case exits 1 naming the value the section does not carry, and the case that reads the three constants by name passes, which is the gap measured rather than argued. Restored from kept bytes and verified byte for byte. **Executed** 2026-09-06, six mutations over `reach_values` one at a time, each killed by a named case: the named-constant arm deleted, the literal arm deleted, either type check dropped, the return-only filter dropped, the one-definition guard weakened to a truth test, and the derivation emptied. The first run left one survivor — dropping the string check on a literal — because no fixture put a non-string literal in a `return`; the fixture was added and the mutation is dead. **Executed** 2026-09-06 in round 1's fix pass, after the review found the walk collecting every string in a `Return` rather than what one hands back: the four over-collection fixtures — a comparison operand, an f-string, a keyword argument, a dict key — seen red against the blanket walk they replace (`4 failed, 10 passed`), then all ten arms and type guards of `handed_back` mutated one at a time and every one dead. Both modules green at `102 passed`, exit `0`. **Executed** 2026-09-06 in round 2's fix pass, after the verifying round found the walk AROUND `handed_back` descending into nested scopes — the same defect one function up: three of the four new nested-scope fixtures seen red against the `ast.walk` they replace, and every line of `own_returns` plus both members of `SCOPE_BOUNDARIES` mutated one at a time and dead. Three deliberate survivors, two of which are the grounds for the two-member set: adding `ast.ClassDef` or `ast.Lambda` back changes nothing on any shape a compilable module can hold. The third is the `child.value is not None` conjunct, a type guard rather than a behaviour guard — `handed_back` answers nothing on a `None` through its fallthrough — and `own_returns`' docstring names it as a survivor where round 2's count did not. Round 3 measured both narrowings: dropping the conjunct leaves the module green at 55 passed, and the one shape on which the two boundary sets differ is a `return` written in a class body, which `ast.parse` accepts and `compile` rejects, so no module this can be handed holds one. Both modules green at `106 passed`, exit `0`; `uvx ruff check` and `uvx ruff format --check` exit `0` on every changed file | 2026-09-24 | **The tidy-up is deleting one of the two cases as a duplicate of the other**, and neither direction is safe — but the reason this row first gave was false, and round 1's finding 1 is that it was asserted rather than measured. Measured: the sentence `only the first is a unit name` is read by the NAMED case alone, so dropping it from the section reddens that one and leaves this one green; a value ADDED to `call_sites` is invisible to the named case and reddens this one; and a rename or a reword reddens BOTH, because the floor here reads the same three constants by attribute name. The false half claimed the rename for the named case alone, and it stood in the docstring where the deletion would be typed. **The second tidy-up is listing the fourth value beside the case** when one is added, which is the defect this row exists against moved one level up: the list would be the thing going stale, and nothing would be reading the generator at all. **The third is the vacuous pass**: a derivation that stops finding anything — `call_sites` renamed, the `return` shapes changed — leaves the assertion loop with nothing to iterate and the case green. So the derived set is required to CONTAIN the three constants the named case reads before the loop runs, which is a floor with content where a bare count would have been a number anybody could lower. **The fourth is deleting the `source` parameter as test-only machinery.** It is what makes the literal arm reachable at all: today's `call_sites` names a constant in every return and writes no literal into one, so against the real module alone that arm is dead code and the mutation deleting it survives — which is what the first mutation run measured, before the fixture that kills it existed. The same parameter is what executes the recorded limit rather than merely stating it **Re-read 2026-09-08 under work item 1788817290**, which gave `call_sites` a `runner_reached` branch for a unit pytest itself reaches (#211). It hands out `PYTEST_ONLY`, an EXISTING value, so the vocabulary is unchanged at five and both derivations still agree — `test_the_section_names_every_reach_value_the_generator_fixes` passed at this SHA. Re-read 2026-09-24 — #526's split re-pointed the anchored case to the document that now holds the section it reads, and widened any absence it asserts of the spec to all three documents; the claim unchanged. **Corrected 2026-09-24** by work item 1790208643's round-1 fix pass (⬜ 11): the Claim cell named `docs/review-chain-spec.md` for §*The fix surface*, which #526's split moved to `docs/round-record-spec.md`, and this branch's *the claim unchanged* note above left the cell naming the old file; it names the file that holds the section now. |

<!-- specs/1788700685-two-value-shaped-odd-rows-end-the-report -->
### 1788700685-two-value-shaped-odd-rows-end-the-report

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows over a branch of four guards and a method. R1 and R2 are the two
crashes the work item was opened for; R3 is the enumeration that found them,
which is the half a later session cannot re-derive from the diff.

R3 carried a fourth shape as deferred, and round 1 showed the deferral rested
on a false measurement: `output_tokens` is the one usage field that never
reaches `token_thirds`'s `round`, and it was the only field measured. Every
other one ends the report. It is closed at `count`, the rider that carried it
is gone — a rider that has been acted on is spent.

**Round 3 then found the shape none of that reaches**, and it is why R3 now
records a limit rather than a closed axis. A funnel answers for the values
that ENTER it. Two counts `count` accepts as finite add to one it would
refuse, and the mean of a third of them ended the report with stdout empty on
both arms — at this branch's base as well, so a member the enumeration missed
rather than a regression. The site is guarded at `token_thirds`. The class is
not closable by a guard and is not closed: enumerating call sites answers
which operations exist and never which values reach them, and this 🔴 arrived
through a call the walk had listed since its first widening.

**R3 is why `seal/ledger.md`'s R6 was corrected in place rather than
removed.** That row states #170's guarantee over the whole class — *no shape
a harness can write ends the report* — and its own Verified cell records the
construction that grounds it: every field the two readers read, crossed with
the seven JSON types and with the field absent. Measured here, on the module
R6 was stamped against: all eight variants of the `timestamp` field exit 0,
and the two shapes this work item closed exit 1. The enumeration is sound on
the axis it ran and structurally blind to the other one, so what the row
needed was the axis named, not deletion. Every anchor it cites still resolves;
this branch changed the CONTENT of two of them — `parse_time` gained the
normalisation, and `count` gained first a rider and then, in round 1's fix
pass, the `math.isfinite` guard that rider had prescribed — which is
re-verification rather than the removal `CLAUDE.md`'s rule is about.

**R3's axis names are the reader's, one level up from `phases/phase-1.md`'s
table.** That table decomposes each PARSED operand into a type half and a
value half, so it files naive-beside-aware under type — a `datetime` carries
its zone in its type. R6's enumeration ran one level up, over the JSON type
each FIELD carries, where a naive stamp and an aware one are both `str`. The
two do not disagree; they are the same axis read at the two levels the code
has, and a row that named only "the type axis" would be read at whichever
level the reader arrived from.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run: two anchors
in `seal/ledger.md` were already drifted at this branch's base for edits
nobody here has read (`templates/config.md#"# Repository config"` and
`round_record.py#swallowed`), and a blanket run would re-stamp them on behalf
of a session that never opened them. The unscoped READ form ran before and
after, and its output is in `phases/phase-2.md`.

`seal/ledger.md`'s R1 and R6 cite `count` and R6 cites `parse_time`, and this
branch drifted both. Neither was re-stamped by a scoped write, for the reason
above; each was given the value the tool itself computed for the SAME anchor
in this fragment, where R1 and R3 cite them. So no hash in that file was typed
from a hand computation, and the two values are checkable against each other
by reading the two files. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · a stamp carrying no zone is read as UTC at `parse_time`, the one place in this file where a string becomes a `datetime`, so a naive stamp beside an aware one no longer ends the report. The funnel closes **twelve** sites and not the four `plan.md` counted — six subtractions and six orderings — and the ordering half is what the issue's framing missed: a transcript with two calls dies in `load`'s `calls.sort` **before `analyse` is entered at all**, so a guard written at the reported crash site would have left the commoner shape standing. The assumption is stated in the docstring because it is now load-bearing, and what it costs is an absolute time read out of a harness writing local naive stamps — nothing here prints one, since every number this file produces is a difference between two stamps and a difference is right whenever the two share a zone | `skills/verify/scripts/session_cost.py#parse_time@ffbd9c9f`, `skills/verify/scripts/session_cost.py#load@63fa07c7`, `tests/test_session_cost.py#test_a_naive_stamp_does_not_end_the_report@bea2c63a` | **Executed** 2026-09-06 in phase 1, the case seen red first; **re-executed** 2026-09-06 in phase 2 against the module as `seal/ledger.md`'s R6 was stamped against it (`6863669`), through a probe deleted after it ran, because the crash SITE is what phase 1 corrected and a correction taken on trust is the thing this row exists to stop. A one-call naive transcript exits 1 with stdout empty and `TypeError: can't subtract offset-naive and offset-aware datetimes`; a two-call one exits 1 with stdout empty and `TypeError: can't **compare** offset-naive and offset-aware datetimes`, which is the sort and not a subtraction; `--json` on the one-call shape exits 1 with stdout empty, the same as the report. Against the module now, all three exit 0 — 621 bytes on the one-call report and 745 on the two-call one | 2026-09-06 | **The tidy-up this row exists against is dropping the naive row instead**, the way `parse_time`'s own first sentence drops an unparseable one — it is the shorter edit, it reuses the branch already there, and it reads as consistency with the rule the docstring states. A transcript whose stamps are ALL naive then loses every row and reports nothing, where today it reports numbers that are internally consistent (`plan.md` Alternatives, row 2). **The second is normalising at the call sites**: eight guards and no funnel, which is how these two arrived. **The third is trusting the issue about where the crash is.** #175, `spec.md` §Scope and `plan.md` §Technical context all name `analyse`'s subtractions, and the measurement above says a two-call transcript never reaches `analyse`. The count was low on both halves too — six subtractions, not four, and two orderings the plan does not mention at all, as the file stood then. Re-read 2026-09-09 for #200 · #202 · #193, which changed four units this row anchors on; the claim is unaffected and the hash is the new content of the same units. **Re-read again 2026-09-09 for #145**, which added four orderings over `parse_time`'s output — `spawn_cuts`' sort key, its `min` and its running `max`, and `in_windows`' `bisect_right`. Eight sites became twelve, six subtractions and six orderings, counted off this file's own syntax tree rather than by hand. The funnel itself is unaffected: all four take their operands from `parse_time` alone, which is what the row claims. The walk reports a thirteenth and it is a false positive read rather than trusted — `slowest`'s `sorted` orders on a float, and the walk sees the datetime subtraction inside the generator that produces it. |
| R2 · a share of a span is taken through `share`, which returns `—` rather than dividing when the span is **not positive**, so a transcript whose only paired call begins and ends on one timestamp prints the span line, the token block and the family table instead of losing all three behind a `ZeroDivisionError`, and no percentage is invented for a denominator that does not exist. One line under the span says why, because a dash alone poses a question it does not answer — and it is **one line per shape**, since a span of zero and a negative span are not the same reading. The guard on the percentage is on the span being positive rather than non-zero: a negative span — a harness writing a result before the call it answers — is the same undefined division with a sign on it, and one condition covers both there. The SENTENCE is decided separately, in `report`, which is the only place that can tell the two shapes apart. It lives in `report` and not in `analyse` **on purpose**: `--json` never calls `report` and already exits 0 on this shape, so moving it upstream would change a number `--json` emits correctly today | `skills/verify/scripts/session_cost.py#share@cb0959b0`, `skills/verify/scripts/session_cost.py#report@ae4d5c2d`, `tests/test_session_cost.py#test_a_span_of_zero_prints_what_it_can_rather_than_dividing_by_it@e1480933` | **Executed** 2026-09-06 in phase 1, the case seen red first; **re-executed** 2026-09-06 in phase 2 against `6863669` through the same deleted probe. The report exits 1 after **36 bytes** — the span line, then the crash — and `--json` on the identical file exits 0 with 491 bytes, which is the asymmetry that decides where the guard goes. Against the module now the report exits 0 with 703 bytes, carrying `span 0.0m (1 tool calls)`, the sentence, a dash in place of each of the three percentages, the token line and the family table | 2026-09-09 | **The tidy-up this row exists against is moving the guard into `analyse`**, so that `span_s` is never zero and `report` needs no branch at all. `--json` reads `analyse`'s output directly and already emits `span_s == 0.0` correctly; a floor applied there would make it emit a span the transcript does not have, which is why the case asserts the `--json` arm as well as the report. **The second is `0%`, or dropping the column.** A share of a span of zero is not zero and not a hundred — both would be read as measurements — and an omitted column leaves the line looking like an ordinary report with the percentage forgotten. **The third is removing the idle block's own `span_s > 0 and …` conjunct**, which an early draft had and which was dropped before the commit on the grounds that `0 > 0` is already false at a span of zero. True at zero and false at a negative span, where the threshold `span_s * 0.1` is itself negative, so the comparison holds and the report printed sixty-five minutes of idle beside a span of minus thirty — measured in round 2, and reachable through this branch's own normalisation rather than only through a hand-built file. The conjunct is restored, and the idle guard is a THIRD independent test of the span rather than something `share` decides: `share` answers what the percentage is, never whether the line prints. **Round 3 found the same defect twice more, one line further down, and the class was enumerated from the AST rather than patched again**: `report` holds nine gates deciding whether a line prints, three reading the span directly, three reading a duration through a local alias, and three reading counts, which cannot go negative. All three alias gates tested a duration for TRUTH — the repeats line, its trailing identical-time figure, and the `nothing obvious` line — so a negative duration printed an hour of work re-run on a transcript where nothing was, and silenced the line that says nothing was found. The enumeration's own first pass missed exactly those three, because a classifier keyed on `data[...]` subscripts cannot see `exact, same = …`; resolving the function's two aliases is what makes the nine complete. **One deliberate mutation survivor**: reverting the trailing figure's guard to a truthiness test kills no case, because that figure prints only inside the repeats line, which a negative duration now suppresses. Reaching it needs `same` positive while `exact` is negative — constructible, since `stripped` merges commands that differ after a pipe while `exact` does not, so a positive pipe-variant pair can outweigh a negative exact repeat — and it is fixed for consistency with its two siblings rather than because a fixture reaches it. **The fourth is deleting a sentence** as noise beside a self-evident dash — each is text a person reads and acts on, so each is pinned by name. **Round 1's finding 3 corrected the division of labour this row first stated as one place.** It read *`share` is the single place that decides what a non-positive span means*, and `report` had always tested the span independently to choose the explanation line — a different question, because `share` is handed two numbers and cannot tell which shape produced them. The consequence was measured: one sentence covered both shapes, so a negative span was announced as a shared timestamp. `share` still decides the dash for both; `report` now tells zero from negative Re-read 2026-09-09 for #200 · #202 · #193, which changed four units this row anchors on; the claim is unaffected and the hash is the new content of the same units. **Re-read 2026-09-09 for #300**, which changed the NEGATIVE-span sentence's text and so drifted `#report`. Every part of this claim stands: `share` still refuses a non-positive whole, there is still one line per shape, the guard is still on the span being positive, and the sentence is still decided in `report` rather than `analyse`. What moved is the wording — *no call ended after the first call began*, because the span now ends at the last call to end — and how often the branch is reached, which is strictly less often. |
| R3 · the class this work item closes is *every arithmetic operand taken out of a transcript*, and it is enumerated on **two independent axes, each closed mechanically**: an AST walk over the module for the operations that consume a value (56 sites, classified), and the two readers' own field surface for the values that can enter (five fields, each with a funnel). Each axis then decomposes into **the type a value carries** and **the value it carries within that type**, which is where both of this work item's shapes come from and where the third came from. **The member this enumeration found on the value half of the number axis is CLOSED at `count`, and the reasoning that first deferred it was wrong on a measurement rather than on a principle.** `json.loads` accepts the bare tokens `NaN`, `Infinity` and `-Infinity`, all three are `float`, and all three pass an `isinstance` check. The deferral rested on one reading — `output_tokens` as `NaN` prints `nan` and exits 0 — and `output_tokens` is the single usage field that never reaches `token_thirds`, where `round()` raises on a non-finite float. Every other usage field ends the report with exit 1 and stdout empty on both arms. `count` now returns 0 unless `math.isfinite` accepts the value, **and the finiteness question is itself asked inside a `try`** — because `math.isfinite` converts to float before it answers, so an `int` too large to have a float raises `OverflowError` from inside the funnel. That was round 2's 🔴: the round-1 guard broke a shape that had worked (`output_tokens` carrying such an int exited 0 before it) without closing the one it was written for. Returning 0 there also keeps the value away from `token_thirds`' division, which raises on the same int one frame later. **What the funnel now swallows, by construction from its three arms**: a value of the wrong type, a non-finite `float`, and an `int` with no float of its own. Each is scored 0 with nothing printed, so a transcript that genuinely carried a 401-digit count reads as zero rather than as refused — measured at 1282 bytes of report before the guard existed and 762 after. That is the silent direction, and it is the trade every funnel in this file already takes: a missing number over a wrong one, and over a report that ends. **What the funnel cannot answer for at all, which is the limit this row is now careful about**: `count` answers for each value that ENTERS, and nothing there can answer for what the file then MAKES of two of them. Two counts it accepts as finite add to one it would refuse, and `token_thirds` — the one site converting a derived number to an `int` — then ended the report with stdout empty on both arms. Measured at `origin/release/v0.8.3` as well as on this branch, so it is a member the enumeration missed rather than a fix's regression. That one site is closed the same way, by computing the mean inside a guard and charging 0 when it is not finite. **The class is not closed and is not closable by a guard**: enumerating call sites answers which operations exist and never which values reach them, and the 🔴 came through a call the walk had listed since the first widening. It is handed over as issue **#192**. **The wrong-number direction stays open too**: the guard is on finiteness, so a finite but nonsensical count — a negative token total, or one merely implausible — passes and is summed | `skills/verify/scripts/session_cost.py#count@d43c2dd2`, `skills/verify/scripts/session_cost.py#parse_time@ffbd9c9f`, `skills/verify/scripts/session_cost.py#token_thirds@25a4cd22`, `tests/test_session_cost.py#test_a_sum_of_entered_values_does_not_end_the_report@dd8ec919` | **Executed** 2026-09-06 in phase 1: the operator walk returned 56 sites, having returned 42 on its first pass and missed `AugAssign` and `UnaryOp` entirely — the gap was found by re-deriving the list, not by reading harder, which is the argument for the method. A transcript whose `output_tokens` is `NaN` prints `output  nan` and exits 0. **Executed** 2026-09-06 in phase 2, the axis claim itself measured rather than argued: on the module `seal/ledger.md`'s R6 was stamped against, the `timestamp` field written as each of the seven JSON types and then removed — eight variants — exits 0 every time, while the two shapes this work item closed exit 1 on the same module. **Executed** 2026-09-06 in round 1's fix pass, the walk re-derived after the reviewer asked what its node set could not see: **60 sites inside it** — 22 `BinOp`, 10 `AugAssign`, 5 `UnaryOp`, 10 `Compare`, 13 walked calls — and **exactly one outside it**, the `round` in `token_thirds`. The three shapes that reach it were reproduced independently at HEAD before the fix: `input_tokens` as `NaN` and one turn's `cache_read_input_tokens` as `NaN` both raise `ValueError`, `input_tokens` as `Infinity` raises `OverflowError`, each exit 1 with stdout empty on the report and on `--json`, while `output_tokens` as `NaN` exits 0 — which is the whole of why one measurement read as harmless. After the fix all four exit 0 on both arms | 2026-09-06 · re-read 2026-09-08 | **The tidy-up this row exists against is calling a node set complete over a category.** The walk names node KINDS — arithmetic `BinOp`, `AugAssign`, `UnaryOp`, ordering `Compare`, five call names — and phase 1 recorded it as complete over the *operations*. A numeric consumer that carries no operator is none of those, and the one this module has, `round`, is where the 🔴 was. So the row states the node set, which a later session can re-run and widen, instead of a category nobody can check. Its own limits are named for the same reason, and the third of them was **stated wrongly and cost a 🔴**: it read *the walk sees only this module*, which points a next editor outside the file, when the call site is inside the module and it is the call's SHAPE the walk misses. Calls are classified by a bare `Name`, so `math.isfinite(value)` is a `Call` on an `Attribute` and invisible although it sits in `count`. Widened by one line in the classifier — accept a `Call` whose `func` is an `Attribute` on an imported module — the module holds **17** such calls against 13 bare-name ones, and exactly one of the seventeen was unguarded: the `math.isfinite` the previous fix had just added. The other two limits stand as written: a call whose function is computed rather than named is invisible to any by-name walk (0 here today), and a consumer name rebound locally would read as the builtin (none here). **The second is reading `no survivor` as the class being closed.** #170's enumeration ran on the JSON type each field carries and reported no survivor after its fix, which is true on that axis and says nothing about a well-typed value: a naive ISO stamp and an equal pair of stamps are both `str`, the type the field already carries. That is the correction this branch made to `seal/ledger.md`'s R6, and the reason it was a correction and not a removal. **The third is dropping one of the two axes as redundant.** Neither is sufficient alone: the operator walk does not say which operands are transcript-derived, and the field list does not say the `datetime` axis has an ordering half — the half that turned out to name the wrong crash site in three documents. **Re-read 2026-09-08 by work item `1788873620-two-in-range-values-make-one-that-is-not` (#192), which closes the class this row hands over.** The sentence above stays half true and its halves come apart: the class is not closable by a **guard**, and it is closable by a **checker** — `tests/test_a_derived_number_reaching_an_int_carries_a_guard.py` enumerates the int-conversion sites from the module's own tree and decides membership by invoking Python's integer-conversion protocol, so a new unguarded site is refused by the suite rather than by the next round. Two things this re-read moved and nothing else: the `#token_thirds` hash, because that unit's docstring now carries the rule and the shapes it does not reach, with no change to the code; and this cell. The rows are in `seal/ledger/1788873620-two-in-range-values-make-one-that-is-not.md`, and the two widenings that work item declined — a subscript bound, and the wider *raises* predicate that would flag `share` — are its `questions.md` Q1 and Q2. The **wrong-number** direction the last sentence of the clause names is still open, on #192's own body, and was deliberately not widened into Re-read 2026-09-09 for #200 · #202 · #193, which changed four units this row anchors on; the claim is unaffected and the hash is the new content of the same units. |
