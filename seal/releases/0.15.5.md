## 0.15.5 — 2026-09-26

<!-- specs/1790381327-an-automation-run-creates-its-worktrees-without-asking -->
### 1790381327-an-automation-run-creates-its-worktrees-without-asking

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A1 · the person's `automation` answer counts as creation consent only when all four hold: it is in this session's own main transcript (the payload's `transcript_path` when its basename is `<session-id>.jsonl`, otherwise the single `<projects>/*/<session-id>.jsonl`); it is a `user` entry, not marked `isSidechain: true`, whose `tool_result` names an earlier `AskUserQuestion` call by a string `tool_use_id`, read from `toolUseResult`; its single-select question's options have exactly the leading phrases `automation`, `per axis` and `no work item`, and the answer is one of those option labels verbatim with the leading phrase `automation`; and the entry's `cwd` is in the same clone. Every other shape is no consent, and nothing raises: the reader handles the two shapes round 1 found raising, and `consent` answers "" for any exception the read throws | `hooks/worktree_consent.py#automation_answered@d9bf2ddf`, `hooks/worktree_consent.py#transcript_for@ce2dac5b`, `hooks/worktree_consent.py#_routing_preset@85b623f0`, `hooks/worktree_consent.py#leading_phrase@a173a76c`, `tests/test_the_guard_asks_once_per_session.py#test_a_result_not_linked_to_an_ask_is_not_consent@bd487897`, `tests/test_the_guard_asks_once_per_session.py#test_automation_on_another_question_is_not_consent@fd953438`, `tests/test_the_guard_asks_once_per_session.py#test_an_answer_given_in_another_clone_is_not_consent@a81739f1`, `tests/test_the_guard_asks_once_per_session.py#test_an_unreadable_transcript_is_no_consent@16049d13`, `hooks/worktree_consent.py#consent@754e5d51`, `tests/test_the_guard_asks_once_per_session.py#test_a_typed_answer_that_qualifies_the_preset_is_not_consent@ef82fe95`, `tests/test_the_guard_asks_once_per_session.py#test_a_sidechain_entry_is_not_consent@53a6a594`, `tests/test_the_guard_asks_once_per_session.py#test_the_reader_itself_answers_both_shapes_without_raising@7fab5ba6`, `tests/test_the_guard_asks_once_per_session.py#test_a_shape_the_reader_did_not_expect_keeps_the_guards_deny@42a886e8`, `tests/test_the_guard_asks_once_per_session.py#test_consent_fails_closed_on_any_exception@9c0afe67` | **Executed** 2026-09-26. All 17 reader cases seen red against the pre-change module, then eight mutations, one at a time, each turning exactly its case red, the module restored from bytes kept before the first write | 2026-09-26 | Any matching answer counts, not the latest: a later `per axis` does not revoke an earlier `automation`, which is the record's standing too. `multiSelect` must be `False`, not absent. The order of question and result is enforced by the single pass, so it has no mutation of its own. Measured transcript shape: `spec.md` §*Data & interfaces*. Corrected 2026-09-26 after round 1: the row said *answered `automation`* and *nothing raises*, and both were false. A typed `Other` answer such as *automation - but ask me before each worktree* was cut at ` -` and read as the preset, and a list-valued `tool_use_id` or a NUL in `transcript_path` raised, which `hooks/dispatch.py` turned from the guard's deny into no decision. Both are fixed, a sidechain entry is refused as well, and the clause now says what the reader requires. The harness writes an `Other` answer into `answers` verbatim, and a pressed option's answer equals its label: round 1 counted 184 pressed single-select answers matching a label and 16 free-text answers matching none, over 67 local transcripts. The three new cases and the dispatch case were seen red against the unfixed reader, and each repair was then undone alone and turned its own case red. Re-read 2026-09-26 against the fix |
| A2 · the reader's labels are the routing question's: `ROUTING_LABELS` equals the three labels of `skills/implement/orchestration.md`'s Question 1 table, and `PRESET` is the first | `hooks/worktree_consent.py#ROUTING_LABELS@d00258b4`, `skills/implement/orchestration.md#"## Orchestrator: how the work is routed — two questions, one call, one file"@ec41c96f`, `tests/test_the_guard_asks_once_per_session.py#test_the_labels_match_the_routing_question_the_orchestrator_asks@993c2733` | **Executed** 2026-09-26, seen red by changing `per axis` to `per-axis` in the constant | 2026-09-26 | What breaks in six months and turns nothing red is the harness renaming `toolUseResult` or `answers`; this pin catches only the drift the tree can see, the question being relabelled here |
| A3 · row 0 of the Bash creation ladder reads the record or the answer, record first, with the record's effect: `allow` for a command that is worktree creation and nothing else, `silent` for anything more, above every other row including ACTIVE; the `allow` reason names which consent it read. `consented` takes `allow` or `silent` only | `hooks/worktree-guard.py#guard_worktree_creation@7aa6ef85`, `hooks/worktree_consent.py#consent@754e5d51`, `tests/test_the_guard_asks_once_per_session.py#test_the_measured_automation_run_is_not_stopped@20cb14b7`, `tests/test_the_guard_asks_once_per_session.py#test_a_bare_creation_after_the_automation_answer_is_allowed@761adf88`, `tests/test_the_guard_asks_once_per_session.py#test_the_answer_outranks_every_row_below_it@499ffd23`, `tests/test_the_guard_asks_once_per_session.py#test_the_allow_says_which_consent_it_read@47175467` | **Executed** 2026-09-26 through `wg.main()`. Seen red against the phase-1 tree (six of eight cases; the other two pin what must not move and were seen red by mutation), and by reading the record alone, by dropping the path argument, and by giving the answer the record's reason | 2026-09-26 | S1 is the measured run of #604: two `git worktree add` lines and a `git switch -c … # [worktree-ok]` in one call, raw stdout empty. Re-read 2026-09-26 after round 1's fixes: `consent` now catches any exception from the read and answers "", and the function's only other change is the `[worktree-ok]` row's text, which reaches no consent verdict |
| A4 · the switch direction reads neither consent: with the answer present, a switch over an ACTIVE session still denies | `hooks/worktree-guard.py#main@e59a08e1`, `tests/test_the_guard_asks_once_per_session.py#test_the_answer_does_not_reach_the_switch_direction@a742d3fd` | **Executed** 2026-09-26, seen red by making the switch ladder exit on consent | 2026-09-26 | A switch written after a creation in one command is never judged when consent is present; that predates this work and is #620 |
| A5 · the Agent/Task `isolation: "worktree"` path counts no sessions and reaches no choice site: silent under consent, otherwise one `ask` whose reason says the agent runs beside this session and names neither *single-stream* nor calling the Agent again without isolation | `hooks/worktree-guard.py#main@e59a08e1`, `tests/test_worktree_guard.py#test_an_isolated_agent_asks_without_counting_sessions@d32c27a0`, `tests/test_worktree_guard.py#test_the_two_reworded_reasons_are_pinned_in_both_languages@6e5b2ee1`, `tests/test_the_guard_asks_once_per_session.py#test_the_agent_path_is_silent_after_the_automation_answer@0909fb77` | **Executed** 2026-09-26 over four tree states, `sessions_in_tree` replaced by a recorder that must stay empty. Seen red against the phase-2 tree, by calling `sessions_in_tree` again, and by ignoring consent | 2026-09-26 | #8. #237's floor is unmoved: the first isolated agent of a session without consent is still one confirmation. Re-read 2026-09-26 after round 1: the S10 case's docstring changed and its assertions did not |

<!-- specs/1790381328-malformed-is-graded-like-drifted-and-reads-prose-as-prose -->
### 1790381328-malformed-is-graded-like-drifted-and-reads-prose-as-prose

| S1–S3 · `MALFORMED` is graded like `DRIFTED`: `exit_code` returns 1 for it on a lenient run and 2 under `--strict`, below `BROKEN or refused`, so a tree holding a `BROKEN` row or a refused record beside it is still 2. `OLD-FORMAT` stays 2 under both readings | `skills/evidence-check/scripts/evidence_check.py#exit_code@8c93a803`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_the_grading_is_one_function_and_the_line_reads_its_answer@838cf2a6`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_a_malformed_row_is_told_what_the_gate_would_say@d01f3066`, `tests/test_a_row_points_by_content.py#test_a_coordinate_that_will_not_parse_is_malformed_under_both_readings@04a7f48b` | **Executed** 2026-09-26: the moved pins red against the phase-1 checker (14 failed across the two modules); mutants one at a time: `MALFORMED` above `BROKEN` turns the grading case red, `MALFORMED` back at exit 2 turns twelve red | 2026-09-26 | The repository owner's answer (b) to #606's Q1, 2026-09-26. One condition with drift rather than a branch of its own, because the owner has moved it and nothing is left to reorder |
| S4 · the lenient notice is one line naming `--strict` alone, and says `exit 2`, `broad-gate`, `NOT SEALED`, `DRIFTED` and `MALFORMED`: the two verdict words are how a reader tells *re-read* from *fix the coordinate* on exit 1 | `skills/evidence-check/scripts/evidence_check.py#LENIENT_NOTICE@28c2ab70`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_the_checker_states_the_sentence_once@01071fc9`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_the_notice_borrows_the_word_the_failing_gate_prints@74d59073` | **Executed** 2026-09-26: the old sentence restored turns five cases red, the literal and the two verdict words among them | 2026-09-26 | One sentence rather than one per cause: choosing between two would restate the grading beside `exit_code`, which the notice's comment refuses |
| S6 · `SKILL.md`'s `MALFORMED` verdict row and the reader table's `MALFORMED is` column state the grading `exit_code` returns, and the `DRIFTED` row no longer calls itself the only verdict graded differently | `skills/evidence-check/SKILL.md#"## Verdicts and what to do"@76fb68c1`, `skills/evidence-check/SKILL.md#"## Run / ### Which reader graded your tree"@d4c61209`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_the_skill_states_the_grading_exit_code_returns_for_malformed@a901469e` | **Executed** 2026-09-26: `SKILL.md` as it stood at 47e32d57, its verdict row alone and its `DRIFTED` row alone each turn the case red | 2026-09-26 | The case reads the grading from `exit_code`, not from a number written in the test, so the next change to the grading moves the page or goes red |
| S7 · the CI `ledger` job's one warning names drift and a malformed coordinate, because it prints on exit 1 and reads no text | `.github/workflows/test.yml#ledger@2d4b4be3`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_the_ci_warning_names_every_verdict_the_lenient_reading_softens@d0f354f7` | **Executed** 2026-09-26: `test.yml` as it stood at 47e32d57 turns the case red | 2026-09-26 | The old warning sent a reader holding a malformed row to `--reverify`, which names the row and writes nothing |
| `evidence-ci`'s lenient recipe, `\|\| [ $? -eq 1 ]` without `--strict`, lets a malformed coordinate through with the rest of exit 1, and `--strict` still refuses it; its step 4, the vendored template's step comment, `SKILL.md`'s `--strict` row and the checker's module docstring say so | `skills/evidence-ci/SKILL.md#"## Procedure"@2fb96671`, `templates/evidence-check.yml#"python3 tools/evidence_check.py --strict ."@b9e2d70d`, `tests/test_evidence_check.py#test_letting_drift_warn_takes_both_halves@028af5ca`, `tests/test_the_lenient_run_says_what_the_broad_gate_will_say.py#test_every_other_page_that_grades_the_flag_names_malformed@0f7211ff` | **Executed** 2026-09-26: the step over a malformed row exits 0 without `--strict` and 1 with it; against the phase-1 checker the first assertion is red (`assert 1 == 0`). The four sentences are held against `exit_code` by a case that goes red with each document restored alone: `SKILL.md` and `evidence-ci`'s `SKILL.md` and the template as they stood at 47e32d57, and the docstring's exit line at its old wording | 2026-09-26 | `OLD-FORMAT` is not softened by the recipe. **Corrected 2026-09-26 by round 1's fix pass of work item 1790381328 (#614):** the claim named step 4 and the template only, and the build's record said the template comment was read only while `overview.md` called step 4 pinned; the recipe's behaviour was pinned and no sentence was (round 1's 🟡 2). The claim now names the four sentences and the case that holds them **Re-read 2026-09-26 by round 2's fix pass of work item 1790381328 (#614):** the case now reads the flag row's lenient exit and the template's softening from `exit_code` too (round 2's 🟡 2); with the grading put back to (a) and step 4 and the docstring moved to match, it goes red on each of the two new assertions alone, where it stayed green before. The claim holds |
| S8–S12 · rule (a) reads prose as prose by five rules, and every cell whose verdict moved against 0.15.4 over a generated shape space falls under one of them: a path followed by `@` takes a hash only of six or more hex characters (`chart.js@4` and `src/a.py@abc` are silent); the per-word rule reads a locator opening with digits no ASCII letter, digit or `_` continues as an issue number, whatever follows the digits (`org/repo#299's` and `docs/a.md#1-scope` are silent, `docs/a.md#1장@abcdef12`, glued, is named); both marks count only where an `@` is glued after a `#`, whitespace allowed only inside a quoted string within a code span and no `"` left unclosed, and otherwise each word is judged alone (`@lru_cache  # memoized`, `docs/a.md#1-scope @abcdef12` and `#handler>"a"b"@abcdef12` are silent, `src/a.py#handler @abcdef12` is named, `` `#handler>"a b"@abcdef12` `` in a span is named and the same text out of one is silent); a file name with no dot takes a locator opening with a letter, `_`, `"` or `<`, never a digit (`Makefile#"all: build"`, `C#"hello"` and `vector#<T>` are named, `Makefile#1x` is not); and a locator opening with a digit that is not a decimal digit is not an issue number (`docs/a.md#²` is named). The search stops each attempt at the next `#` outside a quote, so a run of them is linear; a chain of quoted strings holding escaped quotes is still quadratic | `skills/evidence-check/scripts/evidence_check.py#refused_coordinate@2b6a541e`, `skills/evidence-check/scripts/evidence_check.py#PATH_HASH_RE@f61114b5`, `skills/evidence-check/scripts/evidence_check.py#ISSUE_TAIL_RE@93f5573d`, `skills/evidence-check/scripts/evidence_check.py#GLUED_MARKS_RE@ef4349df`, `tests/test_a_row_points_by_content.py#test_a_file_name_with_no_dot_takes_any_locator@beaf743a`, `tests/test_a_row_points_by_content.py#test_an_issue_number_or_a_version_glued_to_a_word_is_prose@fc399667`, `tests/test_a_row_points_by_content.py#test_a_decorated_line_holding_both_marks_apart_is_prose@edca0e51`, `tests/test_a_row_points_by_content.py#test_a_coordinate_the_opener_list_misses_is_named@ee53dbf9`, `tests/test_a_row_points_by_content.py#test_what_rule_a_gives_up_is_silent_and_says_so@1d41be6c`, `tests/test_a_row_points_by_content.py#test_what_the_dotless_openers_take_up_is_named_and_says_so@ba06f247`, `tests/test_a_row_points_by_content.py#test_a_glued_mark_attempt_stops_at_the_next_hash@0545c36a` | **Executed** 2026-09-26: 13 new parameters red at 47e32d57's checker and green after; ten mutants of the added units, one at a time, each red (`PATH_HASH_RE`'s bound, `ISSUE_TAIL_RE` twice, each opener, glued for both marks, the quoted alternative, the escape, `.match` for `.search`) | 2026-09-26 | The five rules are stated in `refused_coordinate`'s docstring, `spec.md` and `changelog.md`; a case holds one or two examples of each against the docstring and the code, in both directions. **Corrected 2026-09-26 by round 1's fix pass of work item 1790381328 (#614):** the claim did not say the quoted string holds whitespace only in a code span (⬜ 3), the list named four of the eight silences and none of the two new namings (🟡 1, ⬜ 4), and the search was quadratic in a span of many `#` (⬜ 5, 2.7 s at 20 000 characters, 0.0007 s after). Each new case red before its fix: the four new silences and two namings against the old docstring, the stop-at-`#` case against the old pattern (`'###@' == '#@'`) **Corrected 2026-09-26 by round 2's fix pass of work item 1790381328 (#614):** round 2 found the next family the example list missed, a path and a digit-first locator with the hash after whitespace (`docs/a.md#1-scope @abcdef12`, 🟡 1), and two namings it left out (`docs/a.md#²`, `docs/a.md#①`, ⬜ 4); and *linear* held only for a `#` outside quotes (⬜ 3, `#"\"` repeated takes 1.5 s at 20 000 characters, narrowed rather than changed). So the list is built by construction: a throwaway probe generated 2 426 shapes (ten heads, thirty locators, eight hash endings, twenty-six prose and edge shapes), read each in a code span and as bare words beside a good anchor through `malformed_rows` at 47e32d57 and at this fix, and found 484 moved cells of 4 852, 286 named then silent and 198 silent then named. All 484 fall under the five rules (a short hash 8, a digit-first locator 135, marks not glued 43, both of those 100, a dotless opener 168, a non-decimal digit 30), each rule's converse holds over every cell it describes, and no cell is left over. The examples of the three new ones were red against the old docstring |

<!-- specs/1790381329-the-deferred-sentences-and-pins -->
### 1790381329-the-deferred-sentences-and-pins

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · `seal.py` copied without `hooks/config.py` and `hooks/optin.py` beside the plugin layout exits 2 before its `import` lines, with one stderr sentence per missing file naming its path and what it is for (`HOOK_PURPOSES`: `config.py` reads the root's `config.md`, and `optin.py` finds the `seal/` root), and no traceback; its module docstring's exit-code line names 2 | `skills/implement/scripts/seal.py#refuse_without_hooks@f95f693a`, `skills/implement/scripts/seal.py#HOOK_PURPOSES@9d122d4a`, `tests/test_a_script_copied_alone_exits_2.py#CASES@30891988` | **Executed** 2026-09-26: the `seal.py` row of the class case red at `09034979` (exit 1, `ModuleNotFoundError: No module named 'config'` traceback), green after. Mutated one at a time, each red: the missing-file test forced false, the exit forced to 1, the `optin.py` purpose reworded | 2026-09-26 | #610. `mode --check` is the invocation because the load is at import and argparse's own usage error is also exit 2 (`questions.md` Q4). `seal.py` is the `sys.path.insert` + `import` shape, which a search for by-path loaders does not find **Corrected 2026-09-26 in work item 1790381329, round 1's fix pass (🟡 1):** `HOOK_PURPOSES` said `hooks/config.py` reads and writes `config.md`, and it only reads it; a second `seal.py` row now asserts *it is what reads the root's config.md* and that *reads and writes* is absent, red against the old sentence. The module docstring's exit-code line now names argparse's usage error and the conflicting-flags refusal as 2 too (⬜ 6) |
| C2 · `payload_meter.py --calibrate <transcript>` copied without `session_cost.py` exits 2 from `_session_cost`, before the transcript is opened, with one stderr sentence naming the path and that it reads a transcript's spawns, and no traceback; the module docstring lists 2 for the interpreter floor, the missing sibling and argparse's usage error | `skills/verify/scripts/payload_meter.py#_session_cost@ec73910e`, `tests/test_a_script_copied_alone_exits_2.py#CASES@30891988` | **Executed** 2026-09-26: the `payload_meter.py` row of the class case red at `09034979` (exit 1, traceback), green after. Mutated, each red: the file check forced false, the exit forced to 1 | 2026-09-26 | #610. 2 and not 1, because the meter's 1 is *this input could not be measured* and its 2 already meant *nothing ran* (the floor) **Re-read 2026-09-26 in work item 1790381329, round 1's fix pass:** `CASES` gained the second `seal.py` row (C1) and its comment names both purpose corrections; the `payload_meter.py` row is unchanged. The module docstring's exit-code line now names argparse's usage error as 2 too (⬜ 6). `session_cost.py`'s purpose was checked against the file: `payload_meter.py` uses its `DELEGATING`, `spawn_labels`, `count` and `subagent_transcripts`, which name what a delegating call spawned and find the subagent transcripts, so *reads a transcript's spawns and finds their subagent transcripts* holds |
| C3 · every document that places `unverified-check --baseline`'s comparison says what it reads is `git merge-base <ref> HEAD`, the fork point on a branch checkout and the base's tip in CI, whose checkout of a pull request is the head already merged into the base: both hygiene workflows' comments, `unverified_check.py`'s module docstring, `merge_base`'s docstring and `--baseline` help, `skills/verify/SKILL.md`, both README editions and both editions of `docs/one-root-by-lifetime`; neither of the latter still says CI never compares at the tip | `tests/test_unverified_rows_close.py#test_the_documents_state_the_merge_base_footing@e6d6153d`, `skills/verify/scripts/unverified_check.py#merge_base@c2132f1e`, `skills/verify/scripts/unverified_check.py#main@427d71f1`, `tests/test_unverified_rows_close.py#test_the_baseline_help_names_both_places@d07e75c6` | **Executed** 2026-09-26: the extended case red for all eight files it holds to the tip, before the prose; after it, green. The false-sentence assertion red with *never the base branch's moving tip* put back into `docs/one-root-by-lifetime.md`, and again with *base 브랜치의 현재 끝이 아니라* put back into the Korean edition | 2026-09-26 | #612. `docs/release-checklist.md` stays in the case for the merge base and is not held to the tip: its sentence is true at both places and names neither (spec.md *Out of scope*). `folded_items`' docstring now says *the merge base* for the same reason **Corrected 2026-09-26 in work item 1790381329, round 1's fix pass (🟡 2):** the documents case reads `unverified_check.py` whole, where the tip phrase stands three times, so it could not see the `--baseline` help alone revert. `test_the_baseline_help_names_both_places` renders `--help` and reads the help line: green as built, red with the tip dropped from the help alone while the documents case stayed green |
| C4 · the survivor sweep's id rule keeps a removed ledger row measured when its id stands under the same heading at the tip at least as many times as at the left end, so a row corrected in place and split into two rows under its id, its anchor renamed, stays measured; the module docstring, `removed_ledger_rows`' docstring and `docs/review-chain-spec.md` state one silent set: the id does not name the row AND no live row cites every anchor of it that still resolves | `skills/code-review/scripts/survivor_check.py#removed_ledger_rows@8f9647b0`, `skills/code-review/scripts/survivor_check.py#ROW_ID@710de8eb`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_claim_split_into_two_rows_as_it_is_corrected_stays_measured@7c2727aa` | **Executed** 2026-09-26: the new case green with the guard as built (`>=`) and red with it read as equality (`==`), exit 0 and no place reported; the file restored from a copy. The no-id figure re-counted at `a31ad5bf` by a deleted probe through `survivor_check`'s own loaders: 289 of 833 anchored live rows (34.7%) carry no id `ROW_ID` reads | 2026-09-26 | #615. The round measured 280 of 800 at its own tip; the 33 more rows are 0.15.4's fold (32 anchored rows) and this work item's fragment (3). S4 — a row whose id lost its sibling and whose every anchor was renamed — is silent and is not pinned as silent, because a case asserting exit 0 would lock a known gap in (spec.md) |
| C5 · `broad_gate.py#as_cmd_expands` asks the environment before it computes `%CD%` and `%__CD__%`, so a defined `CD` naming no directory leaves `%CD%/bin/test -q` as written, and it leaves a substring or substitution (`%NAME:~0,2%`) as written although the variable names the tree; its docstring, `handed_to_shell`'s and `templates/config.md` §*Broad gate* claim no equality with `cmd.exe` beyond the plain `%NAME%` form, and none says `CD` is in no environment | `skills/verify/scripts/broad_gate.py#as_cmd_expands@a1d0f486`, `skills/verify/scripts/broad_gate.py#handed_to_shell@75af923b`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_name_rooted_in_a_variable_is_judged_where_the_variable_points@f49f4f0d`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_template_says_which_positions_are_rewritten@f0db6d56` | **Executed** 2026-09-26: the defined-`CD` assertion red with `value`'s environment branch moved after the two computed ones (`%CD%\bin\test -q` handed over); the substring row red with the name cut at its `:` before the lookup; the reader case red with the old config sentence restored; each file restored from a copy. `as_cmd_expands` run directly: `%X:~0,200%` and `%X:a=b%` come back as written, and `%UNDEF%Y%CD%` resumes after the undefined name's closing `%` | 2026-09-26 | #616. Where `cmd.exe` itself resumes scanning after an undefined name is not verified: only a Windows run answers it (`questions.md` Q2, `overview.md` *Not verified*), and the docstring claims neither way **Re-read 2026-09-26 in work item 1790381329, round 1's fix pass (⬜ 4):** the variable case's docstring no longer says the part is *expanded the same way*; it says it is expanded as far as `as_cmd_expands` models it. Its assertions are unchanged |
