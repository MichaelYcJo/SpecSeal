## 0.10.0 — 2026-09-10

<!-- specs/1788993115-a-payload-is-written-again-on-every-spawn -->
### 1788993115-a-payload-is-written-again-on-every-spawn

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S4 / S12 · first setup asks one `AskUserQuestion` — the MODE question within it offering two options, shared first as the default; each says what it creates and installs; the path is spelled through `git rev-parse --git-common-dir`; a repository with `seal/` at either place is not asked; the parity question follows; the "nowhere else" sentence is gone | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `tests/test_first_setup_asks_once.py#test_the_two_options_are_named_in_order_with_shared_as_the_default@518663da`, `tests/test_first_setup_asks_once.py#test_each_option_says_what_it_creates_and_what_it_installs@8b6bebf9`, `tests/test_first_setup_asks_once.py#test_the_root_is_spelled_through_the_common_dir_never_the_literal@e161fbb3`, `tests/test_first_setup_asks_once.py#test_a_repository_with_the_root_at_either_place_is_never_asked@6abb28a0`, `tests/test_first_setup_asks_once.py#test_the_parity_question_follows_the_mode_question@9d887343`, `tests/test_first_setup_asks_once.py#test_the_sentence_that_said_nowhere_else_is_gone@975b6084` | **Executed**: the six red at `7e44106` (30 failed, 1 passed for the whole file), green at `d962cf8`. **Executed** by phase 3: `git diff 0b2697c..a497e03 -- hooks/` adds no line naming `AskUserQuestion` — no hook asks anything new | 2026-09-10 | Prompt budget: one question, once per repository, in the batch the skill already asks  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). This claim's own text is untouched by that bullet, so only the section hash moved  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. Two of the six cases now read the bootstrap out of `orchestration.md` (`test_the_root_is_spelled_through_the_common_dir_never_the_literal`) or both halves (`test_the_sentence_that_said_nowhere_else_is_gone`), which is what moved their hashes; each was seen red against the file it left (phase 2's record). The claim holds  **Re-read 2026-09-15** by work item 1789445605 (#401), which added the `Broad gate` row as a SECOND question inside the same `AskUserQuestion`. The call count is unchanged, which is what the prompt-budget note is about; the claim's wording said *two options* of the call rather than of the mode question, and is corrected above  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**. This branch moved the Bootstrap section's hash twice — phase 4 rewrote the paragraph on offering a candidate that carries a pipe, and round 1's fix pass put the condition into what a bare one costs — and the second re-stamp went in with the hash alone, so this row carried a hash of text nobody had read against the claim. Read now: both edits sit inside the second question's paragraphs, which this claim does not reach. The mode question's two options and their order, what each says it creates and installs, the common-dir spelling, the parity question that follows and the absence of the *nowhere else* sentence are all where they were, and the six cases are green. The claim holds |
| Q7 · a repository still on the 0.3.x layout is told, not asked: the session-start hook moves it into `<repo>/seal/` | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `tests/test_first_setup_asks_once.py#test_an_unmoved_old_layout_is_not_asked_but_told@b8d038c3` | **Executed**: red at `7e44106`, green at `d962cf8` | 2026-09-10 | A repository that committed `.specseal/` chose shared; a local root created beside it would be moved into the tree by the hook anyway  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). This claim's own text is untouched by that bullet, so only the section hash moved  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. The claim holds  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**, which found this row re-stamped inside round 1's fix range with the hash alone. The branch moved the section's hash twice, in phase 4 and in round 1's fix pass, both times inside the numbered list; the 0.3.x paragraph this claim is about sits above that list and neither edit reaches it. It still says the session-start hook moves such a repository into `<repo>/seal/`, and `test_an_unmoved_old_layout_is_not_asked_but_told` is green. The claim holds |
| S5 / 🟡 4 · the bootstrap reads the version from `$CLAUDE_PLUGIN_ROOT/.claude-plugin/plugin.json` with a one-line command; the READMEs name `$CLAUDE_PLUGIN_ROOT/templates/hygiene.yml` | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `tests/test_first_setup_asks_once.py#test_the_version_is_read_from_the_plugin_root_and_not_from_the_tree@9949c6e1` | **Executed**: the test red at 2078984, green at f53d615; the one-liner run with `CLAUDE_PLUGIN_ROOT` set to this checkout prints `v0.4.0` | 2026-09-10 | The spelling `skills/update/SKILL.md` already used  **Re-read 2026-09-03** by work item 1788360817, which rewrote one bullet of the Bootstrap section (`seal/ledger.md` is written from `templates/ledger.md` and its clause tables arrive empty, where it used to say 'created empty'; round 3 finding 4 corrected the second half of that same bullet). This claim's own text is untouched by that bullet, so only the section hash moved  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. The claim holds  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**, which found this row re-stamped inside round 1's fix range with the hash alone. The version one-liner and the sentence naming `$CLAUDE_PLUGIN_ROOT/.claude-plugin/plugin.json` are in the shared option's bullet, above both of the edits this branch made to the section; `README.md` and `README.ko.md` still name `$CLAUDE_PLUGIN_ROOT/templates/hygiene.yml`, and `test_the_version_is_read_from_the_plugin_root_and_not_from_the_tree` is green. The claim holds |
| 🟡 1 / 🟡 4 / r3 1 · the path of every TRACKED file under `templates/`, at any depth and tracked dotfiles included, occurs somewhere in the shipped prose documents. What runs is a substring test over `git ls-files templates` against a `.md`-only corpus — not a judgment that the document naming it tells anybody to start from it | `skills/commit-pr-convention/SKILL.md#"## The language is the repository's, and it says so in a file"@a76da269`, `skills/implement/SKILL.md#"### 3. The SDD file set"@0d7a9b38`, `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `skills/code-review/SKILL.md#"## Cross-session records — `seal/specs/<work-item-id>/`"@b57d3739`, `tests/test_the_pull_request_language_is_the_repositorys.py#shipped_templates@12c49cdb`, `tests/test_the_pull_request_language_is_the_repositorys.py#unreachable_templates@c2c2c08d`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_every_template_is_named_by_a_document_that_ships@9b339354`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_templates_check_reads_prose_only_and_descends@e4cc13db` | **Executed**: four unreachable at `1280de9` and none at the working tree. Round 2 🟡 4 narrowed the corpus to `.md` and found two more — `sdd-round.md` was passing on four comments in `chain_check.py` and `ledger.md` on a Korean style example where the file is the specimen rather than the subject — closed by naming each where a session already reads: the round record's fields in `code-review`, and the bootstrap line that used to say `seal/ledger.md` is "created empty". Round 3 finding 1: the listing came from a working-tree glob while the corpus came from `git ls-files`, so an untracked `templates/.DS_Store` — gitignored, and one arrives from opening the folder in Finder — was reported as an unreachable template. Reproduced in a scratch repository: the glob returns it and `git status --porcelain` says nothing about it; `git ls-files` does not | 2026-09-11 | **What this check is worth, stated rather than implied.** It proves a path APPEARS in prose, and round 3's census found six of the twelve templates resting on a single sentence each — all six honest today, and nothing in the check could tell if one stopped being. That is the judgment the clause above deliberately no longer claims. Round 1's clause said a session "has something to copy", which a corpus holding Python had already made false for two templates. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**, which rewrote the review skill's paragraph on where a record starts: `round_record.py new` writes it in the template's shape. `templates/sdd-round.md` is still named there, which is what the substring test this row is about reads. **Re-read 2026-09-07 in work item 1788735085's phase 3** (#98), which corrected a comment inside `shipped_templates` and a docstring inside the descends case. Both are prose: the listing's arguments and every assertion are untouched, all 117 cases in the file pass, and this row's claim is unaffected — only the two hashes moved  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. This row cites four documents and one of them moved; the substring test it is about reads every `.md` in the tree, and `orchestration.md` is one, still naming `templates/ledger.md`. The claim holds. **Re-read 2026-09-11 in work item 1789081272's phase 3** (#84): §3's file table gained a `Written by` column and its `plan.md` row a sentence about the approval line, and the paragraph below it now says `Starts from` by name because the middle of a five-column table is a different column. Every `templates/sdd-*.md` path the table carried is still in it — the column that names them was not touched — so the substring test this row is about reads the same paths from the same cells. Only the hash moved  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**, which found this row re-stamped inside round 1's fix range with the hash alone. The section is cited here for naming template paths in prose, and both of this branch's edits to it name `templates/config.md` exactly as the text did before; the check still reads `git ls-files templates` against a `.md`-only corpus, and both of its cases are green. The claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which changed one phrase of this section's files table about the `Broad gate` cell; the claim is unchanged |
| r3 4 · the bootstrap bullet says the ledger's CLAUSE TABLES arrive empty, which is what the section's fourth bullet already means | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b` | **Read**: `templates/ledger.md` ships an `\| Item \| Value \|` table with two filled rows and placeholder rows in two more, and the live `seal/ledger.md` carries the first of them. "No rows" was false about the file it names | 2026-09-10 | Round 2 replaced "created empty" — which was true of the old state and said nothing about the source — with a sentence that named the source and was wrong about it. The meaning a session needs is that no evidence rows arrive, and the fourth bullet says exactly that  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. The bullet this row is about is the section's first; the one phase 4 reworded is its fourth. The claim holds  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**, which found this row re-stamped inside round 1's fix range with the hash alone. Both of this branch's edits to the section are inside the numbered list below the bullets: the first bullet still says the clause tables arrive empty, and the fourth still says evidence rows are left empty, which is what this row says they mean. The claim holds |
| S7 · the bootstrap names this skill as the way back to the questions it asks once | `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `tests/test_the_settings_have_a_front_door.py#test_the_bootstrap_names_the_way_back_to_its_own_questions@ca656c5c` | **Executed** | 2026-09-10 | A person answering a batch has no reason to expect a way back to it  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. The claim holds  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**, which found this row re-stamped inside round 1's fix range with the hash alone. The closing paragraph naming `/specseal:config` as the way back to the questions sits below the numbered list, and both of this branch's edits to the section are inside that list; `test_the_bootstrap_names_the_way_back_to_its_own_questions` is green. The claim holds |
| S11, S12 · the preset block names the mode question before it instructs the write that creates the root, and the bootstrap records the answer it collects | `CLAUDE.md#"## Git">"Routing, decided at the start"@e62a0723`, `skills/implement/orchestration.md#"## Orchestrator: Bootstrap — create what's missing"@20350f3b`, `tests/test_first_setup_asks_once.py#test_the_preset_sends_a_fresh_repository_to_the_bootstrap@5ee2da45`, `tests/test_first_setup_asks_once.py#test_what_the_installer_copies_carries_it@b29a5f04`, `tests/test_first_setup_asks_once.py#test_the_bootstrap_records_the_answer_it_was_given@1964079b` | **Executed** 2026-09-08. Five of six cases seen red against the documents as they stood at `589cf25`; the sixth reads the gate, which the previous phase had already landed, and it was red before that phase. The `seal mode` instruction was executed against scratch repositories in both modes: bare `seal mode` writes `\| Mode \| shared \|` / `\| Mode \| local \|` from where the folder is and moves nothing, and `seal mode <the same mode>` also writes the row while printing the whole switch narrative | 2026-09-10 | **The extraction is the installer's.** The cases read the span between `<!-- specseal:start -->` and `<!-- specseal:end -->` rather than the whole file, because a sentence below the end marker reaches this repository and reaches no user, and a whole-file read cannot tell the two apart. **The condition leads the rule.** #151's own reading of this fix is that a conditional appended to a block meant to be read straight through only helps a session that notices it applies — so the assertion is about what sits BEFORE the instruction, not about the sentence being present. **The bootstrap has to record, or the gate asks the person it just asked.** That is why the skill's *do not write that config row here* is gone rather than merely widened  **Restated 2026-09-10 by work item 1788993115 (#292)**, which moved the Bootstrap section to `skills/implement/orchestration.md` with its text unchanged: the anchor went with the section, so this row left `seal/ledger.md` and its coordinate is the new file's. The preset block's own sentence now sends a fresh repository to *the Bootstrap section of `skills/implement/orchestration.md`* (phase 3, which is why `CLAUDE.md#"## Git"` moved), and `test_the_bootstrap_records_the_answer_it_was_given` reads both halves (phase 2). The claim holds  **Re-read 2026-09-17 in round 2's fix pass of work item 1789598366 (#415)**, which found this row re-stamped inside round 1's fix range with the hash alone. `CLAUDE.md` is untouched by this branch, so the preset half of this claim cannot have moved, and the paragraph telling the bootstrap to record the answer by running `seal mode` with no argument is where it was — both of this branch's edits to the section are the same paragraph one screen above it, the one saying how a candidate carrying a pipe is offered. The three cases are green. The claim holds |

<!-- specs/1789002694-two-agents-are-forbidden-the-seal-and-nobody-is-assigned-it -->
### 1789002694-two-agents-are-forbidden-the-seal-and-nobody-is-assigned-it

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Ten rows over a branch that adds an agent, two commands and a subcommand.
S10 and S11 arrived with phase 5 and the enumeration below runs to S8; the
last three are the capped run, the rule that one seal is final, and the
subcommand's own refusals.

**S8 is gone, and work item 1789034970 (#120) is what removed it.** Its claim
was that `agents/sealer.md` names its one write as §6's own exception and
states that §2 as it stands forbids its one act — and its second coordinate
was the section written to be deleted, `## §2 as it stands, and #120`. #120
deleted that section, rewrote §6 so that a named write is the rule rather than
an exception, and renamed both cases the row cited. A row whose anchor a
change removes is REMOVED rather than re-pointed, so the successor claim is a
new row in that work item's own fragment,
`seal/ledger/1789034970-the-contract-is-settled-against-the-agents-that-exist.md`.
The enumeration above still names S8 because it is a record of what the branch
judged, and that judgment happened.
What earns a row here is a judgment whose repair looks obvious from the
outside, and a gate that draws a picture has an unusual number of them: a
sampling offset that reads as an off-by-one nobody would defend (S2), a
question asked before a call rather than after it, which reads as the wrong
order (S3), a refusal where a default would be friendlier (S4), a comparison
that runs late rather than always (S5), a second subcommand where one exists
(S6), a draft judgment inside a command that is about a ready pull request
(S7), and a paragraph written to be deleted (S8).

S1 is not one of those. It is the claim R4 of `seal/ledger.md` used to carry a
coordinate for: this work item renamed the case that reads the sentence, and a
renamed unit is a REMOVED anchor rather than a drifted one, so the claim moves
here rather than being re-pointed there.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form. `seal/ledger.md`'s eighteen rows that this
branch drifted were re-read one by one and re-stamped by an UNSCOPED
`--reverify`, which the scoped form cannot reach — and the measurement that
made it safe is in `phases/phase-4.md`: the base commit reads 1055 ok · 0
drifted, so there was no row on that file another session would have to judge.
The unscoped READ ran afterwards and is quoted in the same record. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · `CONTRIBUTING.md` §*Running the checks* says the full run is the **sealer's**, run once after the review rounds settle, and names both the section that forbids it to a segment and the definition that assigns it — and the case that reads the sentence is named for what the sentence says | `` `CONTRIBUTING.md#"## Running the checks"@048eae4c` ``, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_section_says_the_full_run_is_the_sealers@423fdfbd` | Read 2026-09-10 in phase 4, against the section as phase 3 left it; **executed** 2026-09-10, the module green after the rename with the case reading the same two words it read before | 2026-09-25 | **This row exists because a rename is not a re-point.** `seal/ledger.md`'s R4 cited this case under its old name, `test_the_section_says_the_full_run_is_the_orchestrators`, and #30 made that name assert the opposite of what the case checks. Changing a unit's BODY drifts an anchor and changing its NAME removes one, so R4 lost the coordinate and the claim was written here — which is the rule `CLAUDE.md` and `templates/ledger.md` both state and which has, until now, had no worked instance in this repository. **The tidy-up is re-pointing R4 at the new name and deleting this row.** It reads tidier and it is the thing the rule forbids: a row's claim is bound to the code it cited, and a coordinate that resolves is not the test — R4's own claim was ALSO stale (it said the run was the orchestrator's), so a re-point would have carried a false claim onto a true coordinate. Both halves were repaired in the same pass, and only one of them stayed in that file  **Re-read 2026-09-13 in work item 1789296100 (#354).** The sentence is unchanged and still names both the section that forbids the full run to a segment and the definition it is assigned to. **Re-read again 2026-09-13 in round 1's fix pass**, which reworded the paragraph above it and left *Name a module* untouched. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** the `about five minutes` comment became *in parallel — the sealer's run* and a paragraph on the `-n auto` default joined the block (#337); `bin/test` is still named first, the floor still stated once, the fallback still labelled. docstring wording only — *a five-minute suite* became *the whole suite* (#337); the assertions are unchanged. The claim stands. **Re-read 2026-09-25 by work item 1790260567 (#510), phase 3**, which added one paragraph to `## Running the checks` saying the suite runs with `gh` logged out and why. The *Name a module* paragraph this claim reads is untouched; the claim holds. **First read 2026-09-10**, the date this row's `Checked` cell carried before work item 1790260567 reduced it to one reading. **Re-read again 2026-09-25 in round 1's fix pass of work item 1790260567**, which changed the new paragraph to say `GH_TOKEN` and `GH_ENTERPRISE_TOKEN` are set to a placeholder rather than removed. The *Name a module* paragraph is untouched; the claim holds. Re-read 2026-09-25 at the merge of release/v0.15.3 into #448's branch: the release side added `templates/config.md` §*The fold's values* and a `drained`-in-a-fence sentence to `CONTRIBUTING.md`, neither of which this claim is about; #448's own edits are described by its note above; the claim holds. |
| S2 · the disc is **computed** from the 29×32 chart rather than drawn, and it is sampled at each cell's CENTRE — `x + 0.5 - ox` — so the top and bottom rope rows come out equal and the figure cannot be off centre. The letter twin has the same width and height as the block form at every accepted scale, and every accepted scale draws the SAME BYTES in every process — the tie between two chart colours breaks on the count and then on position in the chart, never on set iteration order (round 1's 🟡 6) | `skills/verify/scripts/seal_stamp.py#build@dd47eca8`, `skills/verify/scripts/seal_stamp.py#shrink@e8e6e102`, `skills/verify/scripts/seal_stamp.py#letter_row@626cc1bb`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_disc_is_symmetric_because_it_is_computed@26ba07fc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_twin_and_the_block_form_have_equal_width_and_height@d3c72bf2` | **Executed** 2026-09-06 in phase 1: the source outside the tree sampled each cell at its corner, and the symmetry case is what found it — the top rope row measured 18 letters against the bottom row's 12, with the first line blank. After the fix both read `OooOOOooO`, and the disc is 43×22 at 1.0 and 33×17 at 0.75. Re-read 2026-09-10 in phase 4; the module is green at this branch's head | 2026-09-10 | **The tidy-up is `int()` instead of `math.floor()` in the chart lookup**, which reads as the same thing and is not: `int` truncates toward zero, so it reads stitch 0 twice at the top-left edge and the figure loses a row of symmetry at exactly the place the case measures. **The second is sampling at the corner again**, which is what the source did and what any reader writing `dx = x - ox` from scratch would write. #30's claim is that *a circle that is calculated cannot be off centre*, and that holds only when the sample point is the cell's centre — the case is the whole of what keeps the claim true |
| S3 · the shape is picked from the stream **before** the console is reconfigured to UTF-8, and a pipe gets letters rather than blocks | `skills/verify/scripts/seal_stamp.py#pick_shape@027a2979`, `skills/verify/scripts/seal_stamp.py#main@4b8233a8`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_pick_shape_is_letters_off_a_utf8_terminal@dfa34011`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_command_piped_prints_the_twin@66d0411b` | **Executed** 2026-09-06 in phase 1: `sys.stdout` and `sys.__stdout__` are one object, and `reconfigure(encoding="utf-8")` moves both — under `PYTHONIOENCODING=cp949` the encoding read `cp949` before the call and `utf-8` after it. Asked afterwards, a cp949 console is handed blocks it cannot draw. Simulated under `script` on that codepage: letters. Re-read 2026-09-10 in phase 4 | 2026-09-10 | **The tidy-up is asking after the reconfigure**, which is where every other reader of an encoding in this repository sits and which `plan.md`'s own Technical context prescribed. It is wrong here for one reason: `hooks/console.py#to_utf8` exists to make the stream lie about what the terminal can render, and this is the one question that has to be asked of the truth. **The second is treating a UTF-8 PIPE as a terminal** — `plan.md` said a pipe gets blocks and `spec.md` said the twin is chosen when stdout is not a UTF-8 *terminal*; the spec won, because an agent's stdout is a pipe and §Out says the sealer's returned text carries the twin |
| S4 · a missing `Broad gate` row is a **refusal** — the row is named, exit 2, nothing run — and never a default | `skills/verify/scripts/broad_gate.py#broad_command@22b8cc54`, `skills/verify/scripts/broad_gate.py#missing_row@85b31017`, `` `templates/config.md#"## Broad gate"@2eaa0803` ``, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_without_the_row_the_gate_names_it_and_runs_nothing@54dd2dd0` | **Executed** 2026-09-06 in phase 2 on a fixture repository with no row: the message named the row to write and the file to write it in, and nothing ran. Re-read 2026-09-10 in phase 4. **Re-read 2026-09-15** by work item 1789445605 (#401), which removed the naming-what-to-write half: the message now says the row is a person's and names `/specseal:config`. Re-executed on the same fixture — exit 2, nothing run, the row and the file still named. The CLAIM is unchanged; what it asserted about the message's wording was not. **Restored 2026-09-16**: the merge at `fedb3860` took the release side of this row and put the pre-correction sentence back, so this was re-read against the merged tree and re-corrected | 2026-09-25 | **The tidy-up is a default of `pytest`**, which makes the command work out of the box and is the counterfeit `verify` names: it seals a repository that runs `npm test`, and it seals it silently. `true` is the same mistake with the argument made honestly. **The second is asking a person for the command**, which is the other thing this cannot be — the gate runs unattended, so a refusal is answered by the next reader, where a question stops a session that may have nobody at the keyboard. **Corrected 2026-09-15** by work item 1789445605, and **restored 2026-09-16** after the merge at `fedb3860` reverted it: this read *a refusal naming what to write is answered by the next reader*, and naming what to write is exactly what #401 found the message must not do. Executed against the merged tree — `missing_row` carries no such sentence. The argument survives the correction: what reaches the next reader is now whose the row is and where they answer it. `CLAUDE.md`'s first goal is what decides between them. **Re-read 2026-09-17 by work item 1789598366 (#415), which drifted both code anchors**: a MISSING row is still a refusal that names the row, exits 2 and runs nothing, and still has no default. The branch adds a branch for a row that is present and unparseable, which is a different cause and does not touch this claim **Re-read 2026-09-17 again by #415 phase 4**, which rewrote the template's pipe cell. A missing row is still a refusal with no default; what the branch added is a second refusal for a row that is present and unparseable. **Re-read 2026-09-18 by work item 1789721571 (#429)**, which put one fence rule in front of every walk of this table and left both halves of the stop rule where they were. The unit this row anchors on gained that filter and nothing else; the claim holds and the cases it cites are green. **Re-read 2026-09-25 by work item 1790260567 (#448), phase 1**, which added one paragraph to `## Broad gate` saying which shell runs the row on Windows and that `cmd.exe` is handed each command name with `/` written `\`. The paragraph is about a row that is present; a MISSING row is still a refusal that names the row, exits 2 and runs nothing, and the claim holds. **Re-read again 2026-09-25 in round 1's fix pass of work item 1790260567**, which replaced the template's *Two positions are not rewritten* sentence with the rule and its examples, a built-in's switch among them. A missing row is still refused; the claim holds. Re-read 2026-09-25 at the merge of release/v0.15.3 into #448's branch: the release side added `templates/config.md` §*The fold's values* and a `drained`-in-a-fence sentence to `CONTRIBUTING.md`, neither of which this claim is about; #448's own edits are described by its note above; the claim holds. **Re-read 2026-09-25 in round 2's fix pass of work item 1790260567**, which added two sentences to `## Broad gate` after the built-in example: a `/` straight after any other program is rewritten, and a blank before the switch avoids it (#596). A missing row is still refused; the claim holds. **Re-read 2026-09-25 by work item 1790297086 (#596), phase 1**, which rewrote the sentences of `## Broad gate` that say what `cmd.exe` is handed. The refusal paragraph is unchanged and the claim holds. **Re-read 2026-09-25 by work item 1790297086, in round 1's fix pass**: `templates/config.md` §*Broad gate* gained a paragraph on the two arms left out at `main` and its bound, a sentence saying a `%VAR%` in a name's part is expanded first, and "every leading `@`" for "a leading `@`". The claim does not rest on what changed, and it holds **Re-read 2026-09-25 by work item 1790297086, in round 2's fix pass**: `templates/config.md` §*Broad gate*'s `%VAR%` sentence now says `%CD%` is the directory the row runs in. The claim does not rest on what changed, and it holds |
| S5 · the base comparison is **reactive**: it happens only when a test fails, only on the failing files the base actually carries — the absent ones read `new` without a run, because pytest handed a path that does not exist prints no `FAILED` line and would lose the measurement for every other file in the same run (round 1's 🟡 4) — at the base in a scratch worktree the gate removes, and each failure comes back `new` or `failing on base too`. What it re-runs is what stands before the row's first `&&` | `skills/verify/scripts/broad_gate.py#compare_at_base@efeccb32`, `skills/verify/scripts/broad_gate.py#first_command@8623ea0c`, `skills/verify/scripts/broad_gate.py#failing_files@6d1dc82c`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_failing_test_is_not_sealed_and_is_new_when_the_base_passes@49706137`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_failure_the_base_shares_is_labelled_failing_on_base_too@b5babfda` | **Executed** 2026-09-06 in phase 2, and once on the real repository at `0c04bc3`: the scratch worktree's `bin/test` built its own `.venv` with `uv` in about ten seconds out of a 367-second run, because `run_tests.py` resolves the root relative to the worktree's own copy. A file the base does not carry cannot fail there, so it reads `new`. Re-read 2026-09-10 in phase 4 | 2026-09-10 | **The tidy-up is running the whole suite at the base as well**, which is symmetric and is the waste `skills/verify/SKILL.md` §*The broad gate* already measured — a 19,000-test suite run serially for a baseline before any failure existed. **The second is splitting the row on something cleverer than the first `&&`.** The row is one shell line and nothing in it says which part is the suite runner; `templates/config.md` §*Choosing a value — the criterion* says to put the runner first for exactly this reason, which is a convention the reader keeps rather than a fact the parser can check. **Corrected 2026-09-15** by work item 1789445605, and **restored 2026-09-16** after the merge at `fedb3860` reverted it: this named the config skill as a second carrier, and that copy was folded into the criterion's one home in phase 3. Executed against the merged tree — `grep -c "put the suite runner first" skills/config/SKILL.md` answers 0 |
| S6 · `round_record.py seal` sets the LAST record's `Broad gate` cell and touches nothing else **where a round record exists** — a work item that ran none gets `<item>/broad-gate.md` instead, and three of the refusals below are skipped there because each is a question about a round that ran — and the number of `raise Refused` sites in it is **six**, counted rather than described: no `Pass` box or more than one, `Pass` unchecked, a `Fixes checked by` that reads anything but `no fixes to check`, a cell with no SHA in it, a SHA the repository cannot see, and a `Target SHA` that descends from the run's own commit. It is a subcommand of its own because `close` cannot make this write twice | `skills/code-review/scripts/round_record.py#seal@ad6b9461`, `skills/code-review/scripts/round_record.py#last_record@5e446de7`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_writes_the_last_records_cell_and_nothing_else@30b3531f`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_while_pass_is_unchecked@8aa4c246`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_a_sha_the_target_descends_from@f9794302`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_while_the_fixes_have_been_read_by_nobody@8ce6f088`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_refuses_a_cell_with_no_sha_in_it@11fab069` | **Executed** 2026-09-06 in phase 2: the write is compared byte for byte against every other line of the record, and each refusal names its reason and writes nothing. The failure it removes was **executed** on the other work item of this release — `close --broad-gate` on a record whose fix table had been applied refused with *the fix table has a row for finding 17 (`answered`), which the reviewer already closed … no cell was written*, and the only way through was a fix table with a header and no rows. Re-read 2026-09-10 in phase 4 | 2026-09-24 | **The tidy-up is folding this back into `close` as a flag**, which is where the cell was written until #30 and which looks like one subcommand too few. `close` applies a fix table and refuses a row for a finding the reviewer already closed — correctly — so on a settled record every route through `close` is refused for the table it has to be handed. **The second is having the sealer return the value for the orchestrator to write** (`questions.md` Q3): five such reach-backs were forgotten on one branch, and a sealer with no durable write is #30's *nobody is assigned it* one step later. **A refusal here is exit 2 and no stamp**, never a stamp followed by a note: the alternative prints *sealed* over a record that says the run came too early. **Phase 5 removed the `Needs a fix` refusal and this row lost a coordinate with it**: the case that pinned it was renamed, which removes an anchor rather than drifting one, and the claim it carried moved to S10 below. **Re-read 2026-09-15 by work item 1789425391 (#335), which drifted this anchor.** The COUNT is unchanged — still six `raise Refused` sites — and the third one narrowed: it refused a `Fixes checked by` that was neither a later round nor `no fixes to check`, and it now refuses anything but `no fixes to check`, because `CHECKER_RE` tests the cell's shape and cannot test its position on a LAST record. The clause above is corrected in place; the new claim is a row of that work item's own fragment. The claim above was corrected by re-reading `#seal` rather than by re-stamping it — a re-stamp recomputes a hash and reads nothing, so a row enumerating three refusals over a function that now has two would have passed silently **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** `seal`'s docstring and one comment in its body were corrected: they said a capped run reads `no fixes to check` here, which holds of a capped run's LAST record and not of a capped record that wrote fixes. No arm, constant or refusal moved. The six `raise Refused` sites were re-counted from the AST today and are still six, so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `seal`.** The write keeps a run the cell already held behind the new entry as `earlier run`, for both homes; a `broad-gate.md` already there is read for that one thing before the write. The six refusals are unchanged, and a first seal is byte-identical to before. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1, ⬜ 8), which drifted `seal`.** The keep moved into `kept_broad_gate`, shared with `close --broad-gate`, and a run at the commit the newest entry already names replaces that entry rather than duplicating it; the refusals are unchanged. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| S7 · the gate runs `chain_check.py` judged as a **draft** pull request, on purpose, and leaves a payload GitHub itself wrote alone | `skills/verify/scripts/broad_gate.py#draft_env@61c9c8af`, `skills/verify/scripts/broad_gate.py#gate@5fdd69eb` | **Executed** 2026-09-06 in phase 2 on the `--record` fixture: judged as READY the same run exits 1 with *`Broad gate` is `not yet` on the last round record, and this is a ready pull request* — the cell the gate is about to write | 2026-09-21 · 2026-09-24 | **The tidy-up is judging it ready, since a ready pull request is what the gate is for.** The gate runs BEFORE `seal` writes the cell, so at that moment the last record honestly reads `not yet`, and a ready judgment fails the run on the one row the run exists to fill. **The second is writing the payload unconditionally**, which would overwrite the event GitHub hands a workflow; `draft_env` writes one only when the environment carries none, which is the shape `round_record.run_check` already uses **Re-read 2026-09-21 by work item 1789956662 (#423), which drifted this anchor.** The gate now resolves `--base` once, before any check runs, and hands every consumer the resolved commit; the claim above is untouched by that — `draft_env` and the `env=draft_env(keep)` call site are byte-identical, and the resolution sits above them. **Re-read again 2026-09-21 by work item 1789985781 (#468), which drifted this anchor.** That work item added two arms to `gate()` after the survivor call and one stderr line above the checks; `draft_env` and the `env=draft_env(keep)` call site are byte-identical again, and neither new arm reads the pull-request payload. The claim stands unchanged. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** one argument added to the `panel` call, `gate_copy(root)`, so the stamp says which copy of the gate ran (#475); nothing else in the body moved, and `args.base` is still read once. The claim stands. **Re-read 2026-09-25 by work item 1790297086 (#473), phase 3**, which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` and the gated repository's `hygiene.yml` carries their steps, and print one line saying so. The `chain` arm and its draft payload are untouched; the claim holds. **Re-read 2026-09-25 by work item 1790297086, in round 1's fix pass**: `gate` changed in one comment only, naming `skipped_at_main`'s read of `base.given` as the one exception to `base.commit`. The claim does not rest on what changed, and it holds **Re-read 2026-09-25 by work item 1790297086, in round 2's fix pass**: `gate` changed in one comment only, now naming `moved_line` beside `skipped_at_main` as the two readers of `base.given`. The claim does not rest on what changed, and it holds |
| S9 · the class *what names the owner of the broad run* includes a printed failure MESSAGE, not only a document. `broad_gate`'s **three** fatal refusals name the sealer's spawn and `broad-gate` (the count is that FUNCTION's; `direct_seal` beside it adds a fourth of the same shape for a work item with no round record), the second keeps `close --broad-gate` for the one pass where fixes and the gate land together, and the third — the premature SHA — says to re-take the run rather than telling the reader to take it by hand | `skills/code-review/scripts/chain_check.py#broad_gate@e13776dc`, `tests/test_chain_check_at_the_pull_request.py#test_a_broad_gate_that_never_ran_fails_a_ready_pull_request@0810f067`, `tests/test_chain_check_at_the_pull_request.py#test_a_one_word_cell_is_not_a_way_past_the_arm@0c06ba74` | **Executed** 2026-09-06 in phase 3: three mutation arms — the agent unnamed in each message, and the one-pass route dropped from the second — all red. The two messages were found by `survivor-check`, not by the spec: `spec.md` §Scope 6 lists seven documents and not this file, because it is a script | 2026-09-16 | **The tidy-up is reading `spec.md`'s list as the class.** It is a list of documents, and these are the two places a person actually reads the instruction — at the moment a pull request is refused. Contract §12 is the rule and this is its instance: the finding named a coordinate and the fix was owed to every place the same cause produces. **Two code comments inside `chain_check.py`'s own gate arm were deliberately left**, because they explain why an unparseable cell above the cutoff is a cell somebody chose, and that reasoning survives `seal` joining `close` — both are subcommands, neither is free text. The survivor check does not reach them, and the docstring of the case that pins that arm was corrected to name both writers. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `broad_gate`** in its docstring alone — a paragraph on why the first SHA-shaped word is the run. The three fatal refusals and their messages are as they were. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1), which drifted `broad_gate`** in its no-SHA message alone — it now says `close --broad-gate` takes the same newest-first path; the states and the refusals are as they were. **Re-read 2026-09-24 by work item 1790206437's round-1 fix pass (⬜ 3, the sweep):** `broad_gate`'s docstring now says a re-seal at a new commit records a second run and the same comparison as the newest entry replaces that entry, which is `same_run`'s rule — asked of `entries[0]` alone (narrowed to the newest entry by round 2's fix pass, ⬜ 6); the states and the refusals are as they were and the claim holds |
| S10 · a CAPPED run reaches its seal. `seal` asks the verdict table — the `Pass` box — and never the reviewer's `Needs a fix`, so a record whose findings all closed `deferred <home>` seals while that row still reads `yes`, and the reviewer's row is left exactly as written | `skills/code-review/scripts/round_record.py#seal@ad6b9461`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#capped_item@afa7f6bc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_writes_over_a_capped_runs_needs_a_fix@9203d9a9` | **Executed** 2026-09-10 in phase 5, seen red against the old code first — the refused run printed *round-1.md's `Needs a fix` reads `yes — 🔴 1`, and the broad gate runs after the rounds settle … no cell was written*, which is phase 4's measurement reproduced by the case. Green after the removal, and `seal` ends with `chain_check`, so exit 0 is the whole chain end to end | 2026-09-24 | **The tidy-up is keeping the refusal and teaching the capped run to rewrite `Needs a fix`.** That row is the reviewer's answer from while the round ran, and nothing else in the workflow rewrites what a reviewer wrote; editing it to unblock a gate is the review certifying itself one step removed. **The second is leaving both refusals and calling the capped run an exception at the pull request**, which puts a documented way past `chain_check` in front of every branch. What the removed refusal was reaching for is *is a finding still open*, and `Pass` answers exactly that, one row down, from the table rather than from prose **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** `seal`'s docstring and one comment in its body were corrected: they said a capped run reads `no fixes to check` here, which holds of a capped run's LAST record and not of a capped record that wrote fixes. No arm, constant or refusal moved. `seal` still asks the `Pass` box and never `Needs a fix`; nothing the corrected comment sits beside reads that row, so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `seal`** at the write alone — the newest-first cell; `Pass` is still what is asked and `Needs a fix` still is not. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1, ⬜ 8), which drifted `seal`.** The keep moved into `kept_broad_gate`, shared with `close --broad-gate`, and a run at the commit the newest entry already names replaces that entry rather than duplicating it; the refusals are unchanged. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| S11 · the rule that one seal is final has ONE owner and two linking carriers, and it is held by the same table that holds the other nine — `skills/verify/SKILL.md` states *every agent seals what it verified, and the one seal over the whole project is the sealer's*, and `agents/sealer.md` and `agents/warden.md` each carry a sentence naming that section rather than restating the rule — the warden's said it verbatim before linking until round 1's correction, which is the drift the table exists to catch arriving in the row that added it | `` `skills/verify/SKILL.md#"## Every agent seals what it verified, and one of them is final"@13e77c7f` ``, `tests/test_the_rules_have_one_owner.py#test_every_link_names_the_owner@314ad3a3`, `tests/test_one_word_one_meaning.py#test_the_owner_states_that_one_seal_is_final@cdc94327` | **Executed** 2026-09-10 in phase 5: both parametrised cases red against the definitions as they stood — the owner case on the missing sentence, the link case naming `agents/sealer.md` — and green after. The sweep case that forbids an anonymous instance was red on four files at the same commit | 2026-09-10 | **The tidy-up is renaming the sealer so one agent owns the word.** The product's name is built on the noun and the whole stamp design rests on it, and the ambiguity was never that several seals exist — a smith's proof block over its own slice is a legitimate seal. What was missing is that one of them is final. **The second is writing the rule into each definition rather than linking**, which is the drift `tests/test_the_rules_have_one_owner.py` exists to catch and which its own docstring counts: one rule reached eight carriers and three consecutive rounds corrected them three at a time |
| S12 · `broad-gate` reads BOTH of `seal`'s non-zero exits as unsealed. A refusal raised before the write exits 2; what `seal` returns after the write is `chain_check.main`'s `1 if errors else 0`, and the gate's message tells the two apart by the word `sealed` in the `round-record:` line above it — **round 1 wrote this row saying the line's PRESENCE was the discriminator and round 2 measured that both endings print one**, so what the row recorded as verified was the thing that was wrong; the correction is the word, not the line | `skills/verify/scripts/broad_gate.py#gate@5fdd69eb`, `skills/verify/scripts/broad_gate.py#seal_record@4d582ed3`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_seal_exit_that_is_not_two_leaves_the_tree_unsealed@6a8b01ee` | **Executed** 2026-09-10, round 1's fix pass: the case returned exit 0 against the branch as reviewed, with the disc printed over a failing chain check. **Re-read 2026-09-15 by work item 1789425391 (#334), which drifted no anchor here and is about what stands behind them:** the claim holds — the gate still reads both non-zero exits and still discriminates on the word — and the coordinate it rests on is a case that drives a STUB whose `round-record: sealed …` text the case itself writes, so changing the real print left it green. A case over the real pair now stands beside it and its coordinate is a row of that work item's own fragment | 2026-09-21 · 2026-09-24 | **The tidy-up is reading `== 2`**, which is what shipped and which is the natural reading of a subcommand documented as *exit 2 on a refusal*. The second exit is not a refusal at all — it is the chain check `seal` runs after the cell is written, and it comes back as 1 — so the seal that fell through was over a tree the gate's own check refuses. **The second is treating that 1 as a failure of the checks**: the checks passed, the cell exists, and the message has to say so or the reader hunts a check that is green **Re-read 2026-09-21 by work item 1789956662 (#423), which drifted this anchor.** The gate now resolves `--base` once, before any check runs, and hands every consumer the resolved commit; the claim above is untouched by that — the two-exit reading and the `sealed` discriminator are byte-identical; what moved is the VALUE handed to `seal_record`, from the ref as typed to the commit. **Re-read again 2026-09-21 by work item 1789985781 (#468), which drifted this anchor.** Two arms were added above the `if item is not None` block and one line printed above the checks; `seal_record`, the `== 0` reading and the `sealed` discriminator are byte-identical, so the claim stands unchanged. This row was re-stamped by an `evidence-check --reverify` run whose lenient report had named the coordinate ONCE for three rows that share it — read here rather than left re-stamped unread, and the silence is a row in `seal/follow-up.md`. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** one argument added to the `panel` call, `gate_copy(root)`, so the stamp says which copy of the gate ran (#475); nothing else in the body moved, and `args.base` is still read once. The claim stands. **Re-read 2026-09-25 by work item 1790297086 (#473), phase 3**, which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` and the gated repository's `hygiene.yml` carries their steps, and print one line saying so. The `seal` hand-off after the checks is unchanged; the claim holds. **Re-read 2026-09-25 by work item 1790297086, in round 1's fix pass**: `gate` changed in one comment only, naming `skipped_at_main`'s read of `base.given` as the one exception to `base.commit`. The claim does not rest on what changed, and it holds **Re-read 2026-09-25 by work item 1790297086, in round 2's fix pass**: `gate` changed in one comment only, now naming `moved_line` beside `skipped_at_main` as the two readers of `base.given`. The claim does not rest on what changed, and it holds |
| S13 · a run that ended at the round cap seals and a run whose fixes nobody has read does not, and the two are told apart by `Fixes checked by` rather than by `Pass`. `close` ticks the box the moment a fix table applies, which is one row earlier than the run ending; it re-derives the row to `no fixes to check` where nothing commissioned a fix, which is the capped case. **The row holds three values and, on the last record, everything outside ONE of them is refused** — not `nobody` alone, and, since work item 1789425391 (#335), not everything-but-a-`round-N` either: reading `nobody` alone let a name, a word outside the vocabulary and an empty cell reach the write, and the chain check run AFTER the write then refused on that same row | `skills/code-review/scripts/round_record.py#seal@ad6b9461`, `skills/code-review/scripts/round_record.py#landing_values@db2f01fc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_seal_writes_over_a_capped_runs_needs_a_fix@9203d9a9`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_capped_run_still_seals_beside_the_third_refusal@c2dc3011` | **Executed** 2026-09-10, round 1's fix pass: the new refusal red against the code as reviewed; and the plausible wrong fix — refusing anything but a named checker — turns both capped-run cases red, which is what says the two rows are not one rule | 2026-09-24 | **The tidy-up is replacing the `Pass` refusal with this one**, since this is the stricter test. They answer different questions: `Pass` says nothing in the verdict TABLE is open, and this says the fixes that closed it were read. A record can fail either without failing the other. **The second is the whole history of this cell**: three rows were tried in order — `Needs a fix`, which refused a capped run; `Pass`, which lets an unread fix pass through; and this one. Anything that reads like the obvious single answer has already been one of the three **Re-read 2026-09-14 by work item 1789356180 (#321 #341 #353 #273 #391), which drifted this anchor.** The word `only` came out of the clause: `close` now writes BOTH of `landing_values`' answers. `no fixes to check` where nothing commissioned a fix is unchanged and is what this row is about; the other answer — a reason saying the fixes are written and no round has opened them — is row 7 of this work item's fragment. The three-value refusal this row records is untouched, and `nobody` is still what a closed record carries. **Re-read 2026-09-15 by work item 1789425391 (#335), which drifted this anchor and superseded half its bolded clause.** What the row is FOR still holds: a capped run seals, a run whose fixes nobody has read does not, and `Fixes checked by` is what tells them apart. What changed is how many of the three values the seal accepts on a LAST record — one, not two — because a named checker has to be a later round and the last record has none. The clause is corrected in place rather than the row removed, because the capped-run claim is the row's subject and is untouched; the narrowing is a row of that work item's own fragment. **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** `seal`'s docstring and one comment in its body were corrected: they said a capped run reads `no fixes to check` here, which holds of a capped run's LAST record and not of a capped record that wrote fixes. No arm, constant or refusal moved. The three values and the one value accepted on the last record are unchanged — the corrected comment describes that refusal and is what now says which record it is true of — so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `seal`** at the write alone; the `Fixes checked by` refusal that tells a capped run from an unread one is as it was. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1, ⬜ 8), which drifted `seal`.** The keep moved into `kept_broad_gate`, shared with `close --broad-gate`, and a run at the commit the newest entry already names replaces that entry rather than duplicating it; the refusals are unchanged. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| S14 · the panel's `suite` row carries pytest's own counts and never a linter's, and the discriminator is the WALL CLOCK rather than a list of words. A run in which every test was skipped reports its counts instead of coming back empty, which the panel would have rendered `exit 0` | `skills/verify/scripts/broad_gate.py#suite_counts@d37cdbd7`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_suite_row_reads_pytests_counts_and_not_a_linters@f9362575` | **Executed** 2026-09-10 over round 1's and round 2's shapes: `2 warnings emitted`, `Found 2 errors.`, `3 skipped in 0.10s`, and a decorated `768 passed in 63.21s (0:01:03)` | 2026-09-10 | **The tidy-up is a list of pytest's summary words**, which is what round 1 shipped and what round 2 reopened. It closes the finding on the instance and not the class: the list held `errors` and not `warnings`, so one linter's line still landed on the row — and ADDING the missing words is worse, because `warnings` back in the list revives round 1's original defect. The two halves fight, and a word list is the wrong axis. **The second is reading the LAST line rather than the last match**, which breaks on a runner that prints anything after its summary. **This row lost a coordinate rather than re-pointing one**: the clock pattern stood as a module constant `SUMMARY_TAIL` until `close` refused the fix table for it — a top-level name added by a fix answering a finding inside a unit `round-1.md`'s `New units` names is at depth 2, and a fix pass may not add one. The pattern is inline in `#suite_counts` now, so there is no second anchor and the claim is whole in the first |
| S15 · `quote` takes the platform as an ARGUMENT rather than reading `os.name`, so both branches can be turned red from either machine; and the Windows branch uses double quotes rather than `subprocess.list2cmdline`, because `run(..., shell=True)` there goes through `cmd.exe` and `list2cmdline` builds `CreateProcess` quoting | `skills/verify/scripts/broad_gate.py#quote@147ff2d4`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_path_is_quoted_for_the_shell_of_either_platform@e99855e3` | **Executed** 2026-09-10: `list2cmdline` with the platform as an argument returns `tests/x&y.py` unescaped, and `return path` — which was green before this case existed — fails all three parametrisations. The `cmd.exe` behaviour itself is **read**, from Python's own documentation; there is no Windows here | 2026-09-11 | **The tidy-up is reading `os.name` inside the body**, which is what shipped and which is exactly #103's class made out of the fix for it: the branch runs on CI's `windows-latest` leg and nothing asks what it produced, so the whole unit could be replaced by `return path` with every case green on every leg. **The second is claiming the quoting is complete.** `cmd.exe` expands `%VAR%` inside double quotes, so a path holding percent signs is still mangled; the docstring names it rather than closing it, because the fix for that is not quoting. **Re-read 2026-09-11 (#351).** `quote`'s docstring lost a dead path from its citation of #103's class; the argument and the branch it describes are unchanged |

<!-- specs/1789034970-the-contract-is-settled-against-the-agents-that-exist -->
### 1789034970-the-contract-is-settled-against-the-agents-that-exist

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows over a branch that rewrites three sections of one file. Most of
what this work item claims is claimed by a pin instead: the sections say what
they say, and `tests/test_the_agent_contract_holds_the_universal_rules.py`,
`tests/test_broad_gate_rule.py` and
`tests/test_the_seal_is_taken_once_by_the_sealer.py` are where that is
enforced. What earns a row here is a judgment a pin cannot carry — why §2
names an agent at all when the file's own opening forbids per-role rules (R2),
why §7's heading was left alone while its body widened (R3) — and one claim
this branch inherited by removing somebody else's row (R1).

R1 replaces S8 of
`seal/ledger/1789002694-two-agents-are-forbidden-the-seal-and-nobody-is-assigned-it.md`.
That row cited a section this work item deletes and two cases it renames, and
a row whose anchor a change removes is REMOVED rather than re-pointed. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `agents/sealer.md` names **both** of its permissions in its own file and apologises for neither: the one write, which §6 now says a definition names rather than excepts, and the one run, which §2 leaves each definition to assign. The window paragraph — *this file is the narrower document and the contract is the wider one* — is gone, and the case that pinned it asserts its absence rather than having been deleted with it | `` `agents/sealer.md#"## The one write, and why it is yours"@e15c8288` ``, `` `agents/sealer.md#"## The one run, and why it is yours"@1fd9352f` ``, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_sealer_names_the_one_write_its_definition_is_allowed@aa82091d`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_window_the_sealer_shipped_under_is_closed@ed9e3fc7` | **Executed** 2026-09-10 in phase 3. `test_the_window_the_sealer_shipped_under_is_closed` was run against `agents/sealer.md` as it stood at `8204533` — the window section still in place — and failed; it passes at the working tree. `test_the_sealer_names_the_one_write_its_definition_is_allowed` passed against both, which is honest and is why it is recorded that way: only its name and its grounds changed, and its assertions are the ones S8 already carried. 350 cases green across the eight modules phase 3 touches | 2026-09-22 | **Successor to S8**, removed from work item 1789002694's fragment by this branch. **Deleting the window is not enough on its own.** §2 as rewritten says the assignment lives in the definition rather than in the contract, so a sealer definition that only stopped apologising would leave §2's pointer naming a file that does not answer — which is #30's opening restated one release later. `## The one run, and why it is yours` is what closes that, and `spawned for exactly that` is what the case holds it to.  **Re-read 2026-09-10, round 1's fix pass.** Finding 10 corrected §*The one write*'s recitation of §6's withheld acts — it dropped `post` and added `commit`, and now names the four §6 actually withholds — which moved that section's hash. The claim above is untouched. `spawned for exactly that` also became load-bearing twice over: `test_only_one_definition_assigns_the_broad_gate` counts the definitions carrying it, so §2's `One` is checked rather than asserted. **Re-read 2026-09-11 in work item 1789081272 (#84), phase 2.** The section's count of the definitions that stay silent moved from `three` to `four` when the fifth definition landed, which is the only edit to it. Both permissions are still named, the window paragraph is still gone, and `spawned for exactly that` is verbatim — the claim holds. The count is now derived rather than typed: `test_the_sealer_counts_the_silent_definitions_from_the_glob` reads it off the same glob and the same marker **Re-read 2026-09-15 by work item 1789425391 (#335), which drifted this section.** The recitation of what `seal` refuses outright moved from two things to three, naming the `Fixes checked by` refusal it has raised since #30's round 1 and now narrows — § 14, the sealer being the one party stopped by it. Both permissions are still named in this file, the window paragraph is still gone, and `spawned for exactly that` is verbatim, so the claim holds **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The section gained a paragraph saying a `round-N` in that cell is a capped run that wrote fixes rather than a defect, and that the way past the refusal is a verifying round. Both permissions are still named in the file and the window paragraph is still absent, so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which added two sentences to §*The one write* about the newest-first cell; the write is still one cell and the three refusals are as they were. **Re-read 2026-09-24 by work item 1790206437's round-1 fix pass (⬜ 3):** the same section's *never erases the record of the first* now carries the same-commit-same-base exception `same_run` implements; the two permissions and the three refusals are unchanged and the claim holds. **Re-read 2026-09-24 by work item 1790206437's round-2 fix pass (⬜ 6):** the exception now reads *unless the newest is the same commit against the same base*; the two permissions and the three refusals are unchanged and the claim holds |
| R2 · §2 names one agent, and that is not the per-role exception this file's opening refuses. The **rule** is universal — *Whether it is yours is what your own definition says* — and the naming is a fact about where the one assignment currently lives, stated so the section is not a prohibition with nobody in the room to perform it. The guard is an ABSENCE, scoped to §2's body: the word `orchestrator` must not occur in it | `` `skills/agent-contract/SKILL.md#"## §2 Narrow and often; the broad gate is one act with one owner"@792ad5a1` ``, `tests/test_broad_gate_rule.py#test_the_prohibition_itself_has_one_home_and_it_is_the_contract@74eae03b`, `tests/test_broad_gate_rule.py#contract_section@70f2657a` | **Executed** 2026-09-10 in phase 1: the re-pointed case red against the contract as it stood at `23ca89c` (`bin/test` exit 1, five failures with §2's and §7's moved pins), green at `63d013d`. **Read**: the 15-word duplication margin re-measured after every phase — the widest run any definition shares with any section is 10 words, `agents/smith.md` against §8, unchanged from the base | 2026-09-10 | **Why an absence and not a presence.** A contract naming both owners is the state `test_the_definition_names_the_sealer_as_the_suites_owner` already refuses one file over, and a presence check cannot see it. **Why the body and not the file.** `orchestrator` is legitimate in the contract's opening paragraph, about the prompt a round arrives in; a whole-file absence check would either fail on that sentence or be dropped as over-strict, and a dropped check is worse than a scoped one. `contract_section` is cited because the scoping is the claim |
| R3 · §7's heading is **untouched** while its body widens, and the reason is an anchor rather than taste. `seal/ledger.md`'s row L7 anchors on that heading; a heading is the major level of a content anchor, so rewording it BREAKS the row where a body edit only drifts it — and this repository removes a row whose anchor a change removes rather than re-pointing it. The file form the heading states is still the rule for the common case, so nothing is lost by the widened sentence living below it | `` `skills/agent-contract/SKILL.md#"## §7 A probe is named `test_tmp_*`, one file, run once, deleted"@a677a411` `` | **Read** 2026-09-10 in phase 2: `bin/evidence-check .` reports this anchor DRIFTED — *content changed at 165-190 — re-verify* — and not BROKEN, which is the whole of the claim. **Executed**: the moved §7 pin red against the old body in phase 1's run, green at `94a2451` | 2026-09-10 | The alternative was to reword the heading so a reader skimming headings meets the widened rule. It costs row L7, whose own claim (*the probe rules and the git-from-Python rule are contract §7 and §8*) is still true and would have had to be removed and rewritten here to say the same thing. **This is worth a row because the repair looks obvious from outside**: the next session that reads §7 will see a heading narrower than its body and reach for the edit that breaks L7.  **Re-read 2026-09-10, round 1's fix pass.** Finding 7 added the ownership clause to §7's body — what the probe made for itself is what goes — so the hash moved again and the heading did not. The claim is unchanged, and the heading is now two paragraphs narrower than its body rather than one, which sharpens the hazard this row exists to name |
