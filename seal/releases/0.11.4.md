## 0.11.4 — 2026-09-14

<!-- specs/1789338080-the-one-script-an-agent-is-told-to-run-cannot-be-typed -->
### 1789338080-the-one-script-an-agent-is-told-to-run-cannot-be-typed

<!-- Five rows over a change that adds one command and one sentence per
document. The first is the command itself; the second and third are the two
rules the pin holds the repository to, which are the only parts of this work
that can go false without anybody editing a file this branch wrote; the fourth
is the classification those rules rest on; the fifth is the two typed forms,
which are the half #318 asked for and the half a wrapper alone would have
finished. -->

###### The one script an agent is told to run cannot be typed

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| `round-record` is a bare-word command that reaches the round-record generator with its arguments intact, and a `.cmd` twin ships beside it so Windows reaches the same script through the file it can execute | `bin/round-record#"exec python3 \"$here/../skills/code-review/scripts/round_record.py\" \"$@\""@e412d4d5`, `bin/round-record.cmd#"set \"script=%here%..\skills\code-review\scripts\round_record.py\""@d4062873` | **Executed** 2026-09-14: `bin/round-record --help` exits 0 printing `usage: round-record`, and `bin/round-record new --help` exits 0 printing `--item`. Four cases in `tests/test_the_record_is_generated.py`, all seen red before the two files existed — the pair and its exec bit, the argv both platforms construct, the name the wrapper reaches the generator under, and a subcommand passing through | 2026-09-14 | The name is not this work's invention: the script's own `--help` has printed `usage: round-record` since it shipped, which is what made the absent file a defect rather than a design choice. The second case pins the subcommand because a wrapper that swallowed its arguments entirely would still print the usage line and exit 0 — that is what argparse does with nothing |
| Every shipped document that names a wrapped `skills/*/scripts/*.py` carries the command or the script's repo-relative path, at least once | `tests/test_a_document_that_names_a_script_says_how_to_reach_it.py#reachable@0b378b66`, `tests/test_a_document_that_names_a_script_says_how_to_reach_it.py#named_pairs@a5051935` | **Executed** 2026-09-14: red first against all nine of `round_record.py`'s documents, named one per case, then green after the sweep. Shown red a second time by mutation — the locator deleted from `templates/sdd-round.md` — which failed on that document alone | 2026-09-14 | The rule is per document and not per mention, so a document naming a script forty times pays one sentence. It is satisfied by either form on purpose: the bare command is what a session with the plugin enabled can type, and the path is what a session inside a clone with the plugin disabled can open. `reachable` requires a word boundary on both sides of the command, so `round-record` inside `round-record-writer` does not satisfy the rule for the other name — pinned by a case of its own, which was red under a substring reader. **Round 1 🟡 1 narrowed it further**: a command name with no hyphen in it is an ordinary English word — `seal.py` answers to `seal` — and this reader had no way to tell that word used as a command from the same word in a sentence, so the rule was unfalsifiable for that script. The limit is the reader's, never the script's: `bin/seal` and `bin/seal.cmd` both ship and the command is real. For such scripts the path is the form that counts, which is a reader that can fail. **Round 2 🟡 8 then found #318's own shape inside that fix**: the failure message still offered the command as a repair the rule would reject, so a reader who followed it stayed red and learned nothing. The message now names the path and says why, pinned by `test_the_failure_message_offers_a_repair_that_actually_works` under `skills/agent-contract/SKILL.md` §14. **Round 2 🟡 9 bounded the guard rather than widening it**: the hyphen is not what separates a command from prose — measured over all 43 documents, six commands appear as bare words in documents naming no script, `broad-gate` in nine — and both alternative readers are worse, one still passing *the seal after the rounds* while reddening a live pair and one reddening `skills/code-review/orchestration.md` for `evidence_check.py`. The bound is written at the guard |
| A script that a shipped document names either has a `bin/` wrapper pair or is classified in the pin with its reason, and neither answer may be left to whoever notices | `tests/test_a_document_that_names_a_script_says_how_to_reach_it.py#NO_WRAPPER@c7e69128` | **Executed** 2026-09-14: with the classification empty, the case failed on `chain_check.py` naming both repairs — write the pair, or classify it here with the reason. Green once the row was written | 2026-09-14 | This is `tests/test_the_release_check_watches_what_ships.py::test_every_top_level_entry_is_classified`'s shape applied to scripts rather than to top-level roots, and for the same reason: `bin/` itself was a root nobody had classified, which is how it fell out of the release check unnoticed. A script no shipped document names is skipped rather than demanded, because nobody goes looking for one. **Round 1 🟡 2 added the executable bit** to the same case, which `plan.md` §*Where the wrapper pairs are pinned today* had promised the pin would generalise and which it asserted for neither file: a wrapper committed `100644` resolves on PATH and then refuses. The residual is stated rather than closed, and it is the names because the number has now been wrong three times: the case skips scripts no shipped document names, so **seven of the twelve wrapper pairs still have nobody asserting the bit** — `arm-check`, `broad-gate`, `payload-meter`, `seal`, `seal-stamp`, `survivor-check` and `test`. The new assertion newly covers exactly one, `session-cost`. Round 1's report said eight; round 1's fix pass said four and was wrong the same way the report was, by matching filenames in any module that mentions them rather than opening each `os.access` call; round 2 opened all twelve pairs against every exec-bit assertion in `tests/` and got seven, which this row re-derived the same way. Closing the seven needs a case walking `bin/`, which is mechanism a fix pass may not add — filed as #389 |
| `chain_check.py` keeps no wrapper, and the premise that makes that safe — that no shipped document shows it being invoked — is asserted rather than assumed | `tests/test_a_document_that_names_a_script_says_how_to_reach_it.py#command_forms@f129910e` | **Executed** 2026-09-14: `grep -rn 'chain_check\.py [-a-z]' --include='*.md' agents/ skills/ templates/` exits 1. The case was driven red by planting `chain_check.py --worktree` into `skills/code-review/SKILL.md`, which failed naming the file, the line and both repairs; the plant was reverted. `command_forms` itself was mutated to match nothing, which turned its own fixture case red | 2026-09-14 | A flag is the only sound tell in prose. A subcommand cannot be told from the sentence around it — *`round_record.py new` writes the record* names a subcommand and is a description — so the detector reads the script name followed by whitespace and a dash, which is the same operationalisation the frame measured with. The exemption it defends is deliberate: requiring a path in every document naming an unwrapped script would touch six files for a script nobody is told to run |
| The two places a document shows the generator being typed are spelled `round-record`, which is how every sibling command is spelled where a document shows one | `skills/code-review/orchestration.md#"`round-record close --item <dir> --round N --fixes <file> --range <a>..<b>`"@5bfd2f3a` | **Read** 2026-09-14, per place: `skills/code-review/orchestration.md` held both, at the `close` invocation and inside the `--written-late` fence. Both now read `round-record`, and `tests/test_docs_line_wrap.py` is green for the four covered documents this work edits | 2026-09-17 | This is the half #318 asked for and the smaller half. Thirty-four of the thirty-six mentions describe the generator rather than invoking it, and three of the four agent segments in the incident were `warden`, whose definition never tells it to run the script at all — so a wrapper alone would not have reached them. `plan.md` §*Alternatives considered* holds the rejection of each half taken on its own |

<!-- specs/1789347354-a-wrapped-terminal-line-is-not-one-value -->
### 1789347354-a-wrapped-terminal-line-is-not-one-value

<!-- Five rows over a change that is one regular expression, its sibling, and
the prose of six carriers. The first two are the two patterns, each with the
mutation that killed it; the third and fourth are the two rules Q3 split,
which are the only parts of this work that can go false without anybody
editing a file this branch wrote; the fifth is a defect this work found in the
checker that was supposed to tell it whether its own edit had landed. -->

###### A wrapped terminal line is not one value

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| The terminal value joins a continuation whose first characters look like a block opener, and stops at a line that IS a block — and the guard is a narrowing, so an indented line, an HTML tag and `**bold**` are joined rather than guessed at | `skills/code-review/scripts/round_record.py#BLOCK_START@d248831b`, `skills/code-review/scripts/round_record.py#terminal_value@53032294`, `tests/test_the_record_is_generated.py#test_the_two_spellings_differ_only_by_the_fence_openers@642ad763` | **Executed** 2026-09-14 by mutation, one alternative at a time, each killing its own arm and no other: the `#` lookahead kills both `#N` continuations; the `[-*+]` lookahead kills `**bold**`; the three run-of-three alternatives kill `---`, `___` and `***`; the setext alternative kills `===`; `[.)]` narrowed to `\.` kills `1)`; and the whole-line `$` kills the `--- and the rest of the clause` arm. Seven of the eleven new cases were seen red against the constant at `5e09345` before it was replaced; the module went 107 → 118 green, and 119 once round 1's fix pass added the pin below. **The two spellings are pinned against each other rather than shared**: adding an alternative to the model and not to this module left both modules green at 167 passed, which round 1 measured and this case now turns red | 2026-09-14 | The paste-ready form in `seal/specs/1789034970-…/rounds/round-4-report.md` was re-derived rather than transcribed, and the re-derivation changed the claim: that report's four continuation shapes are red against the widened pattern `3b228f4` shipped, which this clone cannot resolve, and only the two `#N` shapes are red against this branch's base. The two arms that no candidate pattern ever cut are labelled `boundary` in the parametrisation rather than implying a demonstration that did not happen. The whole-line `$` arm is this work's own find: killing that anchor left the entire module green, and it is the one false-positive direction `.github/scripts/issue_claims_check.py` pins and this module did not **Re-read 2026-09-23 by work item 1790173106 (#138), which drifted it.** The unit gained a refusal for a bare `yes` AFTER the join; the join and its three stops are unchanged, so what this row measures still holds. **Re-read 2026-09-24 in that item's round 1 fix pass**, which reworded that refusal's sentence to name each row's own reason; the join is still untouched |
| The sibling segmenter reads a sentence wrapped onto `#120` or `**round 4**` as one sentence, so an n-gram can cross the wrap | `skills/code-review/scripts/survivor_check.py#BLOCK@940410ed`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_whole_line_of_one_marker_ends_the_segment@8d644199`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_ordered_list_item_ends_the_segment@af65574c` | **Executed** 2026-09-14: the `#N` arm seen red against `BLOCK` as it stood, then green; the module whole is 49 green, 47 before, 53 once round 1's fix pass pinned the whole-line alternative at four shapes, and 55 once round 2's pinned the ordered-list one at two. **Which of the constant's five alternatives are pinned depends on the direction of the mutation**, measured 2026-09-14 by mutating each in turn, and the row says which rather than claiming the class. Round 3 corrected a count here that read as though the direction did not matter: killed outright, two are caught and three are not; with the lookahead dropped, three are caught and the ordered-list one is not. #393 carries the two arms that would close the remainder. The measurements: killing `[-*_=]{3,}\s*$` turns four arms red and narrowing it to `[-*_]{3,}` turns the `===` arm alone red; killing `\d+[.)](?=\s)` turns two arms red, though **dropping that alternative's lookahead turns nothing red**; the bullet and heading alternatives are pinned only through their lookaheads, each dropping to one red arm of `test_a_sentence_wrapped_onto_an_issue_number_is_one_sentence`, and killing either outright turns nothing red; and **`>` is pinned by nothing in either direction** | 2026-09-14 | Q4's answer, and it is the same class with a different consequence: nothing is truncated, the sentence is mis-scored, and a survivor whose evidence straddles the wrap is unreachable however high its score would have been. No fence opener is added — this module runs after `blank_struck`, where a fence line is prose that scores nothing — and `>` keeps no space requirement because the markdown needs none. **The whole-line alternative `[-*_=]{3,}\s*$` was pinned by nothing and now is**, at all four shapes: round 1's 🟡 5 answered the build's deferral by pointing out that phase 4 retyped the constant whole, which is the cheapest moment it will have. Round 1's own paste-ready form of that case stayed green under the mutation, because it put a full stop above the rule and `END` ends a sentence there whatever `BLOCK` does; the committed form carries no sentence punctuation, so the block boundary is the only thing that can end it, and all four arms go red when the alternative is killed. **The same trap sat in round 2's paste-ready for the ordered-list case and was caught the same way**, which is what made it the shape to check rather than an accident: its `1.` arm is green whatever `BLOCK` does, so the committed case takes `1)` and `12)` and leaves the `.` half to the comment. Round 2's 🟡 1 is why the ordered-list alternative was the second taken and `>` was not — killing the bullet or heading alternative widens the pattern in the direction this work item argues for and is caught from the other side, where killing this one is caught from neither and merges a real list item into the sentence above it |
| The conformance statement has one owner and two links: the protocol states that a wrapped terminal line is one value and where the join stops, and the warden and the template each name that section rather than restating it | `` `docs/review-handoff-protocol.md#"#### The Needs a fix field — the answer a run ends on"@447a865a` ``, `tests/test_the_rules_have_one_owner.py#RULES@6be9f0b8` | **Executed** 2026-09-14: rule 11 entered in the registry, and each half seen red by stashing the sentence it pins — the owner sentence removed turns `test_the_owner_states_the_rule[11 …]` red, and the template's link and the warden's link each turn `test_every_link_names_the_owner[11 …]` red on their own | 2026-09-22 | Q3 answered **(b) two rules**, and the row is the conformance half alone. Q3 closes with *neither needs a registry row*, which is read here as *neither needs a row to reconcile the two* — the conformance rule taken by itself is one owner and two links, which is what `RULES` models, and `plan.md` phase 3 asks for a case in exactly that file's shape. The reading is recorded rather than sent back to the owner, per the batch this work item spends once **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** `RULES` gained rules 13 and 14, one owner and their links each. Rule 11's owner sentence and its two links are unchanged, and the case that pins them is green, so the claim holds. **Re-read again 2026-09-22**, in round 1's fix pass. Comments were added above rules 13 and 14 recording what each row pins and what the link assertion cannot see. No row, needle or assertion changed. |
| The warden keeps the operative instruction — leave a blank line under the pair — and carries neither the owner's sentence nor its enumeration of the three stops | `tests/test_the_rules_have_one_owner.py#test_the_wrap_rule_is_two_rules_and_the_warden_keeps_the_operative_one@9c1d2538`, `` `agents/warden.md#"## Paste-ready fixes"@63474742` `` | **Executed** 2026-09-14: the case seen red three ways — the blank-line instruction stashed, the protocol's narrowing sentence stashed, and the three-stop enumeration put back into the warden | 2026-09-14 | The third assertion is what makes the split falsifiable. Without it the warden could drift back into restating the owner one sentence at a time and only the first sentence would be caught. **The anchor on `agents/warden.md` is not the section a reader would name**: the wrap paragraph is in §*Report*, and the row below says why the checker cannot see it there. **Re-read 2026-09-24 by work item 1790174138 (#503, #437)**, which added one paragraph under §Report's paste-ready fence — `###` allowed, `&lt;!--` for an opener; the heading stays in a fenced block of the reviewer's contract. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (⬜ 4)**, which made the section-end sentence under §Report's paste-ready fence name both enders; the heading stays in a fenced block of the reviewer's contract |
| A ledger anchor on a markdown section whose body holds a fenced block with `#`-prefixed lines silently stops at the fence, so an edit after it drifts nothing | `skills/evidence-check/scripts/evidence_check.py#file_units@51c206a6` | **Executed** 2026-09-14 by calling `file_units` on `agents/warden.md`: it reports `"## Report"` as lines 285–355 and four further units named `"## Verdicts"`, `"## Executed probes"`, `"## Deferred"` and `"## Paste-ready fixes"` — which are the report's own example headings, shown inside a fence so a reviewer can copy them. `bin/evidence-check .` exits 0 with `1181 ok · 0 drifted · 0 broken` after this branch rewrote the paragraph at line 406 | 2026-09-14 | Found because `spec.md` §*Data & interfaces* predicted `agents/warden.md#"## Report"` DRIFTED and it did not. **`seal/ledger.md` R7's anchor does not cover half of what R7 claims**: the row asserts the report carries the three tables *plus the two terminal lines*, and the terminal lines are at 400–405, outside it. The class is three of the thirty-six markdown files carrying a ledger anchor — `agents/warden.md`, `README.md` and `README.ko.md`, the last two truncating their top-level unit at line ~301 of ~655 on shell comments inside install fences. Fixing the reader re-hashes rows across the whole ledger and is a change to a gate, so it is out of this work item and handed over. **Re-read 2026-09-24 by work item 1790174138 (#503, #437)**, which added example rows and four sentences to §Report; the sentence this row pins is unchanged |

<!-- specs/1789356180-the-two-halves-of-one-generator-refuse-each-other -->
### 1789356180-the-two-halves-of-one-generator-refuse-each-other

<!-- Seven rows over four phases, cut the way `spec.md` cuts the tickets — by
the column of `rounds/round-N.md` each claim is about. The first three are the
`#` column and the verdict vocabulary that reads it, the next two are the
Grounds cell, and the last is the `Fixes checked by` row. The sixth is a
correction to a standing `# RIDER:` whose stated reason measurement did not
support, recorded as a row because the rider it replaces is gone. -->

###### The two halves of one generator refuse each other

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A `#` cell with no digit is a verdict row that commissions nothing — copied into the record as written, never keyed, never asked for a fix-table row, never counted toward `Pass` — **unless the cell is empty or carries 🔴 or 🟡**, the two severities that mean somebody owes the row an answer, which are refused. A cell carrying digits that is not a bare integer is still refused | `skills/code-review/scripts/round_record.py#finding_number@d1579ea7`, `skills/code-review/scripts/round_record.py#DIGIT_RE@2dc5ff00`, `skills/code-review/scripts/round_record.py#verdict_rows@1fa83cac`, `tests/test_a_finding_id_is_a_bare_integer.py#test_six_rows_with_no_id_stand_beside_one_finding_and_commission_nothing@a1f0d95e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_every_no_digit_shape_the_corpus_holds_is_admitted@828cca68` | **Executed** 2026-09-14, every case seen red against `e387bff` first — the six confirmation rows produced `the verdict table has a row whose `#` reads '✅'` and exit 2. The discriminator is the corpus's and was re-derived rather than read: of the 253 cells the old rule refuses, **199 carry digits and 51 carry none**, and the two populations do not overlap on anything a reviewer writes. Three mutations, each killed: admitting any malformed cell, admitting none, and dropping a malformed cell silently. **Round 1's 🔴 1 reopened this and the severity arm is its repair**, seen red 2026-09-14 over `🔴 A`, `🟡 A`, a bare `🔴`, `🟡 fix-surface` and an empty cell — before it, `\| 🟡 A \| … \| open \|` came through `new` at exit 0 and the record was written with `- [x] Pass` beside an open finding in its own verdict table | 2026-09-15 | **The grounds first written for this row read the corpus backwards, and round 1's 🟡 2 is the correction.** The count 51 is right; the reading of it was not. Re-derived through the module's own `table_body`: **44 of the 51 are a severity marker and a single LETTER** — `🔴 A` through `🟢 O`, a finding id in the wrong alphabet — and only **7** are the shape this rule admits (`carried` twice, `🟢 fix-surface` twice, `🟢 fragment`, `🟢 grep`, `🟢 overview`). Neither `✅` nor a bare em dash — the two exemplars five documents led with — occurs in a committed record at all; the 21 bare em dashes are in reviewers' REPORTS, a different corpus. All 44 predate `round_record.py` by two days, so the shape is not what a reviewer reaches for today either. That is why the severity is read as well: it is 44 rows of a mistake the admission would otherwise make silent, and all 26 no-digit cells carrying 🔴 or 🟡 are genuine findings (11 `fixed`, 4 `answered`, 11 `open`). **What still fails, stated:** a reviewer who writes 🟢, ❓ or ⬜ on a row that IS an open finding is not caught — the check reaches the two markers that owe an answer and not a wrongly chosen one. `spec.md` §6 read this population as 54 and 1,992 verdict rows; executed here it is **51 and 1,989** over the 207 records that parse **Re-read 2026-09-15 by work item 1789425391 (#395), which drifted `finding_number` and `verdict_rows`.** The *what still fails* paragraph is the part this work changes: the verdict arm `1ff0a6c` reverted is back, so a row carrying 🟢, ❓ or ⬜ that IS open is caught where its Verdict cell says so — slipping through now takes two mistakes in two different cells. The admission rule itself is unchanged and so is every figure above; the third refusal condition is a row of this work item's own fragment |
| The refusal for a malformed id names EVERY offending row of the table, and it lands at `new` as well as at `close` | `skills/code-review/scripts/round_record.py#id_refusal@1eb57437`, `skills/code-review/scripts/round_record.py#build@ebe41de3`, `tests/test_a_finding_id_is_a_bare_integer.py#test_one_refusal_names_every_malformed_row@cf44943e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_malformed_id_is_refused_at_new_where_the_author_is@f1ca789e`, `tests/test_a_finding_id_is_a_bare_integer.py#test_a_hand_edited_record_still_meets_the_rule_at_close@ac175599` | **Executed** 2026-09-14. Five rows `R2-1` … `R2-5` produce one refusal quoting all five and the phrase `bare integer` exactly once; #303 measured the old message at two round trips per repair. The `new` half goes through `verdict_rows` on the report's own lines rather than through a new call site, so `test_the_rule_is_one_constant_both_tables_read`'s counts — one `FINDING_ID_RE.match`, three `finding_number(` — are unchanged and were re-run. Three mutations killed: never refusing, not naming the format, quoting only the first row | 2026-09-17 | `new` refusing does not retire the reading at `close`: a record is a file somebody can edit, and the hand-edit case holds that half open. Reading the report's verdict table at `new` also closed a disagreement nobody had listed — `build` derived `Pass` from every copied row, so a confirmation row reading `verified` counted OPEN and `new` would tick no box where `close` ticked one. **Re-read 2026-09-24 by work item 1790174138 (#382), which drifted `build`** at the `Target SHA` cell alone; the id refusal and its landing at `new` are untouched |
| `❓ out of verified scope` closes a finding without commissioning anything, and a severity marker leads a verdict cell without being part of it | `skills/code-review/scripts/chain_check.py#OUT_OF_SCOPE@dd4e3e65`, `skills/code-review/scripts/chain_check.py#MARKER@4be749dc`, `skills/code-review/scripts/chain_check.py#verdict_of@5b8a6124`, `tests/test_the_fixes_close_the_record.py#test_a_scope_marker_keeps_its_own_word@b8cb102d`, `tests/test_the_fixes_close_the_record.py#test_a_fix_row_for_a_scope_marker_is_refused_as_already_closed@d4120899` | **Executed** 2026-09-14, both cases seen red first. The marker strip was measured BEFORE it was taken: applied to all **1,989** committed verdict rows it changes the reading of exactly **one** cell, and that cell is `❓ out of verified scope` in `1789081272-…/rounds/round-1.md` — the row #353 is about. Two mutations killed: removing the word from `CLOSED_WORDS`, and dropping the marker strip | 2026-09-14 | It is in `CLOSED_WORDS` and in neither `FIX_WORDS` nor `HOME_WORDS`, so it closes without commissioning a reader — the three import-time assertions that hold those sets in step are unaffected. The `#` column could not reach this at all: #84's finding 15 carries the id `15`, so the row that #353 is about is keyed and the repair had to be in the Verdict column |
| The fix pass's Verdict cell holds the word alone and everything after it goes in `Commit or grounds`; `deferred <home>` is the one word carrying its own suffix, and a cell that begins with `fixed` or `answered` and carries more is refused naming the row to write | `skills/code-review/scripts/round_record.py#fix_table@d2c83250`, `` `docs/review-chain-spec.md#"### The last round verifies, and what it verifies is a diff"@963da078` ``, `tests/test_the_rules_have_one_owner.py#test_a_correction_row_closes_answered_and_never_fixed@d6035e2b`, `tests/test_the_fixes_close_the_record.py#test_the_suffixed_verdict_cell_is_refused_naming_the_two_cell_shape@a4233f32` | **Executed** 2026-09-14: `close` over `\| 1 \| answered — corrected at 6883bfb \| x \|` — the exact spelling `docs/review-chain-spec.md` prescribed — exits 2 with `is none of `fixed`, `answered`, `deferred <home>``, naming three words without saying the cell had begun with one of them. The new arm was seen red on that message. One mutation killed: removing the arm. **Round 1's 🟡 3 found the arm advising a row it then refuses** — `head` was cut at the first `SEPARATORS` character found ANYWHERE in the cell, and `SEPARATORS` begins with a space, so `answered, corrected at <sha>` printed `\| 1 \| answered, \| … \|`. `head` now comes from the arm's own test. Seen red 2026-09-14 by pasting the printed row back into `close`, over four separators — every one that touches the word was wrong, not the comma alone | 2026-09-24 | Both halves of #341's comment hold, and the four cases beside this one were GREEN before any edit: they pin the spelling `agents/smith.md` already prescribed and `fix_table` has always accepted. The live defect was the message a reader met when they followed the owner instead. The owner's own sentence still NAMES the one-cell spelling, to say what it cost — so the case asserts the prescribing clause is gone rather than the string, which is a distinction a blunter pin erases **Re-read 2026-09-14 in the post-gate fix pass, and the drift is `8d8a753`'s rather than the revert's** — `1ff0a6c` changed only lines under the section on a verdict row that commissions nothing, outside this unit, while `8d8a753` rewrote *until 0.11.4* to *until the release that corrected it* inside it. Both halves of the claim stand: the section still reads *the Verdict cell holds the word alone and the correcting commit goes in `Commit or grounds` beside it*, and `already deferred` is still grounds with the verdict cell reading `deferred <home>`. The prescribing clause the case asserts is gone is still gone; only the release name in the sentence recording what it cost changed. **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The subsection now ENDS where the filing ladder's new `###` heading opens, so it no longer encloses the homes table or the paragraphs after it. The two-cell verdict rule sits near the head of the subsection and stayed inside it; `test_a_correction_row_closes_answered_and_never_fixed` is green, so the claim holds. Re-read 2026-09-24 — #526's split re-pointed the anchored case to the document that now holds the section it reads, and widened any absence it asserts of the spec to all three documents; the claim unchanged. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** the section changed only in the paperwork sentence's record paths (and its reflow); the Verdict rule is unchanged, so the claim holds **Re-read 2026-09-26 in work item 1790381329, round 1's fix pass (⬜ 3):** the case's docstring now says the check refuses `Pass` beside `nobody` on the last record *at a ready pull request*; its assertions are unchanged, and the claim holds |
| No verdict word discards the Grounds cell it was not asked to change: all three join the reviewer's grounds after `; `, and a `deferred` row keeps the fix pass's prose beside its home | `skills/code-review/scripts/round_record.py#close@b91242ae`, `tests/test_the_fixes_close_the_record.py#test_no_verdict_word_discards_a_cell_it_was_not_asked_to_change@f83cc267`, `tests/test_the_fixes_close_the_record.py#test_a_deferred_row_keeps_the_fix_passs_reasoning@124cce46` | **Executed** 2026-09-14: the parametrised case red on exactly two of three words before the fix — `fixed` was already the only one that preserved what stood — which is the enumeration §12 asks for, since #391 names one row and the class is three wide. Two mutations killed: dropping the join, and discarding a deferred row's third cell again. **Round 1's 🟡 4 found the home printed twice** where the third cell BEGINS with it: the guard was exact equality, so `\| 1 \| deferred #309 \| #309 — the parity arm is out of scope \|` — #391's own worked example — landed as `#309 — #309 — the parity arm …`. The home now comes off the front of the note; seen red on that exact row | 2026-09-24 | Q6 is answered **no migration arm**, measured rather than argued: 66 committed rows read as their home alone, and only **15** have a committed `round-N-fixes.md` beside them, of which **11** carried prose the record does not hold. For the other 51 the prose is not recoverable from anything in the tree, because a fix table is an input file and is usually not committed — so a migration arm could not run over them even if one were wanted. Rewriting the 11 would assert a state untrue at the record's own `Target SHA`. Nothing machine-reads a verdict row's Grounds cell; `round_record.py` is its only reader. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1), which drifted `close`.** `--broad-gate` now writes the cell through `kept_broad_gate`, the one path `seal` takes, so a run the cell already holds is kept; the table, the range, the surface and the ordering this row is about are untouched. Re-read 2026-09-24 — #556 brought the comment above `close`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| The cut that removes a fix commit from the third cell takes the commit's own code span, so no empty code span is left beside it — and `chain.SEPARATORS`, shared with the `deferred` home reader and with `chain_check`'s readers, is not widened | `skills/code-review/scripts/round_record.py#fix_table@d2c83250`, `tests/test_the_fixes_close_the_record.py#test_a_fix_commit_carries_no_empty_code_span@8da3395f`, `tests/test_the_fixes_close_the_record.py#test_the_empty_span_repair_did_not_widen_the_shared_separators@72f99e94` | **Executed** 2026-09-14. The shape is on **210** committed verdict rows, not the 103 `spec.md` §6 read. Probed after the repair: `\| 1 \| fixed \| `511d6b2` — `token_totals` is summed before the guard \|` lands as `fixed at 511d6b2 — `token_totals` is summed before the guard`, with the second code span intact. One mutation killed: narrowing the cut back to the hex | 2026-09-14 | **The standing `# RIDER:` at this coordinate was right about WHERE and wrong about WHY, and the correction is the reason this is a row.** It said widening `chain.SEPARATORS` would strip the backticks off a home deliberately written as a code span. Measured 2026-09-14: `chain.EMPHASIS` is ``[*_`]+`` and `fix_table` applies it to the verdict cell one line before `SEPARATORS` is reached, so `` deferred `seal/follow-up.md` `` already arrives stripped. The conclusion still holds for the constant's other two callers, which nothing here measured, so it is left alone — and the case pins the constant and the cut site rather than the consequence that turned out not to exist **Re-read 2026-09-26 in work item 1790381329, round 1's fix pass (⬜ 3):** the comment beside the case's `close` call now says the check refuses that pair at a ready pull request and that this run is judged as a draft, where it prints; its assertions are unchanged, and the claim holds |
| `close` writes both of `landing_values`' answers: `no fixes to check` where nothing closed on a fix word, and a reason saying the fixes are written and no round has opened them where one did. It corrects only the landing value, never a cell naming a `round-N` | `skills/code-review/scripts/round_record.py#WRITTEN_CHECKER@5afc4524`, `skills/code-review/scripts/round_record.py#landing_values@1acda347`, `tests/test_the_fixes_close_the_record.py#test_the_checker_cell_stops_saying_the_fixes_are_not_yet_written@60a45a0a`, `tests/test_the_fixes_close_the_record.py#test_neither_chain_check_arm_turns_red_on_the_new_reason@2c174bd4` | **Executed** 2026-09-14, seen red twice — against `e387bff` and again against the committed phase-3 tree after the two phases were split back apart. Q5 measured before the wording was written: `chain_check.checked_by` never mentions `NOT_YET` and splits the word from its reason, and `fix_surface`'s pending arm gates on `CHECKER_RE`, which matches `round-N` and neither spelling of `nobody — <why>` — so **no arm turns red**. Two mutations killed: leaving the cell alone, and rewriting it over any standing value | 2026-09-14 | `nobody` is right and only the reason was wrong, which is narrower than #273 part 1 reads: the ordering rule requires a checker to be a LATER round and at that moment none exists. What makes it worth correcting is the capped run's last record, where no next `new` exists and whatever stands there stands permanently. `landing_values`' docstring carried the old behaviour as deliberate — *the cell stays at the landing value for `new` of the next round to set* — which is true of WHO and false of WHAT, and it is rewritten rather than contradicted **Re-read 2026-09-26 in work item 1790381329 (#613):** `landing_values`' docstring now says the check refuses `Pass` beside `nobody` on a capped run's last record *at a ready pull request*; no code in the unit changed, and the claim holds |
