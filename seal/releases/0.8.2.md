## 0.8.2 — 2026-09-06

<!-- specs/1788613827-a-runs-report-carries-one-comparison-table -->
### 1788613827-a-runs-report-carries-one-comparison-table

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Six rows. Most of this branch is prose that a case reads sentence by
sentence, so a coordinate for each sentence would be an inventory of the
diff. What earns a row is a judgment a later tidy-up would undo in good
faith: the token line's counting rule and the direction each way of
degrading takes (R1), two turn counters that look like one duplicated
(R2), a table stated in one place with its rows named rather than left to
each run (R3), the two carriers that point at that table instead of
copying it — one of which guards its own refusal list against the owner
renaming a row (R4, R5) — and one guard per transcript value, where the
tidy-up is inlining the guard back into its one caller (R6).

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run; the
unscoped READ form ran afterwards and its lines are in `phases/phase-4.md`,
and again in round 1's fix pass.

Round 1's finding 9 is why four of the five rows carry a correction note.
Its finding 2 falsified R1's own claim, so R1 is corrected here rather than
re-pointed — the claim went with the behaviour, not with the code the anchor
names. The other three carry what the fix pass changed about them.

Round 2 added R6 and re-stamped R2, R3 and R4, whose anchors its fixes
edited. R6 is the row round 1 should have written: its finding 2 closed six
members of a class by reading the file, and the seventh was three lines from
one of the six. The row states the guarantee over the whole class rather
than over the members that had been found, and its evidence is an
enumeration instead of a reading. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · the token line sums `output_tokens`, `cache_creation_input_tokens` and `cache_read_input_tokens` over the transcript it was given **and** every `*.jsonl` under the `<session-id>/subagents/` directory beside it, counting one turn per assistant message carrying `usage` and each message's usage once however many rows it is split across, and summed before the no-tool-calls guard rather than after it; no failure ends the report, almost every one makes the totals smaller, and the one that goes the other way is a split message carrying no usable key, which counts once per row instead of once | `skills/verify/scripts/session_cost.py#token_totals@d6239cb8`, `skills/verify/scripts/session_cost.py#subagent_transcripts@469d2608`, `skills/verify/scripts/session_cost.py#count@d43c2dd2`, `skills/verify/scripts/session_cost.py#message_key@ae0dd5d2`, `tests/test_session_cost.py#test_the_token_line_sums_the_run_not_just_the_transcript_it_was_given@309bb25c`, `tests/test_session_cost.py#test_each_column_is_its_own_usage_field@7503144a`, `tests/test_session_cost.py#test_a_split_messages_usage_counts_once@a638ed0c`, `tests/test_session_cost.py#test_a_transcript_that_cannot_be_opened_is_skipped_rather_than_raised@cc01a787`, `tests/test_session_cost.py#test_the_printed_token_line_names_the_transcripts_it_covered@d31a5b06`, `tests/test_session_cost.py#test_a_transcript_with_usage_and_no_tool_call_still_reports_its_tokens@9ce5afef`, `tests/test_session_cost.py#test_a_usage_value_that_is_not_a_number_contributes_zero@f92ebfb9`, `tests/test_session_cost.py#test_a_message_id_that_is_not_a_key_does_not_end_the_report@39000c3b` | **Executed** 2026-09-05 in phase 1, re-read 2026-09-05 in phase 4 at `c720eb5`, and re-read 2026-09-05 in round 1's fix pass at `a306d18`: the hand sum over a run with one subagent reads 2 transcripts and 13 · 130 · 1300; three rows each spending in one field only keep the three columns apart; one message written as two rows counts one turn and its usage once; a path that does not exist returns one fewer transcript and that segment's spend missing, with nothing raised; the printed line reads `tokens 3 transcripts, 3 turns` with `1,234` · `5,678` · `9,012` beneath it; a transcript carrying `usage` and no paired tool call now exits 0 with the token block rather than exit 1 with an empty stdout; `output_tokens` as the string `12` counts zero where it raised `TypeError`, and a `message.id` written as a list keys by row position where it raised `TypeError: unhashable type` | 2026-09-05 | **The three fields are the claim, and a fixture whose fields move together cannot check it** — `cache_creation_input_tokens` and `cache_read_input_tokens` differ by one word in the transcript, so `test_each_column_is_its_own_usage_field` spends in one field per row. **Round 1's finding 2 found this row's own claim overstated, and the row is corrected rather than re-pointed**: it read *every failure makes the totals smaller rather than raising*, and two shapes raised — a `usage` field that is not a number, and a `message.id` that is not hashable. Both are guarded now, so nothing ends the report; what could not be made true is the ONE direction, because a split message with no usable key is counted once per row and that is larger, not smaller. Which is why both printed counts matter rather than only `transcripts` — that one covers the files the walk OPENED, not the files that contributed, and `turns` is where a doubled run would show. The same report's `Agent` call count is the number a reader holds `transcripts` against, and phase 1 measured the pair at 18 against 17 on a real run. `os.walk` rather than `os.listdir` because a harness that nests one segment's transcripts under another still has that spend counted; the `.jsonl` filter is load-bearing rather than cosmetic, since the real directory carries a `.json` sidecar beside every transcript and dropping the filter would double every count (`phases/phase-1.md`) Re-read 2026-09-09 for #200 · #202 · #193, which changed four units this row anchors on; the claim is unaffected and the hash is the new content of the same units. |
| R2 · the token line's turn count and `tools_per_turn`'s denominator are two counters on purpose — a turn here is an assistant message carrying `usage`, there a message carrying a `tool_use` — and both the script and the printed report state the difference where each is computed | `skills/verify/scripts/session_cost.py#load@63fa07c7`, `skills/verify/scripts/session_cost.py#token_totals@d6239cb8`, `skills/verify/scripts/session_cost.py#report_tokens@3b4bed25`, `tests/test_session_cost.py#test_the_token_turns_and_tools_per_turn_count_different_things@0332e678`, `tests/test_session_cost.py#test_the_report_tells_its_two_turn_counts_apart@69115323` | **Executed** 2026-09-05 in phase 1, re-read 2026-09-05 in phase 4 at `c720eb5`, and re-read 2026-09-05 in round 1's fix pass at `a306d18`: one fixture separates them — two turns sent a call and a third only spoke — and reads `tokens.turns == 3` beside `tools_per_turn == 1.0`; the printed report now names each count's own scope, the token line as *a turn is any assistant message, in every transcript counted* and the batching line as *2 calls over 2 turns that sent one, in this transcript alone* | 2026-09-05 | **The tidy-up this row exists against is merging them.** Two functions in one file counting turns two ways reads as a duplication, and the cheap repair is to reuse `load`'s `turns`. That denominator is what the per-segment bars in `docs/review-handoff-protocol.md` are calibrated against, so widening it to messages that only thought would move a published threshold with nothing saying it moved — `spec.md` §Scope's fourth *Out* and `questions.md` assumption 5. The other direction is as wrong: a turn that sent no call is not a turn the batching advisory can read anything into, and a turn that only thought is still a turn the run paid for. `token_totals`' docstring carries both halves. `load`'s own docstring carries the measurement behind its side — counting per `tool_use` block instead of per message pinned the ratio at ~1.00 structurally across five runs — so neither counter's reason lives only here. **Round 1's finding 5 found the two counts printed side by side with nothing between them**: 1.08 tools per turn beside 659 turns and 211 calls, which divides to 0.32. The distinction was in the docstrings and not in the report a person reads, and `analyse` returns `call_turns` for no other reason than to put it there Re-read 2026-09-09 for #200 · #202 · #193, which changed four units this row anchors on; the claim is unaffected and the hash is the new content of the same units. |
| R3 · `skills/verify/SKILL.md` §*Measure the segment, and feed the flow log* owns the run-level table: nine rows each naming where its number is taken from, the counting rule naming all three `usage` fields and the command, partial coverage stated in prose rather than as a column, no verdict, and the rolling log the section already names rather than a destination of its own; the `Location` buckets are the review chain's own definition of a record, the `Broad gate` row asks for what its cell carries, and the section says which file of a project directory is a run's main transcript | `` `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@70aef725` ``, `tests/test_a_segment_feeds_the_flow_log.py#test_the_location_buckets_are_the_repositorys_own_definition_of_a_record@07bf8a6b`, `tests/test_a_segment_feeds_the_flow_log.py#test_the_broad_gate_row_asks_for_what_the_cell_actually_carries@10942f7b`, `tests/test_a_segment_feeds_the_flow_log.py#test_the_section_says_which_transcript_is_the_runs_own@d89a8e34`, `tests/test_a_segment_feeds_the_flow_log.py#test_the_run_level_table_carries_every_row@1220eaa4`, `tests/test_a_segment_feeds_the_flow_log.py#test_every_row_of_the_run_level_table_names_its_source@14098cb5`, `tests/test_a_segment_feeds_the_flow_log.py#test_the_tokens_are_counted_rather_than_estimated@2114351a`, `tests/test_a_segment_feeds_the_flow_log.py#test_the_table_is_not_a_third_destination@0e023a9d` | Read 2026-09-05 in phase 4 at `c720eb5`; **executed** 2026-09-05 in phase 2, where each of the nine cases was seen red and twelve mutations were killed, two of them re-run against a table that exists after the first red proved only that the table was absent. **Re-read 2026-09-06 from work item 1788661274's phase 3**, whose phase 2 replaced the section's two-logs sentence and so drifted this anchor: the run-level table is untouched — nine rows, their sources, the counting rule, the prose about partial coverage, and the transcript sentence all stand as round 2 left them, and none of the seven cases drifted | 2026-09-22 | **The source column is checked structurally, and that is what keeps the table from becoming a list of headings.** `test_every_row_of_the_run_level_table_names_its_source` parses the rows into `[label, source]` pairs and refuses an empty second cell, so a row added later without a source goes red rather than shipping a number the next run produces by feel. The module needed a second reader for it: its existing style collapses whitespace over a section body, which is right for prose and destroys a table into one line of pipes. `run_table_rows()` raises an `assert` naming the header rather than letting `list.index` raise, because deleting the table turns five cases red at once and a traceback four frames deep does not say the section carries no table (`phases/phase-2.md`). The rows name no issue number and no milestone — the skill ships to repositories that have neither, and `test_the_shipped_skill_names_no_repository_specific_tracker_state` reads the same section. **Round 1 corrected two rows and added a sentence, and the row is corrected rather than re-pointed.** The `Location` row split `ledger` from `record` and the share row counted `seal/specs/**` alone, where `docs/review-chain-spec.md` puts a work item's documents and the ledger in one bucket — measured on this branch, the share row reads 45 % as it was written and 48 % under the policy, and a table that can be filled two ways is not a comparison. #170's own §*The rule* states both cells verbatim, and a ticket ranks below a ratified document. The `Broad gate` row asked `how many times` from a cell `round_record.py` replaces rather than appends; whether that cell should hold one entry per run is #174. And *the run's main transcript* named no file, while `--latest` takes the newest file anywhere under the project directory — which on this very fix pass was a segment. **Round 2's finding 3 corrected the same row again**: it defined a record as anything under `seal/`, where `docs/review-chain-spec.md` names three paths and `ls seal/` also lists `config.md`, `follow-up.md` and `README.md`. The row now enumerates the three and says where the other three files go. The case that pins it asserted a bare `` `seal/` `` and stayed green over the corrected wording, because the sentence naming the three contains that substring — so it asserts the three paths one at a time instead, and refuses the old sentence verbatim **Re-read 2026-09-09 for #300**, which rewrote the section's between-the-rows paragraphs and so drifted this anchor. The run-level table is untouched — nine rows, their sources, the counting rule, the partial-coverage prose and the transcript sentence all stand — and none of the seven cases drifted. **Re-read 2026-09-13 for #350**, which rewrote step 1 of this section — the measure step now names `session_cost.py --segments` in place of one invocation per transcript and of the by-hand resume split — and reworded the orchestrator paragraph so that a spawn cycle is no longer called a segment. This claim is about a different part of the section and is untouched; the hash is the new content of the same anchor. **Re-read 2026-09-22 for the flow-log posting command**, which named `session-cost … --post` in place of the two by-hand lookups and the post step, and kept both `gh` forms in the prose beside it. Step 1, the run-level table and the orchestrator-boundary paragraph are untouched. This claim is about a different part of the section; the hash is the new content of the same anchor. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which changed the `Broad gate` row of the run-level table in this section to read the count off the cell — one entry per run, newest first; nothing this row is about moved Re-read 2026-09-24 by work item 1790174138 after the sealer's refusal: the case now refuses a count only where the row's source does not say the cell holds one entry per run (#174 made the count readable off the cell); the claim holds **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** only the `Location` row's record paths gained `seal/releases/`; the nine rows and the counting rule are unchanged, so the claim holds |
| R4 · `docs/review-handoff-protocol.md` §*After the run — the per-segment bars* says the bars judge a segment against its kind and the run-level table judges a run against the last run measured, points at the table's owner by file **and** section, and says the table is not a destination of its own | `` `docs/review-handoff-protocol.md#"### After the run — the per-segment bars"@3ab2569c` ``, `tests/test_the_handoff_before_round_one.py#test_the_bars_and_the_run_level_table_judge_different_things@9be57594`, `tests/test_the_handoff_before_round_one.py#test_the_refused_rows_are_the_owners_own@b9a9c524` | Read 2026-09-05 in phase 4 at `c720eb5`; **executed** 2026-09-05 in phase 3, the assertions seen red one mutation each against a paragraph that exists — the path removed, the section name replaced, the not-a-destination clause deleted | 2026-09-05 | **The case reads a section, not the file, and that is load-bearing here specifically**: `session_cost.py` is named two sections above the bars, so a whole-file search for the pointer would stay green with this paragraph gutted or moved out. The absence half is asserted as hard as the presence half — every one of the owner's nine row labels is refused, because a copy of the rows in a carrier is a second thing to keep in step and the copy that drifts is the one nobody owns. **Round 1's finding 8 is why it is nine and not three**: the refusal covered three labels while the paragraph beside it glossed five of the rows in lowercase prose, which matched none of them. The gloss is gone and `test_the_refused_rows_are_the_owners_own` holds the nine to the owner's wording, the same coupling `tests/test_the_chain_section_has_one_shape.py` keeps over its own list. The pin belongs in this module rather than a new one because the module already owns this document and already pins this very section (`test_the_protocol_names_a_bar_per_segment_kind`), so a reader asking what the bars judge finds both answers in one place (`phases/phase-3.md`). **Round 2's finding 2 found the widening aimed at the wrong half.** The nine labels catch a paste-back OF THE TABLE; the prose finding 8 removed was a lowercase gloss — `rounds, wall clock, commits, findings and tokens` — sharing no substring with any label. Measured: replacing the sentence with the gloss is already red, because `what the whole run cost` goes out with it, but ADDING the gloss beside the sentence passed all 43 cases across the three modules that read this document. So the gloss is refused by its own removed wording, the pattern `test_the_protocol_no_longer_states_the_rules_it_points_at` already uses on this file. A gloss written fresh is caught by neither and by no string: the words are lowercase and generic, and `rounds` occurs twice in that section legitimately — which the docstring now says, because a refusal list that reads as though it closed the class is worse than one nobody expected to |
| R5 · `skills/commit-pr-convention/SKILL.md` §*Pull request bodies* states the chain section's shape — the comparison table first, then what the rounds found, for a work item routed through the review chain — and points at the table's owner rather than copying its rows; the refusal list is itself held to the owner's current wording | `` `skills/commit-pr-convention/SKILL.md#"## Pull request bodies"@9b68f07b` ``, `tests/test_the_chain_section_has_one_shape.py#test_the_section_points_at_the_tables_definition_rather_than_copying_it@4c471de4`, `tests/test_the_chain_section_has_one_shape.py#test_the_pinned_rows_are_the_owners_own@151e96aa` | Read 2026-09-05 in phase 4 at `c720eb5`; **executed** 2026-09-05 in phase 3: the shape sentence seen red stripped of its condition, the pointer red with the path removed and again with the section name replaced, and mutation 11 renamed `\| Findings by severity \|` in the owner's table and watched `test_the_pinned_rows_are_the_owners_own` go red | 2026-09-05 | **A refusal list of another file's strings rots silently, which is why the second case exists.** The first case refuses three row labels taken verbatim from the owner's table; if the owner renames a row, that refusal keeps passing while guarding a string nobody would paste — a green case over a rule that has left the repository. The second asserts the three are still the owner's own. The pin is its own module rather than a case in an existing one: `tests/test_the_pull_request_language_is_the_repositorys.py` holds this skill as its constant but every one of its cases is about the language row, and `tests/test_a_segment_feeds_the_flow_log.py` pins the table's owner rather than its carriers, so a reader asking why the chain section has this shape would open neither (`phases/phase-3.md`). This file is in `tests/test_docs_line_wrap.py`'s `COVERED` at 88 display columns, unlike the two files phase 2 touched |
| R6 · every value `session_cost.py` takes out of a transcript is type-checked before anything uses it as a dict key, an arithmetic operand or a format target, so **no JSON type a harness can write into a field ends the report** — and the guarantee is stated over the whole of that axis rather than over the members that have been found, because the axis was enumerated rather than read. **The axis is the one the enumeration below actually ran: the type each field carries, crossed with the field being absent.** What it does not reach is a VALUE of the type a field already carries. A naive ISO stamp and an equal pair of stamps are both `str` — the type `timestamp` carries anyway — and both ended the report until work item `1788700685` closed them at `parse_time` and `share`. A third member of that second axis is a `usage` field carrying `NaN` or `Infinity` — a `float`, so the type check passes it — which ends the report from `token_thirds`'s `round()` in every field but `output_tokens`, and is closed at `count` by a finiteness test **asked inside a `try`** — the test converts to float before it answers, so an `int` too large to have one made asking the question its own way of ending the report. What stays open on that axis is **not only** the wrong-number direction. A funnel answers for the values that enter it, and nothing answers for what the arithmetic makes of two that did: two counts `count` accepts as finite add to one it would refuse, and the rounding in `token_thirds` ended the report on that — measured at this row's own base as well, so it is a member the enumeration missed rather than a regression. That site is guarded; the class is recorded as a limit and handed to an issue, because enumerating call sites says which operations exist and never which values reach them. So *no member survived* below closes one axis of two, and a reader taking it for the whole class is reading half of one | `skills/verify/scripts/session_cost.py#tool_name@c9a85bac`, `skills/verify/scripts/session_cost.py#count@d43c2dd2`, `skills/verify/scripts/session_cost.py#message_key@ae0dd5d2`, `skills/verify/scripts/session_cost.py#parse_time@ffbd9c9f`, `tests/test_session_cost.py#test_a_tool_name_that_is_not_a_name_does_not_end_the_report@53babdd8` | **Executed** 2026-09-06 in round 2's fix pass, against `73e83d4` and again at `0143a55`. The enumeration: every field the two readers read, taken from `grep '\.get(' session_cost.py` rather than from memory, crossed with all seven types JSON can carry AND with the field being absent, run in both report and `--json` mode — 288 variants. Before the fix, one field survived: a `tool_use` block's `name`, in four variants across two distinct crash sites — `TypeError: unhashable type` at `analyse`'s `by_family` key for a list and for an object, with stdout empty, and `unsupported format string passed to NoneType.__format__` at `report`'s `{name:<12}` for a `null`, with 350 bytes of span and token lines already printed. After it, no member: 288 variants, every one exit 0. **Re-read 2026-09-06 from work item `1788700685`'s phase 2**, whose phase 1 changed the content of two anchors here — `parse_time` gained the zone normalisation and `count` gained a rider — and whose probe measured what the 288 variants reach rather than arguing it: against the module this row was stamped against, the `timestamp` field written as each of the seven JSON types and then removed exits 0 on all eight, while a naive stamp exits 1 with stdout empty and a zero span exits 1 after 36 bytes. The variants stand and the mechanism stands; the sentence saying what they cover is what changed | 2026-09-06 | **The tidy-up this row exists against is inlining `tool_name` back into its one caller**, which is `block.get("name", "?")` — the exact expression the defect was, and a reviewer reading the diff sees a one-line helper used once. Mutation 3 of the fix pass is that edit, and it turns the case red. **Two crash sites, not one, is the whole reason the guard is at the source**: the finding named the unhashable one, and a `null` name passes that site and dies in the printed report instead. Mutation 2 guarded only the shapes the finding named and was killed by the `null` case. **The method is the durable half.** Round 1 closed this class by reading the file, said six members, and left the seventh three lines below one of the six — the enumeration is what turns *I looked and found no more* into a number a later session can re-run and compare. `bool`, `int` and `float` names are not members: they hash and they format, so they print a number where a tool name goes, which is the wrong-value direction `count`'s docstring refuses to take for tokens and accepts here, since a name is not summed. **Work item `1788700685` found this row's guarantee stated one axis wider than its own enumeration, and the row is corrected in place rather than removed.** Removal is for a row whose anchor a change takes out of the tree; every anchor here still resolves, the four funnels still type-check, and phase 1 ADDED to two of them. What was over-stated is the reach: the enumeration crosses fields with JSON types, so a value of the type the field already carries is outside it by construction, not a member it missed — measured above, and the reason the repair is the axis named rather than the claim deleted. The second axis is `seal/ledger/1788700685-two-value-shaped-odd-rows-end-the-report.md`, whose R3 carries the decomposition. **This row narrows to the type axis rather than being re-asserted over the class with the value axis's members ticked off**, and round 1 of that work item is why: the value axis had a third member nobody had measured — a non-finite `usage` field, which ends the report from a `round()` the enumeration's node set could not see — and a row re-asserted over the class would have absorbed it silently. What a row states is only as wide as the method recorded beside it |

<!-- specs/1788632199-the-repository-ships-no-way-to-run-its-own-suite -->
### 1788632199-the-repository-ships-no-way-to-run-its-own-suite

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Eight rows over a branch that is one runner and five documents. What earns a
row here is a judgment whose repair looks obvious from the outside: a warm
call that checks its environment by asking a tool instead of by looking (R1),
an ignore that "uv already writes" (R2), a floor number repeated instead of
named (R3), a fallback command deleted as redundant or kept without its cost
(R4), a rule restated in its second carrier instead of linked (R5), a
runner moved to fix a concealment problem that never existed (R6), a
None-check beside a comparison that reads as redundant (R7), and a paragraph
of vocabulary that reads like a restatement of the table above it (R8).

R2, R3, R5 and R7 carry what review round 1 found: the ignore and the floor
each reached one path and skipped the others, and the class of segments that
run a test had two members and one fix.

R2 and R8 carry what round 2 found. R2's claim was rewritten a second time,
and the rewrite is the point: the ignore had been owed to a LIST of paths,
the list went short twice, and the second miss was created by round 1's own
fix. It is now owed to every exit of one function. R8 is round 2's other
finding read the other way round — the record's cell was correct and the
document that defines it was missing the generator's vocabulary.

R6 is the correction. Phase 1 justified the runner's location with a fact
labelled [executed] that phase 2 falsified with one `ls -a`, and the false
fact had reached three files and a test docstring first. The row states what
the placement actually rests on, so the next reader has the true reason where
the false one was.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run; the
unscoped READ form ran afterwards each time, its phase 3 output in
`phases/phase-3.md` and round 1's fix pass in `rounds/round-1.md`. The
scoped form cannot narrow below a file, so a blanket run would re-stamp
`seal/ledger.md`'s one pre-existing drift on behalf of a session that never
read it. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · `bin/test` pays for its environment once. A warm call decides by the filesystem alone — an interpreter and a `pytest*` script inside `.venv`, and the version `pyvenv.cfg` records — and never reaches the builder, not even to ask a tool whether the environment is current; a cold call reaches it exactly once; and a build that reports success while leaving no pytest behind is a sentence rather than a call that rebuilds forever | `.github/scripts/run_tests.py#has_pytest@2548b29f`, `.github/scripts/run_tests.py#ensure@2c6edb92`, `.github/scripts/run_tests.py#main@5c9e8019`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_built_environment_is_reused_and_never_rebuilt@d77ca17a`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_missing_environment_is_built_once@0d903e72`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_build_that_leaves_no_pytest_is_a_sentence@5fb2baeb` | **Executed** 2026-09-06 in phase 1 (cold 5.24 s, warm 0.60 s and 0.56 s over one 25-case module) and re-measured 2026-09-06 in phase 3 at `a192d47`: two consecutive warm calls of `./bin/test tests/test_the_suite_has_a_command_that_is_cheap_twice.py -q` from the worktree, **1.01 s and 0.66 s** wall for 37 cases, the interpreter line naming the reused `.venv` both times. The reuse case fails the run if `build` is reached at all, the cold case asserts it is reached once, and the half-built case exits with `still not in it` rather than looping | 2026-09-06 · 2026-09-24 | **The tidy-up this row exists against is making `has_pytest` sure.** Its check is `venv_python(venv).exists() and any(scripts.glob("pytest*"))` — filesystem only, no subprocess — and the obvious repair is to run `python -m pytest --version` and read the answer. That pays an interpreter start on every warm call, which is the cost the whole work item removes; the docstring says so where the repair would be typed. **The claim is not that the first call is cheap** — it is that it is the only one. Phase 1's cold figure was taken where uv's wheel cache and a 3.13 interpreter were already present, so on a genuinely cold machine the first call pays what #133 measured and every call after it still pays 0.6 s **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** `main` now calls `add_xdist` after `ensure` returned and appends `-n auto` unless `caller_decided` or the install failed (#337); the warm path still decides by the filesystem — `has_xdist` reads site-packages — and the builder is still reached through `ensure` alone. The claim stands. |
| R2 · the virtualenv is invisible to git on **every exit of `ensure`**, which is every path that can leave one behind, because `main` reaches a virtualenv through that function and through no other. The runner writes the ignore rather than the tool that built it, and writes it from a `finally` rather than from a list of remembered paths — so it covers the build that succeeded, the build that failed partway, the `.venv` merely adopted, the adopted `.venv` the FLOOR **refuses**, and the directory an earlier run left on a machine where neither builder can now finish. `build` keeps a `finally` of its own for the directory it makes. It does nothing when there is no directory, and leaves an existing ignore alone. **Invisible insofar as the write lands**: the guarantee is owed on every exit and delivered on every exit whose `.venv` this process can write to, and a directory the operator has made read-only is theirs — it stays visible to git, and the runner says which file it could not write and what that costs rather than raising over the refusal it has already printed (#177) | `.github/scripts/run_tests.py#hide_from_git@6182d405`, `.github/scripts/run_tests.py#build@06f9f025`, `.github/scripts/run_tests.py#ensure@2c6edb92`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_environment_hides_itself_from_git@70a8f5e5`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_existing_ignore_is_left_alone@64998dce`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_failed_build_still_leaves_no_trace_in_git@f8e6601a`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_adopted_environment_is_hidden_too@f4dbc041`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_refused_environment_is_hidden_too@a4263cef`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_a_directory_no_builder_can_finish_is_hidden_too@2d872b61` | **Executed** 2026-09-06 in phase 3, both tools measured rather than inherited from prose: `uv venv --python ">=3.12"` (uv 0.10.4) writes `.venv/.gitignore` containing `*`, and `python3.12 -m venv` (3.12.7) writes **no `.gitignore` at all**. And the fallback branch end to end, with `uv` stripped from PATH: `build()` announced `python -m venv`, returned `None`, `has_pytest` came back `True`, and the ignore read `'*\n'`. **Executed** 2026-09-06 in round 1's fix pass, the two paths the claim used to skip: a build whose install step exits non-zero after the directory exists, and an adopted `.venv` with no ignore — `git status --porcelain` is empty in a real repository for both, where before the fix each printed `?? .venv/`. **Executed** 2026-09-06 in round 2's fix pass, the two exits the list ALSO skipped, in a real `git init` repository at `2b7eea4`: a `.venv` at `version = 3.11.9` refused by the floor, and a `.venv` with no pytest on a machine faked to have neither `uv` nor a 3.12 — `?? .venv/` for both before the fix, empty for both after. Both cases seen red against `2b7eea4` before they were planted, and both killed by mutating `ensure`'s `finally` to write nothing. **Re-read 2026-09-06 in #177's phase 2** at `d210bac`, after phase 1 guarded the write and drifted this anchor: the mechanism the claim is about is untouched — `ensure`'s `finally` still calls the function on every exit, `build` keeps its own, and the six cases above are unchanged and green. What the anchor gained is the guard and its sentence, whose own evidence is R1 of `seal/ledger/1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback.md` rather than this row | 2026-09-06 · 2026-09-24 | **The tidy-up is deleting `hide_from_git` because uv already writes that file.** True for one of the two strategies and false for the other, and the one it is false for is the fallback — the path taken on exactly the machines least likely to notice a `.venv` in `git status`. The ignore goes inside the directory rather than into the repository's own `.gitignore` so that removing the virtualenv removes the rule with it. **The second tidy-up is moving the call back to the end of a successful build**, where it stood until round 1 found it: the run that ends in a sentence is exactly the run that leaves a half-built directory, so success is the one path that did not need the write. Round 1 reproduced both misses in a clone before the fix. The `is_dir` guard is what makes the failed-build call safe — the first step can fail before any directory exists, and `test_a_failing_build_step_is_a_sentence` goes red without it. **The third tidy-up is turning the `finally` back into calls on the paths that need one**, which reads tidier and is how this row got written twice. The list was tried and went short twice — round 1 added two paths to it, and round 2 found a third with a fourth beside it, one of them created by round 1's own fix. A list is short whenever somebody adds a `return`; an exit-level guarantee is not, and it is why the claim above names a function rather than a count. **The fourth is reading #177 as this row's falsification and removing it.** A row is REMOVED when a change takes away the code it cites, and #177 took nothing away: the function, the two `finally` blocks and all six cases stand, and the claim's whole content — the guarantee is owed to a function's exits rather than to a list of paths — is what the guard was written to keep. What was wrong was never the mechanism but an unstated precondition. The outcome half had ALWAYS depended on the write landing, and on the one path where it does not the row was false before #177 as well, in the worse way: the write raised through the `finally` and replaced the refusal with a traceback. So the caveat states a condition that was always there and is now visible to the reader, which is an under-specified row repaired in place — removing it would delete a true claim and leave the three tidy-ups above unguarded **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** `build` installs `PACKAGES` (`pytest`, `pytest-xdist`) on both strategies (#337); its `finally` and the ignore it writes are as they were. The claim stands. |
| R3 · the supported floor is stated once, as `FLOOR` in the runner, and used four ways from there — to build with `uv venv --python ">=3.12"`, to refuse the standard-library strategy below it, to refuse a `.venv` found below it, and to word the sentence a machine with neither tool reads — while `CONTRIBUTING.md`'s section states the number **once**, in the sentence that names `FLOOR`, so the document and the code are traceable to each other rather than merely equal today, and CI's own version is read out of the workflow rather than asserted in a comment | `.github/scripts/run_tests.py#FLOOR@856e0b4d`, `.github/scripts/run_tests.py#FLOOR_TEXT@34aefb3c`, `.github/scripts/run_tests.py#build@06f9f025`, `` `CONTRIBUTING.md#"## Running the checks"@048eae4c` ``, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_section_and_the_runner_state_the_same_floor@76a80cf1`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_section_states_the_floor_once@8582ac3c`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_ci_runs_the_suite_at_the_floor_the_runner_holds@6c445af9`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_neither_tool_available_names_what_to_install@b75ea303` | Read 2026-09-06 in phase 3 at `a192d47`, both places opened together; **executed** 2026-09-06 in phase 1, where the missing-tool sentence was produced against `/usr/bin/python3` 3.9.6 with `uv` off PATH, and the pinning case reads `FLOOR_TEXT` out of the runner rather than repeating `3.12` | 2026-09-25 | **The tidy-up is writing the number instead of the name** — in the document, where `Python 3.12 is the supported floor` reads complete without the clause naming `FLOOR`, and in the code, where `sys.version_info < (3, 12)` is the shorter comparison. The second has a second reason: at `target-version = "py312"` ruff reads that literal as always-false (UP036), so the constant is what keeps the guard from being linted away. **`>=` rather than an exact pin**: phase 1 measured uv 0.10.4 resolving the specifier to an already-installed 3.13.9, where `--python 3.12` would have downloaded 3.12 onto a machine that has 3.13. A floor is a floor **Re-read 2026-09-10 in work item 1789002694 (#30), phase 4.** The section's *Name a module* paragraph now names the sealer and `agents/sealer.md`. The floor paragraph is untouched and still states the number once, in the sentence naming `FLOOR`, which is the whole of what this row claims about the document.  **Re-read 2026-09-13 in work item 1789296100 (#354).** `CONTRIBUTING.md` §*Running the checks* gained a paragraph naming `broad-gate` and `--strict`. It states no version number, so the floor is still stated once, in the sentence that names `FLOOR`. **Re-read again 2026-09-13 in round 1's fix pass**, which reworded that same paragraph to *three readers of the exit code* and *a check run that comes back exit 1*. Still no version number in it. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** `build` installs `PACKAGES` (`pytest`, `pytest-xdist`) on both strategies (#337); its `finally` and the ignore it writes are as they were. the `about five minutes` comment became *in parallel — the sealer's run* and a paragraph on the `-n auto` default joined the block (#337); `bin/test` is still named first, the floor still stated once, the fallback still labelled. The claim stands. **Re-read 2026-09-25 by work item 1790260567 (#510), phase 3**, which added one paragraph to `## Running the checks` saying the suite runs with `gh` logged out and why. The new paragraph states no version, so the section still states the floor once, beside `FLOOR`; the claim holds. **Re-read again 2026-09-25 in round 1's fix pass of work item 1790260567**, which changed the new paragraph to say `GH_TOKEN` and `GH_ENTERPRISE_TOKEN` are set to a placeholder rather than removed. It states no version; the claim holds. |
| R4 · `CONTRIBUTING.md` §*Running the checks* names `bin/test` before the `uvx` form, shows the narrow `bin/test tests/<file> -q` a segment types beside the sentence saying the full run is the sealer's, and keeps the `uvx` form as a labelled **no-write** fallback carrying the 55–58 seconds that demoted it — with the *Run the broad ones once* rule left standing | `` `CONTRIBUTING.md#"## Running the checks"@048eae4c` ``, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_section_names_the_cheap_command_before_the_slow_one@336480fe`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_slow_form_says_what_it_is_for_and_what_it_costs@b5c81716`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_section_keeps_the_broad_once_rule@bc9dbe35` | Read 2026-09-06 in phase 3 at `a192d47`, against the same section as it stood at the base `dcbf404`, where the `uvx` line was the first command and no runner was named; **executed** 2026-09-06 in phase 2, every case seen red first — two of them against a section that exists, by deleting the sentence each guards | 2026-09-25 | **Two tidy-ups, opposite directions, and the row refuses both.** Deleting the `uvx` form as redundant strands the reader who does not want `bin/test` writing a `.venv` into their working tree, and it is the form CI's `pip install pytest` variant matches. Keeping it without its cost is how the next reader promotes it back to first, which is the state this work item found. It is a no-**write** fallback and not a no-**clone** one — it runs `tests/`, so it needs the clone too, and the section says which. The ordering case asserts an index comparison rather than presence, because a section naming both commands in the wrong order passes every presence check while costing exactly what #156 measured. **A fourth case used to ground this row and no longer does.** #30 gave the full run an owner, so the section's sentence and the case that reads it both say `sealer`; renaming `test_the_section_says_the_full_run_is_the_orchestrators` REMOVED that anchor rather than drifting it, and a removed anchor's claim is not re-pointed. The new claim is S1 of `seal/ledger/1789002694-two-agents-are-forbidden-the-seal-and-nobody-is-assigned-it.md`, and the three coordinates left here are the ones whose units this work item did not rename **Re-read 2026-09-10 in work item 1789002694 (#30), phase 4.**  **Re-read 2026-09-13 in work item 1789296100 (#354).** The new paragraph sits between the fenced block and *Name a module*, so `bin/test` still comes before the `uvx` form and the fallback is still labelled no-write. **Re-read again 2026-09-13 in round 1's fix pass**, which reworded two sentences inside that paragraph and moved no command: the order and the no-write label are where phase 3 left them. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** the `about five minutes` comment became *in parallel — the sealer's run* and a paragraph on the `-n auto` default joined the block (#337); `bin/test` is still named first, the floor still stated once, the fallback still labelled. The claim stands. **Re-read 2026-09-25 by work item 1790260567 (#510), phase 3**, which added one paragraph to `## Running the checks` saying the suite runs with `gh` logged out and why. The paragraph sits after the CI paragraph; the order of `bin/test` and the `uvx` fallback, and the *Run the broad ones once* rule, are untouched. The claim holds. **Re-read again 2026-09-25 in round 1's fix pass of work item 1790260567**, which changed the new paragraph to say `GH_TOKEN` and `GH_ENTERPRISE_TOKEN` are set to a placeholder rather than removed. The order of the two forms and the *Run the broad ones once* rule are untouched; the claim holds. |
| R5 · `docs/review-handoff-protocol.md` §*The handoff before round 1* carries a fifth requirement — a runner the repository ships is **found**, not typed into every prompt — hands a segment the narrow form because the full suite is the sealer's, keeps the old requirement for a repository that ships none, and names the measurement that bought it; **both** segments that run a test link that section by name instead of restating the rule — `agents/smith.md` and `agents/warden.md` — and neither names a command that exists only in this repository, because `agents/` is copied into every install | `` `docs/review-handoff-protocol.md#"- **A runner the repository ships is found, not typed into every prompt.**"@21e2b638` ``, `` `agents/smith.md#"**Find the runner before you invent a command.** A repository that ships"@7604ea71` ``, `` `agents/warden.md#"## Where you work"@a68e1953` ``, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_warden_finds_the_runner_before_building_its_own@574ad2d3`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_no_agent_definition_names_this_repositorys_own_command@6be0962e`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_protocol_says_a_shipped_runner_is_found_not_typed@dd4de649`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_protocol_hands_over_the_narrow_form@e633b0dc`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_protocol_carries_the_measurement_that_bought_it@25060d1b`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_smith_finds_the_runner_before_inventing_a_command@330b8727`, `tests/test_the_handoff_names_the_form_it_ran.py#test_the_requirement_count_moved_with_the_requirement@3f15a73a` | Read 2026-09-06 in phase 3 at `a192d47`, both carriers and the count sentence opened together; **executed** 2026-09-06 in phase 2. The measurement the requirement carries: four build segments of one work item read repeats of **17 s, 2 s, 0 s and 0 s**, the only difference between them being whether the orchestrator had typed the runner into the spawn prompt | 2026-09-10 · 2026-09-24 | **The tidy-up is restating the rule in `agents/smith.md` so a segment need not follow a link.** That is two places to disagree, and `tests/test_the_rules_have_one_owner.py` already holds nine rules to the link pattern. The carrier that matters for the cause is nonetheless `agents/smith.md`, because it reaches a segment at startup with nobody typing anything — which is precisely what went missing on the segments that read 17 s and 2 s. **The count sentence is part of the claim, not bookkeeping**: a fifth bullet under a sentence saying *Four requirements* leaves the list and a skimming reader counting different things, which is why a case in a second module refuses both the old count and the one before it. `skills/agent-contract/SKILL.md` was deliberately not touched — it holds what is true of any agent, and *look for a runner* is one role's verification habit. **The second tidy-up is making either definition concrete by naming `bin/test`.** `agents/` is copied into every install, so that sends a user's segment after a file their repository does not have; both carry the generic shape instead — a wrapper in `bin/`, or whatever the contribution guide names first — and a parametrised case refuses the literal in either file. Round 1 found the reviewing segment left out of the class entirely: `agents/warden.md` said to build a `uv` venv and named no runner, on the segment that runs more cases than the smith does. The `uv` line stays as the branch for a repository that ships none, which is the case it was written for **Re-read 2026-09-10 in work item 1789002694 (#30), phase 4.** The requirement now says the full suite is the sealer's and names what assigns it, and the claim above is corrected to match rather than left asserting an owner the document no longer names. Both carriers, the count sentence and the 17 s / 2 s / 0 s / 0 s measurement are unchanged. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** the first bullet gained the clone directory `<scratchpad>/<work-item-id>/round-<n>/clone` and the round home beneath it (#544); the runner bullet and the report path are unchanged. docstring wording only — *a five-minute suite* became *the whole suite* (#337); the assertions are unchanged. The claim stands. Re-stamped again in round 1's fix pass: *the round you are* became *the round you are in* (⬜ 4); nothing else in the section moved. |
| R6 · the runner sits under `.github/scripts/` because that is where this repository's own automation lives, and what keeps a plugin user out of a five-minute suite is that `test` is a shell builtin so PATH never offers `bin/test` — **not** that `.github/` is absent from a plugin cache, which it is not | `` `bin/test#"# Typed as `bin/test`, never as a bare `test`: `test` is a shell builtin, so"@33e87da2` ``, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_placement_stands_on_what_it_actually_buys@0b11d392`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_the_cheat_sheet_does_not_offer_the_runner@2dc3c3d1` | **Executed** 2026-09-06 in phase 2 and confirmed independently by the orchestrator: `ls -a` on the cached releases shows `.github`, `bin` and `tests` in every one from 0.2.0 through 0.8.1, and `.claude-plugin/marketplace.json` declares `"source": "./"` with no `export-ignore` in `.gitattributes`, so the plugin ships from the repository root and withholds nothing. Read 2026-09-06 in phase 3 at `a192d47`: the corrected sentence stands in `.github/scripts/run_tests.py`'s docstring, in `bin/test`, and in the docstring of `test_a_copy_without_the_runner_beside_it_says_so` | 2026-09-10 · 2026-09-24 | **This row is a correction, and the thing it guards is a reason rather than a behaviour.** Phase 1 recorded the concealment claim labelled **[executed]**, and it reached three files and a test docstring before one `ls -a` falsified it; §5 of the contract is what caught it. The decision did not move, which is why the row is worth writing: a reader who finds a false reason in a file either fixes the reason or moves the runner, and the second is the expensive mistake. The case asserts the absence as hard as the presence — `` `.github/` does not `` is refused and `shell builtin` is required. The same fact carries the `README.md` prohibition: `bin/test` stays out of the cheat sheet not because a user's copy finds no runner (it will, from 0.8.2) but because that table lists commands a reader types, and this is the one command PATH cannot offer. Both of the runner's guards are therefore unreachable from a cache, which is `questions.md` question 7 and still open **Re-read 2026-09-10 in work item 1789002694 (#30), phase 4.** The comment's second paragraph now names the sealer as the suite's owner. The sentence this row exists to protect — what keeps a plugin user out is that `test` is a shell builtin, NOT that `.github/` is missing from a cache — is untouched in all three places. **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** the comment block's last paragraph no longer states *about five minutes* and says both forms run parallel (#337); the `Typed as` sentence this row protects is unchanged. the failure message no longer states the serial figure (#337); what the case asserts is unchanged. The claim stands. |
| R7 · the floor is asked of an **adopted** environment as well as of a built one. `ensure` reads the version out of `pyvenv.cfg` — where `uv venv` writes `version_info` and `python -m venv` writes `version`, so no interpreter starts — and a `.venv` below `FLOOR` is refused with a sentence naming the directory to remove. A version the file does not state is **not** a refusal: only one that is both readable and below the floor is | `.github/scripts/run_tests.py#venv_version@2ddf5db2`, `.github/scripts/run_tests.py#ensure@2c6edb92`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_environment_below_the_floor_is_refused@c9adc816`, `tests/test_the_suite_has_a_command_that_is_cheap_twice.py#test_an_environment_that_says_nothing_about_its_version_is_kept@84138bd1` | **Executed** 2026-09-06 in round 1's fix pass: a `.venv` whose `pyvenv.cfg` reads `version = 3.9.6` is refused with the sentence naming 3.9.6, the floor and what to remove, and one with no `pyvenv.cfg` at all — and one whose file carries `home` and `prompt` and no version — still returns its interpreter. **Executed** in round 1 before the fix: `has_pytest` was `venv_python(venv).exists() and any(scripts.glob("pytest*"))`, with no version question anywhere in it, so a below-floor `.venv` ran the suite silently | 2026-09-06 | **The tidy-up is deleting the `found is not None` guard**, which reads as a redundant None check beside a comparison. It is the whole difference between refusing an old interpreter and refusing every environment whose `pyvenv.cfg` this cannot parse — and six existing cases build a `.venv` with no such file at all, so dropping it turns a silent acceptance into a `TypeError` on the warm path. **The second is starting the interpreter to ask its version.** That is R1's cost, on the call R1 exists to keep free; the version is read from a file both builders already wrote |
| R8 · `docs/round-record-spec.md` §*The fix surface* names the **five values that can stand in the reach half** of a `Contract changes` entry, and says only the first is a unit name: the enclosing top-level unit, the file's basename, `pytest`, `pytest only`, and `no call site found`. The three words are read out of `round_record.py`'s own constants by the case rather than typed, so **those three** cannot drift from the generator: a rename goes red, and so does a reword. An ADDITION does not, and the row that closes it is R2 of `seal/ledger/1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback.md` (#177) | `docs/round-record-spec.md#"**Five values can stand in the reach half, and only the first is a unit"@807c521a`, `skills/code-review/scripts/round_record.py#call_sites@61f9b386`, `tests/test_the_fixes_name_their_surface.py#test_the_section_names_the_words_the_writer_can_put_in_a_reach@12e18e7b` | **Executed** 2026-09-06 in round 2's fix pass: `call_sites` re-run at `57d7afa` over `hide_from_git` returns `['build', 'ensure', 'pytest']`, which is the cell `rounds/round-1.md` carries verbatim. The case was seen red against `2b7eea4`, where the section carried no backticked `pytest` at all, and is killed by dropping either `pytest only` or `no call site found` from the paragraph | 2026-09-24 | **The tidy-up is deleting the paragraph as a restatement**, because the table above it already defines the entry shape and the vocabulary reads like padding. It is not: the section defined the reach as call sites a `grep` can name, named none of the generator's own words, and a review round spent a finding on a correct cell for exactly that reason — round 2's ⬜ 11 of this work item, answered rather than fixed. **The second tidy-up is making `call_sites` name the test callers instead of collapsing them.** A fix's test callers are its pins rather than its reach, and twelve named pins bury the one in-tree caller the row exists to surface. **Re-read 2026-09-06 in #177's phase 2**, which is where the claim's reach was measured rather than assumed: reading three constants by name is three ways of catching a rename and no way of catching a fourth constant, so the paragraph could go stale while the suite stayed green — the drift this row exists against, one shape wider than the row had noticed. The claim above is narrowed to what this case actually holds and the wider one is carried by the derived case, which is a second row rather than a rewrite of this one because neither covers the other. **Which half each one holds was measured in round 1's fix pass, after this row asserted it without measuring.** This case alone reads `only the first is a unit name` — drop that sentence from the section and it goes red while the derived case stays green. The derived case alone sees a value ADDED. A rename and a reword redden BOTH, because the derived case's floor reads these same three constants by attribute name, so the sentence this row used to carry — that a rename is caught only here — was false, and it stood where a maintainer deciding to delete one of the two cases would read it **Re-read 2026-09-08 under work item 1788817290**, which gave `call_sites` a `runner_reached` branch for a unit pytest itself reaches (#211). It hands out `PYTEST_ONLY`, an EXISTING value, so the vocabulary is unchanged at five and both derivations still agree — `test_the_section_names_every_reach_value_the_generator_fixes` passed at this SHA. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. Re-read 2026-09-24 — #526's split re-pointed the anchored case to the document that now holds the section it reads, and widened any absence it asserts of the spec to all three documents; the claim unchanged. **Corrected 2026-09-24** by work item 1790208643's round-1 fix pass (⬜ 11): the Claim cell named `docs/review-chain-spec.md` for §*The fix surface*, which #526's split moved to `docs/round-record-spec.md`, and this branch's *moved … the claim unchanged* note above left the cell naming the old file; it names the file that holds the section now. |

<!-- specs/1788661274-the-roll-names-the-next-version-by-guessing -->
### 1788661274-the-roll-names-the-next-version-by-guessing

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Six rows over a branch that is one script and two documents. What earns a row
here is a judgment whose repair looks obvious from the outside, and this
change has an unusual number of them because every one of its decisions is a
choice between two things that both look correct in the file: a guard that
reads as a special case (G1), a parser that reads as unhelpfully strict (G2),
two `print` calls on a path that exits 0 either way (G3), a constant used once
at each end rather than a literal written once (G4), a format stated in the
tracker document rather than in the skill beside it (G5), and a `gh` argument
that looks like plumbing and is text a person reads (G6).

Four of the six are about the same failure seen from four sides: a roll
that silently does nothing, with the workflow green. That is the class of the
bug being fixed, one step over, and it is what `plan.md` names to design
against.

Nothing from phase 1 or phase 2 reached the ledger, so G1 through G5 are
written in phase 3 against code those phases wrote. The units and cases were
opened at `4695eae` before each row was written, and the two modules were
re-run at the same commit.

**G6, and G2's third instance, are round 1's — written in the fix pass at
`253ac8c`.** G2 said its class was closed at two members and it had three:
the row enumerated what follows the marker and never what precedes it. It now
states the method behind the enumeration — a title is three segments, and a
version comes out of one of them — rather than a list, because a list is what
was complete last time.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, and never by an unscoped run; the unscoped
READ form ran afterwards, its output in `phases/phase-3.md`. The scoped form
cannot narrow below a file, so a blanket run would re-stamp `seal/ledger.md`'s
one pre-existing drift on behalf of a session that never read it — and it
would re-stamp F5, whose anchor this branch moved and whose row is edited by
hand for exactly that reason. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| G1 · the roll fires only where a new version has shipped since the open log opened, and it decides that by **equality** — what the open log's title says it rolled from, against the version in the tree — never by comparing two versions in order and never by asking whether this push was a release | `.github/scripts/roll_flow_measurement_issue.py#roll_is_due@a1bd8c19`, `.github/scripts/roll_flow_measurement_issue.py#main@ce69059f`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_push_that_shipped_no_new_version_rolls_nothing@a6a6c945`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_new_version_since_the_log_opened_rolls_it@6d9c74d3` | Read 2026-09-06 in phase 3 at `4695eae`, the guard and both cases opened together; **executed** 2026-09-06 in phase 1, where both cases were seen red before the condition existed, and re-run 2026-09-06 in phase 3 at the same commit: 28 cases in that module, exit 0 | 2026-09-06 | **The tidy-up is deleting the guard**, because a release always rolls and the branch reads as a special case. That is the code as it stood before #155, and it is the 0.8.1 incident: a patch release closed a log opened for a version that had not shipped, and opened a second one under the identical title. **The second tidy-up is comparing the two versions in order** — `rolled_from(title) < shipped` reads as the same question and is not: it makes the answer depend on version arithmetic at the one moment `docs/branch-and-release.md` says the next number is not knowable, and it is what turns an old-convention title naming `0.9.0` into a log that is never due once `0.9.0` ships. Equality asks only what the title already states. **The third is asking whether this push is a release.** The workflow fires on every push to the default branch, so that fact is not available here; what is available is whether the open log already names the version in the tree **Re-read 2026-09-24 by work item 1790173209 (#198).** The equality decision is untouched; the count is read after it, so the claim holds. |
| G2 · a title this script cannot read as its own is **due**, never silent, and *its own* is the whole of what `log_title` writes, required from the first character. A title is three segments — what precedes the marker, the marker, and what follows it — and a version may be read out of the third only when the first two are exactly what the writer wrote. `rolled_from` answers `None` otherwise, and `None` is a different fact from a version rather than a softer one: not the whole title, not the empty string, and **not a version lifted out of a title with a different prefix** | `.github/scripts/roll_flow_measurement_issue.py#rolled_from@4ff7e2f2`, `.github/scripts/roll_flow_measurement_issue.py#TITLE_PREFIX@daf589d5`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_rolled_from_reads_only_the_title_the_roll_itself_writes@365dd11b`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_title_the_roll_did_not_write_names_no_version@db48506b`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_title_the_roll_cannot_read_is_due_rather_than_silent@6a6080bd`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_title_written_before_this_change_is_always_due@d3813203` | **Executed** 2026-09-06 in phase 1: two mutations survived the first loop and both are killed by name — `rolled_from` answering the whole title where the marker is absent, and answering `""` where the marker is there with nothing after it. **Executed** 2026-09-06 in round 1's fix pass: the third instance, found by round 1 and pinned by `test_a_title_the_roll_did_not_write_names_no_version`, plus two more mutations killed at the same time — the `startswith` guard removed, and `.strip()` removed. Read 2026-09-06 at `253ac8c`, the function and its five cases opened together | 2026-09-06 | **The tidy-up is making `rolled_from` helpful.** Returning the title it was given, or `""`, reads as the forgiving answer and passes every case that goes through `main`, because a wrong answer that is also unequal to the shipped version still comes out as due. The gap opens at a title that happens to BE a version string: read as the log for that version, it is not due, and the log stalls with the workflow green. That is why the pure function is pinned directly rather than through its caller. **The second tidy-up is reading an unreadable title as not due** — do not touch what you do not understand. The two directions are not symmetric: *not due* stops the log forever and nothing goes red, *due* costs at most one roll that was not owed. **The third tidy-up is the one this row missed for a phase and a round — matching the marker anywhere instead of the prefix from the start.** `partition` reads as the same question and is not: it accepts a title with any prefix at all, so `docs: explain flow measurement — after 0.8.2` answered `0.8.2` and the log for 0.8.2 was never due. The row enumerated the segment after the marker, both instances of it, and never looked at the segment before it — which is why the class had a third member the row did not name. One rule then covers four things — an old-convention title, a title somebody tidied by hand, a title this script never wrote at all, and the migration off the old convention, which retires itself at the first release after this change |
| G3 · both outcomes exit 0, so the two printed lines are the only thing that tells a release that rolled from a mechanism that has stopped. `nothing due:` names the log and the version; `rolled:` names the issue closed and the title opened | `.github/scripts/roll_flow_measurement_issue.py#main@ce69059f`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_nothing_due_line_names_the_log_and_the_version@f7fefa42`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_rolled_line_names_the_issue_closed_and_the_title_opened@2bbf0752` | Read 2026-09-06 in phase 3 at `4695eae`, both cases opened against the two `print` calls they read; **executed** 2026-09-06 in phase 1, both seen red before the lines carried what they now carry, and re-run 2026-09-06 in phase 3, exit 0 | 2026-09-06 | **The tidy-up is deleting a `print` as noise**, or collapsing the two into one line that says the job ran. What a workflow log is for is the state a person came looking for, and this script has two states that both leave the job green — the whole point of `spec.md`'s fourth acceptance scenario is that a reader tells them apart without opening GitHub. **The second tidy-up is making *nothing due* a non-zero exit**, on the reasoning that a release should always roll. It should not: a re-run of the job, and a push to `main` that shipped nothing, are both ordinary, and failing them turns the normal case into a red release. Contract §14 is what puts the two lines under cases at all — they are text a person reads and acts on **Re-read 2026-09-24 by work item 1790173209 (#198).** Both outcomes still exit 0 and the two lines still tell them apart; a rolled run may now print one line before `rolled:` — the empty-log sentence, or that the count was unreadable — and neither changes the exit. The claim holds. |
| G4 · `TITLE_PREFIX` is the one string the title is written through and read back through, in full at both ends, so the writer and the reader cannot drift apart in width any more than in wording. `log_title` writes it and `rolled_from` requires it; `TITLE_MARKER` is the substring it is built from and exists because `docs/issues-and-milestones.md` is read against the marker by name. No other place in the module spells the title out | `.github/scripts/roll_flow_measurement_issue.py#TITLE_PREFIX@daf589d5`, `.github/scripts/roll_flow_measurement_issue.py#TITLE_MARKER@55d0d25a`, `.github/scripts/roll_flow_measurement_issue.py#log_title@2f833e6b`, `.github/scripts/roll_flow_measurement_issue.py#rolled_from@4ff7e2f2`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_title_the_roll_writes_is_the_title_the_next_roll_reads@66101365` | Read 2026-09-06 in phase 3 at `4695eae`: the constant, both functions and the round-trip case opened together, and the module read for any other spelling of the title — `open_issue` calls `log_title`, `main`'s recovery message calls `log_title`, `close_issue`'s comment calls `log_title`, and no f-string writes the words. **Executed** 2026-09-06 in phase 1, where the case reads the writer's own output back through the reader rather than comparing either against a literal | 2026-09-06 | **The tidy-up is writing the title where it is used**, which is what the code did before this change: `title = f"chore: flow measurement — {version}"` inside `open_issue`, one literal at the one place a title is created. It reads simpler and it is how the two ends come apart — a wording change to the writer alone leaves `rolled_from` reading for a marker nobody writes, every title answers `None`, every title is therefore due, and the roll fires at every release forever. That is the invariant broken from the other side, and nothing goes red: the failure looks exactly like the old behaviour. `after` is the load-bearing word in the constant, because what follows it is the version the log rolled FROM |
| G5 · the two documents a person reads carry the convention, and the **title format lives in `docs/issues-and-milestones.md` rather than in the shipped skill**. The skill states the rolling log's two ends — it opens at a release and is discarded by the release that ships the next version — and names no title | `` `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@70aef725` ``, `` `docs/issues-and-milestones.md#"## A label answers *what it is about*, and survives the move"@39297113` ``, `tests/test_a_segment_feeds_the_flow_log.py#test_the_rolling_logs_life_is_bounded_by_two_releases@1d551564`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_tracker_doc_states_the_condition_the_roll_carries@becb243a`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_tracker_doc_states_the_title_the_roll_writes@969baa01`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_tracker_doc_says_what_a_title_written_before_this_means@20349ded` | Read 2026-09-06 in phase 3 at `4695eae`, both sections opened against the four cases that read them; **executed** 2026-09-06 in phase 2, each of the four seen red against `deca998` before either document was edited, and both modules re-run 2026-09-06 in phase 3, exit 0. **Re-read 2026-09-08 (#206)**: the sentence in this section saying what the version check refuses was corrected from *whether it has shipped or is still ahead* to at-or-above the running one, with the below-the-running half stated. The title format this claim is about, and the fact that it lives here rather than in the shipped skill, are untouched. **Re-read 2026-09-12 for #361**, which added the `size: now` label's definition to the end of this same section and moved the unit's hash, and re-read again the same day in round 1's fix pass, which corrected two sentences inside that definition. Both halves hold unchanged: the title format `chore: flow measurement — after 1.2.3` is still in this section, and `skills/verify/SKILL.md` still names no title. The four cases this row names were re-run, exit 0 | 2026-09-22 | **This row is the presence F5 is the absence of**, and the two are read together: F5 holds that the shipped skill names no tracker state existing in this repository alone, which is why the title format cannot go there, and this row holds that it went somewhere a reader will find it. **The tidy-up is moving the format into the skill for symmetry** — the skill is where a session is told to post a measurement, so the title belongs beside it — and nothing in the skill's own prose would tell that reader they had crossed a line. **The second tidy-up is deleting the tracker document's *not rewritten* paragraph as history nobody needs.** Both conventions are visible in the tracker forever, because closed logs are not retitled; without that paragraph the obvious tidy is to retitle the old logs into the new form, which falsifies every comment citing them. **The document's case reads the script's own constant rather than a literal**: it feeds every backticked `chore: …` example in the section through `rolled_from` and requires at least one the script can read back, because an example answering `None` is an old-convention title being held up as the current one — which is what a well-meaning edit produces, since the older form is what the tracker still shows **Re-read 2026-09-06 at the 0.8.2 release preparation.** That release's own broad gate refused three released version strings in the cited section and they were rewritten to an illustrative one; #179 carries the class. Both halves hold: the title format is still in that section, and the shipped skill still names no title. Stamped by hand from a copy, because a `--reverify` over `seal/ledger.md` also re-stamps S8, whose claim nobody here has read. **Re-read 2026-09-07 in work item 1788735085's phase 1** (#179), which renamed `test_no_loaded_file_hardcodes_the_running_version` inside this section and widened the sentence around it from "a released version" to a real version at or above the running one. The title format the claim is about — `chore: flow measurement — after 1.2.3` — and where it lives are untouched; the four cases this row names were re-run, 175 passing. The blanket `--reverify` that moved this hash rewrote only the three anchors this branch edited, so S8 was not re-stamped by it. **Re-read 2026-09-09 for #300**, same section rewrite. The title format is still in `docs/issues-and-milestones.md` and still absent from the shipped skill, and the rolling log's two ends are stated as before. **Re-read 2026-09-13 for #350**, which rewrote step 1 of this section — the measure step now names `session_cost.py --segments` in place of one invocation per transcript and of the by-hand resume split — and reworded the orchestrator paragraph so that a spawn cycle is no longer called a segment. This claim is about a different part of the section and is untouched; the hash is the new content of the same anchor. **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The section gained the `from-review` paragraph — what the label marks and what it is counted for. The rolling log's title format and the skill's two-end sentence are untouched — the new paragraph is about a different label and states no title — so the claim holds. **Re-read again 2026-09-22**, in round 1's fix pass. The `from-review` measurement was corrected to 89 / 43 closed / 46 open and dated, after round 1 found the first figures did not reproduce. What this row claims about the section is untouched. **Re-read 2026-09-22 for the flow-log posting command**, which named `session-cost … --post` in place of the two by-hand lookups and the post step, and kept both `gh` forms in the prose beside it. Step 1, the run-level table and the orchestrator-boundary paragraph are untouched. This claim is about a different part of the section; the hash is the new content of the same anchor. **Re-read 2026-09-22 for #450**, which added three paragraphs to the end of this same section — where the sizing judgment is made, what removes a spent `size: now`, and that no backlog sweep is owed — and reworded the sentence that used to say nothing reads the label. The hash moved with the unit. This claim is about a different part of the section and is untouched: the title format `chore: flow measurement — after 1.2.3` is still here (two mentions), and `skills/verify/SKILL.md` still names no title (`grep -c` returns 0). The edit is one hunk from line 145 on, below both \| 2026-09-22 \| **This row is the presence F5 is the absence of**, and the two are read together: F5 holds that the shipped skill names no tracker state existing in this repository alone, which is why the title format cannot go there, and this row holds that it went somewhere a reader will find it. **The tidy-up is moving the format into the skill for symmetry** — the skill is where a session is told to post a measurement, so the title belongs beside it — and nothing in the skill's own prose would tell that reader they had crossed a line. **The second tidy-up is deleting the tracker document's *not rewritten* paragraph as history nobody needs.** Both conventions are visible in the tracker forever, because closed logs are not retitled; without that paragraph the obvious tidy is to retitle the old logs into the new form, which falsifies every comment citing them. **The document's case reads the script's own constant rather than a literal**: it feeds every backticked `chore: …` example in the section through `rolled_from` and requires at least one the script can read back, because an example answering `None` is an old-convention title being held up as the current one — which is what a well-meaning edit produces, since the older form is what the tracker still shows **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold's side of this section is `<!-- specs/<id> -->` marker comments and nothing else; no sentence changed, so the claim holds and the hash is both sides' edits together. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which changed the `Broad gate` row of the run-level table in this section to read the count off the cell — one entry per run, newest first; nothing this row is about moved **Re-read 2026-09-24 by work item 1790173209 (#363).** The section gained the tagged half of the version rule in the paragraph explaining why the log's number is illustrative; the title format and the two documents are untouched, so the claim holds. **Re-read 2026-09-24 by work item 1790173209 (#198).** The section gained the sentence saying an empty log is said so in three places; the title format is untouched, so the claim holds. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** the section changed only in the `Location` row's record paths; the title format and the log's two ends are unchanged, so the claim holds |
| G6 · the comment posted on the log being closed states the convention **in force**, and it names the successor by calling `log_title` rather than by describing it. It used to promise a log *"for the version this release ships next"*, which is the prediction this work item removed — `close_issue` takes the shipped version for no other reason | `.github/scripts/roll_flow_measurement_issue.py#close_issue@236fda1c`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_close_comment_names_the_version_this_release_shipped@dd97c886` | **Executed** 2026-09-06 in round 1's fix pass: the case was seen red with the old wording restored under the new signature, and the whole module re-run green after. Read 2026-09-06 at `253ac8c`, `close_issue` opened beside `issue_body`, which had been rewritten for the same reason in phase 1 and pinned then | 2026-09-06 | **The tidy-up is dropping the `shipped` parameter and rewording the comment generically**, on the reasoning that `close_issue` only closes an issue and the version is the caller's business. A generic sentence is exactly what drifts back: nothing in it is derived from the title the roll actually writes, so the next edit can restate the removed convention and no case notices. Naming the successor through `log_title` is what makes the comment fail when the convention moves. **The second tidy-up is leaving it alone as a workflow detail** — this is a comment posted on a GitHub issue people go on reading after it closes, which is contract §14's own subject, and round 1 found it precisely because `issue_body` was fixed and its class-mate was not **Re-read 2026-09-24 by work item 1790173209 (#198).** `close_issue` takes the log's comment count and appends one sentence when it is zero; the convention sentence and the `log_title` call are as they were, so the claim holds. |

<!-- specs/1788668335-a-fence-under-the-probes-table-closes-after-a-later-heading -->
### 1788668335-a-fence-under-the-probes-table-closes-after-a-later-heading

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Five rows over a branch that is one new function and its cases. What earns a
row is a judgment whose repair looks obvious from the outside, and this
change has five because the guard is built almost entirely out of refusals
to do the direct thing: it does not list the sections it protects (F1), it
is read over a grid of texts and hiders and not over one text (F2), it does
not refuse a fence for
carrying a heading (F3), it does not read the `#` character at all (F4), and
it asks whether a section's rows are ONLY hidden rather than whether any of
them is (F5).

Four of the five are the same failure seen from four sides — a report losing
a whole section or a whole table with the generator exiting 0 — which is the
bug being fixed, and each tidy-up below puts one of those sides back.

The units and cases were opened at `5721a31` in phase 2, before each row was
written; the module was re-run at the same commit. What phase 1 executed is
labelled as phase 1's, and the mutations named in the Notes are its.

**Re-read and re-stamped 2026-09-06 in round 1's fix pass**, at `de75cbc`.
F2's Notes had it backwards and F5 is new; every unit each row cites was
opened again before its hash moved.

**Re-read and re-stamped again 2026-09-06 in round 2's fix pass**, at
`c7ebb29`. `swallowed` and `build` both moved, so every row citing either was
opened again before its hash did: F2's claim is now a grid rather than a list
of two texts, and F1, F3, F4 and F5 were re-read against the changed function
and their claims stand unaltered — the comment question sits above the walks
they are about and changes none of them.

**Re-read and re-stamped a third time 2026-09-08**, by work item
`1788873610-every-copy-out-of-raw-meets-the-hider-question` (#182), which
round 3 of this chain opened. `swallowed` and `build` moved again — the
never-closed pair each of them carried is one `hiders_close` call, because the
order of the two questions is a property of `readable`'s passes and not of any
one text. F2's second sentence is struck: **the grid's row axis was one member
short**, `inherited_rows` being the text it did not name, and the method was
the failure rather than the count. F6 of
`seal/ledger/1788873610-every-copy-out-of-raw-meets-the-hider-question.md`
carries the rule that replaces it, stated over the DESTINATION. F1, F3, F4 and
F5 were opened again and their claims stand unaltered, for the same reason as
last time: what left `swallowed` sits above the walks they are about.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, never by an unscoped run; the unscoped
READ form ran afterwards, its output in `phases/phase-2.md`. The scoped form
cannot narrow below a file, so a blanket run would re-stamp `seal/ledger.md`
on behalf of a session that never read it. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| F1 · what a fence may not cross is **derived** from the generator's own constants and never restated: ~~`REPORT_TABLES`' headings and `TERMINAL_LINES`~~ (**narrowed twice — the heading loop reads `REQUIRED_HEADINGS`; see R3 and its correction in R4 of `seal/ledger/1788749195-…`**). Those constants are the generator's own statement of what it looks up in the report, and nothing in the module read either one before this change — both were defined and referenced only from the tests. So a section added to `REPORT_TABLES` is guarded by that addition alone, and the parametrized case reads its own parametrization back out of the constant, so the case list cannot fall behind it | `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `skills/code-review/scripts/round_record.py#REPORT_TABLES@d761cf9b`, `skills/code-review/scripts/round_record.py#TERMINAL_LINES@bfc50233`, `tests/test_the_record_is_generated.py#SECTION_TEXT@a6f8fb79`, `tests/test_the_record_is_generated.py#test_a_fence_that_takes_a_section_the_generator_reads_is_refused@29053fab` | Read 2026-09-06 in phase 2 at `5721a31`: the guard's two loops opened against both constants, and the base commit `774e76b` grepped for either name to confirm the definitions were the only occurrences in the module. **Executed** 2026-09-06 in phase 1, all three parameters seen red at `c4d7077` — `## Deferred` and `## Executed probes` exiting 0, `## Verdicts` exiting 2 on the wrong message — and re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-07 | **The tidy-up is typing the three section names into `swallowed`**, which is #169's own proposal and reads simpler than two loops over constants. It goes stale the day a section is added, and nothing goes red when it does: the new section is unguarded and has no case, so the silence looks exactly like correctness. **The second tidy-up is deleting the parametrization assert as a tautology** — it compares a list in the test file with the constant it was copied from, which reads as testing the copy rather than the code. It is the only thing tying the case list to the constant, and without it `REPORT_TABLES` can grow while the parametrization does not, which is the first tidy-up arriving through the test file instead. **Re-read 2026-09-07.** `## Paste-ready fixes` is a section with no table, so the two loops no longer read one list: the heading loop reads `REQUIRED_HEADINGS` and the row loop reads `REPORT_TABLES`, whose members are the same headings — only a table can lose its rows to a fence while its heading stands, and a section with no table answers the row question vacuously (R4). **Corrected 2026-09-07 by review round 3, finding 3**: this sentence and the parenthetical above both named `READ_HEADINGS` after `b466cde` had narrowed the loop, so the row pointed a reader at the constant the code had stopped reading. The derivation and the parametrization assert are unchanged in kind — the case now reads `READ_HEADINGS` back — and R3 of this work item's fragment carries the split. **Re-read 2026-09-24 by work item 1790174138 (#505), which drifted `swallowed`.** The row loop's section end is now `section_end` — a heading of the section's own level or shallower — rather than any `#`; the constants the guard derives from are unchanged |
| F2 · **the rule is one sentence and it is asked over a GRID, not a list: every text the generator copies, crossed with every pass `readable` blanks by.** ~~The texts are the report `swallowed` reads report-wide, the `raw` a copied block and a copied row are sliced out of, and the round paragraph `build` splices whole.~~ (**the row axis was one member short and the method was the failure — corrected 2026-09-08 by #182; see F6 of `seal/ledger/1788873610-…`, which states the rule over the DESTINATION instead. `inherited_rows` is a fourth text the list did not name, and a list of the texts somebody can see is the enumeration-by-reading this row's own claim was meant to replace**). The passes are two, and `strip_comments` runs first — so an unterminated HTML comment blanks every line below it exactly as an open fence does, one pass earlier, where no fence question can see it. Each check is a cell of that grid and none is a duplicate of another; the comment question is asked before its fence twin in each text, because an open comment blanks the closing fence of every block below it and the fence question would otherwise name a fence that is closed in the text as written | `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `skills/code-review/scripts/round_record.py#build@ebe41de3`, `skills/code-review/scripts/round_record.py#fenced_after@bda066c0`, `skills/code-review/scripts/round_record.py#NEVER_CLOSED@963e4998`, `skills/code-review/scripts/round_record.py#NEVER_CLOSED_VERBATIM@95185bf2`, `skills/code-review/scripts/round_record.py#COMMENT_NEVER_CLOSED@4f4cdcb9`, `skills/code-review/scripts/round_record.py#ASKED_COMMENT_NEVER_CLOSED@6aea2790`, `skills/code-review/scripts/round_record.py#SENTINEL@30b6ad77`, `tests/test_the_record_is_generated.py#test_an_unclosed_fence_above_the_probes_table_names_the_fence@80ab7d3f`, `tests/test_the_record_is_generated.py#test_a_fence_that_swallows_a_terminal_line_names_the_fence@29fb0368`, `tests/test_the_record_is_generated.py#test_a_fence_opened_inside_an_html_comment_is_refused@5b8e7d70`, `tests/test_the_record_is_generated.py#test_an_unclosed_html_comment_in_the_report_names_the_comment@b048df69`, `tests/test_the_record_is_generated.py#test_an_unclosed_html_comment_in_the_round_paragraph_is_refused@3eb6c313` | Read 2026-09-06 in phase 2 at `5721a31`: `build`'s call site, the comment left where the old raise stood, and both cases opened together. **Executed** 2026-09-06 in phase 1, both cases seen red at `c4d7077` — the unclosed fence exiting 2 on `the report has 0 Needs a fix: lines`, which sends the writer to look for a line they wrote — and re-run 2026-09-06 in phase 2 at `5721a31`, exit 0. **Re-read and re-executed 2026-09-06 in round 1's fix pass at `de75cbc`**: both walks opened side by side, the third case seen red at `861ad16` (exit 0, the record written, both lower sections resolving to 0 occurrences through the shared reader) and green after; deleting the restored raise turns that case red and no other. **Re-read and re-executed 2026-09-06 in round 2's fix pass at `aed3ca0`**: the grid run cell by cell before the fix — the round paragraph with an unterminated comment exiting 1 with the record WRITTEN and four of its five sections resolving to 0 occurrences, the report with one exiting 2 on `the report has 0 Needs a fix: lines` — both cases seen red there and green at `c7ebb29`; four mutations, each alone and restored by digest, turn exactly one case red each: deleting either comment question, and swapping either order | 2026-09-17 | **The tidy-up is taking the raise back OUT of `fenced_after` as a duplicate**, which is what this row said to do until round 1's 🔴 1. The argument for removing it is real as far as it goes and it does not go this far: `build` calls that function for the probes section alone, so a fence opened under the verdict table takes `## Executed probes` with it, the call never happens for a section the report no longer has, and the record gets the empty template — which is why `swallowed` has to exist and be report-wide. It does not follow that the second check is dead, and phase 1 read it as following. The two walks read two texts, and `strip_comments` runs before the first and not before the second: measured at `861ad16`, an opener inside an HTML comment under the probes table wrote the record at exit 0 with `## Inherited coordinates` and `## Deferred` unreadable to every downstream reader. Contract §13 is the rule that was broken — *the two texts agree about where a fence is* was an assumption nobody had removed and tested. Deleting the raise turns `test_a_fence_opened_inside_an_html_comment_is_refused` red and nothing else. **The second tidy-up is dropping `SENTINEL` and asking whether the report's last line survived `blank_fences`** — a report's last line is very often blank for reasons that have nothing to do with a fence, so the question has to be asked of a line that cannot be blank for any other reason. **The third is dropping `strict=True` from the `zip`**: the two reader passes are asserted to be the same file line for line, and a length that differed would truncate the hidden set silently, which is this guard reporting clean because it read less. **The fourth is writing a new guard the way the last one was written** — round 1's fix for the round paragraph asked the fence question of `strip_comments(asked)` while `build` splices `asked` verbatim, which is 🔴 1's own check/copy asymmetry reproduced inside the guard that closed 🟡 3, and round 2 found it. A guard added to this module is a cell of the grid above, and the question to ask of a new one is which pass it reads through and which text the copy reads. **The fifth is asking the comment question after the fence question**, which reads as the same check in a different order: an open comment blanks the closing fence of every block below it, so a report whose fence closes is refused for an unclosed fence. Both orderings have an assertion inside the two cases above, and swapping either turns exactly one case red. ~~**What this row does NOT close is the comment STRADDLE** — balanced in the report, half in the record, because a copied row and a copied block are slices. Its cost was understated by round 1's fix pass, which measured it on a Deferred row where nothing follows: on a verdict row it is three whole sections, executed at `aed3ca0`. It needs a balance-across-the-slice question rather than a never-closed one, and it is at the `# RIDER:` in `swallowed` with that measurement.~~ **Closed 2026-09-08 by #182, and the rider is gone with it.** The balance question it says is missing here is not needed at the destination: `write_record` reads the record back through the shared reader before it writes, so a slice that took half a hider is refused whichever slice took it. **Re-read and re-stamped 2026-09-08 in that work item**, at `2125094`: `swallowed` and `build` both moved — the never-closed pair each of them carried is now one `hiders_close` call, because the ORDER of the two questions is a property of `readable`'s passes and not of any one text, and it had been written out twice with a third text due. The claim's second sentence is struck above; everything in the third and fourth sentences stands, and the two cases that pin the order gained an assertion each, because `open_hider`'s third answer made `HTML comment` alone stop separating a comment that never closes from one that closes outside a block. **The four-mutations sentence above no longer describes the code**: there is one order and one implementation of it, so the two order mutations are one, and it turns both order cases red together — executed 2026-09-08 at `0b99eb9` **Re-read 2026-09-13 by work item 1789296200 (#345), which drifted it.** `build` gained one `cell(...)` call in its field list. It copies no new text from anywhere, so the grid this row is about gains no row and no column. **Re-read 2026-09-24 by work item 1790174138 (#505, #382), which drifted both units.** `build` writes a resolved commit into one cell it already wrote, and `swallowed`'s row loop reads a section to `section_end`; neither adds a text the generator copies nor a pass `readable` blanks by, so the grid is the same grid |
| F3 · what is refused is a report **losing** a section, never a fence **mentioning** one. Both halves of the condition are load-bearing: the heading is among the lines a fence hid, **and** no heading of that name still stands outside a fence — ~~over every heading the generator reads~~ (**narrowed — over the REQUIRED headings only, see R4 of `seal/ledger/1788749195-…`**) | `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `tests/test_the_record_is_generated.py#test_a_fence_quoting_a_heading_is_kept_while_the_real_one_stands@37e0181f` | Read 2026-09-06 in phase 2 at `5721a31`: the `and not reader.sections(lines, heading)` clause opened against the case, which asserts exit 0, the quoted block copied into the record, and the real Deferred table read as written. **Executed** 2026-09-06 in phase 1 and re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-07 | **The tidy-up is dropping the second half of the condition as redundant** — a heading among the hidden lines reads as sufficient on its own, and every refusal case still passes without it. It is what would stop this tool during its own review rounds: a reviewer of the record generator pastes record-shaped blocks, headings and all, so the branch that added the guard would be the first thing the guard refused. The condition is what makes the rule about a loss rather than about a word. **Falsified in scope and corrected 2026-09-07, round 1's 🟡 3 — by the very scenario this cell describes.** A section whose heading is OPTIONAL breaks the premise the two halves rest on: hidden AND absent reads as *swallowed* only where the report must carry the section, and `## Paste-ready fixes` is left out by a round that opened nothing needing a fix. Executed: a report omitting it while its probes fence quoted it was refused at exit 2 with no record, where the base wrote one. The loop now reads `REQUIRED_HEADINGS`; R4 of this work item's fragment carries the narrowing and the false negative it accepts **Re-read 2026-09-08 by work item 1788873610 (#182), which drifted `swallowed`.** The never-closed pair moved out of that function into `hiders_close`; both halves of this condition are in the heading loop, which is untouched, and the loop still runs over the same lines. **Re-read 2026-09-24 by work item 1790174138 (#505), which drifted `swallowed`** in the row loop's section end alone; the heading loop and both halves of this condition are as they were |
| F4 · nothing in the guard reads the `#` character. The lines a fence hid are read off the reader's own two passes — what survives `strip_comments` and is blank after `blank_fences` — so a Python comment at column 0 inside a fence is not a heading, and a Markdown heading inside one is | `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `tests/test_the_record_is_generated.py#test_a_comment_at_column_zero_inside_a_fence_is_not_a_heading@8f378370` | Read 2026-09-06 in phase 2 at `5721a31`: the `hidden` comprehension opened against the case, which asserts exit 0 and the comment line arriving in the record. **Executed** 2026-09-06 in phase 1, where mutating the comprehension to a `startswith("#")` test turned both acceptance cases red; re-run 2026-09-06 in phase 2 at `5721a31`, exit 0 | 2026-09-07 | **The tidy-up is testing `startswith("#")` on the hidden lines**, which reads as asking the question directly and is faster than pairing two passes. A `#` at column 0 is a Markdown heading and a Python comment both, and only the fence tells them apart; the fenced block a probes row owes is usually code, so the direct question refuses the ordinary case. The mutation is named here because it is the argument for the pairing rather than an argument against the constant-keyed shape — F1's tidy-up and this one look like the same simplification from outside and fail differently **Re-read 2026-09-08 by work item 1788873610 (#182), which drifted `swallowed`.** The `hidden` set this claim is about is computed exactly as before — `stripped` is now `reader.strip_comments(report.splitlines())` rather than the same call with the sentinel sliced off, which is the same list line for line, and the `strict=True` zip still states it. **Re-read 2026-09-24 by work item 1790174138 (#505), which drifted `swallowed`.** The row loop's end is `section_end`, which reads the depth of a `#` run and not the character's presence — the guard still reads nothing off `#` to tell a heading from a comment, and the hidden set is computed exactly as before |
| F5 · a heading is only **half** of what a section is to the generator, so the same span rule is asked of the table rows as well — and it asks whether a section's rows stand ONLY inside a fence, never whether any of them is hidden. Two clauses carry that: the section still has visible rows, and the hidden rows are the ones inside THAT section's own range | `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `skills/code-review/scripts/round_record.py#SWALLOWED_TABLE@edf173ea`, `tests/test_the_record_is_generated.py#test_a_fence_hiding_a_whole_table_under_a_standing_heading_is_refused@124eb092`, `tests/test_the_record_is_generated.py#test_a_fence_quoting_table_rows_is_kept_while_the_table_stands@a7e81814` | **Executed** 2026-09-06 in round 1's fix pass: the refusal case seen red at `861ad16` — exit 0 with the record's Deferred section reading `nothing to drain` beside a row the reviewer wrote — and green at `e7d3447`. Both clauses were then measured and **both were green while broken**: dropping either turned no case red at all, which is why the acceptance case exists. With it planted, dropping *the table still stands* turns it red and nothing else, and dropping the positional scoping turns it red and nothing else | 2026-09-07 | **The tidy-up is asking the direct question — is any row of this section hidden.** It reads simpler, it is one clause instead of three, and it refuses the fenced block a probes row owes: `templates/sdd-round.md` asks for a replacement block under the probes table, a reviewer of THIS generator pastes record-shaped blocks with rows in them, and the guard would stop the tool on its own review rounds. That is F3's *mention rather than loss* one level down, and it is why the second clause is not redundant. **The second tidy-up is dropping the index from `hidden`** and asking the question of the whole report at once, which reads as removing bookkeeping: a fence under the probes table then blames a `## Deferred` that is empty for its own reasons, and `nothing to drain` is the answer `templates/sdd-round.md` requires of an empty Deferred section, so the false refusal lands on the ordinary case. **What this row does NOT close is a PARTIAL loss** — some rows fenced beside a table that still stands. It cannot be closed without giving up the limit above, and `overview.md` §Not done carries the judgment and what it costs **Re-read 2026-09-08 by work item 1788873610 (#182), which drifted `swallowed`.** The row loop and its two clauses are untouched. What left the function is the never-closed pair above the walks, which this row's claim does not reach. **Re-read 2026-09-24 by work item 1790174138 (#505), which drifted `swallowed`.** The row loop's SPAN widened — a section now runs to a heading of its own level or shallower, so rows hidden under a `###` inside it are inside the span — and its two clauses are unchanged; `test_a_table_hidden_under_a_subheading_is_still_a_swallowed_table` is the case, red before |
