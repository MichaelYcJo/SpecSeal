## 0.12.1 — 2026-09-20

<!-- specs/1789621028-nothing-reads-a-record-against-the-tree -->
### 1789621028-nothing-reads-a-record-against-the-tree

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| R1 · The sweep's wholeness guard compares the bytes it read against the size of the file it read, so the assertion carries no number that can be wrong | `tests/test_chain_hooks_hardening.py#test_the_questions_are_collected_before_the_work_not_during_it@e305a1b6` | **Executed** 2026-09-17, four mutations applied alone and reverted from bytes kept outside git, exit codes read from `returncode`: `f.read(10)` exit 1, `f.read(2000)` exit 1, the loop's iterable replaced by `()` exit 1, a sentence added to `agents/scribe.md` exit 0. Baseline and restored both exit 0, 51 passed | 2026-09-17 | The floor it replaces was `> 1000`, and the smallest definition is 3764 bytes, so every truncation between 1001 and 3763 truncated all five files and stayed green (#426). Both sides of the comparison are BYTE counts: `f.read(2000)` reads 2000 characters and reported 2006 to 2024 bytes, because these files carry emoji and em-dashes. The emptied loop is caught by the list-equality assertion above the new one rather than by it — `all()` over nothing is true — and the two together close the three mutations the ticket measured |
| R2 · `round_record.py close` refuses to write its own close prefix over a `Grounds` cell that already carries one, and nothing reaches disk when it fires | `skills/code-review/scripts/round_record.py#close@b91242ae`, `tests/test_the_fixes_close_the_record.py#test_re_closing_a_half_restored_record_is_refused_for_every_word@f837c690` | **Executed** 2026-09-17: the three verdict words each refused at exit 2 with the record byte-identical before and after; with the guard deleted, all three fail and a probe reproduces the doubled cell `fixed at 891bfcf; fixed at 891bfcf; executed` at exit 1, on the unrelated `Pass` notice and silent about the duplication | 2026-09-24 | Two arms, because one closes the instance and not the class (§12): `old.startswith(grounds)` is the re-close with the same fix table, which is what a corrected record produces, and `chain.CLOSE_PREFIX_RE` is the cell already carrying a prefix naming a different commit. It REFUSES rather than overwriting (`questions.md` Q4) — overwriting discards the reviewer's sentence, which is what the join at that line exists to preserve. `write_record` runs after the loop the guard raises in, which is what makes the refusal's instruction possible to follow. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1), which drifted `close`.** `--broad-gate` now writes the cell through `kept_broad_gate`, the one path `seal` takes, so a run the cell already holds is kept; the table, the range, the surface and the ordering this row is about are untouched. Re-read 2026-09-24 — #556 brought the comment above `close`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| R3 · Refusing the second write does not block the documented repair: the guard fires on a record whose `Verdict` alone was restored and stays silent on one whose `Grounds` was restored too | `tests/test_the_fixes_close_the_record.py#test_the_documented_repair_still_closes@309a57e0` | **Executed** 2026-09-17: the full repair closes at exit 0 with the row byte-identical to the first close. Seen red under a guard widened to `if old:`, which is the mutation this case is actually about — deleting the guard cannot redden a case that pins an absence | 2026-09-17 | #427 refuses a fix that forbids re-closing, because correcting a record and re-closing it is the documented way out of a record written wrong and the one command for it. This row is the measurement `questions.md` Q4 asked the phase to take |
| R4 · `chain_check` names a record whose `Grounds` cell carries two close prefixes, with NO cutoff, and this repository holds none | `skills/code-review/scripts/chain_check.py#doubled_grounds@abfaa0a0`, `tests/test_chain_check_at_the_pull_request.py#test_the_records_in_this_repository_carry_no_doubled_grounds@9731a197` | **Executed** 2026-09-17: a planted record at exit 1 naming the file, the finding and the repeated text; the same record with one prefix at exit 0; 229 `round-N.md` files walked, 227 with a readable verdict table, 3331 `Grounds` cells, **zero** carrying two | 2026-09-17 | No cutoff is a decision, not an omission. Every other rule in this module carries a `*_FROM` because an absent row has no honest repair on a merged record; a doubled cell is a PRESENT cell that says a thing twice, and restoring it from the round's report is available to whoever wrote it — the line `fix_surface` already draws. A green run is disclosed as "nothing found", never as a catch. Only `fixed` leaves a shape a reader can find: `answered` and `deferred` write the author's own words, so a duplicate of either is indistinguishable from prose that repeats itself, and the write guard covers all three where this covers one |
| R5 · `round_record.py parse_range` refuses a range end that is not a commit somebody can open, at BOTH ends | `skills/code-review/scripts/round_record.py#parse_range@05bbcb74`, `tests/test_the_fixes_close_the_record.py#test_a_range_end_that_moves_is_refused@c583673d` | **Executed** 2026-09-17: `HEAD`, `@`, a branch and a tag each refused at exit 2 at each end — eight cases — with the record's verdict cell still reading `open`. With the refusal deleted all eight fail; narrowed to the second end, the four start-end cases fail and the four finish-end cases pass | 2026-09-17 | An end is accepted when it is seven to forty hex characters AND resolves to a commit it is a prefix of. The second half is what makes the first true; one coincidence stands and is recorded in the code rather than parsed away — a branch whose name is hex and which happens to point at a commit starting with that name. Closing it would mean asking git which refs exist, and the rule would then pass or fail on what somebody else created |
| R6 · `close` writes the resolved range and a derived commit count into the record, and `chain_check` re-reads both halves against the tree behind `RANGE_FROM` | `skills/code-review/scripts/round_record.py#close@b91242ae`, `skills/code-review/scripts/chain_check.py#fix_range@58aaf024`, `tests/test_chain_check_at_the_pull_request.py#test_the_records_in_this_repository_are_not_failed_by_the_new_row@150b18e2` | **Executed** 2026-09-17: a two-commit range writes `` `<a>..<b>`, 2 commits `` matching `git rev-list --count`; a planted record claiming 7 where the tree holds 1 exits 1 naming both numbers; unresolvable ends print and do not fail; moving the cutoff to `1` turns the repository-wide case red. **Re-read 2026-09-17 in round 1's fix pass and one half corrected**: that case read *every* `round-N.md` as producing a notice, which held only while no record carried the row. This work item's own `round-1.md` is the first that does, so the claim is now that a record WITHOUT the row prints and never fails, and that the two groups account for every record. **Round 2 found the arithmetic stronger than that sentence and this row is unchanged by the repair**: the code counted notices as a subtraction from the whole, which asserts that a record CARRYING the row prints none — false the moment its ends stop resolving, which the squash that merges this branch makes true of the one record carrying it. The notices are counted over the no-row group alone now, which is what this claim said all along | 2026-09-24 | Both halves are read because either alone passes what the other catches: the ends say which commits and the count says how many, and a range whose ends still resolve while its count disagrees is what a moving end leaves behind. Unresolvable ends are a NOTICE because this repository squashes feature branches — `Target SHA` falls back to a pull head and a range has no equivalent, since a pull head carries the commits and not the arithmetic between them. What it cannot see is stated beside it: a range with the right count can still be the wrong range, and `fix_surface` measures from the same range so the two agree by construction. **Re-read 2026-09-24 by work item 1790174138 (#436), which drifted `fix_range`.** The row gained the pending arm `fix_surface` already ran on its own rows — the starting value beside a `round-N` in `Fixes checked by` is refused, behind `RANGE_FROM` — and both halves this row claims, the ends and the count, are read exactly as before; the fragment's A3 carries the arm. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1), which drifted `close`.** `--broad-gate` now writes the cell through `kept_broad_gate`, the one path `seal` takes, so a run the cell already holds is kept; the table, the range, the surface and the ordering this row is about are untouched. Re-read 2026-09-24 — #556 brought the comment above `close`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| R7 · A measurement is reproducible only against a named range AND a named version of what measured it | `docs/round-record-spec.md#"## The fix range — `Fix range`"@c1cf414f` | **Executed** 2026-09-17 over the whole-branch row of `seal/specs/1789034970-…/survivors.md` — a row names content and never a position, so the line number is left off. As written, `survivor-check --range origin/release/v0.10.0...HEAD` exits **2** — the release branch was deleted, so the command reads nothing. Pinned to `d35c874...ce0f9fe`: **3** places under `survivor_check.py` as it stood at `d35c874`, **2** under the script as it stands, over 836 files and 88 removed sentences | 2026-09-24 | The 3 is exactly what #344 claims a re-run gives, so pinning the range reproduces the ticket and pinning it is still not enough: 60 insertions and 11 deletions landed in that script since. The row's *sixteen* was an accumulation across the branch's life rather than one run's output, which the row itself says two sentences down. This is the class one level above the one #344 filed, and it is why the fix range's ends are recorded rather than its count alone · Round 3 added what the row could not say about itself: `ce0f9fe` is a squashed-away branch tip that resolves in no clone, so this execution is a past reading and not one a reader can repeat — the rule in the claim, met by the evidence for it. #439. **Re-read 2026-09-24 by work item 1790174138 (#436)**, which added one row to the subsection's check table; the measurement paragraph and its command are unchanged. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. |
| R8 · The corpus the prose-parsing alternative was refused on, measured rather than carried forward | `docs/round-record-spec.md#"## The fix range — `Fix range`"@c1cf414f` | **Executed** 2026-09-17 over `seal/specs/*/rounds/*-fixes.md`, by the command `docs/review-chain-spec.md` §*The fix range* names — one backticked range from each file's first eight lines. At `56945007`: **39** files, **15** stating a range, **5** ending `HEAD`, **12** distinct sentence forms. At this work item's tip the same command prints **4** ending `HEAD`, because the work pinned one of them | 2026-09-24 | `spec.md` and `plan.md` of this work item say 11 files, eleven spellings and 2 naming `HEAD`, and they also say 223 round records where 227 have a readable verdict table. Three of the frame's figures did not reproduce, which is this work item's own class in the document that frames it. None of them changes a decision — 15 files in 12 sentence forms refuses the parser more firmly than 11 in eleven did — and what matters is that the measured figures are what shipped into the specification, with the command that produces them named beside them. `phases/phase-3.md` holds the comparison. **Re-read 2026-09-24 by work item 1790174138 (#436)**, which added one row to the subsection's check table below the figures; the figures and their command are unchanged. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. |

<!-- specs/1789687448-a-tracked-file-the-tree-deleted-stops-the-sweep -->
### 1789687448-a-tracked-file-the-tree-deleted-stops-the-sweep

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| R1 · A corpus built from a git path listing is split into what is on disk and what is not, by one predicate the whole suite shares, and both halves are returned so a caller can decide what a skip means to it | `tests/conftest.py#on_disk@c6b026c7`, `tests/test_a_shrunken_corpus_declines_to_judge.py#test_on_disk_splits_a_listing_by_what_the_tree_actually_has@9171fb1d` | **Executed** 2026-09-18. On the tree a fold leaves — `seal/ledger/*.md` removed from disk, removal unstaged — the six modules print `84 passed` at exit 0. The same tree with the classifying line reverted to `present.append(rel)` gives `test_only_neutral_domains` and `test_only_fixture_user_paths` a `FileNotFoundError` each at exit 1, which is #432 at its own coordinate | 2026-09-18 | `os.path.isfile` rather than `os.path.exists`, and one case pins the difference: a listed path that is now a DIRECTORY has no content to read and `open` raises on it exactly as a missing file does. Returning the pair rather than filtering silently is what phase 3 needed — a caller that cannot see what was skipped cannot tell a skipped file from a deleted entry |
| R2 · A check whose verdict needs the whole corpus declines to judge rather than judging wrongly, with a reason naming every missing path, and it declines only when it has a finding a skip could explain | `tests/conftest.py#decline_if_shrunken@6a48d192`, `tests/conftest.py#shrunken_corpus@38ab0fa6`, `tests/test_no_document_names_the_old_roots.py#keep_entries_not_in_use@b7c54a1e`, `tests/test_a_script_says_which_interpreter_it_needs.py#classifications_of_nothing@73a9367d` | **Executed** 2026-09-18, four mutations applied alone to `tests/conftest.py` and reverted from bytes kept outside git, exit codes read from `returncode`: `pytest.skip(...)` → `pass` gives 4 failed 34 passed; the reason's `", ".join(sorted(missing))` dropped gives 5 failed 33 passed; the reason's `{what}` replaced by a fixed phrase gives 4 failed 34 passed; `isfile` → `exists` gives 1 failed 7 passed. All four exit 1 | 2026-09-18 | The condition is what keeps a mid-edit tree from turning the check off: a skip can only make an entry LOOK unused, never used, so a run that finds everything in place has the right verdict whatever it skipped. Every path by name rather than a count, because a claim removed from a corpus without a word is the direction `seal/follow-up.md`'s first row calls the one a checker of claims must not fail in |
| R3 · A vacuity floor does not decline. `len(files) > 30` is what stops the scan passing on an empty read, so only the named-path half of the same case declines — and only when EVERY named path it finds absent is one the working tree deleted, so a real loss of coverage is still reported beside a neighbour that is merely mid-edit | `tests/test_no_document_names_the_old_roots.py#uncovered@82ece75d`, `tests/test_no_document_names_the_old_roots.py#test_a_skipped_file_does_not_read_as_lost_coverage@51d98bf8` | **Executed** 2026-09-18: `uncovered(files=[], missing=list(COVERED))` raises the skip naming both paths and the `what`; `uncovered(files=[], missing=[])` returns both paths as a finding; `uncovered(files=[], missing=[COVERED[0]])` returns `[COVERED[1]]` rather than declining, which is round 1's 🟡 3. `bin/test -q tests/test_no_document_names_the_old_roots.py`, exit 0, 16 passed | 2026-09-18 | Q3's third case, found by reading the twenty-eight `test_` functions of the five modules rather than by either ticket. `skills/implement/SKILL.md` leaving the corpus through an unstaged `git mv` made the case report the scan as no longer covering a file that is merely somewhere else — a verdict about coverage taken from evidence about the working tree |
| R4 · No scope in `tests/*.py` derives a path list from git without either applying the shared predicate or being classified with grounds, the unit is the CALL SITE rather than the function name, and of the three questions that reading answers only the one a skipped module can explain declines — a scope the reader FOUND that nothing accounts for, and a scope whose call count moved, are evidence about a module on disk and judge what remains | `tests/test_a_shrunken_corpus_declines_to_judge.py#test_no_scope_in_the_suite_lists_paths_from_git_without_a_guard@c43f2df0`, `tests/test_a_shrunken_corpus_declines_to_judge.py#_derives_a_path_list@b3673578`, `tests/test_a_shrunken_corpus_declines_to_judge.py#PATH_LIST_CALLS@ae6953e5` | **Executed** 2026-09-18: 26 scopes across 17 modules at exit 0; a module planted in `tests/` with an unguarded `git ls-files` scope and tracked with `git add -N` gives exit 1 naming it; `suite_modules` returning `[]` gives exit 1 on the reader's own case and on the vacuity case; `test_release_hygiene.py#tracked` returning `listed, []` gives exit 1 at the `_mentions` assertion. **Round 1 🔴 1, re-measured after the fix**: with `tests/test_release_hygiene.py` off disk and the removal unstaged, the module was `2 failed, 12 passed` exit 1 and is now `13 passed, 2 skipped` exit 0, the reason naming the missing module. **Round 2 🟡 1, re-measured after the split**: an unguarded scope planted in a tracked test module was exit 1 and named on a whole tree, and exit 0 `13 passed, 2 skipped` and named NOWHERE with one tracked test module off disk; it is now exit 1 and named on both trees. Four further mutations each red alone — the decline moved back in front of the positive halves, `miscounted_scopes` reporting nothing, the decline removed, and the decline made unconditional | 2026-09-18 | The reader found a sixth helper while it was being written — `tests/test_a_finding_id_is_a_bare_integer.py#committed_records`, which lists with `ls-tree HEAD` and opens from the working tree. The frame's enumeration searched for `git ls-files` and could not see it, which is why `LISTS_PATHS` is kept wider than the forms in use. What it still cannot see is stated in the module: a listing word built from a variable, and a scope handing its list to a helper in another module |
| R5 · A fold leaves one missing path per ungathered ledger fragment, and produces no skipped case: only `tracked_text_files` has a corpus reaching `seal/ledger/`, and both of its callers are sweeps that judge what remains | `tests/test_no_real_identifiers.py#tracked_text_files@f65df380`, `docs/release-checklist.md#"## 3. Verify before committing — all of it, here"@35603f23` | **Executed** 2026-09-18. `python3 .github/scripts/fold_ledger.py --dry-run --version 0.12.1` names one fragment; `gather_changelog.py` removes nothing, and 79 changelog fragments stand. With that fragment removed from disk and the removal unstaged, the six modules print `84 passed, 0 skipped` at exit 0 | 2026-09-18 · 2026-09-24 | Q2. `plan.md` §*Operational impact* predicted `… passed, N skipped` after a fold and that is false for the fold; it is true for a deletion under `docs/`, `skills/`, `templates/` or a shipped `.py`, where a declining check reads the corpus. The checklist sentence is written to the measurement, and the divergence is in `overview.md`. The number moves with the release: one fragment per work item that has not been folded yet **Re-read 2026-09-24 by work item 1790173209 (#363).** §3's table row on the timer sweep gained a sentence saying a tagged version is history; the fold and the skipped-case behaviour this row is about are untouched, so the claim holds. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** §3 gained the release that first has `seal/releases/` and its three readings; a fold still removes paths only under `seal/ledger/`, so the claim holds **Re-read 2026-09-24 by round 1's fix pass of work item 1790208593 (#547):** §3's release reading now says the `ok` total rises at the split while drifted, broken and the distinct-coordinate count do not; a fold still removes paths only under `seal/ledger/` **Re-read 2026-09-24 by round 2's fix pass of work item 1790208593 (#547):** §3's release reading compares table lines by `grep -c '^\|'` over every ledger file before and after the split, with exit 0 and no drifted or broken row; a fold still removes paths only under `seal/ledger/` **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** the suite line is `bin/test -q` in place of the hand-run `uv run … -n auto` form (#337); the fold and skipped-case sentences this row is about are unchanged. The claim stands. Re-read 2026-09-24 by work item 1790208643 (#561): §3's table-line reading is one glob-free `find … -exec cat {} + 2>/dev/null \| grep -c '^\|'` in a fence, compared with step 2's before-reading; the claim holds. |

<!-- specs/1789721571-the-gate-reads-an-example-and-names-rows-nobody-wrote -->
### 1789721571-the-gate-reads-an-example-and-names-rows-nobody-wrote

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| R1 · Every sentence `broad-gate` prints about what a line below which rows were written cost has a subject in each state that line can be reached in: three of `missing_row`'s four arms ask what was actually written below, and the fourth needs no condition because its clause *this one included* names the quoted line itself | `skills/verify/scripts/broad_gate.py#missing_row@85b31017` | **Executed** 2026-09-18. Each of the three conditioned arms was run against its own unfixed code first and seen red on its own sentence: a last-row `Broad gate` line at the `mine is stopper` arm, a `Broad gate` line as the table's only row at the `stopper is None` arm, and a stopping line written last at the `reached` arm. `bin/test tests/test_the_seal_is_taken_once_by_the_sealer.py -q` is `109 passed, 3 skipped` at exit 0 after the fix, and was `4 failed` on the four new site cases before it | 2026-09-18 | The `else` arm is examined and left, which `spec.md` §*Out, each with why* records. Two existing cases asserted the old wording for a last-row fixture and were rewritten; the arm each was written to pin is unchanged and still pinned by the assertion beside it |
| R2 · `refusal`'s `below` cannot answer what lies under the line `missing_row`'s first arm quotes, because that list holds the rows written under the STOPPING line and that arm is the one where no line stopped the reader — so it reads what the table reader returned, which in that arm alone is exactly the rows below the quoted line | `skills/verify/scripts/broad_gate.py#rows_read@4a4ca4d4` | **Executed** 2026-09-18. `hooks/config.py#refusal` over `\| Broad gate \| bin/test -q \| tee out.txt \|` as a table's only row and over the same file with `\| Mode \| shared \|` written under it returns `below == []` both times, while `config_rows` returns `[]` and `[('Mode', 'shared')]`. Two mutations applied alone to `rows_read` and reverted from bytes kept outside git — the body replaced by `return []` and by `return [("x", "y")]` — each give the site-1 case `1 failed` at exit 1, one direction each | 2026-09-18 | The spawn prompt and `plan.md` §*Alternatives considered* both say every arm asks `below`; that is true of three of the four and false of this one. The safety of the substitute is structural rather than incidental: a row parsed ABOVE the quoted line would have set `stopper` to that line, which is a different arm. `overview.md` carries the divergence with both sides quoted |
| R3 · The hidden-row refusal names other rows only where other rows exist: `hides_this_row` is true exactly when `below` holds this gate's row, so a one-element `below` is this row alone and the *every other row* sentence is replaced there | `skills/verify/scripts/broad_gate.py#hides_this_row@2c2be78c`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_hidden_row_alone_under_the_stopping_line_has_no_others@69bff368` | **Executed** 2026-09-18. A refused `\| Notes \|` line stopping the reader with `\| Broad gate \| bin/test -q \|` as the only row under it gives `refusal(...)[1] == [('Broad gate', 'bin/test -q')]`, reaches this branch, and printed *Every other row under that line is gone the same way* before the fix — the case was red on exactly that assertion. The same file with `\| Record language \| Korean \|` added keeps the old sentence | 2026-09-18 | This is the site the two tickets left open as a question: `questions.md` records it as *unreachable with `below` empty and reachable with `below` holding one row*, read rather than run. Measured here, and it holds |
| R4 · This repository's own `seal/config.md` is answered identically by every walk `broad-gate` uses — mode `shared`, the `Broad gate` command byte-identical to the row as written, and no refusal built at all | `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_this_repositorys_own_config_is_answered_exactly_as_before@1576b45e` | **Executed** 2026-09-18, before and after the change, by ad-hoc reading first and then as a case: `declared_mode` is `('mode', 'shared')`, `refusal(text)` is `([], [], None)`, `broad_command` returns `bin/test -q && uvx ruff check . && uvx ruff format --check .` and that string appears in the file as the row's value, and `not_as_written` returns None | 2026-09-18 | A11 of `spec.md`. It was a reading somebody took once per phase and is now a case, so phase 2 — which changes what the reader reads, not just what a refusal says — meets it without anybody remembering to look |
| R5 · A line inside a fenced code block is not part of any `\| Item \| Value \|` table, and one rule says so in front of all three walks of it — the gates' reader, the mode gate's reader and `seal mode`'s writer | `hooks/config.py#unfenced@f14eec51` | **Executed** 2026-09-18. With the rule absent, a fenced example above the live table gives `config_rows` `[('Mode', 'local'), ('Broad gate', 'EXAMPLE')]` and the live table below is never reached; with it, the live rows. Four mutations applied alone to this unit and restored from bytes kept outside git — no fence ever opens, any run closes whatever its character, a backtick fence's info string may hold a backtick, a run shorter than the opening one still closes — each turn a case red, one direction each | 2026-09-18 | The fourth of those mutations survived the first case set, which is what `test_what_counts_as_a_fence_is_commonmarks_rule_as_far_as_it_goes`'s four-backtick assertion was added for. The rule filters in FRONT of each walk: neither half of `config_rows`'s stop rule moved, and a table can now span a fenced block where the fence's delimiter line used to end it |
| R6 · The writer finds the line it overwrites by the same fence rule the readers use, so a fenced example cannot be the row `seal mode` rewrites while every gate reads the live one | `skills/implement/scripts/seal.py#table_span@872d300e` | **Executed** 2026-09-18. `write_row` over a file carrying a fenced `\| Mode \| local \|` above a live table changes exactly one line — the live one — and leaves the fenced block byte-identical; before the rule it rewrote the fenced row and left the live table alone. Asserted on the bytes, and again over a CRLF file, where the write keeps one ending throughout | 2026-09-18 | The shared unit hands back each surviving line's own INDEX rather than surviving text, which is what lets this walk use it: `with_row` overwrites `lines[mode_at]`. A helper yielding text alone would have forced a second fence rule for the writer, which is the split `hooks/config.py` exists to prevent |
| R7 · A `Broad gate` line that exists only inside a code fence is quoted back with the act that reaches it — move the row, or close the fence above it that nothing closed — and never reported as an absent row | `skills/verify/scripts/broad_gate.py#fenced_row@84be339f` | **Executed** 2026-09-18. Over a config whose only such line sits in a fenced block the refusal carries the line as written and does not carry `has no ` + the row name; over a config with no such line anywhere the absent-row refusal is unchanged. The unit stubbed to return None turns three cases red. Run end to end as well: a repository whose live table sits under an unclosed fence exits 2 with nothing run and the fenced line named — the same tree exits 0 with the stamp printed when the fence rule is absent | 2026-09-18 | This is the one question about a fenced line anybody asks, and only on the refusal path: `hooks/config.py` and `hooks/mode-gate.py` say nothing about a fence, because a `PreToolUse` hook that prints is noise on every Bash call. It takes the complement of what the one reader shows rather than walking the file by a rule of its own |
| R8 · `seal mode` writes the `Mode` row only if the row reads back, and a refusal leaves the person's file byte-identical | `skills/implement/scripts/seal.py#write_row@1d969e6e` | **Executed** 2026-09-18. Before the guard, a `config.md` whose table is swallowed by a fence nobody closed grew by a table a run — 9 to 13 to 17 to 21 lines over three runs, `declared_mode` `('none', '')` throughout, `write_row` returning `''` each time. After it the file is unchanged and the refusal names the cause. A `config.md` whose last line carries no ending reached the same guard for a different reason and was told to close a fence it does not have; the message now branches on whether a fence is actually open, measured with `with_row` replaced by one that changes nothing | 2026-09-18 | Round 1's 🔴 and round 2's 🔴, which are one unit and two causes. The enumeration is what moved: the guard's sentence named the cause it INFERRED, and it now names the one it checked |
| R9 · One fence walk answers both questions anybody asks of a fence — which lines the table walks may see, and whether a block was left open — so no second fence rule exists to disagree with the first | `hooks/config.py#fence_map@79dfca74` | **Executed** 2026-09-18. `unfenced` is this walk's surviving lines and every table walk still reads the file through it: the three walks' answers are unchanged over this repository's own config, over a fenced example above a live table and over a CRLF file. Four mutations of the walk each turn a case red | 2026-09-18 | Round 1's 🟡 2. The open-fence state was already computed and thrown away at the `yield`; keeping it is what lets `broad-gate` tell a row pasted into an example from a row the reader cannot reach |
| R10 · The refusal names an unclosed fence only where the fence is ABOVE the row it quotes | `skills/verify/scripts/broad_gate.py#fence_left_open@baaab8cd` | **Executed** 2026-09-18. A config whose row sits in an example block that closes, with a second block opened and never closed below it, printed *Close that fence* and now prints *Move the row into*; the same file without the trailing block printed *Move the row into* both times. Asking about the whole file is still what a caller gets by passing nothing | 2026-09-18 | Round 2's 🟡 2. The sentence said *a fenced code block ABOVE it* while the question asked about the file, so the person was told to close a fence that has nothing to do with their row |

<!-- specs/1789919879-the-outside-contributor-has-no-procedure -->
### 1789919879-the-outside-contributor-has-no-procedure

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| R1 · The refusal a rejected pull request reads names both causes it cannot tell apart — a release that forgot the version bump, and a contribution filed against `main` — asserts neither, and says which of the two edits is the wrong one | `.github/workflows/hygiene.yml#"- name: a change to what ships must move the version"@a9eefea8`, `tests/test_the_release_check_watches_what_ships.py#refusal@bdd23a0c` | **Executed** 2026-09-21. Five cases red against the unfixed workflow (`the refusal never mentions the base branch…`, quoted whole in `phases/phase-2.md`), 35 passed after. The two cases pinning what must NOT change are green in both trees, so each was mutated alone and restored from bytes kept outside git: the guard's `exit 0` and `fi` deleted → 1 failed; only the `exit 0` deleted → 1 failed; the condition replaced by `if false` → 1 failed; `exit 1` changed to `exit 0` → 1 failed; restored → 1 passed, `cmp` identical | 2026-09-21 | The step's logic is untouched, and that is the claim's point: condition, exit codes and the set of refused pull requests are identical before and after, so the whole change is the text a refused author reads. **The first form of the guard case survived its mutation** — a `re.S` span from `then` to `exit 0 … fi` reached a *second* `if … exit 0 … fi` in the same step, the `-z "$ships"` early return. It is rewritten to assert the guard closes before the next `if`. The precedent it was modelled on (`tests/test_a_release_cannot_ship_an_untrue_milestone.py#hygiene_step`) warns about exactly that mutation and its own step has no second `if` to be caught by it. Reading the precedent was not enough; running the mutation was (#443) |
| R2 · `CONTRIBUTING.md` states which ceremonies of this repository's own workflow a contribution is exempt from, and the list is bound to the guard that makes it true rather than left as a promise | `CONTRIBUTING.md#"## Opening a pull request"@5df50125`, `tests/test_the_contributor_has_a_procedure.py#test_the_exemption_list_says_which_guard_makes_it_true@c74e2083` | **Executed** 2026-09-21: 13 of the module's 14 cases red against the tree before the section existed — the fourteenth is `test_the_check_can_fail`, which pins the reader raising on a guide with no such section and so passes correctly in both trees — and 14 passed after | 2026-09-21 | The pin is minimal by decision (`questions.md` Q6): a case per sentence goes red for a reword, which teaches people to write less prose, and prose is what this section is. What it pins instead is the phrase `exits 0 unless the base is `main``, because an exemption list with no guard named is the sentence a new always-on CI step falsifies while every check stays green — the six-month failure `plan.md` predicts. Every other case pins an identifier a reader searches for. The section is the file's first `## ` heading by assertion, not by convention: it used to open on how to run the suite, which is the fourth thing a newcomer needs. **Re-read 2026-09-21, round 1 fix pass (🟡 3 and 🟡 4).** The anchored section drifted twice in that pass and the claim was re-read against both edits, not re-stamped on sight. The grounds sentence above the table used to say every step that would demand one of these exits early on any other base; three of them carry no base guard and pass for a different reason, so the sentence now names which steps do which and sends a reader to the row. The `ledger` job joined the section below as a second check that can ask a contributor for something they do not have, since a BROKEN anchor is exit 2 where drift is a warning. The pinned phrase this row is about, `exits 0 unless the base is `main``, sits in a table cell and neither edit touched it. The module is 17 cases now rather than the 14 this row's Verified behavior cell records: three came from 🟡 5, a parametrized case holding the branch-by-convention rule over `README.md`, `README.ko.md` and the pull request template, each seen red on its own surface **Re-read 2026-09-21, third edit to this unit and the second re-stamp.** Round 2's correction 9 split the always-on sentence: it said three checks pass for a different reason while five run and two — the ledger and the survivor sweep — can refuse. The claim this row makes is unchanged, because it is about the list being BOUND to a guard rather than about the sentence above it, and `test_the_exemption_list_says_which_guard_makes_it_true` is green at the new hash. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** the section changed only where the ledger paragraph names `seal/releases/`; the exemption list is unchanged, so the claim holds |
