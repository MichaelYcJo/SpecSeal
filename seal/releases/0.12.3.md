## 0.12.3 — 2026-09-21

<!-- specs/1789996775-the-gate-states-what-its-own-fixes-disproved -->
### 1789996775-the-gate-states-what-its-own-fixes-disproved

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| W1 · `git check-ref-format --branch` EXPANDS `@{-N}` and then checks what it expanded to, so `@{-1}` is accepted while `HEAD`, `HEAD~1`, `base@{u}` and `topic@{1}` are refused — and `names_a_branch`'s docstring states that property rather than the class `@{…}`, which is not the class the command refuses | `skills/verify/scripts/broad_gate.py#names_a_branch@d5d64ebe`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_the_guard_states_the_property_the_command_actually_has@5c1b04bb` | **Executed** 2026-09-21, on git 2.54.0, in a scratch clone with two branches and one switch back so `@{-1}` resolves. Exit codes read directly from `subprocess.run().returncode`, never through a pipe: `@{-1}` exit 0 with `base` on stdout, and `HEAD`, `HEAD~1`, `base@{u}`, `topic@{1}` each exit 128 with *is not a valid branch name*. The case was red before the docstring moved, at *the docstring does not name the spelling it accepts* | 2026-09-21 | The stdout is the half that decides the wording. A command that merely ACCEPTED `@{-1}` would leave open why; printing the branch it expanded to is what makes *expands, then checks* the statable property, and it is the reason `@{-1}` reaching the resolver is right rather than a leak. Round 2's finding 10 measured the same five spellings and this row re-ran them rather than restating the table (`questions.md` Q2) |
| W2 · The printed line names a runner's ref only where git says a ref could have that name. Where the label built out of the given spelling is not a legal refname, the line says the runner has no counterpart for that spelling instead of naming one, and the question is asked of git rather than of a pattern written in `broad_gate.py` | `skills/verify/scripts/broad_gate.py#a_runner_could_hold@22f18b55`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_the_line_does_not_name_a_ref_a_runners_checkout_cannot_hold@c4a50f96` | **Executed** 2026-09-21. Red first against the shipped line, reproducing round 2's finding 10 whole: *— not the origin/@{-1} a runner reads —*. Green after. `git check-ref-format refs/remotes/origin/<spelling>` separates the class in a scratch repository — `…/base`, `…/release/v0.12.3` and `…/58f3fd5` exit 0, `…/@{-1}`, `…/HEAD~1`, `…/base@{u}` and `…/topic@{1}` exit 1 — and so does the bare `origin/<spelling>` label the gate actually builds. Mutated to `return True` and the case turned red alone | 2026-09-21 | `origin/HEAD` is the one spelling `check-ref-format` accepts that no `--base` reaches, and the mechanism is one link past the cause: `names_a_branch` refuses `HEAD`, so resolution leaves the ref equal to the spelling given, so `Base.moved` is False and `moved_line` returns before the guard is ever called. Round 1 drove the resolver rather than reading it — `--base HEAD` and `--base origin/HEAD` both return None — and the first wording of this cell named the cause without the step that carries it. Both guards are needed and neither is the other's duplicate. The case asserts the PRINTED sentence and not the guard (`agent-contract` §14): `agents/sealer.md` has the sealer quote the line verbatim into a report, and a later change that builds the runner label differently would leave the guard answering about a string nobody prints |
| W3 · The gate's own test module says what its branch did to `tests/test_the_seal_is_taken_once_by_the_sealer.py` at all three of its sites — all but one assertion reading as it did, the one that moved being the `Broad gate` cell's base half — rather than at the one site a grep over the corrected sentence reached | `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_a_repository_with_no_remote_at_all_resolves_to_the_ref_as_given@9642e796` | **Read**, not executed, 2026-09-21. The three sites are prose, so nothing runs them: `grep -n "byte-identical\|exactly as it did\|reading as it did"` over the module returns two lines and neither claims the module was untouched — one reads *all but ONE assertion … reading as it did* and the other *It is NOT byte-identical*. The module stays green at 33 passed. The answerer for whether a third copy of the claim survives somewhere is the review chain, which found the first two | 2026-09-21 | Only one of the three sites is inside a unit an anchor can name; the module docstring and the fixture comment sit at file level. That is the shape of the defect rather than an accident of it — the claim was standing in prose no unit encloses, which is why `survivor-check` was right to print nothing and why three review rounds each found one coordinate of a claim that had more (`agent-contract` §12) |

<!-- specs/1789996780-the-census-and-the-tie-that-nothing-holds -->
### 1789996780-the-census-and-the-tie-that-nothing-holds

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| D1 · The bound on the marker's qualifier is held by a property over the real corpus, not by a literal spelling — every candidate site an unbounded census finds is one `MARKER` also sees | `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_the_bound_covers_every_candidate_marker_site_the_corpus_carries@359304a2`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#candidate_sites@1a6b3255` | **Executed** 2026-09-22. Green as shipped over `seal/ledger.md`. **Red** with `MARKER`'s bound narrowed to four. What the run actually printed was `1 of 425 candidate marker site(s) fall outside the bound on MARKER's qualifier` — 425 being the corpus as it stood when the mutation ran, before this work item made its own ledger edits, and the figure is quoted from the run rather than re-taken afterwards. It names the file, the run length, the spelling `Re-read and re-stamped a third time <date>` and the row as `(prose, outside any row)`. **Green again** with the bound still at four and `candidate_sites` rewritten to walk `cc.MARKER` — the pair is the evidence, and the second run is what makes the instrument choice load-bearing rather than a preference. **Both legs re-run independently by review round 1** at `55ae1b63`, which reproduced them and reported `1 of 429` on the red leg. The case also pins two things the round found held by nothing: `site_row`, whose body could be replaced with `return ""` leaving the module at 50 passed, and `ledger_text`'s index route, which is the branch nothing else reaches. **Re-read after the broad gate**, which refused the branch because `ledger_corpus` derived a path list from git without the predicate five other helpers in this suite share: it now splits its listing with `conftest.on_disk` and is classified in that module's `APPLIES_THE_SHARED_GUARD`. The claim here is unaltered — what changed is that the bespoke `Path.exists()` test, which is true of a directory and raised `IsADirectoryError` on one, gave way to `os.path.isfile` | 2026-09-22 | #469, after the bound had been wrong twice and each repair had pinned one more literal spelling. The case asserts no count on purpose: a number over this corpus is stale the next time anybody records a correction, which is #470. It reads the corpus through the module's own `LEDGER` and `FRAGMENTS`, never a list written in the case, because a hard-coded list of the three fragments would have gone blind at the 0.12.2 fold that deleted them. It refuses an empty corpus and a corpus with no candidate site, since either would make it pass while proving nothing |
| D2 · The census instrument walks verb by verb, because one `verb … date` expression over the file drops any site whose second verb stands inside the first match | `tests/test_a_merge_cannot_silently_drop_a_correction.py#candidate_sites@1a6b3255` | **Executed** 2026-09-22 while the case above was being written. Over `seal/ledger.md`, the consuming form found two candidate sites fewer than `MARKER` matched; the verb-by-verb walk finds exactly as many as `MARKER` matches, with none it does not see | 2026-09-22 | The census meant to be strictly wider than the pattern under test was narrower than it — round 1's mistake of #424 arriving by a third route, caught before it shipped rather than a round later. `Corrected in review. Re-read <date>.` is the shape: the first verb's gap is not a lowercase run, but the match has already consumed the second verb. **The wideness is a property, not a count that happened to agree** — review round 1 established it by reading rather than by measuring: `MARKER`'s gap can never span a date, so an unbounded walk taking the first date on the line discards nothing `MARKER` could reach. This row argued only the consuming form's narrowness until that was added |
| D3 · The parent-naming rule's tie clause is held — where both parents lose the same count of a marker, the report names the FIRST parent | `skills/evidence-check/scripts/correction_check.py#examine@73c871f1`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_a_tie_falls_to_the_first_parent@05b9a1c2` | **Executed** 2026-09-22. Green on shipped code: a merge where both parents carry one marker and the resolution drops it exits 1, prints one entry and names the first parent, and the case asserts the second parent's SHA is absent. **Red** with `examine`'s walk changed to `for parent in reversed(kin)`, which leaves every other case in the module green | 2026-09-22 | #471, and round 2's finding 8 of #424 had pinned only the first clause. The tie is the common path rather than the edge: every marker older than the fork is carried by both parents, so both lose the same count and the tie-break decides which SHA a reader opens. Nothing in the code names the first parent — the behaviour is `max` returning the first of equal keys over a dict built in `kin` order — so the sentence a person acts on rested on an implementation detail of a builtin (`agent-contract` §14) |
| D4 · The bound of five is justified by the tree's longest spelling wherever it stands, and no table row carries a qualifier longer than three words | `skills/evidence-check/scripts/correction_check.py#MARKER@17486986` | **Executed** 2026-09-22 over `seal/ledger.md` at this branch's tip, splitting the unbounded census by whether the site stands on a table row. On rows: runs of 0, 1, 2 and 3 words only. In prose: one run of 2, one of 3, and the single run of 5. So the survival test, which acts per row, would return the same verdicts today at a bound of three. **Re-taken by review round 1 with a second instrument written independently of this branch's** and agreeing on every digit: 429 sites against 429 `MARKER` occurrences with none unseen, 426 on 204 rows, 3 in prose, 385 bare, 44 qualified in ten spellings, run lengths 0: 385, 1: 23, 2: 9, 3: 11, 5: 1, and bounds 428 / 428 / 429 / 429 / 429 at three, four, five, six and eight | 2026-09-22 | Measured because the census note restated a bound comparison without ever splitting it, and the split had never been taken in three review rounds. The bound is NOT narrowed on this ground: the same hands that wrote a five-word qualifier into this file's prose will write one into a row, and `markers()` is applied to whole-file text as well as to rows. What the bound answers to is a property, which D1 holds; this row records what the figure behind it actually is. **A11's *no exit code, verdict or printed line changes* is verified by construction rather than by reading**: review round 1 parsed both revisions of the module across the range and dumped them with docstrings removed, and the syntax trees are identical. Everything this work item moved in that file is prose |
