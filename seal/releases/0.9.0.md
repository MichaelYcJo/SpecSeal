## 0.9.0 — 2026-09-07

<!-- specs/1788735085-a-loaded-file-naming-a-real-version-is-a-timer -->
### 1788735085-a-loaded-file-naming-a-real-version-is-a-timer

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows over two tickets that share a branch. R1 is #179: what the version
check now refuses and, more usefully to a later reader, what it deliberately
still allows. R2 is #98's re-measurement, which is the half a later session
cannot re-derive from the diff — the diff shows a comment changing, and not
the five listings that decide which sentence is true. R3 is the failure
message, which #179's *Done when* makes a deliverable rather than a courtesy:
the reason a real version may not be written has to reach the next author
writing one, and the refusal is where they will be standing.

**R1's exemption list is the row's real content.** The rule itself is one
comparison and a reader can see it. What a reader cannot see is that three
exemptions were each argued separately and that the illustrative one carries a
precondition of its own, because an allow-list entry that stops being true has
no way to say so.

**R2 corrects `seal/ledger.md`'s r5 1 / r5 2 row rather than removing it.**
That row's clause stated the false sentence, so the correction is written into
`seal/ledger.md` itself — `CLAUDE.md`'s one exception to the fragment rule,
for a row an edit falsifies. Its anchor was not removed and still resolves;
what moved is the content inside it, which is re-verification. The new claim
is R2 here.
-->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · a loaded file may name a version **below** the running one and may not name one at or above it, so a document naming a version that has not shipped goes red on the commit that writes it rather than on the release that ships it. Three exemptions and no fourth: the illustrative value the repository already tells authors to write, `docs/experiments/` as a prefix covering only files whose OWN NAME begins with the date, and a version belonging to another product pinned to the file that names it | `tests/test_release_hygiene.py#timers_in@608ccc2e`, `tests/test_release_hygiene.py#as_release@6a784993`, `tests/test_release_hygiene.py#is_a_record_of_a_moment@6876889a`, `tests/test_release_hygiene.py#shipped_versions@b84a86f5`, `tests/test_release_hygiene.py#test_no_loaded_file_names_a_version_at_or_above_the_running_one@e34c88fd`, `tests/test_release_hygiene.py#test_the_illustrative_version_is_not_one_this_repository_could_ship@66b04c20`, `tests/test_release_hygiene.py#test_a_version_below_the_running_one_is_history_and_is_kept@f29571e6`, `tests/test_release_hygiene.py#test_a_two_digit_component_compares_as_a_number@b68a741a`, `tests/test_release_hygiene.py#test_a_number_that_is_not_a_version_is_not_read_as_one@a4201282`, `tests/test_release_hygiene.py#test_a_version_that_ends_a_sentence_is_still_a_version@dee529cd`, `tests/test_release_hygiene.py#test_the_experiments_prefix_covers_only_a_dated_record@85ffdd56`, `tests/test_release_hygiene.py#DATED_RECORD@d7ff41c4` | **Executed** 2026-09-07. The widened case was seen RED on the unmodified tree at `62287e9`, naming exactly one offender — line 28 of `docs/issues-and-milestones.md`, which held `0.9.0` — and green at `86a6e20` once that line was rewritten. Eleven mutations of the unit, one at a time with `tests/__pycache__` cleared between: every one was caught, and each of the seven fixtures went red on at least one. The enumeration behind the exemption count was re-run over the loaded set (64 files, the nine prefixes minus the exempt paths): five distinct version tokens, `0.9.0` · `1.2.3` ×3 · `0.2.0` · `2.1.259` ×2 · `4.4.17`, plus `v0.3.0` on the same line as `0.2.0`. **Re-executed 2026-09-08 (#205)**, against the units this row already cites and with no code change: the four arrangements through both implementations and in both orders. Only the exact-path one differs between them, and only that one moves when the entries are written the other way round — so the anchors are unchanged and the correction is to the reason written beside them | 2026-09-18 | **What the rule deliberately does not do is refuse every version this repository has shipped**, which is #179's second candidate. `docs/issues-and-milestones.md` says in so many words that *the branch `release/v0.3.0` shipped as 0.2.0*, and a check that cannot state that fact is refusing history. Below the running version is history and is kept; that half is pinned by its own case. **The comparison is numeric, and the mutation run is what proved that matters**: `0.10.0` is above `0.8.3` and every string comparison says otherwise, which is the exact version #179's body names as the one the next author writes. **The illustrative exemption asserts its own precondition** in a case of its own — the value must be neither the running version nor one `CHANGELOG.md` records as shipped — because a bare string in an allow-list cannot notice it stopped being illustrative, and on the day this repository ships that number the exemption would wave through exactly the line it sits beside. **`docs/experiments/` is a prefix where `docs/issues-and-milestones.md` is not**, and the difference is the argument `CONTRIBUTING.md` asks for: an experiment record is dated by its own file name and rewriting the version it measured on falsifies it, ~~which is true of every file that directory will ever hold~~ (**false, and corrected by finding 2 below — it was not true of the directory's own two READMEs**); the tracker document is a standing document edited every release, so a prefix there would cover its future lines too. **Re-read 2026-09-07 in review round 1's fix pass, finding 1.** The token regex's trailing `(?![\w.])` refused any following dot, so a version ending a SENTENCE was invisible — a regression against the substring check this branch replaced, which was punctuation-blind and caught it. Split into `(?!\w)(?!\.\d)`, so what is refused is a continuing number rather than any dot. Re-executed: the trailing-context class enumerated over 27 shapes, and the fix widens exactly the four dot-followed ones (`0.9.0.`, `0.9.0...`, `0.9.0.md`, `v0.9.0.`) while `0.9.0.1` stays refused; the loaded set still reports no offender. The mutation run over the new pair found `(?!\w)` had no case behind it — `\d+` is greedy, so `v1.2.30` is matched whole either way — which is round 1's own finding recurring inside its fix, and a letter-suffix assertion now pins it. Eight fixtures now, not seven. **Re-read 2026-09-07 in the same fix pass, finding 2, and this clause moved with it.** The `docs/experiments/` prefix exempted `README.md` and `README.ko.md`, which its own argument — the file name carries the date — is not true of; they are the directory's conventions, the same standing-document shape the plan refused a prefix for in `docs/issues-and-milestones.md`, and the prefix was the one exemption a document could join by choosing where it sits. It now covers only a file whose own name BEGINS with the date. Chasing a surviving mutation found the other half of that door: the date was read from anywhere in the path, so a dated DIRECTORY exempted every undated file inside it — `docs/experiments/2026-09-03-run/README.md` was exempt. Read from the basename now. Re-executed: the exempt set in the loaded tree is exactly five files, the two dated records plus the three exact paths, and the loaded set still reports no offender. **Re-read 2026-09-07 in review round 2's fix pass, findings 4 and 5, and the token rule moved.** `(?!\w)` is gone. It was added for `v1.2.30`, `\d+`'s greed already covers that — measured, with and without, identical — and its only remaining effect was to hide a LETTER-suffixed prerelease of the unshipped version: `0.9.0rc1`, `0.9.0b1`, `0.9.0_final` and `v0.9.0rc1` all answered no offender while `0.9.0-rc1` was refused and the substring check this branch replaced caught every one. **A prerelease of a version that has not shipped is a timer in both spellings**, which is the owner's decision after the same lookahead produced a finding in three consecutive passes. The comment above the constant had stated the disproven `v1.2.30` grounds while the module said the opposite thirty lines down; both now say the same thing. Re-executed over 18 trailing contexts: the removal widens exactly the five prerelease shapes and changes nothing else — `0.9.0.1`, `2.0.1.5`, `v1.2.30`, `0.9.05` and `x0.9.0` all answer as before — and the loaded set still reports no offender. **The leading lookbehind had no case either**, found by mutating this fix: `2.0.1.5`'s tail is below the running version, so the case guarding it answered `[]` with or without the lookbehind. `1.9.9.9`, whose tail is above it, and `x0.9.0` now pin it. **Re-read 2026-09-07, finding 6.** The other-product route could not be taken on the token the refusal prints: the message reports `match.group(0)` — `v9.9.9` — and the lookup read `match.group(1)`, so an author declaring exactly what was printed got no change. Executed both ways round. Either spelling is now accepted. The live entry pins neither arm, because `skills/implement/scripts/seal.py` writes `4.4.17` with no `v` and the two spellings are the same string there; dropping either arm left the tree green until a case declared a `v`-prefixed token both ways. **Re-read 2026-09-07, finding 8.** Round 1's fix had replaced `any()` with a loop that RETURNS on the first `/` entry whose prefix matches, which made `RECORDS_OF_A_MOMENT` order-dependent: an entry written below the prefix that contains it was unreachable, and that list is precisely the one a later author appends to. Back to `any()`, so every entry is considered. Executed: with an exact entry written after `docs/experiments/`, the loop answered `False` where `any()` answers `True`. No instance existed in the tree; the three exact entries sit above the one prefix. **Corrected 2026-09-08 (#205): the reviewer's second example, a narrower prefix appended after a wider one, is NOT the same mechanism and never changed an answer in either implementation.** Every `/` entry takes the same `DATED_RECORD.match(basename)` check whether it is wide or narrow, so where it sits in the list cannot decide it; only an exact entry, which skips that check through `rel == entry`, can. Re-executed 2026-09-08 over the four arrangements in both directions — the `any()` implementation from the tree, the early-return one reconstructed from the docstring that describes it, since the branch carrying it was reverted and its intermediate commits are not in this clone. The exact-path arrangement is the only row where the two implementations differ (`True` against `False`) and the only one whose answer moves when the entries are written the other way round. The one assertion pins the one mechanism there is, and **no assertion was added for the other**: asserting an arrangement that changes no answer is the class this correction belongs to. **Re-read 2026-09-07, finding 9.** `DATED_RECORD`'s shape was unpinned: dropping its trailing `-` and loosening its component widths each left every case green, and the first silently widened the exemption — `docs/experiments/2026-09-03.md` would have become exempt where it is scanned. The pattern itself is right, being the name `docs/experiments/README.md` prescribes (`<date>-<what-was-asked>.md`), so this was case strength rather than a wrong pattern. Two assertions pin it, and all three mutations of the pattern now turn the case red. The exempt set in the loaded tree is unchanged at five files, and the enumeration after every fix in this pass reports no offender. **Re-read 2026-09-11 (#351).** The docstring of `test_no_loaded_file_names_a_version_at_or_above_the_running_one` lost the clause arguing the exemption for the deleted shared checklist, and `RECORDS_OF_A_MOMENT` lost that entry with the file. Nothing this row claims moved: the exemption count is three MECHANISMS, not four entries, and dropping the entry was measured over every tracked loaded file to leave seven offending lines, all seven in the file being deleted and none anywhere else **Corrected 2026-09-23 by work item 1790173209 (#363).** *May not name one at or above it* reads *at or above it and not tagged* now: `timers_in` takes the root's `v*` tags as a `shipped` set and keeps a tagged version wherever it is written, because from the preparation commit until the next bump the running version is one that has already shipped. The exemption count is unchanged — history is not an exemption, and a tagged version is history by the row's own argument one number higher. Re-executed: the loaded set reports no offender with or without the widening, and the mutation table is in the work item's fragment |
| R3 · the refusal names a route the author can actually take for **every** kind of token the check reads as a version, including one that is not a release at all | `tests/test_release_hygiene.py#what_to_write_instead@35ab0bb8`, `tests/test_release_hygiene.py#refusal@2035da87`, `tests/test_release_hygiene.py#test_the_message_has_a_route_for_every_token_the_check_refuses@ff436c20`, `tests/test_release_hygiene.py#test_no_loaded_file_names_a_version_at_or_above_the_running_one@e34c88fd` | **Executed** 2026-09-07 in review round 1's fix pass, finding 3. Four shapes put through `timers_in` at `e25ff8b`: `2026.09.03` is refused as `(2026, 9, 3)` and `3.13.9` as a CPython patch version, while `2026-09-03` is not read at all. The case was seen red on exactly one of its five assertions — the date — with the other four green, which is what settled that only one route was missing. Mutations: deleting the date route and deleting the another-product route each turn the case red. ~~**Re-executed 2026-09-08 (#203)**: `refusal`'s seven elements deleted one at a time, `tests/__pycache__` cleared between and the file restored from bytes kept before the first write — every one caught, none survived~~ (**the seven were real and every one was caught; the word that was not measured is *every*, and review round 1 corrected it below**). **Re-derived 2026-09-08 in that round's fix pass**: `ast.parse` of the returned expression gives four operands whose first is one `JoinedStr` of three parts — six leaves — and thirteen mutations over them leave two survivors, named in the note | 2026-09-18 | **Only the date needed a new sentence, and `3.13.9` did not.** The round left finding 3 open as "three routes and none fits", and the orchestrator asked which of the two shapes was actually unrouted. A version belonging to another product is exactly what `VERSIONS_OF_ANOTHER_PRODUCT` is for, and bash's `4.4.17` already sits in it as the worked precedent — the route existed and the message already named the constant. **A date has no route and gets no exemption**, because it is not a release of anything and an exemption would be a lie about what it is; the message tells the author to write it in a form that is not version-shaped, and names the dashed spelling this repository already uses. **The regex was deliberately NOT narrowed to exclude dates** — that is finding 1's mistake wearing this one's clothes, and #179's *Done when* makes the message the deliverable. **Corrected 2026-09-07 by review round 2, finding 7.** This row recorded, as a measured fact, that *"no assertion can pin which expression an `assert` uses as its message without reading the file's own source, so the floor is one obvious line rather than another checker"*. The sentence about the final line is true and it was the wrong grounds: it was used to justify leaving the message inline in the `assert`, where an edit detaching the routes from the printed text left all 28 cases green. Extracting the whole refusal into `refusal(running, offenders)` — the same move `what_to_write_instead` already had — closes that with an ordinary assertion and no source reading. **Re-executed**: the exact mutation this row called unpinnable, replacing the builder's `what_to_write_instead()` with a literal, now turns the routes case red where it was green at `27c36fe`. What is left unpinned is only the one line `assert not offenders, refusal(running, offenders)`, and bypassing THAT still leaves every case green — a residual of one obvious line rather than of the whole message. **Corrected 2026-09-08 (#203): that last sentence was written as a measured fact and it was not one, which is the second time this row has done it.** Three mutations of `refusal` disprove it, each executed on its own at `7349afc` and each leaving the module at 30 passed: deleting `+ "\n  ".join(offenders)`, deleting the `{running}` interpolation, and deleting the whole paragraph that says why such a line is a timer. What the sentence could truthfully have claimed is the survivor of the mutations that were actually run, which is a smaller claim than *what is left unpinned*. **A third class is outside that set by construction**: nothing pins that nothing was ADDED. A sentence inserted at the end of the timer paragraph, or a line inserted before the routes, leaves the module at 32 passed (round 2, re-measured round 3). The pin is one assertion over the concatenation of the pieces the case already reads — measured green unmutated and red on either insertion in round 3 — and it is declined because that concatenation is `refusal` rebuilt in the test, which `plan.md` rejected. ~~**Re-executed 2026-09-08**: `refusal` is now enumerated by construction as four pieces and three separators — seven elements, the fifth being the `"\n  "` that CLOSES the first literal and so reads as part of the paragraph rather than as an operand, which is the one three consecutive attempts on the original branch each missed. `test_the_refusal_prints_every_piece_it_builds` pins each on its own; all seven deletions were run one at a time with `tests/__pycache__` cleared between, and every one turned that case red.~~ **Corrected 2026-09-08 by review round 1 (#203), and this is the THIRD false completeness claim this row has carried.** *"Four pieces and three separators"* is a reading of the source, not the source: `ast.parse` of the returned expression flattens the `+` chain to four operands whose first is one `JoinedStr` of three parts, which is **six leaves**. Seven was reached by splitting that third part into a paragraph and the separator closing it, and by promoting the `"\n  "` the join is called ON — the receiver, `func.value`, where `args` holds `offenders` alone — to an element of its own. A bare `ast.walk` does return that string; the flattening rule stated before the count is what puts it inside the join rather than beside it (review round 3). What it cost is that the paragraph itself was read only at its two ends: deleting `it goes red on the day that version ships, on the release's own `, or just `preparation commit, `, each left the module at 32 passed, with 86 characters between the two assertions read by nothing. Every leaf is read whole now, the paragraph by two contiguous halves, and both deletions turn the case red. **Two mutations survive that set and neither is a limit.** The check's own `assert not offenders, refusal(running, offenders)` handed a literal leaves the module at 32 passed — and pinning it does NOT mean reading this file's own source, which is what the sentence it replaced claimed: `tracked` and `timers_in` are module globals, and swapping them raises the check with a message comparing equal to `refusal(running, offenders)`, measured in the round. The other is a reordering that carries every separator with the piece it precedes; the naive swap is caught. **What is absent from a measured list is unmeasured** — that sentence was right each time and each time the list beside it was described as something more than a measurement. **Re-read 2026-09-11 (#351).** This row's fourth coordinate re-anchored: `test_no_loaded_file_names_a_version_at_or_above_the_running_one`'s docstring lost the clause arguing the exemption for the deleted shared checklist. Nothing this row claims moved — the refusal's routes and the message that prints them are untouched by that branch **Re-read 2026-09-24 by work item 1790173209 (#363).** The history route reads *below the running one, or one this repository has TAGGED* now, and the routes case pins the new word; every kind of token still has a route, so the claim holds. |
| R2 · `-z` is what makes `git ls-files` safe to split, and `core.quotePath=false` is not the argument that turns the escaping of non-ASCII paths off — **either one does that by itself** | `tests/test_the_pull_request_language_is_the_repositorys.py#shipped_templates@12c49cdb`, `tests/test_the_pull_request_language_is_the_repositorys.py#test_the_templates_check_reads_prose_only_and_descends@e4cc13db` | **Executed** 2026-09-07 on git 2.50.1 (Apple Git-155), in a scratch repository holding `templates/spaced name.md`, `templates/한글.md` and a name carrying a newline. Four variants and a fifth reading for the control character. No arguments: `"templates/new\nline.md"` and `"templates/\355\225\234\352\270\200.md"`, both quoted and octal-escaped. `-z` alone: every name raw, NUL-separated. `core.quotePath=false` alone: the Korean name raw, and the newline name STILL `"templates/new\nline.md"`. Both together: byte-identical to `-z` alone | 2026-09-07 | **The instruction the comment gives was right and its grounds were wrong**, which is why nothing in the call changed. `core.quotePath=false` contributes nothing while `-z` is there — the two listings are identical byte for byte — so it is still the one to prune if either goes. What `-z` does that the config does not is two things: it turns off the escaping of CONTROL characters as well, and it separates on NUL, the one byte a filename cannot hold. The split below the call is on NUL, so dropping `-z` returns the whole listing as a single entry rather than as one entry per file. **This is comment-and-record only and the cases pass either way**, which is exactly how the false sentence sat unnoticed through a round record, a ledger row and a comment. Nothing in the suite could have caught it, and nothing added here would: what a comment claims about a tool is outside what any assertion in this repository reads |

<!-- specs/1788749195-the-record-drops-the-fix-and-a-pipe-truncates-the-row -->
### 1788749195-the-record-drops-the-fix-and-a-pipe-truncates-the-row

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Two rows over the two tickets that share this branch. R1 is #189: not the
escaping, which a reader can see, but WHICH reading of a row is taken and on
what evidence — the measurement over this repository's own records is what
ruled out the obvious repair, and it is not recoverable from the diff. R2 is
#187's empty arm, which is a decision about what a generator may claim rather
than a behaviour anyone would guess from the code.

Neither row records the fact that a pipe used to truncate a row or that the
fenced blocks used to reach no file. Those are in the issues, in
`plan.md`'s Technical context, and in the two phase records; a ledger row
carries the claim the code grounds today. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · a report row is re-serialised into the record rather than copied, in three readings: the plain one wherever it fits, then the code-span reading CAPPED at the header's width, then the plain cap, which leaves the surplus in the LAST cell. Every reading goes through `raw_cells`, so a `\|` inside an HTML comment is never a break. Not a fold of already-split cells, which cannot restore the reviewer's spacing | `skills/code-review/scripts/round_record.py#split_cells@a3412574`, `skills/code-review/scripts/round_record.py#row_cells@a20d32f9`, `skills/code-review/scripts/round_record.py#copied_row@793dbca0`, `tests/test_the_record_is_generated.py#test_the_plain_reading_is_the_readers_own@2b22ee7a`, `tests/test_the_record_is_generated.py#test_a_pipe_inside_a_code_span_stays_in_its_own_column@68ff63b3`, `tests/test_the_record_is_generated.py#test_a_pipe_in_a_column_that_is_not_the_last_stays_there@92b188ea`, `tests/test_the_record_is_generated.py#test_a_bare_pipe_before_the_last_column_keeps_its_text@6a08a411` | **Executed** 2026-09-07. The sweep that chose the shape: every body row of all 125 committed `seal/specs/*/rounds/round-*.md` records split and compared against its own header — eight are over-wide, and all eight have their pipe inside a code span. Replayed through `copied_row` at `007436a`, all eight come out at header width and seven recover the reviewer's own column. Nine mutations, one per site and one per branch, all red | 2026-09-07 | **The measurement is the row's real content.** The obvious repair — fold a row's surplus cells into the last column, because the last column is the free-text one in every table the record copies — is refused by two of the eight. One of them (work item `1788433011`, round 2) has its pipe in the probes table's FIRST column, which is a command, and folding moves half the command into `Result`. Another (work item `1788501054`, round 9) has it in the Finding column, where folding shifts every later column left and the verdict cell — which `chain_check` reads — comes out holding the location. **The cap is a guess and it is the only one available**: nothing in a flattened row says which column a bare `\|` came from, so a bare one before the last column lands the rest of the row in the last cell. That is never worse than the record's earlier behaviour, because the leading cells land at the same indices either way, and it makes the text visible rather than dropped. **`split_cells` with both knobs off has to BE `reader.split_row`**, which is why `row_cells` takes the reader's own answer wherever it fits and reaches for its own spelling only for the over-wide row: the record is read back through that function at the pull request, so a cell composed by one spelling and read by another is a cell CI reads differently from the record. **Corrected 2026-09-07 by review round 1, findings 1 and 2.** Two things were wrong and both were about a text this row did not name. `table_body` counts a row's columns on `strip_comments(report)` and this function is handed `raw`, so a `\|` inside an HTML comment was a break to one and not the other: the record lost a column at exactly header width, nothing downstream complained, and the Location stood in the Verdict cell `chain_check` reads — the placement `plan.md` refuses the fold for, reached by the fix that refused it. And a row carrying BOTH a code-span pipe and a bare one was over-wide under either reading alone, so it fell to the plain cap, which re-split the reviewer's code span with the same result. The comment rule now lives in `raw_cells` rather than as a flag at each call site: written per site, removing any ONE left every case green, because a later reading recovers what an earlier one splits wrongly. The uncapped span reading that stood first is REMOVED — a reading landing exactly on the width is unchanged by a cap of width minus one, and it is reached only where the plain reading is over — and it was measured identical over all 4128 body rows of this repository's committed records and 432 generated rows before it went; while it stood, dropping its `spans=True` left the module green. Re-run after the repair: `copied_row` changes exactly the eight over-wide rows and no other of the 4128. **The eighth row is the stated limit made visible** — its backtick runs are unbalanced, so the code-span reading swallows every break and lands at 2 against a header of 5, the plain reading takes it, and its tail lands in the last column. That is why the code-span reading is CHECKED against the width rather than trusted. The eight rows themselves belong to other work items and are untouched here |
| R2 · the record's `## Paste-ready fixes` says what the generator OBSERVED — `no paste-ready fix in the report` — and never that none was needed, and a report with no fence under the heading is written rather than refused | `skills/code-review/scripts/round_record.py#NO_PASTE_READY@5a1e9728`, `skills/code-review/scripts/round_record.py#PASTE_READY@754a5748`, `skills/code-review/scripts/round_record.py#READ_HEADINGS@3f93dc12`, `tests/test_the_record_is_generated.py#test_a_report_with_no_paste_ready_fix_is_still_a_record@db2d5d71`, `tests/test_the_record_is_generated.py#test_an_empty_paste_ready_section_says_the_report_carried_none@976a01eb`, `tests/test_the_record_is_generated.py#test_prose_under_the_paste_ready_heading_stays_in_the_report@726d0673` | **Executed** 2026-09-07. A report whose verdicts all close and whose heading is absent writes the record with that sentence; so does a heading with only prose under it. Six mutations of the section's wiring, all red | 2026-09-07 | **A generator cannot tell a round that needed no fix from a reviewer who wrote none**, and either claim would be the record asserting something nobody checked. The sentence names the report because the report is what was read. Beside an open row in the verdict table two sections up, that line IS the gap written down — which is the whole of what `plan.md` offered against a reviewer omitting the heading, so it is a load-bearing wording rather than a default string. **The empty arm is a scenario and not an edge case**: a verifying round that opens nothing writes no fix, and `spec.md` has it as a row. A refusal there would stop an unattended run over a report that is correct. **The section carries the fences and no prose**, which is what keeps a file `chain_check.py` reads free of text nobody parses — the reason `plan.md` refused keeping the report verbatim below the tables. **`READ_HEADINGS` is why the guard covers it**: `swallowed`'s heading loop reads that constant and its row loop still reads `REPORT_TABLES`, because only a table can lose its rows to a fence while its heading stands |
| R3 · `swallowed`'s two loops read two lists. The heading loop reads ~~`READ_HEADINGS`~~ (**corrected by R4 below — `REQUIRED_HEADINGS`**) and the row loop reads `REPORT_TABLES`, because only a table can lose its rows to a fence while its heading stands | `skills/code-review/scripts/round_record.py#READ_HEADINGS@3f93dc12`, `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `tests/test_the_record_is_generated.py#SECTION_TEXT@a6f8fb79`, `tests/test_the_record_is_generated.py#test_a_fence_that_takes_a_section_the_generator_reads_is_refused@29053fab` | **Executed** 2026-09-07. Mutating the heading loop back to `REPORT_TABLES`, and `READ_HEADINGS` back to the table headings alone, each turn the parametrized case red on its new member | 2026-09-07 | **This narrows `seal/ledger.md`'s F1, which said the derivation was from two constants**, and F1 is corrected there rather than removed: its anchor still resolves and what moved is the content inside it. The derivation itself is unchanged in kind — a section added later is guarded, and gets a case, by being added to a constant, and the parametrized case still reads its own parametrization back out of one. What the split records is WHY there are two lists, which is the sentence a later reader would otherwise have to rediscover by asking why the loops differ. **`PASTE_READY` is the instance that made the constant earn itself**: it arrived after `READ_HEADINGS`' predecessor was introduced against a hypothetical, and it was guarded by one line changing **Re-read 2026-09-08 by work item 1788873610 (#182), which drifted `swallowed`.** Both loops still read the two constants this row names; the never-closed pair that sat above them is one `hiders_close` call now, and neither list moved. **Re-read 2026-09-24 by work item 1790174138 (#505), which drifted `swallowed`** at the row loop's section end alone; both lists are read as before |
| R4 · the swallow guard refuses a report over the REQUIRED headings only, in BOTH of its loops. Its premise — a heading hidden by a fence and absent outside it was swallowed — is an inference that holds for a section the report must carry and not for one it may leave out, and the row loop answers the same question vacuously for a section with no table | `skills/code-review/scripts/round_record.py#REQUIRED_HEADINGS@7e722eb8`, `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `tests/test_the_record_is_generated.py#test_a_fence_quoting_the_optional_heading_is_kept_when_it_is_absent@f2d4a118`, `tests/test_the_record_is_generated.py#test_a_fence_that_takes_a_section_the_generator_reads_is_refused@29053fab` | **Executed** 2026-09-07. A report omitting `## Paste-ready fixes` while its probes fence quoted it: exit 2 and no record before, exit 0 and the record written after, with the quoted block still carried inside the probes fence. Four mutations — the loop widened back, the constant widened, the loop emptied, the second half of the condition dropped — all red | 2026-09-07 | **This corrects `seal/ledger.md` F3 and R3 above, and the scenario that falsified it is the one F3's own Notes describe**: a reviewer of this generator pastes record-shaped blocks, headings and all, and the round that quotes the empty section's own sentence is exactly the round that wrote no fixes. F3's two-half condition still holds; what was wrong is the set it ran over. **The false negative is accepted rather than unnoticed**: a report that DOES carry fixes and whose heading a closed fence swallows now writes a record saying it carried none. Nothing can tell that from a round that wrote none — outside the fence the two texts are identical — and the record shows the gap beside the open rows in the verdict table above. A refusal would stop an unattended run over the legitimate case to catch the unlikely one, which is the trade `CLAUDE.md`'s first goal decides. **The two constants differing by exactly the optional section is what the pair of cases asserts**, so a section added to one and not the other is a red case rather than a silent widening of what may be refused. **Extended 2026-09-07 while closing round 2's 🟡 1, by assuming a further instance of its class rather than waiting for one.** The ROW loop's exclusion of the optional section was pinned by nothing: widening it to `READ_HEADINGS` left all 78 cases green, and it is not a dormant branch — a section with no table of its own answers *do its rows stand only inside a fence* vacuously, so any fenced fix carrying a markdown table would read as hidden rows and be refused. This repository's own paste-ready fixes carry tables. Executed: with the loop widened, a report whose fix is a fenced Deferred table is refused; the new case is red there and green here **Re-read 2026-09-08 by work item 1788873610 (#182), which drifted `swallowed`.** The premise and the vacuous-answer clause are unchanged; the only edit in that function is the never-closed pair leaving it. **Re-read 2026-09-24 by work item 1790174138 (#505), which drifted `swallowed`.** The premise is unchanged; what moved is how far a section reaches in the row loop, which is where a `###` used to end it |
| R5 · the pre-merge reminder decides a record is closed by reading it through the shared reader, so a closing word counts only where a reader would read it — ~~inside a fenced block~~ (**both arms: `readable` is `blank_fences(strip_comments(...))`, and it is the COMMENT arm that changes the verdict on every real record, corrected 2026-09-07 by round 2's 🟡 1**). Without the reader it falls back to the raw text rather than raising | `hooks/review-history-guard.py#is_closed@24b27511`, `hooks/review-history-guard.py#reader@20048595`, `tests/test_chain_hooks.py#test_a_pasted_fix_does_not_read_as_a_closing_note@de709750`, `tests/test_chain_hooks.py#test_a_real_closing_note_still_silences_the_merge_reminder@21068743`, `tests/test_chain_hooks.py#test_the_guard_falls_back_to_the_raw_text_without_the_reader@504b6136`, `tests/test_chain_hooks.py#test_the_reader_is_what_makes_a_fenced_closing_word_not_count@062d781a` | **Executed** 2026-09-07, round 1's 🟡 8. A record whose only closing word stands inside a pasted fix, beside a live Deferred row: the reminder was silent before and fires now. Four mutations — the reader never consulted, `reader()` always None, an unreadable record made to nag, the drain phrase dropped from the pattern — all red | 2026-09-08 | **The section is what made a pre-existing defect reachable everywhere.** A probes fence could already carry a closing word, so the shape predates this branch; putting the reviewer's code into EVERY record is what turned an unlikely coincidence into the ordinary case, in a run whose own fixes are fixes to this vocabulary — the record that found it carries `closed` and `drained` inside a fence. **`reader()` returns None rather than raising**, and §13 is why the fallback is executed rather than argued: the reader is reached by a relative path from `hooks/` into `skills/`, and a copy of the plugin without that directory must leave the hook printing a possibly-wrong reminder rather than stopping a session's Bash call. The case removes the guarantee by pointing the constant at a file that does not exist. **What the fallback gives up is stated**: without the reader the hidden word counts again, which is the pre-fix behaviour. **`reader()` has ~~three~~ FOUR reachable failure paths and each takes a different input** (**corrected 2026-09-07 by round 3's finding 1, which measured a fourth path with no constructible input and one reachable path no case watches — the ticket carries it; corrected again 2026-09-08 by #209's work item, which enumerated the arms out of the function's own source instead of reading them: the except tuple is `(OSError, ImportError, SyntaxError)`, THREE members, so the reachable count is four and not three, and `ImportError` was a second arm no case watched and no ticket named. All four have a parameter now — see T1 of `seal/ledger/1788844300-…`**), which one case could not see: an absent path raises `FileNotFoundError` into the `except`, while a file Python has no loader for makes `spec_from_file_location` return None and never raises. Executed both; with a single parameter, deleting the `spec is None` arm left the module green. **Corrected 2026-09-07 by round 2's 🟡 1, and the correction is which arm does the work.** Every sentence written for this row named the fence, and the fence is the synthetic half: swapping `readable` for `blank_fences` left all 24 cases of the module green, while swapping it for `strip_comments` turned two red — so the arm nothing watched was the one carrying the behaviour. Re-executed over the 127 committed round records: the reader changes the verdict on three, **all three on the comment arm and none on the fence arm**, each of them an HTML comment the round wrote to narrate itself, beside live `Deferred` rows — **corrected 2026-09-07 by round 3's finding 4**: two are the record's header comment (*It closed all five*) and the third is a note beside the field table, seventeen lines in, saying the loop is *not closed by one more small fix*. The load-bearing half is the comment, not its position. The case is parametrized over both passes rather than written for one, and each parameter turns red on its own arm |

<!-- specs/1788761915-a-record-states-what-nothing-reads -->
### 1788761915-a-record-states-what-nothing-reads

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows over two tickets that share a branch. R1 and R2 are #190, split at
the line the two arms are actually different: R1 is the boundary and the
identifier arm, whose whole content is the narrowing a measurement forced; R2
is the stamp arm, which is a refactor rather than a new reader and whose real
content is the two things that must NOT carry across from the ledger. R3 is
#207.

**R2 re-points five `seal/ledger.md` rows rather than removing them.** The
anchor loop moved out of `check_ledger` into `check_text`, so five rows whose
claim is about that loop now cite the unit that holds it. Nothing was removed
and every claim is unchanged — this is `CLAUDE.md`'s one exception to the
fragment rule, for rows an edit falsifies by moving the content out from
under them. The two rows whose claim is about `check_ledger` ITSELF — a
ledger nobody can read is reported, and every ledger path a person reads goes
through `display_name` — were left where they are. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · a record of a work item that has **not shipped** may not state a compound backticked identifier nothing outside `seal/specs/` carries; the boundary is the presence of `seal/ledger/<id>.md`, and a line carrying `NAME NOT IN TREE` is not read at all | `skills/evidence-check/scripts/evidence_check.py#unshipped@156ff0f6`, `skills/evidence-check/scripts/evidence_check.py#record_files@d660319b`, `skills/evidence-check/scripts/evidence_check.py#compound@8ca6d002`, `skills/evidence-check/scripts/evidence_check.py#stated_names@1b419db2`, `skills/evidence-check/scripts/evidence_check.py#tree_names@1d5e6d8f`, `skills/evidence-check/scripts/evidence_check.py#check_records@316d8a23`, `tests/test_a_record_states_what_the_tree_has.py#test_this_repositorys_own_records_state_nothing_the_tree_lacks@b0c78283` | **Executed** 2026-09-07. The arm was seen RED on this repository's own tree before anything was corrected, naming exactly the four occurrences of `chain_module` in work item `1788749195`'s `rounds/round-2.md` and `rounds/round-3.md` — the instance that work item's own round 3 found by reading, three weeks of records later — and green at `6de1bca` once those three lines carried the marker. 176 names read to find them, 169 after the correction. Eleven mutations of the unit, one at a time with `tests/__pycache__` cleared between: every one caught, and the binary sniff was caught only after a case was written for it | 2026-09-11 | **The narrowing is the row's real content.** A backticked name is read as a claim only when it is COMPOUND — it carries an underscore — and that was forced by measuring rather than chosen. Of the 55 distinct names that appear nowhere outside `seal/specs/`, the 19 without an underscore are a shell command, six stdlib names, an errno, an environment variable, a lint code, a probe value, five words of ordinary prose in backticks, and three verdict words a checker used to emit. Not one is a claim about a unit; all 36 compound ones are. The narrowing removes 56 of 146 occurrences and loses no true positive, which is `plan.md`'s own repair for the false positive it predicts — *narrow the pattern, do not widen the exemption* — taken up front. What it gives up is a one-word unit renamed away, and that is the cheaper mistake: the alternative asks an author to write `NAME NOT IN TREE` beside `rpartition`, which is marker noise on a real name. **The corpus excludes exactly two directories, and they are the two a work item writes about itself** — `seal/specs/` and `seal/ledger/`. `seal/ledger.md` is IN: its S15 note keeps a renamed unit's old name beside the new one on purpose, so a reader coming from an older record can follow it, and excluding the whole of `seal/` refuses five occurrences of that name in work item `1788735085`'s records, every one of them that work item narrating its own rename. The FRAGMENTS are out, and that was found by writing this work item's own rows: doing so put four names into the corpus and silenced four refusals in its own `phase-2.md`, so a work item could clear the check on its records by naming the unit in its own ledger file. The fold moves a fragment into `seal/ledger.md` at the release, which is the same moment the work item stops being live, so nothing changes hands at the boundary. **The 129 occurrences in shipped work items are history and were spot-checked, not assumed.** Six were opened at their coordinates: `header_of`, `row_baseline`, `_correction_traces` and `test_ledger_stamps_resolve` are units that existed when the record was written and have been renamed or removed since; `cmp` and `rpartition` are the false-positive class the narrowing removes **Re-read 2026-09-11 in work item 1789081272 (#84), round 1 fix pass.** `test_this_repositorys_own_records_state_nothing_the_tree_lacks` now asserts the arm's own refusal grading — anything neither DRIFTED nor EXTERNAL — where it asserted the whole finding list. The identifier arm this row is about is unchanged and still fatal; what the case stopped doing is failing the tree on advisory drift, which `main` grades at exit 1 because a live work item's branch edits the very units its records stamp. **Re-read 2026-09-24 by round 1's fix pass of work item 1790208593 (#547):** `tree_names`'s docstring names the release file as where the fold moves a fragment; the boundary is still the fragment's presence |
| R2 · a `path#unit@hash` a record wrote down is resolved by the ledger's own reader, one line at a time, and graded the way a ledger row is — an anchor that does not resolve fails, drift reports | `skills/evidence-check/scripts/evidence_check.py#check_text@2328e19a`, `skills/evidence-check/scripts/evidence_check.py#stated_stamps@c6b68ccd`, `tests/test_a_record_states_what_the_tree_has.py#test_a_location_column_is_not_an_unmigrated_coordinate@ca2fc7b1`, `tests/test_a_record_states_what_the_tree_has.py#test_a_records_drift_does_not_fail_the_run_and_a_broken_anchor_does@8263fb70` | **Executed** 2026-09-07. Seven mutations of the arm, all caught. The decisive one is the alternative design — `check_ledger` over the whole record file instead of `check_text` line by line — which turns five cases red, the Location one included. The three real stamps in this tree were resolved directly to measure what the boundary is doing: the two real ones both read DRIFTED and the fixture one reads BROKEN, and all three sit in work items that have shipped, so the arm never opens them | 2026-09-07 | **Two things must not carry across from the ledger, and both were found by writing a case.** `old_format_rows` refuses `path:line` and tells the author to run the migrator, which is right for a ledger row and wrong for a record: a verdict table's `Location` column is `path:line` by design, prescribed by `templates/sdd-round.md`, and a checker calling every one of those an unmigrated coordinate would refuse the format it asks for. And **drift in a record is exit 1 the way drift in a ledger is** — a live work item's branch is editing the very units its records stamp, so failing on drift is red by construction, which is the state `.github/workflows/test.yml`'s `ledger` job already refuses in its own comment. **The arm reads line by line because the refusal has to name the line**, the way the identifier arm does; a fresh `seen` per line follows from that, since two lines stamping one unit are two claims, and the repo-wide scan cache is shared across them so a broken anchor's scan is paid once. **There are no stamps at all in the two unreleased work items** — `0 stamps read` — so this arm's proof on this tree is its cases and its mutations rather than a live occurrence |
| R3 · `round_record.py new` prints which of three states the run's bound is in as it writes the record — nothing, `one reopening remains`, or `this record ends the run` — keyed to the **earliest** earlier record whose floor row reads `no` | `skills/code-review/scripts/round_record.py#floor_and_fixes@7f50a7a7`, `skills/code-review/scripts/round_record.py#bound_line@e28906f3`, `skills/code-review/scripts/round_record.py#new@e7a15af2`, `tests/test_the_record_is_generated.py#test_the_floor_record_is_the_earliest_and_not_the_latest@d7221ca3`, `tests/test_the_record_is_generated.py#test_a_first_round_says_nothing_about_the_bound@bf05cd4f` | **Executed** 2026-09-07. Eight mutations of the two units, all caught: nothing printed at all, the latest floor record taken instead of the earliest, a floor of `yes` counting as met, an unreadable value counting as met, the fix walk never finding one, every later record counting as a fix, the reopening line printed where no record met the floor, and the capped exit dropped from the message | 2026-09-13 | **Which floor record the bound is keyed to is the whole of the design, and `plan.md` did not name it.** Keyed to the LATEST, the walk restarts at every record it stops at — because every record it stops at is itself a record that met the floor — which is the *unbounded by construction* failure `chain_check.stopping_floor` records in its own docstring for the count. The discriminating case is round 1 met the floor · round 2 closed on a fix · round 3 met the floor again: from the earliest, writing round 4 ends the run; from the latest, it reads as having a reopening left. **Three states and no fourth.** A run whose floor has not been met is the same silence as round 1 — the cap governs there and the cap is not this line's subject — and so are a record that cannot be read and a floor row outside the vocabulary, both of which `stopping_floor` already reports at the gate. A second reader inventing a sentence about them here would be #207's own failure one file over. **The bound is read from disk where `chain_check` reads from `HEAD`**, and the two are not inconsistent: `read_record` asks git because it is enforcing at a pull request, where the working tree is exactly what CI cannot see, and this line is printed to whoever just ran the command. **The message carries `chain.CAPPED_EXIT` verbatim** — a line saying the run ends and not what to do with what is still open is the wall the refusal at the gate already refuses to be **Re-read 2026-09-08 by work item 1788873610 (#182), which drifted `new`.** `bound_line` is still called after the record is written and before the check runs, and the earliest-floor keying is untouched. What changed is that a record no reader can read is now refused before the write, so no bound line is printed about a record that does not exist. **Re-read 2026-09-13 by work item 1789296200 (#345), which drifted it.** `new` gained a line about the commit the round read, printed beside the `wrote` line. `bound_line` is still called after the record is written and before the check runs, the earliest-floor keying is untouched, and the new line is about a different fact — so there is still no fourth state in the bound's vocabulary. **Re-read 2026-09-23 by work item 1790173106 (#138), which drifted `floor_and_fixes`.** The reopening read goes through `chain.says_reopened` instead of the helper's own `== FLOOR_YES`, so a bare `yes` is no reopening here either; the three states and the earliest-floor keying are unchanged. **Re-read 2026-09-24 by work item 1790174138 (#218), which drifted both units.** The three states are still the three: what changed is WHICH walks bound the run — a count walk that stopped after reaching two now fires, as the error the gate already returns at its floor record, where the line used to fall through to `one reopening remains`. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (⬜ 5), which drifted `bound_line`.** A running count walk that already reached two prints the gate's own refusal at its floor record, as the stopped walk does; the one-record running sentence and the reopening branch are as they were |
| R4 · the printed bound reads BOTH of the gate's walks — the reopening walk and the count walk — and runs each from EVERY record whose floor row reads `no`, naming the floor record the firing count walk started from; it is silent for a work item the gate grandfathers, guarded per walk: the count from `FLOOR_FROM`, the reopening from `REOPEN_FROM` | `skills/code-review/scripts/round_record.py#floor_and_fixes@7f50a7a7`, `skills/code-review/scripts/round_record.py#bound_line@e28906f3`, `tests/test_the_record_is_generated.py#test_two_quiet_rounds_after_the_floor_end_the_run@121db425`, `tests/test_the_record_is_generated.py#test_the_two_walks_are_grandfathered_against_different_constants@7ba2ae4d` | **Executed** 2026-09-07, round 1's fix pass. Eleven mutations of the two units against the six new cases, all killed — including the three the build had left surviving (`fixes[0]` to `fixes[-1]`, `except OSError` to `raise`, and the post-floor `continue` dropped). The reproduction is the reviewer's: floor `no`, then two quiet rounds, and `new` printed `one reopening remains` at round 2 while `chain_check` refused round 3 with *the count of round records after this one reaches 2*. **Corrected and widened 2026-09-07 by round 2's fix pass (🟡 1)**: reading both WALKS is not reading them from every record the gate reads them from. `stopping_floor` is called on EVERY record, so a second floor record starts walks of its own and an earlier count walk that had already stopped hid it — `new` printed `one reopening remains` at round 4 of this work item while the gate returned an error at `round-2.md`. Executed: the intermediate-floor case red before the fix and green after, and the plural branch of the message pinned for the first time (⬜ 6) | 2026-09-07 | **A quiet run is bounded by only one of the two walks, and it is not the one the line carried.** The reopening walk counts fix-closing records wherever they sit; the count walk counts every later record up to and including the first that reopened or closed on a fix, and refuses a second. Floor `no` followed by two quiet rounds trips only the second, so reading the first invited exactly the round the gate would refuse — #207's own defect, made by the line written to end it. **Whether the count walk is still RUNNING is the fourth return value, and it is load-bearing.** A record that reopened without writing fixes stops the walk at the gate, so the record being written is not counted and the gate allows it; counting it would print `ends the run` over a round about to be permitted, which is the same false sentence one direction over. **The grandfathering is per walk because the gate's is.** `stopping_floor` excuses the count below `FLOOR_FROM` and the reopening below `REOPEN_FROM`, so a work item between them — `1788500000-an-item` — has one bound really enforced and the other only noticed. One guard for both silences an enforced bound; no guard declares a run capped where the gate prints. Silence rather than a hedged sentence: the line exists to bound the decision to spawn again, and where the gate will not refuse there is no bound to state **Re-read 2026-09-23 by work item 1790173106 (#138), which drifted `floor_and_fixes`.** Only the reopening read moved, to the gate's own reader of the cell; both walks, the naming of the firing walk's floor record and the per-walk grandfathering are as they were. **Re-read 2026-09-24 by work item 1790174138 (#218), which drifted both units.** The count walk still runs from every floor record and the firing walk's own floor record is still the one named; what widened is what counts as firing — a stopped walk that reached two fires too, with `running` false, and the line then reports the gate's error at that record. The differential over 584 sequences against `stopping_floor` went from 16 disagreeing to 0. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (⬜ 5), which drifted `bound_line`.** A running count walk that already reached two prints the gate's own refusal at its floor record, as the stopped walk does; the one-record running sentence and the reopening branch are as they were |
| R5 · in the records arm a FENCED region and an HTML comment that begins a line are quotations rather than claims, each to its own end — a comment to the position of its `-->`, a fence to a close of the same character, at least as long as the opener and with nothing after it — while a fence the record never closes is a malformed record whose lines are read; `EXTERNAL` is exit 0 as it is in a ledger, the run says how many work items it did not read, and the corpus reaches a local-mode `seal/` | `skills/evidence-check/scripts/evidence_check.py#claim_lines@e5006cc3`, `skills/evidence-check/scripts/evidence_check.py#unread_items@1edf61c5`, `skills/evidence-check/scripts/evidence_check.py#tree_names@1d5e6d8f`, `skills/evidence-check/scripts/evidence_check.py#main@ceb63d8d`, `tests/test_a_record_states_what_the_tree_has.py#test_a_fenced_name_is_a_quotation_and_not_a_claim@b6b2b7b5`, `tests/test_a_record_states_what_the_tree_has.py#test_the_run_says_how_many_work_items_it_did_not_read@cc265e0c` | **Executed** 2026-09-07, round 1's fix pass. Thirteen mutations against the new cases, all killed, and the tree's own count moved from 227 stated names to 208 once quotations stopped being read. **Corrected 2026-09-07 by round 2's fix pass.** The `208 names read` above was already stale at `88c7110`, where the arm reads 218, and it is 233 at this row's own commit — an aggregate moves with every line of prose a branch writes, which is why a count is not a coordinate and why this one now carries the commit it was taken at. The clause was also true of a FIRST LINE only (🟡 2, 🟡 3): a two-line template comment had its second line refused at exit 2, and a fence nobody closed took every claim under it at `0 names read` and exit 0. Both fixed here, five new cases seen red first — including the marker's exemption on a held line, which the reviewer's paste-ready fix dropped, and the aside's END, whose mutation survived the other four. Eight mutations across the round's whole fix surface, one at a time with the caches cleared, all eight killed | 2026-09-07 · 2026-09-13 · 2026-09-26 | **A paste-ready fix is code the tree does not have yet — that is what it IS.** Refusing a fenced name asks the writer to mark up a block they copied verbatim, and a marker inside a fence changes the fix somebody pastes; an HTML comment is a template describing a field rather than a record asserting a unit. One reader, `claim_lines`, for both arms, because a stamp in a fence is a quoted anchor exactly as a name in one is a quoted name. **`N unread` is the boundary's other half.** *A fragment exists* answers *is live*, and its converse does not: a work item that has not written its rows yet is skipped, and `0 names read` with exit 0 said the same thing for *every record is clean* and *no record was opened* — this work item sat in the second state through five of its six phases. The COUNT and not the thirty-eight names behind it: a notice nobody reads is the state this is fixing. **What is NOT fixed and why**: an untracked or `.gitignore`d file still supplies a name, because closing that means asking git what it carries and this checker calls git for nothing outside `--migrate` (`README.md`). The hole leans safe — CI reads a clean checkout, so CI is the stricter reader — and `questions.md` Q1 puts the exception to the owner. **`unread_items` re-read 2026-09-08 in work item 1788826000's round 2 fix pass.** Its hash moved in that work item's round 1 fix pass with no note saying who read what. Executed: across the whole branch, `git diff <base>..HEAD` over `evidence_check.py` is ONE line — the rider's own stamp, `Verified 2026-09-07 at 70c272c` becoming `Verified 2026-09-07 against unread_items@9046e0b6`. The date is unchanged, the claim did not move, and a ledger hash covers comments (round 2, finding 14)  **Re-read 2026-09-13 in work item 1789296100 (#354).** The records arm is untouched. `main` changed only where it grades and where it prints, both after that arm has run. **Re-read 2026-09-24 by work item 1790174138 (#217), which drifted `claim_lines`.** The clause *a fence the record never closes is a malformed record whose lines are read* now holds of an HTML comment the record never closes too — `aside_held` beside `held`, cleared at `-->` the way `held` is cleared at the fence's close, and the marker still exempting a held line. The comment-to-its-`-->` and fence-to-its-marker halves are unchanged; the fragment's A4 carries the new half **Re-read 2026-09-24 by round 1's fix pass of work item 1790208593 (#547):** the same `tree_names` docstring edit; the quotation rule is unchanged **Corrected 2026-09-25 by work item 1790260566 (#220), phase 4:** the claim is narrowed to what the code does and now documents. A comment is an aside only where it begins a line or the remainder after a closer, which was already true of the code and is why `seal/follow-up.md`'s records-arm row existed. A comment ends at its `-->` rather than at the end of that line, so a name after the closer is read. The fence close is the shared delimiter rule, so a shorter or info-carrying run no longer closes a block. **Re-read 2026-09-25 by work item 1790297087 (#299), phase 2:** `main`'s change is the malformed count on the ledger arm's totals lines; the records arm's reading of quotations is untouched, so the claim holds. **Re-read 2026-09-26 by work item 1790381328 (#614), phase 3:** `main`'s change is `--strict`'s help text, which now says drift and malformed coordinates also fail; the grading itself moved in `exit_code`, which `main` calls as before. The records arm is untouched, and the claim holds |
| R6 · in the records arm a directory the walk cannot LIST is `UNREADABLE` and exit 2, the way an unreadable file already is — one work item's own folder, and `seal/ledger/` itself, whose empty answer used to take the whole arm quiet; a directory that is ABSENT stays an empty answer | `skills/evidence-check/scripts/evidence_check.py#record_files@d660319b`, `skills/evidence-check/scripts/evidence_check.py#unshipped@156ff0f6`, `skills/evidence-check/scripts/evidence_check.py#check_records@316d8a23`, `tests/test_a_record_states_what_the_tree_has.py#test_a_records_directory_that_cannot_be_listed_is_named@fcb7f354`, `tests/test_a_record_states_what_the_tree_has.py#test_a_ledger_fragment_directory_that_cannot_be_listed_is_named@02c05002`, `tests/test_a_record_states_what_the_tree_has.py#test_a_missing_ledger_folder_is_still_an_empty_answer@6321c79c` | **Executed** 2026-09-07 by round 2's fix pass. All three cases seen red first: with the walk unguarded a work item whose `rounds/` refused `os.scandir` produced no findings at all, and with `unshipped` swallowing the listing the whole arm returned `([], 0, 0)`. Built by monkeypatching `os.scandir` and `os.listdir` rather than by `chmod 000`, which is nothing to root and a read-only flag on Windows (`agent-contract` §13). The pair case is the third: written against the undistinguished form, where every `OSError` refused and an absent `seal/ledger/` became a finding | 2026-09-07 | **The class was enumerated by construction, not by reading for more of the shape.** Every way this file reads a directory — `os.walk`, `os.listdir`, `os.scandir`, `glob` — asked whether its failure reads as *nothing found*. Three of six do. `record_files` is the instance round 2's 🟡 4 opened; `unshipped` is the same defect one directory up and strictly worse, fixed through an optional `refused` list rather than a widened return, because three call sites and seven cases read that return and a fix pass threads a channel instead of re-shaping a contract. `unread_items` moves a COUNT rather than a finding and is deferred as a rider at its own line, which is where `seal/follow-up.md` sends anything tied to a coordinate. The other two refuse MORE rather than less — `tree_names` losing names makes the tree read as missing them, and the look-alike scan only weakens a hint beside a row already refused — so they are not this class |
| R7 · every coordinate the records arm prints is separated with `/`, because the arm BUILT it — `display_name` returns the caller's own spelling, which is right for a `--ledger` pattern a person typed and wrong for a path that came out of `os.walk` | `skills/evidence-check/scripts/evidence_check.py#built_name@be6f7037`, `tests/test_a_record_states_what_the_tree_has.py#test_a_built_coordinate_prints_with_forward_slashes_on_windows@227123d6` | **Executed** 2026-09-07. Seen red the only way this one can be: with the `.replace` deleted the case returns `seal\specs\1780000000-live\rounds\round-2.md` and fails, and the module goes `1 failed, 57 passed`. The live proof is CI — the Windows leg was RED on the records arm's own coordinate assertion — `coord.endswith` a record path and its line — from `d80469a`, the commit that added this arm, through **three review rounds and two fix passes**, and green at the commit that carries this row | 2026-09-07 | **Three rounds and a broad gate missed it because all five ran on macOS, where the fix is a no-op.** That is `agent-contract` §13 in the plainest form it has taken on this branch — a defence resting on a platform guarantee nobody removed — and #103 is the ticket for the leg that finds these last. `flavour` is what removes the guarantee from a POSIX machine: the case passes `ntpath` rather than skipping off Windows, which is the pattern `test_the_printed_ledger_name_is_the_file_that_was_read.py` already establishes for the same unit's sibling. **The class was enumerated by provenance, not by grep.** Of the eleven `display_name` call sites, the four that print a `--ledger` path and the one that prints a narrowing notice keep the operator's spelling by design; the five inside `check_records` print paths the arm built and are the class. The split is *who spelled it*, which is also why one helper rather than five replacements: a sixth built path added later goes through `built_name` or it does not print. **Two arms printing one coordinate two ways is round 1's finding 1 in a different currency** — there the same anchor graded `OK` in a ledger and `BROKEN` in a record; here it prints `/` from a ledger row read out of a file and `\` from a record path built by a walk |
