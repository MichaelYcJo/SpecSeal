## 0.13.1 — 2026-09-22

<!-- specs/1790076050-the-release-tail-is-three-acts-no-document-names -->
### 1790076050-the-release-tail-is-three-acts-no-document-names

<!-- seal/ledger/1790076050-the-release-tail-is-three-acts-no-document-names.md

What this work item settled by opening code, one row per claim. The release
that ships it folds this file into `seal/ledger.md` and removes it.

**T1 carries a claim forward from a row this branch removed.** `seal/ledger.md`
carried R2 under `1789172128-a-release-is-sized-by-a-count-and-cut-by-urgency`:
*`size: now` is defined where labels are defined, in the prefix form, in two
states, and **nothing reads it***. The last conjunct stopped being true here —
`close-issues-on-release.yml` reads the label, and only to take a spent one off
the issue it is closing — so the row was REMOVED there rather than re-pointed,
which `CLAUDE.md` §*A change writes fragments* and §*Appended is the word, and
a removal is not one* between them license, and the corrected claim is written
here against the coordinates that carry it now. Both of R2's anchors still
resolve; what failed was the claim, not the anchor, and a row whose claim has
gone false is not one to re-stamp.

R2's own Notes said the gap out loud: *nothing pins the label against the
tracker — a label that is never created, or created under a different
spelling, leaves the document describing something absent, and no check can
see that.* T2 is that gap closed from the only side a check can reach: the
declaration and the document are held together in the tree, and the workflow
is what carries the declaration to the tracker.

**What deliberately has no row.** The release-note workflow (phase 1) and the
directory command (phase 3) are new mechanism whose whole behaviour is pinned
by their own modules, case for case; a row per arm would be an inventory of
the diff. What earns a row here is a judgment a later tidy-up would undo in
good faith — a delegation that looks like indirection, an invariant that
looks like it was simply relaxed, and a claim about a document that a reader
cannot check without the tracker in front of them.

Every hash here was stamped by `evidence-check --reverify --ledger '<this
fragment>'`, the scoped WRITE form, never by an unscoped run. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| T1 · `size: now` is still defined where labels are defined, in the prefix form and in two states, and **one thing reads it — only to spend it**. No workflow, no check and no script reads it to decide what happens next; `close-issues-on-release.yml` removes it from each issue it closes, after the close rather than before | `docs/issues-and-milestones.md#"## A label answers *what it is about*, and survives the move"@39297113`, `.github/scripts/close_issues_on_release.py#main@ce0d9964`, `.github/scripts/close_issues_on_release.py#drop_label@f50fccb5`, `.github/scripts/close_issues_on_release.py#spend_label@dfb24ccf`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_label_is_two_states_and_nothing_schedules_from_it@1d7b8fbe`, `tests/test_a_declared_label_reaches_the_tracker.py#test_the_close_comes_first_so_a_failed_label_write_cannot_cost_it@475ab90c` | **Executed** 2026-09-22. The reworded sentence's literal was moved in the same commit and the case re-run green; deleting either half of the new sentence reds it. The ordering case asserts the close's index in the recorded call list precedes the label edit's, rather than asserting both happened — both succeeding says nothing about which survives the other failing — and the refusing-tracker case leaves the issue closed with `could not remove … closed either way` on stdout | 2026-09-22 | **This replaces `seal/ledger.md`'s R2**, whose final conjunct this work falsified. The ordering is the part a later tidy-up undoes in good faith: reading the two writes as one act invites putting the cheaper one first. An issue that closed and kept a stale label is a wrong answer on a tracker; an issue left open because a label write failed is a release that did not finish, and only the second is unrecoverable without a person. That asymmetry is why `drop_label` is the one place this script does not fail loudly, against a docstring three paragraphs long about why it otherwise does. **Re-anchored 2026-09-22 in round 1's fix pass (finding 4)**: the case this row cites was named `…_and_nothing_reads_it` while its body asserted that one thing does read it, so the coordinate a reader opens to check this claim was named for the claim's negation. The case is `…_and_nothing_schedules_from_it` now and the anchor follows it; nothing about the claim changed **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold's side of this section is `<!-- specs/<id> -->` marker comments and nothing else; no sentence changed, so the claim holds and the hash is both sides' edits together. **Re-read 2026-09-23 by work item 1790173209 (#536).** `main`'s close loop now collects refusals instead of exiting on the first: the close goes through `close_issue`, an issue both routes refused keeps its label and is named at the end. The label still comes off after the close and only after it, so the claim holds. **Re-read 2026-09-24 by work item 1790173209 (#363).** The section's version-rule paragraph changed and nothing about the label did; the claim holds. **Re-read 2026-09-24 by work item 1790173209 (#198).** The section's `flow-measurement` paragraph gained a sentence; the label is untouched, so the claim holds. |
| T2 · a label a document specifies is reconciled onto the tracker by the workflow that already runs when `main` moves, and the declaration names the document that specifies each | `.github/scripts/tracker_labels.py#LABELS@a6833af9`, `.github/scripts/tracker_labels.py#missing@37ed496e`, `.github/workflows/close-issues-on-release.yml#"- name: create the labels the documents specify and the tracker lacks"@dcdc58b4`, `tests/test_a_declared_label_reaches_the_tracker.py#test_a_missing_label_is_created_with_the_documents_own_sentence@f88ad34b`, `tests/test_a_declared_label_reaches_the_tracker.py#test_reconciling_an_already_complete_tracker_writes_nothing@3cf690f5` | **Executed** 2026-09-22 over a fake tracker: a missing label produces exactly one `gh label create` carrying the document's own sentence, and a complete tracker produces no write at all. **Read** 2026-09-22: `gh label list` on the live tracker still does not list `size: now`, which reproduces #450's 2026-09-20 measurement — the label has never existed and the specifying section has described an absent object since 0.11.1 | 2026-09-23 | **The act was a person's, and that is what made it invisible.** The work item that wrote the section ended its changelog entry with *creating the label and applying it are the repository owner's, after this merges*; a person has no queue this repository can read, so three releases were cut with nothing anywhere to notice. The `specified_by` field is the part that looks droppable: it is never read by code and never printed except in the missing-label report, and without it this file is a copy of a decision whose original a reader cannot find. A gate for a missing label was rejected rather than forgotten — it would be red on the very branch that adds one, since no agent in this repository's chain may write to the tracker, and it would grow `broad_gate.py`'s partition as well **Re-read 2026-09-23 by work item 1790134781 (#515).** The first `--apply` after a release was refused by the tracker with a 422, because `size: now`'s description was 119 characters and GitHub caps one at 100, so the label this row says the workflow creates was still not created. The description is now the specifying sentence's clause in 95 characters, keeping both halves this row's case asserts — what the label means and when it comes off — and the case's second literal moved with it. The claim holds: the reconcile still produces one `gh label create` carrying the document's own words, and the cap is a new claim in that work item's fragment. Round 1 of the same work item then gave the workflow step this row cites `if: ${{ !cancelled() }}`, so it runs even after the close step before it failed; that moved the anchor's hash and widens nothing this row claims, since the step still runs on every push to `main` that is not cancelled. The condition is that fragment's C2 |
| T3 · an issue's labels are read in one place, by the module that owns the 404-tolerant read they are built on, and the sibling delegates | `.github/scripts/close_issues_on_release.py#issue_labels@1c7a1dce`, `.github/scripts/label_merged_on_release_branch.py#issue_labels@7afd8565`, `tests/test_a_declared_label_reaches_the_tracker.py#test_the_label_reader_has_one_source@a549f867` | **Executed** 2026-09-22: `tests/test_a_merged_ticket_says_so_on_the_tracker.py` re-run green unchanged, which is the load-bearing half — its fixtures monkeypatch `closer._issue_api`, and the delegation still reaches that patch, so the sibling's whole suite is a control on the move | 2026-09-22 | The reader lived in the sibling while the sibling was its only caller, reaching for `closer._issue_api` with a docstring explaining that reaching for a private name was deliberate. The closing script then needed the same read, and two near-identical readers is the exact thing that script's own docstring says it exists not to have. **The case pins the CALL, not the word**: a first version asserted `_issue_api` appeared nowhere in the sibling and went red on the docstring sentence explaining the history, which would have made recording why the move happened the thing that fails |

<!-- specs/1790076060-the-cap-is-read-as-bounding-fixes-and-the-pile-is-nobodys -->
### 1790076060-the-cap-is-read-as-bounding-fixes-and-the-pile-is-nobodys

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · the document that owns the cap states that three and five count ROUNDS, that a run the round cap stopped may still write a fix, and that the test is *who owns the unit now* — with each record's `New units` row named as where the evidence already sits | `docs/review-chain-spec.md#"### The cap bounds rounds, and not the fixes of the round it stopped"@3e7be78c`, `tests/test_the_rules_have_one_owner.py#RULES@6be9f0b8` | **Executed** 2026-09-22: rule 13's two assertions seen red one mutation at a time, eight in all — the owner's sentence and each of the seven links — each run exiting 1 and naming the case it was meant to redden, with the module 58 green before the first mutation and 58 green after the last restore | 2026-09-22 | The substitution the measured run made was *when did the defect start*, and the subsection names it as the wrong question rather than leaving the next reader to avoid it. The rule has more link carriers than any other row of that registry, because the sentence it corrects had already been copied into six of them **Re-read 2026-09-23 by work item 1790119502 (#514)**, which folded work item 1790076060 by putting its marker on two lines of this subsection and changed no sentence: the rounds-not-fixes rule, the ownership test and the two exits read word for word as before, and `tests/test_the_rules_have_one_owner.py` is green. The claim holds. |
| C2 · two bounds exit a run `capped` and the permission to write a fix is the round cap's alone: `chain_check.py`'s reopening walk refuses a SECOND fix-closing record after a floor `no`, so the reopening bound's terminal record commissions nothing | `docs/review-chain-spec.md#"### The reopening — one, and then the run is capped"@75c0e8a6`, `skills/code-review/scripts/chain_check.py#stopping_floor@1878a277` | **Read** 2026-09-22, and **executed** as an absence: `git diff 6d410023..HEAD -- skills/code-review/scripts/chain_check.py` touches the module docstring and nothing else, so the walk that refuses the second record is byte-identical to the base. `tests/test_the_reopening_is_one.py` is green over the corrected subsection | 2026-09-24 | Writing the permission into the reopening subsection would have named a state its own checker refuses, which is #341 arriving again. The two are named apart instead, and each subsection says which of them it is **Re-read 2026-09-23 by work item 1790173106 (#138), which drifted it.** The unit's own-row read of `Needs a fix` now goes through `says_reopened` and refuses a bare `yes` in the floor row's words; the count walk, the reopening walk and the count refusal's message are unchanged, so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#218)**, which changed only the `What new prints` table in the spec's reopening subsection; the two bounds and the `capped` exit are as they were. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (⬜ 5)**, which named the running sibling in the `this record ends the run` row of the subsection's `What new prints` table; the reopening rule and the `capped` exit are as they were. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. |
| C3 · a capped record that wrote fixes reads `round-N` and is NOT the last record; the verifying round that read those fixes is, and `round_record.py seal` is what requires it by refusing `Broad gate` on a last record whose cell reads anything but `no fixes to check` | `docs/review-chain-spec.md#"### The cap bounds rounds, and not the fixes of the round it stopped"@3e7be78c`, `skills/code-review/scripts/round_record.py#seal@ad6b9461` | **Read** 2026-09-22 against the shipped pair: `seal/specs/1790039346-settle-reads-a-marker-inside-a-commented-out-draft/rounds/round-6.md` is a capped record with five findings closed on fixes and `Fixes checked by: round-7`, and `round-7.md` reads `no fixes to check` beside `Broad gate: 5e2ede92 against 3cdfd8ad`. That branch shipped green, so the shape the sentence permits is one the checkers already accept | 2026-09-24 | The sentence this corrects said a capped record's cell reads `no fixes to check`, which is true of a record that closed nothing on a fix and false of one that did. Both shapes are in the tree, one work item apart **Re-read 2026-09-23 by work item 1790119502 (#514)**, which folded work item 1790076060 by putting its marker on two lines of this subsection and changed no sentence: the rounds-not-fixes rule, the ownership test and the two exits read word for word as before, and `tests/test_the_rules_have_one_owner.py` is green. The claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174), which drifted `seal`** at the write alone; the `Fixes checked by` refusal that requires the verifying round is as it was. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (🟡 1, ⬜ 8), which drifted `seal`.** The keep moved into `kept_broad_gate`, shared with `close --broad-gate`, and a run at the commit the newest entry already names replaces that entry rather than duplicating it; the refusals are unchanged. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. |
| C4 · a finding is filed where somebody will act on it: an open finding takes the first rung that fits, and a new issue is the third rather than the default. The test is read off the reviewer's `## Deferred` table, whose `Who answers it` column already existed — so no parsed field, verdict word or template column was added | `docs/review-chain-spec.md#"### Where a leftover goes — the ladder, and why a new issue is not the default"@e52cc6d4`, `skills/code-review/scripts/round_record.py#DEFERRED_HEADER@ab8ad6ea`, `tests/test_the_rules_have_one_owner.py#RULES@6be9f0b8` | **Executed** 2026-09-22: rule 14's two assertions seen red one mutation at a time, six in all — the owner's sentence and each of the five links — the module 60 green before the first and 60 green after the last restore. **Read**: `DEFERRED_HEADER` is unchanged from the base by the same diff C2 cites | 2026-09-22 | What the fourth rung costs is written into the rule: a real defect that can name nobody stops being visible in the tracker and lives in the round record and the pull request body. `seal/follow-up.md` sits on the third rung and takes the same test, which is what keeps every sentence elsewhere about that file true **Re-read 2026-09-23 by work item 1790119502 (#514)**, which folded work item 1790076060 by putting its marker above the ladder and added one paragraph after the rung-4 cost: `CAPPED_EXIT` and `DEPTH_EXIT` still say a refused finding becomes an issue, and rewording them is the repository owner's decision. The ladder, its test and its table are unchanged, so the claim holds. **Re-read again 2026-09-23** in round 1's fix pass of the same work item, which extended that paragraph: `CAPPED_EXIT`'s `Fixes checked by` half and the prose carriers of the depth-exit wording are now named in it. The ladder, its test and its table are still unchanged, so the claim holds. **Corrected 2026-09-23** in round 2's fix pass: that paragraph first listed four carriers and missed `templates/sdd-round.md`; it now names the search that finds them, *or becomes an issue* with whitespace collapsed, rather than a list, and says not every carrier points at the ladder. The ladder, its test and its table are still unchanged, so the claim holds. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which added one paragraph after the sealer sentence in this section — the cell keeps every run, newest first; the ladder this row is about is unchanged. **Re-read 2026-09-24 by work item 1790206437's round-1 fix pass (⬜ 3):** that sealer sentence now says the same comparison as the newest entry replaces that entry (narrowed to the newest entry by round 2's fix pass, ⬜ 6); the ladder is unchanged and the claim holds |
| C5 · the `from-review` label is documented where the tracker's conventions are owned — what it marks and what it is counted for — so the share behind the ladder is one query rather than a reconstruction | `docs/issues-and-milestones.md#"## A label answers *what it is about*, and survives the move"@39297113` | **Executed** 2026-09-22: `grep -c "from-review" docs/issues-and-milestones.md` returns 2 where it returned 0 before this branch | 2026-09-22 | 89 issues carried the label and no document in the tree named it. The number the ladder rests on — 43 of them closed, 48%, on 2026-09-22 — is not re-derivable from a label nobody documented, which is why documenting it was in scope for a work item that adds no mechanism. **Corrected 2026-09-22** in round 1's fix pass: this row and the two documents first said 47% closed with 47 open, which is arithmetic the tracker does not hold, and carried no as-of date. Round 1 finding 4 re-ran the query and the fix pass re-ran it a third time; all four carriers now state 89 / 43 closed / 46 open, dated, with this run's own issue excluded **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The fold's side of this section is `<!-- specs/<id> -->` marker comments and nothing else; no sentence changed, so the claim holds and the hash is both sides' edits together. **Re-read 2026-09-24 by work item 1790173209 (#363).** The section's version-rule paragraph changed and the label paragraph did not; the claim holds. **Re-read 2026-09-24 by work item 1790173209 (#198).** The section's `flow-measurement` paragraph gained a sentence; the label paragraph is untouched, so the claim holds. |
| C6 · after round 1's fix pass no LIVE document tells a reader that an open finding at a capped exit becomes a new issue. The class was enumerated by construction over `docs`, `skills`, `agents`, `templates`, `CLAUDE.md`, `CONTRIBUTING.md` and both READMEs, on fifteen independent spellings of the destination, and the one remaining site is a runtime message left standing on purpose | `docs/review-chain-spec.md#"### The reopening — one, and then the run is capped"@75c0e8a6`, `skills/code-review/scripts/chain_check.py#CAPPED_EXIT@8bd0d100` | **Executed** 2026-09-22: the sweep returns four hits, and all four are outside the class — `chain_check.py`'s `CAPPED_EXIT` (the runtime message, disclosed in `overview.md`) and three carriers of *deferred with a named answerer, or becomes an issue*, which names two homes generically and is the depth exit rather than the capped one. Before the pass the same sweep returned seven, three of which the reviewer had not named | 2026-09-24 | The reviewer enumerated four live sites; the sweep found three more — `skills/code-review/orchestration.md`'s and `skills/implement/SKILL.md`'s copies of the no-mechanism destination, and `agents/warden.md`'s `deferred #N` candidates. That is the third time in one work item that a carrier list was shorter than the class, which is why this row records the sweep rather than the list. Two pins require `deferred #N` to survive — `test_the_reopening_is_one` on the spec's subsection and rule 4's link count over the review skill's two halves — so the phrase is kept in both places as the spelling for the issue home, and the paste-ready fixes were adapted rather than applied verbatim. **Re-read 2026-09-24 by work item 1790174138 (#218)**, which drifted the `### Review arm` region through its `What new prints` table alone; the capped-exit sentences this row is about are unchanged. **Re-read 2026-09-24 by work item 1790174138's round 1 fix pass (⬜ 5)**, which named the running sibling in the `this record ends the run` row of the subsection's `What new prints` table; the reopening rule and the `capped` exit are as they were. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. |

<!-- specs/1790076070-the-fold-ships-and-the-corpus-is-still-on-disk -->
### 1790076070-the-fold-ships-and-the-corpus-is-still-on-disk

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A fold marker is matched whole, so it cannot be wrapped — and the wrap limit skips it rather than the document leaving the covered set | `tests/test_docs_line_wrap.py#prose_lines@76df356d`, `tests/test_docs_line_wrap.py#test_a_fold_marker_is_skipped_and_the_line_beside_it_is_not@bbed20be`, `tests/test_docs_line_wrap.py#test_a_marker_that_is_not_on_a_line_of_its_own_is_still_prose@7144339a` | **Executed**: with the skip arm deleted, the marker case yields `[1, 2]` where it asserts `[2]` and the parametrised case on `docs/the-evidence-ledger.md` fails at 89 columns; both green with it. Measured over this fold's 88 markers: one reaches 89 columns, 87 fit | 2026-09-22 | The length is not the document author's to control — it is `<!-- specs/ -->` plus a work item id, and an id is `<unix-epoch-seconds>-<slug>` with nothing bounding the slug. 87 of 88 fitting is luck, which is what makes this a class rather than one long directory name. **Re-read 2026-09-23 for #497's round 1 ⬜ 7.** The skip now matches the raw line rather than the stripped one, because the reader anchors the marker at column 0; an indented marker is prose again, and the marker-in-a-sentence case gained that arm, seen red with the skip matching the stripped line. The claim holds: a marker on a line of its own is still skipped and the line beside it is not |
| A `routing.md` this pull request RETIRED is not one it declared, and the marker, or the rule arm's predicate asked of the merge base, is what tells a retirement from a deletion | `skills/code-review/scripts/chain_check.py#main@47efa0ab`, `tests/test_chain_check_at_the_pull_request.py#test_a_declaration_this_branch_retired_is_not_one_it_made@aa5a5b74`, `tests/test_chain_check_at_the_pull_request.py#test_a_deleted_declaration_with_no_marker_is_still_refused@dc635c09` | **Executed**: `chain_check --baseline origin/release/v0.13.1` failed all 88 retired declarations before the arm and prints 88 `retired:` notices after it, leaving one error — this work item's own absent round record. With the arm removed the excusing case fails and the refusing case stays green, so the pair is not checking one thing twice | 2026-09-22 | `unverified_check.folded_items` grew this distinction when `settle` shipped and this reader did not, so two readers disagreed about one tree. `changed_routing` already excludes a rename for the same reason; this is the other way a declaration leaves a diff **Corrected 2026-09-23 by work item 1790138190 (#517), phase 4.** The clause said the marker is the *only* thing that can tell the two apart, which the rule arm made false: `settle --retire` also removes a released directory with no `spec.md` and nothing open, with no marker, and `main` now reads such a declaration as `retired: by the rule` when `reader.retired_by_rule` holds at the merge base and the directory is gone. A directory with a spec or an open row at the base is still refused. |
| Five permanent ledger rows anchored into a work item's own `spec.md`, where `spec.md` G3 enumerated one anchored at a `rounds/` record | `seal/releases/0.4.0.md#"### 1788331011-two-roots-hold-three-lifetimes"@dbc82269`, `seal/releases/0.5.0.md#"### 1788398967-local-modes-records-never-leave-the-clone"@e6b5771f` | **Executed**: `evidence_check.py --strict .` after the retirement reported `5 broken`, each `file not found` on a `seal/specs/<retired-id>/spec.md#"<heading>"` anchor — work items 1788331011, 1788354065, 1788398967, 1788420760 and 1788789330. After the repair, `1435 ok · 0 drifted · 0 broken`, exit 0 | 2026-09-24 | G3 called line 78 "the one permanent row that reads one", and that row was safe because its directory is kept. It counted rows anchored at a `rounds/` record and missed five anchored at a `spec.md`. The two anchored here keep a live code anchor and lose only the dead one; three had no other anchor and are REMOVED per `CLAUDE.md`. **The multi-anchor case is the owner's** — `seal/ledger.md` §1788354065's S12 row already named it as such, and this branch is its second instance **Re-read 2026-09-23 at the merge of `release/v0.13.1` into #497's branch.** The release side added re-read notes to the review-arm row under this heading and moved its hash; no row here gained an anchor into a retired `spec.md`, and `evidence-check` reports 0 broken. **Re-read 2026-09-23 by work item 1790138190 (#517), phase 4**: the `### 1788331011` section moved only by the dated *Re-read* notes that work item appended to two of its rows; the live anchors this claim is about are unchanged. **Re-read 2026-09-23 by work item 1790154759 (#518).** Its one change under this heading is a re-read note on the review-arm row and that row's hash, twice — the build and round 1's fix pass; no row gained an anchor into a retired `spec.md`. **Re-read 2026-09-24 by work item 1790173106 (#138, #241), which drifted the first anchor.** The `### 1788331011` region it hashes holds the review-arm heading row this work item annotated twice; the five rows this claim is about are where they were, and the re-stamp took two `--reverify` passes because the row it hashes was itself being re-stamped in the first. **Re-read 2026-09-24 by work item 1790206437 (#366), phase 4:** the same two-pass shape — the opt-in-headings row inside this section was re-stamped for the split-at-two-depths sentence, and this row's hash of the section moved with it; the five rows are where they were and the claim holds. Re-read 2026-09-24 — #526's split added Re-read notes to rows inside the anchored section and re-pointed none of its `spec.md` anchors; the claim unchanged. **Re-read 2026-09-24 by work item 1790208593 (#547, #553), phases 1–4:** the `### 1788331011` section moved only by dated *Re-read* notes on its rows (the S1 layout row among them) and their re-stamps; no row there gained or lost an anchor into a `spec.md`, so the claim holds. At the release that ships #547, `--split` rewrites both anchors of this row to `seal/releases/0.4.0.md` and `seal/releases/0.5.0.md`, hash unchanged. **Re-read again 2026-09-24 (#547), phase 5:** the section moved by one more dated note (S13's) and its re-stamp; no anchor into a `spec.md` changed. **Re-read again 2026-09-24 (#547), round 1's fix pass:** the section moved by one more dated note (the opt-in headings row's) and its re-stamp; no anchor into a `spec.md` changed. Re-read 2026-09-24 at the merge of `release/v0.15.1` (#558, #559) into #526's branch, at `4a077852`: both sides added dated notes to rows inside the section, and the merge kept both; no row there gained or lost an anchor into a `spec.md`, so the claim holds. |
| A range that retires 88 work items reports 12,100 survivors, and one range row excuses every one of them | `skills/code-review/scripts/survivor_check.py#whole_range@5c8d9d1b`, `skills/code-review/scripts/survivor_check.py#report@803b4586` | **Executed**: `survivor-check --range origin/release/v0.13.1...HEAD --exempt <this item>/survivors.md` exits 0 with `every survivor is excused by a row above (12100)` | 2026-09-23 | The range-row mechanism was built for a shape whose largest measured instance was 153. At 12,100 the per-sentence alternative is not an escape anybody takes, which is the argument the row was shipped on. **Re-anchored 2026-09-23 for #497's round 1 🟡 6.** The row used to anchor at this work item's own `survivors.md`, which the next fold retires with the directory, and that is the class phase 11 repaired in five `seal/ledger.md` rows. It now anchors at the checker that lets one range row excuse a range: `whole_range` returns the first declaration that resolves onto the run's range and belongs to a work item the range touches, and read on this date it still does **Widened 2026-09-23 after round 2's second note**: `whole_range` selects the declaration and `report` is where a matched range becomes every candidate's grounds, so the row anchors both and drifts when either changes. **Re-read 2026-09-24 by work item 1790206435 (#543, #307, #439), phase 4**: `whole_range` still returns the first declaration that resolves onto the run's range and belongs to a work item the range touches; what changed is a declaration that resolves nowhere, which is now asked the same ownership question before it is printed. `report` is untouched. The claim holds. **Re-read 2026-09-24 by the same work item, phase 5 (#551)**: `whole_range` reads its `changed` list with `--no-renames`; the declaration it returns is chosen as before, and the claim holds. **Re-read 2026-09-24 in round 1's fix pass (⬜ 2)**: one sentence of `report`'s docstring now says `unresolved` holds the declarations this run could have used, which `whole_range` decides; what `report` does with a matched range is unchanged, and the claim holds. |

<!-- specs/1790076080-every-orchestrator-rule-is-a-sentence -->
### 1790076080-every-orchestrator-rule-is-a-sentence

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Seven rows. O1 to O3 are the table and what holds it; O4 to O6 are the posting
command, and O7 is round 1's repair to it. Nothing here corrects an existing
`seal/ledger.md` row: the nine rows
anchored at `skills/verify/SKILL.md#"## Measure the segment, and feed the flow
log"` were re-read in place on 2026-09-22 and all nine still hold, so each
carries a `Re-read` note there rather than a replacement here. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| O1 · every act addressed to the orchestrator has a row naming what delivers it, and the table is held against both orchestration files from **both** sides: an act with no row fails, and a row naming an act no file carries fails | `skills/implement/orchestration.md#"## Orchestrator: which of these acts runs itself"@d02bed82`, `tests/test_every_orchestrator_act_names_its_delivery.py#findings@64f2f5b5` | **Executed** 2026-09-22. The real-tree case was run before the section existed and failed on the header line being absent, naming the file; it is green after. Four planted directions, each a temp tree with one defect — an act with no row, a row naming a heading no file carries, a row naming `bin/does-not-exist`, and `still a sentence` with an empty grounds cell — plus a floor case asserting the same tree without the defect is clean. Eight mutations of the units behind it, one at a time, all red | 2026-09-22 | The `Delivered by` cell answers one question — when the orchestrator forgets this act, what notices — because several sections carry more than one act and *what delivers this section* has no single answer. The four values are a command that exists, a check that exists, `part of its parent's act`, and `still a sentence` with grounds. The discipline beside it is that a row whose delivery reaches only part of its act names the part it does not reach, which **twelve of the thirteen delivered rows** do. **Corrected 2026-09-22 in round 1's fix pass**, which is why the number is stated against a denominator: this cell read *eight rows*, a count nobody had taken, and a count in a ledger row outlives the round that wrote it. Round 1 read it as *eleven of fourteen* — eleven was right before its own finding 4 was fixed, and fourteen was not, because `command:` and `check:` rows number thirteen and the reviewer's own list of eleven plus two sums to that. The thirteenth, `Then say who checked them, in the record`, carries no limit sentence because it has no gap to name |
| O2 · the table carries **twenty** rows and not the nineteen the frame counted, because the section holding it is itself a marked `##` heading and the row rule reads it | `tests/test_every_orchestrator_act_names_its_delivery.py#test_the_table_reads_the_section_that_holds_it@4781bd05` | **Executed** 2026-09-22 both ways: the parser read exactly nineteen marked headings against the tree before the section landed, at the line numbers `plan.md` lists, and twenty after | 2026-09-22 | `spec.md` says nineteen and, in the same bullet, that the new section *"makes the section a row of its own table"*. Nineteen is the count before this work writes anything. Exempting the section holding the table was the alternative and was rejected: an exemption is a special case that hides an act, which is the state #330 reports. The self-referring row's own delivery is this test |
| O3 · the `Orchestrator:` marker is read by **one** parser, which both modules that ask about it call | `tests/test_a_section_marked_for_one_role_reaches_only_that_role.py#headings@078aa552` | **Executed** 2026-09-22: `marked_headings` is now a filter over it and its own module is green at 22 passing with the new one; breaking `headings` so it stops reading `###` turns the new module red | 2026-09-22 | Two parsers of one marker is how half of them come to keep the old answer, which is `hooks/routing.py`'s own reason for being a module. The widening is what the second reader needed: `marked_headings` returns marked lines and the row set needs the level and the nearest `##` as well. *Directly beneath* means the nearest `##` rather than the nearest MARKED one, which `test_a_third_level_heading_under_an_unmarked_section_is_not_an_act` pins — read the other way, every `###` after the last marked section is swept in, which in `skills/code-review/orchestration.md` is most of the document |
| O4 · `--post` resolves the label and implements five states, and **opens no issue in any of them** | `skills/verify/scripts/session_cost.py#post@8cb47ede`, `skills/verify/scripts/session_cost.py#open_log@9d328b5c`, `tests/test_session_cost_post.py#test_no_state_ever_opens_an_issue@f95b434e` | **Executed** 2026-09-22 with `run_gh` replaced in every case, so nothing in the suite touches a network. All 22 cases written before the end-to-end pair were run against the module as it stood at the parent commit: 22 failed, 0 passed. Fourteen mutations of the new units, one at a time, all red | 2026-09-22 | The spec names four states and all four are facts about the tracker. The fifth is a fact about the machine: a lookup that could not run — no `gh`, no authentication, no repository, or output that is not the JSON it asked for. It and `NO_HISTORY` exit 0 with nothing posted, because most installed repositories never create the label and a machine with no `gh` is the same shape; a command red in the ordinary case is one people stop running. The no-issue property is asserted over all four tracker states rather than in the one case about it |
| O5 · `gh issue list --label <a label that exists nowhere> --state all` exits **0 with empty stdout and empty stderr**, so an empty reading is a label with no history and a non-zero exit is a lookup that failed | `skills/verify/scripts/session_cost.py#open_log@9d328b5c` | **Executed** 2026-09-22 against this repository with the control label `zzz-no-such-label-9x`: exit 0, no stdout, no stderr. The same command with `flow-measurement` returned five rows, one OPEN and four CLOSED | 2026-09-22 | This is `questions.md` Q1, which the frame could not settle because it is a fact about the `gh` binary rather than about this tree. One lookup answers both of the skill's questions, since every open issue is in `--state all`; the default `--limit` of 30 had to be raised, because the history reading counts closed logs and a label with more than thirty of them would read as one that never existed. `test_the_lookup_reads_the_label_and_the_whole_history` pins `--state all` and a limit above 30 |
| O6 · the one act #330 measured is **outside** the table's class, and the section says so rather than carrying a row that reads closed | `skills/implement/orchestration.md#"## Orchestrator: which of these acts runs itself"@d02bed82`, `skills/verify/SKILL.md#"## Measure the segment, and feed the flow log"@70aef725` | **Read** 2026-09-22. Posting a segment's reading lives under a heading carrying no marker, in a file the row rule does not read, so no row can name it and the test would refuse one that did. Its delivery is named in the section's prose instead, with the sentence that nothing makes it run | 2026-09-22 | Marking that heading was considered and rejected on the repository's own one-word-one-meaning rule: the marker means *this section must not reach an agent's payload*, and `skills/verify/SKILL.md` is in no agent's `skills:` list, so marking it would assert something about payloads that is not the reason for marking. Renaming a heading eight documents cite is the other cost. This is the first live instance of the blind spot the table's own prose states — the check reads the marker, not the meaning. **Re-read 2026-09-24 by work item 1790174138 (#174)**, which changed the `Broad gate` row of the run-level table in this section to read the count off the cell — one entry per run, newest first; nothing this row is about moved **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** only the paperwork sentence's record paths changed in the section, so the claim holds |
| O7 · what is printed locally and what is posted are not the same text: the captured body carries the transcript's basename, and an empty `--says` is refused before the log is looked up | `skills/verify/scripts/session_cost.py#emit@661fe5e9`, `tests/test_session_cost_post.py#test_the_posted_body_does_not_carry_the_transcripts_path@01fa2c66`, `tests/test_session_cost_post.py#test_an_empty_reading_is_refused_rather_than_posted@1a349b41` | **Executed** 2026-09-22 in round 1's fix pass. Four cases, each run against the reviewed commit `238dbeaf` before the repair and each red there, green after: the posted body carrying the path under `--segments` and under `--spawns`, an empty `--says` posting a fence with nothing above it, and that refusal reaching `gh` at all. `run_gh` is replaced in every one, so no network call is made | 2026-09-22 | The rule was already in this file and reached one place of three: `main` prints `--latest`'s `# {path}` line BEFORE `emit` captures, so that line never enters the body, while `report_segments` and `report_spawns` print the path themselves on the empty branch — which is the branch the documented invocation hits whenever the named transcript has no subagents beside it. The repair is that same rule at the seam all three reports pass through, not a new convention. The class was enumerated rather than patched at the two coordinates the finding named: every line that could put a path in the body prints the ONE `path` argument, because `measure_segments` stores a segment row's transcript as `os.path.relpath` against the file it was measured beside, so every row label is already relative. The residual is stated in `emit`'s own docstring — a future report line printing some OTHER absolute path would not be covered, and today none does. The live post on #496 was clean: that run used `--spawns` over eight spawns, so the non-empty branch rendered and the leaking line never fired |
