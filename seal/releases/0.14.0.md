## 0.14.0 — 2026-09-23

<!-- specs/1790134781-a-label-description-past-100-characters-fails-every-release -->
### 1790134781-a-label-description-past-100-characters-fails-every-release

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Two rows: C1 is the cap the work item was opened for, and C2 is the workflow
condition round 1 found missing behind it. The existing row it also
touched, `seal/ledger.md`'s T2, was re-read rather than replaced: its claim —
the reconcile creates a declared label carrying the document's own words —
still holds, so it carries a `Re-read` marker there and no row here. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · every label description this repository sends to `gh label create` fits GitHub's cap, which is named once as `LABEL_DESCRIPTION_LIMIT` with where the number comes from, and a case holds each description under it — the declared labels one by one, and the merged-label description at a version far longer than any release will carry | `.github/scripts/label_merged_on_release_branch.py#LABEL_DESCRIPTION_LIMIT@2ec877be`, `.github/scripts/label_merged_on_release_branch.py#label_description@134b3579`, `.github/scripts/tracker_labels.py#LABELS@a6833af9`, `tests/test_a_declared_label_reaches_the_tracker.py#test_every_declared_description_fits_the_trackers_cap@061f30bd`, `tests/test_a_merged_ticket_says_so_on_the_tracker.py#test_the_label_description_fits_the_trackers_cap_for_a_long_version@9a3cb393` | **Executed** 2026-09-23. The declared-labels case was red on the 119-character description before it was shortened (`[('size: now', 119)]`) and green at 95. Each unit was then broken on its own: the 119-character value back reds the cap case and the A9 case; the limit tightened to 50 reds both cap cases, which is what shows they read the constant rather than a literal; the merged-label template grown past 100 at the long version reds its case alone; and the removal half dropped from the description reds A9 alone. **Read** 2026-09-23: the cap's value. GitHub's REST reference for *Create a label* says of `description`: "A short description of the label. Must be 100 characters or fewer." It states no limit for `name`. That matches the 422 quoted in #515's body (*description is too long (maximum is 100 characters)*); the run that body cites, `35796513013`, returns 404 on this repository, so the log itself was not opened. **Corrected 2026-09-23 in round 1's fix pass**: this cell called the value unverified; the reviewer read the reference and the fix pass re-read it | 2026-09-23 | **The number is not this repository's to raise.** A description that does not fit is shortened; bumping the constant turns the suite green and moves the 422 back to the workflow after the merge, where it skipped the step behind it — the flow-measurement roll — and nobody was looking. The merged-label description is the same class because it goes to the same call, and it had 20 characters to spare at `9999.9999.9999` when this was written; pinning it costs one case and removes the question of whether a longer template would still fit |
| C2 · in the workflow that runs when `main` moves, the label step and the flow-measurement roll each run unless the job was cancelled, so a failure in an earlier step no longer skips them; the job still goes red on that failure | `.github/workflows/close-issues-on-release.yml#"- name: create the labels the documents specify and the tracker lacks"@dcdc58b4`, `.github/workflows/close-issues-on-release.yml#"- name: roll the flow-measurement issue to the next version"@ad314c8f`, `tests/test_a_declared_label_reaches_the_tracker.py#test_a_failed_step_cannot_skip_the_independent_steps_after_it@e5f8d26b` | **Executed** 2026-09-23. The case, one parameter per step, was red with both conditions removed (the tree at `a479a38`), red on exactly the one step whose condition was removed or turned into `always()`, and green with both. **Read**: `roll_flow_measurement_issue.py` reads the version in the checked-out tree and the open `flow-measurement` issue, and `tracker_labels.py` reads the declared labels and the tracker's list. Both read what checkout wrote, and the roll reads an issue the close step could close, so what holds is narrower than *nothing an earlier step writes*: neither needs a step after checkout to have succeeded. **Corrected 2026-09-23 in round 2's fix pass**: this cell said nothing either reads is written by a step before it. **Read** 2026-09-23, GitHub's expressions reference, *Status check functions*: "A default status check of `success()` is applied unless you include one of these functions"; `cancelled()` "Returns `true` if the workflow was canceled"; and it recommends `if: ${{ !cancelled() }}` over `always()`. **Unverified**: that a real runner does what that reference says after a failed step; the next release reaching `main` answers it when a step fails, and nothing forces one to | 2026-09-23 | **Round 1, finding 1.** The finding named the roll; the label step is the same case one step up, since a failed close skipped it the same way, so both carry the condition (§12). `always()` was not chosen because it also runs after a cancel, which is a person stopping the job |

<!-- specs/1790138190-settle-leaves-twelve-directories-with-no-way-out -->
### 1790138190-settle-leaves-twelve-directories-with-no-way-out

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

G4 re-founds the claim of the row this work item REMOVED from
`seal/ledger.md` §*Who checked the last round's fixes*: that row's only anchor
lay inside `seal/specs/1788184145-…/rounds/round-3.md`, a directory the next
fold retires, so the claim moves to the prose that already states it. No row
here anchors under `seal/specs/` (`spec.md` G4), or the next fold's guard
would keep this directory. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| G4 · the last round's fixes are read by nobody, and the box saying the review passed is ticked by the session that wrote them — measured on two consecutive work items (#33), and the reason a round record carries `Fixes checked by` | `docs/review-chain-spec.md#"## Two records, and what each of them says"@b7f81131` | **Read** 2026-09-23: the section states the #33 measurement — round 2 of the first work item found seven defects inside round 1's fixes and round 2's own went in unread; the work item after it ended the same way, four findings fixed by the orchestrator, opened by nobody, beside `- [x] Pass` — and derives the `Fixes checked by` field from it | 2026-09-24 | Replaces the `seal/ledger.md` row of the same claim, REMOVED by this work item because its one anchor lay inside `1788184145`'s `rounds/round-3.md`, which that row's own Notes said `settle` would have to drop. The section still names that record's path in prose; the path resolves until the fold that retires the directory, and `settle`'s citation listing hands it to that fold. Re-read 2026-09-24 — #526's split re-pointed a citation inside the anchored unit to the document that now holds the section; the claim unchanged. |
| G3 · `settle` names every ledger row the checker reads — every line of `seal/ledger.md`, of every `seal/ledger/*.md` and of any `docs/**/_evidence.md`, the rows above the first section marker and inside a fence included — whose anchor path lies under a released work item's directory, and `settle --retire` keeps each candidate such a row anchors into, removes the rest and exits 1, naming each row by file, line and first cell | `skills/settle/scripts/settle.py#anchored_rows@87f3fba5`, `skills/settle/scripts/settle.py#retire@393f6fad`, `tests/test_settle_reads_before_it_removes.py#test_a_row_anchored_inside_a_candidate_keeps_that_directory@083ac143`, `tests/test_settle_reads_before_it_removes.py#test_a_row_above_the_first_marker_is_read_by_the_guard@df800902`, `tests/test_settle_reads_before_it_removes.py#test_a_row_in_a_fragment_is_read_by_the_guard@ca52c26b` | **Executed** 2026-09-23: five cases red against the unfixed `retire` (`5 failed, 64 passed`); eight mutations each red alone — fragments not read, liveness ignored, rows above the first marker skipped, `retire` ignoring the anchored set, the verdict always REMOVED, the report omitting the list, the first cell blanked, the live count zeroed. `./bin/settle` on this branch at `62a7c6b` named the one `seal/ledger.md` row on the #33 measurement, against `1788184145`, and nothing else. **Corrected 2026-09-23 in round 1's fix pass** (finding 1): the guard read live lines of two addresses while the checker reads every line of three, so a fenced row's directory was removed and reported BROKEN after it. It now asks `evidence_check.py#default_patterns` for the ledgers and skips no line; `test_a_fenced_anchor_still_keeps_the_directory` and `test_a_row_at_the_old_evidence_address_keeps_the_directory` were each red against the unfixed guard (`2 failed`) | 2026-09-23 | #511. The guard is its own read, not `coordinates`: that reader attributes a row to the section holding it and skips everything above the first marker, where the one anchored row sat. The guard reads the checker's own ledger list and every line, with `COORDINATE_RE`; `coordinates` still reads through `unverified_check.py#live_lines`, because a section is a claim about liveness and a BROKEN row is not. `retire` asks it of its own candidates rather than of `survey`'s list **Re-read 2026-09-24 (#547)**: the checker's list gained `seal/releases/*.md`, and the guard follows it without a change of its own — a row there anchored inside a retiring directory keeps it (case red first); only the docstring's enumeration changed. |
| G3 · per anchored row the command says what `CLAUDE.md` requires — REMOVED when every anchor lies in what is retiring, narrow when a live one remains, with the sentence that the multi-anchor answer is the repository owner's — and edits no row | `skills/settle/scripts/settle.py#verdict@dce2bcd8`, `skills/settle/scripts/settle.py#REMOVED_SAYS@ef88820d`, `skills/settle/scripts/settle.py#NARROW_SAYS@79522084`, `tests/test_settle_reads_before_it_removes.py#test_a_row_with_a_live_anchor_beside_the_dead_one_is_narrowed@a4fd9700` | **Executed** 2026-09-23: the narrow case red against the unfixed code; the mutation making every verdict REMOVED turns it red | 2026-09-23 | `questions.md` Q2's default. The owner's question is recorded against `seal/ledger.md` §1788354065's S12 row |
| D3 · one predicate decides a rule retirement, asked of the working tree or of a ref: the directory existed there, no commit reachable from there ever held its `spec.md`, and its `overview.md` `## Not verified` and `evidence-todo.md` held no open row — an unreadable section counts as open | `skills/verify/scripts/unverified_check.py#retired_by_rule@57093b80`, `skills/verify/scripts/unverified_check.py#open_record_rows@fc556161`, `skills/verify/scripts/unverified_check.py#todo_open_rows@d4aa24b7`, `skills/verify/scripts/unverified_check.py#wrote_a_spec@2c1e77d1`, `tests/test_settle_reads_before_it_removes.py#test_the_predicate_asks_every_condition_itself@7bdf760d`, `tests/test_chain_check_at_the_pull_request.py#test_a_spec_deleted_by_an_earlier_merge_is_not_a_rule_retirement@388a8663`, `tests/test_unverified_rows_close.py#test_a_spec_deleted_by_an_earlier_merge_is_still_a_deletion@aba52e2a` | **Executed** 2026-09-23: fifteen mutations over the predicate and the rule arm; three survived the first pass because `settle` checks `spec.md` and the evidence-todo guard before the predicate decides, and the direct case over the tree and `HEAD` turned all three red. **Corrected 2026-09-23 in round 1's fix pass** (finding 3): the spec was asked of the tree at the ref alone, so a `spec.md` deleted by an earlier merged pull request let the next one retire the directory with no marker; `wrote_a_spec` asks history. Four cases red against the unfixed predicate (`4 failed`). **Corrected 2026-09-23 in round 2's fix pass** (finding 6): `git log` pruned a spec added and dropped on the far side of a merge commit, which is how every release reaches `main`; it now reads `--full-history`, and `tests/test_unverified_rows_close.py#test_a_spec_dropped_on_the_far_side_of_a_merge_is_still_written@d06e7df2` was red against the unfixed call (`1 failed`) | 2026-09-23 | The frame's one condition narrower than D3's sentence (open rows keep the directory) is a judgment the owner may overturn (`questions.md`). `todo_open_rows` is the evidence-todo rule moved from `settle.py#open_rows`, which delegates to it |
| D3 · `settle` prints a released spec-less directory under its own heading — *retired by the rule* or *kept by the rule* with every open row — and `--retire` removes the first with no marker and keeps the second at exit 1; a directory a ledger row anchors into is kept either way | `skills/settle/scripts/settle.py#survey@1f842a04`, `skills/settle/scripts/settle.py#retire@393f6fad`, `skills/settle/scripts/settle.py#RULE_HEADING@3a1d86a1`, `skills/settle/scripts/settle.py#RULE_KEPT_HEADING@75654443`, `tests/test_settle_reads_before_it_removes.py#test_the_rule_arm_removes_it_with_no_marker@bbcdc975`, `tests/test_settle_reads_before_it_removes.py#test_an_open_memo_row_keeps_a_spec_less_directory@22508000`, `tests/test_settle_reads_before_it_removes.py#test_a_row_anchored_inside_a_rule_arm_directory_keeps_it@6a64feea` | **Executed** 2026-09-23: six cases red first; `./bin/settle` on this branch lists eight to retire and keeps `1788177600` and `1788395377` with four rows; on a scratch clone (A9) `settle --retire` removed exactly those eight and exited 1 | 2026-09-23 | The two headings are a divergence from `plan.md`'s *the new heading*, recorded in `overview.md` |
| S3 · for every directory a retirement would take, by either arm, the report lists the open `## Not verified` rows, the paths outside `seal/specs/` citing into it (a `specs/<id>…/` path, an abbreviation included, never a marker or a bare id), and once for the run the `tests/` files naming `seal/specs` | `skills/settle/scripts/settle.py#citations@f2224c61`, `skills/settle/scripts/settle.py#names@2255cf11`, `skills/settle/scripts/settle.py#tracked_text@e7f93bf5`, `tests/test_settle_reads_before_it_removes.py#test_the_report_lists_what_a_retirement_would_take@2ebebc3e` | **Executed** 2026-09-23: red first; mutations dropping the abbreviation, the full id and the `tests/` listing each red. On this tree the scan agrees with an independent `git grep`: the only mentions of the eight directories outside `seal/specs/` are two `CHANGELOG.md` markers | 2026-09-23 | D1's table: the three things #514's frame found by hand |
| G5 · `unverified_check.py --baseline` names a directory removed whole that held no `spec.md` and nothing open at the merge base as *retired by the rule*, apart from folds, and still refuses an open row or a spec at the base, and a memo removed from a directory that stays | `skills/verify/scripts/unverified_check.py#main@694f8621`, `tests/test_unverified_rows_close.py#test_a_rule_retirement_is_not_this_branchs_deletion@07e4db5c`, `tests/test_unverified_rows_close.py#test_a_spec_at_the_base_is_still_a_deletion@2b91148c`, `tests/test_unverified_rows_close.py#test_a_removed_memo_in_a_directory_that_stays_is_not_a_retirement@b5a9d6aa` | **Executed** 2026-09-23: red first; the predicate not asked, the directory-gone condition dropped and the arm never taken each red. A9: against the clone's parent, `6 overviews · 10 open · 2 closed · 0 unreadable · 1 retired by the rule`, exit 0 | 2026-09-23 |  |
| G5 · `chain_check.py --baseline` prints `retired: by the rule — …` for a declaration whose directory is gone and was retired by the rule at the merge base, and still refuses one with a spec or an open row there, or removed from a directory that stays | `skills/code-review/scripts/chain_check.py#main@47efa0ab`, `tests/test_chain_check_at_the_pull_request.py#test_a_declaration_the_rule_arm_retired_is_not_one_it_made@24741446`, `tests/test_chain_check_at_the_pull_request.py#test_a_spec_at_the_merge_base_is_still_a_missing_declaration@3633c94a`, `tests/test_chain_check_at_the_pull_request.py#test_a_declaration_removed_from_a_directory_that_stays_is_refused@2dd7c120` | **Executed** 2026-09-23: red first; a directory-gone mutation survived until the stays case was added. A9: eight `retired: by the rule` notices, exit 0 | 2026-09-23 | `test_a_deleted_declaration_with_no_marker_is_still_refused`'s fixture gained a `spec.md`: without one its directory is the rule arm's, and G6 accepts that direction |
| G5 · `survivor_check.py` leaves a work item directory the range retired — gone at the right end, and folded or retired by the rule at the left — out of the range on both sides, so a fold owes no `survivors.md` row; a sentence the range removes elsewhere is measured as before | `skills/code-review/scripts/survivor_check.py#retired_directories@6a368229`, `skills/code-review/scripts/survivor_check.py#corrected@695ace00`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_retired_directory_is_not_corrected_wording@e1f6690a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_spec_less_directory_that_stays_is_still_in_the_range@992c64ba` | **Executed** 2026-09-23: red only after the fixture was rebuilt twice — a verbatim copy is one run and scores at most 1.0 against the 1.6 floor, so the standing copies are restatements. A9: `against 0 sentence(s) the range … removed`, exit 0 | 2026-09-24 | The gone-at-`b` question goes through `unverified_check.py#tree_at`, so the module's path-list call sites stay at the count its own case holds **Re-read 2026-09-24 by work item 1790174139 (#507, #308, #460, #304), phase 4**: `corrected`'s first filter is now `records_a_past_state`; the retired-directory filter after it is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790206435 (#543, #307, #439), phase 4**: `corrected`'s list is now narrowed by `records_a_past_state` and a gathered-fragment predicate together, and the retired-directory filter after them is unchanged; the two anchored cases re-run green, and the claim holds. **Re-read 2026-09-24 by the same work item, phase 5 (#551)**: the list is read with `--no-renames`, so a retired directory's files are listed at their old paths whatever git would have paired them with; the retirement test is unchanged, and the claim holds. **Re-read 2026-09-24 in round 1's fix pass (🟡 1)**: the held count added for `CHANGELOG.md` sits after the retired-directory filter and touches no other path; the claim holds. **Re-read 2026-09-24 in round 2's fix pass**: the held count is narrowed to what this range released and may write its wording; it is still `CHANGELOG.md`'s alone and after the filter, and the claim holds. **Re-read 2026-09-24 by work item 1790221963 (#557, #555), phase 4**: the gathered-text filter reads only `seal/specs/<id>/changelog.md` at `a` and acts on `CHANGELOG.md`'s released wording after the retired-directory filter; the retirement test is unchanged, the two anchored cases re-run green, and the claim holds. **Re-read 2026-09-24 in that work item's round 1 fix pass**: the held sentences' shared n-grams are `CHANGELOG.md`'s alone and written after the retired-directory filter; the claim holds. **Re-read 2026-09-24 in that work item's round 2 fix pass**: they are now returned apart from `written` and subtracted in `score` from `CHANGELOG.md` sources alone; the retirement test is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790260564 (#564), phase 1**: the gathered-fragment reader lists the tree at `a` for the paths `a_gathered_fragment` accepts, apart from the path list and after the retired-directory filter; the retirement test is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790260564 (#563), phase 2**: `corrected` pairs sentences across the paths left after the retired-directory filter, so a retired directory's sentences are in neither side to pair; the retirement test is unchanged, the two anchored cases re-run green, and the claim holds. |
| G5 · every reader of a retirement reaches the answer by calling `retired_by_rule` on the module that owns it, and none defines a second one | `tests/test_unverified_rows_close.py#test_every_reader_of_a_retirement_calls_the_one_predicate@ed13f06b`, `tests/test_unverified_rows_close.py#test_the_baseline_arm_asks_the_one_predicate@fd435535`, `tests/test_settle_reads_before_it_removes.py#test_the_predicate_is_what_settle_asks@5e702dba` | **Executed** 2026-09-23: the behavioural halves monkeypatch the predicate for `settle` and `unverified_check.py`; the source half parses `chain_check.py`, `survivor_check.py` and `settle.py` | 2026-09-23 | `plan.md` §*What breaks in six months* |
| G7 · an empty or absent `seal/specs/` under a present `seal/` root is a settled state: `settle` and `settle --retire` exit 0 saying so, and `unverified_check.py` does the same for that root's own `specs` path while any other missing path is still exit 2 | `skills/settle/scripts/settle.py#main@0aa29e22`, `skills/settle/scripts/settle.py#SETTLED@7bed1266`, `skills/verify/scripts/unverified_check.py#settled_root@18330a6b`, `tests/test_settle_reads_before_it_removes.py#test_a_settled_root_is_green_and_says_so@5a0aafb7`, `tests/test_unverified_rows_close.py#test_the_roots_own_specs_path_is_settled_when_nothing_is_under_it@a2292a34`, `tests/test_unverified_rows_close.py#test_any_other_missing_path_is_still_a_typo@198e010d` | **Executed** 2026-09-23: eight cases red first. A10 on scratch clones: at `3103415` a fresh checkout of the emptied tree gave `settle` exit 2 and `unverified_check.py` exit 2; at `be1579e` every reader exits 0 on the next pull request, empty and absent | 2026-09-23 | A local-mode repository still gets exit 2 from `unverified_check.py`, because `settled_root` requires `<repo>/seal/` to exist |
| G8 · a population floor over the real round records is replaced by an independent listing — a disk walk with `git cat-file -e` per file — that a sweep's corpus has to cover at any size; properties about what a record looks like are asserted over corpora the cases build | `tests/conftest.py#committed_round_records_on_disk@6d9566c9`, `tests/test_chain_check_at_the_pull_request.py#_the_walk_found_every_committed_record@d51bb736`, `tests/test_chain_check_at_the_pull_request.py#test_the_walk_guard_names_what_either_route_missed@8e21029f` | **Executed** 2026-09-23: each re-pointed floor red with its defect planted (eleven plants); on an emptied scratch clone the 44 modules naming `seal/specs` failed 10 (empty) and 13 (absent) at `3103415` and passed at `be1579e` | 2026-09-23 | Per path rather than a listing, so `tests/test_a_shrunken_corpus_declines_to_judge.py` needs no new classification. G8 was one short: `test_this_repositorys_own_overviews_are_all_readable`, answered by `settled_root` |
| D1 · a fold is not a work item — no directory, no routing question, the no-review token in front of every commit, judged at its pull request — and it changes `seal/ledger.md` only by removal and re-verification | `` skills/settle/SKILL.md#"## A fold is not a work item"@1aaa7ffe ``, `` skills/settle/SKILL.md#"## What a fold branch owes"@39938f1b ``, `tests/test_settle_reads_before_it_removes.py#test_the_skill_says_a_fold_is_not_a_work_item_and_owes_no_range_row@55f71354`, `tests/test_settle_reads_before_it_removes.py#test_the_skill_says_what_a_fold_does_to_the_ledger@f38e9422` | **Executed** 2026-09-23: both cases red with the skill stashed | 2026-09-23 | `questions.md` Q3's default replaces *Nothing in `seal/ledger.md` moves*; Q1, whether a fold travels as a release at all, is open **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** `seal/ledger.md changes only by removal and re-verification` stands verbatim, followed by one sentence extending it to the release files, so the claim holds |

<!-- specs/1790154759-the-review-arm-asks-where-no-reviewer-compares -->
### 1790154759-the-review-arm-asks-where-no-reviewer-compares

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

One row: the review arm's half of the line `seal/ledger.md`'s S11 already
claims for the parity arm. S11's anchor on `DOC_ROOTS` did not move — the
comment above the constant is outside the unit — so it was left as it is. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| R1 · the commit gate's review arm reads no paths: in a repository that opted in, a commit confined to `docs/` and `seal/`, with no declaration, no review mark and no waiver, is stopped exactly as a code change is, and in a migration repository the same commit wakes the review arm alone. `DOC_ROOTS` and `touches_code` are the parity arm's, and the decision table and the wake/quiet table say so | `hooks/commit-review-gate.py#judge@189da4a0`, `hooks/commit-review-gate.py#touches_code@8ec7f7aa`, `tests/test_chain_hooks_hardening.py#test_the_review_arm_asks_on_a_document_only_commit@8fc4d4f1`, `tests/test_chain_hooks_hardening.py#test_a_document_only_commit_wakes_one_arm_and_not_two@c9e6db4a`, `tests/test_chain_hooks_hardening.py#test_the_review_arms_missing_path_line_is_written_where_it_is_met@abbdccee` | **Executed** 2026-09-23. With `and touches_code(cwd, invocations)` added to the review arm's condition, both behaviour cases fail; with it removed from the parity arm's condition, the migration-repository case fails beside the existing `test_parity_gate_ignores_document_only_commits`. The prose case fails on its own when the `docs/` row, the grounds paragraph, the declaration row or the wake cell's added clause is deleted. Each mutation was restored from a saved copy. **Corrected 2026-09-23 in round 1's fix pass**: this cell claimed the `seal/` half as executed when both behaviour cases staged a `docs/` file only, and it gave the gate modules as 139 cases. Both cases now take one path per root. An exemption in the review arm for a commit confined to `seal/` alone fails the two `seal/ledger.md` parameters and passes the `docs/` ones; the same exemption for `docs/` alone fails the two `docs/` parameters. The prose case fails with the paragraph's rule sentence deleted. `tests/test_chain_hooks_hardening.py` with `tests/test_gate_judges_the_repo_it_commits_to.py` then passed, 142 cases | 2026-09-24 | **The line is kept off on purpose, and the measurement is the reason** (`spec.md` M1–M4, `docs/review-chain-spec.md` §*Review arm*). A lighter tier for a documentation pass is the `straight to the PR` declaration, chosen before the first edit; it is never inferred from paths **Re-read 2026-09-23 by work item 1790173106 (#241), which drifted `judge`.** Option 1 of the review arm's prompt now says `straight to the PR` turns off the reviewer alone and that CI still requires the sealer's `broad-gate.md` of it; no decision, path or hint in the unit moved, so the claim holds. Re-read 2026-09-24 — #526's split re-pointed a citation inside the anchored unit to the document that now holds the section; the claim unchanged. Re-read 2026-09-24 — #526's split re-pointed the anchored case to the document that now holds the section it reads, and widened any absence it asserts of the spec to all three documents; the claim unchanged. |

<!-- specs/1790154760-the-ledger-grows-and-nothing-takes-a-row-out -->
### 1790154760-the-ledger-grows-and-nothing-takes-a-row-out

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Two rows: C1 is the memo the work item was opened for, and C2 is the cost
sentence it corrects. Three shared rows the edit drifted were re-read rather
than replaced, and carry a `Re-read 2026-09-23` marker in `seal/ledger.md`:
the `py_spans` row (anchors resolve through `ast`), and the two rows citing
`scan_candidates`, whose docstring alone changed. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| C1 · the checker parses each distinct Python text at most once per process: `py_spans` is memoised on the file's TEXT and never on its path, so an edited file is never served the spans of the text before it, and every caller gets a fresh dict of fresh lists, so no caller can change the answer the next one gets. Every finding the checker prints is unchanged | `skills/evidence-check/scripts/evidence_check.py#py_spans@c45c4942`, `skills/evidence-check/scripts/evidence_check.py#parsed_spans@2d6ca94d`, `tests/test_a_row_points_by_content.py#test_rows_citing_one_file_cost_one_parse@04669ff8`, `tests/test_a_row_points_by_content.py#test_a_file_edited_between_two_reads_gets_its_new_spans@80e10446`, `tests/test_a_row_points_by_content.py#test_changing_the_returned_spans_does_not_reach_the_next_caller@4b2c447c` | **Executed** 2026-09-23. The parse-count case was red on the checker as it stood at `f8f1c9de` (`10 parses of one file for five rows`) and green after. Each of the other two was red against a mutant of its unit: the memo keyed on `len(text)`, `py_spans` returning the stored dict, and `py_spans` copying the dict but not its lists. The old and the new checker, run on the same tree, printed byte-identical stdout and stderr for `--strict .` and identical findings for every row of every ledger plus a perturbed copy of `seal/ledger.md` (3,058 lines, 143 BROKEN and 234 DRIFTED). **Read**: no caller mutates what `py_spans` returns (`check_text`, `reverify`, `rider_check.py#region_lines` and `#inferred_anchor`, `file_units`) | 2026-09-23 | `--strict .` on this repository went from 16.3 s to 2.0 s. The one output that changed is on stderr: a file whose source raises a `SyntaxWarning` warns once per parse, so the BROKEN path's scan printed it 143 times before and once after. `phases/phase-1.md` of the work item holds the measurements and the residual profile |
| C2 · the advisor's module docstring states what the hook costs per commit with the instrument, the date and the platform, and what it cost before #519, in place of an undated "about 114 ms" | `hooks/evidence-advisor.py#"by somebody remembering to run a command. The checker makes no git calls, and"@7cfca84d` | **Executed** 2026-09-23: `/usr/bin/time -p python3 hooks/evidence-advisor.py` fed a `git commit` payload for this worktree, three runs each, 15.16 / 15.39 / 15.44 s before the memo and 1.77 / 1.74 / 1.73 s after, stdout empty in all six. **Read**: the other two places stating "114 ms", `evidence_check.py#scan_candidates`'s docstring and the docstring of `test_past_the_file_cap_the_scan_degrades_and_says_so`, now state the property without a number. **Corrected 2026-09-23** in round 1's fix pass: "the other two places" counted only the literal "114 ms". Enumerated by what they say, a stated cost of the check or its hooks, there were two more, `hooks/ledger-migrate.py` ("~130 ms" for the checker's full run, "~24 ms" and "~60 ms" for its own scan and group) and `skills/evidence-check/SKILL.md` ("~110 ms" and "~36 ms" for the rename scan); C3 holds them | 2026-09-23 | The "4.6 s at four times the rows" in the sentence is the framing measurement in the work item's `spec.md` §2, not re-taken here |
| C3 · every other place that states what the check or one of its hooks costs names the instrument, the date and the platform: `hooks/ledger-migrate.py`'s silent-path bullet and the rename-scan bullet in `skills/evidence-check/SKILL.md` | `hooks/ledger-migrate.py#"  - **silent when there is nothing to migrate** — measured 2026-09-23 on"@05176b04`, `skills/evidence-check/SKILL.md#"  degrades to the row's own file and says so on the line. Measured"@81c52dec` | **Executed** 2026-09-23, Python 3.12.11 on macOS, median of five: the ledger-migrate scan in process (checker load plus `old_format_rows` over the three ledger files) 157–189 ms; `hooks/ledger-migrate.py` wall 192–206 ms and `dispatch.py session-start` wall 291–307 ms with a SessionStart payload, both silent; the CLI on one BROKEN row with 199 one-line candidate files plus its own 68–82 ms, past the cap 65–78 ms, an empty ledger 63–84 ms. **Read**: `grep` over `hooks/`, `skills/`, `docs/`, `README*.md` and `CONTRIBUTING.md` for a number of ms or seconds near evidence, checker, ledger, advisor, hook or migrate | 2026-09-23 | Enumerated and left: `hooks/dispatch.py`'s "a bare interpreter is ~92ms and each gate ~110-180ms" is the start-up cost of a gate process, which this work did not change and which says whose machine measured it; the rider numbers in `evidence_check.py` near `ANCHOR_RE` time a regex and carry their own reading; `skills/verify/scripts/session_cost.py`'s figures are about sessions, not the check. The old "~24 ms" scan was taken on a smaller ledger: the scan reads every byte, and the shared file is 1.2 MB now **Corrected 2026-09-23** by round 2, whose grep reached two places this enumeration missed: `README.md`'s and `README.ko.md`'s "220ms → 104ms before a Bash call, 323ms → 120ms after" is the gate group's start-up cost per Bash call, which this work did not change, and the advisor in the post-bash group returns before loading the checker on a call that is not a commit; left on the dispatch.py grounds. `seal/ledger.md`'s `scan_candidates` row states 110 ms / 36 ms / 130 ms for its 2026-09-10 fixture, dated by its `Checked` cell and already re-read by this work item. |

<!-- specs/1790154761-folded-statements-pile-into-one-spec -->
### 1790154761-folded-statements-pile-into-one-spec

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Three rows, one per rule the next fold meets: E1 the editions are paired, S1
a folded statement names what enforces it, P1 a document over the ceiling
takes no new fold. Nothing in `seal/ledger.md` moved, because nothing
anchored there was removed. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| E1 · every top-level `docs/X.ko.md` beside a `docs/X.md` has the same sequence of live-heading levels, and under each heading position the same multiset of fold-marker ids; `CONTRIBUTING.md` owns the rule and names the check; the Korean one-root edition carries the section and ten markers it lacked | `tests/test_both_editions_carry_the_same_folds.py#outline@950b0cd6`, `tests/test_both_editions_carry_the_same_folds.py#disagreements@d1792e2e`, `tests/test_both_editions_carry_the_same_folds.py#HEADING@89453dac`, `CONTRIBUTING.md#"## House rules">"heading levels and, under each heading position, the same fold markers. A"@fe81ef3b`, `docs/one-root-by-lifetime.ko.md#"## 저장소가 스스로 정하는 것, 그리고 그것을 읽는 방법"@9aeb18ea` | **Executed** 2026-09-23. At the base the real-tree case was red on the heading levels (23 positions against 22); with the Korean heading added and no markers yet it was red on the missing ids position by position; green with the ten markers placed. Mutations, one at a time: a Korean marker moved to another section, the per-position comparison off, the level comparison off, the heading pattern without its space — each red. The guide pin was red with `CONTRIBUTING.md` at `HEAD`. **Corrected 2026-09-23 in round 1's fix pass**: a heading indented one to three spaces is read, as CommonMark reads it, and four spaces is not; the case was red on the old pattern. **Corrected 2026-09-23 in round 2's fix pass**: the hashes may be followed by a space, a tab or the end of the line, so `##<tab>A` and a bare `##` are headings; the case was red on the space-only pattern, and restoring it reds it again | 2026-09-23 | Markers are read with `unverified_check.py#live_lines` and `#FOLD_MARKER`, so the check counts exactly what the fold counts. The two READMEs are outside `docs/` and not read by it |
| S1 · a fold marker whose work-item id is at or above `SHAPE_CUTOFF` heads a statement that opens bold and carries exactly one live `Enforced by:` line, whose targets each resolve to a file inside the repository (normalised, so `.`, a directory and `../x` are refused) and, with `::name`, to a `def` or `class` found by `ast`, or which says `nothing — <why>` with a reason; stacked markers share one statement, which ends at the next marker, heading or end of file; settle §2 owns the rule | `tests/test_a_folded_statement_names_what_enforces_it.py#SHAPE_CUTOFF@dd68a93b`, `tests/test_a_folded_statement_names_what_enforces_it.py#statements@834d3ec9`, `tests/test_a_folded_statement_names_what_enforces_it.py#HEADING@5040ce15`, `tests/test_a_folded_statement_names_what_enforces_it.py#BOLD_OPENING@dc7810e2`, `tests/test_a_folded_statement_names_what_enforces_it.py#target_problem@394a1779`, `tests/test_a_folded_statement_names_what_enforces_it.py#shape_problems@e11742aa`, `skills/settle/SKILL.md#"### 2. Write one standing statement per segment">"**A standing statement has one shape: the rule, its grounds, and what enforces"@fbcf0ce5` | **Executed** 2026-09-23. Ten mutations of the reader, one at a time, each red: bold check off, count relaxed, targets not resolved, `::name` lookup off, empty reason accepted, cutoff lowered to 0 (the real `docs/` goes red), cutoff raised, stacking off, headings not ending a statement, live filter off. The settle pin was red with settle at `HEAD`. **Corrected 2026-09-23 in round 1's fix pass**: before it, a target of `.`, a directory or `../x` resolved (round 1, finding 2, executed by the reviewer); the new case was red on the old unit, and turning off the containment check or the file check each reds it again. A first line of bare `**` no longer counts as bold, red first the same way. **Corrected 2026-09-23 in round 2's fix pass**: `***Rule.***` opens bold and bare `***` does not; a heading after a tab or with no text ends a statement; both sides of the containment test go through `realpath`, so a symlink inside the root that opens a file outside it is refused. Each case was red on the old unit and red again under its mutation. **Read**: `docs/` holds no marker at or above the cutoff yet, so the real-tree case binds none and the fixtures carry the bound side | 2026-09-23 | What it cannot read is whether a target really enforces the rule, or whether two statements contradict; settle §2 says both |
| P1 · a top-level `docs/*.md` outside `OVER_CEILING` stays at or under `LINE_CEILING` lines; a listed one must still exceed it, must exist, and must carry exactly its frozen marker count and, at that count, exactly the marker ids whose digest `FROZEN_IDS_DIGEST` holds, so a fold that swaps a statement in and another out is refused; settle §2 owns the placement rule and `docs/the-evidence-ledger.md` states the three values, pinned against the constants | `tests/test_a_document_has_room_for_the_next_fold.py#LINE_CEILING@122ed4fe`, `tests/test_a_document_has_room_for_the_next_fold.py#OVER_CEILING@9510ca40`, `tests/test_a_document_has_room_for_the_next_fold.py#ceiling_problems@0f822e8e`, `tests/test_a_document_has_room_for_the_next_fold.py#FROZEN_IDS_DIGEST@b1c9b377`, `tests/test_a_document_has_room_for_the_next_fold.py#marker_digest@5e2955d0`, `skills/settle/SKILL.md#"### 2. Write one standing statement per segment">"**One subject, one document, and a document over its ceiling takes no new"@ab3c0c05`, `docs/the-evidence-ledger.md#"## The fold, and what tells it from a deletion">"over that ceiling takes no new statement.** Two folds had put 29 of the 101"@9566c4e3` | **Executed** 2026-09-23. Red: the evidence ledger at `HEAD`, each of its four stated values edited by one digit, settle without the placement rule, and five reader mutations (ceiling off, growth-only comparison, outlived entry, missing listed file, live filter off). The real tree is green: `docs/review-chain-spec.md` at 2,159 lines and 29 markers. **Corrected 2026-09-23 in round 1's fix pass**: the count alone passed a swap (round 1, finding 1, executed by the reviewer). The digest over the 29 sorted ids is `8f8c4d85f213`, computed here and matching the reviewer's figure; the swap case was red before the unit existed, and red again with the digest comparison off or with one id dropped from the digest. **Corrected 2026-09-23 in round 2's fix pass**: after a marker is removed on purpose, both messages tell the folder to recompute `FROZEN_IDS_DIGEST` with `marker_digest()`, and the digest message no longer reports a swap as fact (round 2, finding 3); both cases were red on the old messages | 2026-09-24 | The count is compared for equality, so a removed marker lowers the frozen count rather than leaving room to refill. Home of the entry: MichaelYcJo/SpecSeal#526. Re-read 2026-09-24 — #526's split emptied `OVER_CEILING` and `FROZEN_IDS_DIGEST` and rewrote the evidence ledger's bullet to say no document is listed; the claim holds with the listing empty. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the values paragraph became three folded statements, so the minor anchor moved to the ceiling statement's sentence; the document still states the cutoff, the ceiling and the empty list, and the constants case still pins them, so the claim holds. |
