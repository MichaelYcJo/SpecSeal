## 0.15.4 — 2026-09-25

<!-- specs/1790297083-the-release-job-goes-red-on-a-record-it-need-not-judge -->
### 1790297083-the-release-job-goes-red-on-a-record-it-need-not-judge

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A1 · `added_on_branch` passes `--full-history --topo-order` to its `git log --diff-filter=A`, so its first line is the LATEST add across a merge and under a skewed clock; a merge is never itself the add | `skills/code-review/scripts/chain_check.py#added_on_branch@de3ddaed`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_re_add_merged_back_from_a_side_branch_is_the_latest_add@c3ac2d4e`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_re_add_on_a_side_branch_with_an_older_clock_is_the_latest_add@40195b0a`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_added_on_a_merged_side_branch_is_read_at_its_own_add@bd88ccb2` | **Executed** 2026-09-25: the merged re-add and the older-clock cases red on the pre-phase line (exit 0, the early add taken); the older-clock case red with `--full-history` alone while the merged re-add is green there; the side-branch add case green on both and red under an added `--first-parent`, which names the merge | 2026-09-25 | #529. Two members of one class: history simplification follows one parent of a merge that matches it at the path, and the date order lists the early add first when the side's clock is behind. The test clock puts every commit in one second, which is a date tie, so the merged re-add case dates its side branch ahead to isolate the first member (`phases/phase-1.md`) |
| A2 · `restored_from(root, fork, rel)` names a commit in the merge base's history that carries `rel`'s current bytes (HEAD's, or the working tree's under `--worktree`) at `rel`, found by `git log --topo-order --find-object=<blob> <fork> -- <rel>` and taken as the last line; None when `fork` is None or git fails | `skills/code-review/scripts/chain_check.py#restored_from@3abcc872`, `tests/test_chain_check_at_the_pull_request.py#test_a_record_restored_from_the_bases_history_makes_no_reachability_claim@26b2ff42`, `tests/test_chain_check_at_the_pull_request.py#test_a_restored_record_the_pull_request_then_edits_is_its_own_claim@b05ef81e`, `tests/test_chain_check_at_the_pull_request.py#test_bytes_the_base_held_only_on_a_merged_side_line_are_found@93c3b512`, `tests/test_chain_check_at_the_pull_request.py#test_the_named_commit_is_where_the_bytes_entered_whatever_the_clock@40679114`, `tests/test_chain_check_at_the_pull_request.py#test_a_restore_not_yet_committed_is_read_from_the_working_tree@8202e656` | **Executed** 2026-09-25, one mutation at a time: the predicate answering None (six cases red), the working-tree branch reading HEAD (the uncommitted-restore case), the first line named instead of the last (three), no `--topo-order` (the clock case), and `--find-object` dropped so any commit touching the path counts (the edited-after-restore case) | 2026-09-25 | #598 instance 1. **No `--full-history`, by measurement**: every case stayed green without it, and a probe showed `--find-object` lists the commits of a side line that changed the record and changed it back before a `--no-ff` merge, which a plain path-limited `git log` simplifies away. `--topo-order` matters only where the retirement is a side line merged back and the add's clock ran ahead; in date order the line then names the retiring commit, which does not carry the bytes |
| A3 · a restored record is not the pull request's claim in the two arms keyed on its own add: `main` sets `refs` to None for a restored last record and prints that it was restored from the named commit, and `written_late` returns nothing for one after finding its fixes and before reading its add. Everything else is still read on it | `skills/code-review/scripts/chain_check.py#main@2bb97fac`, `skills/code-review/scripts/chain_check.py#written_late@29a5295d`, `tests/test_chain_check_at_the_pull_request.py#test_a_restored_record_is_still_read_for_everything_else@8921d85f`, `tests/test_a_record_precedes_the_fixes_it_commissions.py#test_a_record_restored_from_the_bases_history_makes_no_claim@3e287475` | **Executed** 2026-09-25: the reachability case and the `written_late` case red on the pre-phase code (`not an ancestor of`; `this record was ADDED by <restoring commit>`). `main` not asking the predicate turns five cases red; `written_late` not asking it turns its own case red | 2026-09-25 | `questions.md` Q2: a `fork` argument, one definition, two call sites in `main`. Measured against PR #597 in framing: all four records it restored are found at `a211a5d7` (`spec.md` Scope 2) |
| A4 · in a draft pull request `checked_by` prints `Pass` beside `nobody — <why>` on the last record of a work item at or after `STRICT_FROM`, naming the verifying round and saying *Ready for review* re-runs the check and fails if the cell still says `nobody`; at ready and at an unreadable state it fails as before, and no other refusal in the row reads `strict` | `skills/code-review/scripts/chain_check.py#checked_by@7fa1deb4`, `skills/code-review/scripts/chain_check.py#main@2bb97fac`, `tests/test_the_last_rounds_fixes_are_checked.py#test_pass_beside_nobody_prints_on_a_draft_and_names_what_re_arms_it@7217e63a`, `tests/test_the_last_rounds_fixes_are_checked.py#test_pass_beside_nobody_still_fails_a_ready_pull_request@a4898234`, `tests/test_the_last_rounds_fixes_are_checked.py#test_a_draft_is_excused_the_pass_half_and_no_vocabulary_refusal@04227905` | **Executed** 2026-09-25: the draft case red on the pre-phase code. The draft arm firing at ready too turns the two ready-side cases red; `main` not passing `strict` turns the draft case red; a draft excusing every refusal in the row turns five cases red | 2026-09-25 | #598 instance 4. `round_record.py#run_check` tells the check `draft` unless `gh` says the pull request is ready, so `close` in the window now prints this notice and exits 0; `tests/test_the_fixes_close_the_record.py::test_pass_is_ticked_when_nothing_is_open_and_the_gate_is_the_flag` asserts that (`phases/phase-3.md`) |
| A5 · `skills/code-review/orchestration.md` says the window from `close` ticking `Pass` until the verifying round's record commits is not red on a draft, and that *Ready for review* fails the pull request if the cell still says `nobody` | `skills/code-review/orchestration.md#"## Orchestrator: the pull request opens before round 1, and a phase is re-run"@3fbfe924`, `tests/test_the_rules_have_one_owner.py#test_the_release_leg_is_not_red_until_the_verifying_rounds_record_commits@79d92f2d` | **Executed** 2026-09-25: the pin red with the sentence deleted | 2026-09-25 | Replaces *"It is red once more … and that window is expected"*, whose pin was renamed with it |

<!-- specs/1790297084-the-sweep-reads-removed-and-moved-text-as-a-correction -->
### 1790297084-the-sweep-reads-removed-and-moved-text-as-a-correction

| P1 · where one key left two paths and arrived at a third, the arrival pairs with the departure whose path shares the most keys with its own (the distinct keys that left the one and arrived at the other), then with the one whose path is gone at the range's tip, then with the first in path order; so a move pairs with its own origin, and the report's `corrected` line names the correction and never the path that only moved, with the places and scores unchanged | `skills/code-review/scripts/survivor_check.py#paired_across_paths@53cabf4b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_move_pairs_with_its_own_origin_and_the_correction_is_named@178d519a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_split_pairs_with_its_own_origin_while_both_files_remain@9c499a46`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_path_order_does_not_decide_which_departure_is_the_source@9e1642f2`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_one_sentence_move_pairs_with_the_path_gone_at_the_tip@26faea18` | **Executed** 2026-09-25. At `2e0e2fa7` the whole-file move, the split and the one-sentence move each named the moved file, `docs/m.md`, as the correction; the case where the moved file sorts first was green there by design. Green after. Mutated from Python, the file restored from a copy each time: affinity out of the order, the split red; gone-at-`b` out, or the paths at `b` not passed, the one-sentence move red; the order reduced to reversed path order, the sorts-first case red; the one-for-one pop replaced by pairing whole paths, M6, M7 and all four red | 2026-09-25 | #592. Counted per path rather than per sentence pair, so a fold moving hundreds of rows that share a date cell costs one entry per path pair. The verdict and score cannot move, because the key and so its n-grams are the same for every departure it could pair with. `RELEASE_RANGES`' four pinned ranges report the same coordinates at the phase's commit |
| P2 · a work item directory the range retired is read at the range's left end, and its sentences take part in the pairing across paths before they leave the removed wording; so a sentence a fold carries verbatim from the retired `spec.md` into `docs/` pairs with its own origin and is held, and a correction the same range made elsewhere stays a source and its other copies are reported | `skills/code-review/scripts/survivor_check.py#corrected@e3c7e3f3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_verbatim_fold_hides_no_correction_the_same_range_made@739e30ce`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_retired_directory_is_not_corrected_wording@e1f6690a` | **Executed** 2026-09-25. The fold case red at `4665def0` (phase 1's tip, whose retirement handling is the base's): exit 0, `against 0 sentence(s)`. Green after. Mutated from Python, the file restored from a copy each time: the retired paths dropped before the pairing again, the fold case red; the drop after the pairing removed, the fold case and #517's A8 red. #581's real fold, `f673e3b1..8e13036c` and its squash `3c3f2342^..3c3f2342`, at `2e0e2fa7` and at `4910e445`: exit 0, 350 files, 44 removed, 0 places under both | 2026-09-25 | #591. The fold case's tie is one shared key each way, so the pairing's gone-at-`b` rank (P1) is what sends the arrival to the retired `spec.md`: a retired directory is always gone at the tip. Failure direction: toward reporting, because retired departures can only take pairings away from live ones, which keeps those as sources and their arrivals out of `written` **Re-read 2026-09-25 by work item 1790297084 (#603), phase 3**: after the pairing across paths `corrected` now also drops the sentences of a ledger row the range removed because an anchor of the row left the code (`removed_ledger_rows`), beside the retired-directory drop; nothing this row claims reads a removed ledger row, and the claim holds |
| P3 · after the pairing across paths, the sweep drops from the removed wording the sentences of a ledger row the range removed, where the row was a live table row of a ledger file at the range's left end, its line is not in that file at the tip, at least one of its anchors resolves at the left end and not at the tip (`evidence_check.py#resolve_unit`, loaded by path), and no live row of that file at the tip cites every anchor of it that still resolves there and no live row under the same heading at the tip carries its row id, counted -- an id the tip carries fewer times under that heading than the left end did names no row; so a row removed because its code was is no correction, and a row corrected in place stays measured where its id stands under the same heading or a live row cites every anchor of it that still resolves, as does a row quoted in a fence or comment or removed with every anchor standing | `skills/code-review/scripts/survivor_check.py#removed_ledger_rows@8f9647b0`, `skills/code-review/scripts/survivor_check.py#corrected@e3c7e3f3`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_removed_with_its_anchor_is_not_a_correction@4fc0057a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_corrected_in_place_is_still_a_correction@4b149e9f`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_removed_while_its_anchors_resolve_stays_measured@e2e14d33`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_one_anchor_leaving_a_file_that_remains_is_enough@e4b7abb1`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_quoted_in_a_fence_or_a_comment_stays_measured@0272a0d8`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_rows_claim_carried_to_a_new_row_is_held_not_written@df409bbe`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_corrected_in_place_while_an_anchor_is_renamed_is_a_correction@87f69a04`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_row_sharing_one_live_anchor_with_another_row_takes_the_exit@c4492e1f`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_whose_anchor_never_resolved_here_stays_measured@f0171ead`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_whose_line_still_stands_takes_no_exit@511e6863`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_rows_cells_are_the_lines_the_row_stood_on@08b450cb`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_measured_range_that_removed_three_rows_reports_nothing@318c0eb4`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_corrected_in_place_while_its_only_anchor_is_renamed_is_a_correction@c2511049`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_row_corrected_in_place_while_its_heading_is_retitled_is_a_correction@9d95d59e`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_removed_rows_id_standing_in_another_section_is_another_row@87c535ba`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_hyphenated_id_corrected_while_its_anchor_is_renamed_is_a_correction@e0ce41da` | **Executed** 2026-09-25. At `4910e445` the removed-row case and the one-anchor case exited 1, and `58629718^..58629718` exited 1 with 8 places against 98 removed sentences; the re-pointed case exited 0 at `afb03a4c`, before the still-standing condition existed. Green after; `58629718` exits 0 against 52. Mutated from Python, the file restored from a copy each time, thirteen mutations over the sixteen cases, each red at least once: every removed row line exiting; the standing-line test, the live-line test, either half of the anchor condition, the still-standing test, "every" read as "any", the empty-set guard, a 0-based line count, the drop moved before the pairing, the releases shape, and the refusal each taken out | 2026-09-25 | #603. The still-standing condition was not in `plan.md`: Q2 found it (`phases/phase-3.md`). On #589's squash `seal/releases/0.15.1.md` R1 was corrected in place while the same commit renamed one of its tests, so its corrected claim took the exit under the plan's three conditions. It only keeps rows measured, so it moves toward reporting. What stays silent: a row retracted as false in a range that also removes one of its anchors, which the ledger's rules route to correction in place instead (`plan.md` §*What breaks in six months*) **Corrected 2026-09-25 in round 1's fix pass (🟡 1, ⬜ 4)**: the claim said a row with no anchor still resolving at the tip always takes the exit, so a row corrected in place and re-pointed at a renamed unit or a retitled heading went silent. A removed row whose id (`R1 ·`) still stands under the same heading at the tip is now that row and stays measured, and the claim says so. `test_a_row_corrected_in_place_while_its_only_anchor_is_renamed_is_a_correction` and `test_a_row_corrected_in_place_while_its_heading_is_retitled_is_a_correction` red at `8f70ca94` (exit 0), green after; `test_a_removed_rows_id_standing_in_another_section_is_another_row` red with the heading left out of the id's key **Corrected 2026-09-25 in round 2's fix pass (🟡 1, 🟡 2, ⬜ 5)**: the claim read an id standing at the tip as naming the row, and a claim split across rows repeats its id in one section (`seal/releases/0.14.0.md` G5 is four rows), so a removed row of such a group stayed measured through a sibling; the id now counts, and a key standing fewer times at the tip sends its rows to the anchor rule. `ROW_ID` reads `P1-1 ·` as well. The closing clause said a corrected row always stays measured, and it now names the condition as built; `removed_ledger_rows`' docstring states the silent set. `test_a_removed_row_sharing_one_live_anchor_with_another_row_takes_the_exit` (its neighbour back to `R1`) and the hyphenated-id case red at `66df04df`, green after; each red again with its half of the fix mutated out **Re-read 2026-09-26 in work item 1790381329 (#615):** `removed_ledger_rows`' docstring now states the silent set as both rules failing together and says *at least as many rows* for the id's count, and the anchored neighbour case's docstring says the same; no code changed, and the claim, which already says an id carried fewer times names no row, holds. The count guard's direction now has its own case, C4 in `seal/ledger/1790381329-the-deferred-sentences-and-pins.md` |
| P4 · the sweep's four ledger locations, `LEDGER_SHAPES`, are the tails of `evidence_check.py#default_patterns` in shared mode, and a copy of the sweep without `evidence_check.py` beside it refuses with exit 2 and a sentence naming the file, rather than reading every removed row as a correction | `skills/code-review/scripts/survivor_check.py#LEDGER_SHAPES@ff90ea38`, `skills/code-review/scripts/survivor_check.py#evidence@e8efd32b`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_ledger_shapes_are_the_ones_the_ledger_checker_reads@9c20f2d6`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_missing_ledger_checker_refuses_rather_than_measuring@39a87e69` | **Executed** 2026-09-25. Both cases red before the units existed (AttributeError at `4910e445`). Mutated: the releases shape dropped, the shapes case and the real-range case red; the missing-file test disabled, the refusal case red | 2026-09-25 | Loaded only when the range's left end holds a ledger file, as `retired_directories` loads its reader only when it has a candidate. Work item E of this release edits `evidence_check.py`; this work reads `ANCHOR_RE`, `resolve_unit` and `default_patterns` and edits nothing there |

<!-- specs/1790297085-settle-retires-a-directory-main-has-not-seen-closed -->
### 1790297085-settle-retires-a-directory-main-has-not-seen-closed

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| M1 · a shipped script copied without the sibling it loads by path exits 2 with one stderr sentence naming the missing path and what that file is for, and no traceback: `fold_check.py`, `settle.py` and `round_record.py` refuse in their loaders, before anything is read, and `chain_check.py` already did in `main`; `settle.py` and `round_record.py` look each sibling's purpose up by path, so `hooks/optin.py` is named as what finds the `seal/` root and not as the fold record's reader | `skills/settle/scripts/fold_check.py#load@7bfcabf5`, `skills/settle/scripts/settle.py#load@cb0e9197`, `skills/settle/scripts/settle.py#PURPOSES@cfe9eac9`, `skills/code-review/scripts/round_record.py#load@4c6d397b`, `skills/code-review/scripts/round_record.py#PURPOSES@bfe7848a`, `tests/test_a_script_copied_alone_exits_2.py#test_a_script_copied_alone_exits_2_and_names_what_it_misses@2d6dfea8`, `tests/test_settle_reads_before_it_removes.py#test_a_missing_sibling_reader_is_a_sentence_and_not_a_traceback@6596501d` | **Executed** 2026-09-25. The class case, four scripts each copied alone into an empty directory and run as a subprocess, was red for three of them at `1880cf92` (exit 1: two sentences and one `FileNotFoundError` traceback) and green for `chain_check.py`; green for all four at `3c1be03a`. Mutated at `3c1be03a`, each red: `fold_check.py#load`'s exit back to 1, `settle.py`'s `hooks/optin.py` purpose back to the fold record's, `settle.py#load`'s exit back to 1 (2 red), `round_record.py`'s purpose for `chain_check.py` reworded, `round_record.py#load`'s file check removed | 2026-09-25 | #590. The `# RIDER:` that stood on `round_record.py#load` since 2026-09-08 asked for exactly this and is removed. `chain_check.py` is not edited by this work item; its exit 2 is pinned by the class case alone. **Re-read 2026-09-25 in round 1's fix pass**: CI's windows-latest leg on PR #605 failed the class case for `chain_check.py`, whose refusal quotes the `OSError` and so doubles every backslash of the path; the case now accepts the path as written or as `repr` spells it (`names_path`), and `test_the_path_is_found_as_a_windows_oserror_spells_it` was red against the plain substring test with a Windows-shaped path. The claim holds |
| M2 · `settle` asks the rule arm's predicate, `unverified_check.py#retired_by_rule`, of the working tree and of the merge base of `--released-at` and `HEAD` — the revision the CI readers ask it of while the base has not moved past the fork, and an earlier one after, since on a pull request they stand on the merge ref and ask at the base's tip; a released spec-less directory that passes the tree and fails the base is listed under *kept until the closure reaches <base>* with every row open there, or, where the base has moved, under *kept: the closure has not reached <base>*, whose heading says to merge `--released-at` into this branch (**Corrected 2026-09-25 in round 3**: this named the old heading for the moved case), never under *retired by the rule*, and `settle --retire` keeps it at exit 1; a directory with a row open on disk stays under *kept by the rule*; a `--released-at` sharing no commit with `HEAD` is exit 2 in both arms with nothing read, and `retire` handed a survey with no base refuses at 2 by itself | `skills/settle/scripts/settle.py#survey@39d9620a`, `skills/settle/scripts/settle.py#retire@c15f488a`, `skills/settle/scripts/settle.py#main@94cf5673`, `skills/settle/scripts/settle.py#write_rule_kept@df37f57d`, `skills/settle/scripts/settle.py#RULE_BASE_HEADING@918358e4`, `tests/test_settle_reads_before_it_removes.py#test_a_closure_the_base_has_not_seen_keeps_the_directory@c5f0515f`, `tests/test_settle_reads_before_it_removes.py#test_an_uncommitted_closure_keeps_the_directory@803415c6`, `tests/test_settle_reads_before_it_removes.py#test_a_closure_the_base_has_seen_lets_the_directory_go@07fc6e8c`, `tests/test_settle_reads_before_it_removes.py#test_the_report_does_not_promise_what_the_retirement_refuses@c9e7f9c6`, `tests/test_settle_reads_before_it_removes.py#test_no_merge_base_is_refused_and_nothing_is_removed@e9a09a67`, `tests/test_settle_reads_before_it_removes.py#test_the_retirement_refuses_a_survey_with_no_base@d31e7fb4` | **Executed** 2026-09-25. Against the code at `ee3afefc`: S4 and S5 red with the directory removed and exit 0, S7 red with the directory under *retired by the rule*, S8 red at exit 0 and 1, the heading pin red; S6, the counter-case, green before and after. Green at `4f77ecdc`, 118 cases in the module, 111 of them the cases that stood before (none of which encoded a working-tree-only closure as retirable, `questions.md` Q2). Mutated at `4f77ecdc`, each red: `survey` asking the tree where it asks the base, `retire` doing the same, `retire` not counting what it kept there, the report omitting the heading, `main`'s refusal off, `survey` walking on with no base, the rows' text dropped, the heading's remedy dropped; `retire`'s own no-base guard off turned its case red at `cac3341f`. `./bin/settle` on this repository at `4f77ecdc`, read and not `--retire`: exit 0, `3 to retire by the rule, 0 kept by it, 0 kept until the closure reaches origin/main` | 2026-09-26 | #602, the 0.15.3 run's #598 instance 2. The predicate is asked of the tree first, so a directory with a row still open on disk keeps its old heading and remedy. A stale `origin/main` can read differently from CI in either direction, as a moved base can, and the heading names the ref so a reader can see it. **Corrected 2026-09-25 in round 2's fix pass (⬜ 3)**: this said a stale `origin/main` keeps more than it must, never less; a record file the base adds to the directory after the fork is read by CI and not by `settle`, so `settle` can retire a directory CI then refuses. **Corrected 2026-09-25 in round 1's fix pass (🟡 1, ⬜ 2)**: the claim said the merge base of `--released-at` and `HEAD` is the revision both CI readers ask; `.github/workflows/hygiene.yml` checks out the merge ref, so theirs is the base's tip. The claim now says when the two agree, and the moved case's heading, the no-merge-base sentence naming a shallow clone, and `report`'s own no-base refusal are M5 **Re-read 2026-09-26 in work item 1790381329 (#611):** the S7 case, `test_the_report_does_not_promise_what_the_retirement_refuses`, now also pins the summary line `1 kept because the closure has not reached <base>`; it was red with the pre-bc63b91d wording (`kept until the closure reaches`) restored in `settle.py#main`. No code this row cites changed. The claim holds |
| M3 · the skill, the evidence-ledger policy and both README editions say the rule arm is asked at the merge base of `--released-at` and `HEAD`, and that a closure has to reach the branch the release merges to before the directory goes; `RULE_KEPT_HEADING` says the same | `skills/settle/SKILL.md#"### 1. Read what is waiting"@e4d66e42`, `skills/settle/scripts/settle.py#RULE_KEPT_HEADING@259eff1a`, `tests/test_settle_reads_before_it_removes.py#test_the_documents_say_the_closure_has_to_reach_the_base@0152e03e` | **Executed** 2026-09-25: the case red for each of five deletions made one at a time at `4f77ecdc` — the skill's paragraph, the policy's sentence, each README's clause, the kept heading's clause — and green with all of them | 2026-09-26 | The policy statement's `Enforced by:` line names `test_a_closure_the_base_has_not_seen_keeps_the_directory` beside the rule arm's case, and `fold-check`'s real-tree case reads it **Re-read 2026-09-25 in round 2's fix pass of work item 1790297085 (#602):** the skill's paragraph now says CI asks at the base's tip, the same commit as `settle`'s until `--released-at` moves past the fork, and names merging it into this branch after; both README rows say the same, and the case reads the raw README so its row is the row (round 2, ⬜ 5), red with either row's claim reverted. The claim holds **Re-read 2026-09-26 in work item 1790381329 (#611):** the skill's paragraph now names both kept-at-base headings, *kept until the closure reaches <base>* and, where `--released-at` has moved, *kept: the closure has not reached <base>*; the case asserts the second too and was red before the skill edit. The claim holds |
| M4 · `gather_changelog.py --version`, with or without `--dry-run`, refuses at exit 1 an ungathered fragment carrying a line that starts `## ` — exactly the predicate `insert` and `publish_release_note.py#section_body` end a released section with, so a `## ` inside a fence is refused and a `###`, a bare `##`, an indented `## ` and `##` with no space are gathered — before it writes or prints a section, naming each fragment, the 1-based line number in the file and the line, and saying to demote it to `###` or lower in a pull request into the release branch and gather again; a fragment already in the file is not read again | `.github/scripts/gather_changelog.py#main@ed9590ec`, `.github/scripts/gather_changelog.py#section_lines@a204301e`, `.github/scripts/gather_changelog.py#SECTION_LINE@20cfb242`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_fragment_carrying_its_own_section_line_is_refused@4035d43b`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_line_that_ends_no_section_is_gathered@f94c8166`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_section_line_inside_a_fence_is_refused_too@b5e54452`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_gathered_fragment_is_not_refused_again@cbda1f51`, `tests/test_the_changelog_is_gathered_at_release.py#test_the_documents_say_a_fragment_carries_no_section_line@4ae656e7` | **Executed** 2026-09-25. Against the code at `8f6648d0`, with only the cases added: 4 red — S9 in both arms, the fenced line, the documents — and the five S10 lines and the gathered-fragment case green. Green at `127dbe64` with `tests/test_a_release_publishes_its_note.py`, 56 passed. Mutated at `127dbe64`, each red: the predicate widened to `##` (4), the predicate reading stripped lines (1), line numbers from 0 (3), the refusal off (3), every fragment on disk read instead of the ungathered (1), and one deletion each of the docstring's clause, `docs/branch-and-release.md`'s sentence and `CONTRIBUTING.md`'s (1 each). `gather_changelog.py --dry-run --version 0.15.4` on this repository printed the section with this work item's fragment and exited 0 | 2026-09-25 | #586, decided by the owner as option (a). Catching the line at the fragment's own pull request is `questions.md` Q3, not built. `publish_release_note.py` is not edited: a fragment that cannot reach the file cannot reach the note |
| M5 · where `--released-at` has moved past the fork, `settle` keeps a directory whose record is open at the fork under a heading that says the ref has moved and, where the ref already holds the closure, to merge the ref into this branch and run `settle` again, and never that the CI readers ask there; `base_heading` picks that heading when `survey`'s `base_moved` is set and the old one otherwise; the no-merge-base refusal names a clone too shallow to reach the shared commit and `git fetch --unshallow`; `report` refuses a survey with no base at exit 2 with the sentence `retire` prints; and each real sibling's missing file prints its own `PURPOSES` entry | `skills/settle/scripts/settle.py#RULE_MOVED_HEADING@6932f273`, `skills/settle/scripts/settle.py#base_heading@95eb0154`, `skills/settle/scripts/settle.py#NO_BASE_SAYS@ed88ad2d`, `skills/settle/scripts/settle.py#report@32ef6da0`, `skills/settle/scripts/settle.py#main@94cf5673`, `tests/test_settle_reads_before_it_removes.py#test_a_base_that_moved_past_the_fork_names_the_merge_that_moves_it@e237aeff`, `tests/test_settle_reads_before_it_removes.py#test_the_report_refuses_a_survey_with_no_base@6472f5eb`, `tests/test_settle_reads_before_it_removes.py#test_a_shallow_clone_is_not_told_the_refs_share_no_commit@57c9b052`, `tests/test_settle_reads_before_it_removes.py#test_each_sibling_is_refused_with_its_own_purpose@82538e1f` | **Executed** 2026-09-25. Against the code at `32993c08`, with only the cases added: the moved-base case red (the heading lacked the merge), the report case red on `KeyError: 'grouped'`, the shallow case red on the old sentence; the per-sibling case green, pinning what held. Green at `c5e06d26`, 126 in the module. Mutated at `c5e06d26`, each red: `READER`'s and `CHECKER`'s purposes swapped (2), `base_heading` ignoring a moved base (1), `base_moved` never set (1), `report`'s guard off (1), the shallow clause dropped (1) | 2026-09-25 | Round 1's 🟡 1, ⬜ 3, ⬜ 4 and ⬜ 5. `.github/workflows/hygiene.yml` checks out the merge ref with no `ref:`, which is what puts CI's merge base at the base's tip **Re-read 2026-09-25 in round 2's fix pass of work item 1790297085 (#602):** the moved heading now opens *kept: the closure has not reached <base>* rather than waiting for a closure to reach a fixed past commit, the report's summary line says the same, and `main`'s comment no longer claims the fork keeps more and never less; the remedy sentence is unchanged and the claim holds |

<!-- specs/1790297086-the-broad-gate-says-what-ci-says -->
### 1790297086-the-broad-gate-says-what-ci-says

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| S1 · where the shell is `cmd.exe`, a command name has its `/` written `\` only where the part before its first `/` (its `"` and `^` removed, every leading `@` dropped, and any plain `%NAME%` expanded by `as_cmd_expands`: from the gate's environment, with `%CD%` and `%__CD__%` as the directory the row runs in where the environment defines neither, and `$NAME`, `%%` and a substring or substitution left as written) names a directory where the row runs; an empty part is the drive's root and counts as one without asking. One decision per name covers every `/` in it, `CMD_BUILTINS` is still asked first, and every other name — `xcopy/e/i`, `findstr/s`, `timeout/t`, `ipconfig/all` — reaches `cmd.exe` as written. The scan takes the question as a predicate and does no I/O; `handed_to_shell` asks `os.path.isdir(os.path.join(root, part))`, with `None` meaning the current directory, and `run` passes its own `root`, so `compare_at_base` asks its scratch worktree | `skills/verify/scripts/broad_gate.py#command_names_backslashed@b18da1d2`, `skills/verify/scripts/broad_gate.py#handed_to_shell@75af923b`, `skills/verify/scripts/broad_gate.py#run@d70c6fb0`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_switch_against_another_program_reaches_cmd_exe_as_written@474f15aa`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_name_that_starts_in_no_directory_is_handed_over_as_written@ca083523`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_scan_asks_about_a_name_and_reads_nothing_itself@2fec1a85`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_run_judges_the_directory_where_the_row_runs@0466847c` | **Executed** 2026-09-25 on macOS. The four switch rows were red against the frame commit `cf5428b1` (`xcopy\e\i`, `findstr\s`, `timeout\t`, `ipconfig\all`), and the scan there took one argument (`TypeError` for the predicate case). Each unit mutated in turn went red: every name turned (29), the empty part asked (2), `@` kept (6), quotes kept (7), carets kept (3), the decision not reset per name (7), decided at every `/` (9), a quoted `/` ignoring the decision (3), a quoted name's start not set (1), a `^`-opened name's start not set (1), `root` ignored (6), `run` not passing `root` (1). The last two start-not-set mutations survived until two rows with a quoted and an escaped name after a separator were added | 2026-09-25 | **Read, not executed:** what real `cmd.exe` does with a glued switch on a non-built-in. `questions.md` M1 holds it, answered only by CI's `windows-latest` leg **Round 1's fix pass, 2026-09-25** (🟡 3, ⬜ 9). Executed: `%SPECSEAL_PROBE_TREE%/bin/test -q` and `@@bin/test` were red before the fix (handed over as written) and are rewritten after it; `%SPECSEAL_PROBE_NOWHERE%/bin/x` and `xcopy/e/i` stay as written **Round 2's fix pass, 2026-09-25** (🟡 1). `ntpath.expandvars` read only the environment, so `%CD%/bin/test` stayed as written. `as_cmd_expands` replaced it. Executed: `%CD%/bin/test -q` and `%__CD__%bin/test` were handed over as written before the helper, and `$HOME/x` was rewritten with `HOME` naming the tree; after it the first two are rewritten and `$HOME/x` stays as written. Mutated: the `CD` branch, the `__CD__` branch, the environment read, a case-sensitive `CD`, and `expandvars` put back each went red **Corrected 2026-09-26 in work item 1790381329 (#616):** the claim said *any `%NAME%`* and gave `%CD%` the row's directory unconditionally; `as_cmd_expands` asks the environment first, so a defined `CD` wins, and leaves `%NAME:~0,2%` and `%NAME:a=b%` as written. The code did not change; both are now pinned in `test_a_name_rooted_in_a_variable_is_judged_where_the_variable_points` (C5 in `seal/ledger/1790381329-the-deferred-sentences-and-pins.md`), and `handed_to_shell`'s docstring was re-read against the edit |
| S1 · `templates/config.md` §*Broad gate* states the directory rule, that a glued switch on any other program reaches `cmd.exe` as written, that a blank before it works too, and both bounds — a directory made or entered earlier in the row is not seen, and a root directory named like a program makes its glued switch read as a path — and that a plain `%VAR%` in the part is expanded first, `%CD%` as the directory the row runs in unless the environment defines `CD`, and a substring or substitution not at all, and no longer names #596 as open. `handed_line` says a command name that starts in a directory has its `/` handed over as `\`, pinned verbatim | `templates/config.md#"## Broad gate"@51792f91`, `skills/verify/scripts/broad_gate.py#handed_line@8bcead5c`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_the_template_says_which_positions_are_rewritten@f0db6d56`, `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_rewritten_row_is_kept_and_said_beside_the_row_as_written@32a0d966` | **Executed** 2026-09-25: each new template sentence cut in turn and the template case went red (7 of 7), and putting back "Telling the two apart is #596" went red too; the old `handed_line` text put back turned the kept-row case red | 2026-09-25 | **Round 1's fix pass, 2026-09-25** (🟡 3). The `%VAR%` needle went red with its sentence cut **Round 2's fix pass, 2026-09-25** (🟡 1). The `%CD%` needle went red with its clause cut **Corrected 2026-09-26 in work item 1790381329 (#616):** the section's sentence now names only the plain form, the defined-`CD` precedence and the substring bound, and the reader case pins the new words; this claim quoted the old sentence |
| S1 · on `cmd.exe`, `where/q cmd` run through `run(..., shell=True)` from a root with no `where` directory is handed over as written and exits 0 | `tests/test_the_gate_hands_cmd_a_path_it_can_run.py#test_a_switch_against_a_program_runs_on_the_real_platform@7a1f0d4a` | **Unverified** here: skipped on macOS, where the shell is not `cmd.exe` | 2026-09-25 | Only CI's `windows-latest` leg executes it (`questions.md` M1). A red there means a glued switch never ran on `cmd.exe`, and goes back through review as a finding **Round 2's fix pass, 2026-09-25** (M1). The case now fails rather than skips where `os.name == "nt"` and the shell is not `cmd.exe`, so a pass on `windows-latest` means it ran; forced on macOS, that branch went red |
| S2 · the suite has one comment rule for a workflow's text, `tests/conftest.py#code_line`: a line whose first non-blank character is `#` is not code, and neither is the rest of a line from a `#` that follows a blank outside `"…"` and `'…'`. Inside `"…"` a backslash escapes the next character, so `\"` does not close it; a quoted string that spans lines is not tracked, and the docstring names that as the one shape that drops code. `tests/test_ci_gives_the_checks_what_they_need.py#strip_comments` is a use of it | `tests/conftest.py#code_line@1cf405fa`, `tests/conftest.py#code_lines@7f9638ce`, `tests/test_ci_gives_the_checks_what_they_need.py#strip_comments@4a6f581e`, `tests/test_a_workflow_is_read_the_one_way.py#test_one_rule_says_what_a_comment_is@b56b64b4` | **Executed** 2026-09-25. Mutated in turn, each went red: the trailing branch removed (6), quotes not tracked (3), a `#` needing no blank before it (1), all stripping removed (6). With only the whole-line branch removed, the indented comment rows stay green because the trailing branch also ends an indented comment at its `#`; the column-0 row is what pins that branch (2 red). Measured over `.github/workflows/*.yml`: the rule removes exactly the lines the whole-line rule removed, so no current reading moved | 2026-09-25 | `templates/hygiene.yml` carries one trailing comment (`fetch-depth: 0 # both checks diff against the base branch`); no case reads that file through this rule **Round 1's fix pass, 2026-09-25** (⬜ 7). `run: echo "a \" # b"` and its twin with the quote closed were red before the escape rule and green after it; `.github/workflows/*.yml` reads the same lines as before |
| S2 · `workflow_steps` reads every step of every job's `steps:` list, a step running from its `- ` at the list's indentation to the next item there or the first shallower code line, blank lines kept; `workflow_step` returns the one step whose `- name:` value, unquoted as `broad_gate.py#unquote` does, is the name, and `step_running` the one step whose code lines name the script. Both assert exactly one, so a comment naming a script cannot move the region (#482) | `tests/conftest.py#workflow_steps@0634edd6`, `tests/conftest.py#workflow_step@e789cc73`, `tests/conftest.py#step_running@a81987ad`, `tests/test_a_workflow_is_read_the_one_way.py#test_a_comment_naming_a_script_cannot_move_the_step@05bbbdf0`, `tests/test_a_workflow_is_read_the_one_way.py#test_a_step_is_found_by_its_name_and_ends_at_the_next_one@6e3ea599`, `tests/test_a_workflow_is_read_the_one_way.py#test_every_step_of_every_job_is_read_and_a_nameless_one_has_none@41f15a18`, `tests/test_a_workflow_is_read_the_one_way.py#test_a_reader_that_does_not_find_exactly_one_step_says_so@c68de6e9` | **Executed** 2026-09-25. B4 red with comment stripping removed: `2 steps run 'correction_check.py'`. Mutated in turn, each went red: a blank line ending the list (7), names not unquoted (2), a step never ending at the next item (4), `workflow_step` asserting nothing (1), `step_running` asserting nothing (2). Over the real `hygiene.yml` it reads 14 steps, the first unnamed | 2026-09-25 |  |
| S2 · the six cases that sliced `hygiene.yml` on a bare token read through the shared reader and keep what they assert: the three A9 cases through `step_running(…, "correction_check.py")`, the body case through `step_running(…, "issue_claims_check.py")` with its whole-file second assertion unchanged, and the two release-only cases through `workflow_step` by the step's name | `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_a9_the_leg_runs_the_check_and_is_allowed_to_fail@fd0cdbfb`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_a9_the_leg_asks_the_range_the_pull_request_is_about@9fdda44c`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_a9_the_leg_skips_a_release_pull_request_and_says_why@0e7357f3`, `tests/test_a_body_naming_two_issues_claims_one.py#test_the_body_reaches_the_script_through_the_environment@5507af34`, `tests/test_the_changelog_is_gathered_at_release.py#test_the_check_only_runs_for_a_release@64afd049`, `tests/test_the_ledger_fragments_fold_at_release.py#test_the_check_only_runs_for_a_release@b58272cc` | **Executed** 2026-09-25: each module alone, green (52, 50, 26 and 69 passed). The class was enumerated by `grep` for slices of the workflow's text over the modules that read `hygiene.yml` | 2026-09-25 | **Read, not executed:** that no seventh site exists. Nothing structural forbids one (`spec.md` §*Out, and why*) |
| S2 · `base_spellings` reads code lines only, counts a `BASE:` only as a key of an `env:` mapping (the nearest shallower code line above it is `env:`), and returns `""` for an empty quoted value; the spelling check is `assert_every_base_is_remote_tracking`, which fails an empty spelling with `AssertionError` naming it (#462, #463) | `tests/test_the_gate_asks_the_range_ci_will_ask.py#base_spellings@2b8ba9dd`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#keyed_under_env@2e7523c7`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#assert_every_base_is_remote_tracking@aaed6770`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#MISLEADING_WORKFLOW@e15fdeda`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_a_flag_in_a_comment_is_not_a_base@3a7c04c5`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_only_a_base_keyed_under_env_is_a_base@f5ce0fb8`, `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_an_empty_base_fails_the_spelling_check_by_name@9813d503` | **Executed** 2026-09-25. At the frame commit the reader returned `[None]` for `--baseline ""` and the check raised `TypeError`, and it read `origin/other`, `bar` and a `with:` `BASE:` as bases. Mutated here: comments read (2 red), the `env:` rule removed (1), `quoted or bare` put back (2), any shallower key accepted (1) | 2026-09-25 | The `WORKFLOW_SHAPES` row for `BASE:` now carries the `env:` line above it, because a `BASE:` with no mapping is not a base under this rule |
| S3 · `gate` does not run the `survivors` and `corrections` arms when both hold: the base the caller gave, with one leading `origin/` removed, is `main`; and the gated repository's `hygiene.yml` carries that arm's step in its `release` job. It says so on one stderr line before the checks run, pinned verbatim; the panel and the `workflow` count are unchanged, and a repository with no such workflow runs both arms at `main` as before. The arms are one constant, `SKIPPED_AT_MAIN`, beside `PARTITION`, and `main` is written once | `skills/verify/scripts/broad_gate.py#MAIN@0d49c7ed`, `skills/verify/scripts/broad_gate.py#SKIPPED_AT_MAIN@7ad0708e`, `skills/verify/scripts/broad_gate.py#skipped_at_main@bb9bbfc2`, `skills/verify/scripts/broad_gate.py#skipped_line@4734f7d7`, `skills/verify/scripts/broad_gate.py#gate@5fdd69eb`, `tests/test_the_gate_names_every_step_ci_runs.py#test_the_arms_are_left_out_only_at_main_where_the_workflow_carries_them@1929e988`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_release_pull_request_is_sealed_as_ci_would_judge_it@be038412`, `tests/test_the_gate_names_every_step_ci_runs.py#test_the_same_branch_against_its_release_branch_is_not_sealed@9199ff41`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_repository_with_no_workflow_runs_both_arms_at_main@6c265d08` | **Executed** 2026-09-25. C1 was red at the frame commit `cf5428b1`: NOT SEALED, exit 1, with the dropped correction named; its twin against `base` is NOT SEALED here as there. Mutated in turn, each went red: one arm out of the constant (5), a third arm in it (2), `origin/` not removed (1), every `origin/` removed (1), the base not asked (5), the release job's steps not asked (2 each, two spellings), no line printed (1), each arm run anyway (1 each), and skipping at `main` whatever the workflow turned C2 red end to end | 2026-09-25 | `base.given` is the caller's spelling, as `agents/sealer.md` tells the sealer to pass the base the pull request merges into. A caller that passes a SHA for a release pull request gets both arms, the direction that over-asks rather than under-asks **Round 1's fix pass, 2026-09-25** (🔴 2, ⬜ 4, ⬜ 6, ⬜ 8). C1's pinned line now spells the path as `gate.WORKFLOW`; with `WORKFLOW` built by `ntpath.join` the old literal no longer matched the printed line and the new one does. The bound (a spelling, a present step, not the step's guard) is stated in `skipped_at_main`'s docstring and in the template, each red with its sentence cut; `agents/sealer.md` tells the sealer to quote the line, red with the sentence cut; `gate`'s comment names the one read of `base.given` |
| S3 · for each arm `PARTITION` mirrors, its step in `.github/workflows/hygiene.yml` skips at `main` (a `run:` block opening with `if [ "${{ github.base_ref }}" = "main" ]; then … exit 0`) exactly where `SKIPPED_AT_MAIN` holds the arm. Total over the mirrored steps, read through `tests/conftest.py#workflow_step`: two carry the guard and three do not | `tests/test_the_gate_names_every_step_ci_runs.py#SKIPS_AT_MAIN@a59bb028`, `tests/test_the_gate_names_every_step_ci_runs.py#test_the_arms_left_out_at_main_are_the_arms_whose_steps_skip_there@9e5df99e`, `tests/conftest.py#workflow_step@e789cc73` | **Executed** 2026-09-25: red with `corrections` taken out of the constant, and red with `chain` added to it | 2026-09-25 | It reads a guard on the base and no other kind of condition, such as a job-level `if:` or an event filter; `skills/verify/SKILL.md` keeps that class named |
| C4 · `skills/verify/SKILL.md` §*What the count does not say* says #473's instance is closed, names where the arms are declared, and keeps the class open for conditions other than a guard on the base | `skills/verify/SKILL.md#"## The broad gate — after the rounds, then compare against the base"@58c17ac6` | **Read** 2026-09-25 | 2026-09-25 |  |
| D1 · the no-workflow case asserts that stderr carries none of the names of the release job, `` `release` `` as `coverage_line` spells the job, `gate.WORKFLOW` and every `PARTITION` step name, and no longer cuts echoed paths out of the stream before searching it for a bare word, so a checkout under a `release/` directory cannot turn it red (#499) | `tests/test_the_gate_names_every_step_ci_runs.py#test_a_repository_with_no_hygiene_workflow_is_sealed_exactly_as_before@8d7056b4` | **Executed** 2026-09-25. Green from a scratch copy under a directory named `release-m2`, before the edit (73f8fded) and after it (5dab37f1). Red three ways with a line forced onto the stream of a run with no workflow: a coverage line naming `` `release` ``, the same line naming only the workflow's path, and a bare step name | 2026-09-25 | `questions.md` M2: the ticket's own instance was already green before the edit, closed by 1404b3a7 (#549) |

<!-- specs/1790297087-a-ledger-row-that-will-not-parse-is-counted -->
### 1790297087-a-ledger-row-that-will-not-parse-is-counted

| Every `# RIDER:` carries a `Verified <date> against <anchor>@<hash>` stamp, and no stamp names a commit | `tests/test_a_rider_reaches_its_file.py#test_every_rider_carries_a_verification_stamp@62106019`, `tests/test_a_rider_reaches_its_file.py#test_no_rider_stamp_names_a_commit@946811d6` | **Executed** 2026-09-25, both cases: 2 passed | 2026-09-25 | Replaces the `seal/ledger.md` row *Every `# RIDER:` carries the date and SHA it was verified at, and the SHA is an ancestor of HEAD*, which phase 1 of this work item removed. Its coordinate quoted a `STAMP = re.compile(…)` line with bare `"` inside, so it never parsed and was never checked (#299); the line it quoted is now `OLD_STAMP`, kept only so a case can name the pre-#239 form, and the claim that a stamp names a commit has been false since #239. `seal/releases/0.9.1.md`'s S1 row states the checker's side of the same rule |
| A `Code grounds` cell holding a coordinate neither `ANCHOR_RE` nor `OLD_COORD_RE` parses, or citing none while the row claims something, is `MALFORMED`: named with its remedy, counted on the per-ledger and `total:` lines at zero as well, and exit 2 with or without `--strict` | `skills/evidence-check/scripts/evidence_check.py#malformed_rows@c020ae61`, `skills/evidence-check/scripts/evidence_check.py#refused_coordinate@e123b5d3`, `skills/evidence-check/scripts/evidence_check.py#URL_RE@8b6eb1b1`, `skills/evidence-check/scripts/evidence_check.py#PATH_HASH_RE@c0ffd92a`, `tests/test_a_row_points_by_content.py#test_a_coordinate_the_opener_list_misses_is_named@4d67c799`, `tests/test_a_row_points_by_content.py#test_a_directive_or_a_string_holding_a_hash_is_prose@a5403467`, `tests/test_a_row_points_by_content.py#test_prose_marks_beside_a_good_anchor_are_not_refused@4ba3bfd7`, `tests/test_a_row_points_by_content.py#test_an_unticked_coordinate_with_one_mark_is_named@9af32b6e`, `skills/evidence-check/scripts/evidence_check.py#grounds_cells@5507b31b`, `skills/evidence-check/scripts/evidence_check.py#exit_code@317c80c8`, `tests/test_a_row_points_by_content.py#test_a_coordinate_that_will_not_parse_is_malformed_under_both_readings@d66d736b`, `tests/test_a_row_points_by_content.py#test_a_claim_whose_grounds_cite_nothing_is_malformed@ebe294a4`, `tests/test_a_row_points_by_content.py#test_malformed_is_on_both_totals_lines_even_at_zero@87b356c5` | **Executed** 2026-09-25: every S1–S10 case red against the phase-1 checker (`e1679906`), where each shape read `0 ok · 0 drifted · 0 broken` at exit 0, and green after; each added unit mutated one at a time with a case going red for every mutation | 2026-09-25 | `questions.md` Q1 default (a); the `exit_code` branch is its own so a later answer moves one branch **Re-read 2026-09-25 by round 1's fix pass (#299):** rule (a) now counts a leftover, in a code span or a bare word alike, only when it holds both marks or a `#` opening a locator, and never in a URL (🟡 1): `#299`, `@cache`, `@Transactional` and an address beside a good anchor are prose, and a bare `src/service.py#Box` or `b.py#g>h` is named. Both halves red before the fix; this tree still reads 0 malformed. The claim holds **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved **Re-read 2026-09-25 by round 2's fix pass (#299):** `refused_coordinate` now reads a leftover by the coordinate's own shape: with every URL blanked, both marks, or a `#` with a path or a file name attached before it and a locator that is not digits only, or a path followed by `@` and a hash; a word's closing punctuation is the sentence's. `LOCATOR_OPEN_RE` is gone. The shapes round 2 found silent (a bare quote in a quoted line holding a URL, `path@hash`, locators opening with a digit, a non-ASCII letter or nothing) were red at `aca4f283` and are named; `#ifdef`, `#define`, `org/repo#299,` and an address are prose. A mutation of each of the rule's ten conditions turns a case red, and this tree reads 0 malformed. The claim holds |
| Only the `Code grounds` cell is read — the column headed so, or the second cell of a row under no header — so the template's notation row, a scope-decisions table, shorthand in a Notes cell, notation in a Clause cell and a row in a fence that closes are not refused; a fence that does not close is read | `skills/evidence-check/scripts/evidence_check.py#grounds_cells@5507b31b`, `tests/test_a_row_points_by_content.py#test_what_is_not_a_claim_is_not_refused@36b09d0c`, `tests/test_a_row_points_by_content.py#test_a_fragment_row_after_a_headed_table_is_still_read@0fbfee30` | **Executed** 2026-09-25: over this repository's ledgers the arm names nothing; over `seal/ledger.md`, `seal/releases/0.4.0.md` and `seal/releases/0.12.0.md` as they stood at `ca2afdb9` it names exactly the five rows `spec.md` lists | 2026-09-25 | A renamed column takes its table out of the arm, which `skills/evidence-check/SKILL.md` §*Known limits* states **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| `--reverify` names each `MALFORMED` row with a `LEFT` line and the remedy, writes nothing to it, and exits 1 | `skills/evidence-check/scripts/evidence_check.py#reverify@cc41254a`, `tests/test_a_row_points_by_content.py#test_reverify_names_a_malformed_row_and_leaves_it@f7a01d9d` | **Executed** 2026-09-25: red against the phase-1 checker, which printed `0 rows re-verified` at exit 0 | 2026-09-25 | **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| A bare `"` inside a quoted locator is named with the escape `\"`, and the escaped coordinate resolves | `skills/evidence-check/scripts/evidence_check.py#malformed_remedy@5414b35d`, `skills/evidence-check/scripts/evidence_check.py#BARE_QUOTE_RE@0a3ecaca`, `tests/test_a_row_points_by_content.py#test_a_quoted_locator_with_no_hash_is_not_told_about_a_bare_quote@60764afa`, `tests/test_a_row_points_by_content.py#test_a_bare_quote_in_a_quoted_locator_is_malformed_and_the_escape_repairs_it@21c56b97` | **Executed** 2026-09-25: red against the phase-1 checker; the remedy's quote branch removed turns it red again | 2026-09-25 | The repair phase 1 applied to four live rows **Re-read 2026-09-25 by round 1's fix pass (#299):** `BARE_QUOTE_RE` no longer matches at the end of the text, so a quoted locator with no hash (`a.py#"line"`, `a.py#f>"g"`) gets the missing-hash remedy rather than the bare-quote one (🟡 2), red before the fix. The claim holds **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
| The plugin's copy splits a row with the shared reader's `split_row`, and a copy `evidence-ci` vendors alone uses its own, held in step | `skills/evidence-check/scripts/evidence_check.py#cell_rule@fdce4ca0`, `skills/evidence-check/scripts/evidence_check.py#vendored_split_row@75592b17`, `tests/test_evidence_check.py#test_the_vendored_cell_rule_agrees_with_the_shared_one@9a0d3f3a` | **Executed** 2026-09-25: red before `cell_rule` existed; red with the vendored split's escape lookbehind removed, and with `cell_rule` returning the vendored split | 2026-09-25 | |
| The commit advisor keeps `MALFORMED` rows beside `BROKEN` and `OLD-FORMAT` and prints a block naming each row with the checker's remedy, and its docstring says so | `hooks/evidence-advisor.py#failing_rows@4ddc821f`, `hooks/evidence-advisor.py#main@d43eea30`, `tests/test_dispatch.py#test_a_commit_with_a_malformed_ledger_row_is_told_so@9da2850e`, `tests/test_a_row_points_by_content.py#test_the_advisory_docstring_names_what_it_prints@c911c4c2` | **Executed** 2026-09-25: both cases red first — the commit printed nothing, and the docstring named no `MALFORMED` — and green after; the filter entry, the block and its row lines each removed in turn turn the dispatch case red | 2026-09-25 | The filter `OLD-FORMAT` joined in round 4 (🟡 6), for the same reason **Re-read 2026-09-25 by round 1's fix pass (#299):** the block's header now counts Code grounds texts, which is what the checker counts, says nothing a row citing nothing would make false, and the closing line that repeated each text's own remedy is gone (⬜ 3); S11 and the docstring pin assert all three, red before the fix. The claim holds **Re-read 2026-09-25 by round 1's fix pass (#299):** the cited cases now write and read their fixture ledgers as UTF-8, because the `windows-latest` leg read a cp1252-written `—` back as the replacement character; nothing they assert moved |
