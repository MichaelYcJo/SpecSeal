## 0.12.2 — 2026-09-21

<!-- specs/1789956662-the-gate-and-ci-ask-about-different-ranges -->
### 1789956662-the-gate-and-ci-ask-about-different-ranges

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| R1 · `broad-gate` resolves `--base` to the ref CI will read — the given ref's upstream where the checkout declares one, else `refs/remotes/origin/<base>`, else the ref as given — and the order is what a clone tracking a second remote turns on, and both steps are asked only of a spelling `git check-ref-format --branch` accepts | `skills/verify/scripts/broad_gate.py#resolve_base@997d23fb`, `skills/verify/scripts/broad_gate.py#Base@1a6c136b` | **Executed** 2026-09-21. Eight cases red against the function's absence (`AttributeError: module 'specseal_broad_gate_for_range_tests' has no attribute 'resolve_base'`), 8 passed after. Six mutations applied alone and restored from bytes kept outside git — the two steps reordered, each of the three steps deleted, `moved` pinned false, `short_commit` answering for nothing — each turning a case red, one direction each. Deleting the upstream step alone turns exactly ONE case red, the second-remote case written for it, because the two steps agree in every ordinary checkout **Re-read 2026-09-21, round 1 fix pass (finding 6).** The rule gained a guard and the claim above is widened to carry it: `@{upstream}` is a suffix on ANY revision expression, so `--base HEAD` resolved to the current branch's upstream. Guarding step 1 alone was measured moving the defect one step down — a clone creates `refs/remotes/origin/HEAD`, and the case went from `resolved to origin/base` to `resolved to origin/HEAD`. One guard in front of both steps, and it is git's own answer to what a branch name is | 2026-09-21 | `plan.md` §*Alternatives considered* argues the order from the fork case rather than the common one, and the measurement above is why: in a plain clone `origin/` answers identically, so nothing but a second remote can tell the two orders apart The guard needs no exception for a short SHA: `check-ref-format --branch` accepts one, no ref exists for it, and it reaches the fallback the way A9 asks — verified by a case that sets an upstream on the base branch first, so the SHA path cannot pass by accident |
| R2 · Every consumer of the base inside one gate run takes the resolved COMMIT, and the closure is structural: `args.base` is read exactly once in `broad_gate.py`, at the resolution | `skills/verify/scripts/broad_gate.py#gate@624322ca` | **Executed** 2026-09-21. The count case was red at `args.base is read 7 times in broad_gate.py` and is 1 after. Each consumer routed back alone and restored from bytes kept outside git: `unverified` 2 failed, `chain` 2 failed, the survivor range 3 failed, `compare_at_base` 1 failed, the cell 1 failed, the panel 1 failed, the resolution itself 3 failed | 2026-09-21 · 2026-09-24 | `spec.md` §*The class* enumerates six CONSUMERS and the seventh read is the refusal sentence, which quotes the spelling rather than consuming it — `overview.md` carries the divergence. `compare_at_base` runs only on a failing suite that names test files, so no case reaches it with a behind base; the count is its whole pin, and that is what *the closure is structural, not a list* means in practice Round 1 verified both parsers at their coordinates: `chain_check.py` takes `SHA_RE.findall(...)[0]` in `broad_gate`, and `round_record.py` does the same in its fix-table reader. Those are the two a re-reader should open **Re-read 2026-09-21 by work item 1789985781 (#468), which drifted this anchor.** That work item added two arms to `gate()` and a line printed above the checks, and the count is what the claim rests on: `args.base` appears three times in the file today and exactly one of them is a read — the `resolve_base(root, args.base)` call — the other two being the sentences that quote the spelling. Neither new arm reads it; both take `base.commit`, which is what R2 asserts **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** one argument added to the `panel` call, `gate_copy(root)`, so the stamp says which copy of the gate ran (#475); nothing else in the body moved, and `args.base` is still read once. The claim stands. **Re-read 2026-09-25 by work item 1790297086 (#473), phase 3**, which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` and the gated repository's `hygiene.yml` carries their steps, and print one line saying so. The new guard reads `base.given`, the caller's spelling, and not `args.base`, which is still read once; every check it runs still takes `base.commit`. The claim holds and `tests/test_the_gate_asks_the_range_ci_will_ask.py` is green. |
| R3 · The survivor arm reports what CI reports on the branch the two used to disagree about — a branch that has MERGED the remote base in and replaced wording that base carried | `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_the_survivor_arm_reports_what_ci_reports@ee8f9f31` | **Executed** 2026-09-21. Red against the gate as it stood, with the disc drawn in the failure output: *the gate sealed a branch the same check refuses at CI*. Green after, at exit 1 with `docs/echo.md` named in the kept survivor output **Re-measured 2026-09-21, round 1 finding 1, in the other direction.** A base whose OWN newer commit deletes wording, merged into the branch: stale exit 1 over one removed sentence, resolved exit 0 over none. So the arm can pass where it used to refuse as well as refuse where it used to pass | 2026-09-21 | The ticket's *a stale base can only narrow the range* is wrong and `spec.md` §*What is wrong* corrects it. A fixture built from the ticket's wording reproduces nothing: `survivor_check.py#parse_range` resolves `A...B` through `git merge-base`, so both spellings agree entirely until HEAD already carries the base's newer commits. The merge is the condition, not the staleness That measurement is why `changelog.md`, `plan.md` §*Operational impact* and `overview.md` no longer claim the gate blocks more and never less. What a stale base guarantees is a disagreement, not a direction — which `spec.md` §*What is wrong* said from the frame |
| R4 · The gate prints one line where resolving MOVED the answer and nothing where it did not, and the line has a second filling for a given spelling that names no commit in this checkout, it never says CI reads a ref CI does not read, and its counts name what they are measured against | `skills/verify/scripts/broad_gate.py#moved_line@91a38149` | **Executed** 2026-09-21. Five mutations applied alone and restored from bytes kept outside git: the line printed unconditionally (A4 red), never printed (2 red), stripped of its distance (1 red), stripped of the given ref (1 red), its second filling deleted (1 red) **Re-read 2026-09-21, round 1 fix pass (findings 2 and 7).** The claim is widened twice. The line said `CI reads other/base` in a clone whose base tracks a second remote, and no runner reads that — every base-taking step of the workflow spells `origin/<base>`. It now says what this checkout declares and names the runner's own spelling beside it; a second case holds the other direction, so the repair cannot be to stop mentioning CI at all. `1 ahead, 0 behind` named no subject while both refs stood in the sentence and now reads `origin/base is 1 ahead and 0 behind base`. Both seen red first, the false sentence quoted whole **Corrected 2026-09-21 by work item 1789996775 (#461), and re-read there.** The clause *it never says CI reads a ref CI does not read* was recorded above as **Executed** and it was not true when this row was written. `--base @{-1}` passes `names_a_branch`, because `check-ref-format --branch` EXPANDS `@{-N}` before it checks, and the runner label built out of it is `origin/@{-1}` — a name no ref can have. Round 2's finding 10 quotes the line offering it as *the ref a runner reads*. What makes the claim true is the code, not a narrowing of the row: `a_runner_could_hold` asks git about the constructed label and the line now says the runner has no counterpart for that spelling. Seen red first on the shipped line, and the new sentence is pinned by `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_the_line_does_not_name_a_ref_a_runners_checkout_cannot_hold`, with `a_runner_could_hold` mutated to `return True` turning exactly that case red. **Re-read 2026-09-21 by the same work item, which drifted this anchor**: the two fillings, the silence where nothing moved and the counts' subjects are all unchanged; what moved is the third branch of the runner clause | 2026-09-21 | A4 asserts an ABSENCE, so before the line existed it passed for the wrong reason — the mutation is the only thing that shows it holds. The second filling is not in `spec.md`'s wording, which says *the given ref and the resolved ref are different commits*; a clone that never made a local branch for its base has no given commit at all and used to be exit 2. `overview.md` carries it `agents/sealer.md` has the sealer quote this line in its report, which is what makes a sentence that can be wrong here a sentence that is wrong in a report |
| R5 · The panel names the ref the base came from beside the commit, and it survives the rendering rather than only `panel`'s rows, and a ref too long for the row says so rather than being cut at the frame | `skills/verify/scripts/broad_gate.py#panel@ab36a0ff` | **Executed** 2026-09-21. The case was red at `assert ('base', 'bbbbbbb') in [… ('base', <Base object at 0x…>) …]`, green after, and asserts the rendered stdout as well as the rows. Deleting the row turns it red **Re-read 2026-09-21, round 1 fix pass (finding 5).** `seal_stamp.letter` gives a value 23 columns and cuts with no ellipsis, measured: `origin/release/2026-09-21-hotfix` rendered as `origin/release/2026-09-` and read as a whole ref. Nothing prints beside the row where the two bases agree (A4), so there was no second statement to correct it. The gate elides first and keeps the tail. `PANEL_VALUE_WIDTH` is stated in `broad_gate.py` and measured in the case by rendering a value nothing could fit and counting what survives | 2026-09-21 · 2026-09-24 | `seal_stamp.letter` cuts a value at 23 columns, so `origin/release/v0.12.0` (22) fits and a longer ref does not. `questions.md` W1 assumed the printed line covers the cut; it covers it only where resolving moved the answer, because A4 requires silence otherwise — the divergence is in `overview.md` This repository's own release refs are 22 columns and fit, which is why the first case set never exercised the cut — the case now asserts a ref that does not fit and a ref that does **Re-read 2026-09-21 by work item 1789985781 (#468), which drifted this anchor.** That work item gave `panel` a fifth parameter and a `workflow` row after the `chain` row. The `from` row, its elision and the width it elides at are byte-identical, and the new row is appended below rather than inserted among them, so the claim stands unchanged **Re-read 2026-09-21 by work item 1789996775 (#464), which drifted this anchor.** That work item rewrote `panel`'s first and last docstring paragraphs and nothing else: the first led with the retired argument that the printed line is authoritative and this row only context, which A4's silence where the two bases agree disproves, and the elision's grounds assumed the dropped prefix is always `origin/`, which round 1's finding 2 disproved. `panel`'s rows, the `from` row's elision and `PANEL_VALUE_WIDTH` are byte-identical, so the claim stands unchanged. Read by hand before `--reverify` ran, because the drift report names this coordinate once and two rows cite it. **Read a second time in the same work item**, at the commit where `survivor-check` found the removed sentence still standing in the case's own docstring and the elision paragraph took round 2's finding 13 paste-ready wording instead: the width, the rows and the elision are byte-identical again, and what moved is the qualification on which prefix a reader can infer **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** gained the `gate` row beside `from` (#475); the `from` elision and every other row are unchanged. The claim stands. |
| R6 · The gate's base spelling is held against `.github/workflows/hygiene.yml`'s, from both sides, so the two readers of one question cannot drift apart in silence again and over every shape the workflow names a base in | `tests/test_the_gate_asks_the_range_ci_will_ask.py#test_the_gate_reaches_for_the_spelling_the_workflow_uses@f2171b0f` | **Executed** 2026-09-21. Six mutations across both files, each restored from bytes kept outside git, each turning the case red: the workflow dropping `origin/` from its range and from a baseline, the gate's fallback naming a local ref path, its label and its ref path parting, the upstream form ceasing to be an upstream, and `gate()` no longer calling the resolver **Re-read 2026-09-21, round 1 fix pass (finding 4).** The reader saw three of the four base-taking steps `plan.md` §*Technical context* enumerates: it required the flag at the START of a line and a double-quoted value, so the `BASE: origin/${{ github.base_ref }}` assignment on `.github/workflows/hygiene.yml`'s milestone step was invisible to it, as was the flag on the same line as its command. It now reads four. `base_spellings` takes TEXT so the reader itself is driven over six spellings in a case — the finding was that a reader nothing drives is a reader nobody can tell is partial | 2026-09-21 | Each revision the workflow names as a base is read on its own, because one substring search for `origin/${{ github.base_ref }}` stays green while a fourth base-taking step is added without it — and round 1's finding 4 is that reading only the ARGUMENTS was itself too narrow, since the `BASE:` assignment is not an argument at all. The first form of the constant assertion was wrong — `UPSTREAM_BASE` doubles its braces to survive `str.format`, so `"@{upstream}" in UPSTREAM_BASE` is false against correct code — and it failed on the true tree, which is the cheap direction A base passed through `$BASE` is skipped rather than refused: the `env:` assignment it names is checked on its own, and refusing both would turn the case red for a refactor that left the workflow correct **Re-read 2026-09-25 by work item 1790297086 (#462, #463), phase 2**, which lifted the spelling check this case makes into `assert_every_base_is_remote_tracking` and narrowed `base_spellings` to code lines and to a `BASE:` keyed under `env:`. The case still asserts every workflow spelling against the remote-tracking ref and the gate's constants against it, so the claim holds; the real workflow still yields its four bases, and the module is green (36 passed). |
| R7 · The sealer's own definition says the base it passes is resolved, names what it resolves to, and names the panel row a reader uses to tell a fresh base from a stale one | `agents/sealer.md#"## The command"@0dd54c8e` | **Executed** 2026-09-21. Exactly ONE block of `agents/sealer.md` carries both halves of the fact; deleting every such block turns the pin red (`1 failed`). The panel-row limb is red when the phrase naming `from` is removed. `broad_gate.py` is deliberately NOT in the document list — its `REMOTE_BASE` and `UPSTREAM_BASE` constants sit in one block and answer for the whole file, measured green with the docstring's paragraph deleted — so the gate's docstring is checked through `ast.get_docstring` instead, which is red for the same deletion | 2026-09-25 | A check that cannot fail is the counterfeit `skills/verify/SKILL.md` §*The Seal Test* is about, and the first form of this pin was one for one of its three files. `test_the_block_reader_can_fail` drives the reader both ways so the cases built on it are not asserting something nothing can break **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** gained the paragraph on which copy of the gate runs and the capture-file sentence (#475, #544); the command, the base resolution and every refusal paragraph are unchanged. The claim stands. Re-stamped again in round 1's fix pass: the capture-file sentence now says to make the directory first (`mkdir -p`, ⬜ 3); nothing else in the section moved. **Re-read 2026-09-25 by work item 1790260567 (#448), phase 2**, which added three sentences to the *Exit 1, not sealed* bullet of `## The command`: a failing `suite` carries pytest's counts or a line saying its exit code is not a count of failing tests, and that line goes on unedited. The sentences on the resolved base and the panel row that tells a fresh base from a stale one are untouched; the claim holds. **First read 2026-09-21**, the date this row's `Checked` cell carried before work item 1790260567 reduced it to one reading. |

<!-- specs/1789969379-a-conflict-resolved-by-side-reverts-the-other-sides-corrections -->
### 1789969379-a-conflict-resolved-by-side-reverts-the-other-sides-corrections

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| C1 · A correction marker is the verb and the date — a rewording of the sentence after it is not a loss, and a short qualifier before it is part of the same marker | `skills/evidence-check/scripts/correction_check.py#markers@4f42d0e2`, `skills/evidence-check/scripts/correction_check.py#MARKER@17486986` | **Executed** 2026-09-21. `test_a_marker_is_the_leading_verb_and_date_and_nothing_after_it`, `test_a_reworded_sentence_around_a_standing_marker_is_not_a_loss`, `test_a_qualifier_between_the_verb_and_the_date_is_the_same_marker`, `test_a_resolution_that_rewords_the_qualifier_is_not_a_loss`, `test_a_qualified_marker_reverted_at_a_merge_is_reported`, `test_a_run_long_enough_to_be_a_sentence_is_not_a_qualifier` and `test_a_capital_word_after_the_verb_is_not_a_qualifier` and `test_the_longest_qualifier_the_tree_carries_is_seen`. Three mutations, each red on its own case: the qualifier narrowed back, the bound widened to twelve words, and the lowercase gate opened | 2026-09-21 | **Corrected 2026-09-21 by review round 1, finding 1.** This cell read *at least three spellings of the `Corrected` sentence stand in `seal/ledger.md` today*, which was true and was not the measurement the claim needed: all three vary AFTER the date. Measured over the whole file: **403 marker occurrences on 190 rows**, of which a `<verb> <date>` pattern sees 365. The other 38 put a qualifier between the verb and the date in nine spellings — `again` 19 times, `and re-executed` 5, `a third time` 4, `a fourth time` 3, `and re-stamped` 2, `a fifth time` 2, and one each of `and re-stamped again`, `and widened` and `and re-measured` — and one row carried no other spelling, so it was invisible to the check entirely. **Corrected again 2026-09-21 by review round 2, finding 6.** This cell said *the check now sees 403 of 403*, and it was 403 of **404**: the count came from the pattern itself, so the spelling it could not see was missing from the census that justified its bound — the same circle one layer down, since the sentence above it was written to correct exactly that error in the frame. `seal/ledger.md` spells one marker `Re-read and re-stamped a third time <date>`, five lowercase words where the bound was four, in the prose paragraph opening `**Re-read and re-stamped a third time <date>**, by work item 1788873610-every-copy-out-of-raw-meets-the-hider-question (#182)`. The figures here are now taken by an instrument that is NOT the pattern and has no bound — for every verb, the next date on the same line, counting the words between — and, measured over `seal/ledger.md` at the SHA round 3 read, they read **404 occurrences in the file**: 401 standing on the 190 rows that carried one and 3 in prose, of which a bare `<verb> <date>` sees 365 and 39 carry a qualifier in ten spellings. The run lengths are 0, 1, 2, 3 and 5 words and there is no run of four, so the old bound matched exactly what a bound of three would have. The check sees 404 of 404 and invents no qualifier the file does not carry. **Corrected a third time 2026-09-22 by work item 1789996780 (#470).** This cell read *404 occurrences on 190 rows, 401 in cells and 3 in prose*, which puts both halves of a contradiction in one sentence: 404 is the file's total and 401 is the part standing on rows, so the file never carried 404 occurrences on 190 rows. The distinction is what the check acts on — `correction_check.py`'s module docstring says under *Markers in prose are out of scope, by construction* that text outside a table row has no row for the survival test to decide, so the survival test acts on the part in cells and never on the part in prose. Re-measured at this branch's tip with the same unbounded instrument, over `seal/ledger.md` alone and after every marker this work item writes into it — this one, row C2's, and the two that record C3 and C4 being re-read — was already in place: **429 marker occurrences in the file — 426 standing on the 204 rows that carry one, and 3 in prose the survival test never acts on.** This figure names its moment because the corpus moves under one: the 0.12.2 release (`a8bf2a86`) folded three `seal/ledger/*.md` fragments into this file and deleted them, and writing these two markers moved it again. Taking the figure before the branch's own last ledger edit is #470's finding 9 repeated by the fix for it, which is why it is taken here and last. The run lengths are 0, 1, 2, 3 and 5 words with no run of four, so a bound of four still matches exactly what a bound of three matches. The one five-word run stands in this file's prose rather than on any row, and no row carries a qualifier longer than three words — so the bound is justified by the tree's longest spelling wherever it stands, not by anything the survival test has ever acted on |
| C2 · Both verbs count, and `Re-read` is the common one — a check watching `Corrected` alone would watch a small fraction of the marked rows and ignore the rest | `skills/evidence-check/scripts/correction_check.py#VERBS@0257b870` | **Executed** 2026-09-21. `test_both_verbs_count_because_re_read_is_the_common_one`, and two merge-level cases that go red separately so a check watching one verb cannot pass both | 2026-09-21 | Counted over `seal/ledger.md` when this row was written: `grep -c` gave 10 rows for `Corrected` and 185 for `Re-read`, with occurrence counts of 12 and 353, because a row can carry a marker in more than one cell. The work item merged immediately before this one re-read and widened four rows of the shared file and wrote `Re-read` on every one. **Corrected 2026-09-22 by work item 1789996780 (#470).** This row's claim cell read *would watch 10 rows and ignore 185*, a figure with no moment on it over a corpus that moves — the 0.12.2 release (`a8bf2a86`) folded three `seal/ledger/*.md` fragments into this file, so the split was stale before anybody re-read the row, and the claim is now stated as a proportion that survives the next release. The figures above keep their moment instead of being re-taken, because they are what this row was verified against. Re-measured at this branch's tip for a reader who wants the present split, after every marker this work item writes: 16 rows carry a `Corrected` marker and 196 carry a `Re-read`, 204 carry either, and the occurrences standing on rows are 21 and 405. `Re-read` dominates on every one of those counts, which is the claim, and no count in it is what the claim rests on |
| C3 · A marker that vanishes WITH its row is `REMOVED` and correct; one that vanishes while its row stands is the defect. The check reports only the second | `skills/evidence-check/scripts/correction_check.py#losses@47f747b5`, `skills/evidence-check/scripts/correction_check.py#standing@ed00be8f` | **Executed** 2026-09-21. `test_a_marker_that_went_with_its_whole_row_is_not_a_loss` (A3) and `test_a_merge_that_removes_the_whole_row_reports_nothing`, both red against a `standing` that accepts any row of the result | 2026-09-22 | `CLAUDE.md` §*A row whose anchor a change removes is REMOVED, not re-pointed* is the rule, so this is the repository's own distinction rather than one the check invents. The permissive mutation is the one that matters: the first mutation tried returned the PARENT's row, the marker counts cancelled, and A3 stayed green over a survival test that had been switched off. **Re-read 2026-09-21** after round 1's fixes moved `losses` and `standing`: the claim stands unchanged, and `test_a_marker_that_went_with_its_whole_row_is_not_a_loss` is still red against a permissive `standing`. **Re-read 2026-09-22** by work item 1789996780 (#470): `standing`'s docstring lost a measured row count that the 0.12.2 fold had made stale, and nothing else in the unit moved, so the claim stands and the hash was re-stamped after the row was read rather than before. Opened once more in the same day's fix pass for round 1's finding 1, which took the last figure out of that docstring: again prose only, again no code moved, and the claim stands unaltered. No second marker is written for it — one reading on one date by one party, recorded where the first was, because a marker per edit would move the very corpus this work item's figures are counted over. |
| C4 · A row is identified by its first cell and by its content anchors, and NEITHER identity answers where it holds two answers | `skills/evidence-check/scripts/correction_check.py#Row@d249f5f5`, `skills/evidence-check/scripts/correction_check.py#_index@7933d9ac`, `skills/evidence-check/scripts/correction_check.py#standing@ed00be8f` | **Executed** 2026-09-21. `test_a_row_whose_own_claim_cell_was_corrected_is_found_by_its_anchors`, `test_an_anchor_two_rows_share_does_not_decide_survival`, `test_two_result_rows_sharing_one_anchor_decide_nothing_either`, `test_two_rows_sharing_a_first_cell_decide_nothing_either`, `test_a_row_ambiguous_on_both_identities_is_not_identified` and `test_a_key_two_result_rows_share_falls_through_to_the_anchors`. Four mutations, each red on its own case: either side's guard removed, on either identity | 2026-09-22 | **Corrected 2026-09-21 by review round 1, finding 3.** This row claimed the ambiguity guard for the anchor route alone, and the cheap identity needed the same argument: removing one of two rows that share a first cell reported a loss because its twin still stood, which is A3 broken by the identity that runs FIRST rather than by the fallback. Latent when it was measured — 0 of the 189 marked rows shared a key — and not unreachable: `seal/ledger.md` repeats its section table headers, so well over a hundred of its rows carry one of two first cells. A row ambiguous on one identity falls through to the other, and one ambiguous on both is not identified at all. **Re-read 2026-09-22** by work item 1789996780 (#470): the reachability figure was stated in the present tense over a corpus that grows at every release, and the two sentences carrying it — here and in `standing`'s docstring — are now a proportion that survives one. The guard code did not move, so the claim stands; still no marked row shares a key, out of 204 that carry a marker. Opened once more in the same day's fix pass for round 1's finding 1, which replaced the count in that docstring with the proportion the sentence here already carried: prose only, no code moved, the claim unaltered, and no second marker written for one reading on one date by one party |
| C5 · A marker a parent deleted relative to the merge base is that parent's decision rather than the merge's, and the check stays silent there | `skills/evidence-check/scripts/correction_check.py#honoured@db6170d0`, `skills/evidence-check/scripts/correction_check.py#marker_counts@8de78ff3` | **Executed** 2026-09-21. `test_a_marker_a_parent_deleted_is_not_a_loss_at_the_merge`, red when `honoured` always answers False. Measured over history: the rule without the base reports 1 merge and 5 markers over 37 reachable merges, and with it reports 0 | 2026-09-21 | This diverges from `spec.md` §Scope 1, which reads *either parent*. M1 is the grounds: the one merge that rule reports is `87eced1`, where `release/v0.9.3` re-anchored a row whose section had moved file and rewrote the cell holding four historical `Re-read` sentences. The merge decided nothing. `overview.md` carries the divergence row |
| C6 · A blob the check could not read is named under `not judged` rather than passing — a ledger over the size cap used to read as a ledger with no rows | `skills/evidence-check/scripts/correction_check.py#ledger_listing@873f8f32`, `skills/evidence-check/scripts/correction_check.py#examine@73c871f1` | **Executed** 2026-09-21. `test_a_ledger_too_big_to_read_is_named_rather_than_passed`, red when the unreadable branch is skipped | 2026-09-21 | Found by the mutation sweep rather than by reading: `read_blobs` had always returned an unreadable blob as absent and said why in its docstring, and nothing downstream acted on the difference. The listing is per rev for exactly this — a path a rev does not carry and a path whose blob did not come back are different facts, and the union collapses them. **Re-read 2026-09-21** after round 1, finding 2 rewrote `examine`'s reporting loop: the unreadable-blob branch is untouched and its case is still red when the branch is skipped. **Re-read 2026-09-24 (#547)**: `ledger_listing` lists `RELEASES` beside `LEDGER` and `FRAGMENTS`, so a marker lost from a release file is reported (case red first); the per-rev listing and the `not judged` branch are unchanged. |
| C7 · The merges the check reads are reachable from the merge ref a pull-request job checks out, and gone after the squash | `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_m2_the_branchs_own_merge_is_reachable_where_the_leg_runs@907129a4`, `tests/test_a_merge_cannot_silently_drop_a_correction.py#test_m2_the_squash_is_what_puts_the_merge_out_of_reach@f08b3bf9` | **Executed** 2026-09-21. Both cases build a release branch, a feature branch that merges it in and resolves by taking a side — #424's incident — and read the check's answer at the merge ref and after the squash | 2026-09-21 | This is M2, and it is what the leg's placement rests on. `spec.md` §*What this repair cannot see* claimed it in prose; these two execute it. A negative answer would have moved the leg rather than dropped it |
| C8 · `CLAUDE.md` and `CONTRIBUTING.md` both say what to do when the shared ledger conflicts, and the two are held against each other | `` `CLAUDE.md#"## Repo rule — a change writes fragments, never the shared file"@2cb5c90f` ``, `` `CONTRIBUTING.md#"## House rules"@a0c75785` `` | **Executed** 2026-09-21. `test_a8_both_rule_documents_say_what_to_do_at_the_conflict`, driven red from each side in turn by replacing the instruction in one document only | 2026-09-23 · 2026-09-24 | Those two have already disagreed about this rule once — one forbade editing the shared file at all while the other forbade appending to it, which left a branch that had falsified a row with no reading that permits the only correct act. The needles are the instruction and its argument rather than whole paragraphs, so a rewording that still says it stays green **Re-read 2026-09-23 by work item 1790154761 (#520).** #520 widened the README rule in the same section; `test_a8_both_rule_documents_say_what_to_do_at_the_conflict` was executed after it, green **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** `CONTRIBUTING.md`'s conflict paragraph now names the release files and keeps every needle the case holds; `CLAUDE.md` is unchanged, so both still say what to do, and the case is green, so the claim holds **Re-read 2026-09-24 by the orchestrating session in work item 1790208593 (#547), which drifted this anchor.** The owner authorized editing `CLAUDE.md` in this session: the conflict rule's heading now names every ledger file (`seal/ledger.md` or a `seal/releases/<X.Y.Z>.md`), and the fold paragraph names `seal/releases/X.Y.Z.md` as the fold's destination. The instruction the claim is about — resolve hunk by hunk, read both sides, never `--ours` or `--theirs` — is unchanged, and `test_a8_both_rule_documents_say_what_to_do_at_the_conflict` holds its needles in both documents (executed, green). The claim stands. **Re-read again 2026-09-24 by the orchestrating session, round 2 of work item 1790208593, which drifted this anchor again.** Round 2 found three more `CLAUDE.md` sentences this change had made false, and the owner's authorization for the first edit covered them: the instead-of table and the removal rule name the release files beside `seal/ledger.md`, and the conflict rule's heading names a fragment two stacked branches both edited, which is where this work item's own merge conflicted. The instruction the claim is about is unchanged, and `test_a8_both_rule_documents_say_what_to_do_at_the_conflict` is green (executed, with every module that reads `CLAUDE.md`, 1182 passed). The claim stands. **Re-read 2026-09-24 by round 2's fix pass of work item 1790208593 (#547):** `CONTRIBUTING.md`'s conflict heading names `seal/ledger.md`, a release file and a fragment two stacked branches both edited, as `CLAUDE.md`'s does; every needle the case holds stands, and both documents still say what to do. Re-read 2026-09-24 by work item 1790208643 (#526), phase 3: §House rules gained #488's sentence (a removal or an edit is kept true in the file the row is in, and neither is an append) and #509's halves paragraph (the notes are a union, the hash is the editing side's, run `evidence-check` after the resolution); the claim holds. **Re-read 2026-09-24 by the orchestrating session in work item 1790208643 (#526's), which drifted this anchor a third time.** #488's and #509's sentences entered `CLAUDE.md`: the removal rule now names an edit beside a removal, and a new paragraph says hunk by hunk has two halves, the notes a union and the hash the editing side's. Both documents still say what to do at the conflict, and `test_a8_both_rule_documents_say_what_to_do_at_the_conflict` holds its needles in both (executed, green). The claim stands. **Re-read again 2026-09-24 by the orchestrating session in round 1's fix pass of work item 1790208643.** Round 1 found the halves rule's *exactly one side* false where both sides edited the unit, and the edit arm silent on an edit that makes the claim false; `CLAUDE.md` now says the hash is neither side's where both edited and the row is re-read against every edit, and that a falsified claim is corrected in place with a `Corrected <date>` note before it is re-stamped. The conflict instruction the claim is about is unchanged, and `test_a8_both_rule_documents_say_what_to_do_at_the_conflict` is green (executed). The claim stands. Re-read 2026-09-24 by work item 1790208643's round-1 fix pass: §House rules' ledger answers are three now (a claim the edit made false is corrected in place with a `Corrected <date>` note), and the halves paragraph gives the hash to neither side where both edited; the claim holds. **Re-read 2026-09-25 by work item 1790260566 (#487), phase 3:** the open-row sentence in the anchored section now says a `drained` line counts only where it is live and a row inside a closed fenced example is not a row; the conflict paragraphs this row holds against each other are untouched |
| C9 · One marker gone from one row is ONE report, whichever parents carried it, and the closing count says one | `skills/evidence-check/scripts/correction_check.py#examine@73c871f1` | **Executed** 2026-09-21. `test_a_marker_both_parents_carried_is_reported_once`, red when the losses are appended per parent again | 2026-09-21 | Every marker older than the fork is carried by BOTH parents, so the per-parent loop printed one row's one loss twice and ended on `2 correction marker(s)` — a reader sent to open two hunks when there is one. Found by review round 1, finding 2. The parent named is the one that lost the most occurrences, which is the side whose text most needs reading; ties fall to the first parent, the side the person resolving had checked out. A row that carried one marker in two cells and carries it in none is still two entries, which is why the grouping is by `(row key, marker)` rather than a deduplication |

<!-- specs/1789985781-the-gates-arm-list-is-maintained-by-hand -->
### 1789985781-the-gates-arm-list-is-maintained-by-hand

| Claim | Code grounds | Verified behavior | Date | Notes |
|---|---|---|---|---|
| G1 · Every step of `hygiene.yml`'s `release` job is mirrored by a named arm or excluded with a written reason, and the partition is held against the workflow from BOTH sides — a step added there and a row naming a step renamed away each go red on their own | `skills/verify/scripts/broad_gate.py#PARTITION@53860d6c`, `tests/test_the_gate_names_every_step_ci_runs.py#test_every_step_the_workflow_runs_is_classified@14575fb1`, `tests/test_the_gate_names_every_step_ci_runs.py#test_every_entry_of_the_partition_names_a_step_the_workflow_has@c11b6549` | **Executed** 2026-09-21. Both cases red against `PARTITION`'s absence (`AttributeError: module … has no attribute 'PARTITION'`, 10 failed), green after. Three mutations of the workflow, applied alone and restored from bytes kept outside git: a fourteenth step added reddens A1 alone; a step deleted whose row stands reddens A2 alone with A1 green; a step renamed reddens both | 2026-09-24 | #423's finding 4 was a narrow reader — it saw three of four base spellings — and it named both directions; the deletion mutation is what shows this one reads both: the two cases fail on different edits rather than on one edit each way. `plan.md` §*The failure scenario at six months* names the hole this does NOT close — an exclusion written to make the case green rather than to state a truth. **Corrected 2026-09-24** by work item 1790208643 (#474 item 3): this cell called #423's finding 4 *half a pin*, and it was a narrow reader naming both directions, as the module docstring now says too. |
| G2 · The step names are read out of `hygiene.yml` as text, with no YAML parser, and the reader takes text so it can be driven over shapes the file does not have | `skills/verify/scripts/broad_gate.py#job_steps@afc42ebb` | **Executed** 2026-09-21. Six cases drive it over fragments this repository's workflow does not carry: two jobs, an unnamed step, a commented-out step, a job that does not exist, a file with no `jobs:` at all, a quoted name, and a name carrying a colon. Four mutations each red on their own case — the job filter removed, `unquote` neutered, `STEP_RE` splitting a name at its first colon, `STEP_RE` unanchored from the dash | 2026-09-21 | #424's finding 4 is the precedent and its repair is the shape copied here: a reader nothing drives is a reader nobody can tell is partial. Driving it is what found the first spelling partial — splitting on `"\njobs:\n"` cannot see a `jobs:` on a file's first line, and no test over `hygiene.yml` could have said so. A fifth mutation left the module green at 10 passed and took a unit out of the tree with it: the line dropping comment lines is unreachable, because both patterns already require the first non-blank character to be the dash or a job key's letter. **Re-read 2026-09-21, round 1's fix pass.** The claim is unchanged and the docstring now carries the one gap round 1 found by driving the reader further: a TRAILING comment joins the step name, where YAML ends an unquoted scalar at ` #`. Not live, fails loudly through A1 rather than silently, and shared with `strip_comments` in `tests/test_ci_gives_the_checks_what_they_need.py`, which is why round 1 recorded it as a correction rather than a fix |
| G3 · Six steps of the `release` job had a local answer and no arm; two of the six run a check the plugin ships, and those two are now arms | `skills/verify/scripts/broad_gate.py#gate@624322ca` | **Executed** 2026-09-21. `test_every_arm_the_partition_names_is_an_arm_the_gate_runs` and its opposite, red when either arm is deleted from `gate()` while its row stands. The classification itself was taken by construction over all thirteen steps rather than by reading the ones that looked relevant | 2026-09-21 · 2026-09-24 | This is `questions.md` M1, and the frame refused to guess it for a reason: the answer is six and not the one the issue's title suggests. The line between the six and the two is whether the plugin ships the check — `correction_check.py` and `seal.py` are under `skills/`, while `gather_changelog.py`, `fold_ledger.py` and `claude_block.py` are under `.github/scripts/`, which exists in this repository and in no other clone. `seal/config.md`'s `Broad gate` row is where a repository names checks of its own, and whether these three go there is Q2, for the owner **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** one argument added to the `panel` call, `gate_copy(root)`, so the stamp says which copy of the gate ran (#475); nothing else in the body moved, and `args.base` is still read once. The claim stands. **Re-read 2026-09-25 by work item 1790297086 (#473), phase 3**, which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` and the gated repository's `hygiene.yml` carries their steps, and print one line saying so. Both are still arms, and still run at every other base; the claim holds. |
| G4 · A branch whose merge dropped a correction the ledger had made is refused by the gate, and the same branch with the correction kept is sealed | `tests/test_the_gate_names_every_step_ci_runs.py#test_a_merge_that_dropped_a_correction_is_not_sealed@ec36cfa3` | **Executed** 2026-09-21. Red against the gate as it stood, and the red is the defect itself: **exit 0 with the stamp drawn**, every other arm green and the ledger row reading `1 ok . 0 broken`, over a branch CI would have refused. Green after at exit 1 with `corrections` named and `Corrected 2026-09-15` quoted. A second mutation, the arm asking about `HEAD..HEAD`, reddens it alone | 2026-09-21 | This is the instance #468 was opened for. The paired case — the same fixture resolved the other way, sealed — is what keeps the first from passing against a gate that refuses everything. The arm takes the survivor arm's range spelling and inherits its known non-equivalence with the step: the workflow skips both when the base is `main` and the gate skips neither, which predates this work. **Corrected 2026-09-21 by review round 1, finding 2.** This cell sent that non-equivalence to #423, which ships in this release, so the fact would have had no open home the moment 0.12.2 reached `main` — and #423 is about the base the gate resolves rather than about a guard. Its home is **#473** **Corrected 2026-09-25 by work item 1790297086 (#473), phase 3**: the note above says the gate skips neither arm when the base is `main`, and that stopped being true. Where the base names `main` and the gated repository's `hygiene.yml` carries the two steps, the gate now leaves `survivors` and `corrections` out, as the workflow does (`broad_gate.py#SKIPPED_AT_MAIN`); #473's instance is closed. The clause itself holds: this case gates against `base`, not `main`, and it is green, and its twin against `main` is sealed in `tests/test_the_gate_names_every_step_ci_runs.py#test_a_release_pull_request_is_sealed_as_ci_would_judge_it`. |
| G5 · A `Mode` row that disagrees with where the folder actually is stops the seal | `tests/test_the_gate_names_every_step_ci_runs.py#test_a_mode_row_that_disagrees_with_the_folder_is_not_sealed@c50c742f` | **Executed** 2026-09-21. Red against the gate as it stood — the fixture sealed green — and red again when the arm is deleted while its row stands | 2026-09-21 | A3 is structural and a structural pin alone can only be broken by a reader of the source. The behavioural half drives the one direction of that disagreement CI can ever reach: a row saying `local` beside a root committed in the tree. `seal.py mode --check` takes no `--root` and resolves from the working directory, which `run` already sets to the tree being gated |
| G6 · A run says what it did not answer — the count on the panel, the names on the stream beside the command — naming a step the partition EXCLUDES with a reason apart from one it has no row for at all, and a run that answers every step says so rather than going quiet | `skills/verify/scripts/broad_gate.py#coverage_line@f3dd1189`, `skills/verify/scripts/broad_gate.py#panel@ab36a0ff`, `tests/test_the_gate_names_every_step_ci_runs.py#test_the_stamp_says_how_many_steps_the_seal_did_not_answer@b4f25713`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_step_no_row_classifies_is_not_said_to_carry_a_reason@1074c0aa`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_step_a_row_excludes_is_still_pointed_at_its_reason@47e81b14`, `tests/test_the_gate_names_every_step_ci_runs.py#test_the_two_clauses_render_together_and_hold_the_right_names@92338333` | **Executed** 2026-09-21. Five mutations, each red on its own case: the stderr line never written, the panel row removed, `unanswered` answering nothing, `workflow_text` never finding a workflow, and `coverage_line` going quiet when nothing is unanswered | 2026-09-21 · 2026-09-24 | `questions.md` W1 asked a count or the names and the answer is both, on different streams: a panel value is 23 columns (`PANEL_VALUE_WIDTH`) and `2 of 3 not answered` fits where a step name does not, while A6 asks that a reader can tell WHICH. The rendered assertion and the width assertion cannot be one assertion — `seal_stamp.letter` cuts at the frame with no marker, so a value one column too wide reads on the drawing as a shorter true statement, and the width is measured off the panel data before the frame touches it. **Re-read 2026-09-21, round 1's fix pass (finding 3), which widened the claim.** The line used to tell every unanswered step's reader that a reason for it stands in `PARTITION`, which is false for a step no row classifies — executed in a scratch repository with a `release` job of its own, where it named `deploy to staging; notify the channel` that way. The two populations are now named apart, and two cases hold both halves: one red when they are merged again, one red when the pointer to `PARTITION` is deleted from the half that has one. **Re-read 2026-09-21, round 2's fix pass**, where the Claim cell was widened to say what the Notes had been carrying alone — a Claim a re-reader audits has to reach the case that pins it. A third case renders the two clauses TOGETHER, which neither earlier fixture did: `UNCLASSIFIED_WORKFLOW` leaves the excluded half empty and this repository's workflow leaves the unknown half empty, so the joined sentence a mixed repository's reader parses was rendered by nothing. It is red when either clause learns to claim every unanswered step **Re-read 2026-09-21 by work item 1789996775 (#464), which drifted the `panel` anchor this row shares with R5 of work item 1789956662.** That work item rewrote two of `panel`'s docstring paragraphs and touched no row: the `workflow` row, the count it carries and `coverage_line` are byte-identical, so the claim stands unchanged. Read here rather than left re-stamped unread — the lenient drift report names `panel` ONCE for the two rows that cite it, which is the silence `seal/follow-up.md` already has an open item for. **Read a second time in the same work item**, at the commit where that docstring's elision paragraph took round 2's finding 13 paste-ready wording: the `workflow` row and `coverage_line` are byte-identical again **Re-read 2026-09-24 in work item 1790206436 (#337 · #475 · #544).** gained the `gate` row beside `from` (#475); the `from` elision and every other row are unchanged. The claim stands. **Corrected 2026-09-24** by work item 1790208643 (#474 item 1): the Claim was widened in round 2 and `Code grounds` was not, so the three cases that pin the two populations and the joined sentence were named in the Notes alone; they are in `Code grounds` now, and the claim is unchanged. |
| G7 · A repository with no workflow this gate can read — absent, or present and not UTF-8 — is sealed exactly as it was: no panel row, no line, the same rows in the same order, and never a traceback | `skills/verify/scripts/broad_gate.py#workflow_text@1f3fd87c`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_repository_with_no_hygiene_workflow_is_sealed_exactly_as_before@8d7056b4`, `tests/test_the_gate_names_every_step_ci_runs.py#test_a_workflow_that_is_not_utf8_leaves_the_run_as_it_was@6a38c03a` | **Executed** 2026-09-21. Two mutations, each red: the panel row filled from `PARTITION` when no workflow is there, and the coverage line written from `PARTITION` for the same case | 2026-09-21 · 2026-09-24 | A7 is what `questions.md` Q1's answer (a) rests on — the partition lives in the gate, and the gate ships to repositories whose CI it describes nothing of. Holding it required the gate to read the workflow of the tree it was pointed at rather than to print from the table: a repository with a differently-shaped `hygiene.yml` is then told, truthfully, that this gate answers none of its steps. The case asserts the panel's row labels against the exact set they were before the row existed, so an added row is red whatever its content. **Re-read 2026-09-21, round 1's fix pass (finding 5), which widened the claim to the shape that ENDED the run rather than changing it.** `workflow_text` promised *or None* and caught `OSError` alone; `UnicodeDecodeError` is a `ValueError` and `main` catches only `Refused`, so a workflow file that is not UTF-8 came out as a traceback — reproduced at `'utf-8' codec can't decode byte 0xe9 in position 45`. A third case now runs the gate over a latin-1 workflow and asserts the seal, no traceback and no panel row; it is red against `except OSError` alone, measured with the mutation anchored on this unit's own body after a first attempt matched an unrelated handler earlier in the file. **Re-read 2026-09-21, round 2's fix pass**: the Claim cell read *a repository with no `.github/workflows/hygiene.yml`*, which a latin-1 workflow is not, so a re-reader auditing this row never reached the case that pins the decode path. An under-claim rather than a false one, and the Claim column is the checkable half **Re-read 2026-09-24 in work item 1790206436 (#475), after its seal.** The case's `release` absence assertion stripped only the fixture root, and #475's running line writes the gate's absolute path to stderr on every run, so a checkout whose path contains `release` read as a workflow line — a false red measured from a clone under `release-probe/`. The assertion now strips what the gate echoes (`tmp_path`, the gate's realpath, `sys.executable`) and is still red when the gate itself writes `release`. The claim stands: no workflow row, no coverage line, no traceback; the `gate` row and the running line are what #475 gives every repository, and `HISTORICAL_ROWS` records the row. **Corrected 2026-09-24** by work item 1790208643 (#474 item 1): the Claim names the not-UTF-8 shape and `Code grounds` did not name the case that pins it; it does now, and the claim is unchanged. **Re-read 2026-09-25 by work item 1790297086 (#499), phase 4**, which made the case assert that stderr carries none of `` `release` ``, the workflow's path and every `PARTITION` step name, instead of cutting echoed paths out and searching for a bare word. The run still carries no line about the workflow and the same panel rows; the claim holds, and the case is green from a checkout under a directory whose name holds `release`. |
| G8 · A reason is prose a person wrote and names the thing out of the gate's reach; a category word is refused | `tests/test_the_gate_names_every_step_ci_runs.py#test_every_exclusion_carries_prose_rather_than_a_category@1ba9f807` | **Executed** 2026-09-21. Red against a reason cut to `EXCLUDED`, and the second case red against a reason that is prose and names nothing out of reach — *this one is not worth the trouble right now* | 2026-09-21 | Two structural rules stand in for a judgment no checker makes: several words, and not a marker. What neither catches is a category in prose clothing — *not applicable here* passes both and says as little as `EXCLUDED` — and that is written into the case rather than left for a reader to discover. The vocabulary the second case looks for is what the kinds of unreachable thing are called here, so a fifth kind is a word to add rather than a case to delete |
