## 0.15.0 — 2026-09-23

<!-- specs/1790173106-a-bare-yes-sets-the-run-length-and-a-session-review-has-no-row -->
### 1790173106-a-bare-yes-sets-the-run-length-and-a-session-review-has-no-row

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

B1–B3 are #138. B2 re-founds `seal/ledger.md`'s F8, which this branch REMOVED
there: F8 said the floor refuses a bare `yes` and `Needs a fix` does not
because that row's reason is the verdict table directly below it — a reading
decided while nothing read the row, and false once the floor's bound did. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| B1 · one reader answers the reopening question, and both walks and the generator's printed bound read through it: `yes — <what>` reopened, `no` with or without a reason did not, and a bare `yes` — like an empty cell or a word outside the vocabulary — is no reopening wherever it is read, so it stops the count of nothing | `skills/code-review/scripts/chain_check.py#says_reopened@f8d8e71b`, `skills/code-review/scripts/chain_check.py#run_reopened@729b16e0`, `skills/code-review/scripts/round_record.py#floor_and_fixes@7f50a7a7`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_says_reopened_is_the_one_reader_of_the_reopening_question@de4f0a4e`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_bare_yes_on_a_later_record_does_not_stop_the_count@22ac147c`, `tests/test_the_record_is_generated.py#test_a_bare_yes_on_a_later_record_is_no_reopening_here_either@60e5cf29` | **Executed** 2026-09-23. Seen red at `659b4229`: the walk case passed with exit 0 because `run_reopened` answered True for the bare `yes`, and the generator case printed `one reopening remains` over a round the gate refuses. Then by mutation, restored from kept bytes: reading `yes` alone as True killed the unit case and three walk cases; reading the cell through `yes_or_no` alone in `run_reopened` killed the walk case and nothing else, which is what says the walk case pins the walk rather than the record's own row | 2026-09-23 | Before this `run_reopened` returned `word == FLOOR_YES` and `floor_and_fixes` compared the same cell inline, so a hand-edited bare `yes` read as a reopening at the gate and in the printed bound alike — three characters bought the run a round past its floor, the spend #110 exists to stop. The direction is *blocks more*, the one `plan.md` declares for the row: a cell the checker refuses must never be the thing that quiets a refusal. **Re-read 2026-09-24 by work item 1790174138 (#218), which drifted `floor_and_fixes`.** The reopening read still goes through `chain.says_reopened` and no `== FLOOR_YES` came back (`grep` executed); what moved is the count walk's firing condition, one loop below the read |
| B2 · a bare `yes` in `Needs a fix` is refused on the record that carries it, in the floor row's words, for a work item begun at or after `NEEDS_FROM`, and prints before it under the row's existing grandfathering — no cutoff of its own, because no committed record at or after `NEEDS_FROM` carries one. The specification's `Needs a fix` table, the template's comment and the checker's inventory each say so | `skills/code-review/scripts/chain_check.py#stopping_floor@1878a277`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_bare_yes_in_needs_a_fix_fails_after_the_cutoff@ed31f57f`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_a_bare_yes_in_needs_a_fix_prints_before_the_cutoff@1b000795`, `tests/test_the_record_is_held_to_the_floor_and_the_depth.py#test_each_carrier_says_a_bare_yes_is_refused@bd74dd1e`, `` `docs/review-chain-spec.md#"### `Needs a fix` — the row the bound above rests on"@3a485606` `` | **Executed** 2026-09-23. Both cases seen red at `659b4229` — exit 0 and no message — then green; the carrier pin seen red by deleting the sentence from `templates/sdd-round.md`. Q1 measured on the tree: `grep` over `seal/specs/*/rounds/*.md` and `tests/` for a `Needs a fix` cell reading `yes` alone found no file among 20 live cells, `git branch -r` lists no branch beyond `main` and the two release branches, and `gh pr list --state open` returns nothing | 2026-09-24 | Replaces F8, whose claim went with the code. F8's grounds were that the reason for this row is the verdict table directly below the cell, so the tail after `yes` could be required by the floor and not here; that was true of what a person gets from the record and stopped being true of what the checker does with it once the floor's bound read the cell (#110, #117). The pin's phrase is *`yes` alone is refused the way the floor row's is*, chosen because the floor's own table already says *`yes` with nothing after it* — a whole-file substring pin is live only where the words occur nowhere else (R12). Re-read 2026-09-24 — moved by #526's split, the claim unchanged. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the section gained a folded statement on the bare `yes` at both ends; the table and its grandfathering are unchanged, so the claim holds. |
| B3 · `round_record.py new` refuses a report whose `Needs a fix:` or `Loses a record or crashes:` line is a bare `yes`, naming the label and that the line carries no reason — at the writer, one command earlier than `chain_check.py` would, in the shape `written_late_cell` already refuses an empty `--written-late` in | `skills/code-review/scripts/round_record.py#terminal_value@53032294`, `skills/code-review/scripts/round_record.py#written_late_cell@213ea56d`, `tests/test_the_record_is_generated.py#test_a_bare_yes_on_either_terminal_line_is_refused_at_the_writer@fac7b920` | **Executed** 2026-09-23. Seen red at `659b4229` for both labels: `new` wrote the record and `chain_check.py` refused it one command later, which is the failure the refusal exists to move. Then green, and the whole generator suite with it | 2026-09-23 | The class is both terminal lines, not the one the ticket names (§12): the reader already refused the floor's bare `yes`, so refusing it at the writer changes no verdict there and saves the round trip for both. **Re-read 2026-09-24 in round 1's fix pass, which drifted `terminal_value`.** The refusal's sentence explained `Needs a fix`'s count under either label (round 1's third ⬜); it now names why each row is refused. The branch, the labels it reaches and the case are unchanged |

<!-- specs/1790173209-the-release-tail-stops-at-the-first-issue-it-cannot-close -->
### 1790173209-the-release-tail-stops-at-the-first-issue-it-cannot-close

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Seven tickets, one phase each; the rows are numbered by phase. Every phase's
case was seen red at the commit before its fix and the red output is quoted in
`seal/specs/<id>/phases/phase-N.md`. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| P1 · the release closer attempts every issue a release's pull requests claimed: a refusal on `gh issue close` takes the REST route (a PATCH of the state, then the same comment through `gh api …/comments` where the refused route did not already post it), a refusal on both routes leaves the issue open with its label, and the run exits non-zero only after the last issue was tried, naming each failure with both errors and the re-run that repairs it | `.github/scripts/close_issues_on_release.py#attempt@359f9b06`, `.github/scripts/close_issues_on_release.py#close_issue@757a600f`, `.github/scripts/close_issues_on_release.py#closing_comment@a90e2312`, `.github/scripts/close_issues_on_release.py#main@ce0d9964`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_a_refused_close_takes_the_rest_route_and_the_run_finishes@08345492`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_a_refusal_on_both_routes_is_named_at_the_end_and_the_rest_still_close@bbfb1afe`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_the_spent_label_comes_off_what_closed_and_stays_on_what_did_not@92000664`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_attempt_answers_the_exit_code_and_the_error_rather_than_exiting@ebef629c` | **Executed** 2026-09-23. All seven cases red at `2fda77e8`: the refused close raised `SystemExit: gh issue close 2 … failed: GraphQL: Something went wrong while executing your query` after `closed #1`, and the both-routes case reported `{1: 'closed', 2: 'open', 3: 'open'}`. Green at `0a4e1194`, 136 passed across the five modules that drive the closer. Eight mutations one at a time with `tests/__pycache__` cleared between: a PATCH refusal reported as closed (3 red), no fallback (4 red), failures never exiting (3 red), the label spent on an unclosed issue (1 red), the REST comment carrying another body (1 red), the fallback line not printed (1 red), and `attempt` reporting every write refused — which survived the first run because every case faked it, and is red (1) once the case driving the real function against a fake `subprocess.run` was added. `DRY_RUN=1` against the previous release's own inputs, `BEFORE=1bafeb78143bf05430177e33927905205018d9d4 AFTER=cbb58091fddb9a02e3136156d7963dcd66798d1b`: six pull requests, six issues each `already closed`, exit 0, nothing written | 2026-09-23 | **The fallback is taken only after a refusal, never from the start.** The four fakes and the AST case pin `gh issue close` as the one close, and a route nobody has run across a release is not the one to make the default. **A red job for a close that landed was rejected**: the fallback line is printed per issue instead, so a release where every close took it exits 0 with as many lines as issues, and a person reading the log can see the GraphQL route has died. **The dry run printed six pull requests where the frame said seven**: a frame's count is an aggregate, and the tracker answered six. **Corrected 2026-09-24 by round 1's fix pass (finding 1).** *The same comment through `gh api …/comments`* was true of the code and false of the tracker: `gh issue close --comment` posts the comment and THEN sends the close, so the refused route had already left it, and the fallback posted it a second time. Executed by the round, read only: the issue the previous release's run could not close carries three identical closing comments, one per refused run and one from the hand repair's PATCH-and-comment pair. The clause reads *where the refused route did not already post it* now; P1d below is the unit that decides it |
| P1d · the fallback reads the issue's comment count through `_issue_api` before and after the refused `gh issue close`, and posts the closing comment through the REST route only where the count did not grow by one; a read that cannot say posts it — a duplicate is the smaller wrong answer than a close nobody explained | `.github/scripts/close_issues_on_release.py#comments_on@c2d27624`, `.github/scripts/close_issues_on_release.py#close_issue@757a600f`, `tests/test_the_closer_carries_on_past_a_refusal.py#Tracker@cbafaa5a`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_the_fallback_posts_the_same_comment_the_first_route_carries@dba8cdd1`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_a_refusal_before_the_comment_landed_still_gets_it_from_the_fallback@bf2220c6`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_a_tracker_that_does_not_count_comments_gets_the_comment_posted@118b0de8` | **Executed** 2026-09-24. The fake corrected first — it records the comment before it refuses, the order the tracker showed — and the same-comment case red at `50734814`: `#2 carries 2 closing comments; the refused route had already posted one`. Green at `611df55b`: 151 passed over the closer's five modules; the AST case still counts one `gh issue close` and no `gh issue comment` (the read is `gh api`, the same `_issue_api` every fake answers). `DRY_RUN=1` against the previous release's inputs unchanged: six pull requests, six `already closed`, exit 0 — a dry run reaches no close, so no count is read. Three mutations, one at a time: the skip never taken (1 red), `comments_on` never counting (1 red), the skip taken when the count did NOT grow (2 red, the other-order case among them) | 2026-09-24 | **Two orders, both covered.** The refusal after the comment landed (measured) skips the fallback comment; the refusal before it (`refuse_before_comment`, unmeasured but the other shape a refused mutation can take) posts it. **A read that cannot say posts**: a tracker whose issue read carries no `comments` count gets the comment twice rather than not at all, and a case pins that direction. **Added in a fix pass, one level deep**: `comments_on` pins a finding in a unit this branch created, and the unit is read by the round that follows |
| P1b · the fallback adds no verb the AST case forbids: the closer still carries exactly one `gh issue close` argv, one `gh issue edit --remove-label`, and reaches no `gh issue comment` — the REST comment is `gh api` on the issue's `/comments` endpoint | `tests/test_release_hygiene.py#test_the_script_closes_and_takes_off_one_named_label_and_nothing_else@7df1490f`, `tests/test_the_closer_carries_on_past_a_refusal.py#test_the_fallback_comments_through_the_api_and_not_through_issue_comment@d5e5af1b` | **Executed** 2026-09-23: the AST case green at `0a4e1194` with the fallback in, and the source case red at `2fda77e8` (`the fallback posts no comment at all`) | 2026-09-23 | The AST reader counts `["gh", "issue", verb]` prefixes over every `ast.List` and every `ast.Call`'s arguments, so `attempt("gh", "api", "-X", "PATCH", …)` is outside what it counts by construction rather than by luck |
| P1c · the two documents a person reads at the release say the closer carries on past a refusal and how a partial close is repaired | `docs/branch-and-release.md#"## Cutting a release / ### Work accumulates on a release branch"@c387290f`, `docs/release-checklist.md#"## 6. After the merge"@761f59ff` | Read, not run: the sentences were written beside the paragraphs the existing cases already read (`test_the_document_says_where_an_issue_actually_closes`, the §6 cases in `tests/test_the_release_tail_does_not_end_at_the_tag.py`), and those cases were run green after the edit | 2026-09-24 | The checklist's new box is a box because the failure it names is one a person finds in a job log; the command under it is the same script with the run's own three inputs, which is what repaired the release this ticket measured **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** the section changed only in the paperwork sentence's record paths, so the claim holds **Re-read 2026-09-24 for #572**, which added a sentence to §6's release-note box — the note's summary and `### 🙌 Thanks to` line, and the log line when the pull requests cannot be listed — and moved this unit's hash. The claim holds unchanged; nothing it cites was edited. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the closer's paragraph gained its fold marker and an `Enforced by:` line and no word of its text changed, so the claim holds. Re-read 2026-09-25 by work item 1790263216 (#565), phase 5: the squash paragraph above the closer's now says a rider stamp names content (work item `1788826000`), and §6 gained an `Enforced by:` line; neither touches the closer's sentences, so the claim holds. **Re-read 2026-09-25 by work item 1790260566 (#487), phase 3:** the open-row sentence in the anchored section now says a `drained` line counts only where it is live and a row inside a closed fenced example is not a row; the closer's paragraphs are untouched Re-read 2026-09-25 at the merge of release/v0.15.3 (#589, #594) into #444's branch: this row's notes from both sides are kept, and each anchored unit's edits are described by one of them, so the claim holds; re-stamped after the merge. |
| P2 · a closing keyword inside a tilde fence, a fence indented under a list item, or a double-backtick span is masked before the closer reads a body, and the same masking reaches `issue_claims_check.py` because it imports the two patterns; a four-space indented block and an HTML comment are still read as claims, each pinned by a case and argued at the pattern | `.github/scripts/close_issues_on_release.py#FENCE@2106c1c3`, `.github/scripts/close_issues_on_release.py#SPAN@eae73576`, `tests/test_release_hygiene.py#test_a_tilde_fence_masks_a_keyword@0a5dabc1`, `tests/test_release_hygiene.py#test_a_fence_indented_under_a_list_item_masks_a_keyword@ed746712`, `tests/test_release_hygiene.py#test_a_double_backtick_span_masks_a_keyword@7ff47903`, `tests/test_release_hygiene.py#test_a_four_space_indented_block_is_still_read_as_a_claim@7d98d3ba`, `tests/test_release_hygiene.py#test_an_html_comment_is_still_read_as_a_claim@237edcc5`, `tests/test_a_body_naming_two_issues_claims_one.py#test_the_shapes_the_closer_masks_since_266_are_masked_here_too@32f49933`, `tests/test_a_body_naming_two_issues_claims_one.py#test_the_check_imports_the_closers_definition_rather_than_restating_it@1358abfa` | **Executed** 2026-09-23. The three masked-shape cases and the check's case red at `e99b88e9` — `assert ['1', '4'] == ['4']`, `['2', '4']`, `['3', '4']`, and the check reporting `(['11'], ['22'], …)` for a tilde-fenced body; the two unmasked-shape cases green there, as intended. Green at `0025d851`: 172 passed over the six modules that read the patterns, `rider_check.py` at `25 ok` with the rider gone. Six mutations of the two patterns, one at a time: fence at column 0 only (2 red), backtick fence only (2 red), fence closed by either delimiter (1 red, the `\1` assertion), single-backtick span only (2 red), fence matching nothing (5 red), and the double form forbidden a backtick inside — which survived until the inner backtick in the case was made UNPAIRED, because with a pair the one-backtick arm masks the claim anyway; red (1) since | 2026-09-24 | **Direction, stated once here and at the pattern.** Masking more closes fewer issues; an unclosed issue is visible and a re-run reaches it, a wrong close is a false record. **The four-space block is unmasked because it is a bullet continuation in this repository's own bodies** — measured by reading the bodies rather than the CommonMark spec, which calls it a code block. **The HTML comment is unmasked because nobody has measured GitHub on it**; `questions.md` Q1 names the measurement, and until it is taken the shape is read as it always was. **The identity case is what makes one widening two**: the check takes `FENCE` and `SPAN` from the closer's module object, so there was never a second definition to keep in step Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the docstring's citation moved from the retired `questions.md` Q1 to `docs/issues-and-milestones.md` §*A keyword claims the one number after it*; the assertion is unchanged, so the claim holds. |
| P3 · a gather for a version `CHANGELOG.md` already heads appends the new entries at the end of that section, before the next `## `, keeps the section's own date, and writes no second heading; the dry run shows the heading the entries join; and a file that heads one version twice is refused by hygiene, naming the version and both lines | `.github/scripts/gather_changelog.py#existing_date@f3044010`, `.github/scripts/gather_changelog.py#insert@8dabc919`, `.github/scripts/gather_changelog.py#main@6473835a`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_second_gather_for_the_same_version_appends_into_its_section@f19dff55`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_dry_run_of_a_second_gather_shows_the_section_it_appends_into@055abfba`, `tests/test_release_hygiene.py#duplicated_version_headings@a083a574`, `tests/test_release_hygiene.py#test_a_version_heading_appears_once_in_a_changelog@e9bf8287`, `tests/test_release_hygiene.py#test_no_version_heads_two_sections_of_this_changelog@c4c51662` | **Executed** 2026-09-23. The append case red at `1a3e65d8`: `the second gather wrote a second heading, or re-dated the first: ['0.2.0 — 2026-09-16', '0.2.0 — 2026-09-15', '0.1.0 — 2026-09-01']`; the dry-run case red there with today's heading printed. The real-tree hygiene case seen red against a duplicate heading planted in this repository's own file and restored from a kept copy: `CHANGELOG.md heads <the running version> twice, at lines [3, 7]`. Green at `9657bcd0`: 83 passed over the gather, hygiene, release-note and release-tail modules; `test_gathering_twice_writes_one_copy` unchanged and green. Six mutations, each red on exactly one case: the append arm never taken, the existing date ignored, entries appended at the section's head, the block's heading kept on append, the dry run printing today's heading, the hygiene reader reporting nothing | 2026-09-23 | **Append, not refuse.** A flag at the moment somebody is repairing a red release is a cost paid at the worst moment, and `test_gathering_twice_writes_one_copy` already pins the no-new-fragment re-run as exit 1 with one copy — nothing about that changed. **The first date is kept** because it is the release date; a re-date on a repair moves a published date. **`insert` takes the version now**, and the one other caller (`tests/test_a_release_publishes_its_note.py`) was updated in the same commit. **Corrected 2026-09-24 by round 1's fix pass (⬜ 2 and ⬜ 3).** The append arm re-joined the blank lines it had walked back over, leaving two before the next `## ` and one extra at the end of the file; and `main` asked `existing_date` whether the section exists while `insert` asked the heading pattern, so an undated heading — a hand edit's shape — got a fresh heading in the dry run and an append in the write. P3b below carries the repair |
| P3b · `section_heading` is the one predicate `main` and `insert` ask for *is there a section*, `existing_date` answers the date alone, and the append arm drops the tail's own blank lines so the file carries one blank line before the next heading and ends with one newline; the dry run and the summary line print the file's own heading, dated or not | `.github/scripts/gather_changelog.py#section_heading@bc9721e5`, `.github/scripts/gather_changelog.py#existing_date@f3044010`, `.github/scripts/gather_changelog.py#insert@8dabc919`, `.github/scripts/gather_changelog.py#main@6473835a`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_second_gather_for_the_same_version_appends_into_its_section@f19dff55`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_second_gather_into_the_last_section_ends_the_file_with_one_newline@89304283`, `tests/test_the_changelog_is_gathered_at_release.py#test_a_second_gather_into_an_undated_heading_says_what_the_write_does@cee87fb0` | **Executed** 2026-09-24. The three cases red at `50734814`: a run of blank lines before `## 0.1.0`, a file ending `- the late one\n\n`, and the undated dry run printing `## 0.2.0 — 2026-09-16`. Green at `611df55b`: 82 passed over the gather, release-note and hygiene modules. Three mutations, one at a time, each red on one case: the tail's blank lines kept, no strip at the file's end, `main` asking `existing_date` for existence | 2026-09-24 | **The gatherer never writes an undated heading**; a hand edit does, and the two readers of the file have to agree on it whoever wrote it. **The heading the dry run prints is the file's**, so what a person reads before the write is what the write leaves |
| P4 · a version the root being swept has a `v*` tag for is history to the timer sweep and is kept wherever it is written; the shipped set is the root's tags and never `CHANGELOG.md`; a version bumped into `plugin.json` and not yet tagged is still refused; a checkout that can read no tag fails the sweep loudly rather than refusing the running version in silence; and the refusal text names the tagged route | `tests/test_release_hygiene.py#shipped_tags@41b85665`, `tests/test_release_hygiene.py#timers_in@608ccc2e`, `tests/test_release_hygiene.py#timer_offenders@3ac6ea7f`, `tests/test_release_hygiene.py#what_to_write_instead@35ab0bb8`, `tests/test_release_hygiene.py#test_a_tagged_version_is_history_and_is_kept@729c630f`, `tests/test_release_hygiene.py#test_the_shipped_set_is_the_tags_of_the_root_being_swept@cee75989`, `tests/test_release_hygiene.py#test_this_checkout_has_tags_to_read_the_shipped_set_from@320e6073`, `tests/test_release_hygiene.py#test_the_message_has_a_route_for_every_token_the_check_refuses@ff436c20` | **Executed** 2026-09-23. Red at `7fac7bec`: `TypeError: timers_in() got an unexpected keyword argument 'shipped'` and `NameError: name 'shipped_tags' is not defined` twice. Green at `fbfa199b`: 52 passed over the hygiene and sizing modules, `test_a_version_below_the_running_one_is_history_and_is_kept` unedited. Six mutations one at a time: `shipped` never consulted (2 red), `timer_offenders` passing no tags (1), a non-version tag kept (1), the shipped set read from the changelog (3), the shipped set empty (2 — the real-tree case among them), and the refusal text dropping the tagged route, which survived until the routes case gained a `TAGGED` assertion; red (1) since. On this tree at `fbfa199b`: `plugin.json` is the version the last release tagged, `git tag --list` carries its `v` tag, and the loaded set reports no offender with or without the widening — nothing in `docs/` names it yet | 2026-09-23 | **Tags, not the changelog**, per the ticket: the preparation commit writes the heading and the bump together, so a changelog-derived set would wave through the version being cut — `shipped_versions()` stays, read by the illustrative-version case alone, and its docstring says why it is not this set. **A tagless checkout is loud** because the silent reading is the OLD rule working: a wrong deny that looks like a pass. **The fixture tags its running version**, not a version below it as the spec's S10 wrote (a tag below the running version changes no answer, so that fixture could not go red); the divergence is in `phases/phase-4.md`. **What this changes in the shared ledger**: R1's *may not name one at or above it* now reads *at or above and not tagged*, corrected on the row; R3's reason for prose citations is gone and the paragraph says so, the citations stay prose |
| P5 · the completeness gate's inputs are the entries the hygiene step passes — `REPO`, `HEAD_SHA`, `HEAD_BRANCH`, `BASE`, `GH_TOKEN` — and removing one at a time gives `REPO` exit 1 (`KeyError`), `HEAD_SHA` exit 0 with nothing printed about the fallback, `HEAD_BRANCH` exit 0 with the *not a release branch* line, `BASE` exit 0 on the default, `GH_TOKEN` exit 1 (`gh` fails); the module docstring and the pinning case's docstring both say so, without a count or a superlative, and a case holds the two texts to each other | `.github/scripts/release_completeness_check.py#main@c31467b4`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_every_input_the_script_reads_is_handed_to_it_by_the_step@e88d2d0d`, `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_the_script_and_this_file_draw_the_input_boundary_the_same_way@67c30dc3` | **Executed** 2026-09-24, against `HEAD_BRANCH=release/v1.2.3` (a fixture name; no such milestone exists, so the control run printed the gate's *verified NOTHING* warning and exited 0), `HEAD_SHA` the branch tip, `BASE=origin/main`, the real repository, and an empty `GH_CONFIG_DIR` so that removing `GH_TOKEN` removes the only credential. Exit codes read from `$?` directly, never through a pipe. The five rows are the table above, verbatim from that run. The docstring case red at `ac5d6da7` on the old superlative and green at the closing commit; reverting the module docstring's paragraph alone reds it | 2026-09-24 | **The step itself is `hygiene.yml`'s *the milestone this release claims is the work it carries*, read by `test_every_input…` through `hygiene_step()` rather than anchored here — a workflow step name is not a heading the checker can resolve.** **The boundary chosen is the step's, not `os.environ`'s**: `GH_TOKEN` never reaches `os.environ[...]` in the script and is still the input whose absence fails it, and the case that guards the step was already right to loop over it. **No count in either text** — the table enumerates, and a count is what rotted the previous sentence the next time an input was added. **The sibling `label_merged_on_release_branch.py` draws its own `os.environ` boundary in one line** (`BRANCH`, `BEFORE`, `AFTER`, `REPO`); it was not touched, because its case does not disagree with it, and the ticket's fix surface is the two texts that did |
| P6 · when the roll closes a `flow-measurement` log carrying zero comments, the close comment on it, the body of the log it opens, and the job's output each say the cycle closed with no measurement written; a log with one or more comments gains no such sentence; a listing that carries no `comments` field rolls exactly as before and prints that the count was unreadable; the count is read off the one `gh issue list --json number,title,comments` call and never through a second read; the exit is 0 in every case | `.github/scripts/roll_flow_measurement_issue.py#list_open_issues@9348bf88`, `.github/scripts/roll_flow_measurement_issue.py#comment_count@2bcb3176`, `.github/scripts/roll_flow_measurement_issue.py#empty_log_note@22de14fc`, `.github/scripts/roll_flow_measurement_issue.py#close_issue@236fda1c`, `.github/scripts/roll_flow_measurement_issue.py#issue_body@fac90bc5`, `.github/scripts/roll_flow_measurement_issue.py#main@ce69059f`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_log_closed_with_no_comment_is_said_in_three_places@6180c88a`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_a_log_with_comments_says_nothing_new@d61ac926`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_an_unreadable_count_rolls_as_before_and_says_so@6bd8dcc6`, `tests/test_a_release_rolls_the_flow_measurement_issue.py#test_the_one_list_call_asks_for_the_comments@7f744d93` | **Executed** 2026-09-24. Three of the four new cases red at `6d55e1d9` — the close comment without the sentence, the output `rolled: closed #89, opened …` with no line about the count, and the list call asking `number,title` — and the fourth (a log with comments gains nothing) green there, pinning the reading that did not change. Green at `1ea20391`: 89 passed over the roll module, the workflow-step module and the flow-log module, the 30 cases the roll had before among them once seven fakes took a `comments=None` parameter. Nine mutations one at a time, each red on exactly one case: the count never read, a missing field read as zero, the close comment dropping the note, the body dropping the note, the note added whenever a count was read, `main` silent on an unreadable count, `main` silent on an empty log, the list asking for `number,title` only, and the count read from an empty listing instead of the log's | 2026-09-24 | **Never a refusal**: the ticket's *Done when* says the release must still ship, and `CLAUDE.md`'s first goal decides between a check that says and a check that stops. **One read, no write**: `questions.md` Q2 chose the list call's `--json` over a `gh api` read because every fake that reaches `main` already answers `list_open_issues`, and a second read would have sent one case to the network. **Zero is the only count that speaks**: a sentence about emptiness on a log whose count could not be read would be a false record, so `None` says only that it could not be read. **The count is the log's as it stood before the close**, read from the listing `main` already holds |
| P7 · `/specseal:update` reads the installed copy between installing and summarising: `installed_plugins.json`'s one `installPath`, its `CHANGELOG.md`'s first heading against the version the installer reported; a mismatch stops the procedure before step 3's summary, names both versions and prints the repair for the user to type, with the `.in_use/` PID caution and the one-`installPath` caution | `skills/update/SKILL.md#"## Procedure"@7d01d8df`, `tests/test_release_hygiene.py#test_update_reads_the_installed_copy_before_it_summarises@c61da456` | **Executed** 2026-09-24. The case red at `0cf5fb5f`: `no step reads the installed copy between 2 and 3`. Green with the step in: 99 passed over the hygiene, first-setup and identifier modules. Mutations of the step, one at a time: the stop turned into a note (red), the repair block dropped (red), the whole cautions paragraph dropped (red — after the case learned to pin the word `PID` rather than the path `.in_use`, which the repair's `mkdir` also carries and which left the first spelling of the case green on a caution-only deletion) | 2026-09-24 | **The skill compares and refuses; it does not extract.** Replacing a version directory a live `.in_use/` PID may hold, or the one `installPath` names, is a person's act, so the repair is printed for the user to type — the same reason step 5 names the reload and does not run it. **Step 3 still reads the clone**, which was always the correct file; 2b is what makes its summary true of the copy that will load. **Paths are `~/.claude/...`**, and the case refuses a `/Users/` path in the step |

<!-- specs/1790174138-the-report-the-record-and-the-cells-disagree-on-one-format -->
### 1790174138-the-report-the-record-and-the-cells-disagree-on-one-format

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

A1–A2 are #505 and #382 (phase 2), A3–A4 are #436 and #217 (phase 3), A5 is
#174 (phase 4), A6–A7 are #503, #437 and the comment-opener rule (phase 5).
Rows are written at each phase's close. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| A1 · a section ends at the first heading of its own level or shallower, and a deeper heading is inside it — one definition for both scripts, `chain_check.section_end`, read by the generator's `section_body` (so `table_body` and `fenced_after` carry what a `###` labels), by `swallowed`'s row loop (so a table standing only inside a fence under a `###` is refused rather than silently lost) and by the checker's `verdict_table` (so a 🔴 row below a hand-inserted `###` is still a blocker `open_blocking` sees) | `skills/code-review/scripts/chain_check.py#section_end@98b519d1`, `skills/code-review/scripts/chain_check.py#heading_level@2c3ea83a`, `skills/code-review/scripts/chain_check.py#verdict_table@11437156`, `skills/code-review/scripts/round_record.py#section_end@061c10b1`, `skills/code-review/scripts/round_record.py#section_body@44800ecb`, `skills/code-review/scripts/round_record.py#swallowed@c4faa432`, `skills/code-review/scripts/round_record.py#fenced_after@f091142c`, `tests/test_the_record_is_generated.py#test_paste_ready_fixes_under_subheadings_are_carried_in_order@4536d7bd`, `tests/test_the_record_is_generated.py#test_a_section_still_ends_at_a_heading_of_its_own_level@22011c79`, `tests/test_the_record_is_generated.py#test_a_table_hidden_under_a_subheading_is_still_a_swallowed_table@ae0270b9`, `tests/test_chain_check_at_the_pull_request.py#test_a_blocking_finding_below_a_subheading_is_still_in_the_table@5d08a32b` | **Executed** 2026-09-24. All three cases seen red at `47f6f97a`: six fenced fixes under six `###` entries produced `no paste-ready fix in the report`; a fence under a `###` in the probes section was not carried; a table fenced under a `###` wrote a record at exit 0 with the template's empty table. Then green with the whole generator module (126 passed). Five mutations, restored from kept bytes: the old any-`#` rule kills the three; `<` for `<=` kills eleven (every `##` stops ending a section); `swallowed` keeping its own any-`#` scan kills the hidden-table case alone, which is what says that case pins the row loop and not `section_body`; every line read as depth 1 kills the three | 2026-09-24 | #505. Ten of the sixteen committed reports at cbb58091 use `###` under `## Paste-ready fixes` (read by the frame), and five records read `no paste-ready fix in the report`. `heading_level` keeps the reader's own test for a heading — `startswith("#")` — and adds only the depth, so a `#120` at column 0 still ends a section as it always did; what changes is the deeper heading, and only that. The paste-ready fixes of a report written with `####` under `###` are carried too, because the rule is *same level or shallower ends it*. **Widened at the close (executed 2026-09-24):** the survivor sweep over the range reported `chain_check.verdict_table`'s own copy of the any-`#` loop, which the frame's A3 had not named — a third reader of the same class, over records rather than reports. Its case was seen red at `d8ce75a9` (a `###` between the header and an open 🔴 row, `Pass` checked, exit 0) and is green after; the definition moved to `chain_check.section_end` and the generator's `section_end` delegates to it, so the two scripts cannot drift apart. Direction *blocks more* on a hand-edited record only, prompt budget zero, no platform guarantee **Re-read 2026-09-25 by work item 1790260566, round 1's fix pass:** `fenced_after` now closes a block only on a run with nothing after it and opens none on a backtick run whose info string holds a backtick; it still reads the section through `section_body`, so what a `###` labels is still carried |
| A2 · `round_record.py new` writes the commit `--target` resolves to into `Target SHA`, never the revision as typed, and still refuses a flag that does not resolve | `skills/code-review/scripts/round_record.py#build@ebe41de3`, `tests/test_new_says_when_head_is_not_the_target.py#test_a_target_given_as_a_revision_is_named_by_its_sha@8d5819e6`, `tests/test_the_record_is_generated.py#test_the_target_is_the_flag_and_it_has_to_resolve@6c813b1c` | **Executed** 2026-09-24. The extended case seen red at `47f6f97a` with the cell reading `HEAD~1`, then green; the refusal case unchanged and green; mutation — the cell taking the flag as typed — red on the one case | 2026-09-24 | #382. `reader.commit_of` is the resolver `resolves` already wrapped, so the refusal and the cell read one answer; `head_moved` resolves the same flag for its printed line, and the two agree by construction. `chain_check.target_shas`, `broad_gate`'s descends-from test, `written_late` and `close` read the cell for SHA-shaped words, so a resolved commit is what every reader wanted |
| A3 · `chain_check.fix_range` refuses the template's pending value — `none — the fixes are not yet written` — beside a `round-N` in `Fixes checked by`, for a work item begun at or after `RANGE_FROM`, and prints it before; beside `nobody — <why>` or `no fixes to check` the pair is untouched. The arm is the one `fix_surface` already runs on its own two rows, keyed on the same cell and normalized the same way | `skills/code-review/scripts/chain_check.py#fix_range@58aaf024`, `skills/code-review/scripts/chain_check.py#fix_surface@dd0a5d68`, `tests/test_chain_check_at_the_pull_request.py#test_a_fix_range_still_pending_after_a_round_read_the_fixes_fails@c16796e4`, `tests/test_chain_check_at_the_pull_request.py#test_a_fix_range_still_pending_prints_for_a_work_item_begun_before_the_row@c722a019`, `tests/test_chain_check_at_the_pull_request.py#test_a_fix_range_pending_beside_nobody_is_the_honest_mid_run_state@99c36612`, `` `docs/round-record-spec.md#"## The fix range — `Fix range`"@c1cf414f` `` | **Executed** 2026-09-24. The two refusing cases seen red at `65195f49` — exit 0 and the row never named — then green with the module's thirteen `fix_range` cases; the `nobody` case green before and after, which is the direction that must keep passing. Three mutations, restored from kept bytes: the arm firing whatever the checker cell says kills the `nobody` case and the terminal `no fixes to check` case; the arm never firing kills both refusing cases; the arm never excused kills the before-the-cutoff case alone | 2026-09-24 | #436. No cutoff of its own: the row has carried the pending value from birth since it shipped, so `ORDER_FROM` would excuse nothing `RANGE_FROM` does not, and the frame read zero committed records at or after `RANGE_FROM` holding the pair (23 records, cbb58091). Direction *blocks more*; prompt budget zero — the refusal lands at the pull request or at `round-record close`'s own `chain_check --worktree`, with the repair named. Re-read 2026-09-24 — moved by #526's split, the claim unchanged. |
| A4 · in the records arm an HTML comment the record never closes is a malformed record whose lines are read, symmetric with a fence the record never closes: the lines are held in `aside_held`, cleared when a comment does close, read at the end, and the `NAME NOT IN TREE` marker still exempts a held line | `skills/evidence-check/scripts/evidence_check.py#claim_lines@e5006cc3`, `tests/test_a_record_states_what_the_tree_has.py#test_a_comment_the_record_never_closes_does_not_silence_what_follows@9d2374a5`, `tests/test_a_record_states_what_the_tree_has.py#test_the_marker_exempts_a_line_a_never_closed_comment_held@acac3739`, `tests/test_a_record_states_what_the_tree_has.py#test_a_closed_comment_lets_go_of_the_lines_it_held@e4415613` | **Executed** 2026-09-24. The first case seen red at `65195f49`: exit 0 and `0 names read` over a `plan.md` whose comment never closes and whose prose names `gone_helper`; then green, 61 passed. The other two are pins seen red by mutation rather than at HEAD: a held line ignoring the marker kills the marker case alone, and a closed comment keeping what it held kills the closed-comment case alone. The first fixture of the marker case wrapped the marker in `<!-- -->` on the held line, which closes the comment and holds nothing — the mutation survived until the marker was written bare | 2026-09-24 | #217. The ticket's paste-ready applied as written, its `aside_held` name included. What is given up is stated in the docstring: inside a never-closed comment a fence is not recognised, so a fenced name is read — the mirror of the fence half's own limit, and the direction is to read rather than to drop. Q3 measured 0 live records ending inside a comment (executed, phase 3), so the arm fires on nothing at the pull request **Re-read 2026-09-25 by work item 1790260566 (#220), phase 4:** `claim_lines` now treats `-->` as a position and reads the rest of its line, and asks the shared fence rule; `aside_held` is filled, cleared and read exactly as before, and the three cited cases are green |
| A5 · the `Broad gate` cell holds one entry per full-suite run, newest first: `round_record.py seal` writes the new `<sha> against <base>` in front of a run the cell already held and keeps that run behind it as `; earlier run: …` — unless the newest entry is the same commit against the same base, which the new entry replaces (`same_run`) — for the last round record and for `broad-gate.md` alike; a first seal — or a seal over `not yet` — is one entry, byte-identical to before; and `chain_check.broad_gate` still takes the first SHA-shaped word as the run, so `direct_seal` and the chain arm read the newest | `skills/code-review/scripts/round_record.py#seal@da11285c`, `skills/code-review/scripts/round_record.py#EARLIER_RUN@e1fba3dd`, `skills/code-review/scripts/round_record.py#new_broad_gate_file@42eea32e`, `skills/code-review/scripts/chain_check.py#broad_gate@e13776dc`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_re_seal_keeps_the_earlier_run_and_the_reader_takes_the_newest@56961b20`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_first_seal_is_byte_identical_to_a_cell_that_was_never_a_list@eeed157b`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_direct_home_takes_the_same_shape_on_a_re_seal@fe3af4d3` | **Executed** 2026-09-24. The two re-seal cases seen red at `48acddf6` with the constant present and the writer unchanged — the cell held the second run alone, `'48a76cf against base' == '48a76cf agai... against base'` — and the first-seal case green there, which is A13's control; then green with the module's 124. Four mutations, restored from kept bytes: the old replacing writer kills the two re-seal cases; the earlier run written first kills the same two; `not yet` kept as an earlier run kills seven, every first seal in the module; `broad-gate.md` not read before the write kills the direct case alone. The reader half is asserted through `broad_gate` and `direct_seal` themselves with `WORKTREE` on: no error and no notice on a two-entry cell | 2026-09-24 | #174, option 2 of the ticket. Q4: the separator is `; earlier run: ` — no `\|`, no SHA-shaped word, no `<!--`, and the newest SHA first, the three constraints the readers impose. Rejected: newest last (every reader changes for an ordering nobody reads) and a `spent` marker on the replaced entry (whether a run was spent or red is the sealer's report's to say). The seven documents that describe the cell say *one entry per run, newest first*, and `skills/verify/SKILL.md`'s run-level row reads the count off the cell. **Re-read 2026-09-24 by work item 1790206437 (#542), phase 3:** `new_broad_gate_file`'s comment states the same-commit-same-base replace in the round-3 report's words, and a case reads the written file for it; the writer's behaviour this row claims is untouched and the claim holds. **Re-read 2026-09-24 by the same work item's round-1 fix pass (⬜ 3, the sweep):** `chain_check.broad_gate`'s docstring now carries the same-comparison exception too; the reader still takes the first SHA-shaped word and the claim holds. **Re-read 2026-09-24 by work item 1790206437's round-2 fix pass (⬜ 6):** that exception names the newest entry now, as `same_run(entries[0], value)` does; the reader and the writer are untouched and the claim holds. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved. The Clause's *keeps that run behind it* does not name that replace, which is #556's class in this row; its correction is left to phase 6 of work item 1790208643. **Corrected 2026-09-24** by work item 1790208643 (#556): the Clause said `seal` keeps every run the cell held, and `kept_broad_gate` replaces the newest where it is the same commit against the same base; the Clause names that replace now. **Re-read 2026-09-25 by work item 1790297083's round 2 fix pass (#598).** `seal`'s last-record refusal changed one phrase, to *fails a ready pull request*; nothing this row claims moved, so the claim holds. |
| A6 · the report standard is shown where the reviewer copies from, not restated as a rule: `agents/warden.md` §Report's fenced skeleton carries a numbered 🟡, a bare 🟢 carried closure with `confirmed` and a bare ❓, says the severity goes in the `#` cell and an empty cell is the one shape refused, and names the five markers with `✅` outside them; the worked carried-closure row — a bare marker, `confirmed` never `fixed`, no 🔴 anywhere in it — is one text in the warden, `skills/code-review/SKILL.md` §Findings format, `docs/round-record-spec.md` §*A verdict row that commissions nothing* and `templates/sdd-round.md`'s comment; and the skeleton itself is a report `round_record.py new` accepts | `tests/test_the_record_is_generated.py#test_the_reviewers_skeleton_is_a_report_the_generator_accepts@5022c86c`, `tests/test_the_report_standard_is_one_in_three_places.py#test_every_carrier_shows_the_same_carried_closure_row@9aafccf6`, `tests/test_the_report_standard_is_one_in_three_places.py#test_the_warden_names_the_five_markers_and_the_tick_as_not_one@c670ac1f`, `tests/test_the_report_standard_is_one_in_three_places.py#test_the_warden_says_where_the_severity_goes@10605407`, `` `agents/warden.md#"## Verdicts"@7bd28852` ``, `` `docs/round-record-spec.md#"## A verdict row that commissions nothing"@e0f84eec` `` | **Executed** 2026-09-24. Every case seen red against HEAD's `agents/warden.md` restored from git over the phase 5 text (from a kept copy): the skeleton held two header rows and no example, and none of the four sentences stood. Then green. Six mutations, each restored: the row deleted from the template and the row's `confirmed` made `fixed` in the spec each kill the carrier case; the tick sentence, the opener sentence and the `###` permission each kill their own pin; numbering the skeleton's carried row kills the executed skeleton case, because `new` then keys a row the fix table is never asked to close. Q7 executed: `new` at `0ce4987b` writes a record at exit 0 over a `\| ✅ \| … \| verified \| … \|` row and copies it through as a row that commissions nothing, so the sentence *admitted and not a marker* stands as the frame wrote it | 2026-09-24 | #503, #437. The control round in #503 was one sentence in a spawn prompt; the example rows are that sentence where every reviewer reads it. Rejected: softening `new` to accept an empty `#` cell (an empty cell says nothing, and admitting it ticks `Pass` over an open finding) and refusing `✅` (a gate change with nothing red behind it). Re-read 2026-09-24 — moved by #526's split, the claim unchanged. **Corrected 2026-09-24** by work item 1790208643's round-1 fix pass (⬜ 11): the Claim cell named `docs/review-chain-spec.md` for §*A verdict row that commissions nothing*, which #526's split moved to `docs/round-record-spec.md`, and this branch's *moved … the claim unchanged* note above left the cell naming the old file; it names the file that holds the section now. Re-read 2026-09-24 by the 0.15.3 fold (MichaelYcJo/SpecSeal#579): the section gained a folded statement restating this row's standard with its marker and `Enforced by:` line; the carried-closure row is unchanged, so the claim holds. |
| A7 · the reviewer may group `## Paste-ready fixes` and `## Executed probes` under `###` subheadings, one per finding, and writes `&lt;!--` wherever a comment opener is meant, a code span included — the generator reads the report with comments stripped, so a literal opener inside backticks opens a comment and takes the rest of the report; code-span-aware parsing is declined twice in the tree | `tests/test_the_report_standard_is_one_in_three_places.py#test_the_warden_tells_the_reviewer_to_write_the_opener_escaped@a7dbcb01`, `tests/test_the_report_standard_is_one_in_three_places.py#test_the_warden_allows_subheadings_under_the_two_fenced_sections@10404062`, `` `agents/warden.md#"## Paste-ready fixes"@63474742` `` | **Executed** 2026-09-24. Both pins red against HEAD's warden, green after; the opener sentence weakened to *a code span excepted* and the permission negated each kill their own pin. The `###` permission is the reviewer-facing half of A1, whose cases execute it | 2026-09-24 | The opener rule is a sentence rather than a parser: `chain_check.says_none`'s docstring and the rider at `round_record.py#inherited_rows` both decline code-span parsing, and two committed reports already write `&lt;!--` |
| A8 · the release-sizing sweep's three widenings are each pinned against the reader itself — the noun-form alternatives of `STATES_A_SIZE`, `hits`'s joined-line branch, and `CLAUDE.md` in `SCANNED` — so that reverting any one of them turns its case red where the sweep's own *no offender* answer stayed green | `tests/test_a_release_is_sized_by_a_criterion.py#NOUN_FORMS@7c3d9aa1`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_pattern_catches_the_noun_forms_and_not_only_the_verb@76d9158d`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_sweep_reads_a_statement_whose_wrap_falls_inside_it@4e4197e8`, `tests/test_a_release_is_sized_by_a_criterion.py#test_the_scanned_set_reaches_the_file_a_rule_gets_restated_in@e73e5e2a`, `tests/test_a_release_is_sized_by_a_criterion.py#hits@d323992a`, `tests/test_a_release_is_sized_by_a_criterion.py#STATES_A_SIZE@9dccc48a` | **Executed** 2026-09-24. Green at `09549123` by construction — what the cases pin already stands — and each seen red by reverting its own widening from kept bytes: the three noun alternatives dropped, `hits` reduced to a plain line scan, `CLAUDE.md` removed from `SCANNED`; one case red each time, the other ten green | 2026-09-24 | #366's first half. The cases were drafted in `1789172128-…`'s round 2 report and refused there at depth 2 because the finding that commissioned them named `hits` among its coordinates; here they pin shipped code and no fix pass is involved, so the depth rule has nothing to say. The second half — `depth_two` and a `Location` spanning two depths — stays on the ticket (`overview.md` §Not done) |
| A9 · every writer of the `Broad gate` cell takes one path, `kept_broad_gate`: `seal` and `close --broad-gate` both write the new entry in front of a run the cell already holds, and a run the newest entry already records — the same commit against the same base, `same_run` — replaces that entry rather than standing beside it, while a run at that commit against another base is kept behind the new entry as any earlier run is, so a second run never erases the first and the count of entries is the count of distinct comparisons, commit and base, since a same-run re-seal replaces its entry (**Corrected 2026-09-24** by round 3's paperwork row: it read *the count of runs*) | `skills/code-review/scripts/round_record.py#kept_broad_gate@43a33862`, `skills/code-review/scripts/round_record.py#close@b91242ae`, `skills/code-review/scripts/round_record.py#seal@da11285c`, `skills/code-review/scripts/round_record.py#same_run@c1b1316a`, `tests/test_the_fixes_close_the_record.py#test_close_broad_gate_keeps_a_run_the_cell_already_holds@3ad55787`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_re_seal_at_the_commit_the_cell_names_replaces_that_entry@bf75337a`, `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_a_re_seal_at_the_same_commit_against_another_base_keeps_both@a13b531c` | **Executed** 2026-09-24, round 1's fix pass. Both cases seen red at `83988abd`: `close --broad-gate` wrote `<b> against base` alone over a held run, and a third seal at the second run's commit produced three entries with that commit twice. Then green with the close module (231) and the seal module (125). Three mutations, restored from kept bytes: `close` building the cell from the flag alone kills its case; the same-commit rule dropped kills the replace case alone; `kept_broad_gate` never keeping kills the three re-seal cases | 2026-09-24 | Round 1's 🟡 1 and ⬜ 8. #174's class is every writer of the cell, and phase 4 moved one of two while three documents said the second *writes the same cell*; the three now say it writes through the same path. The same-commit rule compares the first SHA-shaped words by prefix and asks git nothing, because the caller has already refused an unresolvable value. **Re-read 2026-09-24 by round 2's fix pass (🟡 1, ⬜ 3):** the replace is keyed on commit AND base through `same_run` now — keyed on the SHA alone it erased the first base, which the sealer's binding of a seal to both halves and four sentences promising *a second run never erases the first* forbid; `test_a_re_seal_at_the_same_commit_against_another_base_keeps_both` seen red at `43afbc1e` (the cell held the new entry alone) and the existing case re-shaped to a same-base third seal. And the docstring's *the caller resolved the value* was true of `seal` alone: `close --broad-gate` never resolves its flag, and the docstring says so. **Re-read 2026-09-24 by work item 1790206437 (#542), phase 3:** the docstring's count sentence now reads the round-3 report's block verbatim — *the count of distinct comparisons — commit and base — which is what the run-level table reads off the cell*; the body of `kept_broad_gate` is byte-identical and the claim holds. Re-read 2026-09-24 — #556 brought the comment above `close`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. Re-read 2026-09-24 — #556 brought the comment above `seal`'s `kept_broad_gate` call to the same-run replace; no behaviour moved and the claim unchanged. **Re-read 2026-09-25 by work item 1790297083's round 2 fix pass (#598).** `seal`'s last-record refusal changed one phrase, to *fails a ready pull request*; nothing this row claims moved, so the claim holds. |
| A10 · a row that repeats the verdict table's header — a second table's header under a `###` inside `## Verdicts`, or one pasted by hand — is not a verdict row to either reader: `table_body` skips it the way it skips the separator, so the record carries one header, and `verdict_table` skips it so no row with a `#` cell reading `#` reaches `open_blocking` or `close` | `skills/code-review/scripts/round_record.py#table_body@628fbfd1`, `skills/code-review/scripts/chain_check.py#verdict_table@11437156`, `tests/test_the_record_is_generated.py#test_a_second_tables_header_under_a_subheading_is_not_a_verdict_row@fa6d0d41`, `tests/test_chain_check_at_the_pull_request.py#test_a_repeated_header_row_is_not_a_verdict_row@42dc2903` | **Executed** 2026-09-24, round 1's fix pass. Both cases seen red at `83988abd`: the record's `## Verdicts` carried the header twice at exit 0, and `verdict_table` returned the header as a row whose `#` cell read `#`. Then green; one mutation each — the generator copying the repeated header, the checker reading it as a row — kills its own case | 2026-09-24 | Round 1's 🟡 3, reachable since #505 let a section reach past a `###`. The rows under the second header are verdict rows and are carried; only the row naming the columns is dropped |
| A11 · the eight documents that describe the `Broad gate` cell each say it holds one entry per run, newest first, and none still describes it as one run — pinned as gone/stands pairs, the gone half being each carrier's own wording at `0b8dc4b2`; the ninth carrier, the comment `new_broad_gate_file` writes into `broad-gate.md`, says the same and was a code comment no test pinned (**Corrected 2026-09-24** by work item 1790206437 (#542), phase 3: the comment states the same-run replace in the round-3 report's words now, and `tests/test_the_seal_is_taken_once_by_the_sealer.py#test_the_written_broad_gate_file_says_a_same_run_re_seal_replaces_its_entry` reads the written file for it, seen red on both halves) | `tests/test_the_broad_gate_cell_keeps_every_run.py#GATE_CARRIERS@247d1ff4`, `tests/test_the_broad_gate_cell_keeps_every_run.py#test_every_carrier_says_the_cell_holds_one_entry_per_run@1f15138b`, `tests/test_the_broad_gate_cell_keeps_every_run.py#test_no_carrier_still_describes_the_cell_as_one_run@9c214458`, `` `agents/warden.md#"## Role"@466a9805` `` | **Executed** 2026-09-24, round 1's fix pass. The ninth carrier, `agents/warden.md` §Role's *the SHA it ran at and the base it was compared against*, found by the round's grep and rewritten; the pin seen red by restoring `skills/verify/SKILL.md`'s old *one SHA with the base it was compared against* from kept bytes, which kills the gone half alone | 2026-09-24 | Round 1's 🟡 2. `plan.md` §*What breaks in six months* promised a red test rather than a stale sentence for exactly this class and phase 4 wrote none; the pin is that test, and the ninth carrier is the stale sentence it would have caught. **Corrected 2026-09-24 by round 2 (the paperwork row):** this clause said *nine documents*; `GATE_CARRIERS` pins eight documents, and the ninth carrier of `spec.md`'s list is the comment `new_broad_gate_file` writes, which no test greps — the count is now the pin's. **Re-read 2026-09-24 by round 2's fix pass (⬜ 2):** the verify skill's stands phrase was `earlier run`, which stood in that file at the base under §Counterfeits in another sense, so its pair held only its gone half; the stands phrase is the seal block's new line now, absent at `0b8dc4b2` (executed: 0 at the base, 1 at the target). **Re-read 2026-09-24 by work item 1790206437 (#366), phase 4**, which added the split-at-two-depths paragraph to `agents/warden.md` §Role's verifying-round item; the `Broad gate` sentence this row anchors on is unchanged and the claim holds. Re-read 2026-09-24 — #526's split re-pointed a citation inside the anchored unit to the document that now holds the section; the claim unchanged. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** only the paperwork sentence's record paths changed, so the claim holds. Re-read 2026-09-24 at the merge of `release/v0.15.1` (#558, #559) into #526's branch, at `4a077852`: #558 widened the paperwork sentence's record paths and #526's split re-pointed one citation to the record document; the claim holds. |

<!-- specs/1790174139-survivors-md-silences-what-it-quotes -->
### 1790174139-survivors-md-silences-what-it-quotes

| E1 · the work item's own `survivors.md` is out of the sweep on **both** sides — the range's path list and the pool — so a row's quote is neither wording the fix wrote nor a further carrier of the phrase, and the survivor it quotes is found, read against the row, and printed under `exempt` with the grounds; a range that edits only the file removes no sentence | `skills/code-review/scripts/survivor_check.py#records_a_past_state@c8dd4d40`, `skills/code-review/scripts/survivor_check.py#corrected@695ace00`, `skills/code-review/scripts/survivor_check.py#corpus@672d8026`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_exemption_file_the_range_added_does_not_subtract_the_survivor_it_quotes@1c2686e0`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_exemption_file_in_the_pool_does_not_dilute_the_survivor_it_quotes@666e28d4`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_report_is_the_same_with_the_exemption_file_and_with_it_deleted@1a81ce6f`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_an_exemption_file_the_range_edited_does_not_become_a_source@8c4cf289` | **Executed** 2026-09-24. Four cases seen red at `531cc723` against the unedited module, in the directions the frame named: the range half at exit 0 with no `exempt` line, the pool half at exit 0 with each of the two runs weighed at 0.5, the invariant with two different reports, the reflowed row at exit 1 as a source. Green after. Mutated: the `survivors.md` arm removed, 4 red; `corrected` and `corpus` each put back on `records_a_past_round`, 6 and 7 red over the whole module at `84c6c5ff` — **Corrected 2026-09-24** by round 2: the 6 and 6 written after `d3e544d6` were round 1's counts on the 69-case module, and both arms of the parametrised pool case go red under the `corpus` reversion (the path-list case among them both times; the 5 and 3 first written here were counts under `-k` selections, corrected by round 1's ⬜ b); the whole class collapsed to round records, 7 red | 2026-09-24 | Q4's default taken: one wider predicate that calls `records_a_past_round`, so `seal/ledger.md` S6 of `1788873640` still anchors on an untouched function. The reach is the existing predicate's — `docs/specs/<name>/survivors.md` matches it as `docs/specs/<name>/rounds/` already did. A `survivors.md` one level deeper than the layout is prose and stays in the sweep on purpose; `OWNER_DIR` (E3) is where a deeper file is a question **Re-read 2026-09-24 by work item 1790206435 (#543, #307, #439), phase 4**: `records_a_past_state` is untouched; `corrected` and `corpus` now apply a sibling predicate beside it for a gathered changelog fragment, which needs the tip's `CHANGELOG.md` and so could not be a pure path predicate. The exemption file leaves the sweep on both sides exactly as before, the four anchored cases re-run green, and the claim holds. **Re-read 2026-09-24 by the same work item, phase 5 (#551)**: `corrected`'s list is read with `--no-renames`; the predicate on it is unchanged, and the claim holds. **Re-read 2026-09-24 in round 1's fix pass (🟡 1)**: `corrected` counts the tip's released changelog sections as held for `CHANGELOG.md` alone; the exemption file's exclusion is unchanged, and the claim holds. **Re-read 2026-09-24 in round 2's fix pass**: the held set is now what this range put under a version heading, not every released section; the exemption file's exclusion is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790221963 (#557, #555), phase 4**: `corrected` gained a filter on what a release writes, from the gathered fragments read at `a`; the exemption file's exclusion on the path list is unchanged, the four anchored cases re-run green, and the claim holds. **Re-read 2026-09-24 in that work item's round 1 fix pass**: the held sentences' shared n-grams are written after the list is filtered; the exclusion is unchanged, and the claim holds. **Re-read 2026-09-24 in that work item's round 2 fix pass**: they are now returned apart from `written` and scored against `CHANGELOG.md` alone; the exclusion is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790260564 (#564), phase 1**: `corrected` reads the gathered fragments at every path `a_gathered_fragment` accepts at `a`; the exemption file's exclusion on the path list is unchanged, and the claim holds. **Re-read 2026-09-24 by work item 1790260564 (#563), phase 2**: `corrected` pairs sentences across the paths left after the exclusion, so a `survivors.md` row's quote is in neither side to pair; the exclusion is unchanged, the four anchored cases re-run green, and the claim holds. |
| E2 · over the three pull-request ranges `spec.md` §*The measured state* names, the exemption file had silenced 29 of the 36 rows written into it; with the file out of the sweep the count of `exempt` lines equals the count of places standing without `--exempt`, and deleting the file at the tip changes neither | `skills/code-review/scripts/survivor_check.py#"## What is excluded, by construction rather than by list"@57f0406a`, `skills/code-review/scripts/survivor_check.py#records_a_past_state@c8dd4d40` | **Executed** 2026-09-24, at `531cc723` and again at `af5c4044` and `e88cdebc`, one Python script over the worktree and a scratch clone in which the file is deleted at the tip. #525: 7 `exempt` lines → **14**, 14 standing without the file, 14 with the file deleted; #528 and #527: 0 in every state, at every stage, and 0 at `--floor 1.0`. Every `exempt` line carries a row's grounds (14 of 29 rows consulted, 22 of 36 dead, none edited). The old probe's fifteenth candidate was another work item's `survivors.md` standing in the pool as prose, now out by construction; the row #525 wrote for it is dead rather than consulted | 2026-09-24 | The frame's read claim that #528's four rows printed under `exempt` at a tip without the file does not reproduce over the squash range, where the places they quote clear no floor; the claim was taken over the branch's own range at the time. Nothing in the fix turns on it. Five places on #525 appear only when its `overview.md`, `changelog.md` and ledger fragment also leave the sweep — the class `spec.md` §*Out* keeps in on purpose, listed in `overview.md` §*Not done* |
| E3 · a work item's `phases/` records are out of the sweep on both sides, the way its round records are: a phase record the range added subtracts nothing, one the range edited is not a source, and one standing in the pool is neither reported nor a carrier that dilutes the real survivor | `skills/code-review/scripts/survivor_check.py#records_a_past_state@c8dd4d40`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_phase_record_the_range_added_does_not_subtract_the_survivor_it_quotes@2a2b87c9`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_phase_record_the_range_edited_does_not_become_a_source@97bc293a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_phase_record_standing_in_the_pool_is_not_a_survivor@e64fda0b` | **Executed** 2026-09-24. Three cases seen red at `c1311c53`: the added record at exit 0, the edited one at exit 1 naming the record as a source, and the pool one at exit 0 — by dilution rather than by the report the frame expected, a fourth carrier halving the weight of every phrase it quotes. Green after; the three real ranges unchanged coordinate for coordinate. Mutated: the `phases/` arm removed, 3 red. **Re-read 2026-09-24 in round 1's fix pass (finding 1)**: on a four-file pool the reported-record direction could never be the assertion that failed, so the pool case now runs on 4 and 33 files; with the `phases/` arm removed the `[0]` arm went red at exit 0 with nothing standing and the `[29]` arm red on the `named` assertion, the record reported beside `guide.md` at 1.60 — both directions seen red, each by its own assertion. Green restored, `2 passed` | 2026-09-24 | What this gives up — the HTML-comment correction shape #423's pass caught only because phase records were in the pool — is a `seal/follow-up.md` row, the repository owner named (Q5, default *no checker*). Being edited in place while the work item is live does not keep a record in: the exclusion is about what the file is, never about when it was last written |
| E4 · the work item a `--exempt` file belongs to is read off the `seal/specs/<id>` prefix at any depth, so a `survivors.md` one directory below the layout is asked the ownership question and printed under `not yours` when the range touches nothing of that work item (in local mode, when the range's tip is off the branch that work item's `routing.md` names); at the layout position nothing changes | `skills/code-review/scripts/survivor_check.py#OWNER_DIR@482225d6`, `skills/code-review/scripts/survivor_check.py#whole_range@cc77852a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_a_declaration_one_directory_deeper_still_has_an_owner@bd9591fc` | **Executed** 2026-09-24. The deeper arm seen red at `95956de8` — the declaration excused the run at exit 0 with no line printed — and the layout arm green before and after. Mutated: the old tail `[^/]+$` restored, 1 red and 1 green | 2026-09-24 | The pre-0.4.0 top-level `specs/` root is left out of `OWNER_DIR` on purpose (`spec.md` §*Out*): harm needs every exemption file in a tree handed to one run, which only `hygiene.yml` does, over `seal/specs/*` alone. S8's enumeration of every path-reading pattern in the module is in `phases/phase-3.md`; `RANGE_CELL` reads a cell rather than a path, and a path cell spelled `a/../b.md` matches it — loud, under `unresolved`, and no row in the tree is spelled that way **Re-read 2026-09-24 by work item 1790206435 (#543, #307, #439), phase 4**: `OWNER_DIR` is untouched; `whole_range` reads the owner off it one step earlier, for an unresolved declaration as well as a resolved one. The deeper-file case re-run green at both depths, and the claim holds. **Re-read 2026-09-24 by the same work item, phase 5 (#551)**: `whole_range`'s `changed` list is read with `--no-renames`; `OWNER_DIR` and the question asked of it are unchanged, and the claim holds. **Corrected 2026-09-25 by work item 1790260564 (#554), phase 3**: the claim said a deeper file is refused when the range touches nothing of its work item, which is the only test there was. A local-mode file's directory is never committed, so no range touches it and every local declaration was refused, its own work item's included. In local mode the question is now whether the range's tip is on the branch the work item's `routing.md` names (`on_its_branch`), and the claim carries that clause. `OWNER_DIR` is unchanged; the anchored case gained an assertion that a shared-mode refusal still names the diff test as its reason, and re-runs green. **Re-read 2026-09-25 in round 1's fix pass of that work item (🟡 1, 🟡 2)**: in local mode the refusal also covers a tip on a branch the declared one was cut from, and prints the reason `on_its_branch` returns; the claim's clause is a condition that refuses, not the only one, and `OWNER_DIR` and the shared-mode test are unchanged. The claim holds. **Re-read 2026-09-25 in that work item's round 2 fix pass (⬜ 1)**: only `whole_range`'s docstring changed; the claim holds. |
| E5 · the module docstring names every member of the excluded class with the side each holds on, and the pinning case reads them as a list rather than one paragraph, the round record first, so a member excluded in code and absent from the docstring turns the case red | `skills/code-review/scripts/survivor_check.py#"## What is excluded, by construction rather than by list"@57f0406a`, `tests/test_a_corrected_sentence_survives_elsewhere.py#test_the_docstring_names_both_sides_of_the_round_record_exclusion@1abe4cd8` | **Executed** 2026-09-24. Seen red with each new paragraph absent (``**The work item's own exemption file.** is no longer an exclusion stated``, and the same for `**A phase record.**`); mutated after with each opener's full stop removed, 1 red each | 2026-09-24 | `agent-contract` §14, extended: the negative assertion refusing the pre-#365 one-sided sentence stays on the round record's paragraph alone, because that is the sentence which shipped |
