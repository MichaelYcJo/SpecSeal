## 0.6.0 — 2026-09-03

<!-- specs/1788433011-every-spawn-prompt-is-retyped-from-memory -->
### 1788433011-every-spawn-prompt-is-retyped-from-memory

<!-- One work item's rows. No header — `fold_ledger.py` writes the `###` at
the release and moves this file into `seal/ledger.md`.

Five rows, and the last of them is a defect this work item produced and its
own last phase found. Recorded rather than repaired quietly, because the
mechanism that let it through is the general lesson and the fix is one line. -->

| Clause | Code grounds | Verified behavior | Checked | Notes |
|---|---|---|---|---|
| L1 · `skills/agent-contract/SKILL.md` is the single home for every rule true of all agents, and a definition that carries a section's own sentences fails rather than passing unnoticed | `tests/test_a_moved_rule_leaves_its_definition.py#test_no_definition_carries_a_sections_own_sentences@546722ba`, `skills/agent-contract/SKILL.md#"## How the sections are numbered"@838b3c7d` | **Executed**: the case is parametrised on both globs, so it is one case per section per definition and a §17 or a fourth agent is checked on the day it lands. Its mutation — a section's body pasted back under a new heading — is red for every pair | 2026-09-03 | The half of Q2 that says *the definition keeps only its own application* had nothing behind it until this module. Measured by the orchestrator at `e51fd0b`: §9 pasted into `agents/warden.md` left `test_broad_gate_rule`, `test_edits_go_through_the_edit_tool`, `test_every_agent_reads_the_contract` and `test_the_agent_contract_holds_the_universal_rules` all green, 81 passed, because every assertion in them is a presence check |
| L2 · the duplication check is a 15-word verbatim window, and both ends of its margin are measured rather than chosen | `tests/test_a_moved_rule_leaves_its_definition.py#test_the_window_sits_between_what_was_measured@67d34a26` | **Executed**: the longest run any KEPT application shares with any section is **10** words — `agents/smith.md`'s *a bare word is a pathspec and git rejects it*, which §8 and the design gate's waiver paragraph both spell because both are about one git fact. The smallest run a REAL duplication produced, measured on the same files before phase 4 removed them, is **25** (§10), then 31 (§9), 37 (§16), 59 (§11) and 66 (§3) | 2026-09-10 | This is the number a later session would otherwise re-derive, and re-deriving it needs the pre-phase-4 files. The case prints both bounds and fails when either end of the margin closes, so raising the window to quiet a failure is red rather than silently mis-tuned. Verbatim is deliberate: a paraphrase check would have to decide when two sentences say the same thing, and it would refuse the application prose the rule exists to permit  **Re-read 2026-09-10 in work item 1789034970 (#120), round 1's fix pass.** A `# RIDER:` was added inside `test_the_window_sits_between_what_was_measured`, which moved the case's hash and changed no assertion — it records round 1's finding 11, that the bound is on the MAXIMUM rather than per pair, so a second definition newly reaching 10 leaves the case silent. The claim holds: `WINDOW` is 15, `LONGEST_KEPT_APPLICATION` 10 and `SMALLEST_REAL_DUPLICATION` 25, and all three ends are still asserted here. |
| L3 · the protocol is a pointer, and the sentence a prompt is actually governed by is what stands where the rules stood | `docs/review-handoff-protocol.md#"## What every spawn prompt used to carry / ### What a prompt is left holding"@2de7af10`, `docs/review-handoff-protocol.md#"## What every spawn prompt used to carry / ### Where each half went"@c6842fab`, `tests/test_the_handoff_before_round_one.py#test_the_protocol_no_longer_states_the_rules_it_points_at@7f94150d` | **Executed**: the presence case asserts the section names the contract path, names `agents/<name>.md`, and still says *a prompt carries what is specific to the round and nothing else*; the absence case asserts five phrases verbatim from what phase 5 removed, so a paste-back is red rather than invisible | 2026-09-03 | The section title moved with the content — *What every spawn prompt carries, until the definitions carry it* asserted an interim state that stopped being true in the same commit. A heading is what a reader scanning the document acts on, and one document named the old title. Re-read 2026-09-25 by work item 1790263216 (#565), phase 4: *Where each half went* gained one `Enforced by:` line after its last paragraph and no sentence changed; the claim holds |
| L5 · a rule with one written home is deleted by the move that was built to stop rules going missing, and an absence check alone cannot see it | `agents/smith.md#"## Phases"@12c56e52`, `tests/test_the_handoff_before_round_one.py#test_the_smiths_definition_mandates_mutating_every_unit_it_added@6a971c8f` | **Executed** 2026-09-03: at `dcdf4e4` a search for *mutation-test every unit added* across `agents/`, `skills/`, `docs/`, `templates/`, `CLAUDE.md` and `CONTRIBUTING.md` returned nothing. The new presence case is shown red four ways — the rule deleted, the cadence changed, the timing dropped, the observation dropped — and the first draft's `one at a time` assertion PASSED under the second, because the design gate holds that phrase in an unrelated sentence a hundred lines up | 2026-09-24 | The row above it in the same module asserts the phrase has LEFT the protocol, and it went green over a rule that had left the repository altogether. That is the general claim: an absence check with no presence check beside it cannot tell a rule that moved from a rule that was deleted. The other four rows of that table point at contract sections, which `test_the_agent_contract_holds_the_universal_rules.py` pins from the other side; this one pointed at a definition and nothing pinned it. Contract §15 is the neighbour and not the rule — it asks that one case be seen red on the day it is planted, where this asks that every unit a branch added be broken before the branch leaves. **Re-read 2026-09-05 in work item 1788597030's round 1 fix pass**: the region gained the fix-pass paragraph at step 3 — the `## Fixes` table, one row per OPEN finding, a correction closing `answered — corrected at <sha>` — the reach-back sentence naming `round_record.py new`, and the depth measured by `close` rather than declared. The mutation rule at step 4 is untouched. **Re-read 2026-09-06 in work item 1788632199's phase 3**: step 4 gained a paragraph telling a segment to find the runner its repository ships before inventing a command, and to type the narrow form. It sits above the mutation rule and touches neither it nor the moving-rule claim this row is about. **Re-read 2026-09-08 in work item 1788826000's round 1 fix pass**: the only edit to `## Phases` was the design-gate rider's own stamp, re-anchored from the sentence it quoted to this heading, so the region moved and the claim did not. Step 4's mutation rule is untouched **Re-read 2026-09-08 by work item 1788873630 (#265).** The five `Orchestrator:` sections left `skills/code-review/SKILL.md` for `skills/code-review/orchestration.md`, so this unit's prose gained the new path and its design-gate rider gained a stamp. Neither is what the row claims, and the claim holds. **Re-read 2026-09-08 in work item 1788873640**, twice in one branch: step 3 gained the survivor-check paragraph telling a fix pass what its range left standing, and the design-gate rider was corrected where it stated a consequence measured to be false. Neither touches step 4's mutation rule nor the moving-rule claim this row is about, and the claim holds. **Re-read 2026-09-09 by work item 1788912166 (#296, #295, #297).** Step 3 of `## Phases` gained the paragraph telling a fix pass that a range which DELETES a shipped section takes one whole-range row in `survivors.md` instead of one row per survivor. Step 4's mutation rule is untouched, and the moving-rule claim this row is about holds. **Re-anchored 2026-09-09, not re-verified**: the section's hash moved because this work item edited a different sentence inside `## Phases`, and the claim was not re-read. The row's sibling at the same kind of state says the same thing in the same words, and two rows on one anchor must not disagree about who read it. **Re-read 2026-09-10 in work item 1789002694 (#30), phase 4.** `## Phases` gained the coverage-probe paragraph and names the sealer where it named the orchestrator. Step 4's mutation rule — the rule this row is about — is untouched. The section moved once more in the same phase and for a reason that is not an edit to it: `rider_check.py --reverify` re-stamped the `# RIDER:` comment inside it, which this branch's own phase-3 edits had drifted. A rider stamp and a ledger anchor over the same unit move each other, and the second re-stamp reads nothing new. **Re-read 2026-09-11 in work item 1789081272 (#84), phase 2.** Step 1 gained the paragraph telling a builder to say whether the frame holds before building to it, the design gate's opening lost the clause about the two utility skills, and step 2's last paragraph became a pointer at `agents/framer.md` instead of the call itself. Step 4's mutation rule and the moving-rule claim this row is about are untouched, and the claim holds. The section then moved a second time in the same phase for the reason the sentence above already records: `rider_check.py --reverify` re-stamped the design-gate rider, which those same edits had drifted. **Re-read 2026-09-22 by work item 1790076060 (#492, #493).** The section gained two paragraphs: that a capped run does not end the smith's fixes, and that the first home a leftover takes is a rung rather than a default. The mutation rule this row is about is untouched, and `test_the_smiths_definition_mandates_mutating_every_unit_it_added` is green, so the claim holds. **Re-read again 2026-09-22**, in round 1's fix pass. The no-mechanism destination sentence changed from *is an issue* to *takes the ladder*; nothing this row is about moved. **Re-read a third time 2026-09-22**, when the broad gate's refusal fired the `# RIDER:` anchored on this section: its stamp was re-read, its measured half re-measured and corrected, and the rider re-stamped. The rider is about the `[no-review]` waiver example at the head of the section and nothing this row claims is in it. Re-read 2026-09-24 — #55's sentence joined the design gate's waiver paragraph (the waiver is the last shape, and contract §8 names the two before it); the claim unchanged. **Re-read 2026-09-25 by work item 1790260564 (#554), phase 3**: the section's survivor-sweep paragraph gained a parenthesis on local mode; no rule left the section, and the claim holds. |
| L6 · the record-language instruction has two homes and neither is a definition — the whole list in `implement`, the short form as contract §11 | `skills/agent-contract/SKILL.md#"## §11 The language the records are written in"@bdd6d99f`, `skills/implement/SKILL.md#"## The language the records are written in"@f307fad8` | **Executed**: the suite over the modules that read the definitions, 604 passed at `5e610a3` | 2026-09-03 | Replaces row S2 of work item 1788420760, removed from `seal/ledger.md` by this branch. That row's grounds named `agents/smith.md` and `agents/warden.md`, and phases 3 and 4 moved the section out of both — the coordinates read BROKEN, which is CI's failing exit rather than its warning |
| L7 · the probe rules and the git-from-Python rule are contract §7 and §8, and the definitions point rather than restate | `` `skills/agent-contract/SKILL.md#"## §7 A probe is named `test_tmp_*`, one file, run once, deleted"@a677a411` ``, `tests/test_a_probe_that_commits_says_so.py#test_the_contract_carries_all_three_shapes@287934dc`, `tests/test_a_probe_that_commits_says_so.py#test_the_fact_finder_points_at_the_rule_it_no_longer_restates@9a67764a` | **Executed**: the module's cases now read the contract for the shapes and the definition for the pointer, which is the re-pointing Q2 answered `full` to | 2026-09-10 | Replaces the row under *An agent can write a probe that commits*, which claimed BOTH agent files carry the three shapes. `spec.md` records why that claim was always half wrong: the smith writes probes too and nothing said so anywhere, so the rule reached two agents of three  **Re-read 2026-09-10 in work item 1789034970 (#120).** §7's body gained the leavings paragraph, the #30 story and the ownership clause; its heading, which is this row's anchor, is untouched by design (see R3 of that item's fragment). The claim was re-checked against all four definitions: `agents/scribe.md` points at §7 and §8, `agents/warden.md` points at §8 twice, and neither `smith.md` nor `sealer.md` restates either rule. The definitions still point rather than restate, and `skills/code-review/SKILL.md` — which is not a definition — is the one place that states the probe rule, corrected by finding 1 in the same range. |
| L8 · the contract-override refusal stands ONCE, as §3, with the 28-minute case named in it | `skills/agent-contract/SKILL.md#"## §3 A spawn prompt cannot widen the verification scope"@207e4d1d`, `tests/test_broad_gate_rule.py#test_the_refusal_is_stated_once_and_names_both_directions@174490ef`, `tests/test_broad_gate_rule.py#test_the_prohibition_itself_has_one_home_and_it_is_the_contract@74eae03b` | **Executed**: both cases green at `5e610a3`, and the second is the one that would go red if a definition took a copy back | 2026-09-10 | Replaces the row claiming the refusal *stands in both agent contracts*, whose five smith coordinates and one warden coordinate all drifted when phases 3 and 4 removed the prose. The word that changed is *both* → *once*, and it is the whole point of #107  **Re-read 2026-09-10 in work item 1789034970 (#120).** The hash moved because this row's third coordinate, `test_the_prohibition_itself_has_one_home_and_it_is_the_contract`, was re-pointed from the orchestrator to the sealer. §3 itself is untouched: the refusal stands once, and the 28-minute case is still named in it. |
| L9 · §10 holds the batching rule and the serial-loop caveat; each definition holds only its own number | `skills/agent-contract/SKILL.md#"## §10 Batch independent reads and runs"@fdc474d5`, `tests/test_the_handoff_before_round_one.py#test_the_smiths_contract_does_not_demand_what_a_serial_loop_cannot_give@f69a6137` | **Executed**: the case asserts the caveat against the CONTRACT and the number against the DEFINITION, which is where each can go missing without the other noticing | 2026-09-03 | Replaces the row claiming the expectation is *stated where it pays and caveated where it cannot be met* in the two definitions. The split is deliberate: 1.89 judges a reviewer and 1.08–1.17 is what an edit-test loop gives, so the figure stays with the agent it measures |
| L10 · the `seal/…` root sentence is contract §16 and the skill's layout section, and the definitions gave it up | `` `skills/agent-contract/SKILL.md#"## §16 Where a `seal/…` path resolves"@bd939022` ``, `skills/implement/SKILL.md#"## Document layout — two roots, three lifetimes"@5fa9c9ee`, `tests/test_first_setup_asks_once.py#test_the_session_rule_appears_once@1b472f16` | **Executed**: the parametrised case asserts the sentence appears exactly once per file it belongs in | 2026-09-16 | Replaces row Q1 of work item 1788398967, which claimed the sentence sits *once at the top of each agent*. Phase 1's divergence row holds why §16 exists at all: §11 sends an agent to `config.md` under the `seal/` root and never says where the root is, so the contract could not be followed as it stood  **Re-read 2026-09-10 by work item 1788993115 (#292)**: the layout section lost the Bootstrap subsection from under its heading (phase 2), which is what moved the hash; the `seal/…` root sentence is still in it once, and `test_the_session_rule_appears_once` is green **Re-read 2026-09-22** by work item 1790027178 (#458), which reworded that section's `docs/` row. The root sentence is still there, still says `<repo>/seal/` where it exists and `$(git rev-parse --git-common-dir)/seal/` otherwise, and only the whole-section hash moved. **Re-read 2026-09-24 by work item 1790208593 (#547), phase 5:** the layout section gained the `releases/` entry; the root sentence is unchanged, so the claim holds |
