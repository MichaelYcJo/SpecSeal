# Follow-up

**What belongs here, and it is a narrow list.**

A **schedulable item in a repository with no tracker** — something a person
could plan, in a project that has nowhere else to file it. This repository has
a tracker, so it should normally hold none of those.

**Every row names a person, with no condition attached.** An answerer column
reading `repository owner, next time X is opened` is a
condition wearing a person's clothes: nobody agreed to open `X`, so nobody
answers. A row that
cannot name who answers it does not belong in this file.

**Anything tied to a coordinate is a `# RIDER:` comment at the line it is
about.** `grep -rn "RIDER:"` is the repository-wide list, and it needs no file
kept in sync. A rider is not "someone should do this" — it is *"if you open
this file, do this too"*, and its whole value is arriving at the person who
opens the file. Moving it to the coordinate is what makes `next time X is
opened` do its job instead of being written down as though a person had
promised it.

An issue is no better for that purpose. It reaches whoever browses the
tracker; nobody greps the issue list before editing a hook. And a rider has no
completion of its own — it closes when the file it rides on is next changed,
which nothing can schedule.

**What that costs.** Nothing forces a rider to be deleted, so a comment can
outlive the fix it asked for and the next reader cannot tell a live one from a
spent one. Each therefore carries **the date it was read and the content it
was read against** — `Verified <date> against <anchor>@<hash>`, where the
anchor is the ledger's own: a symbol name, a heading path, or a quoted line,
resolved in the rider's own file. `.github/scripts/rider_check.py` checks
every one and `--reverify` re-stamps them; the path the ledger writes is left
off because a rider IS the coordinate. The judgment is that a rider outliving
its fix costs a confused reader for a minute, while a rider nobody ever sees
costs the defect. **This is written down so it can be overturned**: if the
stamps go stale faster than they are read, the trade was wrong and the list
comes back.

**A stamp used to name a commit, and this repository's merge rule destroyed
the commits it had to name** (#239). A fix pass runs on a feature branch, a
feature branch squashes into its release branch, and the squash keeps none of
the branch's own commits — so the check failed on the RELEASE branch, where
whoever met it was never whoever caused it, and no mistake was required
anywhere. `skills/evidence-check/SKILL.md` already cited that failure as one
of the four grounds for deriving a ledger anchor from content; this is the
same repair reaching the mechanism that supplied the evidence.

A drifted rider is **the rider firing**, not a chore: it says somebody edited
the unit and did not answer the comment sitting in it, which is the arrival
this whole arrangement is for. Read it, then either do what it asks and delete
it, or re-stamp.

## Schedulable items with nowhere else to go

| Item | Who must answer |
|---|---|
| `evidence-check` ignores a ledger row whose coordinate is malformed instead of naming it. Found while writing a row during round 1's fix pass of #237: a coordinate written `path#unit@0` — a placeholder hash that is not eight hex characters — produced **no row at all**. Executed: the total stayed at 771 with the row present, `--strict` reported `0 broken`, and `--reverify` reported `0 rows re-verified`; the same row with `@00000000` was picked up and re-verified on the next run. So a typo in a hash does not drift and does not break, it removes the claim from the ledger silently, which is the one direction a checker of claims must not fail in. The fix is to count a row whose coordinate cell does not parse and name it, the way an old-format row is named; the judgment it needs is whether that counts as BROKEN or as a fourth state, since a row nobody can resolve is not a claim that drifted. **A second trigger for the same silence was met and measured on 2026-09-13, writing the ledger fragment of #354: a MINOR anchor written as a bare identifier rather than a quoted line.** `ANCHOR_RE` accepts a bare name at the major level and only a quoted string after the `>`, so two rows written that way produced no coordinate at all — the run reported `3 ok` where five coordinates were present, `--reverify` rewrote three hashes and named neither missing row, and the exit code stayed 0. Quoting both minor anchors brought the count to `5 ok` with nothing else changed. So the hole is not one malformed hash: it is every coordinate shape a reader can write that the pattern does not match, and the two instances arrived from opposite ends of the row — one from the hash, one from the locator. That widens what the fix has to catch and does not change who decides what to call it. **A third shape was measured on 2026-09-13 during round 1 of #354, and unlike the other two it is already standing in the shared ledger**: a quoted locator whose inner quotes are bare rather than escaped as `\"`. `ANCHOR_RE` matches nothing across it, so the coordinate is dropped whole — **five rows of `seal/ledger.md` are anchored by nothing today while the run prints `1148 ok` for that file**, the rows citing `test_a_rider_reaches_its_file.py`, `test_what_the_reader_understands.py`, `chain_check.py` twice, and `hygiene.yml`. Executed twice over one method, by the reviewer and again in the fix pass that answered the finding: the module's own `ANCHOR_RE` applied per ledger file against every backticked coordinate-shaped token in it drops seven, and two of those are path-less prose shorthand inside Notes cells rather than row anchors, which is where five comes from. The checker's own `1148 ok` for the shared file and `5 ok` for #354's fragment reproduce the 1153 it totals. So this silence has live instances rather than only a hazard, and the fix has three shapes to catch. No line numbers are written here on purpose — this file sends anything tied to a coordinate to a `# RIDER:`, and `CLAUDE.md` says a row names content and never a position, so the five positions rot while the shape does not | the repository owner |
| Should `chain_check.py --worktree` read routing declarations from the WORKING TREE, so `round_record.py`'s own chain-check reports *declared* in local mode instead of *examined nothing*? Work item `1788817289-local-mode-from-first-setup-to-the-gate` took the notice only (#225, Q3 of its `questions.md`), and the argument is in that item's `spec.md` §*The sharp question*. It is not unsafe on the axis `read_record`'s docstring warns about — finding a declaration makes the check demand round records, so reading the working tree there is STRICTER than reading HEAD. What is against it: the local run would then assert a chain verdict CI can never reproduce, which is a second guarantee the tree has to keep true, and a declaration is committed before the first edit by rule, so an uncommitted one is not the transient state that flag was built for | the repository owner |
| Bring `agents/smith.md` and `agents/scribe.md` under `tests/test_docs_line_wrap.py`, together. `spec.md` of work item 1788433011 put this out of scope as a sweep at 148 and 160 columns. Both are now measured, with that test's own rules, at phase 4 of that item (2026-09-03, `4b85d80`): the scribe has **one** prose line over the limit (`:22`, 160 columns) and the smith **two** (`:22`, 148; `:95`, 109) — three lines in total, which is a rewrap and not a sweep. The size argument is therefore spent, and what is left is the one that decides it: adding a path to `COVERED` changes what a test guards, which `CONTRIBUTING.md` asks a separate argument for, and covering one definition and not the other reads as an oversight. So the two go in one change, with that argument | the repository owner |
| **Writing a `survivors.md` row silences its survivor a SECOND way, and that way does not rot.** An exemption row quotes the standing text, so once the commit adding the row is inside the range, `corrected` reads that quote as wording the range ADDED and `wanted` subtracts its n-grams from what is looked for at all. Executed during round 1's fix pass of #297: over `7355201..a18754c` the check reported **eleven** places before the file existed, and **one** afterwards with no `--exempt` flag passed — ten survivors went quiet through the diff rather than through the exemption. What that costs is the property the escape is built on: the quote is the anchor so an exemption *degrades to reported again* when the standing text changes, and this path has no anchor to degrade — it silences whatever the row's words match, whether or not the row still holds. The candidate fix is to leave `seal/specs/*/survivors.md` out of `corrected`'s path list, the way `rounds/` is left out by construction: an exemption file is a judgment about the range, not prose the range wrote. **What needs a person is whether that exclusion is right** — a branch may legitimately edit prose in the same commit as its exemption file, and by-construction exclusions are what this check's own docstring argues for over lists. **The `rounds/` half of that sentence was corrected by #365, which is where the comparison now points**: `rounds/` was out of the corpus and in the range until 2026-09-12, so the fix this row proposes had no completed instance to be modelled on when the row was written. It has one now, on both sides of the list. **Q1 of that work item asked a person whether `survivors.md` should join it and the answer was *out*, so this row stays open and is tracked as #371** (closed; see the correction at the end of this row), which carries #361's round 3 two-run measurement and the argument against. **#371 and #308 are the two halves of one fix and should be scheduled together**, which neither ticket says: #371 is the RANGE half, where the row's quote joins `corrected`'s added n-grams and `wanted` subtracts it, and #308 is the CORPUS half, where the same quote joins the pool as a further carrier, raises `df` and drops the score under the floor before `--exempt` is consulted. Leaving `seal/specs/*/survivors.md` out of both is one change; landing either alone leaves the row silenced by the other path. Found in round 1's fix pass of #365, which had no scope to touch either. A fix pass may not add mechanism, which is why this is a row and not a commit (`skills/code-review/orchestration.md` §*A fix pass adds the unit that pins it*). **The eleven-to-one figure is from a range no gate runs, and round 2 corrected it**: over `origin/release/v0.9.5...090bbd0`, which IS what CI runs, the check reports 41 removed sentences and **no** survivors — with the four exemption files, without `--exempt` at all, and with `survivors.md` deleted from HEAD so it cannot be on the added side. So the mechanism is real and its reach at the gate is unmeasured, which is what a person has to weigh the exclusion against. **#371 is CLOSED as completed and the half it describes reproduced live on 2026-09-18**, in round 1 of work item `1789687448-a-tracked-file-the-tree-deleted-stops-the-sweep`: a `survivors.md` written that day quoted the phrases the check had matched, and over the branch's own range the check then reported no survivor **with no `--exempt` passed at all** — exit 1 before the file existed, exit 0 after it, and no `exempt` line either way. The branch stopped reproducing the phrases and the contract held again, so what is left is the class. The open home is #308, and this row says *tracked as #371*, so a reader meeting it today is told a live defect is done. Whether #371's closure was meant to cover the RANGE half is the judgment, and it is the owner's; the measurement is not | the repository owner |
| **Five more scripts die below the supported floor with a bare traceback, the way `round_record.py` did before #226.** Enumerated by construction in `seal/specs/1788789985-round-record-dies-on-python-3-9/spec.md` §*The class, enumerated by construction*, which is where the file and line of each one lives, and re-enumerated on every suite run by `tests/test_a_script_says_which_interpreter_it_needs.py#test_no_shipped_script_needs_more_than_the_floor_without_saying_so`, so this row is a decision waiting rather than a fact that can go stale. **No coordinate is repeated in this row, on purpose**: the rule above sends anything tied to one to a `# RIDER:` at the line, and `tests/test_a_rider_reaches_its_file.py#test_no_schedulable_row_carries_a_coordinate` enforces it — the first version of this row carried five and turned that case red, which is the rule catching exactly what it was written for. `gather_changelog.py` and `fold_ledger.py` use `datetime.UTC` (3.11) and are named after a literal `python3 ` in `CONTRIBUTING.md` and `docs/release-checklist.md` §3, so a release run on a 3.9 or 3.10 `python3` fails at the moment it writes the dated heading. `session_cost.py` is the same construct spelled behind `import datetime as dt`, and it ends the run report `docs/review-handoff-protocol.md` §*After a run* points at. `seal.py` is the same construct and **was not touched because another work item in this release holds that file**. `root-migrate.py` uses `zip(..., strict=True)` and is reached through `hooks/hooks.json`'s session-start dispatch, so it fails while migrating a 0.3.x layout, in a hook, where nobody is reading. The fix is fifteen lines each, copied from `skills/code-review/scripts/round_record.py#below_floor`, which was written to be copied and says so. **What needs a person is not the fix, it is whether these belong in the tracker instead**: this file's own opening says a repository with a tracker should normally hold none of these, and the handoff for #226 asked for them here | the repository owner |
| **`hits()`'s wrap-awareness is anchored in the ledger and pinned by no case, and the rule that stops this run adding one is the depth rule.** `tests/test_a_release_is_sized_by_a_criterion.py#hits` searches each line and each line joined to the one after it, which is the whole reason the sweep sees a statement whose wrap falls inside the phrase. Measured in round 2's fix pass: deleting the joined-line branch leaves the module green at 7 passed, so nothing in the tree distinguishes the wrap-aware reader from the line scan it replaced. A case would close it — round 2's report drafted one and measured it red under exactly that mutation. It was refused rather than dropped: `hits()` was created by **round 1's fix pass**, so a case pinning it is depth 2, which `skills/code-review/orchestration.md` §*A fix pass adds the unit that pins it, and that unit ships unreviewed* refuses. The two cases that DID land in that pass are depth 1, because `STATES_A_SIZE` and `SCANNED` came from a build phase. **What needs a person is only whether to plant the drafted case**, which is one commit outside a run; the measurement and the draft are in `seal/specs/1789172128-a-release-is-sized-by-a-count-and-cut-by-urgency/rounds/round-2-report.md`. No coordinate is repeated here beyond the anchor the ledger row already carries | the repository owner |
| **The refusal for an unparseable `Broad gate` cell tells a person what to write, and no case pins that half.** `chain_check.py`'s new arm refuses a cell above the cutoff that carries no SHA-shaped word, and the message's second half — what a person should put there instead — is asserted nowhere. Its sibling in the same commit, the divergent-SHA notice, IS pinned on the phrase `different line of history`. A refusal is read by whoever is stopped by it, so the half that says what to do is the half that has to survive a reword. Found by round 2 of `1788912166-red-for-following-the-documents-green-for-ignoring-one`; a case is mechanism a fix pass may not add after a run has ended. **The rider clause this row first carried was wrong and is corrected here by whoever wrote it**: it named `session_cost.py`'s anchors — `#report_spawns` and `#analyse` — for a coordinate that is in `chain_check.py`, carried over from #145's round 3 without re-checking which file was being talked about, and the count was short too (four rows anchor at `#analyse` across the two ledger files, not two). Whether a `# RIDER:` at `chain_check.py`'s refusal branch drifts anything is unmeasured, and it is one `evidence-check` run to find out | the repository owner |
| **`deferred #N` is a third `Status` value in `templates/sdd-plan.md`'s Phases table, and the argument for it lives inside one work item.** The template says *Status is empty, or the commit that closed the phase*, and refuses a tick and the word `done` because both can be typed without anything having happened. #84's `plan.md` row 5 carries `deferred #350` and argues that an issue number is the same kind of thing a commit is — a past state somebody can open — which is the rule's own stated reason rather than an exception to it. This repository already writes `deferred #N` in every round record's fix table, so the vocabulary is not new; what is new is a phase row using it. **What needs a person is whether the template gains the third value** — the next work item reads the template and not #84's plan, so a session meeting a deferred phase today either invents a spelling or leaves the row empty, which asserts the phase is open. Against it: a value the template blesses is a value that can be typed as easily as `done`, and the thing that makes `deferred #350` checkable is an issue that exists, which the template cannot require. Raised by round 1 of #84 as a finding with a sound argument and no home; a fix pass may not add mechanism, which is why this is a row and not a template edit | the repository owner |
| **Seven `seal/ledger.md` rows are anchored on a whole heading path, and the measurement that `CLAUDE.md` makes the condition for narrowing them now exists.** That file says to cite the unit and leave the minor level off *unless whole-unit hashing has been measured to drift rows on unrelated edits*. Work item 1789296300 rewrote one step of `skills/verify/SKILL.md`'s measure-the-segment section and drifted all seven rows anchored on it — two of them, F5 and R3, make claims about paragraphs that branch never touched, so both were re-read and re-stamped for an edit that could not have reached them. `CLAUDE.md` already offers the narrowing, `path#major>minor@hash`. **What needs a person is whether to spend it**: a minor anchor is stale-widens-to-the-unit by design, so narrowing trades a drift a reader must dismiss for a re-read the checker asks for later, and five of the seven rows belong to work items that shipped in earlier releases and would be re-anchored by somebody who did not write them. Found by round 1 of 1789296300, whose fix pass re-stamped the seven and had no scope to re-anchor them — and which is the second branch to pay this, the memory of 0.11.2 carrying it as *the pinning case whose unit was one altitude too high* | the repository owner |
| **A subagent's span median was published at about a thousand seconds and that figure is the mean.** Work item 1788908215 (#145) took it from three orchestrator transcripts before anything could read a segment's own file, and it reached the shipped script twice, the `seal/ledger.md` row about how the harness writes an `Agent` result, and that work item's own records. Measured 2026-09-13 with the per-segment mode over every segment row of the 43 runs with a `subagents/` directory on this machine: the median is 716 s over 381 named rows and 664 s over all 433, while the mean is about 1,020. Round 1 of 1789296300 corrected the three places its own branch had added and disclosed the rest at `measure_segments`, which is as far as a fix pass reaches. **What needs a person is the ledger half**, because the others are #145's records and hold what was true when they were written: the evidence cell of a row in the shared ledger asserts the figure as verified, that row cites code this branch does not remove, and `CLAUDE.md` sanctions touching the shared file for a removal and not for a correction. Whether the same re-derivation is owed to the `1.5-3.7 seconds across 67 spawns` range in the same cell — 0.4 to 26.3 s over 302 spawns today — is the same question and should be answered with it | the repository owner |
| **The third routing axis's template pin is self-consistent by construction and cannot see the two files drift.** `tests/test_waiver_decided_at_start.py#test_the_template_PARSES_into_the_three_answers_it_ships` substitutes each value of `IMPLEMENTATION_ANSWERS` into the template's own `Implementation` row and parses it back — so renaming the constant renames the substitution with it, and the case stays green while `templates/sdd-routing.md` goes on offering a word the parser no longer accepts. A session filling the row in as the comment instructs would then record nothing, silently, in every work item that copies the file. Executed 2026-09-11 in #84's phase 4 on the FOURTH axis, where the same shape was built and then measured: renaming `BY_FRAMER` with the template untouched left the substitution case green. The repair there was to read the two answers out of the comment a person follows and compare them to the constants, which is red from either side, and the same four lines carry over. **What needs a person is whether it is worth a work item of its own** — the third axis's comment is longer and states its vocabulary twice, so the regex that reads it is not the fourth's, and #84's phase 4 has no row for the third axis | the repository owner |
| **`evidence-check` reads a ledger row written inside a code fence as a live row, which is #429's defect in a second reader.** Q1 of `seal/specs/1789721571-the-gate-reads-an-example-and-names-rows-nobody-wrote/questions.md` asked whether this repository's other markdown-table readers share the blindness `hooks/config.py` had. Measured 2026-09-18 with a scratch root whose `ledger.md` carries a fenced ```markdown block holding one example row above the live table, exactly as a file documenting its own format would: the run reports `BROKEN  nosuchfile.py#nothing  file not found` for the example's coordinate and exits 2. So a ledger whose file explains its own row format fails the broad gate over a row nobody wrote, and the `--reverify` writer would rewrite a hash inside somebody's example. The reader already tracks fences elsewhere — it distinguishes ``` from ~~~ and treats an unclosed one as a malformed record — so what is missing is the fence rule in front of the ROW walk, which is the shape #429 put into `hooks/config.py`. **What was NOT measured is the other two readers Q1 names**, `round_record.py` over a review report and `chain_check.py` over a round record: both need a whole record chain to drive, where the ledger reader takes one file, and one measured instance was enough to answer the question Q1 actually asked. **The ticket is open as #444**, carrying both measurements and naming the two readers that were not driven; what is left for a person is scheduling it. This row stays until that ticket ships, because the two unmeasured readers are part of it | the repository owner |
| **`evidence-check`'s records arm reads a backticked name inside an HTML comment, and its own skill says it does not.** `skills/evidence-check/SKILL.md` §*What counts as a claim* states that a fence is a quotation and an HTML comment is an aside, and that neither is read. Measured 2026-09-21 in round 1's fix pass of #443, on this repository's own tree: a compound backticked name placed inside a single-line HTML comment that begins part-way along a markdown table row is refused `NOT-IN-TREE`, exit 2 — and the same name written bare on the same line passes. The fence half of that sentence holds, checked in the same run: the identical name inside a fenced block on the line above is not refused. So the aside rule either does not reach a comment that opens mid-line, or does not reach one inside a table cell, and the skill's sentence is unqualified. **Why it costs something**: the whole repair a session reaches for when the arm refuses a record is to move the name into a comment that explains it, which is what this work item's own `spec.md` did — it passes today only because the name there is written bare, and a later editor restoring the backticks inside that comment turns the pull request red with a document saying that cannot happen. The marker is the repair that does not depend on it, and round 1's three refusals now carry it. **What needs a person is which side is wrong** — widening the aside region to reach a mid-line comment, or narrowing the sentence to say a comment is an aside only when it holds the whole line. The first is a change to what a gate reads and carries `CONTRIBUTING.md`'s higher bar; the second is one sentence. A fix pass may add neither, and this is outside #443's scope either way | the repository owner |

| **`evidence-check`'s drift report names a coordinate once and `--reverify` re-stamps every row that cites it, so the documented repair writes *somebody read this* over rows nobody opened.** Measured 2026-09-21 while building #468, on this repository's own tree: the lenient run reported `1 drifted` for `seal/ledger.md` against one changed unit, and `--reverify` then reported `5 rows re-verified` across the two files — three rows of the shared ledger cite that one unit, and the reader had opened one of them. The rows are S7, S12 and the `not_as_written` row; all three claims turned out to hold, and all three were read and marked by hand before the branch closed, so nothing is standing re-stamped-but-unread today. **Why it costs something**: `CONTRIBUTING.md` §*House rules* and `CLAUDE.md` both send a reader whose anchor drifted to *re-read it and run `evidence-check --reverify`*, and following that instruction exactly is what produces the unread re-stamp. A stamp is the one thing in the ledger that cannot be checked against anything — it asserts an act of reading — which is the same direction as this file's three existing rows about the checker's silences: the failure is always that a claim leaves the ledger, or enters it, without a reader. **What needs a person is which side moves**: the report could name every ROW rather than every coordinate, which is a change to what a gate prints and carries `CONTRIBUTING.md`'s higher bar; or `--reverify` could refuse a coordinate carried by more than one row until each is named, which stops a release step; or the two documents could say *read every row that cites it*, which is one sentence and leaves the count in the reader's hands. Not measured: whether the other checkers that walk the same table — `correction_check.py`, `round_record.py` — deduplicate the same way | the repository owner |
| **`CHANGELOG.md` §0.12.2 carries a sentence whose three work-item copies now carry an evidence label, and the released copy does not.** Work item 1789996775 (#464) added *read, not executed* and an answerer to the baseline half of the direction claim — *the two baselines move the same way, a baseline carried forward drops the rows the base's own newer commits added* — in `seal/specs/1789956662-…/changelog.md`, `plan.md` §*Operational impact* and `overview.md`. The fourth copy stands in the released `CHANGELOG.md` §0.12.2 and is the one a reader outside the work item actually reads. It was left alone deliberately: `CONTRIBUTING.md` §*House rules* says one branch edits that file and it is the one based on `main`, and that branch was not it. **Why it does not close itself**: `gather_changelog.py#ungathered` decides by marker and never by content, so the corrected fragment is never re-gathered and the two copies diverge with nothing able to see it — no checker compares a gathered section against the fragment it came from. **What needs a person is whether a later release branch carries the label into §0.12.2**, which is an amendment to a released record and reversible only by another one, against leaving a true sentence unlabelled where most readers meet it. The full argument, both readings of policy and the default that was built are Q1 of `seal/specs/1789996775-the-gate-states-what-its-own-fixes-disproved/questions.md`, and the divergence is a row of that work item's `overview.md` §*Not verified* | the repository owner |

| **The released `CHANGELOG.md` §0.12.2 states that `seal/ledger.md` carries *404 marker occurrences on 190 rows*, and the corrected fragment beside it now says otherwise.** The figure is false in its own terms — 404 is the file's total and 401 of them stood on the 190 rows, the other 3 being in prose the survival test never reads — and work item 1789996780 corrected every present-state site except this one. It is left because `CONTRIBUTING.md` §*House rules* says one branch edits `CHANGELOG.md` and it is the one based on `main`, which the branch that found this is not. **Nothing can see the divergence**: `gather_changelog.py` decides by marker and never by content, so a fragment corrected after it was gathered is never re-gathered, and the released copy and the fragment simply drift apart. The next release branch based on `main` can carry the one-line correction. **This is the same decision work item 1789996775 left open about a different sentence in the same released section** — one answer settles both, and both are Q1 of their `questions.md` | the repository owner |

| **Nothing enumerates the class *a figure about a corpus, stated with no moment*, and every hand enumeration of it has shipped short.** The record across one lineage: review round 3 of #424 listed six sites and the list was three short; work item 1789996780's phase 3 grep found those three and missed four more that `survivor-check` then caught; its phase 4 widened the module's own rule and still left two, which review round 1 found — one of them a sentence asserting that no such site remained. Each enumeration was a grep, and **a grep matches a spelling while the class is a kind of claim**: `1.07 MB`, `0 of 189`, `185 rows … against 10` and `all 1520 lines` share no token with `404 ... on 190 rows` or with each other. What would close it is a checker that reads a figure-shaped token and asks whether a corpus, an instrument and a moment stand near it — mechanism, which a fix pass may not add and which needs its own argument about false positives before anybody builds it. Reported by round 1 as the proportionate answer to findings 1, 2, 3, 7 and 8, all five of which are the same class at a different coordinate | the repository owner |
| **Nothing in this repository measures the width of a comment line in a shipped script, and the two instances found so far were both found by a person reading.** `uvx ruff check` and `ruff format --check` do not: `E501` is not selected in `ruff.toml`. `tests/test_docs_line_wrap.py` does not: it reads markdown, not a `.py` comment. Review round 2 of work item 1789996780 found a comment line at 138 characters in a block that wraps at 75, ending mid-sentence; review round 3, one round later, found a second at 90 in the same block, four lines away, blamed to 2026-09-01 and in no line of that branch's diff. **What needs a person is whether the class earns a check**, and the question is not either line: a comment block states its own wrap by example alone, so a reader reflowing one line has nothing to learn the band from, and both instances were caught by a reviewer's eye rather than by anything that runs. Against it: selecting `E501` reaches every line of every shipped script at once, which is a repository-wide reflow with its own argument to make, and a check that measures comment lines alone is mechanism nobody has asked for yet. Named under `agent-contract` §12, which asks for the class rather than the coordinate — and the coordinates are deliberately not repeated here, because a row tied to one belongs in a `# RIDER:` at that line and `tests/test_a_rider_reaches_its_file.py#test_no_schedulable_row_carries_a_coordinate` is what says so. Both are in work item 1789996780's round 2 and round 3 records | the repository owner |
| **A `seal/ledger.md` claim can be falsified by code a branch ADDED, and neither document that routes a drifted row has an arm for it.** `CONTRIBUTING.md` §*House rules* gives two, and `CLAUDE.md` carries the same pair: the claim still holds, so re-read and `--reverify`; or the claim went with the code, so remove the row and rewrite it in your own fragment. Both are conditioned on the cited code CHANGING. Measured on work item 1790076050: a row claimed *`size: now` … and **nothing reads it***, that branch added a workflow step that reads it, and the row's two anchors both still resolve — `evidence-check` reports no drift in the claim's direction at all, only a hash move from an unrelated edit to the same unit. Neither arm names the case. The act taken was the second arm's, on the ground that `--reverify` asserts somebody read the claim and found it TRUE, so following the first arm writes a lie under a fresh hash. **What needs a person is whether §*House rules* gains a third arm** — *the claim went false, however it went false* would subsume both existing ones and is one sentence; against it, the two arms are phrased around the code because the code is what a checker can see, and a rule keyed to a claim going false is a rule only a reader can apply. Found by round 1 of 1790076050, whose fix pass corrected the grounds its own phase record cited and may not change policy | the repository owner |
| **`console.to_utf8()` is owed by an entry point and nothing says which files are entry points, so 6 of the 13 `.py` files under `.github/scripts/` call it and 7 do not — every one of the 13 carrying a `__main__`.** Measured 2026-09-22 at `11f3cde8`, and re-derived at the same commit by the session that wrote this correction rather than carried from the round that reported it; the same corpus was 5 and 8 before that commit's fix pass, and 3 and 7 of 10 files at the release branch this work was cut from. `hooks/console.py`'s docstring says the call belongs at *each entry point*, and `tests/test_console_is_not_utf8.py` enumerates `hooks/` only — so no check reads `.github/scripts/` at all, and the tree gives a session no answer either way. The cost is not theoretical: under cp949, cp932, cp936 or a bare `LC_ALL=C`, a `print` carrying an em dash raises `UnicodeEncodeError` and the process dies with stdout empty, which is how a gate says *nothing to see here*. Round 1 of work item 1790076050 found one such script it had just added (`tracker_labels.py`, whose exposed arm is the `--check` a person types) and fixed that instance; the 7 standing non-callers all predate that branch and are outside any one branch's scope. **The figure in this sentence was wrong when it was written** — *five of nine … and four do not*, copied from the round's own deferral cell rather than re-derived, with no denominator that produces nine and no moment attached. Round 2 caught it, and it is the class the row above this one enumerates, one file over from where that row was raised. **What needs a person is whether the rule is *every module with a `__main__`* or something narrower**, and whether the existing case's corpus widens to `.github/scripts/` — which is a change to what a gate reads and carries `CONTRIBUTING.md`'s higher bar. A fix pass may not add mechanism, which is why this is a row | the repository owner |
| **The guard that stops an answerer from being a condition tests one spelling, and the rule it guards is a shape.** `tests/test_a_rider_reaches_its_file.py#test_the_header_stops_the_answerer_that_is_really_a_condition` asserts that one two-word phrase — the one the file's own header uses in its example — does not appear anywhere in a row of §*Schedulable items with nowhere else to go*. The header it enforces forbids an answerer with **any** condition attached, and only that one phrasing is caught: *at the next change to X*, *when X is reopened*, *whoever gets to X first* and *after the following release* all pass it. Measured 2026-09-22 at `11f3cde8` — that module is green, 29 passed, against a row whose answerer read *the repository owner, at the next change to `CONTRIBUTING.md` §House rules*, which is the header's own example in a different spelling. Round 2 of work item 1790076050 found it; the instance is corrected and two answerer cells of this file now name the owner flat. **A second measurement, from writing this row**: the guard joins the WHOLE row and searches that, so it does not distinguish an answerer cell from prose — a first draft of this row quoted the guard's own literal while describing it and went red, in a row whose answerer was already unconditional. So the check cannot be discussed in the file it guards. **What needs a person is whether the guard reads the class**, which is `agent-contract` §12 applied to a checker: a shape-reader means deciding what counts as a condition in prose a person wrote, and the obvious widenings — any comma in the cell, any future-tense clause — refuse honest rows. Against leaving it: this guard exists because seventeen rows once carried the defect at once, so the spelling it tests is the spelling of a defect already repaired, and the next one will be worded by somebody who has not read #34. A fix pass may not add mechanism, which is why this is a row | the repository owner |
| **Three cases assert what a script may invoke by searching its folded source, and a fold normalises whitespace and nothing else.** The pattern is `folded = " ".join(read(SCRIPT).split())` followed by a forbidden-verb list — `assert '"gh", "issue", "<verb>"' not in folded`. It survives the argv being wrapped one word per line, which is why the fold was introduced, and it does not survive a comment between two of those words. **Measured 2026-09-22** in round 2 of work item 1790076050: with `gh issue edit`'s argv rewritten across lines with one comment between its words, the folded count of that literal reads **0**, and the case built on it exited 0 while the script assigned a person on every label removal. That instance is repaired — `tests/test_release_hygiene.py#test_the_script_closes_and_takes_off_one_named_label_and_nothing_else` parses with `ast` now and searches nothing — and the same folded reader still stands in `tests/test_a_release_cannot_ship_an_untrue_milestone.py#test_the_gate_writes_nothing` and `tests/test_a_merged_ticket_says_so_on_the_tracker.py#test_the_script_only_ever_creates_a_label_and_adds_one`, about two other scripts. **What converting them would buy, stated at what it actually is.** An earlier wording of this row said the fold does not survive *a value moved into a constant, or the words being spelled through a variable*, and implied the `ast` reader closes those. **It does not, and round 3 measured it both ways**: four spellings of a planted `gh issue reopen` against the converted case — plain and comment-separated both red it; the first word moved into a module constant, and the verb moved into one, both leave it green. `words` keeps only `ast.Constant` strings and the comparison is against the first three of them, so a name anywhere in those three positions shifts the prefix and the verb is never matched. So converting buys **layout immunity and nothing else**: the argv wrapped, commented, or re-spaced in any way stops being an escape, and a value that is not a literal remains one for the converted reader exactly as it does for the fold. **Only the FORBIDDEN half is weak either way**: a positive assertion that a string is present fails when the spelling moves, which is the safe direction, so what is at risk is exactly the *this script may not do X* claims. **What needs a person is whether the two remaining cases are converted for that benefit**, which costs an `ast` reader in each of two modules that had no finding against them — and, separately, whether the constant-valued escape is worth closing at all, which means resolving module-level names before comparing and is a different checker with its own false-positive argument to make. A fix pass may not add mechanism and had no scope for the other two, which is why this is a row | the repository owner |

## Riders waiting on a file another branch holds

| Target | Who must answer |
|---|---|
