# 1788597030-a-runs-rounds-come-mostly-from-the-tools-own-fixes-and-records — overview

📋 implement applied
· spec:     CLAUDE.md §The goal a design is chosen against; docs/review-chain-spec.md §The review run has a bound … §The last round verifies, §The reopening — one, and then the run is capped, §The cycle; docs/review-handoff-protocol.md §round-N.md, §Loses a record or crashes, §The handoff before round 1 and its subsections; templates/sdd-round.md; templates/sdd-phase.md; skills/code-review/SKILL.md §Orchestrator sections and §Findings format; agents/warden.md §Role, §Report; agents/smith.md :100-200; skills/implement/SKILL.md §2, §5; docs/flow.md §Order inside a ticket; issue #161 body and three comments; the last item's rounds/round-4.md and round-12.md
· evidence: `seal/ledger/1788597030-a-runs-rounds-come-mostly-from-the-tools-own-fixes-and-records.md`, eight rows (R1–R8), every hash stamped by the scoped `--reverify --ledger '<fragment>'` and never by an unscoped run; the unscoped read ran after and its lines are in `phases/phase-5.md`
· verified: executed — the inter-commit measurement over 3ef565d^..pull/162/head (70 commits, 12.8 h before docs commits, 3.6 h before fix commits, gaps capped at 2 h); phase 2's 20 cases and the 801 across every module reading the generator or the two agent files; 48 mutations over `close`'s units (phases/phase-2.md); phase 3's 37 cases and the 387 across every module reading the checker, the generator or the spec's review arm; 9 mutations over the walk and the vocabulary, 9 killed (phases/phase-3.md); phase 4a's 40 sentence mutations over the three owner files, 0 alive, and the 957 across the 24 modules reading those files (phases/phase-4a.md); phase 4b's 24 sentence mutations over the five linking carriers and `templates/sdd-phase.md`, 0 alive after one pin was re-pointed, and the 40 modules reading the six files before (1415) and after (phases/phase-4b.md); phase 5's three cases seen red first, the generator's two suites (60) and the three modules reading the skill (80), six mutations over `landing_values` and the `close` write, none alive, and the four gates named in `phases/phase-5.md` with their output; read — everything above. **unverified** — the broad gate; see below

## Why this work exists

The last branch spent 12.8 of its 18.8 active hours in front of record commits and half its findings were located in records, so the record is generated here rather than written, and the loop that reviewed the tool's own paperwork loses its habitat.

## Where spec and implementation diverged

| Divergence | Spec says / code did | Chosen | Grounds |
|---|---|---|---|
| The landing value of `Fixes checked by` | Spec: *`Fixes checked by` at its landing value* — one value, `nobody — the fixes are not yet written`. Code: that value when a verdict is open or closed on a fix word; `no fixes to check` and a bare `none` in both surface rows when every verdict closed without one | code | `chain_check.py#fix_surface`: *"`no fixes to check` beside a pending row, which the TERMINAL record of every run carries. There the pair is not merely unrefused but wrong: a round that commissioned no fixes will never have any, so *not yet written* is false the moment it is written"*; and `checked_by` refuses `nobody` beside a checked `Pass` on the last record, so the spec's own scenario *A capped run has a legal end* needs `no fixes to check` written by the generator |
| How the check is told the pull request's state | Spec: *runs `chain_check --worktree` on the work item before it returns*. Code: the same, with a `draft` event payload handed to the check through `GITHUB_EVENT_PATH` unless `gh pr view --json isDraft` says the pull request is ready | code | `chain_check.py#pull_request_state`: outside a workflow the state is `unknown` and *judged as* ready, where an unchecked `Pass` is an error — every mid-run record has one, so the check the spec asks for would exit 1 on every round that found something. `phases/phase-1.md` carries the reasoning |
| Which findings the fix table must cover | Spec: *one row per finding of round N's verdict table*, and *a finding of round N left with no row* is refused. Code: a row for every finding whose verdict is OPEN; a finding the reviewer closed in the report (`withdrawn`, `not a defect`) needs none, and a row for it is refused — it was applied if given until round 1's 🟡 3 | code | a fix pass has nothing to say about a finding the reviewer closed, and a row there would overwrite the reviewer's verdict with the smith's — `test_a_finding_the_report_already_closed_needs_no_row` and `test_a_row_for_a_finding_the_reviewer_closed_is_refused` pin both halves; `phases/phase-2.md` carries the reasoning |
| Which comma the surface rows refuse | Plan: *a case feeds it a comma inside a unit name and sees it refused*, and phase 1's `cell` refuses a comma anywhere in `Contract changes` and `New units`. Code: a comma inside a NAME is refused (`part`), and the comma between call sites is written by the writer, because `fix_surface`'s own grammar is `unit → site, site` | code | the two are right about different commas — the one between sites is structure the writer adds and the one inside a name is the prose the refusal exists for; `cell` keeps its refusal for a string that arrives whole and phase 1's comma case stays green (`phases/phase-2.md`) |
| Who writes the capped end's `Fixes checked by` | Plan, row 2: *`Fixes checked by` left as it stands* for `new` of the next round to set. Spec, scenario *A capped run has a legal end*: the last record reads `no fixes to check` and the check passes — and a capped run has no next round | code, in phase 5 | phase 3 probed it: `close` over a table of only `deferred #N` rows ticked `Pass`, left the cell at `nobody — the fixes are not yet written`, and the check it runs exited 1 refusing that pair on the last record. `questions.md` A6 stated the two ways to close it; `landing_values` — the derivation `new` already made, now one unit both subcommands call — writes `no fixes to check` when the verdicts the table left closed on no fix word, and leaves the cell alone otherwise. `test_a_capped_runs_last_record_reads_no_fixes_to_check_and_the_check_exits_zero` was red at `3320945` and is green at `edda427` |
| What the surface rows read on a capped end | Phase 5's spawn prompt: *derive `Fixes checked by` = `no fixes to check` and the two surface rows = bare `none`*. Code: the checker cell is derived; the two surface rows keep the measurement `close` makes from the range on every path | code | a bare `none` is what an empty range measures to, so the capped case the prompt describes reads `none` either way (the case asserts it); a unit the range DID add beside a table of deferrals is still a unit nobody has reviewed, and `fix_surface`'s own words for the row are *a unit the fixes created has been reviewed by nobody* — a derived `none` over it would hide the one thing the row exists to show. Neither `checked_by` nor `fix_surface` refuses `no fixes to check` beside a measured surface |
| Where the derivation lived | Phase 5's spawn prompt: *the same derivation `new` already performs in `terminal_value` (read it and call it)*. Code: `terminal_value` reads the report's two `<label>:` lines; the derivation sat inline in `build` | code | §5 — opened before anything was built on it. Calling it meant extracting it: `landing_values` is the four lines from `build`, called from `build` and `close`, so there is still one spelling. It is a new unit at depth 1, named in the hand-back |
| Which files phase 4a's sweep reached | Plan, row 4b: the sweep for *the orchestrator writes the record* over the five linking carriers. Phase 4a found five copies in its own three owner files, which 4b could not reach | both — 4a corrected its five, 4b its three plus a template | a sentence saying the orchestrator writes a record cell became false the moment `new` existed, in whichever file it sat; the split by file group was for segment length, not for leaving false sentences a phase longer. 4b's grep then missed three more carriers, one of them `templates/sdd-phase.md` outside the five files, and corrected them on the plan row's own words, *every remaining carrier* (`phases/phase-4a.md`, `phases/phase-4b.md`) |
| What the rule-9 subsection cites | Phase 4b's spawn prompt: the subsection was to say *four phases were checked that way, each matching its hand-back*. Code: it says the work item was checked that way from its first phase and points at `spec.md` rule 9 | code | nothing in the tree records which phases the orchestrator re-ran, so the count had no coordinate — an aggregate is not a coordinate (`docs/review-handoff-protocol.md`), and a document citing one would be citing a session transcript |
| `docs/review-chain-spec.md:22`, *written by the review orchestrator* | Plan, row 4b, and the spawn prompt: reword to name `round_record.py` | the sentence stands | it is about the reviewed-HEAD mark `<git-dir>/specseal-reviewed`, which the orchestrator does write and the generator does not (`grep -n reviewed round_record.py` returns the `--target` help line alone); naming the generator there would make a true sentence false. The same reading kept `agents/warden.md` §Role's §6 bullet and the warden row of both READMEs (`phases/phase-4b.md`) |

## Not verified

| Item | Who must answer |
|---|---|
| ✅ `gh pr view` answering for a real pull request — the `#N — url` cell and the draft/ready read. Pinned by injection only; a scratch repository has no remote | round 1's record read `#168 — https://github.com/MichaelYcJo/SpecSeal/pull/168` and the draft state from `gh`, 2026-09-05 |
| ✅ `worktree_files` and `read_record --worktree` on the Windows leg: the path is rebuilt from `/`-split parts with `os.path.join`, which is what the rest of the checker does, and no case ran there | the Windows leg green at `a2d0494` and `da047ab` (runs 33964679997, 33965965021), the `--worktree` cases in the suite it ran |
| ✅ The full suite, the repository-wide lint and the typecheck after phases 1, 2, 3, 4a, 4b and 5 (§2). Phase 5 ran the generator's two suites, the three modules reading the review skill, and `ruff` on the two Python files it touched | the broad gate at `da047ab`: 2258 passed · 1 skipped with `-n auto`, `ruff check .` and `ruff format --check .` clean, 2026-09-05, in `rounds/round-3.md` |
| ✅ The count-rule ceilings phase 4a's walk asserts (4 and 0) after phase 4b's sweep of the five linking carriers, and rule 5's owner phrase in `agents/smith.md` under 4b's edit | measured at 4b's close: `grep -rn` over `docs skills agents templates` reads 4 and 0, and the rule-5 pin is green at `12c6db6` (phases/phase-4b.md) |
| The reopening walk on a real capped run, and `close` writing `no fixes to check` into its last record — the first is whichever 0.8.x chain caps first; every fixture here is built, and this repository's own records reopen at most once | the orchestrator, at the first `chain: capped` |
| ✅ `close` on the Windows leg: `git grep` output is split on `:` and paths in git's output are `/`-separated, which is what `call_sites` and `under_tests` assume; no case ran there | the same two green Windows runs, `test_the_fixes_close_the_record.py` among what they ran |
| ✅ `close` on a real fix pass of this branch — the first is this branch's own round 1 | round 1's fix pass: `close` over `c24344f..319309f` measured two contract changes and nineteen units, all depth 1, 2026-09-05 |
| The three-hour target — build plus two rounds for a change under three hundred code lines. This branch is not under three hundred lines and is the item that builds the machine, so it cannot be its own reading | the next three 0.8.2 items, under the `flow-measurement` label |
| Whether one round per session holds without enforcement | the same three items |

## Not done

**The verifying round as a probe re-execution script.** It needs a probe list in a runnable shape, which is a new parsed field, which rule 8's moratorium refuses; its value is unmeasured while the generator's is 12.8 h. 0.8.2 measures whether what shipped reaches the target without it, and the shape it would take — a fenced block under a probe row, `questions.md` A5 — is stated rather than built.

**Redesigning what a record carries.** The generator writes the record as it is; the essay under the table becomes optional and short because nobody is asked to write it. The moratorium holds the field set still until the next minor release.

**Enforcing one round per session.** Rule 7 is a sentence in `skills/code-review/SKILL.md`; whether it holds without a mechanism is read on 0.8.2's items, and it is a row above.

**#145, #155, #156, #160, #163** — 0.8.2's, and the first readings of this work's target: build plus two rounds inside three hours for a change under three hundred code lines.

**`close` writing `no fixes to check` into a capped run's last record** was here from phase 3 to phase 5 — `questions.md` A6 — and is built at `edda427`. Until that commit the capped end the spec describes was reachable only by hand-editing one cell after `close`; the row moves to the divergence table above.

**Four sentences the sweep for *the orchestrator writes the record* returned were left as they are**, because each is about the reviewed-HEAD mark `<git-dir>/specseal-reviewed`, which the orchestrator does write and the generator does not: `docs/review-chain-spec.md:22` under §The cycle (the spawn prompt for phase 4b and `plan.md`'s row 4b asked for it to name `round_record.py`, and that would be false), `agents/warden.md` §Role's §6 bullet, and the warden row of both READMEs. `phases/phase-4b.md` carries the reading, and the divergence table carries the decision.

**`templates/sdd-round.md`'s severity comment under the verdicts table reads without ⬜.** The skill owns rule 3 and a generated record carries no comment, so the line reads short rather than wrong — ⬜ under rule 3, fixed in passing or not at all, and it was not.

## Fed back into the spec

Every item below is *inferred during implementation*, and a planner may overturn it.

- `docs/review-chain-spec.md` §*The reopening — one, and then the run is capped* (phase 3). `questions.md` Q2 chose the bound; the subsection's table of what the check does per shape, the failure direction with what it lets through, and the vocabulary paragraph are this work's reading of what a rule owner has to carry under `CONTRIBUTING.md` §*What a change to a gate must carry*.
- The nine rules' owners (phases 4a and 4b), written into `spec.md` as the paragraph *Where each rule landed*. `spec.md` listed the rules and said one carrier owns each; WHICH carrier owns which — the spec for 1 and 4, the skill for 2, 3, 6, 7 and 9, `agents/smith.md` for 5, the spec's §*What the record carries* for 8 — is the implementation's, and the pin table in `tests/test_the_rules_have_one_owner.py` is the same assignment in a shape a change goes red against.
- Rule 9 itself, added to `spec.md` on 2026-09-05 at the owner's request after phase 1 was checked that way; `docs/review-handoff-protocol.md` §*After a phase — the hand-back's claim is re-run* is its link, and it cites the spec rather than a count of phases (the divergence table says why).
- `questions.md` A1–A6, each an assumption stated rather than asked: the non-Python heuristic (A1), the reports uncommitted (A2), this branch as the generator's first fixture (A3), `deferred <home>` as a verdict word rather than a `Needs a fix` value (A4), the tables copied and nothing else of the report (A5), and the capped end's cell derived by `close` (A6, built in phase 5).
- `spec.md`'s scenario *A capped run has a legal end* is satisfied by a derivation the spec did not name: `plan.md` row 2 left `Fixes checked by` to `new`, and a capped run has no `new`. The clause now reads as `landing_values`' docstring states it.
