# 1788597030-a-runs-rounds-come-mostly-from-the-tools-own-fixes-and-records — routing

| Axis | Answer |
|---|---|
| Review | through the review chain |
| Destination | open the pull request |
| Implementation | smith |
| Branch | feat/161-a-runs-rounds-come-mostly-from-the-tools-own-fixes-and-records |

Answered 2026-09-05 by the repository owner, before the first edit, in one
batch with the two design answers `questions.md` records.

## Why this way

#161 alone in 0.8.1, because it is what every later item runs under. The last
branch spent 12.8 of its 18.8 active hours in front of record commits, so the
lever is the record, and the record is generated here rather than written.

**This branch's own chain runs under the rules it adds, from round 1.** Each
round is told: a finding whose `Location` is a record owes a correction and no
round; a fix pass adds no mechanism; 🟡 is a defect the release would ship; the
floor's reopening is one. The last branch applied these from round 14 and
ended two rounds later.
