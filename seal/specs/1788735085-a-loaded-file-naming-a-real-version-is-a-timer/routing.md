# 1788735085-a-loaded-file-naming-a-real-version-is-a-timer — routing

<!-- seal/specs/<unix-epoch-seconds>-<slug>/routing.md — the answer given before the
first edit, in the batch the `implement` skill collects (§1). -->

| Axis | Answer |
|---|---|
| Review | through the review chain |
| Destination | open the pull request |
| Implementation | smith |
| Branch | fix/179-98-a-loaded-file-naming-a-real-version-is-a-timer |

Answered 2026-09-07 by the owner, before the first edit.

## Why this way

#179 asks which of three answers a repository gives when a loaded document
names a real version, and two of them change what a check refuses — that is a
policy judgement rather than an edit. #98 rides this branch because it is the
first one 0.9.0 opens, and its correction lands inside a hashed region two
ledger rows are anchored on, so the re-verification is a reviewer's to read.
The owner answered all three axes in one batch before the run began.
