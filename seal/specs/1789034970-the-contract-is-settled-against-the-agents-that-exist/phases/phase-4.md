# 1789034970-the-contract-is-settled-against-the-agents-that-exist — phase 4

| Field | Value |
|---|---|
| Phase | 4 |
| Commit | `c4e1bfc` |
| Ran by | specseal:smith on Claude Opus 5 (1M context) |

## What this phase was asked

`plan.md`'s row 4. `templates/sdd-routing.md`'s `Implementation` comment gains
the criterion from #120's first comment: finding out goes to `scribe`; writing
down stays with the session — unless the expected diff is large enough to
threaten what the orchestrator still has to hold, which is the one case
`smith` answers. Written as a criterion and **not** as a default, so that
neither answer becomes the silent one, and the row's placeholder must keep
parsing as unanswered.

## What this phase found

**The threshold is the part a case can hold, and the way to hold it is to pin
that it is unmeasured.** The criterion's last clause names a size nobody has
measured. A comment that invented a number would be a default wearing a
criterion's clothes, and the frame says so. `test_the_optional_row_ships_the_
criterion_that_decides_it` therefore asserts `a number nobody has` and
`REPLACEABILITY, not cost` alongside the criterion itself — the second is what
keeps the clause from being read as the cost argument #263 measured and #120's
comment explicitly separates it from.

**The measured figures from #263 were deliberately left out of the template.**
#120's comment quotes three token totals for three `smith` spawns against a
tool-call count for the parent. Those are #263's numbers, not this work item's,
and contract §5 calls an aggregate with no coordinate an assertion nobody has
opened. The template names #263 and says what it measured; the number stays
where it was taken.

**Nothing in the routing parser reads a comment**, so the criterion cost the
row nothing. `test_the_template_PARSES_into_the_three_answers_it_ships` still
reads `implementation is None`, and
`test_the_template_ships_the_vocabulary_the_parser_accepts` still finds both
answers — they are in the row and in the comment, and the parser only ever
reads the row.

## What this phase removes

| Removed item | Where it must land |
|---|---|
| none — the criterion is inserted into the existing comment block and no sentence of it was replaced | none |
