# Questions: the contract is settled against the agents that exist

<!-- seal/specs/1789034970-the-contract-is-settled-against-the-agents-that-exist/questions.md
— asked in one batch before the first edit, answered by the owner. A question
a document could have answered was never a question; these four are the ones
no document in the tree settles. -->

Asked and answered 2026-09-10, in one batch, before the first edit.

## Q1 — routing

The three axes. **Answered: `smith` · through the review chain · open the
pull request.** The file is `routing.md`; the grounds are there.

## Q2 — how far into the contract this work item reaches

Two documents disagreed and the disagreement was real. #120's body says
*not a request to split the contract now… the line gets drawn when there is
something to draw it against*, written when neither the sealer nor the framer
existed. `docs/flow.md`'s #292 row says *#120 removes sections from a payload
and a before-and-after number is what says whether that worked*, which reads
as a trim. And the sealer now exists, so there IS something to draw a line
against — the count was put in front of the owner with the number measured:
the sections the sealer never touches (§5, §7, §8, §9, §10, §12–§15) are
5,946 B of the contract's 13,765, or 43% of the only skill body its spawn
carries.

**Answered: §2 and §6 only. The contract stays one file, sixteen sections.**

The grounds, as the owner gave them and as the reviewer should hold this work
to: the defect is contradiction, not irrelevance. §12–§15 are vacuous for the
sealer rather than false about it, and a section that is merely unused costs
tokens — which is #292's subject, not this one's. A line drawn at four agents
is redrawn when the framer arrives in 0.11.0. And a `§N` citation that no
longer names one file is a cost every round record this repository has already
written would pay retroactively, which is the auditability #107 sells.

## Q3 — §7 and a probe whose leavings are not a file

From #120's second comment: a probe that created a git worktree satisfied
every word of §7 and still left something behind, and the reviewer that left
it had followed the section to the letter. Three shapes were weighed — widen
the sentence, enumerate the shapes (a file, a worktree, a branch, a venv), or
leave it and write down why.

**Answered: widen the sentence.** A probe leaves nothing behind, whatever
kind of thing it made.

The objection that was weighed and accepted: the widened form names no
mechanism, which is what #180 would raise against it. The reason it wins
anyway is the one this repository has already paid for — every enumeration in
the tree has rotted, and the next leaving is a kind nobody has met.

## Q4 — `smith`'s scope, and what lands instead

#120's first comment asks that `smith`'s §-by-§ scoping NOT be frozen here:
three answers are live (keep it as the default implementer, retire it,
re-scope it as a conditional executor spawned on size), and they imply
different contracts. The same comment asks for the criterion to be written
into `routing.md` first, so the next release measures the row rather than
re-arguing it.

**Answered: the criterion lands in `templates/sdd-routing.md` in this work
item; `smith`'s sections are the one thing deferred.**

The criterion, as it goes in: finding out goes to `scribe`; writing down
stays with the session — unless the expected diff is large enough to threaten
what the orchestrator still has to hold, which is the one case `smith`
answers. The threshold in that clause is a number nobody has, and the comment
says so rather than inventing one: the case for a delegate there is
replaceability, not cost.
