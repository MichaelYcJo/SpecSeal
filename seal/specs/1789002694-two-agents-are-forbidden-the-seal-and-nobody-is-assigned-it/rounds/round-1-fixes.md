## Fixes

| # | Verdict | Commit or grounds |
|---|---|---|
| 1 | fixed | 1f5d0c4 — the gate reads every non-zero exit from `seal`, not only 2, and tells a refusal before the write from a check that failed after it |
| 2 | fixed | 000ec8f — a third refusal reads `Fixes checked by`; the `Pass` refusal stays, and a capped run reading `no fixes to check` is untouched |
| 3 | fixed | 04a672f — the panel's `lint` row states what ran rather than asserting clean |
| 4 | fixed | 221b7ba — a file the base does not carry no longer makes every other one read `new` |
| 5 | fixed | 4b9ffb7 — a linter's warning count no longer displaces pytest's counts |
| 6 | fixed | 18b8e8f — the same scale draws the same disc in every process |
| 7 | fixed | 29eeae7 — the runner behind `bin/test` names the sealer |
| 8 | fixed | 6bd1681 — the third refusal names the spawn rather than telling the reader to run it by hand |
| 9 | fixed | 3dcfcdd — the gate names the command it sealed over |
| 10 | fixed | bce4ece — a fourth referent of `seal`, and three more the hand enumeration found that the check could not |
