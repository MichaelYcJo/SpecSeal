## Fixes

| # | Verdict | Commit or grounds |
|---|---|---|
| A | deferred #334 | the gate's discriminator is a literal another package prints and no case binds the two; the repair is mechanism, and the run had spent its reopening |
| B | deferred #335 | `seal` accepts a `round-N` whose shape is right and whose position on a last record makes it false, and writes the cell before the chain check refuses it; the same |
| C | answered | the sweep's new guard reports an absent span as the last-heading span, both being what `partition` returns; corrected at the closing commit |
| D | answered | the wall-clock axis narrowed the class rather than closing it, and an all-skipped run now reads honestly while the gate still seals — recorded rather than repaired, because the second half is the gate's judgment and not the reader's |
| E | answered | the refusal names `nobody — <why>` among the row's three values and then refuses it; corrected at the closing commit |
