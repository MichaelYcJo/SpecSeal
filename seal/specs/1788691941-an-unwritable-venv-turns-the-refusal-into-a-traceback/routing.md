# 1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback — routing

<!-- seal/specs/<unix-epoch-seconds>-<slug>/routing.md — the answer given before the
first edit, in the batch the `implement` skill collects (§1). -->

| Axis | Answer |
|---|---|
| Review | through the review chain |
| Destination | open the pull request |
| Implementation | smith |
| Branch | fix/177-an-unwritable-venv-turns-the-refusal-into-a-traceback |

Answered 2026-09-06 by the owner, before the first edit.

## Why this way

The change restores a property the module states about itself in its own
opening docstring — every failure here is a sentence rather than a traceback —
and it corrects a ledger row whose guarantee this path falsifies. Both are
things a reviewer has to judge rather than a formatter. The owner answered all
three axes in one batch before the run began, for the whole of 0.8.3.
