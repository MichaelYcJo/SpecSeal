# 1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback — review round 2

| Field | Value |
|---|---|
| Target SHA | 96b2084 |
| Ran by | specseal:warden on claude-opus-5[1m] |
| PR | 188 |
| Broad gate | not yet |
| Fixes checked by | round-3 |
| Contract changes | none |
| New units | SCOPE_BOUNDARIES (depth 1); own_returns (depth 1) |
| Needs a fix | yes — finding 6 (`reach_values` walks nested scopes, and three records, two of which ship, assert the call arm is the only over-reach) |
| Loses a record or crashes | no |

- [x] Pass

## What this round was asked

Round 2 of work item `1788691941`, the **verifying round**. Target is the record's commit; the surface is the **diff of round 1's fixes**, `682660d..HEAD`, not the branch. Base `origin/release/v0.8.3`, draft pull request #188, issue #177.

Round 1's verdicts were reached at `682660d` and are not re-walked. The job is the answers: for each verdict round 1 recorded as closed, is it actually closed.

**One surface in that diff is a finding surface rather than a verification surface.** `close` derived `New units | REACH_REFUSAL (depth 1); test_the_refusal_names_the_other_direction_too (depth 1); handed_back (depth 1)` — three units the fixes created, reviewed by nobody. Treat them as *is this correct*, not as *did it close the finding*. Verify the derivation itself too: `Contract changes | none` is a claim about the range, not an inheritance.

What round 1 recorded closed:

1. **🟡 1 — the kept-cases argument was false in seven places.** The pass measured the four mutations itself before acting and reports the same split the reviewer measured: rename and reword redden **both** cases, the section sentence reddens the named case alone, an addition reddens the derived case alone. It found a **seventh** carrier the reviewer's list of six did not name — the test module's own header comment — by grepping rather than by taking the list. **Ask:** is the corrected argument true of the code as it now stands, in all seven places and in each one's own wording; did the grep actually close the class or is there an eighth; and did narrowing `phase-2.md`'s **Executed** label leave that record true.
2. **🟡 2 — `reach_values` over-collected.** The pass took the repair rather than the justification, on the grounds that a loud failure handing out a wrong instruction is worse than a plain false alarm. `handed_back` replaces the blanket `Return` walk with one arm per way a value travels outward. It reports the four over-collection fixtures seen red first, all ten arms mutated and dead, and fixtures enumerated twice over — one per arm and one per measured over-collection shape. **Ask:** is `handed_back` a decomposition or a list of the shapes that happened to be measured; what does it do with a `Starred` element, a `dict` value, a comprehension, a `BoolOp`, a walrus, a nested function; and does the enumeration-by-arm argument survive an arm being added later.
3. **The pass reports one deliberate over-reach it kept** — the call arm, where `return elsewhere(WORD)` reads `WORD`. **Ask:** is keeping it right, is it recorded where a reader meets it, and does the refusal wording actually let a reader tell which direction they are in.
4. **⬜ 3 — the remedy clause.** Reworded to say what has to become true rather than what to change, so it covers all four errors `except OSError` catches. Pinned, and the pin seen red against the superseded wording. **Ask:** is the new sentence true for a full disk and a read-only filesystem, and do the two records that quote it now quote the current words.
5. **⬜ 4 — the fixture count.** Corrected in place and marked, with a note that the count moved again when the derivation was rebuilt. **Ask:** whether the number in the record matches `DERIVATIONS` as it now stands.
6. **⬜ 5 — the recorded limit.** Split into under-reach and over-reach, naming five shapes. **Ask:** whether each of the five is actually invisible to the current `handed_back`, since the function changed under the paragraph.
7. **The second hand re-stamp.** `seal/ledger.md`'s `hide_from_git` anchor was hand-stamped again, `08f18fc6` → `6182d405`, because `--reverify` narrows to a file and that file holds two anchors nobody here has read. The pass reports the value matched the tool's own for the same anchor in the fragment. **Ask:** is the stamp right, and did anything else in the shared ledger move that was left unstamped.

Facts, labelled:

- **executed by the orchestrator at `737d1b6`** — `./bin/test tests/test_the_fixes_name_their_surface.py tests/test_the_suite_has_a_command_that_is_cheap_twice.py -q` → 102 passed, exit 0. `uvx ruff check .` → All checks passed. `./bin/evidence-check .` unscoped → 699 ok · 2 drifted · 0 broken, the two being `templates/config.md#"# Repository config"` and `round_record.py#swallowed`, both the base's.
- **read** — `.venv` carries no `ruff` module; use `uvx ruff`. The suite runs through `./bin/test`. Do not touch this repository's own `.venv`.
- **unverified** — everything in the seven items above.

Run the ledger check unscoped.

## Verdicts

| # | Finding | Location | Verdict | Grounds |
|---|---|---|---|---|
| 1 | Round 1's 🟡 1 — the kept-cases argument, in seven places | `tests/test_the_fixes_name_their_surface.py:45-49,711-718`, `seal/ledger.md:1088`, `seal/ledger/1788691941-…md:38`, `seal/specs/1788691941-…/changelog.md:56-63`, `overview.md:21,59-60`, `phases/phase-2.md:135-141` | answered | Closed. **Executed** in a `--no-local` clone at `96b2084`, four mutations one at a time with `__pycache__` scrubbed and `-B` between them: rename `PYTEST_ONLY` (declaration and use) — named exit 1, derived exit 1; reword the constant alone to `pytest-only!` leaving the section untouched — named exit 1 `the section does not name`, derived exit 1 `the derived reach set carries it`; drop `only the first is a unit name` from the section — named exit 1, derived exit 0; add a sixth constant to `call_sites` — named exit 0, derived exit 1 naming `a sixth value`. That is the split all seven carriers now state. A cache warning for whoever repeats this: the reword mutation keeps the byte length identical, so a stale `.pyc` survives the restore and the next mutation reads the mutated constant — my first run reported the section mutation reddening BOTH cases for exactly that reason and the clean run does not. Class closed independently by a `git grep -n -E` over every `.md` and `.py` for the false claim's shapes: no eighth carrier |
| 2 | Round 1's 🟡 2 — `reach_values` over-collected | `tests/test_the_fixes_name_their_surface.py:592-620` (`handed_back`) | answered | Closed as to `handed_back`. **Executed**, ten mutations one at a time, each exit 1: `Constant` arm deleted (5 failed), its `isinstance(node.value, str)` guard dropped (1), `Name` arm deleted (7), `ast.List` / `ast.Tuple` / `ast.Set` dropped from the sequence tuple one at a time (6, 1, 1), `IfExp` deleted (2), `BinOp` deleted (2), `Call` deleted (1), and the fallback widened back to a blanket `ast.walk` for string constants (2). Every arm has a killing fixture, so it is a decomposition and not a list of what was measured. The `IfExp` arm's fixture sits in the over-collection half rather than the arms half, so the comment's *one entry per ARM* is loose about placement while true about coverage. Not closed as to the walk around it — see finding 6 |
| 3 | Round 1's deliberate over-reach on the call arm, kept | `tests/test_the_fixes_name_their_surface.py:664-671`, `REACH_REFUSAL` at 480-485 | answered | Keeping it is right, and it is recorded where a reader meets it. **Executed**: `return elsewhere(WORD)` derives the word, `return len(W)` derives it too. Pinned per §14 — dropping `over-reached and the fix is here` from `REACH_REFUSAL` reddens the new case (exit 1), and so does the derived case ceasing to cite the constant. The refusal does let a reader tell which direction they are in, for this shape. It misdirects for the shape in finding 6, where the fix is not in `handed_back` |
| 4 | Round 1's ⬜ 3 — the remedy clause | `.github/scripts/run_tests.py:165-172`, `seal/ledger/1788691941-…md:37`, `changelog.md:10-16` | answered | True for a full disk and for a read-only filesystem: `until that write can succeed` names no cause and `strerror` above it carries which one it was. Pinned in both directions in `test_the_unwritable_sentence_is_the_same_on_every_platform` — `until that write can succeed` asserted present, `make it writable` asserted absent — so the superseded wording cannot return beside the new one. Both records that quote it carry the current words. A third does not: finding 8. Note without a finding: the docstring says *the remedy clause names no cause* while the shipped parenthetical does name three of the four, omitting `IsADirectoryError`; read as *the remedy is `until that write can succeed`, the parenthetical an aside*, both stand |
| 5 | Round 1's ⬜ 4 and ⬜ 5 — the fixture count and the recorded limit | `phases/phase-2.md:165-172`, `tests/test_the_fixes_name_their_surface.py:648-663` | answered | The count matches `DERIVATIONS` as it stands, with the second move marked and pointed at `rounds/round-1.md`. All five recorded under-reach shapes re-measured against the CURRENT `handed_back`, each returning `set()` with no complaint: a bare local, a constant through an attribute, an `AnnAssign`, a `yield`, and a value only another function knows. The paragraph is true of the rebuilt function. It is short of the shapes the rebuild created — see finding 7 |
| 6 | 🟡 `reach_values` walks nested scopes, so a `def` or `class` inside `call_sites` hands the derived set a word the function never returns — the exact door round 1's finding 2 closed, one function up. Three records assert there is only one over-reach and two of them ship | `tests/test_the_fixes_name_their_surface.py:697-700`; the false claim at `:664-671` (*the one remaining way*), `seal/ledger/1788691941-…md:38` (*a call's positional arguments are read*), `seal/specs/1788691941-…/changelog.md:77-78` (*one shape reads a value*) | **fixed** `7bfabd8` | fixed at 7bfabd8 — ``. `own_returns` reads one scope's own returns and stops at every node that opens another. The reviewer's four-member boundary set was narrowed to two after measuring: `ClassDef` and `Lambda` are members no mutation can kill — a lambda body is an expression and cannot hold a `Return` at all, and a method is a `FunctionDef` the walk already stops at, so a class is reached THROUGH a boundary rather than past one. Both are kept as deliberate survivors in the mutation run rather than as a claim. Four fixtures, three of them red against the `ast.walk` they replace, the fourth pinning the recursion; **Executed** at `96b2084`: `call_sites` containing a nested `def inner()` returning `["never handed out"]` beside `return [W]` derives both words; a nested class method returning `["from a method"]` derives that word with `call_sites` itself returning nothing. The derived case then exits 1 naming a word the function cannot produce and `REACH_REFUSAL` says `handed_back` over-reached and the fix is here — but `handed_back` never saw the node, so the reader is sent to the wrong function. §12: the fix closed the coordinate (`handed_back`) and left the class open at the caller. **The repair, executed — with it applied the two modules read `103 passed`, exit `0`, the five recorded under-reaches are unchanged and the call-arm over-reach is unchanged.** At line 697 the loop body is untouched; only its iterator changes, from `ast.walk(found[0])` with the `ast.Return` filter, to a new generator `own_returns(scope)` placed beside `handed_back`, which iterates `ast.iter_child_nodes(scope)`, skips any `FunctionDef`, `AsyncFunctionDef`, `ClassDef` or `Lambda` child, yields a `Return` whose value is not None, and recurses into every other child. And one `DERIVATIONS` fixture, in a third group whose comment reads *a nested scope is not a way OUT of `call_sites`*: a module constant, a `call_sites` holding a nested `def` that returns a word of its own, and a `return` of the constant — wanting only the constant's word. §15 is already satisfied for it: the fixture is red at `96b2084` and green with the repair. Then the three records' *one remaining way* becomes true rather than needing rewording |
| 7 | ⬜ The recorded under-reach list names five shapes and gives a reason that does not fit the ones the rebuild created; a local shadowing a module constant is read as the module's value | `tests/test_the_fixes_name_their_surface.py:648-663` | **fixed** `7bfabd8` | fixed at 7bfabd8 — ``. The second under-reach class is recorded with the `BinOp` and `BoolOp` asymmetry named. Two corrections measured in this pass rather than adopted: a `Starred` loses only a value reached SOLELY through the unpacking, and the shadowing is a second OVER-reach rather than a miss — which made the one-over-reach claim false independently of finding 6, so closing 6 alone would not have made those records true; **Executed**, each returning `set()` at `96b2084`: a `Starred` element, a dict VALUE (where the docstring's *a dict* covers only the key), a comprehension, a `BoolOp`, a walrus, a subscript, and a keyword argument. The `BinOp` and `BoolOp` asymmetry is the trap — a `+` between a name and a list IS read and an `or` between them is not. None of these needs a value followed across a statement or across a call, which is the reason the paragraph gives for why the five stand, so the reason does not carry to them. Separately measured: a module-level constant shadowed by a local of the same name inside `call_sites` derives the MODULE's value — a word the function does not hand back, not merely a missing one, and the recorded *bare local* line says only that the local is missed. Suggested: after the five, record that the rebuild moved a second class of shape outside the derivation — each is one `isinstance` arm away rather than a statement or a call away, and is left out because no `call_sites` has ever written one — and that a local shadowing a module constant is read as the module's value rather than as nothing |
| 8 | ⬜ A third record quotes the superseded refusal verbatim, unmarked | `seal/specs/1788691941-…/phases/phase-1.md:152-154` | **fixed** `7bfabd8` | fixed at 7bfabd8 — ``. `phases/phase-1.md` gains a marker above the transcript saying the wording was superseded, that the block is phase 1's record of what it ran, and where the current sentence is pinned; The block carries the wording finding 3 removed, which the platform-independent case now asserts must NOT appear. It sits in a `What an operator sees` transcript, so leaving the text is defensible as a record of what phase 1 ran; leaving it unmarked is not, since every other correction in this work item carries an HTML-comment marker and a reader meeting this block takes it for the shipped sentence. Suggested: one comment above the fence saying it was CORRECTED in round 1's fix pass, that this transcript is phase 1's, and where the current wording is pinned. Finding 3's carriers were enumerated as a list of two while finding 1's were enumerated by grep, which is why this one was missed |
| 9 | ⬜ The changelog fragment ships a mutation count from the superseded derivation | `seal/specs/1788691941-…/changelog.md:83-84` | **fixed** `7bfabd8` | fixed at 7bfabd8 — ``. The changelog carries ten mutations over the derivation's branches and eight over the walk, and the one line over the wrap width is rewrapped; It reads `Six mutations over the derivation, one at a time, each now killed by a named case`, six paragraphs after describing the rebuilt `handed_back`. Six was phase 2's figure over the old `reach_values`; round 1's fix pass ran ten over `handed_back` and I reproduced all ten dead. The ledger fragment R2 keeps the two runs apart correctly and the changelog does not, and the changelog is what folds into `CHANGELOG.md`. Suggested: ten, over the derivation's arms. Line 78 of the same file also over-runs the fragment's wrap width by about 60 characters |
| 10 | ⬜ A record describes the docstring's recorded limit as it was before the rebuild | `seal/specs/1788691941-…/phases/phase-2.md:151-153` | **fixed** `7bfabd8` | fixed at 7bfabd8 — ``. `phases/phase-2.md` gains the same marker form used twice already in that file, pointing at both round records for the current shape; It names two under-reach shapes where the docstring now records five and one over-reach, and the shape it calls *formatted at run time* is in neither list — it is a fixture expecting an empty set. Suggested: the same HTML-comment correction form used twice already in this file, pointing at `rounds/round-1.md` for the current shape |
| 11 | ⬜ Round 1's record silently loses half of finding 2's Grounds to an unescaped pipe, and no check sees it | `seal/specs/1788691941-…/rounds/round-1.md:45` | **fixed** `7bfabd8` | fixed at 7bfabd8 — ``. One separator escaped in `rounds/round-1.md`; every Verdicts row is five cells on unescaped separators, verified at the file by the orchestrator too. The wider repair is issue **#189** and was deliberately not built — it is mechanism, and a fix pass adds none; **Executed**: splitting the row on the cell separator gives SIX cells where the header declares five, at `c09e141`, `737d1b6` and `96b2084` alike. The cause is an unescaped augmented-assignment operator inside the cell, from round 1's report; every Markdown renderer drops the sixth column, so everything from that operator to the end of the cell — including both paste-ready fixtures the round commissioned — is invisible in the rendered record and survives only in raw text. `close` did not cause it but re-serialised it. `chain_check --baseline origin/release/v0.8.3` does not flag it; it flags only the `Pass`-beside-`nobody` state. This is the base's tool, not this branch's — `round_record.py` already carries a RIDER about a sibling defect in the same cell handling. Correction: escape the separator in that cell. The wider repair, refusing or escaping a bare separator inside a Verdicts cell, belongs in an issue against `round_record.py`. Every Grounds cell in THIS report avoids a raw one for the same reason |

## Executed probes

| What was run | Result |
|---|---|
| `git clone --no-local` of the repository, checked out at `96b2084`; every probe below ran in it and it was deleted afterwards | clone at `96b2084241c10747ef411362ce8d08880d8bd528` |
| `./bin/test tests/test_the_fixes_name_their_surface.py tests/test_the_suite_has_a_command_that_is_cheap_twice.py -q`, exit code read directly | `102 passed`, exit `0` — the orchestrator's figure reproduced at the target rather than at `737d1b6` |
| `git diff --stat 737d1b6..96b2084` | one file, `rounds/round-1.md`, 8 insertions and 8 deletions — so the orchestrator's test, ruff and ledger figures carry to the target by construction |
| `./bin/evidence-check .` unscoped, exit code read directly | exit `1`; `699 ok · 2 drifted · 0 broken · 0 external · 0 old-format`. The two drifted are `templates/config.md` and `round_record.py#swallowed`, both the base's. `seal/ledger.md` itself reports only those two, which confirms the hand re-stamp `hide_from_git@6182d405` and that nothing else in the shared ledger moved unstamped. Both fragments read `10 ok · 0 drifted · 0 broken` |
| Four mutations for the kept-cases split, one at a time, `__pycache__` scrubbed and `python -B` between them | rename both red; reword-the-constant-only both red; section sentence dropped, named red and derived GREEN; sixth constant added, named GREEN and derived red — the corrected argument, confirmed |
| The same four without scrubbing the cache | the section mutation read both-red, because the reword mutation is byte-length-identical and its `.pyc` survived the restore. Recorded so the next run does not repeat it |
| `git grep -n -E` over every `.md` and `.py` for the false claim's shapes | no eighth carrier |
| Ten mutations of `handed_back`'s arms and type guards, one at a time | every one exit `1` — `Constant` (5 failed), the str guard (1), `Name` (7), `List` (6), `Tuple` (1), `Set` (1), `IfExp` (2), `BinOp` (2), `Call` (1), the fallback widened to a blanket walk (2) |
| Two mutations of the new pin: `REACH_REFUSAL` loses its over-reach half; the derived case stops citing the constant | both exit `1` |
| `reach_values` against 22 shapes not in `DERIVATIONS` | the five recorded under-reaches confirmed invisible; seven unrecorded under-reaches found; two nested-scope over-reaches found; findings 6 and 7 |
| The proposed `own_returns` repair applied with its fixture, then both modules run | `103 passed`, exit `0`; the nested `def` derives only the constant's word and the nested class method an empty set, while the five recorded under-reaches and the call-arm over-reach are unchanged |
| `skills/code-review/scripts/chain_check.py --baseline origin/release/v0.8.3` | exit `1`, on `Pass` checked beside `Fixes checked by: nobody` — the mid-run state round 2's record closes, not a defect. It does NOT flag the six-column row at finding 11 |
| Row-shape count of `rounds/round-1.md` row 2 at `c09e141`, `737d1b6` and `96b2084` | six cells against a five-column header at all three — finding 11 |

## Inherited coordinates

| From | Coordinate | Why it is still worth opening |
|---|---|---|
| round-1 | `seal/ledger.md:1088` (R8 Notes), `seal/ledger/1788691941-an-unwritable-venv-turns-the-refusal-into-a-traceback.md:30` (R2 Notes), `seal/specs/1788691941-…/changelog.md:53`, `tests/test_the_fixes_name_their_surface.py:584-586`, `seal/specs/1788691941-…/overview.md:21`, `seal/specs/1788691941-…/phases/phase-2.md:131` | round 1's 1 — fixed |
| round-1 | `tests/test_the_fixes_name_their_surface.py:568-571` | round 1's 2 — fixed |
| round-1 | `.github/scripts/run_tests.py:159-163` | round 1's 3 — fixed |
| round-1 | `seal/specs/1788691941-…/phases/phase-2.md:165` | round 1's 4 — answered |
| round-1 | `tests/test_the_fixes_name_their_surface.py:538-548` (`reach_values` docstring) | round 1's 5 — fixed |

## Deferred

| Finding | Where it went | Who answers it |
|---|---|---|
| nothing to drain | — | — |
