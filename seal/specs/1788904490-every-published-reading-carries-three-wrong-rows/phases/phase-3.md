# 1788904490-every-published-reading-carries-three-wrong-rows — phase 3

| Field | Value |
|---|---|
| Phase | 3 |
| Commit | 74baaf2 |
| Ran by | the session on `claude-opus-5[1m]` — `routing.md` names `the session`, so there is no spawn and no segment transcript of its own |

## What this phase was asked

#200 — the family names the runner, and says what it cannot name.

## What this phase found

**The ticket left the decision open and named three shapes; a fourth was
needed to make any of them honest.** The widened pattern is the cheapest and
carries the ticket's own objection — it fixes this repository and leaves the
next one with the same defect *and no sign of it*. So the report now prints
what the table could not name whenever `other` leads by time. The objection is
answered rather than accepted, and the config-row shape stays available with
the condition under which it becomes right written into `questions.md` Q2.

**The measurement found the ticket's second half to be the larger one.** The
ticket names one file write charged to `test`. Over 180 transcripts there are
**644**, and `lint/type` and `build` lose the same kind of false positive —
361 → 175 and 177 → 47. A command's whitespace is flattened before the family
is taken, so every document written through a heredoc arrives with its whole
body on the command line.

**Three mutants survived the first fixture, and the fixture was rebuilt.**
Taking the arrival-order entry, taking the entry without grouping across
pipes, and collecting over every family rather than over `other` all passed,
because the fixture's first, largest and only-unnamed command were the same
call. It now carries a cheap first command, two pipes of one command, and a
named family larger than every unnamed one.

## What this phase removes

| Removed item | Where it must land |
|---|---|
| none | none |
