# 1788904490-every-published-reading-carries-three-wrong-rows — phase 1

| Field | Value |
|---|---|
| Phase | 1 |
| Commit | 1b33418 |
| Ran by | the session on `claude-opus-5[1m]` — `routing.md` names `the session`, so there is no spawn and no segment transcript of its own |

## What this phase was asked

#193 — a charged third is not a baseline.

The ticket arrived with the patch already written and executed, so the work
was the two cases and the second shape its own text carries as a comment.

## What this phase found

**The ticket's second shape needed narrowing before it could be built.** It
reads as *the filter drops a zero and keeps a negative*, and only the second
half is a defect. `count` answers 0 both for a field a harness never wrote and
for one it wrote as 0, so this file cannot tell a turn that spent nothing from
a turn nobody measured — dropping both is the only behaviour available, and
the fix is that a negative leaves with them. Building it as written would have
put turns with no reading at all into the mean.

**Both directions of the first shape are in one case.** With the uncomputable
third FIRST the line printed off a zero baseline; with it LAST the line was
suppressed. The second arm was already correct, and a conjunct that bought its
silence by silencing the line would have passed a case that watched only the
first — so a third case asserts the line still prints on a real 10 → 100
growth, with both numbers in it.

## What this phase removes

| Removed item | Where it must land |
|---|---|
| none | none |
