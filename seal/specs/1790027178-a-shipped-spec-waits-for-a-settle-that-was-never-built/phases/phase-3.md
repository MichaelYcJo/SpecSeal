# 1790027178-a-shipped-spec-waits-for-a-settle-that-was-never-built — phase 3

| Field | Value |
|---|---|
| Phase | 3 |
| Commit | `ccee10e2` |
| Ran by | `specseal:smith` on Opus 5 (1M context) — the agent definition names no `model`, and the spawning session passed no override, so the segment inherited the session's model |

## What this phase was asked

**`skills/settle/SKILL.md`.** The procedure: what goes up and who decides it,
where it lands (merge into an existing `docs/` document, create one only for a
new area), the work-item-id comment each folded sentence carries, newest-wins,
and the retirement as the second half of the fold and never its own act —
including the `survivors.md` range-row a fold branch owes **and the population
floors a repository's own checks carry over `seal/specs/`, with the three ways
to answer one**.

Verified by A6, A11, and the shipped-document rules: the wrapper pair, the
command named, 88-column wrap, the interpreter floor.

## What this phase found

**The skill is on-demand rather than user-invoked, and that decides two
numbers in phase 5.** It carries no `disable-model-invocation: true`, so a
session working down `docs/release-checklist.md` loads it at the step that
names `settle` rather than waiting for somebody to type `/specseal:settle`.
`tests/test_chain_hooks_hardening.py#test_the_readme_group_counts_are_derived_too`
reads the three groups off the tree, so the consequence is mechanical: the
*on demand* group goes from eleven to twelve and the total from twenty-three
to twenty-four.

**A6 costs a path rather than a command, and the reason is in the rule.**
`settle` has no hyphen in it, and
`tests/test_a_document_that_names_a_script_says_how_to_reach_it.py#reachable`
refuses to read a hyphenless command as a locator — every document in this
repository contains the word `seal`, and the same argument reaches `settle`.
So the skill carries `skills/settle/scripts/settle.py` in full. Verified by
running that module: both the classification case and the locator case pass
over the new pair.

**A11 is the section this phase exists for, and it is general rather than this
repository's.** `docs/one-root-by-lifetime.md` §*The dependency rule* names
two readers that break on removal, and the real list is longer — fourteen test
modules read the corpus and at least six carry a population floor. That
document was written when the repository had thirteen work items. So the skill
says the list is not the whole list, gives the `grep -rn "seal/specs" tests/`
that finds them, and names the three answers — retire, re-point at a fixture
corpus, decline — with the fourth named as the one that turns a check into a
comment.

**Three cases are red from this commit until phase 5 closes them**, and this
is the plan's ordering rather than a defect:
`test_both_readmes_count_the_skills_that_are_actually_there`,
`test_every_shipped_skill_is_named_in_both_readmes` and
`test_the_readme_group_counts_are_derived_too`. Each was run and read here so
that phase 5 knows the exact four numbers it owes — `Twenty-four` / `스물네`
in the skills row, `Twelve` / `열두` in the on-demand group, `settle` in both
editions' name lists, and `NUMBER_WORDS[24]`.

## What this phase removes

| Removed item | Where it must land |
|---|---|
| nothing — the phase only adds | none |
