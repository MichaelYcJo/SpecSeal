# two roots hold three lifetimes — questions for the planner

<!-- specs/1788331011-two-roots-hold-three-lifetimes/questions.md — decisions
only a human can make, extracted so nothing ships on a silent assumption.
The design record pre-answered most of the gate; every row below is either a
name the record left to the owner or a fact the framing spawn found that the
record does not cover. Each has a default so phase 1 can start without an
answer; an answer that overturns one changes phase 1 and is cheapest before
it begins. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q1 | The names. The record recommends and this frame uses: plugin stays SpecSeal · root `seal/` · work items `seal/specs/<id>/` · ledger `seal/ledger.md` + `seal/ledger/<id>.md` · `follow-up.md` and `parity.md` unchanged · `templates/seal-README.md` and `templates/ledger.md` · the per-machine state directory stays `~/.claude/specseal/` (it is keyed to the plugin, not the root, and two existing markers live there) | (a) as listed. (b) `.seal/` — one directory rename, and the S15 list changes. (c) `work/` or another sub-directory — the CHANGELOG and ledger markers then read `specs/` for history and the new name for new entries, or Q2 reopens. (d) rename the plugin — the record says 0.4.0 is the one cheap moment, and it is out of this item's scope | (a) | ⬜ |
| Q2 | The marker text `<!-- specs/<id> -->` in `CHANGELOG.md` and `seal/ledger.md`. Both `--check`s look for it, and a changed text makes every released entry read as ungathered (`gather_changelog.py:15-25`) | (a) unchanged — it names the work item by its sub-directory, which is still `specs/`. (b) `<!-- seal/specs/<id> -->` for new entries and both texts accepted by `--check` — two spellings forever, in two scripts. (c) rewrite history to the new text — a release pull request that edits released entries | (a). `plan.md` alternative E | ⬜ |
| Q3 | Whether `optin.home()` already looks at `<git-common-dir>/seal/` in 0.4.0. The ticket's done-when says the hooks look at both places; the record's Order says 0.4.0 is shared mode only. Nothing in 0.4.0 creates the second place | (a) look at both now — two `isdir` calls, one extra `git rev-parse --git-common-dir` only when the first is absent; #80 then changes creation only. A `.git/seal/` someone makes by hand before #80 opts in with no CI. (b) `<repo>/seal/` only — #80 edits `optin.py` and every hook that joins a path under the root | (a). Spec S2 | ⬜ |
| Q4 | The `scratch` opt-out (record's open decision 4). It has a live use the record's "a fixture creates no `seal/`" misses: a copy of a real repository carries `seal/`, and #78's S6 probe was exactly that | (a) keep it, as the file `<git-common-dir>/specseal-scratch` — cannot be committed, so the hygiene test that guards against a committed one goes away, and a linked worktree shares it. (b) drop it — a copied repository has its gates on, and the person deletes `seal/` or answers the prompt. (c) keep `seal/scratch` — the committed-silence hazard stays and so does its test | (a). Spec S4, `plan.md` alternative F | ⬜ |
| Q5 | Whether the migration overwrites a user repository's `.specseal/README.md` with `templates/seal-README.md` after moving it. The file is plugin-owned and its old text describes a layout that no longer exists; the overwrite is visible in the staged diff and the old text is in history | (a) overwrite — the same licence `preset-setup` takes with the CLAUDE.md marker block. (b) move only — every migrated repository carries a README naming `.specseal/map.md` until someone edits it | (a). Spec §"The move, in order", step 3 | ⬜ |
| Q6 | Whether the `implement` skill's once-per-repo moment asks the mode question in 0.4.0. The record's "What first setup asks" says one question remains; its Order says shared only, so the question has one answer this release | (a) do not ask — create `<repo>/seal/`, say what was created, ask the parity question as today; #80 adds the mode question with its second answer. (b) ask now with one option, so the prompt exists before its second answer does | (a). Spec S14 | ⬜ |
| Q7 | One ledger row cites a round record: `.specseal/map.md` → `specs/1788184145-the-gate-stops-the-session-editing-its-tests/rounds/round-3.md`. It is the only anchor under a moved path (executed: 1 of 190). After the move it reads `seal/specs/…`, and `settle` (#83) will remove that directory, which breaks the row | (a) re-point it here and leave a note in its Notes cell naming #83, so the `settle` item finds it in the ledger — the row stays true until then. (b) re-anchor it now to whatever the claim is really about, if that is a file that outlives the work item. (c) drop the row | (a). Spec §"The dependency rule, applied" | ⬜ |
| Q8 | When the marker already carries a root but the old layout is still there — a refused-then-never-committed repository, or a move that stopped and was never resumed — the ledger hook's rule says stay silent once per repo, with a loud checker as backstop. Here the backstop is silence from every gate | (a) the ledger hook's rule, as written in the spec table's last row: silent, once per repo. (b) keep printing the dirty-tree or stopped-move line at every session start until the tree is moved, because nothing else will say so | (a), because it is the rule the sibling hook has; (b) if the owner weighs the silent gates heavier | ⬜ |

Answered rows feed back into `docs/one-root-by-lifetime.md`'s "Decided after
the thread" table before this directory's work merges, or stay as defaults
named in the pull request body.
