# the baseline is the moving pull-request base — questions for the planner

<!-- seal/specs/1788873600-the-baseline-is-the-moving-pull-request-base/questions.md — decisions
only a human can make. Nothing here blocked the build: each row states the
assumption that was taken and continues. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q1 | The ticket's repair section says *"the other two arms already read only this branch's own files"*, and the row-count arm does not — it reads the base revision through `git show <ref>:<path>`. Should that arm keep the moving base? | **(a) No, one resolution point** — both arms read the merge base, one report names one revision, and a base that gains a row in this branch's own overview stops reading as rows this branch never had. **(b) Yes, the arms diverge** — the row arm keeps the tip, on the grounds that no fixture has ever moved it and the corpus lays out one overview per work item, so the misfire is reachable only if a branch edits another item's overview. (b) costs a fifth pair of readers in the module whose docstrings record four pairs drifting apart across three review rounds | **(a) taken.** The ticket asked for this to be read rather than assumed, and reading it says the arm does call the base. The repair is one substitution at the single resolution point, so (a) is also the smaller diff | ⬜ repository owner |
| Q2 | A shallow clone whose base ref resolves but whose common ancestor is beyond the graft now exits 2 (`share no history`) where it used to compare against the base tip. Is the louder failure right for a caller who is not CI? | **(a) Exit 2** — a comparison against the wrong revision is the quiet-zero state this module refuses everywhere else, and it already exits 2 for a shallow clone missing the base ref entirely. **(b) Fall back to the ref's tip with a notice** — keeps a shallow caller comparing, at the price of the arm firing on a moved base again, which is this work item. **(c) Deepen the clone from inside the tool** — a checker that fetches is a checker that writes to the repository it is reading | **(a) taken.** Both workflows set `fetch-depth: 0`, so neither CI leg is in that state, and (b) reintroduces the defect on exactly the branches that hit it | ⬜ repository owner |
| Q3 | `docs/one-root-by-lifetime.md` lists this arm under *"would break on removal, so must change before `settle` lands"*, wanting it scoped to the work items a pull request touches. The merge base does not answer that: a `settle` commit that removes a merged work item's directory removes it relative to the fork point too, so the arm still refuses | **(a) Leave it to `settle`** — the bullet stays, with the footing corrected to the merge base, and the scoping stays that item's to design. **(b) Scope it here as well** — a diff-scoped arm cannot see a directory deleted wholesale, which is `test_renaming_the_work_item_directory_fails_the_baseline`, so it would trade this defect for that one | **(a) taken.** The bullet is corrected where it names the compared revision and left standing where it names the `settle` problem | ⬜ repository owner |

Answered rows feed back into `docs/` before this directory's work merges.
