# 1789996780-the-census-and-the-tie-that-nothing-holds — routing

| Axis | Answer |
|---|---|
| Review | through the review chain |
| Destination | open the pull request |
| Planning | framer |
| Implementation | smith |
| Automation | yes |
| Answer pressed | automation |
| Branch | fix/the-census-and-the-tie-that-nothing-holds |

Answered 2026-09-21 by the repository owner, before the first edit.

## Why this way

The `automation` preset was pressed once, for the whole of the 0.12.3 run,
and in the same call the owner chose how the milestone's six issues are cut
into work items — two of them, by the file each set of statements sits in.
This row says the answer was run-wide rather than work-item-wide.

The work is #469, #470 and #471 — everything `correction_check.py` states
about the corpus it watches, and the half of its parent-naming rule that
nothing holds. All three are #424 leftovers, left open where that run capped.

**`Planning` says `framer`, and the row was added in this commit rather than
the one before it** — an absent row reads as *not answered*, which was the
true state while the frame was being drawn. The spawn was told not to open
with a parallel read burst and to write a skeleton `spec.md` inside its first
few calls, which is what the sibling work item's framer was told, and both
finished rather than dying to the harness's 600-second watchdog the way the
0.12.2 run's two spawns did.
