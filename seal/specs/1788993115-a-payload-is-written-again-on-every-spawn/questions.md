# a payload is written again on every spawn — questions for the planner

<!-- seal/specs/1788993115-a-payload-is-written-again-on-every-spawn/questions.md
— decisions only a human can make, extracted so nothing ships on a silent
assumption. Before adding a row, check the inheritance rule: if policy is
silent but existing behavior answers it, inherit and record — only genuinely
NEW rules belong here. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q1 | The 3,024 B the repository's `CLAUDE.md` and `~/.claude/CLAUDE.md` carry identically — every spawn in this repository pays it twice | (a) keep both, accept the payload · (b) delete the repository copy, −3 KB per spawn, a contributor without the plugin loses the rules · (c) one source and a build step: `templates/claude-md-block.md` is the block, `install.sh` reads it, a script regenerates the `CLAUDE.md` copy and hygiene checks it — the two cannot drift, and the per-spawn cost stays | (c) | ✅ answered 2026-09-10 by the repository owner, in the one batch before the first edit: (c) |
| Q2 | Whether the audience marker is a heading prefix or frontmatter | (a) the `Orchestrator:` heading prefix — already in `code-review`, grep-visible, and the reason #255's grep came out short is that it read `##` and skipped `###`, so the check reads both levels · (b) a frontmatter field — machine-readable, and no shipped skill has it | (a) | ✅ decided by the default: the existing convention, with the check reading both heading levels |
| Q3 | Whether `implement`'s orchestrator half goes to `skills/implement/orchestration.md` | (a) yes, the shape #265 gave `code-review`, so an orchestrator reads two files with the same name in two skills · (b) a differently named file · (c) leave the sections in place and mark them only — impossible, because the check this work adds fails on exactly that | (a) | ✅ decided by the default: (a) — the check forces the move, and one name for the orchestrator's half across skills is what makes it findable |
| Q4 | The exact token count of one file. Bytes do not track tokens at one ratio across agents (`spec.md` §*Measured*), and the per-file figures the meter prints are estimates from a per-agent ratio | A probe agent definition under `.claude/agents/` whose `skills:` lists one file, spawned once with *reply ready*, read the same way as the six spawns here. Such a definition is read at session start, not at spawn: three were written and none was spawnable in the session that wrote them. So the measurement belongs to **the next session started in this repository**, with the three probe definitions left in `.claude/agents/` (gitignored) for it, and `payload-meter --calibrate` reads them once they exist | per-agent measured ratio, every per-file figure labelled `estimated` | ⬜ a measurement answers it — not a person — in the next session |
| Q5 | Whether a subagent's payload prefix is reused across spawns under the conditions the issue's 49 readings did not cover | Measured here rather than asked: the second spawn of the same agent inside five minutes read its whole prefix from cache; every write is `ephemeral_5m_input_tokens` — the transcript's own field, NAME NOT IN TREE. The 49 readings saw writes because no two spawns of one agent in a chain are five minutes apart | — | ✅ the work answered it, 2026-09-10 (`spec.md` §*Measured before the first edit*) |
| Q6 | Where the meter lives: `bin/` + `skills/verify/scripts/` (ships, on every user's PATH) or `.github/scripts/` (this repository's own tooling) | (a) ships: a user with their own agent definitions can meter them, `README.md` names the command, and the hygiene pattern already watches `bin/` · (b) stays home: nothing new on a user's PATH, and the number exists only for this repository | (a) | ✅ decided by the default: (a). `session-cost` set the precedent — the meter measures the same transcripts |
| Q7 | `agents/smith.md`'s design-gate paragraph restates the routing span this work moves | (a) move it here too · (b) leave it for #84, which takes the whole design gate to the framer | (b) | ✅ decided by the default: (b), so the paragraph moves once and not twice |
| Q8 | Whether the meter should refuse a transcript whose spawns read an earlier tree when no `--baseline` is given. With one, the same spawn over a different byte count is detected and the earlier ratio kept (phase 4); without one, nothing in a transcript says which tree its spawns read, and the meter derives a ratio over bytes the spawn did not read | (a) leave it: the docstring states the rule and `--baseline` is how an after-number is taken · (b) compare the spawn's timestamp against `git log` for the measured files and warn — the meter would call git, which nothing here does · (c) record the tree's byte count per agent in the transcript-adjacent JSON at spawn time — no hook exists at spawn | (a) | ⬜ the repository owner |

Answered rows feed back into docs/ (policy clause or open-questions section)
before this directory's work merges.
