## Fixes

| # | Verdict | Commit or grounds |
|---|---|---|
| 1 | fixed | c75915b — every ratio entry carries `spawn` and `over_bytes`; `_spawn_of` reads `spawn` first; `payload-after.json` regenerated from the same transcript, −13,462 B / −4,691 tokens unchanged |
| 2 | fixed | 2dadb10 — `heading_starts` tracks fences the way the check does |
| 3 | fixed | 2853caa — `frontmatter` reads an inline list, a bare comma list and an unindented block list |
| 4 | fixed | 312a151 — `short_name` applied in `calibration_of` and in `measure` |
| 5 | fixed | 63ae1a5 — the real-tree case asserts `agents_in(ROOT)` is non-empty first |
| 6 | fixed | 8561bda — lines read with `newline=""`, blocks compared without endings, `--write` keeps the target's ending |
| 7 | fixed | a42a060 — the interpreter floor guard copied from `round_record.py`, so a 3.9 `python3` gets the sentence and exit 2 |
| 8 | fixed | c75915b — `measure` records the baseline file and `render` names it for every entry shape |
| 9 | answered | corrected at b4a9a1d — `overview.md` reads five rows over six anchors, the README row carrying two |
| 10 | fixed | b4a9a1d — `hooks/mode-gate.py`'s docstring names `templates/claude-md-block.md` |
| 11 | deferred questions.md Q4 | needs a spawn, which contract §6 forbids the reviewer; Q4's probe in the next session answers it |
