# Survivors — a payload is written again on every spawn

<!-- seal/specs/1788993115-a-payload-is-written-again-on-every-spawn/survivors.md
— places `survivor-check` reports as still carrying wording this branch
removed, each judged and excused with a quote from the standing text. The
quote is the anchor: the row stops holding when that text changes. -->

## Places

| Path | Quote | Grounds |
|---|---|---|
| `tests/test_routing_is_recorded.py` | Measured: a session read the missing option as a chicken-and-egg the design does not have and put it to the user twice. | The range deleted the `# RIDER:` at `implement`'s wake/quiet table, which said one session *"treated the review arm's ordinary wake on a `specs/`-only commit as a chicken-and-egg the design does not have and put it to the user twice"*. The rider was answered — the sentence it sat under now names the PARITY arm — not corrected, and this docstring is that test's own record of the same measured event, in its own words. It pins what the test guards and it is still true |
