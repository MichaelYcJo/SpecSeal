# a wrapped terminal line is not one value — excused survivors

<!-- Read by `survivor-check --exempt`. The QUOTE is the anchor, so an
exemption stops applying the moment that text changes, and an excused survivor
is still printed with its grounds. There is no value meaning "check nothing". -->

Both survivors of round 1's fix range are the same sentence in the same file,
and the file is `phases/phase-4.md`. A phase record states **what that phase
found at its own commit** — `templates/sdd-phase.md` says it mirrors
`rounds/round-N.md`'s shape for the build side, and `skills/implement/SKILL.md`
admits a commit into the contract precisely because a commit asserts a past
state somebody can open.

Phase 4 genuinely did hand the whole-line alternative over, and said so with
its grounds. Round 1's 🟡 5 then took it, on the better argument that phase 4
had retyped the constant whole and that is the cheapest moment it will ever
have. **Rewriting phase 4's record now would make a past-state document assert
a conclusion nobody reached at that commit** — which is the same thing
`spec.md` §*Scope* refuses for the round records this work item declined to
migrate, and for the same reason.

What a reader needs instead is the correction where a reader acts on it, and
it is in three places that are not past-state: the ledger fragment's row 2,
which is what folds into `seal/ledger.md` at the release; `overview.md`
§*Not verified*, where the row is closed with a ✅ and what closed it; and the
fix table handed back with this pass.

| Path | Quote | Grounds |
|---|---|---|
| `seal/specs/1789347354-a-wrapped-terminal-line-is-not-one-value/phases/phase-4.md` | `A second gap, and it is not this branch's to close.` | Phase 4's own conclusion at `ac49d7f`, correctly recorded and correctly grounded at the time. Round 1's 🟡 5 overturned it four commits later on an argument phase 4 did not have — that retyping a constant whole is the moment to pin it. The record keeps what the phase concluded; the ledger fragment and `overview.md` carry what is true now |
| `seal/specs/1789347354-a-wrapped-terminal-line-is-not-one-value/phases/phase-4.md` | `so it is handed over rather than taken` | The same sentence's second half, reported separately because the range corrected the ledger fragment's copy of the same wording. The fragment is a present-tense claim about the tree and was corrected; the phase record is a past-state claim about one commit and is not. Both halves of this exemption die the moment anyone edits either quote |
