# a merged ticket looks unstarted until the release ships — questions for the planner

<!-- seal/specs/1789108681-a-merged-ticket-looks-unstarted-until-the-release-ships/questions.md —
decisions only a human can make, extracted so nothing ships on a silent
assumption. -->

Sorted by `Who can answer`. **Two rows need the repository owner**; everything
below them is settled by running something or by the phase that meets it, and
neither kind should be queued behind a person.

The routing axes are not here — `routing.md` records them, answered before the
first edit — and neither is the choice of this work item over #103.

| # | Question | Who can answer | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|---|
| Q1 | The gate refuses on `M \ D`: the milestone claims an open issue the release branch does not carry. Separately it finds `L ≠ D` — a label that disagrees with the commits. Does that disagreement **fail** the release pull request, or report and pass? | a person | **(a) fail.** A label written by hand, or a signal run that did not happen, stops the release until somebody repairs the labels — a red build for something that does not change what ships. **(b) report and pass.** The labels can rot silently, and the human-readable signal quietly stops being true, which is the defect one layer over. **(c) fail one direction only** — `L \ D` (a label naming a release the issue is not in) fails, `D \ L` (the signal missed one) reports. The repository's own precedent is split: `issue_claims_check.py` reports and never fails because "a check that goes red on prose stops a release for a false positive"; `survivor_check.py` fails | **(c)**. `L \ D` is always somebody's hand-edit and is always one command from fixed; `D \ L` is the signal failing, and blocking a release on it makes the release pay for the signal's outage | ✅ **(c)** — 2026-09-11, the repository owner, taken as the default the frame proposed. `L \ D` (a label on an issue this release does not carry) FAILS: it is always a hand-edit or a squash subject that lost its `(#N)`, and one command repairs it. `D \ L` (the signal missed one) REPORTS: a release must not be held for a failure of the signal rather than of its contents. The gate names the direction either way. |
| Q2 | The release that ships this gate is the first thing it judges. At `a01d679` the milestone `release: 0.11.1` holds nine open issues and two of them will be in the release (#351, #359); the other seven — #354, #350, #345, #343, #330, #198, #103 — are scheduled work that has not been built, and the gate as specified fails the release pull request naming all seven. What happens before that pull request opens? | a person | **(a) the milestone is made true by hand** before the release pull request opens: the seven move to another milestone. No extra code, the gate blocks from its first day, and `docs/release-checklist.md` step 0 gains the line naming that act — which a later release needs anyway. **(b) the gate ships with a documented escape for its own first release** — a dated special case somebody has to remember to remove, which is the class `docs/release-checklist.md` and `CONTRIBUTING.md` are both written against. The choice is also a release-planning act: which of the seven, if any, is actually going out in this release | **(a)**. It costs seven milestone edits once and leaves no code to retire, and the milestone is already meant to be true — `docs/issues-and-milestones.md` says a milestone answers *what is in 1.2.3* | ✅ **(a)** — done 2026-09-11, before this row was read. The milestone was made true rather than the gate excepted: #354, #350, #345, #343, #330, #198 and #103 moved to a new `release: 0.11.2`, and the rest of the 0.11.x line shifted back one number the same day. `release: 0.11.1` now holds #351 and #359 alone, which is what the release carries. The frame measured nine at `a01d679`, before the move. `docs/release-checklist.md` step 0 still gains the line naming the act, because the next release meets the same requirement with no ticket to explain it. |
| Q3 | Can a GitHub Actions job holding `permissions: issues: write` create a label (`POST /repos/{owner}/{repo}/labels`) and add one to an issue with the default `github.token`? | a measurement | `close-issues-on-release.yml` already declares `issues: write` and closes issues with it, so adding a label is very likely covered — labels sit under the Issues API. Creating one is the half nobody here has run. Settle it in phase 1, from GitHub's permission table and from one real run, before the script is trusted | assume `issues: write` covers both; the script creates the label only when the read says it is absent, so the create path is reached at most once per release | ⚠️ **unverified, and it cannot be settled from here** — 2026-09-11, phase 1. Both halves need a write: a real workflow run needs a push, and a label create is a write to the tracker, and this segment is permitted neither. **Read**, not executed: GitHub's REST reference for *Create a label* names no fine-grained permission at all, and the Actions `permissions` reference says only that `issues: write` "permits an action to add a comment to an issue" — neither page enumerates labels (both fetched 2026-09-11). So the default was taken as specified: `contents: read` + `issues: write`, the smallest set that reaches the Issues API, with the create reached at most once per release and failing loudly with the label name if the token does not cover it. **Who must answer: the repository owner, by watching this branch's own squash into `release/v0.11.1`** — the first real run, and the run `spec.md` S1 already calls for |
| Q4 | `hygiene.yml` declares no `permissions:` block and no step in it uses `gh`. What token does the gate step get, and does `gh issue list --milestone` work in that job without a block being added? | a measurement | One run settles it. If the default is read-only for issues that is enough — the gate writes nothing — but "the default" comes from a repository or organisation setting nobody has read. Phase 2 | add an explicit `permissions:` block with `contents: read` and `issues: read`, and pass `GH_TOKEN: ${{ github.token }}` on the step, rather than relying on a default | ✅ **the default was taken, and it answers the question by removing it** — 2026-09-11, phase 2. What the default IS cannot be measured from here: GitHub's own reference says the token's permissions "are initially set to the default setting for the enterprise, organization, or repository" (read, fetched 2026-09-11), which is an account setting rather than a fact about this tree. So the block is stated — `contents: read`, `issues: read` — and `GH_TOKEN: ${{ github.token }}` goes on the step. **Why this was safe to take inside this work item rather than in one of its own:** every step in that job reads and none writes, so a stated read-only block is *narrower* than any default it could be replacing and cannot take a scope an existing step was relying on. Executed: `gh issue list --milestone`, `gh issue list --label` and `gh api …/milestones` all exit 0 against this repository's tracker — which measures the commands, not the workflow token |
| Q5 | Does `gh issue list --milestone "release: X.Y.Z" --state open` exclude pull requests that carry the same milestone? | a measurement | One command. It matters because a pull request in the milestone would never carry `merged: X.Y.Z`, so it would sit in `M \ D` and block the release forever with no way for anyone to fix it | `gh issue list` excludes pull requests; if the measurement disagrees, the gate filters on the `pull_request` key the way `close_issues_on_release.py#_issue_api` already does | ✅ **it excludes them** — executed 2026-09-11, phase 1. No pull request in this repository carries a milestone, so the question was measured on the general property instead, which is what it turns on. `gh api "repos/<repo>/issues?state=all&per_page=6"` returned #359, **#358**, #357, **#356**, **#355**, #354, the three bold ones carrying a `pull_request` key; `gh issue list --state all --limit 6` returned #359, #357, #354, #353, #351, #350 — none of the three — and `gh pr list --state all --limit 3` returned exactly those three. All three commands exit 0. REST `/issues` is the endpoint that includes pull requests and `gh issue list` is the one that does not, so the gate reads `M` with `gh issue list` and needs no filter |
| Q6 | Does adding a label an issue already carries error, or is it a no-op? | a measurement | One command, and it decides whether the signal needs a read-before-write or can add unconditionally. A re-run of a push, or a re-push, reaches this path | it is a no-op; the signal adds unconditionally and never removes | ✅ **the default was overturned, and the question stopped being load-bearing** — 2026-09-11, phase 1. It cannot be measured without a write to the tracker, and GitHub's REST reference for *Add labels to an issue* says only that the endpoint "adds labels to an issue's existing labels" and lists no response for the duplicate case (read, fetched 2026-09-11). So the signal reads the issue's labels first and adds only what is missing: S3's guarantee holds by construction rather than on an unmeasured assumption, and the path GitHub's answer would govern is never reached. The same shape settles the label itself, where the answer IS known and is the opposite — `gh label create` on an existing name errors, so an unconditional create would fail every push after a release's first squash. Executed: dropping either read turns `test_an_issue_that_already_carries_the_label_is_left_alone` and `test_an_existing_label_is_not_created_again` red |
| Q7 | Do the new scripts import `close_issues_on_release` directly, or do the shared readers move into a module of their own? | the work | `plan.md`'s alternatives table rejects the extraction for now: `FENCE` carries a `# RIDER:` stamp resolved against its own file, and moving the symbol forces a re-stamp on a comment whose subject is issue #266's open decision. Phase 1 sees how much of the closer it actually needs and confirms or reverses that | import directly | ✅ **confirmed** — 2026-09-11, phase 1. The signal needs five of the closer's readers: `MERGED_PR`, `keywords_in`, `pull_request_body`, `arrived` and the 404-tolerant `_issue_api`, which is most of the module rather than a corner of it. Extraction would buy nothing and would move `FENCE`'s rider anchor. What the phase added to the answer: the **name mapping** — branch → version → milestone → label — is new, belongs to neither script by inheritance, and lives in the signal, which phase 2's gate imports for it. One source, the way the readers are one source |
| Q8 | Is `$(git merge-base origin/main HEAD)..HEAD` the range, or does something about how the release branch was cut make a simpler one correct? | the work | It matters only once `main` has moved during a release's life, which a hotfix does. Phase 2 writes the case that pins whichever it chooses | the merge-base range | ✅ **the merge-base range** — 2026-09-11, phase 2. The two forms give the same set under every history this repository has today and come apart the moment a hotfix that was also merged into the release branch moves `main` mid-release. `merge-base` is what *what this release accumulated since it was cut* means. The case pins the two commands — `git merge-base <base> HEAD`, then `git log --format=%s <that>..HEAD` — rather than the resulting set, because the resulting set cannot tell the two spellings apart here |
| Q9 | Does `docs/branch-and-release.md` need an edit too, or does `docs/issues-and-milestones.md` alone carry what changed? | the work | `branch-and-release.md` §*So a workflow reads the keywords instead* describes the closer and is still true of it. Whether a reader of that section needs to be told a second mechanism now reads the same keywords is a judgement made while the prose is being written, in phase 4 | the milestone document alone; `branch-and-release.md` is untouched except that this work item edits no sentence in it | ✅ **the default is overturned — one paragraph is added, and no sentence in it is edited** — 2026-09-11, phase 4. Read in full, §*So a workflow reads the keywords instead* is still true of the closer and every sentence in it survives untouched. What stops being true is the conclusion a reader draws from the section around it: it says a keyword written into a release base does nothing until `main` moves, and after this work the same keyword is read on the release-branch push to write the label. So the paragraph names the second reader, says it writes no close, and sends the reader to `docs/issues-and-milestones.md` for both. It is a statement about a workflow rather than about the sequence, which is the line `spec.md` §*Out* itself draws when it lets the release checklist's sentence be corrected |

Answered rows feed back into `docs/` — a policy clause or an open-questions
section — before this directory's work merges.
