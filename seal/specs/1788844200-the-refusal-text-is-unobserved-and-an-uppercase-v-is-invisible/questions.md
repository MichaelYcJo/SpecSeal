# 1788844200-the-refusal-text-is-unobserved-and-an-uppercase-v-is-invisible — questions for the planner

<!-- The routing batch was answered before this work item was spawned, and each
of the four tickets carries its own *What would close it*. Q1 arrived during
the work and is written here rather than raised, per the spawn's own
instruction. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q1 | Should a case pin the two documents that name `test_no_loaded_file_names_a_version_at_or_above_the_running_one` agreeing with each other? #206's cause is that they disagreed, and nothing stops them disagreeing again. | **Build it** — a case reading both and asserting each states at-or-above; catches the next drift, and is new mechanism in a fix the four tickets scope to prose. **Leave it** — the sentence is corrected and the class stays unenforced; this is the third time a sentence about this check has needed correcting. | Left, and the ledger row S4 says so in as many words so the gap is a record rather than an omission. | ⬜ |
| Q2 | Should the case pinning the check's own `assert not offenders, refusal(running, offenders)` be planted? Review round 1 wrote it and ran it — swap the module globals `tracked` and `timers_in`, call the check, compare the raised `AssertionError` to `refusal(running, offenders)`; it reads no source and it passes. | **Plant it** — the last survivor of the mutation set closes, and a unit added by a fix pass ships without a round reading it. **Leave it** — the survivor stays on the case's own list, where it is a measured fact rather than a limit, and a later work item plants it under review. | Left. The round's finding was a false sentence in prose, and correcting the sentence is what closes it; the pin is a unit the finding does not require. | ⬜ |

Answerer: the review orchestrator, or the repository owner it asks. It is a
scope decision rather than a technical one — the correction #206 asked for is
complete either way.
