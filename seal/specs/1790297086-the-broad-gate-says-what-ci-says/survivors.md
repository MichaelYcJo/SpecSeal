# 1790297086-the-broad-gate-says-what-ci-says — survivors

| Path | Quote | Grounds |
|---|---|---|
| `seal/specs/1790260567-the-broad-gate-hands-cmd-a-forward-slash/spec.md` | telling a program from a directory is #596 | The spec of the work item that shipped #448 in 0.15.3, recording what its round 2 decided and deferred to #596. It is a past state, and this work item is what closed #596; the design record keeps what was true when it was written |
| `seal/specs/1790260567-the-broad-gate-hands-cmd-a-forward-slash/spec.md` | A switch written with a blank before it (`xcopy /e`) is left as written, and the template says so | Same record, same grounds. The sentence is still true of the scan: a blank before a switch is never rewritten |
| `seal/releases/0.15.3.md` | and #596 holds the behaviour fix | Round 2's dated **Corrected** note on row A2, kept as the history of the claim. The **Corrected 2026-09-25 by work item 1790297086 (#596)** note after it on the same row says that it stopped being true and what replaced it |
| `seal/releases/0.10.0.md` | which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` | Phase 3's dated **Re-read** notes on S7 and S12. They share their opening with the Re-read note round 1's ⬜ 5 replaced on 0.12.2 R2, and that note went because R2's own claim turned false, not because the shared words did. The claims these notes stand on still hold, and each note is a reading somebody performed on that date |
| `seal/releases/0.12.0.md` | which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` | The same phase 3 **Re-read** note, on the row about the value being looked at before a shell gets it. Its claim does not depend on which arms run, and it holds |
| `seal/releases/0.12.2.md` | which made `gate` leave the `survivors` and `corrections` arms out where the base names `main` | The same phase 3 **Re-read** note, on G3. Both arms are still arms and still run at every other base, so the claim holds; R2, row 9 of the same file, carries the **Corrected** note that replaced its own |
| `skills/verify/scripts/broad_gate.py` | so a case can drive the `cmd.exe` branch from any machine the way `quote` is driven | `run`'s docstring, about its `windows` and `comspec` arguments. It shares two phrases with the comment round 2's 🟡 1 removed from `handed_to_shell`, which said `ntpath` reads `%VAR%` on every platform; that comment went because `ntpath.expandvars` went. This sentence is about a different fact, and it holds: `run` passes both arguments through |
