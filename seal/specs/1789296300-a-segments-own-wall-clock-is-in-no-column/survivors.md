# survivors — round 1's fix pass

<!-- `survivor-check --range <a>..<b> --exempt <this file>`. Every row quotes
the standing text, so the exemption stops holding as soon as that text
changes. Range: d146bd7..HEAD, the fix pass answering round 1. -->

| Place | Quote from the standing text | Grounds |
|---|---|---|
| `skills/verify/scripts/session_cost.py#spawn_cycles` | `rows = []` then `for index in range(len(cuts) + 1):` over the orchestrator's own bands | **The two loops are deliberately different and one of them must not be changed.** `segment_slices` now drops a window holding no call, because a cut there is a coordinator's message and an empty window between two of them is a stretch the agent never worked. `spawn_cycles` slices the ORCHESTRATOR's own transcript into bands, and its rows partition the run's calls: a cycle in which the orchestrator issued nothing is a real band, and `test_a_row_with_no_call_keeps_its_place_in_the_partition` pins exactly that. Carrying the fix across would turn that case red and break the partition the between-the-rows arithmetic rests on. The report is a true one about shape and a false one about meaning, and this row is here so the next reader meets the reason rather than the resemblance |
| `seal/specs/1789296300-a-segments-own-wall-clock-is-in-no-column/phases/phase-4.md` | The instrument found the breach on real data, thirteen times | A phase record holds what was true when it was written, which is the boundary this repository already draws for its own sweeps — `seal/ledger/1789296300-…`'s tenth row states it, and `CHANGELOG.md` and every `seal/specs/…` directory are left alone on the same ground. The sentence is also not false: 13 runs do carry the line. What the fix pass added is which of the 13 name an agent §6 reaches, and that correction belongs in the live claims — the ledger fragment's evidence cell, `changelog.md` and `overview.md` — all three of which were corrected rather than exempted |
| `seal/specs/1788360817-the-pull-request-language-is-fixed-inside-a-skill/overview.md` | `tests/test_docs_line_wrap.py`, `tests/test_first_setup_asks_once.py` | Tokenisation noise, and the eight rows below it are the same one. Finding 8's fix widened this work item's own proof block from one module count to seven, so its `verified:` line now holds a long list of `tests/test_*.py` names. Every other work item's proof block and phase table holds such a list too, so the shared n-grams are `py tests test`, `wrap py tests test` and `identifiers py tests test` — the punctuation between module names, with no claim in them. Nothing was removed from any of these places and nothing in them is now false |
| `seal/specs/1789172128-a-release-is-sized-by-a-count-and-cut-by-urgency/overview.md` | `verified: **executed** — `tests/test_a_release_` | Same as above — a module list colliding with a module list |
| `seal/specs/1788277657-one-bar-misreads-two-of-the-three-segment-kinds/plan.md` | `tests/test_docs_line_wrap.py tests/test_one_word_one_meaning.py tests/test_no_real_identifiers.py` | Same as above |
| `seal/specs/1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in/plan.md` | `tests/test_no_document_names_the_old_roots.py`, `tests/test_no_real_identifiers.py` | Same as above |
| `seal/specs/1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in/spec.md` | `tests/test_no_real_identifiers.py`, `tests/test_no_document_names_the_old_roots.py` | Same as above |
| `seal/specs/1788433011-every-spawn-prompt-is-retyped-from-memory/plan.md` | `tests/test_the_set_a_work_item_always_has.py tests/test_one_word_one_meaning.py` | Same as above |
| `seal/specs/1788433011-every-spawn-prompt-is-retyped-from-memory/plan.md` | `tests/test_the_changelog_is_gathered_at_release.py tests/test_the_ledger_fragments_fold_at_release.py` | Same as above |
| `seal/specs/1788993115-a-payload-is-written-again-on-every-spawn/plan.md` | `tests/test_a_row_points_by_content.py tests/test_the_ledger_fragments_fold_at_release.py` | Same as above |

**What was corrected rather than exempted.** `seal/ledger/1789296300-a-segments-own-wall-clock-is-in-no-column.md`'s seventh row carried the same `13 of the 43 carry a finding, one of them a specseal:warden` sentence as the phase record, and it was corrected instead — a ledger row is a live claim about the tree, re-read and re-stamped, so it owes the number that is true now. That is the whole distinction this file is drawing: records keep what they were written with, claims do not.
