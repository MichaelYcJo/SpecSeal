# settle leaves twelve directories with no way out — questions for the planner

<!-- seal/specs/1790138190-settle-leaves-twelve-directories-with-no-way-out/questions.md
— decisions only a human can make. The run is `automation`: nobody is asked,
so every row says why the tree could not settle it, and every default
continues. -->

## What the tickets left open and the tree answered — do not reopen

| Settled | Where the grounds are |
|---|---|
| what a spec-less directory's retirement records — nothing: it is retired by the rule, with no marker and no acknowledgement. The spawn prompt's summary called the record's shape the open design question; #517's owner comment of 2026-09-23 closed it (D3) | `spec.md` §*What the owner already decided* |
| which of D1's two answers a fold runs under — outside the chain, `: '[no-review]';` on each commit, judged at its pull request, no directory; not a lightweight route | `spec.md` §*Which of D1's two answers*: the chain's records live only in `rounds/`, and a second home for them is the log D2 refuses |
| that the rule arm also requires no open `## Not verified` and no open `evidence-todo.md` row — one condition narrower than D3's sentence. **A judgment, and the owner can overturn it**: the alternative is a rule arm that drops open rows, which is #514's measured L2 and which `unverified_check --baseline` refuses unless it is taught the same drop | `spec.md` G2 |
| that D3 overturns a `docs/` statement (*kept by name*) although a ticket ranks below policy: the owner who ratifies `docs/` made the decision in a dated comment, and the overturned paragraph itself named #517's design comment as where the question was carried. The pull request is where the owner sees the rewritten sentence | `spec.md` Grounding, `docs/the-evidence-ledger.md` §*The fold* |
| that the guard keeps per directory and exits 1, rather than refusing the whole run | `plan.md` §*Alternatives*; the evidence-todo guard's shape and the standing *the directory stays* |
| the answer for the one row the guard finds today — REMOVED, the claim re-founded on the `docs/review-chain-spec.md` unit that already states it | `spec.md` G4; #517's body, repair step 2 |
| that every CI reader asks one predicate, and survivor-check leaves a retired directory out of its range | `spec.md` G5, `plan.md` §*What breaks in six months* |
| that this branch retires nothing | #517 §*Sequencing*; `spec.md` O2 |
| that an absent `seal/specs/` under a present root is a settled state, for `settle` and for `unverified_check.py` | `spec.md` G7 |
| that #514's F1–F3 are **not** green at zero, and are re-pointed with the rest | `spec.md` G8, read at `_the_walk_found_every_committed_record` |
| that `seal/follow-up.md` holds no row this work is the prerequisite of | read, every row of §*Schedulable items* |
| the record language — English | `seal/config.md` has no `Record language` row |

## The rows that remain

| # | Question | Who can answer | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|---|
| Q1 | Should a fold travel as a release at all, or go to `main` as its own pull request with a merge commit, no release branch, no milestone and no changelog fragment? This is #517's second owner comment, marked **to be examined, not decided**. What reading `.github/workflows/hygiene.yml` established (read, nothing run): a fold touches no path the version step counts as shipping, so *a change to what ships must move the version* has nothing to refuse; the survivor and correction steps exit early on `main`; the changelog-gathered and ledger-fold steps and the milestone step all run on a pull request into `main`, and whether each passes for a fold was not read past the step's comment; `CONTRIBUTING.md` §*Opening a pull request* and `hygiene.yml`'s version message both say a pull request into `main` **is** a release, so a fold would be the named exception. Not established: whether `Closes #N` fires on a direct merge to `main` (GitHub documents that it closes on the default branch — one merged fold would show it), how often an open release branch edits the `docs/` files a fold writes, and which checks the `main` ruleset requires with no release preparation running | **a person** — the repository owner; the comment reserves it, and the answer changes `docs/branch-and-release.md`'s merge table, which `CLAUDE.md` says is never chosen on one's own | through the open release branch → today's route, nothing more to build · straight to `main` → `docs/branch-and-release.md`, `CONTRIBUTING.md` and `docs/release-checklist.md` §2b name the exception, and the version step learns to recognise a fold by the paths it touches | the fold keeps today's route, a pull request into the open release branch. **Nothing here waits on it**: phase 4's range exclusion makes a fold pass the survivor step on either route, and the rest of the checks behave the same on both | ⬜ |
| Q2 | A ledger row with more than one anchor, one of them inside a directory being retired: narrowed to its live anchors, or REMOVED? | **a person** — the repository owner; `seal/ledger.md` §1788354065's S12 row already reserves it, and #511's body repeats that the multi-anchor answer is the owner's | narrowed → the row keeps its claim on what still holds · REMOVED → the claim is written anew wherever it still stands | the guard prints *narrow* for such a row, naming the owner's question in the same line, which is what `docs/the-evidence-ledger.md` §*The fold* already states. No such row exists today (`spec.md` G3), so the default decides no edit on this branch | ⬜ |
| Q3 | `skills/settle/SKILL.md` §*What a fold branch owes* says *Nothing in `seal/ledger.md` moves*, and both folds moved it — #504 removed rows, #516 re-verified four. #511's deferral note says the repository owner answers the sentence | **a person** — the repository owner, named by the deferral itself | keep the sentence → the skill disagrees with `docs/the-evidence-ledger.md` and with what the guard now asks a fold to do · replace it → the skill says what the policy already says | replaced, to agree with `docs/the-evidence-ledger.md`: a fold appends nothing to `seal/ledger.md`; it removes a row the guard named, and re-reads and re-verifies a row whose anchored unit its prose edited (phase 6) | ⬜ |
| Q4 | Does every module in `spec.md` G8, and every CI reader, pass on a tree with no `seal/specs/` at all — and is G8 the whole list? | **a measurement** — G8 was read from the readers; a read is not a run, and the list was drawn by `grep` plus reading, which is how #514's frame shipped F1–F3 wrong | green → nothing more · red → the phase adds the reader to G8 in its record and answers it by `skills/settle/SKILL.md` §3 | assume G8 is complete; phase 5's scratch clone runs every module `grep -rln "seal/specs" tests/` names, not only G8's | ⬜ |
| Q5 | Which §3 answer each floor in G8 gets — an independent listing that holds at any size, or its property moved into a `tmp_path` corpus | **the work** — the answer depends on what each case's property is about, which is read when the case is opened | independent listing → the case still reads the real tree and says so at zero · `tmp_path` corpus → the case stops depending on what the repository holds | the listing where the case asks *did the walk read what is there*; a `tmp_path` corpus where it asks *does a record of this shape exist* (`assert teeth`, `assert mirrors`). Never a smaller literal | ⬜ |
| Q6 | Does the survivor sweep report sentences this branch's own rewrites leave standing — the *kept by name* rule, for one, is quoted in `seal/specs/1790119502-…/spec.md`? | **the work** — the sweep's score over this branch's range is unknowable until the rewrites exist | nothing reported → no `survivors.md` · survivors → each judged, and a quote row with its grounds for each one correct where it stands | phase 7 runs the sweep and writes rows only for what it reports | ⬜ |

## Couplings named, not designed

| Issue | Where it touches this work | Why nothing here designs it |
|---|---|---|
| #518 — the commit gate's review arm asks for a work item on a change no reviewer has anything to compare | D1 puts `: '[no-review]';` in front of every fold commit. A lighter tier for `docs/` and `seal/` would remove those waivers from the fold, but #518's own table lists a `seal/specs/**` removal as *already guarded by the fold marker* and `docs/` as the place a fold's real risk lives | named out in the spawn; #518 says a line must not be drawn before its measurement exists |
| #519 — `seal/ledger.md` grows every release and nothing takes a row out | the guard (phase 1) and G4 are the first path that takes a row out on purpose. A ledger compression that keeps only anchors would change what the guard reads, not whether it reads | named out in the spawn; #519 says to measure before designing |
