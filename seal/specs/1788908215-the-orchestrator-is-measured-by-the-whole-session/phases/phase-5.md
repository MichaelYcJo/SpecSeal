# 1788908215-the-orchestrator-is-measured-by-the-whole-session — phase 5

| Field | Value |
|---|---|
| Phase | 5 |
| Commit | the reading's own commit; **posting is not done** and is the orchestrator's |
| Ran by | unknown — the spawn prompt named no model. `templates/sdd-phase.md` makes this row the spawning session's and forbids a segment sourcing it from its own idea of what it is, so it is left for the orchestrator to fill |

## What this phase was asked

Produce the reading — the mode over this release line's own orchestrator
transcripts, with what it covers stated beside the number — and leave the
draft comment body in a file under the work item directory. The spawn prompt
reserved the posting itself for the orchestrator and forbade running
`gh issue comment`. It was not run.

## What this phase found

**Which transcripts.** Eleven transcripts in this project carry an `Agent`
call. Three are the 0.9.x line's own orchestrator runs, dated 2026-09-06,
2026-09-07 and 2026-09-08 — 61 cycles, 3 head rows, 3 tail rows, 953 calls.
This release's own run is excluded and the reading says so: it was still open,
so it has no tail row, and a partial transcript quoted without saying so is
what `skills/verify/SKILL.md` §*A comparison against a run whose transcript
covered only part of its branch* is about.

**The reading found the defect in its own premise**, which is the finding
phases 3 and 4 were then corrected for and `questions.md` Q4 now carries.
The `delegated` column read seconds on every real cycle, and rather than
publish a column of near-zeroes the pairing was measured: 1.5–3.7 seconds
across 67 spawns, and 61 of 67 subagent transcripts opening within one second
of their spawn's result stamp. The result is written when the spawn is
accepted. The delegated wall clock is in the next row's `model`, and past
900 seconds in no column at all — which is what the runs' 10%, 41% and 43%
unaccounted shares are.

**1.00 tools per turn, and it is real.** Every row of every run reads exactly
1.00, which is the shape of a meter defect this repository has already had, so
it was counted from the raw transcripts instead of through the meter: the
calls-per-message distribution is `{1: 497}`, `{1: 147}` and `{1: 309}`.
Not one batched call in 953. Even consecutive spawns are consecutive
single-call turns. At a mean gap of 18–29 seconds a cycle, that is the
batching advisory's own case made by the orchestrator against itself, and it
is the sharpest thing in the reading.

**23 of 61 cycle rows hold nothing but their own spawn.** A group of spawns
sent back to back closes a cycle at each result, so a group of four yields
three near-empty rows and charges the whole following window to the fourth.
The reading separates them, because a band taken across all 61 without
separating them would halve every median — and a later reading has to
separate them the same way to be comparable at all.

## What this phase removes

| Removed item | Where it must land |
|---|---|
| none | none |
