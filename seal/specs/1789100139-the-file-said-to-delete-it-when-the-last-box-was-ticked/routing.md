# 1789100139-the-file-said-to-delete-it-when-the-last-box-was-ticked — routing

| Axis | Answer |
|---|---|
| Review | through the review chain |
| Destination | open the pull request |
| Implementation | smith |
| Planning | the session |
| Branch | docs/351-the-file-said-to-delete-it-when-the-last-box-was-ticked |

Answered 2026-09-11 by the repository owner, before the first edit.

## Why this way

Removing a document moves what it carried into four other places and takes a
procedure section into a skill, which is instructions a session reads and acts
on — the top rung of the SDD ladder, so the chain runs. `Planning` reads `the
session` because `framer` shipped in 0.11.0 on this branch's own base and no
installed session has spawned it yet; this frame was written here, and the row
records that rather than leaving it unanswered.
