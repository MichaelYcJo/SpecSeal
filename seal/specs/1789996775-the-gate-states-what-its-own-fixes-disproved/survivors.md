# the gate states what its own fixes disproved — excused survivors

<!-- Read by `survivor-check --exempt`. The QUOTE is the anchor, so an
exemption stops applying the moment that text changes, and an excused survivor
is still printed with its grounds. There is no value meaning "check nothing". -->

Both rows below survive the same removed sentence, and both survive only its
true half. A3 removed `panel`'s

> `seal_stamp.letter` cuts a value at 23 columns, so a ref longer than that is
> cut here — which is why the line the gate prints when resolving MOVED the
> answer is the authoritative statement and this row is context

for its SECOND clause. The first clause is a measurement that still holds —
`PANEL_VALUE_WIDTH` is 23 and `test_the_panel_value_width_is_what_the_stamp_actually_gives`
re-measures it against `seal_stamp.letter` on every run. What the range
corrected is the inference drawn from it, and neither standing text draws it.

| Path | Quote | Grounds |
|---|---|---|
| `seal/ledger.md` | `so `origin/release/v0.12.0` (22) fits and a longer ref does not` | R5 of work item 1789956662, Notes. It states the width and what fits at it, which is why the first case set never exercised the cut — and it goes on to say the case now asserts both a ref that fits and one that does not. It never says the printed line is the authoritative statement. Correcting it would mean deleting a measurement that is still true out of a row this work has just re-read, and `CLAUDE.md` permits touching the shared file to leave a row true, not to leave it shorter |
| `tests/test_the_gate_asks_the_range_ci_will_ask.py` | `A4 means nothing prints beside the row on an agreeing run — so a ref that does not fit has to say so in the row itself` | `test_a_ref_too_long_for_the_panel_says_it_was_cut`'s docstring, and it is the reasoning A3 moved INTO `panel` rather than out of it: the printed line is silent on an agreeing run, so the row has to speak for itself. It is the opposite of the clause the range removed, and it shares only the subject of the sentence |
