# 1789919879-the-outside-contributor-has-no-procedure — survivor exemptions

## Round 1's fix pass — the phase record keeps its own account

`survivor-check --range c8e7a9d0..HEAD` reports three places. One is a real
survivor and was corrected: `tests/test_the_contributor_has_a_procedure.py`'s
docstring still called `survivor_check.py` "the one check", which 🟡 4 is
exactly about.

The other two are phase 5's record of phase 5. 🔴 1's repair rewrote
`overview.md`'s divergence row — the wrong coordinate is now corrected with
the `NAME NOT IN TREE` marker rather than by writing the name bare — and that
row used to narrate the same two events `phases/phase-5.md` narrates. What the
range removed is `overview.md`'s copy, not the fact. A phase record that lost
these sentences would leave the second correction attempt unexplained, and a
phase record is a past-state document: it says what that phase did, which is
still what that phase did.

| Path | Quote | Grounds |
|---|---|---|
| `seal/specs/1789919879-the-outside-contributor-has-no-procedure/phases/phase-5.md` | "**The first correction attempt failed the same check**, because the comment quoted the old name in backticks and the checker reads backticked names." | **Phase 5's own account of an event in phase 5.** The range did not correct this claim; it removed `overview.md`'s duplicate of it, because that row is now about round 1's repair instead. The sentence states what happened when phase 5 corrected the coordinate, and it stays true whatever later rounds do to the lines it produced. Removing it would leave phase 5's second correction attempt with no stated cause |
| `seal/specs/1789919879-the-outside-contributor-has-no-procedure/phases/phase-5.md` | "Corrected to `body_from` in the same row, with an HTML comment holding what it was and why." | **The same shape, and still a true description of `spec.md`.** That row does carry `body_from` and an HTML comment holding the retired name and the reason. `overview.md`'s near-identical sentence went because its row now describes the marker repair; this one describes what phase 5 wrote and what the tree still holds |
