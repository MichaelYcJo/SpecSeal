# a piped broad gate row takes every config row below it (#415) — questions for the planner

<!-- seal/specs/1789598366-a-piped-broad-gate-row-takes-every-config-row-below-it/questions.md
— decisions only a human can make, extracted so nothing ships on a silent
assumption. Before adding a row, check the inheritance rule: if policy is
silent but existing behavior answers it, inherit and record — only genuinely
NEW rules belong here. -->

| # | Question | Who can answer | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|---|
| Q1 | **Is the `Broad gate` cell a markdown cell or a shell line?** The two answers build different code, which is why this one blocks. Everything else in `seal/config.md` is short prose that never needs an escape, so this row is the first place the file's format has had to be decided out loud | **a person** — the repository owner. It is what the product is, and `templates/config.md` presents the row to them as *one shell command line* | **(a) A markdown cell, and it takes markdown's escape.** `\| Broad gate \| bin/test -q \\\| tee out.txt \|`. The reader learns the escape, phase 1 exists, and the pipe becomes writable. `templates/config.md` already writes its own cells that way, so the file's other tables would stop disagreeing with the reader. Costs: a person typing a shell command line has to know to escape one character, and the row's documentation has to teach it. **(b) A shell line, and a pipe stays out of it.** The reader is not taught the escape, phase 1 does not happen, and only phase 2's message lands — a piped row is refused by name instead of by the wrong cause, and the documentation says to use `&&` or a wrapper script. Costs: a form the criterion calls legal has no spelling at all, which is the state #415 was opened about; and a `Mode` row hidden below a piped row is still lost silently, so `seal mode` can still write a second `Mode` row into a person's file | **(a)**, and the grounds are the file rather than a preference: `seal/config.md` is markdown, `\|` is markdown's own answer for a pipe in a cell, and the plugin's own template already relies on it | ⬜  ✅ **answered 2026-09-17 by the repository owner — (a), a markdown cell, with one binding constraint.** The grounds the frame gives stand: `seal/config.md` is markdown, `\|` is markdown's own answer for a pipe inside a cell, and `templates/config.md` already writes its cells that way. **The owner added the constraint, and it decides the implementation rather than the choice: the unescape must be exactly the two characters `\|`, never a general backslash unescape.** Measured 2026-09-17 against the reader as it stands — `| Broad gate | C:\Python\python.exe -m pytest |` comes back with the path intact, and that is the state the repository has already synced across operating systems. A blanket `re.sub(r'\\(.)', r'\1', value)` turns it into `C:Pythonpython.exe`; `value.replace('\\|', '|')` leaves it alone. So a case must pin that a value carrying Windows path separators survives the reader unchanged, beside the case that pins the pipe. The unescape happens in the READER, before any shell sees the value, so `/bin/sh` and `cmd.exe` both receive a plain `|` and `cmd.exe` not treating backslash as an escape character never arises. |
| M1 | Does `skills/implement/scripts/seal.py#with_row` insert a **second** `Mode` row when the first sits below a line `table_span` cannot parse? | **a measurement.** One read of the written file settles it; asking a person is the wrong instrument | If it does, it is the worst outcome in this work item and the strongest argument for phase 1 — a person's config left two rows deep, which `table_span`'s own comment says no command can bring back into agreement. If it does not, phase 1's second case pins the agreement instead of a repair | Read, not executed: `table_span` stops on the same rule as `config_rows` and `with_row` inserts at `end`, so it should duplicate. **Stated as read, not as measured** | ✅ **Executed 2026-09-17 in phase 1 — it duplicates.** `write_row` over a config whose `Mode` row sits below a piped `Broad gate` row left the file **two `Mode` rows deep**: `declared_mode` answered `('none', '')` for a file whose own row says `shared`, `table_span` returned `(-1, 5)`, and `with_row` inserted at the end it could see, above the person's own row. It is the worst outcome in this work item and the strongest argument for phase 1. `phases/phase-1.md` has the measurement and the file it produced |
| M2 | Does any production path read `Record language` or `Commit and pull request language` through `config_rows`? #415 says both *fall back to their defaults with no message anywhere* | **a measurement.** One grep over the tree | Read: nothing does. Both rows are read by sessions following `skills/commit-pr-convention`, `skills/implement` and `skills/agent-contract` §11 from the raw file, where a human sees the line the parser refuses. The only code reader is the test-local copy phase 3 removes. If that holds, the harm restates: what a piped row actually takes is `Mode` and `Broad gate`, and the language rows are a hazard for a future code reader rather than a live loss | Read: **no production reader**. The ticket's sentence is true of `config_rows` and not yet true of anything that calls it | ✅ **Confirmed 2026-09-17.** The one code reader outside `config_rows` was `tests/test_the_pull_request_language_is_the_repositorys.py#items`, which phase 3 closed by pointing it at the one reader. What a piped row actually takes is `Mode` and `Broad gate`; the language rows are a hazard for a future code reader. Every record this work item writes says that rather than repeating the ticket's sentence |
| M3 | Does the escape-aware pattern change what `config_rows` returns for any `\| Item \| Value \|` table already in this tree or in the test corpus? | **a measurement.** Read every such file before and after; A6 is the acceptance scenario it answers | ~~Widening a cell pattern can only make more lines into rows, never fewer, so nothing should move.~~ **Corrected 2026-09-17 by round 1 🟡 2: that is an argument about the tree and not a property of the pattern, and as a property it is false.** The measurement below is what the row was for and it stands; the sentence around it claimed more than one run over one tree can. A file whose reading DOES move is a divergence row in `overview.md` naming the file — never a reason to weaken A6 into something that passes | Nothing moves | ✅ **Executed 2026-09-17 in phase 1 — nothing moved.** Population: 1,449 tracked files read in full, ten of which hold a readable `\| Item \| Value \|` table. Every row of all ten is byte-identical before and after the change (`diff` over the two dumps, exit 0). A6's *nothing else moved* half holds and there is no divergence row to write. **Re-measured 2026-09-17 in round 1's fix pass, line by line rather than table by table**: over 1,460 tracked files, 84 lines read differently through the two patterns, 83 of them lines that were not rows before and are now, and **one** that was a row and is not — a fixture inside `rounds/round-1-report.md`, quoting the very shape 🟡 2 is about. So the reading of every `\| Item \| Value \|` table in the tree is unmoved, and the narrowing has no live instance here |
| W1 | Does the new refusal quote the **whole** refused line, or only its first cell? | **the work.** The phase that builds the message decides it and records a divergence row | Whole line: the person sees exactly what they typed, which is what makes the message actionable. First cell only: shorter, and it cannot spill a long command into a refusal that is already several sentences. `CLAUDE.md` §*no real identifiers* binds either way — the line comes from the person's own file at runtime, so nothing about it reaches a fixture | The whole line, quoted, because a refusal that does not show the line sends the reader back to the file to guess which one it meant | ✅ **Decided 2026-09-17 in phase 2 — the whole line.** The refusal quotes it as written, names `\|` as the way to write the pipe, and says every row below it is lost too. No divergence row was written: the default was taken, so there is nothing to diverge from. `phases/phase-2.md` carries the decision and the grounds |

**`Who can answer` takes one of three values and nothing else.** One row here
is a person's and blocks; two are measurements that must not queue behind
them; one belongs to the work and travels no further than the phase that
meets it.

**Q1's two answers both continue.** Neither is a `no` that leaves the work
with nowhere to go: (a) builds four phases, (b) builds three. What (b) costs
is written into its own cell rather than left for the answerer to derive,
because a question whose options do not price themselves is a question asked
twice.
