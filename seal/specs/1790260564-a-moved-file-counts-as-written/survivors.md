| Path | Quote | Grounds |
|---|---|---|
| `hooks/review-skill-gate.py` | answers relative from a main tree | That comment describes the gate's own `rev-parse --git-dir` reader, which it joins onto `cwd`, and it is true of that code. Round 1's fix pass removed `local_specs`'s copy of the same observation because the reader moved to `hooks/optin.py#git_common_dir` (⬜ 6); the fact itself was not corrected |
| `tests/test_a_corrected_sentence_survives_elsewhere.py` | a copy without it cannot say whose a local-mode declaration is | O5's docstring is about `hooks/routing.py`, and that is what `routing.py` says, so it is true. Round 2's fix pass removed the same words from `hook`'s docstring because `hook` also loads `hooks/optin.py`, which answers where local mode's root is (⬜ 3); the routing reader's sentence was not corrected |
