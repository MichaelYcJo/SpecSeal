# the checker is wrong about itself, and nothing goes red — questions for the planner

<!-- seal/specs/1789425391-the-checker-is-wrong-about-itself-and-nothing-goes-red/questions.md
— decisions only a human can make, extracted so nothing ships on a silent
assumption. Before adding a row, check the inheritance rule: if policy is
silent but existing behavior answers it, inherit and record — only genuinely
NEW rules belong here. -->

**Six rows, and one of them blocks the build.** `Who can answer` takes one of
three values and the count is the point: the two judgments the tickets state
and refuse to settle — #335 (may `seal` know it is holding the last record) and
#333 (does a file-level attribution still refuse) — are **not** here, because
`docs/review-handoff-protocol.md` and `docs/review-chain-spec.md` answer both.
They are decided in `plan.md` §*The two judgments*, with the passage that
decides each. A question a document could have answered was never a question
(`skills/implement/SKILL.md` §1).

| # | Question | Who can answer | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|---|
| **Q1** | Phase 4 makes `no fixes to check` the only value `seal` will write a `Broad gate` cell beside. `docs/review-chain-spec.md` §*What the record carries* records, as **open**, that this same value beside a `Contract changes` or `New units` row still reading *none — the fixes are not yet written* is *the one place the pair is not merely unrefused but wrong*: a round that commissioned no fixes will never have any, so *not yet written* is false the moment it is written, and nothing refuses it. Narrowing `seal` makes every sealed record a member of that population instead of an edge of it. **Does this work item settle that arm too, or does it stay open?** | **a person** | **(a) Leave it open.** The work item is six tickets and this is a seventh; the spec's own reason for hesitating stands — *the refusal would land on merged records whose repair is honest, unlike the `nobody` case* — and this repository's records are the ones it would land on. Cost: the spec goes on recording a state it calls wrong, and the next reader meets it in a wider population than before. **(b) Refuse the pair here.** One arm, in phase 4, beside the narrowing that makes it pressing. Cost: it is mechanism no ticket commissioned, it fails records already merged, and `spec.md`'s scope says *exactly these six*. **(c) Open it as a ticket for 0.12.0**, beside #174, which is about the arity of the same cell. | **(a) — leave it open, and say in the closing memo that phase 4 widened the population the spec's open note is about.** This is the answer that changes no code, and the scope line in `spec.md` is what it rests on. | ✅ **(a)** — the repository owner, 2026-09-15, before the build |
| **Q2** | #142 states that the real-records lister *sorts before filtering, so `round-2-draft.md` raises `TypeError`*. Read at HEAD, both walkers filter on `routing.round_number` **before** the keyed sort, and the filter landed 2026-09-08 in `14c2a57` (#234) — four days after #142 was opened. **Is that clause stale in all three corpus readers, or does one of them still sort first?** | **a measurement** | One command, and it settles which of #142's three "Done when" rows phase 1 actually has work for. It must cover `tests/test_chain_check_at_the_pull_request.py#_real_records`, `tests/test_the_reopening_is_one.py#_real_records`, and `tests/test_a_finding_id_is_a_bare_integer.py`'s corpus reader — put a `round-2-draft.md` in a scratch clone's `rounds/` and run each module. | **Stale in all three**, on a reading of the three call sites. Phase 1 writes no case for a crash that cannot happen and says so in `phases/phase-1.md` rather than leaving the ticket's clause to look unanswered. | ✅ **stale in all three** — executed 2026-09-15 by phase 1, in a `git clone --no-local` with two `round-N-draft.md` siblings committed beside a real record: all four cases pass, exit 0, no `TypeError`. `phases/phase-1.md` carries the mechanism |
| **Q3** | #334's case needs a genuine *cell written, then the chain check refuses* ending from the **real** `seal`. Today the cheapest route to it is #335's own defect, which phase 4 removes. **Which route is still reachable?** Candidates: a stray unnumbered record under `rounds/`, a `Target SHA` git cannot resolve, an earlier record's row that `chain_check` refuses but `seal` does not read. | **a measurement** | One fixture per candidate, run against the real pair; the first that reaches the state is the route, and the case asserts it **as** the route so it cannot quietly stop reaching it. If none is reachable, that is itself the answer and #334's repair has to bind the two literals some other way — at which point the shared-constant alternative in `plan.md` comes back on the table and this row becomes a person's. | None assumed. Phase 4 establishes it before writing the case, and `phases/phase-4.md` records which candidates were tried. | ✅ **an earlier record's `New units` emptied, left UNCOMMITTED** — phase 4, 2026-09-15, executed. Three of four candidates reach the state and a stray file under `rounds/` does not; and the route has to be uncommitted, because `broad_gate` runs `chain_check` at HEAD before it calls `seal` while `seal` runs it `--worktree` after the write |
| **Q4** | `plan.md` counts the shared-ledger rows anchored on the units this work moves, from `grep` over `seal/ledger.md`. A count of anchors is not a run of the checker. **Which rows actually degrade to DRIFTED, and does any of them make a claim this work makes false rather than merely moves?** | **a measurement** | `evidence-check --strict` at each phase boundary names them. Two are already known to need re-reading on substance rather than re-stamping: **R2** (anchored on `depth_two`, asserting the refusal comes before any cell is written) and **S12** (`broad_gate.py#gate`, `#seal_record`, and the stubbed case). §5 of the contract is why this is a row: an aggregate is not a coordinate. | The claims hold and the rows are re-read and re-stamped in one write per phase boundary. A row whose claim this work makes false is REMOVED and rewritten in this work item's own fragment — never re-pointed. | ✅ **eight rows across five phase boundaries, one write each; every claim holds and one sub-clause was superseded** — measured with `evidence-check` at each boundary rather than counted by grep, and the plan's forecast overshot at every phase (`build`, `id_refusal`, `last_record` and `broad_gate.py` never moved). R2 holds on all three of its counts. **S13's bolded *everything outside two of them is refused* is superseded** and corrected in place rather than the row removed, because the capped-run claim is the row's subject and is untouched; the narrowing is a row of this work item's fragment. **S12 drifted nothing and that is the finding**: its claim rests on a case driving a stub whose text the case itself writes |
| **Q5** | #333's repair attributes a unit to the row whose fix commit added it. `close` already resolves every `fixed` commit and places it inside the range, but `measure` compares only the range's **two ends** (`reader.show(root, a, rel)` against `reader.show(root, b, rel)`) and never asks which commit introduced a unit. **Is the per-commit answer derivable from what `close` already holds, or does it need a second AST pass per fix commit?** | **the work** | Derivable: no new cost, and the walk gains the attribution for free. A second pass: one `measure` per fix commit, which is N times the AST work on a range that is usually two or three commits — acceptable, and it must be measured rather than assumed. Either way the file-level fallback stays, because a single commit answering two findings resolves to nothing at any cost. | The second pass, bounded by the fix range. Phase 5 decides it there and records the divergence if it goes the other way. | ✅ **not derivable — the second pass, as defaulted** — phase 5, 2026-09-15, measured: 127.8 ms against 36.4 ms for the range pass on a two-commit, one-file fix range (3.51×), dominated by git subprocesses rather than by AST work. `measure` compares the range's two ends and never asks which commit introduced a unit |
| **Q6** | #395's ⬜ 5: the short-row guard raises on the **first** offending row, where `id_refusal` beside it names every one — that shape is #303, measured at two round trips per repair. Round 4 left it a note rather than a fix. **Does phase 2 make the guard a list, or does it take the sentence only?** | **the work** | Round 4's grounds for the note: the guard was already an immediate raise before the commit — only its condition widened — and zero committed verdict rows are short, so the cost falls on a reviewer's first draft and nowhere else. Against them: `id_refusal` sits beside it doing the opposite, so the inconsistency is visible to whoever next opens the function, and that is how the class comes back. | **The sentence**, on round 4's grounds, with the list taken only if it is free where the phase already stands. Either way the comment says which and why, so the next reader does not rediscover the argument. | ✅ **the sentence** — phase 3, 2026-09-15. Round 4's grounds re-checked: the guard was immediate before the arm and only its condition widened, and 0 of the 2,044 committed verdict rows are short. The comment says which, why, and what the repair is |

**The `Status` column is ticked by whoever answered, never by whoever asked.**
Q1 is the only row that blocks the build; Q2, Q3 and Q4 are settled by running
something, and queueing them behind a person would spend a round trip on the
wrong instrument; Q5 and Q6 are decided by the phase that meets them and do not
travel back here.

Answered rows feed back into `docs/` — a policy clause, or the open-questions
section — before this directory's work merges.
