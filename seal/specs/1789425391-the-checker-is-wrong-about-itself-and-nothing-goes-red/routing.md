# 1789425391-the-checker-is-wrong-about-itself-and-nothing-goes-red — routing

| Axis | Answer |
|---|---|
| Review | through the review chain |
| Destination | open the pull request |
| Planning | framer |
| Implementation | smith |
| Branch | fix/142-333-334-335-342-395-the-checker-is-wrong-about-itself-and-nothing-goes-red |

Answered 2026-09-15 by the owner, before the first edit.

## Why this way

The first work item of 0.11.5, and the whole of it: six tickets that every one
came out of a review round exercising the chain's own checker, and every one is
that checker saying something false while the suite stays green — a refusal
naming the wrong finding (#333), a cell written and then refused by the same
run (#335), two records in one commit disagreeing (#342), a verdict word
matched on a boundary four documents do not describe (#395), a literal one
package prints and another reads with nothing binding them (#334), and the case
over the real records reading the index while its docstring says HEAD (#142).
The work changes what a checker accepts and what a refusal says, which is
`skills/implement/SKILL.md` §3's top rung, so the framer draws the frame. The
diff reaches three packages and six tickets' worth of cases, which is the size
the `Implementation` row's comment names as the one case for a delegate, so
smith builds it. The owner answered all three axes in one batch before the
first edit.
