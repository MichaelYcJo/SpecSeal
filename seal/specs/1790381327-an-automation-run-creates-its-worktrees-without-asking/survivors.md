# 1790381327-an-automation-run-creates-its-worktrees-without-asking — survivors

| Path | Quote | Grounds |
|---|---|---|
| `docs/worktree-guard-spec.md` | An Agent call carries prose instead, and reading the token out of a prompt was tried and taken back after both halves failed | The history of why the Agent path reads no token. It shares two phrases with the `single_stream` comment phase 3 removed from `guard_worktree_creation`, which went because the parameter went. The fact it states still holds: the Agent path reads no token, and `main`'s Agent block now says so in its own words |
| `seal/releases/0.9.4.md` | The `if consented == "allow"` ternary's else branch is now unreachable from either production caller | Row S3's note from 2026-09-08, the reading #257 recorded. The **Corrected 2026-09-26** note after it on the same row says phase 2 of this work item removed that branch and its rider. A note is a reading somebody performed on its date, and the ledger keeps it |
| `tests/test_worktree_guard.py` | Both paths end at a person anyway, so the token only ever bought deny -> ask. | `test_the_agent_verdict_does_not_depend_on_the_prompt`'s docstring, which the plan required to pass unchanged. It shares two phrases with the Agent block's old token comment in `main`, which phase 3 rewrote. The sentence still holds: the Agent path asks, and a token could only have bought deny to ask |
| `tests/test_the_guard_asks_once_per_session.py` | Silent rather than `allow`: that call is a worktree creation PLUS an agent with a prompt, and the record is about the first half only. | `test_the_agent_path_reads_the_record_a_bash_creation_wrote`'s docstring. It shares two phrases with the `consented="silent"` comment phase 3 removed along with the Agent path's call to `guard_worktree_creation`. The Agent path is still silent under consent for that reason, now in `main`'s Agent block |
| `docs/worktree-guard-spec.md` | **Why the Agent/Task path is silent rather than an allow.** That call is a worktree creation *plus* an agent with a prompt | The same argument, in §*Creation consent*. It holds for the record and for the routing answer alike, and §B's Agent section points back to it |
