# 1789455558-the-record-chain-disagrees-with-itself-in-five-places — questions for the planner

<!-- seal/specs/1789455558-the-record-chain-disagrees-with-itself-in-five-places/questions.md — decisions
only a human can make, extracted so nothing ships on a silent assumption.
Before adding a row, check the inheritance rule: if policy is silent but
existing behavior answers it, inherit and record — only genuinely NEW rules
belong here. -->

**Nothing here blocks the build.** Both person rows are about records and
ticket fields outside this branch, and each carries a default that continues.
The other four are a measurement or the work, and neither kind is a person's.

| # | Question | Who can answer | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|---|
| Q1 | #414 removes the cause. The eleven cells it already rendered wrong live on `fix/401-402-the-broad-gate-row-runs-unchecked-and-is-never-asked-for` — nine repaired by hand at `9919b265`, and `rounds/round-2.md:39` and `:40` still standing. Who repairs those two, and on which branch? | **a person** | **(a)** that branch repairs them before it merges — the release carries no wrong cell, and this branch touches nothing of another branch's; **(b)** a follow-up row after both merge — one edit, but `release/v0.12.0` ships with two records that read wrong; **(c)** this branch repairs them — refused, because two branches would then edit one file and collide at the merge | **(a)**, stated to that branch rather than done here. This branch's phases touch no file under another work item's directory | ⬜  ✅ **answered 2026-09-15 by the orchestrating session.** Neither (a) nor (b): a fourth work item, cut from `release/v0.12.0` after `fix/401-402-…` has merged into it, repairs both cells. That branch is already sealed and marked ready, and a seal binds to a tree state — one commit there spends it and buys the whole broad gate again, for a stray period in two committed cells that misstate nothing. The same fourth work item carries #413, whose coordinate is a case module that exists only on that branch, so it has to wait for the same merge either way and the two travel together at no extra cost. What this branch does is unchanged: it removes the cause, so no record written after it carries the rendering. |
| Q2 | Four of the six issues are milestoned `release: 0.12.0`, #408 is `release: 0.12.1`, and #406 is `backlog: review chain & agents`. This branch ships all six into `release/v0.12.0`. Should the two fields be corrected, or the two issues held back? | **a person** | **(a)** correct the two fields to `release: 0.12.0` — the tree and the tracker agree, and nothing about the build changes; **(b)** hold #406 and #408 back — phases 4 and 5 drop, the subsystem ships half repaired, and #408's message defect stands through another release | **(a)**. `routing.md` already records the owner's answer that the six are one branch because they are one subsystem, so the fields are what is out of step, not the branch. No code changes either way | ⬜  ✅ **answered 2026-09-15 by the orchestrating session — (a), and done.** #406, #408 and #414 now read `release: 0.12.0`, as does #413; #415 went to `backlog: gates & hooks`. The owner's routing answer put all six on one branch because they are one subsystem, so the tracker fields were what was out of step. No code changes either way. |
| Q3 | #405's narrowing: apply the accounting to **every** `close` run, or only where `filled == 0`? Unconditional also catches a table truncated in part; it also fires where round N gained a verdict row after round N+1's `new` had run | **a measurement** | Apply the predicate to every committed `round-N`/`round-N+1` pair under `seal/specs/`. **No unaccounted pair** → unconditional, and the corpus is the evidence. **Any unaccounted pair** → narrow to `filled == 0` and record the shape that forced it | Unconditional. Phase 2 runs the measurement first and the plan's alternatives table names what would overturn it | ⬜ |
| Q4 | #404's alternative: should a coordinate carrying two verdict rows be **refused** rather than resolved first-wins? | **a measurement** | Count the committed records whose verdict table repeats a `Location`. **Zero** → refusal is affordable and is the stricter read. **Any** → refusal would refuse records this repository already wrote, and first-wins is the only option left | First-wins. Round 2 of the previous work item reported dozens of such records; phase 1 re-measures rather than inheriting the count, because an aggregate in prose is not a coordinate (`agent-contract` §5) | ⬜ |
| Q5 | #414's class: is the stray-period shape reachable at the other four `strip(chain.SEPARATORS)` call sites, or only at the fix-table note cut? | **a measurement** | `grep -n 'strip(chain.SEPARATORS' skills/code-review/scripts/round_record.py` and read each. **Only the cut** → the class has one member and phase 3 closes it. **More than one** → each additional member is repaired in phase 3 or, where repairing it changes a vocabulary, written to `seal/follow-up.md` with the owner named | One member. The other three sites read a whole cell rather than cutting a span out of one, so nothing is left behind by the cut | ⬜ |
| Q6 | #408's second arm: what should the message say when a row's verdict word is outside the vocabulary? | **the work** | Settled when the arm is written and the case that pins it is drafted. The shape is fixed by `agent-contract` §14 — the new sentence is pinned in the same commit — and by the defect itself: it must not name 🔴, and it must name the vocabulary, because the reader who is stopped by it is the one who has to choose a word | The sentence phase 4 drafts, reviewed by the round that reads it | ⬜ |
