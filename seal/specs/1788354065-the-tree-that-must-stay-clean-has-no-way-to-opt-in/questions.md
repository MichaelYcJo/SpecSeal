# the tree that must stay clean has no way to opt in — questions for the planner

<!-- seal/specs/1788354065-the-tree-that-must-stay-clean-has-no-way-to-opt-in/questions.md —
decisions only a human can make, extracted so nothing ships on a silent
assumption. The design record decided the shape; the owner decided two
things before this frame and pre-approved every default below. A ⬜ row is
one the record is silent on: the default is what phase 1 or 2 builds, and
the orchestrator's spawn of that phase is the approval. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q0-a | Shared mode's CI. What the shared answer installs, and from where | (a) a new `templates/hygiene.yml` that installs the plugin at its marketplace tag and runs the pull-request checks, the `templates/evidence-check.yml` pattern; written to `.github/workflows/hygiene.yml` at the shared first setup only when absent; local mode installs nothing. (b) vendor the checks the way `evidence-ci` vendors the checker — refused: `chain_check.py` loads `hooks/routing.py` and `hooks/optin.py` beside it, so a vendored copy is three files that drift together | (a) | ✅ owner, 2026-09-02 |
| Q0-b | Every other decision | the record's default where it has one; where it is silent, the default named in the row below, recorded as ⬜ | as listed | ✅ owner, 2026-09-02 |
| Q1 | How a SESSION — not a hook — finds the root in local mode. The hooks resolve it in code; a skill says "write `seal/specs/<id>/rounds/round-N.md`" and a session in local mode has to know that means `.git/seal/…`, and in a linked worktree not even that. Counted: 29 `seal/` paths in `skills/implement/SKILL.md`, 6 in `code-review`, 5 each in `parity-setup` and `legacy-parity`, 5 in `agents/smith.md`, 4 in `agents/warden.md` | (a) one definitional sentence, in the `implement` skill's layout section and in both agents' first lines: every `seal/…` path means `<repo>/seal/` where it exists, else `$(git rev-parse --git-common-dir)/seal/`; every other path stays spelled `seal/…`. (b) a `bin/seal-home` wrapper printing `optin.home(cwd)`, so a session types `$(seal-home)/specs/…` — two more shipped files (`sh` + `.cmd`) and a test, and a spelling nobody outside the plugin recognises. (c) rewrite every path in every skill as `<root>/…` — 60-odd edits that make the shared-mode reader, who is most readers, resolve a variable for a path that is `seal/` | (a). The sentence is the same rule `hooks/optin.py`'s docstring already states, moved to where a session reads | ⬜ |
| Q2 | How `evidence_check.py` resolves the root. It is vendored by `evidence-ci` into a user repository's `tools/`, so it cannot import `hooks/optin.py` unconditionally; today it joins `seal/ledger.md` and `seal/parity.md` under its `root` argument (`evidence_check.py:1378-1382`, `:813`), and `bin/evidence-check` runs the plugin's copy | (a) the plugin's copy imports `hooks/optin.py` by path relative to its own file (`../../../hooks/optin.py`) and uses `home_at(root)`; when that file is absent — the vendored copy, which runs in CI, which is shared mode — it reads `<root>/seal/` as today. One resolver, no copy of it. (b) a second `home_at` inside the checker — the divergent-copies hazard `optin.py`'s docstring names. (c) `bin/evidence-check` resolves the root and passes `--ledger`; the hooks that load the checker (`evidence-advisor.py`, `ledger-migrate.py`) glob for themselves — the CLI run by hand in a local-mode repository through any other path still misses | (a). `--ledger` overrides either way | ⬜ |
| Q3 | `root-migrate.py`'s stamp when nothing old is left. **Read** at d877282 (`hooks/root-migrate.py#main`, the `if not units` block): it stamps only when `<repo>/seal/` is a directory, so a local-mode repository with nothing old is never stamped and re-lists the old names at every session start; and if such a repository later checks out a branch carrying `.specseal/`, the hook moves it into `<repo>/seal/` and stages it — into the tree the person chose to keep clean | (a) stamp when `seal/` exists at EITHER place, read directly (two `isdir` calls, not `home_at`, so the scratch marker does not hide the root from this rule). The old layout on that later branch is left alone, silent, which is the rule's existing backstop (#79 Q8). (b) leave it: a local-mode repository is moved onto the old branch as today, and the person reviews the staged diff | (a). Spec S9 | ⬜ |
| Q4 | What the hygiene template pins. The owner's "marketplace tag" is `v<version>` of the plugin repository (`git tag` lists `v0.0.1` … `v0.4.0`); the version comes from the installed `plugin.json` at setup | (a) `actions/checkout` with `repository: MichaelYcJo/SpecSeal`, `ref: v<version>` pinned into the file by the skill at setup, `path: ${{ runner.temp }}/specseal` — deterministic, and updating is re-running the setup or editing one line, which the header says. (b) `ref: main` — every CI run takes whatever main serves that day, the drift `evidence-ci` refuses for the checker. (c) the plugin's install directory on the runner — there is none; CI does not run Claude Code | (a). The repository name is the plugin's own, not a user's; `tests/test_no_real_identifiers.py` hunts domains and `/Users/` paths and passes `README.md`'s install lines already | ⬜ |
| Q5 | The commit gate's stop text in local mode. It names `seal/specs/<work-item-id>/routing.md` as the file to write (`commit-review-gate.py:860`, `:886`); in local mode that path is `.git/seal/specs/<id>/routing.md`, and a session that types the printed path writes a declaration the gate never reads | (a) name the path under the resolved root, relative to the repository, the way `review-history-guard.py` and `implementer-notice.py` already do (`os.path.relpath(item, top)`), keeping the literal spelling only when the repository is not opted in. (b) leave the text and rely on the Q1 sentence — the gate's text is read by a session that may have loaded no skill | (a). Spec S6 | ⬜ |
| Q6 | The `implement` skill's option text: does the shared option say "and commits it"? The record's sentence is "creates `<repo>/seal/` and commits it"; the skill creates files and the session commits them with the routing declaration it already mandates, and `evidence-ci` refuses to commit for the user | (a) "creates `<repo>/seal/` in the tree; the routing commit you make before the first edit carries it" — true of the flow that exists. (b) the bootstrap commits the root itself, one more commit and one more place the commit gate has to stay silent | (a). Spec S4 | ⬜ |
| Q7 | Whether the skill asks the mode question when a repository has an OLD layout still unmoved (`.specseal/` present, `seal/` at neither place). `root-migrate.py` moves it at the next session start, into `<repo>/seal/` | (a) do not ask: the skill checks `.specseal/` too and says "this repository is on the 0.3.x layout; start a new session and the plugin moves it, or follow the README's by-hand sequence" — a repository that committed `.specseal/` already chose shared. (b) ask, and let a local answer create `.git/seal/` beside an old `.specseal/` that the hook then moves into the tree anyway | (a). Spec S9 | ⬜ |
| Q9 | How the hygiene template puts the plugin on the runner. S5 and Q4 say an `actions/checkout` of the plugin repository at `ref: v<version>` into `${{ runner.temp }}`; that action resolves `path:` under `GITHUB_WORKSPACE` and refuses one outside it, so the two halves of that sentence cannot both hold | (a) `git clone --depth 1 --branch v<version>` of the plugin repository into `$RUNNER_TEMP/specseal` — outside the tree, deterministic, one line; the template's comment says why it is not the action. (b) `actions/checkout` with `path:` under the workspace, e.g. `.specseal-plugin/` — the action as written, but the plugin lands inside the user's tree for the run, in a work item whose point is that the tree carries nothing extra. (c) the ref unpinned — the drift `evidence-ci` refuses | (a), taken in phase 2 (`2af85e8`); the record's decided-after row says "a clone". Found by reading the action, not by running the workflow — **unverified** until a shared-mode repository runs it, answered by the owner or by the first such CI run | ✅ orchestrator, 2026-09-02 — the default, under the pre-approved batch (`plan.md`: every row takes its default and the spawn is the approval); S5 rewritten to the clone in phase 3, and the runner half stays in `overview.md` §Not verified |
| Q8 | The design record's own text. Two rows read against the code: "Decisions left open" row 4 (the `scratch` opt-out) was closed by #79 as `.git/specseal-scratch`; "Shared or local" says the hygiene workflow "would refuse a pull request with no round records", and the checks would instead go green having examined nothing (`chain_check.py:789`, `unverified_check.py:611`). #79's `questions.md` says answered rows feed back into "Decided after the thread" | (a) phase 2 adds this work item's rows to "Decided after the thread" (the mode question's shape, the template, the stamp rule, the session sentence), corrects the two sentences, and mirrors the edits into `docs/one-root-by-lifetime.ko.md` — the language policy keeps the twin. (b) leave the record as the thread's record and put the corrections in the pull request body only | (a). The record is the policy this spec cites, and a policy that contradicts the code gets fixed (`implement` §1) | ⬜ |

Answered rows feed back into `docs/one-root-by-lifetime.md`'s "Decided after
the thread" table before this directory's work merges (Q8), or stay as
defaults named in the pull request body.

At the close of phase 3, Q1–Q8 stay ⬜ at the defaults the owner pre-approved
on 2026-09-02, each built as its row says; the pull request body names every
one of them as a default taken, so a reader who would have chosen otherwise
finds the row rather than the code.
