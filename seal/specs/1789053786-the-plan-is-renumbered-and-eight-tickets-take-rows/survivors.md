# Survivors — the plan is renumbered and eight tickets take rows

## Whole ranges

| Range | Grounds |
|---|---|
| `origin/release/v0.11.0...HEAD` | **This branch deletes `docs/flow.md`'s 0.10.0 section, and every sentence of it standing elsewhere is the deletion working rather than a defect.** The file's own rule is why: *"A shipped version's section is deleted, not kept — the design record, the CHANGELOG and the tickets are the durable copies."* The 23 places reported are those copies. Nineteen are records of shipped work: `CHANGELOG.md`'s gathered 0.10.0 entries, and the `spec.md`, `plan.md`, `questions.md`, `overview.md`, `routing.md`, `changelog.md`, `handoff.md` and `phases/` of the three work items the section listed plus two earlier ones. **Not one of them may be corrected**: a released changelog entry and a shipped work item's design record are records of what a release did, and rewriting them to match today's plan destroys the thing they are kept for. **Three more are not records but the live homes the deleted rows pointed AT**, which is the same shape #328's range row names one instance of — `templates/sdd-routing.md`'s `Implementation` criterion, `tests/test_waiver_decided_at_start.py` pinning it, and `skills/verify/scripts/payload_meter.py`'s note that a prefix is cached for five minutes. A pointer going away is not its destination going away, and correcting these would delete the rule the section was summarising. The last is `docs/flow.md` itself, where the rows this branch wrote quote what the deleted section said about the same tickets — which is the point of writing them. **Third occurrence of this range shape**: #293's reported 153, #328's 34, this one 23. The mechanism is #297's, and #307 argues that a gathered changelog entry should leave the corpus by construction the way `rounds/` already does. |
