# Survivors — four shipped work items wait unfolded

## Whole ranges

| Range | Grounds |
|---|---|
| `origin/release/v0.13.2...HEAD` | **This branch removes four released work items' directories whole, and every sentence of them standing elsewhere is the fold working rather than a defect.** `skills/settle/SKILL.md` §*What a fold branch owes* names this branch shape and ships the range row for it: "a fold removes shipped sections whole, and their sentences stand in the durable copies by design". The durable copies are what the fold is **for** — each work item's rules were written into a top-level `docs/` file under its `<!-- specs/<id> -->` marker before `settle --retire` removed the directory, and for `1790076060` the rules already were the sentences of `docs/review-chain-spec.md`, marked in place. The rest are records of what a release did: `CHANGELOG.md`'s gathered entries and `seal/ledger.md`'s folded sections, which name these work items as provenance. **None of them may be corrected.** A released changelog entry and a folded ledger row record a past release, and a marked `docs/` sentence is the fold record itself — correcting it away would leave `settle` reading the directory as removed with nothing absorbing it. This row is anchored on the range **and** on this work item, so it stops holding the moment the check runs over a different range. |
