# 1788700685-two-value-shaped-odd-rows-end-the-report — review round 3

| Field | Value |
|---|---|
| Target SHA | f17f367 |
| Ran by | specseal:warden on claude-opus-5[1m] |
| PR | 191 |
| Broad gate | not yet |
| Fixes checked by | round-4 |
| Contract changes | none |
| New units | TOP_FLOAT (depth 1); test_a_sum_of_entered_values_does_not_end_the_report (depth 1) |
| Needs a fix | yes — findings 1 and 2 |
| Loses a record or crashes | yes — finding 1 ends `session_cost.py` with exit 1 and stdout empty on both the report and `--json` |

- [x] Pass

## What this round was asked

Round 3 of work item `1788700685`. Target `f17f367`, base `origin/release/v0.8.3`, draft pull request #191, issue #175.

**Two rounds have each answered `Loses a record or crashes: yes`**, and each 🔴 was opened by the preceding fix. Round 1's guard broke a shape that worked; round 2 found it. The cap is three to five, so this is where the run converges or does not — and the question this round exists to answer is whether the floor is met at last, not whether the fixes read well.

`close` derived `Contract changes | turn_at → pytest only` and `New units | HUGE_INT (depth 1)`. Verify that derivation rather than inheriting it, and treat `HUGE_INT` as a finding surface — it was reviewed by nobody.

## What round 2 recorded closed

1. **🔴 1 — the finiteness question itself ended the report.** The question is now inside a `try`, and an `OverflowError` scores 0. **Ask the question that has now failed twice: what did this guard's own widening bring in?** Both previous 🔴 came from a fix's new call. Then ask whether `try/except OverflowError` around one call is the right shape or whether the funnel should refuse by asking something that cannot raise — and what happens to a value that is finite but absurd, a negative token count for instance, which the fix pass says is out of the funnel's direction.
2. **🟡 2 — the walk widened to dotted calls.** 17 sites where the by-name set gave 13; exactly one unguarded, and it was the previous fix's. **Ask whether 17 is a class or a list**: what does the widened classifier do with a call through an alias (`import math as m`), a `from math import isfinite` bare name, a call on an object rather than a module, a call reached through a variable holding the module, and a comprehension over `getattr`. And ask what the walk still cannot see now, and whether the record states it as something a next editor can act on.
3. **🟡 3 — the idle line's positive-span conjunct restored**, and three records corrected. **Ask** whether the conjunct is now the only place deciding whether the line prints, and whether any fourth record still argues the branch is unreachable.
4. **🟡 4 — the negative sentence** now says the last call to begin ended before the first call began. **Ask** whether that is true of every negative span this file can compute, including one produced by the mixed-zone normalisation rather than by a synthetic fixture.
5. **🟡 5 and 🟡 6 — `docs/flow.md`'s clause and `turn_at`.** Ask whether the flow line is now true, and whether `turn_at` through `stamp_at` reproduces the stamps the existing cases assert — a helper rewritten under passing cases is a place a silent change hides.
6. **⬜ 7 and ⬜ 8 — two record corrections.** Ask whether each now states something true of the code as it stands.

## And the thing the fix pass raised itself, which no verdict table carries

**A refused value becomes 0 with nothing said.** A transcript that genuinely carried a 401-digit count now reads as zero, and the report shrank from 1282 bytes to 762. The pass recorded it in the fragment with the measurement rather than fixing it, on the grounds that every funnel in this file chooses the same direction — a missing number over a wrong one, and a missing number over a report that does not finish. **Judge that trade.** It is the silent half of the rule this whole work item is about, and a reader who sees `output 0` cannot tell it from a transcript that produced nothing.

## Facts, labelled

- **executed by the orchestrator at `8d2a7d2`** — nine combinations, three usage fields by three shapes (a 401-digit integer, `NaN`, `Infinity`), all exit 0 on both the report and `--json`. `./bin/test tests/test_session_cost.py -q` → 35 passed, exit 0. `uvx ruff check .` → All checks passed.
- **read** — `.venv` carries no `ruff` module; use `uvx ruff`. The suite runs through `./bin/test`.
- **unverified** — everything above.

Run the ledger check unscoped.

## Verdicts

| # | Finding | Location | Verdict | Grounds |
|---|---|---|---|---|
| 1 | Two `usage` values `count` accepts as finite sum to one it would refuse, and the report ends with exit 1 and stdout EMPTY on both arms. `count` guarantees each VALUE is finite; nothing answers for a SUM. Four records state the opposite — that the ended-report direction of the number axis is closed and only the wrong-number direction remains: `seal/ledger.md` R6, `seal/ledger/1788700685-….md` R3, `seal/specs/1788700685-…/changelog.md:58`, and `phases/phase-1.md`'s type-and-value table row *a number* | `skills/verify/scripts/session_cost.py#token_thirds` (line 355), reached from `#load`'s per-turn pair at line 262 | **fixed** `7361a3e` | fixed at 7361a3e — `` for the site, with the class deferred to **#192**. The one site is closed and the class is recorded as a limit, which is `skills/code-review/SKILL.md` §*Verdicts that close too early* applied: the sites are countable and the value range is not, so an enumeration over it is a recorded limit rather than a closed finding. `spec.md`'s In criterion is *operands that END the report*, and round 1 admitted the non-finite member on exactly that ground, so the fourth member is not excluded by a different reading of the same sentence. Reproduced before acting: the two-float shape and the six-turn shape end the report at `origin/release/v0.8.3` and did so at every commit on this branch; the integer pair dies only at the base, since round 2's guard already catches it, while an integer that does have a float of its own still died and is what this fix closes. `token_thirds` is the only place in the module that converts a derived number to an int, so the mean is computed inside a guard and a non-finite result scores 0. **Re-verified independently by the orchestrator at `b7e9ef3`**: four shapes, both arms, all exit 0; 🔴. **Executed** at `f17f367` in a `--no-local` clone, three-turn transcripts, report and `--json` each. Two float fields at the top of the double range: both pass the finiteness question, `load` adds them to infinity, rounding raises `OverflowError: cannot convert float infinity to integer` — exit 1, 0 bytes, both arms. The same fields as integers of that magnitude: each has a float, the sum does not, the division raises `OverflowError: integer division result too large for a float` — exit 1, 0 bytes, both arms. One such field alone over SIX turns: a third then holds two elements and the sum overflows on its own — exit 1, 0 bytes, both arms. **Executed against every commit on the branch and against the base**: all three end the report identically at `origin/release/v0.8.3`, `ae3fe18`, `a4ec518`, `5ed9abf` and `f17f367`, so this is a member the enumeration missed rather than a regression. **Reproduced independently by the orchestrator at `f17f367` and at the base.** The class, stated so it can be re-run: `count` closes the values that ENTER, and axis 2 of the enumeration stops there — it never asks what the operations make of two entered values, and this file's addition reconstructs a non-finite value from two finite ones. **Fix, verified by the reviewer.** Replace `token_thirds`' return comprehension with a loop, which is the one site in the module that converts a derived number to an int: take the third length, then for each of the three slices skip an empty one, compute the mean inside a `try` that answers infinity on `OverflowError`, and append the rounded mean when it is finite and 0 when it is not. Executed with exactly that patch: all three shapes exit 0 on both arms, and the module suite gives 35 passed, exit 0. Then correct the four records to say the sum is what the funnel does not answer for, and extend the shapes tuple with a two-field float arm, an int arm, and a six-turn arm for the summed-third path — each seen red first against `f17f367` |
| 2 | The negative-span class was closed at the idle coordinate and not enumerated. The repeats line is printed on a truthiness test, which a NEGATIVE repeat time satisfies, so the report prints a negative amount of time as work re-run for a result already in hand; and the *nothing obvious* line below is suppressed by the same value. Same defect as round 2's finding 2, one line further down the same function | `skills/verify/scripts/session_cost.py#report` (lines 567-573 and 596) | **fixed** `7361a3e` | fixed at 7361a3e — `` — the class was counted this time rather than the coordinate. Nine statements in `report` decide whether a line prints, taken from the syntax tree: three read the span directly and were already handled, three read a time value through a local name and tested it **for truth alone**, and three read counts and cannot go negative. All three broken ones are fixed — the repeats line, the identical-time clause inside it, and the *nothing obvious* line; 🟡. **Executed** at `f17f367`, two transcripts, report and `--json`. A hand-built negative-span file with one command run twice prints a repeats line of minus sixty minutes. A mixed-zone file — call rows naive-local, result rows aware, the shape `parse_time`'s own paragraph describes — prints a repeats line of minus 539.8 minutes at a span of minus 534.8, exit 0, and `--json` carries the negative repeat seconds. `by family` and `slowest` print negative durations on the same file; those are the measured times, which `share`'s docstring commits the file to, and are NOT part of this finding. **Fix, verified.** Three token edits: the repeats guard compares the repeat time against zero rather than testing it for truth, its trailing conditional does the same for the exact figure, and the *nothing obvious* guard tests the repeat time as at most zero rather than as falsey. Executed with that patch: the mixed-zone report drops from 881 to 780 bytes with no repeats line, the positive-span fixtures still print theirs, and the module suite gives 35 passed, exit 0. Extend `test_a_negative_span_says_what_it_actually_saw` with a second call carrying the SAME command as the first and an assertion that the repeats line is absent — seen red first |
| 3 | The corrected residual list names three blind spots of the widened walk and omits a fourth of the same shape: a consumer imported by name is a `Call` on a bare `ast.Name`, inside the classifier's node shape and outside its NAME LIST. The record frames the limit as the node shape, so a next editor re-running the walk widens the wrong dimension | `seal/specs/1788700685-…/phases/phase-1.md` §*What the widened set still cannot reach* (lines 160-186) | answered | corrected at `7361a3e`. The coordinate is `phases/phase-1.md`, so this is a record correction. The walk's limit was written as the node shape, which would send a next editor to widen the wrong axis; it is the **name list**. The record now names three forms that sit inside the accepted node shape and outside the name list — a consumer imported by name, a module bound to an alias, a module held in a local variable — and says that widening means widening the name list and the alias table |
| 4 | The new assertion raises on the very shape the case was extended for. The comment beside it says the finiteness question was chosen over an int conversion so a leaked value reads as a failed assertion rather than a test error; the finiteness question raises on a huge int, so for those arms it reads as a test error, which is what the comment says it avoids | `tests/test_session_cost.py:1037` | **fixed** `7361a3e` | fixed at 7361a3e — ``. The coordinate is a test file but the content is code rather than a record, so it was fixed rather than corrected. The assertion raised on the huge-integer arms it had just been extended for, which is the reading problem its own comment claims to remove. An integer of any size is now accepted without conversion and only a float is required to be finite; ⬜. **Read** at `f17f367`, against the six shapes the case now carries. The case still goes red either way, so the cost is a confusing traceback rather than a missed defect. Fix — replace the assertion with one that answers for both shapes without converting: accept any value that is not a float, and for a float require it to be finite; then rewrite the comment to say that an integer of any size is accepted without conversion while a non-finite float fails the assertion |
| 5 | Round 2's finding 1 — the finiteness question inside a `try`, an `OverflowError` scored 0 | `skills/verify/scripts/session_cost.py#count` (line 99) | answered | Closed at the coordinate it names. **Executed** at `f17f367`: all four `usage` fields crossed with a non-number, a positive and a negative infinity, a 401-digit int and its negation — 20 combinations, report and `--json` each — exit 0 on both arms every time, which reproduces and widens the orchestrator's nine. §15 checked rather than inherited: reverting the `try` to round 1's plain call turns the non-finite case red, 1 failed, so the two new arms were genuinely seen red. What the widening brought in is nothing new — the `except` catches the one exception the conversion raises, and the type check above it means no other type reaches the call. The finite-but-absurd direction is confirmed open and harmless to the exit code: a negative count in any field exits 0 and prints a negative context line, which is the wrong-number direction the records already declare open. What is NOT closed is finding 1 above, and it is the ended-report direction |
| 6 | Round 2's finding 2 — the idle line's positive-span conjunct restored, three records corrected | `skills/verify/scripts/session_cost.py#report` (the idle guard), `phases/phase-1.md` §*One funnel, not two*, `overview.md` divergence row, `seal/ledger/1788700685-….md` R2 | answered | Closed, and the conjunct is now the only place deciding whether the line prints — `share` is called inside the guarded block and answers only what the percentage is. §15 checked: dropping the conjunct turns the negative-span case red, 1 failed. **Read** across the four records: `phase-1.md:111`'s original *a branch no case exercises* survives, but only under the blockquote at `:127` that reverses it, and the fragment's R2 Notes and `overview.md`'s divergence row both carry the correction — no fourth record still argues the branch is unreachable. The class the guard belongs to is finding 2 above |
| 7 | Round 2's finding 3 — the negative-span sentence now says the last call to begin ended before the first call began | `skills/verify/scripts/session_cost.py#report` (line 524) | answered | True of every negative span this file can compute, checked by construction rather than by fixture: the span is the last element's end minus the first element's start over a list sorted by start, so a negative span is exactly the proposition the sentence states, including the one-call case where the two are the same call. §15 checked: restoring the old sentence turns the case red, 1 failed. **Executed** on a negative span produced by the mixed-zone normalisation rather than by a synthetic fixture — call rows naive-local, result rows aware — span minus 534.8 minutes, exit 0, the new sentence printed and the shared-timestamp sentence absent |
| 8 | Round 2's findings 4 and 5 — `docs/flow.md`'s clause, and `turn_at` built off `stamp_at` | `docs/flow.md:68`, `tests/test_session_cost.py#turn_at` (line 955) | answered | **Read** and **executed** at `f17f367`. `docs/flow.md:68` is now true as far as it goes: the rider is gone from `count` and the non-finite member is closed there. It omits that the guard itself then broke a working shape and needed round 2's repair, which is history the line is not obliged to carry. `turn_at` reproduces the old stamps exactly for the indices in use and carries past nine where the interpolated prefix produced an invalid stamp. No silent change: the module suite gives 35 passed, exit 0, and the case's own turn count is unchanged at three |
| 9 | The `close` derivation — `Contract changes` naming `turn_at` reaching pytest only, and `New units` naming one constant at depth 1 — re-derived rather than inherited | `tests/test_session_cost.py:27` and `:955` | answered | **Executed**: a grep for the three names across `tests/`, `skills/` and `bin/` returns hits in `tests/test_session_cost.py` alone, so `turn_at`'s changed parameter meaning reaches pytest and nothing else, and the new constant has one reader. It is also the only name the fix diff adds at module level — the others are locals. Both derivations hold. Judged as code rather than as a fix, per this round's brief: the constant does raise on conversion rather than returning an infinity, as its comment says, and its magnitude is far past what the claim needs — correct, with a deliberate margin |
| 10 | Round 2's findings 6, 7 and 8 — three record corrections | `phases/phase-1.md` §*What the widened set still cannot reach* and §*Each axis then decomposes*, plus round 1's findings 4 and 5 | answered | **Read** at `f17f367`. The residual is now filed as the call's SHAPE rather than as the module, which is the actionable form — subject to finding 3 above, which adds a fourth blind spot to the same list. The type-and-value table's *a number* row now reads closed at `count` for the non-finite shapes; finding 1 is why that cell is still wider than the code. The mixed-zone paragraph and the four-becomes-three correction are both true of the code as it stands, re-derived: three `share` call sites in `report`, and both docstrings say three |

## Executed probes

| What was run | Result |
|---|---|
| Two float fields at the top of the double range, three turns, report and `--json`, at `f17f367` | **exit 1, stdout empty, both arms** — `OverflowError: cannot convert float infinity to integer` at `token_thirds` |
| The same fields as integers of that magnitude, report and `--json` | **exit 1, stdout empty, both arms** — `OverflowError: integer division result too large for a float` |
| One such field alone over six turns, so a third sums two, report and `--json` | **exit 1, stdout empty, both arms** — same infinity conversion |
| The three shapes above against `origin/release/v0.8.3`, `ae3fe18`, `a4ec518`, `5ed9abf` and `f17f367` | Identical exit 1 with stdout empty at every SHA including the base — a missed member, not a regression |
| Four `usage` fields crossed with a non-number, both infinities, a 401-digit int, its negation, a top-of-range float and a negative count, three turns, report and `--json` | 28 combinations, exit 0 on both arms in every one. Reproduces and widens the orchestrator's nine |
| The 401-digit int control against every SHA | exit 0 at the base, exit 1 with stdout empty at `ae3fe18` and `a4ec518`, exit 0 at `5ed9abf` and `f17f367` — reproduces round 2's finding 1 and its fix |
| Negative-span transcript with one command run twice, report and `--json` | exit 0. Prints a repeats line of minus sixty minutes; `--json` carries the negative repeat seconds on the mixed-zone twin |
| Mixed-zone transcript — call rows naive-local, result rows aware, one command twice, report and `--json` | exit 0 both arms. Span minus 534.8 minutes, the new sentence, no idle line, and the negative repeats line. The shape is reachable through this branch's own normalisation |
| Mutation — revert `count`'s `try` to round 1's plain finiteness call | exit 1, 1 failed — the non-finite case killed it |
| Mutation — drop the idle line's positive-span conjunct | exit 1, 1 failed — the negative-span case killed it |
| Mutation — restore the old negative-span sentence | exit 1, 1 failed — the same case killed it |
| Patched module — `token_thirds` guarded, repeats and the *nothing obvious* line guarded on a positive repeat time — against all four shapes | exit 0 on every one, both arms; no repeats line on either negative-span file; positive-span fixtures keep theirs |
| The module suite against that patched module in the clone | 35 passed, exit 0 — neither fix disturbs an existing case |
| `./bin/test tests/test_session_cost.py -q` at `f17f367` | 35 passed, exit 0. Reproduces the orchestrator's fact |
| `./bin/evidence-check .` unscoped at `f17f367` | exit 1 — 708 ok · 2 drifted · 0 broken. The fragment's 7 rows all resolve; the two drifted are the base's own |
| `./bin/deferral-check` | exit 0 — `tests: resolves` |
| Grep for the three new names across `tests/`, `skills/`, `bin/` | Hits in `tests/test_session_cost.py` only — the `Contract changes` and `New units` derivations hold |
| Grep for a machine consumer of the `--json` arm outside its own test module | None. A `--json` arm emitting a bare infinity for a summed float total is read by nobody, so it stays a note rather than a finding |
| Clone removed, probes deleted, tree checked | `--no-local` clone restored clean before removal; main tree clean at `f17f367` |

## Inherited coordinates

| From | Coordinate | Why it is still worth opening |
|---|---|---|
| round-1 | `skills/verify/scripts/session_cost.py:335` | round 1's 1 — fixed |
| round-1 | `seal/specs/1788700685-…/phases/phase-1.md:120-126` and `seal/ledger/1788700685-….md` R3 | round 1's 2 — answered |
| round-1 | `skills/verify/scripts/session_cost.py:490-494` | round 1's 3 — fixed |
| round-1 | `skills/verify/scripts/session_cost.py:68-72` | round 1's 4 — fixed |
| round-1 | `skills/verify/scripts/session_cost.py:474` and `tests/test_session_cost.py:843` | round 1's 5 — fixed |
| round-1 | `seal/specs/1788700685-…/overview.md:17-21` | round 1's 6 — answered |
| round-2 | `skills/verify/scripts/session_cost.py#count` | round 2's 1 — fixed |
| round-2 | `skills/verify/scripts/session_cost.py#report` (the idle guard), and the records at `seal/specs/1788700685-…/phases/phase-1.md` §*One funnel, not two*, `overview.md` divergence row *How many sites there are*, `seal/ledger/1788700685-….md` R2 Notes | round 2's 2 — fixed |
| round-2 | `skills/verify/scripts/session_cost.py#report` (the sentence naming the last result) | round 2's 3 — fixed |
| round-2 | `docs/flow.md:68` | round 2's 4 — fixed |
| round-2 | `tests/test_session_cost.py#turn_at` | round 2's 5 — fixed |
| round-2 | `seal/specs/1788700685-…/phases/phase-1.md` §*What the widened set still cannot reach* | round 2's 6 — fixed |
| round-2 | `seal/specs/1788700685-…/phases/phase-1.md` §*Each axis then decomposes into type and value* | round 2's 7 — answered |
| round-2 | `skills/verify/scripts/session_cost.py#parse_time`, `#share`, `tests/test_session_cost.py#test_a_span_of_zero_prints_what_it_can_rather_than_dividing_by_it` | round 2's 8 — answered |

## Deferred

| Finding | Where it went | Who answers it |
|---|---|---|
| The full suite, the repository-wide lint and the typecheck | `agent-contract` §2 reserves the broad gate; this round ran the one module it reviewed and declines to widen | the orchestrator, once the rounds settle |
| `templates/config.md#"# Repository config"` and `round_record.py#swallowed`, both DRIFTED | Confirmed present at the base and untouched by this branch; already carried in `overview.md` §Not verified with an answerer | the orchestrator, or the branch whose edits drifted them |
| Round 2's process fact — an import written above its use site is deleted by the formatter as unused, the edit tool reports success, and 34 cases go red | Still nowhere in the tree; the fact remains in one handover transcript | the orchestrator |
| `token_totals` summing finite floats to an infinity, so the report prints an infinite output figure and `--json` emits a bare infinity | Measured at exit 0 on both arms, so it is the wrong-number direction rather than the ended-report one, and no machine consumer of `--json` exists in this repository. Belongs with the finite-but-absurd residual the records already declare open | whoever next closes the wrong-number direction; the fragment's R3 carries the method |
