# a release is sized by a count and cut by urgency — questions for the planner

<!-- seal/specs/1789172128-a-release-is-sized-by-a-count-and-cut-by-urgency/questions.md —
decisions only a human can make, extracted so nothing ships on a silent
assumption. -->

Sorted by `Who can answer`. **Four rows need the repository owner** and the
first of them decides the shape of the work rather than a detail of it.
Everything below them is settled by running something or by the phase that
meets it, and neither kind should be queued behind a person.

The routing axes are not here — `routing.md` records them, answered before the
first edit — and neither is the `Planning` row, which
`skills/implement/SKILL.md` §3's ladder decided rather than a person.

| # | Question | Who can answer | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|---|
| Q1 | #361's `## Done when` requires the rule to cite the two releases that demonstrate it, **and the repository's own checker refuses to let a document in `docs/` name either of them.** `timers_in` refuses every version-shaped token at or above the running `0.11.0`; the two releases are 0.11.0 and 0.11.1. It does not clear at the release: the 0.11.1 preparation commit makes 0.11.1 the running version, so that number stays refused. None of the three existing exemptions is a truthful home, and the checker's own comment argues `RECORDS_OF_A_MOMENT` against this exact file. Which route? | a person | **(a) prose, no numbers** — *the release that shipped the framer*, *the release that replaced the deleted checklist with a gate*. Ships no gate change, prompt budget zero, and the paragraph says why the numbers are not written, the way the document already does for the illustrative `1.2.3`. Costs a reader one `CHANGELOG.md` grep, and the reason expires once 0.11.2 ships. **(b) correct the checker so a version this repository has SHIPPED is history** — derived from git tags, not from `CHANGELOG.md`, because the preparation commit adds the heading and bumps `plugin.json` together and a CHANGELOG-derived exemption would wave through the version being cut. Sound, but it is a change to a gate: `CONTRIBUTING.md`'s four items land on a prose ticket, and it only unblocks 0.11.0 — 0.11.1 has not shipped and stays refused. **(c) (a) now, and (b) filed as its own issue.** `plan.md`'s alternatives table carries the two rejected routes — a dated `docs/experiments/` record, and citing the milestone descriptions — with why each fails | **(c)**. The rule lands this release with a citation a reader can resolve, and the off-by-one is a real defect in a gate that deserves its own test seen red rather than a paragraph of someone else's ticket. **The frame does not take (b) on its own judgment: widening a checker so a document may say something is exactly the change that must not be made because it is the easiest way to green** | ✅ **(c)** — 2026-09-12, the repository owner, taken as the default the frame proposed. The rule ships this release with the citation as prose and no version numbers, and the off-by-one is filed as **#363** with `CONTRIBUTING.md`'s four items answered there rather than here. The orchestrating session re-derived the defect before filing: `plugin.json` reads `0.11.0`, `v0.11.0` is tagged, `origin/main` is the 0.11.0 release merge and `CHANGELOG.md` carries `## 0.11.0 — 2026-09-11` — so the refused number is one the repository has demonstrably shipped, which is the argument `timers_in`'s own docstring makes for keeping history. #363 also records the trap: derive *shipped* from git tags and never from `CHANGELOG.md`, because the preparation commit adds the heading and bumps the version together. |
| Q2 | The proposed label name is **`now`**. Measured: `now` is used as an ordinary adverb on **131 lines across 69 files** of the loaded tree, and twice inside the very document that would define it. Nothing enforces against it today — `tests/test_one_word_one_meaning.py` holds six hand-written words and `now` is not one, and no check reads label names — but #331 is the census that would meet it. Separately and more sharply: `docs/issues-and-milestones.md` §*A label answers what it is about, and survives the move* gives the milestone field to *when* and a label to a concern that **outlives a schedule**, and `now` is purely a schedule answer that is spent the moment the release ships. Does the name stand, and how is that tension resolved? | a person | **(a) `now` stands**, and the label section states the exception explicitly — the precedent is in the same section, which already names `flow-measurement` as *a label that is not an index*, so this is the second acknowledged non-topic label. Cheapest, and it keeps the ticket's own word. **(b) a name that is not an ordinary English word** — `goes-next`, `blocks-the-next-item`, or a `now:` prefix in the shape of the existing `merged:` and `release:` families, which are the tracker's established form for a state. Costs the ticket's wording; buys a name a reader cannot mistake for prose and a census that has nothing to decide. **(c) the criterion ships and the label does not**, leaving the second `## Done when` bullet for a later ticket. The name is the owner's under every option, because #361 proposes it and a label is a tracker artifact | **(b)**, on the prefix form — the tracker already spells a state as `merged: X.Y.Z`, and a label a reader can confuse with the adverb beside it is a word carrying two meanings in a repository that has a check about exactly that. **Stated as a proposal and not taken:** the frame does not rename what the ticket named | ✅ **(b), the prefix form** — 2026-09-12, the repository owner. The frame offered this and declined to take it, on the ground that a frame does not rename what its ticket named; the owner took it. So the label is spelled in the shape the tracker already uses for a state, beside `merged: X.Y.Z` and `release: X.Y.Z`. The reasoning that decided it is the section's own: `docs/issues-and-milestones.md` §*A label answers what it is about* gives the milestone field to *when* and a label to a concern that outlives a schedule, and a bare `now` is purely a schedule answer spent the moment the release ships. Measured by the orchestrating session over the `LOADED` set rather than inherited: `now` as an ordinary adverb on **130 lines across 34 files**, twice in the document that would define it. Phase 3 writes the exact spelling and reconciles it with that section; `flow-measurement` is still the standing precedent for a label that is not a topic. |
| Q3 | The `## Done when` row reads *a `now` label exists … and **exactly the tickets that meet it carry it***. Creating a label and applying it to some of thirty-five open issues are writes to the tracker. Who does that, and when relative to this pull request merging? | a person | **(a) the owner does both, after the document merges.** This work item defines the criterion and touches no tracker; the document briefly describes a label that does not exist yet, which is a real state and has to be an accepted one. **(b) the owner creates the label and approves a list, and the build applies it** — the application is then a phase with the owner's approval in front of it, not an assumption. **(c) the build decides which tickets meet the criterion and applies the label.** This is the class the owner stopped twice on 2026-09-11 — eighteen issues proposed for a milestone move, and a two-item release read as under-size — and #361 exists because of it | **(a)**. Framing the criterion so a reader can apply it is the deliverable; applying it is a scheduling act, and scheduling is what this ticket exists to stop being taken silently | ✅ **(a)** — 2026-09-12, the repository owner, taken as the default the frame proposed. This work item defines the criterion and writes nothing to the tracker: no label is created and none is applied. The document describing a label that does not exist yet is the accepted real state for as long as that lasts. What the build owes instead is a criterion a reader can apply without asking, which is what phase 3 is measured against when it applies the written wording to #362. |
| Q4 | Two release milestone descriptions carry a **`Size.`** paragraph stating a count — `release: 0.11.2` reads *Seven issues, about five work items*, and `release: 0.11.1` reads *Eight issues, about five work items* while the milestone holds **three**, so it is already false. These are the same subject as the criterion, one surface over. In scope? | a person | **(a) out of scope.** The `## Done when` sweep says *nothing else in **the tree*** and a milestone description is on the tracker; editing one is a tracker write, and the stale one is a release-planning correction rather than this ticket's. **(b) in scope as a tracker act the owner takes** alongside Q3's, so the count does not survive in the one place a person reads before cutting a release. **(c) in scope for the document**, which states that a milestone description records grounds for order and **not** a size — which would make the `Size.` paragraphs wrong by rule and leave existing ones to be corrected. Note for (c): **one sentence of `release: 0.12.0`'s description — why it is a minor bump rather than a patch — is a previous session's inference and not the owner's words**, and is not cited as the owner's position anywhere in this frame | **(a)**, with the stale 0.11.1 figure reported to the owner rather than fixed. A sweep that reaches the tracker is a sweep with no command behind it | ✅ **(a)** — 2026-09-12, the repository owner, taken as the default the frame proposed. Out of scope, and reported instead: `release: 0.11.1`'s `Size.` paragraph reads *Eight issues, about five work items* while the milestone holds **three**. The orchestrating session read the description in full and found the staleness is not confined to that line — its `Order, and why` paragraph still names #330, #343, #345, #350, #198, #103 and #354 as the release's order, all of which left for `release: 0.11.2` on 2026-09-11. So it is a release-planning correction across the whole description rather than one count, which is a second reason it is not this ticket's. Nothing in this work item edits a milestone description. |
| Q5 | Does `timers_in` actually refuse `0.11.0` and `0.11.1` in a `docs/` file at the running version, and is a shipped-but-running version refused? | a measurement | One `test_tmp_*` probe importing `tests/test_release_hygiene.py` and calling `timers_in("docs/x.md", text, version())` with each token. Settles the frame's central claim by execution instead of by reading the constants, and settles the off-by-one Q1 option (b) rests on. Phase 1, before any prose is written | both refused, and the shipped `0.11.0` refused with them — read from `LOADED`, `VERSION_TOKEN`, the three exemptions and the `>=` comparison, and from `v0.11.0` being tagged and on `main` with a `## 0.11.0` heading in `CHANGELOG.md` | ⬜ |
| Q6 | What is the sweep command for the last `## Done when` row, and what does it return today? | a measurement | `git grep -niE "three or four\|sized in work items\|release is sized" -- docs skills agents templates tests README.md README.ko.md CONTRIBUTING.md`, exit code read directly. The frame ran the same shape and found **one** line in the loaded tree, `docs/issues-and-milestones.md:24`; `docs/review-chain-spec.md:53` and `agents/smith.md:244` state a *round* ceiling, which is a different subject and must not be swept into this one. Earlier work items' `seal/specs/*` are records of a moment and are not rewritten | one owner, and the sweep's own wording is what the new case asserts | ⬜ |
| Q7 | Does editing the sizing paragraph drift `seal/ledger/1789100139-…md`'s S3 row, and does that fail CI or warn? | a measurement | `bin/evidence-check .` before and after, exit codes read directly. Read already: the heading survives so the row DRIFTS rather than breaking, and `.github/workflows/test.yml:70-93` runs the checker without `--strict`, annotates exit 1 as a warning and fails only at 2 or above. Phase 1 takes the before-reading; phase 4 the after | drift, warning, not a blocker | ⬜ |
| Q8 | S3's claim is *the rule that a release is sized in work items, three or four, is stated in exactly one document*. Its **wording** goes with this change; its **substance** survives and is exactly what the new sweep asserts. Re-verify the row, or remove it and write the new claim into this work item's own fragment? | the work | `CONTRIBUTING.md` §*House rules* gives both: re-verify where the claim still holds, remove and re-write where the claim went with the code. The complication is that the row lives in **another work item's fragment**, and nothing says whether a later work item may edit one — the fragment rule is about two concurrent branches not sharing a file, and both fragments already sit in `release/v0.11.1`. Phase 4 decides with the edited sentence in front of it, and records the reading | **remove and re-write.** The claim names *three or four* in its own text, so re-verifying it would leave a row asserting a rule the tree no longer states; the replacement row belongs to this work item and goes in this work item's fragment. If the reading in phase 4 contradicts that, the divergence is recorded in the closing memo rather than decided quietly | ⬜ |
| Q9 | Does the criterion, as finally worded, answer **#362** without anybody being asked? | the work | #362 was opened unmilestoned by #359's review run precisely because its release is a sizing call. It is prose in two coordinates that costs no behaviour today, so nothing has to start with it in effect — under the criterion it takes no label and rides the next release that carries it. Phase 3 applies the written wording to it and records the answer in `phases/phase-3.md`. A criterion that cannot decide it is the wrong criterion, and that is the phase's finding to report rather than a question to send back | it answers, with no label. **What the criterion does NOT answer is which milestone #362 then lands in** — the criterion decides *now or not now*, and ordinary scheduling decides the rest. Saying so in the document is better than letting a reader expect more of it | ⬜ |

Answered rows feed back into `docs/` — a policy clause or an open-questions
section — before this directory's work merges.
</content>
