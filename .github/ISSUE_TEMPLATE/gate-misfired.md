---
name: A gate blocked or allowed the wrong thing
about: The commit gate, worktree guard, or evidence check made a wrong call
labels: gate
---

**Which gate, and which direction was it wrong?**
<!-- e.g. worktree-guard denied a switch with no other session running -->

**The command you ran**
```
```

**What the gate said** (paste the full prompt or block reason)
```
```

**Environment**
- OS and version:
- How Claude Code is running (terminal / VS Code extension / other):
- `python3 --version`:

**Anything about the tree that might matter**
<!-- other Claude sessions open, linked worktrees, detached HEAD, submodules -->
