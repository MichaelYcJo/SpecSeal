# 1788310269-the-implementer-leaves-a-mark — questions for the planner

<!-- specs/<unix-epoch-seconds>-<slug>/questions.md — decisions only a human can make,
extracted so nothing ships on a silent assumption. Nothing here blocked the
work: each carries a default that was taken, in writing. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q1 | Issue #26 records the decision NOT to build this, with two objections and two reopening conditions: *a session declares `smith` and implements the work itself, and it costs something*, or *`dispatch.py`'s import-failure silence is closed*. Neither is met in the form written. This item's `routing.md` was committed by the repository owner with `Implementation = smith` and a "Why this way" naming the mark and the notice as the unfinished half. Does this pull request close #26? | **Close it** — the objection that could be measured (a second `pre-agent` gate taking the worktree guard down) is measured by S13 and does not hold: `run_gate` isolates per gate. **Leave it open** — the incidence objection still stands as written, and the issue would then record a design that ships against its own reasoning. **Amend it** — record S13's measurement and the owner's routing decision as what changed the answer | **Leave it open and name it in the PR body.** The owner wrote the reopening conditions and the owner routed this item; which sentence overrides which is theirs | ⬜ the repository owner |
| Q2 | The mark is written at `PreToolUse`, before the spawn runs. When the worktree guard in the same group denies or asks about that spawn, the mark stands for a smith that may never have run. Should the mark be written only when the group allows? | **Write it anyway** (what ships) — the gates in a group do not see each other's decisions, and a session that meets the guard's question and retries without `isolation` spawns smith on the retry, which writes the mark again. **Write it only on allow** — needs `dispatch.py` to run the mark after the merge, which makes the dispatcher know one gate's semantics | **Write it anyway.** The wrong direction is a missed notice for a session that answered the guard by not spawning smith at all, once per session, and the mark is overwritten by the next spawn on this branch | ⬜ the repository owner |
| Q3 | The mark goes into the git dir of the repository at the spawn's `cwd`, and the notice reads the git dir of the repository at the commit's `cwd`. A session sitting in one repository and committing into another with `git -C` is not judged, the same line `review-history-guard.py` draws. Should the notice follow `-C` the way the commit gate does? | **No** (what ships) — following it means re-deriving every target the commit gate derives, to decide whether to print one line. **Yes** — one rule for both readers of a command | **No.** The cost of the default is a missed reminder for a shape the routing declaration itself does not describe (a declaration names a branch in the repository it lives in) | ⬜ the repository owner |

Answered rows feed back into docs/ (policy clause or open-questions section)
before this directory's work merges.
