# 1788305134-the-reader-stops-where-it-need-not — questions for the planner

<!-- specs/<unix-epoch-seconds>-<slug>/questions.md — decisions only a human can make,
extracted so nothing ships on a silent assumption. Nothing here blocked the
work: each carries a default that was taken, in writing. Q1–Q3 are carried
from the original item (`fix/stops-the-reader-need-not-make`, orphaned by the
history rewrite); Q4–Q5 are this item's own. -->

| # | Question | Options & what each implies | Default until answered | Status |
|---|---|---|---|---|
| Q1 | Should a quoting bash does not expand — `'$SB'`, `\$SB` — be filled like a double-quoted one? The original item measured the family at three shapes: the `-C` operand, a `cd` operand, and a backslash escape, all of which posix-mode `shlex` delivers as the same token | **Fill it** (what ships) — telling them apart means carrying quote provenance out of a splitter three readers share. **Refuse it** — matches bash, which names a directory literally called `$SB`, and costs a new field on every token | **Fill it**, pinned as a decision by `tests/test_a_path_the_command_wrote_out.py::test_a_single_quoted_operand_is_filled_too_and_that_is_the_cost`. The direction is fail-open, which is why it is written here rather than only in a comment | ⬜ the repository owner |
| Q2 | Should an assignment inside a `\|\|` branch enter the environment? | **Yes** — the environment is linear. **No** — the branch may not have run | **No, and it forgets.** The original item's round 1 measured `SB=/one; [ -d /nope ] && SB=/two; git -C "$SB" commit` answering `/two` where bash has `/one`; a conditional branch forgets the name. Carried as a record | ✅ |
| Q3 | Does the commit invisible inside a loop body — `for f in *; do git commit -m x; done` finds no invocation in either gate — want an issue in this repository? | **Yes** — it is an ordinary shape. **No** — the `# RIDER:` at `hooks/cmdline.py`'s git-options reader reaches whoever opens that file | The rider stands and predates this branch; whether it also wants an issue is the repository owner's | ⬜ the repository owner |
| Q4 | The body stack reads posix-mode tokens, so it cannot tell a quoted word from a reserved one. After round 1's stack, exactly ONE shape still answers where bash disagrees: a quoted closer written alone as a command inside a body that does not run — `if false; then "fi"; SB=/three; fi; git -C "$SB"` answers `/three`, bash `/one`. A quoted `")"` no longer does (`)` pops only a `(`), a closer in argument position never did, and a glued closer (`make)`) is not counted, which costs a prompt. Round 3 added a second shape that answers wrongly, unrelated to quoting: a subshell whose first statement is a subshell (`( ( echo hi ); SB=/three; true )`), recorded as issue #72 with the single rule that would replace the `)` special cases. Accept, or carry quote provenance out of the splitter? | **Accept** (what ships) — the one wrong shape needs a bare quoted closer as a whole command, which nothing writes; the glued-closer prompt is pinned by `::test_a_glued_closer_costs_a_prompt_and_not_an_answer`. **Provenance** — closes Q1 and this together; a new field on every token through a splitter three readers share | **Accept.** Every other direction was shaped so the error is a prompt | ⬜ the repository owner |
| Q5 | The directory half reads a body's later `cd` the same way: `if false; then :; cd /two; fi; git commit` moves the shell to `/two`. It reports `/session` beside it, because the `cd`'s own failure is parked and `;` merges it back, so the gate over-asks rather than passes. Should the body count refuse a `cd` inside a body as it refuses a name? | **Leave it** (what ships) — a superset is a stop, not a silence, and refusing would turn `if [ -d x ]; then echo; cd x; fi; git commit` from two candidates into `Unresolved`. **Refuse it** — one rule for both halves, at the cost of prompts on bodies that do run | **Leave it**, measured and recorded. A different answer changes the directory half, which this item does not touch | ⬜ the repository owner |

Answered rows feed back into docs/ (policy clause or open-questions section)
before this directory's work merges.
